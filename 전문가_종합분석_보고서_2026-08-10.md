# 관리앱(phone-lock 시스템) 전문가 종합 분석 보고서

작성일: 2026-08-10 | 대상: phone-lock-android, phone-lock-desktop, browser-extension (37차 세션 기준 코드베이스)

> 이 보고서는 3개 조사 에이전트(코드/버그/보안/성능 담당, 기능기획 담당, 창의기능/로드맵/아키텍처 담당)가 실제 소스코드(Android ~8,630줄, Desktop ~8,326줄, 브라우저 확장 ~700줄)와 HANDOFF/CHANGELOG/DECISIONS/BUGS/IDEAS 5종 문서를 근거로 작성했습니다. 모든 항목에 파일:라인 근거를 표기했습니다.

---

## 목차
1. 프로젝트 분석
2. 코드 분석
3. 버그 탐지 (신규 발견분)
4. 성능 분석
5. 보안 분석
6. UX/UI 분석
7. 리팩토링 제안
8. 코드 개선 예시
9~10. 기능 추가 및 기획 (30건 + 구현계획)
11. 창의적인 기능 제안 (10건)
12. 기능 확장 로드맵
13. 아키텍처 개선
14. 테스트 전략
15. 문서화
16. 최종 평가 + TOP 20

---

## 1. 프로젝트 분석

**목적/핵심 기능**: 개인 디지털 디톡스용 앱·프로그램·사이트 차단 시스템. 스케줄/일일한도 기준 차단 + 해제 시 "실행 확인"(타이머+무작위 클릭+회유 멘트) 절차로 꼼수를 방지하고, 재확인할수록 대기시간이 늘어나는 escalation 시스템을 가진다. 별개 웹앱("공부앱")의 타이머/캘린더/계산기를 관리앱에 완전 네이티브로 재구현해 통합했다.

**기술 스택**
- Android: Kotlin, Jetpack Compose, Room DB(v21), AccessibilityService, WorkManager
- Desktop: Kotlin, Compose Desktop(Windows), JNA(Win32 API), `com.sun.net.httpserver.HttpServer`
- 브라우저 확장: Chrome Manifest V3(service worker), 순수 JS
- 동기화: Firebase Realtime Database REST API(양쪽 플랫폼 모두 SDK 대신 직접 HTTP, 익명 인증)

**데이터 흐름(3자 동기화)**
```
                    ┌─────────────────────────┐
                    │   Firebase RTDB (REST)   │
                    │  confirmSync / dailyUsage │
                    │  / pomodoro / studyLog /  │
                    │  calendar / calculator    │
                    └───────────▲────▲──────────┘
                     읽기+쓰기  │    │  읽기+쓰기
                    ┌───────────┘    └───────────┐
              ┌─────┴─────┐               ┌──────┴──────┐
              │  Desktop  │               │   Android    │
              │ Repository│               │PhoneLockRepo │
              │(JSON파일) │               │  (Room DB)   │
              └─────┬─────┘               └──────────────┘
                    │ HTTP 127.0.0.1:47882
                    │ (토큰 인증 /pair, /confirm, /tick, /heartbeat)
              ┌─────┴──────┐
              │ 브라우저 확장 │  ← Firebase 직접 접근 없음, 항상 데스크탑 경유
              └──────────────┘
```

검증된 동기화 패턴: `confirmSync`(그룹명 키, 최신값 승리) / `dailyUsage`·`studyLog`(기기별 키에 각자 쓰고 읽는 쪽이 합산) / `pomodoro`(웹앱→관리앱 단방향 신호, fail-closed) / `calendar`·`calculator`(문서 전체 LWW).

**설계 패턴 평가**: MVVM이 아니다. ViewModel 계층 없이 `@Composable`이 `Repository`를 직접 호출("뚱뚱한 Repository, 얇은/없는 ViewModel"). 두 플랫폼이 동일 방식이라 불일치는 없으나 테스트 용이성 면에서 약점.

**아키텍처 총평**: 표시값-판정로직 분리, `ConfirmationGate.kt` 불가침 원칙, "원본 유지+호출부 병합" 확장 패턴 등이 잘 지켜지고 있음. 구조적 리스크는 `Repository`가 6개 도메인을 가진 God Object라는 점과, 최근 크로스디바이스 기능이 "블로킹 I/O는 뮤텍스 밖" 원칙을 부분적으로 어기고 있다는 점(→ 3절 최우선 버그).

---

## 2. 코드 분석

**중복 코드**
- `CalcEngine.kt`(android/desktop) — 완전 동일, 드리프트 없음.
- `ui/theme/Color.kt`(android/desktop) — 완전 동일, 웹앱 CSS와도 hex값 일치.
- Desktop `PomodoroSyncClient.kt`에 구조적으로 거의 동일한 HTTP 요청 함수 ~14개 반복.
- Desktop만: persuasion 스텝퍼 UI가 `GroupEditScreen.kt`/`GroupListScreen.kt`에 3중 인라인 복제(Android는 `PersuasionStepper.kt`로 이미 분리됨) → 낮은 우선순위 리팩터링 후보.

**긴 함수/클래스(God class)**
| 파일 | 규모 | 비고 |
|---|---|---|
| `android/data/PhoneLockRepository.kt` | 1,182줄 | 그룹/사용량/escalation/타이머/캘린더/계산기/백업 6개 도메인 |
| `desktop/data/Repository.kt` | 1,141줄 | 동일 패턴, `synchronized(lock)` 하나로 전부 직렬화 |
| `android/service/AppMonitorAccessibilityService.kt` | 821줄 | 이벤트처리+틱루프+릴스감지+사이트감지+공부잠금+오버레이 View 생성 한 클래스 |

→ CLAUDE.md "불필요한 리팩터링 금지" 원칙상 **분리 리팩터링은 권하지 않음**. 신규 도메인 추가 시점에 재검토 권장.

**죽은 코드**: `browser-extension/quotes.js`의 `MOTIVATIONAL_QUOTES`가 항목 1개뿐이라 무작위 선택이 무의미(항상 같은 문구).

**순환 의존성**: 없음.

**테스트 용이성**: **Android/Desktop 양쪽 모두 테스트 코드 0줄**(`src/test`/`src/androidTest` 자체가 없음, desktop `build.gradle.kts`에 JUnit 의존성도 없음). `CalcEngine`처럼 순수/결정적인 로직조차 커버리지 0%.

---

## 3. 버그 탐지 (BUGS.md 미기재, 신규 발견분)

### 🔴 Critical — Desktop: `pushUsageToFirebase`가 뮤텍스 두 개를 잡은 채 동기 HTTP 호출
**위치**: `phone-lock-desktop/.../data/Repository.kt:169-174`

자매 함수(`pushStudyLogToFirebase` 등 4개)는 전부 `Thread{}.start()`로 비동기 처리하는데 이 함수만 동기 호출. 호출 경로: `EnforcementService.tick()` → `tickMutex.withLock{}` → `Repository.addUsageSeconds()`(`synchronized(lock)`) → 이 함수. **`tickMutex`와 `Repository.lock` 둘 다 잡은 채 최대 3~6초 네트워크 I/O를 블로킹** — 일일한도 그룹 사용 중 30초마다 다른 그룹 모니터링과 앱 전체 Repository 호출이 정지될 수 있음. 과거 "확인 대기는 뮤텍스 밖" 원칙이 막으려던 것과 같은 유형의 버그가 신규 코드로 재발.

**수정**: 락 안에서 필요한 값만 캡처하고, 실제 호출은 다른 push 함수들처럼 `Thread{}.start()`로 이동. 단 `flushPendingUsage()`(셧다운훅 경로)는 예외 처리 필요 — 무조건 비동기화하면 JVM 종료 시 마지막 동기화가 유실될 수 있음.

### 🟠 High — Desktop: `peerUsageSeconds`/`mergedEscalation` 읽기도 락 안에서 블로킹
**위치**: `Repository.kt:123-132`, `:259-275` — 10초 캐시 만료 시마다 락 안에서 동기 GET. 위와 동일한 리스크. **수정**: "락 밖에서 갱신, 락 안에서는 캐시만" 패턴으로 전환.

### 🟠 High — Android: `PreMigrationBackup`이 25~27차 신규 4개 테이블을 누락
**위치**: `data/PreMigrationBackup.kt:21-23` — `TABLES`가 5개뿐(`study_log_entry`/`calendar_task`/`calc_task`/`calc_saved_item` 누락). `fallbackToDestructiveMigration()`이라 다음 스키마 변경 시 이 4개 테이블 데이터가 유일한 안전망 없이 통째로 사라질 수 있음(Firebase 미설정 사용자는 완전 유실). **수정**: `TABLES`에 4개 추가.

### 🟠 High — Android: 캘린더/계산기 Firebase 동기화의 삭제→재삽입 구간에 트랜잭션 부재
**위치**: `PhoneLockRepository.kt:657-668`(캘린더), `:1035-1083`(계산기) — 같은 파일 `importBackupJson()`은 `db.withTransaction{}`으로 안전한데 동기화 함수는 미적용. 삽입 도중 프로세스가 죽으면 로컬 데이터가 빈 상태로 남을 수 있음(다음 동기화로 자연 회복되나 그 사이 사용자 눈엔 유실처럼 보임).

### 🟡 Medium
- Android `ConfirmOpenActivity.recordConfirm()`(`:29-36,76-81`)이 `Repository.kt` 자체 주석이 경고하는 `lifecycleScope` 패턴을 어기고 즉시 `finish()` — 확인 기록 유실 가능성.
- Android `addUsageSeconds`(`PhoneLockRepository.kt:165-181`)의 read-modify-write가 뮤텍스 없이 동시 실행 가능(escalationCache는 뮤텍스 있는데 이쪽만 없음) — 사용시간 유실 가능.
- Android `PomodoroSyncClient`의 `tokenCache`/`statusCache`가 동기화 없는 공유 var(`@Volatile` 없음).
- Android 오버레이 카운트다운 잡 시작이 non-atomic(`AppMonitorAccessibilityService.kt:596-604,622-630`) — 초 표시가 튈 수 있음.
- 양 플랫폼: HTTP 함수 다수가 예외 시 `conn.disconnect()`를 스킵 — `try/finally` 필요.

### 🟢 Low / 문서 불일치
- Desktop `LocalApiServer.kt`가 사실상 단일 스레드 executor — Critical 이슈와 겹치면 확장 요청 전체가 순차 대기.
- **문서-코드 불일치**: BUGS.md의 "Alt-Tab 우회" 버그가 Open으로 기록돼 있으나 `background.js:202-215`에 이미 `onActivated`/`onFocusChanged` 리스너가 구현되어 있고 주석도 "이 버그의 수정"이라 명시. 코드는 고쳐졌는데 문서가 안 따라간 것으로 보임 — 실기기 검증 시 함께 확인해 BUGS.md 정리 권장(단, `tabs.create({active:true})` 같은 엣지케이스는 미검증).

---

## 4. 성능 분석

| 항목 | 현재 문제 | 개선 방법 | 예상 효과 |
|---|---|---|---|
| Desktop 모니터링 루프 | 3절 Critical/High 버그로 30초~10초 주기 앱 전체 정지 가능 | 락 밖에서 네트워크 I/O | 체감 프리징 제거 |
| `LocalApiServer` | 단일 스레드 | 고정 스레드풀(2~4) 지정 | 확장 요청이 Firebase 호출에 종속 안 됨 |
| Android `PomodoroSyncClient` | ~14개 HTTP 함수 중복 | 공통 요청 헬퍼로 통합 | 코드량 감소(성능 영향은 미미) |
| Android usage 갱신 | 동시 갱신 시 유실 가능 | DB 원자적 UPDATE | 정확도 향상 |
| 캐시 활용 | TTL 캐시 이미 잘 적용됨 | — | 양호 |
| 병렬처리 | 개인용 앱 특성상 큰 이득 없음 | 현행 유지 | — |

**결론**: 이 앱의 성능 문제는 알고리즘이 아니라 **동시성 설계(뮤텍스 범위)**에 집중 — 3절 버그와 동일 근본 원인.

---

## 5. 보안 분석

위협 모델: 원격 공격자가 아니라 **"본인이 우회하는 것을 막는 것"**.

| 항목 | 위험도 | 평가 |
|---|---|---|
| 로컬 API 토큰 평문 저장 | 낮음(사실상 무해) | 파일시스템 접근 가능한 "본인"에겐 토큰 탈취보다 `data.json` 직접 편집이 더 쉬운 우회 경로 — 토큰 보안 강화는 실제 방어선이 아님 |
| `/pair`/`/confirm`/`/tick`/`/heartbeat` POST+토큰 필수 | 양호 | 정상 확장 요청 구분 충분 |
| `/overlay-status`만 CORS 전면 개방 | 낮음 | 상태 변경 없는 조회 전용, 설계상 의도된 완화 |
| Firebase 익명 인증 + REST | 낮음~중간 | 클라이언트 SDK 일반 모델과 동일. **RTDB 보안 규칙이 실제로 걸려있는지는 코드 밖(콘솔) — 확인 가치 있음** |
| 브라우저 확장 XSS | 낮음 | `innerHTML`/`eval` 사용 없음, 전부 `.textContent` |
| 로그/입력검증 | 낮음 | 민감정보 로깅 없음, 로컬호스트 전용이라 실질 리스크 낮음 |

**결론**: 이 앱에서 의미있는 "보안"은 "본인이 판정 로직을 우회하는 걸 얼마나 번거롭게 만드는가"이며 이미 설계 차원에서 다뤄지고 있음. 유일하게 확인해볼 가치가 있는 건 Firebase RTDB 보안 규칙 설정 여부(코드 밖).

---

## 6. UX/UI 분석

- **웹앱과 색상/UI 일치도 매우 높음**: `공부앱/index.html` CSS 변수 9개와 양 플랫폼 `Color.kt` 대조 결과 hex값까지 전부 일치.
- **접근성 공백**: 전체 코드베이스에서 `contentDescription`/`semantics` 사용 단 7건뿐 — 6,000줄 넘는 화면 코드 대비 스크린리더 라벨 대부분 누락 가능성.
- **의도된 고마찰 UX**: 실행확인 절차(무작위 체크포인트, 백그라운드 이동 시 진행률 리셋)는 버그가 아니라 핵심 목적에 부합하는 설계.
- **피드백 부족**: 데스크탑 앱이 꺼져 있으면 브라우저 확장이 조용히 fail-open — 사용자가 "왜 갑자기 차단이 안 되지" 원인 파악이 어려움.
- `quotes.js` 콘텐츠 공백(2절)도 UX 문제 — "무작위 문구"가 실제론 항상 같음.

---

## 7. 리팩토링 제안 (우선순위별)

**1순위(버그 수정에 가까움, 최소 변경)**: `pushUsageToFirebase` 비동기화(1줄 패턴 변경) / `PreMigrationBackup.TABLES`에 4개 테이블 추가(1줄).

**2순위(락 밖 I/O 통일)**: `peerUsageSeconds`/`mergedEscalation` 읽기 패턴 전환 / Android `addUsageSeconds` 뮤텍스 또는 원자적 UPDATE.

**3순위(견고성)**: HTTP 함수 `try/finally{disconnect()}` 일괄 적용 / `ConfirmOpenActivity.recordConfirm()` ioScope 전환 / `LocalApiServer` 고정 스레드풀.

**4순위(선택적)**: `quotes.js` 문구 보강 / BUGS.md Alt-Tab 항목 상태 확인 / Desktop persuasion 컴포넌트 분리(최하위).

**명시적으로 권하지 않음**: `Repository`/`PhoneLockRepository` God class 분리, `AppMonitorAccessibilityService` 책임 분리 — 현재 잘 작동 중이고 프로젝트 원칙과 충돌.

---

## 8. 코드 개선 예시

### 8-1. Desktop `pushUsageToFirebase`
```kotlin
// 기존 (Repository.kt:169-174)
private fun pushUsageToFirebase(groupId: Long, dateStr: String, usedSeconds: Int) {
    val group = data.groups.find { it.id == groupId } ?: return
    com.phonelock.desktop.monitor.PomodoroSyncClient.writeDailyUsage(
        data.fbDatabaseUrl, data.fbApiKey, data.fbUser, dateStr, group.name, DAILY_USAGE_DEVICE, usedSeconds
    )
}
```
```kotlin
// 개선 — 다른 push 함수와 동일한 Thread{}.start() 패턴
private fun pushUsageToFirebase(groupId: Long, dateStr: String, usedSeconds: Int) {
    val group = data.groups.find { it.id == groupId } ?: return
    val url = data.fbDatabaseUrl; val key = data.fbApiKey; val user = data.fbUser
    Thread {
        com.phonelock.desktop.monitor.PomodoroSyncClient.writeDailyUsage(
            url, key, user, dateStr, group.name, DAILY_USAGE_DEVICE, usedSeconds
        )
    }.start()
}
```
**주의**: `flushPendingUsage()`(셧다운훅)는 별도로 동기 유지 또는 `join()` 필요.

### 8-2. Android `PreMigrationBackup`
```kotlin
// 기존
private val TABLES = listOf(
    "app_group", "group_member", "usage_record", "group_site", "confirm_escalation"
)
// 개선
private val TABLES = listOf(
    "app_group", "group_member", "usage_record", "group_site", "confirm_escalation",
    "study_log_entry", "calendar_task", "calc_task", "calc_saved_item"
)
```

(실제 파일은 수정하지 않았습니다 — 분석/제안용입니다.)

---

## 9~10. 기능 추가 및 기획 (30건 + 구현계획)

IDEAS.md 기존 백로그, DECISIONS.md의 이미 폐기된 방향(remoteCommand, 캘린더-계산기 연동)과 중복되지 않게 구성. 크로스디바이스 신규 기능은 "신호"(pomodoro/confirmSync 패턴) vs "누적 데이터"(dailyUsage/studyLog 패턴) 중 하나를 반드시 따름.

### 범주 A. 사용자 편의
1. **그룹 일시정지(스누즈)** — 30분 등 짧은 예외를 전체 회유 절차 없이 자동 원복. 난이도 하, 3~4h, **적용 권장**. `Group.snoozedUntilEpochMillis` 필드 + `LockEvaluator`/`SiteEnforcement` 가드 한 줄.
2. **현재 화면 앱/사이트 원클릭 그룹 추가** — 트레이 메뉴/알림 액션에서 즉시 추가. 난이도 중, 6~8h, 적용 권장.
3. **잠금 해제/스누즈 사유 메모** — 자기 성찰용 로컬 전용 기록. 난이도 하, 4h, 선택.

### 범주 B. 관리자(설정) 기능
4. **그룹 프리셋 라이브러리**(SNS/게임 차단 세트) — 로컬 전용. 난이도 중, 8h, 선택.
5. **스케줄 프로필(주중/주말/시험기간) 전환** — 판정 진입점 3곳(LockEvaluator/EnforcementService/SiteEnforcement)에 오버라이드 레이어 필요, 민감 영역. 난이도 상, 16~20h, 선택(세부 설계 재확인 필수).
6. **설정/그룹 가져오기·내보내기(JSON)** — 기기 교체/재설치 시간 단축, 실기기 검증 반복이 잦은 이 프로젝트에 실용적. 난이도 중, 8h, **적용 권장**.

### 범주 C. 자동화
7. **기간 지정 자동 강화 규칙**(시험기간 강제 on) — 기능5보다 훨씬 저렴한 대안. `Group.forceEnabledFrom/Until` 필드만. 난이도 하, 4h, **적용 권장**(5보다 우선 검토).
8. **Wi-Fi SSID 기반 위치 자동 프로필**(안드로이드 전용) — 위치 권한 필요, 비대칭 기능. 난이도 중, 10~12h, 선택.
9. **목표 달성 보상 크레딧**(캘린더 완료 → 잠금 예외시간 적립) — 게이미피케이션, 판정 로직에 크레딧 체크 추가. 난이도 중, 10h, 선택(사용자 의향 우선 확인).

### 범주 D. 생산성(공부앱)
10. **공부 세션 종료 후 짧은 회고 입력** — `StudyLogEntry.note` 필드 추가만으로 기존 크로스디바이스 동기화에 자연 포함. 난이도 하, 5h, **적용 권장**.
11. **"오늘 한눈에" 통합 요약 위젯** — 새 데이터/API 불필요, 기존 3개 조회 조합만. 난이도 하, 3h, **즉시 적용 권장**(비용 대비 효과 최고).
12. **주간 집중 리포트 이미지 내보내기** — Compose 오프스크린 렌더링. 난이도 중, 8h, 선택.
13. **공부 잠금 진입 시 방해금지 모드 자동 on**(안드로이드) — DND 권한 연동. 난이도 하, 4h, 적용 권장.

### 범주 E. 성능 향상
14. **안드로이드 접근성 이벤트 디바운싱** — 판정 지연 없는 순수 성능 최적화 확인 후 적용. 난이도 하, 2~3h, 적용 권장.
15. **Firebase 폴링 주기 적응형 조정**(포그라운드/백그라운드) — 37차 20분 신선도 컷오프와 상호작용 검토 필요. 난이도 중, 6h, 선택.

### 범주 F. 보안
16. **로컬 API 토큰 만료/재발급** — 위협 모델 대비 과함일 수 있음. 난이도 하, 4h, 선택.
17. **그룹 삭제/완화 시 PIN 확인** — 자기통제 목적에 직결, 가치 큼. 편집 면제 시간대와의 관계, PIN 분실 복구 경로 설계 필수. 난이도 중, 8h, **적용 권장**(세부 설계 재확인 필요).
18. **로컬 데이터 파일 암호화**(Windows DPAPI) — 위협 모델 대비 공수 큼, 마이그레이션 리스크. 난이도 상, 12~16h, 선택.

### 범주 G. 데이터 관리
19. **로컬 다세대 백업(로테이션)** — "어제 상태로 복원" 가능. 난이도 하, 5h, 적용 권장.
20. **사용 기록 CSV 내보내기** — 난이도 하, 4h, 적용 권장.
21. **오래된 통계 데이터 자동 정리** — 캘린더의 기존 6개월 정리 패턴 재사용. 난이도 하, 4h, 적용 권장.

### 범주 H. AI 기능
22. **로컬 휴리스틱 기반 그룹 추천**(외부 API 불필요, 순수 통계) — 프라이버시/비용 이슈 없음. 난이도 중, 10h, **적용 권장**.
23. **(신중 검토) 외부 LLM 기반 맞춤 회유 멘트 생성** — 실행확인 핵심 흐름에 외부 의존성을 넣는 리스크, remoteCommand 폐기 교훈과 유사한 함정. 반드시 판정 로직과 격리, 오프라인 폴백 필수. 난이도 상, 16h+, 선택.

### 범주 I. 협업(가족/코치)
24. **보호자용 읽기 전용 대시보드** — "관리앱은 읽기 전용" 원칙 유지, 별도 Firebase 경로에 집계값만. 난이도 상, 20h+, 선택(필요성 우선 확인).
25. **캘린더 완료 알림 공유**(웹훅) — 가벼운 버전. 난이도 하, 5h, 선택.
26. **스트릭 리더보드** — 1인 도구 목적에서 이탈이 큼, 최하위 우선순위. 난이도 상, 24h+, 선택.

### 범주 J. 접근성
27. **큰 글씨/고대비 모드** — 전 화면 하드코딩 폰트 전수 점검 필요. 난이도 중, 10~14h, 적용 권장.
28. **TalkBack 라벨 보강** — 실행확인/차단 화면처럼 핵심 흐름부터 우선. 난이도 중, 12~16h, **적용 권장**.

### 범주 K. 설정
29. **알림 사운드/진동 커스터마이징** — 난이도 하, 4h, 선택.
30. **다국어(영어) UI 지원** — 전면 문자열 추출, "불필요한 리팩터링 금지" 원칙과 충돌 가능. 난이도 상, 40h+, 선택(ROI 낮음, 최하위).

### 범주 L. 통계
31. **전주/전월 대비 비교 뷰** — 새 조회 불필요. 난이도 하, 5h, 적용 권장.
32. **그룹별 "위반 시도" 카운터**(재확인 트리거 횟수) — 37차 패턴("원본 유지, 호출부만 카운터")과 동일해 안전. 난이도 하, 5h, **적용 권장**.

### 범주 M. 모니터링
33. **데스크탑 트레이 미니 대시보드** — 난이도 중, 8h, 선택.
34. **이상 사용 패턴 알림**(7일 평균 대비 급증 감지) — 난이도 중, 8h, 적용 권장.

### 범주 N. 개발자 편의
35. **인앱 디버그 로그 뷰어** — "배포 반영 안 됨" 문제(33/36차 전례)를 실기기에서 직접 확인 가능. 난이도 하, 4~6h, **적용 권장**(검증 작업 자체를 가속).
36. **로컬 API 요청 로그(개발자 모드)** — 확장↔데스크탑 통신 디버깅. 난이도 하, 3h, 선택.

### 범주 O. 프로젝트 특화
37. **스트릭 프리즈**(월 1~2회 제한, 습관 이탈 방지) — 기존 스트릭 로직의 "일정 없는 날" 분기 재사용. 난이도 하, 5h, 선택.
38. **"긴급 해제" 신뢰인 승인 코드**(오프라인 챌린지-응답) — remoteCommand 폐기 교훈 반드시 준수, 네트워크 왕복 없는 완전 오프라인 설계 필수. 난이도 상, 20h+, 선택(기능17로 충족되는지 우선 확인).

**요약**: 즉시 적용 권장 — #11. 적용 권장 — #1,2,6,7,10,13,14,17,19~22,27,28,31,32,34,35. 나머지는 선택(효과 있으나 범위/재확인 필요). 각 기능의 상세 구현계획(수정파일/신규파일/순서)은 위 원본 조사 결과에 전체 기록되어 있으며, 필요시 IDEAS.md에 채택 항목만 옮겨 기록 권장.

---

## 11. 창의적인 기능 제안 (10건)

1. **재확인 통과 패턴 분석** — 기존 escalation/사용량 로그만으로 "언제 의지가 약해지는지" 자기 인식 리포트.
2. **캘린더 완료 스트릭 → 잠금 강도 자동 연동("보상형 완화")** — `overlayLevelStepsToMax`를 고정값 대신 스트릭 함수로. 37차 "표시값-판정로직 분리" 패턴 그대로 확장.
3. **계산기 "마감 위험도"를 공부 잠금 강도에 반영** — 같은 기기 로컬 데이터 참조만, 동기화 불필요.
4. **"유예시간 공유"를 그룹 태그 단위로 확장** — 37차 기기 간 패턴을 그룹 간으로 재사용.
5. **오프라인 회유 멘트를 캘린더/계산기 데이터로 개인화** — "오늘 안에 안 끝내면 D-2 남음" 같은 실데이터 삽입.
6. **"실행확인 레벨"을 공부 통계 지표로 역노출** — 절제력의 정량적 피드백 루프.
7. **3중 플랫폼 "허용 목록 제안"**(참고용, 자동 반영 아님) — 24차 "기기별 로컬" 원칙 유지하며 힌트만 제공.
8. **"완전 오프라인 모드" 배지** — 새 로직 없이 기존 특성을 사용자에게 명시적으로 노출.
9. **잠금 화면에 계산기 진도율 미니 위젯** — 차단(부정)에서 대안 제시(긍정)로 톤 전환.
10. **회독 색상 규칙을 응용한 그룹별 위험도 배지** — 앱 전체가 하나의 색 언어 공유.

---

## 12. 기능 확장 로드맵

**Phase 0(전제조건, 신규 기능보다 우선)**: HANDOFF.md 최우선 과제인 1~5단계 실기기 검증 + 37차 크로스디바이스 신규 기능 2건 검증. **이게 끝나야 새 버그가 "검증 안 된 37차 기능" 탓인지 "신규 기능" 탓인지 구분 가능.**

**Phase 1 — 관찰**(새 저장/판정 없이 기존 로그를 다시 보여주기만): 창의기능 1, 6, 10.

**Phase 2 — 연결**(같은 기기 로컬 데이터끼리 참조): 창의기능 3, 5, 9.

**Phase 3 — 표시값 자동 조정**(37차 패턴 확장, 판정 로직처럼 보일 위험 있어 범위 재확인 필수): 창의기능 2, 4.

**Phase 4 — 크로스디바이스 참고**(제어 아님, 정보만): 창의기능 7, 8.

---

## 13. 아키텍처 개선

**KMP(Kotlin Multiplatform) 검토**: 안드로이드/데스크탑이 파일명 단위로 거의 1:1 대칭(`CalcEngine`/`ConfirmationGate`/`PomodoroSyncClient`/`LockEvaluator`/UI 화면 5개/`theme/` 4개).

- **강력 추천**: `calc/CalcEngine.kt`만 `commonMain`으로 분리 — 순수 함수, 리스크 낮고 이득 명확(계산 로직 변경 시 한 곳만).
- **조건부**: `theme/Color.kt`/`Shape.kt`(값만 있는 파일) — Compose Multiplatform 계열이라 가능성은 있음.
- **비추천**: `Repository`/서비스 레이어/UI 화면 — 플랫폼 API에 강결합, DECISIONS.md에 "자연스러운 차이"로 이미 문서화됨. 억지 통합은 CLAUDE.md "불필요한 리팩터링 금지"와 충돌.

**결론**: KMP는 `CalcEngine` 단일 모듈만 실질적 개선 대상. 그 이상은 1인 프로젝트 규모에 과잉 설계.

**그 외**: `SiteEnforcement`/`EnforcementService`의 `overlayStatus` 중복은 IDEAS.md에 이미 "세 번째 사례 생기면 추출" 조건이 있어 현행 유지. `JsonStore.kt` 수동 직렬화는 리팩터링보다 "필드 추가 시 parse/save 둘 다 확인" 체크리스트화가 이 규모에 맞음.

---

## 14. 테스트 전략

**확인 결과 테스트 코드 0개**(`src/test`/`src/androidTest` 디렉토리 자체 없음). 1인 프로젝트에 풀 테스트 스위트는 과잉 — **가장 리스크 큰 순수 함수/판정 로직 위주 최소 Unit 테스트**만 권장.

**우선순위 1 — `CalcEngine`**(31차 실제 크래시 전례 있음):
```kotlin
class CalcEngineTest {
    @Test fun `daily target below remaining capacity returns success outcome`() { /* ... */ }
    @Test fun `finishDiffDays is negative when deadline is exceeded by required pace`() { /* ... */ }
    @Test fun `zero remaining work returns already-complete state without division by zero`() { /* ... */ }
}
```

**우선순위 2 — 캘린더 자동 회독 생성/미완료 복사**(대칭 페어, 리그레션 위험):
```kotlin
class CalendarAutoScheduleTest {
    @Test fun `completing red schedule creates orange task one day later`() { /* ... */ }
    @Test fun `reverting completion removes the auto-created next task if untouched`() { /* ... */ }
}
```

**우선순위 3 — 37차 쿨다운 병합 로직**(`ConfirmationGate.kt` 원본은 안 건드리되, 병합 함수는 회귀 방지용으로 고정):
```kotlin
class SyncedCooldownTest {
    @Test fun `local cooldown wins when more recent than synced value`() { /* ... */ }
    @Test fun `stale synced signal beyond staleness cutoff is ignored`() { /* ... */ }
}
```

**의도적 제외**: E2E(OS 레벨 상호작용이 핵심이라 자동화 비용 > 실효익, 사람이 실기기 검증하는 현재 방식이 합리적) / Stress·Performance(단일 사용자라 무의미) / 자동 보안 테스트(과잉) / Firebase 통합 테스트(REST 직접 조회 수동 절차가 이미 확립됨).

---

## 15. 문서화

5종 문서 체계가 37차까지 일관되게 잘 갱신되고 있어 **정말 부족한 것만** 제안.

- **필요**: 로컬 API 엔드포인트 표(IDEAS.md에 이미 후보로 있음) — `LocalApiServer.kt` 상단 주석 표로 충분, 별도 파일 불필요.
- **필요**: Firebase 스키마 6개 경로 요약 — HANDOFF.md에 표 하나 추가로 충분.
- **불필요**(이미 충분함): API 문서(외부 공개 API 없음), 아키텍처 다이어그램(HANDOFF "중요한 파일" 표 + DECISIONS.md가 이미 이 역할), 배포 가이드(HANDOFF "실행 방법" 섹션에 이미 상세).

---

## 16. 최종 평가

| 항목 | 현재 상태 | 개선 후 기대 |
|------|----------|--------------|
| 안정성 | 코드/빌드는 완료됐으나 실기기 검증 대부분 미완(과거 "반영 안 된 재배포" 오보 전례도 있음) | Phase 0 검증 완료 후 실사용 기준 안정성 확보 |
| 성능 | 알고리즘 문제 없음, 동시성 설계(뮤텍스 범위)에 리스크 집중 | 3절 수정 후 모니터링 루프 프리징 제거 |
| 보안 | 위협모델(본인 우회 방지) 기준 적절, RTDB 규칙 미확인 부분만 남음 | 낮은 비용으로 충분한 수준 유지 |
| 유지보수성 | DECISIONS.md가 "왜 이렇게 했는지"를 매우 상세히 기록 — 실제로는 양호. 대칭 복제(43+43개 파일)가 부담 요소 | `CalcEngine` KMP 분리로 중복 소폭 축소, 나머지는 의도된 차이로 유지 |
| 확장성 | 표시값/판정로직 분리, 신선도 컷오프 등 확장 패턴 정립됨 | Phase 1~4 로드맵으로 안전하게 확장 가능 |
| 테스트 | **0개** — 순수 함수조차 커버리지 없이 31차 크래시가 실사용 중 발견됨 | CalcEngine/캘린더/쿨다운 병합 3곳만 최소 커버해도 재발 방지 효과 큼 |
| 사용자 경험 | 웹앱 기준 UI 일관성 확보(32차), Alt-Tab 버그 문서상 미해결 상태로 남음 | 문서 정리 + Phase 1 관찰형 기능으로 자기통제 인지 강화 |
| 코드 품질 | CLAUDE.md 원칙(최소 변경, 기존 패턴 재사용)이 실제로 잘 지켜짐 | 유지, 신규 기능도 동일 원칙으로 확장 |

### 가장 먼저 수정해야 하는 TOP 20

1. 1~5단계 전체 실기기 검증(HANDOFF.md 최우선 후보)
2. 37차 크로스디바이스 신규 기능 2건 실기기 2대 검증
3. 37차 Android 수정분 폰 재설치 여부 확인
4. 37차 원격 신호 잠금의 20분 신선도 컷오프 실기기 재현 검증
5. Alt-Tab 사이트 차단 우회 버그 실제 수정(원인/해결방향 이미 확정, 승인만 필요)
6. Android `POST_NOTIFICATIONS` 실기기 권한 요청 플로우 확인
7. **[신규 발견] Desktop `pushUsageToFirebase` 뮤텍스 안 블로킹 I/O 수정**(3절 Critical)
8. **[신규 발견] Android `PreMigrationBackup` 누락 4개 테이블 추가**(3절 High)
9. `CalcEngine` 최소 Unit 테스트 추가(31차 크래시 전례 고려 시 검증보다 먼저일 수도)
10. 캘린더 완료/미완료 자동 복사 로직 리그레션 테스트 추가
11. `effectiveRemainingCooldownSeconds`(37차 병합 로직) 테스트로 고정
12. 로컬 API 엔드포인트 표 작성(CORS 정책 문서화)
13. 데스크탑에서 타이머 미러/공부기록 동기화 실사용 확인
14. 캘린더 날짜 상세 폴더 팝오버·애니메이션 실기기 검증
15. 계산기 결과 카드/타이머/캘린더 UI 웹앱 일치 재확인(32차분)
16. 안드로이드 허용 앱 선택창 실기기 확인(33차분)
17. 계산기 카드 그리드 반응형 확인(33차분)
18. `overlayLevelStepsToMax` 값 변경 시 오버레이 밝기 실동작 확인
19. Firebase 스키마 요약 표 HANDOFF.md에 추가
20. Phase 0 검증 완료 후 — 위 9~10절 "적용 권장" 기능부터 사용자와 우선순위 논의(#11, #35 등 저위험·고효율 기능 우선)

**배치 원칙**: 1~6번은 HANDOFF.md에 이미 명시된 최우선 검증 그대로. 7~8번은 이번 분석에서 새로 발견한 실제 버그라 검증 직후 최상위로 배치(수정 자체는 1~2줄로 매우 저렴). 9~11번은 검증과 병행 가능한 저비용 안전망(테스트). 12번 이후부터 신규 기능이 등장하며, 검증되지 않은 기반 위에 새 기능을 쌓지 않는다는 원칙을 지켰다.

---

## 부록: 이번 분석에서 실제로 발견한 신규 이슈 요약

| 우선순위 | 이슈 | 파일 | 예상 수정 규모 |
|---|---|---|---|
| Critical | `pushUsageToFirebase` 뮤텍스 안 블로킹 I/O | `desktop/data/Repository.kt:169-174` | ~5줄 |
| High | `PreMigrationBackup` 테이블 누락 | `android/data/PreMigrationBackup.kt:21-23` | 1줄 |
| High | 캘린더/계산기 동기화 트랜잭션 부재 | `android/data/PhoneLockRepository.kt:657-668, 1035-1083` | 함수 감싸기 |
| Medium | `peerUsageSeconds`/`mergedEscalation` 락 안 블로킹 | `desktop/data/Repository.kt:123-132, 259-275` | 패턴 전환 |
| Medium | `ConfirmOpenActivity.recordConfirm()` 스코프 위험 | `android/ui/ConfirmOpenActivity.kt:29-36,76-81` | ioScope 전환 |
| Medium | `addUsageSeconds` race condition | `android/data/PhoneLockRepository.kt:165-181` | 뮤텍스/원자적 쿼리 |
| Low | HTTP `disconnect()` 누락 다수 | 양 플랫폼 `PomodoroSyncClient.kt` | try/finally |
| 문서 | Alt-Tab 버그 BUGS.md Open ↔ 코드는 이미 수정됨 | `background.js:202-215` vs `BUGS.md` | 문서 확인/갱신 |

이 표의 항목들은 실제 코드 결함이므로, HANDOFF.md의 최우선 실기기 검증과 별개로 **다음 세션에서 우선 검토**하시는 걸 권장합니다. 실제 파일 수정은 진행하지 않았습니다(요청에 따라 분석/제안만 수행) — 원하시면 이어서 수정 작업을 진행하겠습니다.
