# DECISIONS

중요한 설계 결정만 기록. 단순 작업 내용은 CHANGELOG.md 참고.

---

## 포인트/보상 시스템 — 범위 축소, 잔액은 원장 합산, 새 탭 대신 기존 구조에 얹음 (2026-09-10, 102차 세션)

IDEAS.md "최우선 후보"(게이미피케이션)가 포인트/배지/레벨업/캐릭터키우기/친구초대 6개 하위 아이디어를 한 항목으로 묶고 있었는데, 착수 전 사용자에게 범위를 물어 "포인트 적립 + 오늘의 보상 언락"만 1차로 확정받았다 — 한 세션에 6개를 다 넣으면 각각이 제대로 검증되기 전에 서로 얽혀 어느 하나가 실패해도 원인을 못 좁히게 될 위험이 크다고 판단, IDEAS.md에 체크박스로 남겨 순차 진행하기로 함.

**잔액을 별도 필드로 저장하지 않고 매번 원장을 합산**하기로 했다 — 이 프로젝트가 이미 이 패턴을 쓰고 있다(`RoutineEngine.currentStreak`가 로그를 매번 다시 훑어 스트릭을 계산, 저장된 스트릭 값이 없음). 포인트도 저장된 balance 필드를 따로 두면 "포인트 적립 로직"과 "balance 필드 갱신"이 두 곳에서 따로 관리되며 어긋날 여지가 생기지만, 원장 합산은 항상 소스 오브 트루스 하나(ledger)에서 파생되므로 구조적으로 어긋날 수 없다. 원장이 무한히 커지는 문제는 이번 범위에서 다루지 않음(다른 로그 테이블들도 별도 정리 배치 없이 쌓이고 있어 기존 관례와 동일).

**루틴 완료/캘린더 완료로 적립된 포인트는 완료가 취소되면 그대로 회수**하기로 했다 — reason+refId+dateKey 조합을 "그 적립의 영수증"으로 취급해서, 취소 시 정확히 그 영수증만 찾아 지운다(중복 삭제·중복 적립 둘 다 방지). 공부시간 적립만 예외로 두고 회수 로직을 안 만들었는데, 공부 세션은 "완료를 취소"하는 개념 자체가 없어서(타이머 로그는 기록되면 끝) 대칭시킬 대상이 없기 때문.

**Firebase 문서를 루틴과 합치지 않고 `users/{user}/points`로 분리**했다 — 루틴 동기화(`syncRoutinesFromFirebase`)는 원격이 최신이면 로컬 루틴/로그를 통째로 delete+insert하는 방식인데, 포인트 원장을 같은 문서에 넣으면 루틴만 바뀌어도 포인트 동기화 타이밍에 얽혀 들어가 불필요하게 복잡해진다. 별도 노드+별도 LWW 타임스탬프(`pointsTs`)로 완전히 독립시켰다.

**데스크탑 `CalendarTask`에는 안정적인 id가 없다**(안드로이드는 Room autoIncrement id, 데스크탑은 dateKey+배열 순서로만 식별) — 포인트 refId를 `calendar:{dateKey}:{ordinal}`로 근사했다. 일정을 재배열하면 ordinal이 바뀌어 이론적으로 옛 refId와 어긋날 수 있지만, 캘린더의 기존 자동생성/이월 로직도 이미 같은 ordinal 주소 방식을 쓰고 있어(`setCalendarTaskStatus`의 `dayGlobalIndices(dateKey).getOrNull(ordinal)`) 새로운 위험을 추가한 게 아니라 기존 코드베이스가 이미 감수하던 위험 수준에 맞춘 것뿐이다.

**새 최상위 탭을 만들지 않고 "루틴" 섹션의 3번째 서브탭으로 얹었다** — `MainActivity.kt`에 "관리/공부/루틴/소셜/설정" 3~5개 최상위 탭 아래 각 섹션이 자체 서브탭을 갖는 구조가 이미 확립돼 있고(주석으로 "새 화면은 기존 top tab의 서브탭으로" 명시), 포인트를 적립하는 두 축(공부·루틴) 중 루틴 섹션에 이미 "오늘/연속기록" 2탭이 있어 자연스러운 자리였다. 공부 섹션에 넣지 않은 이유는 루틴 섹션이 이미 "활동 집계" 성격의 탭(연속기록)을 갖고 있어 같은 맥락으로 묶이기 때문.

## 모임 이름/설명 수정 — 새 기능이 기존 기능을 확장할 땐 기존 권한 모델을 따른다 (2026-09-10, 101차 세션)

모임 설명(description) 필드를 추가하는 작업을 시작할 때 "모임장(ownerUid)만 수정 가능"으로 설계하고 별도 함수(`updateGroupInfo`)를 새로 작성했으나, 착수 전 기존 코드를 검색하는 과정에서 "모임 이름 수정" 자체가 77차에 이미 구현돼 있고(`updateGroupName`), 그 기능의 UI 게이트가 모임장 전용이 아니라 **모임장 또는 관리자(`isAdmin`)** 로 이미 배포돼 있다는 걸 발견했다.

- **결정**: 새로 만든 owner-only 함수를 버리고, 기존 `updateGroupName`에 `description` 파라미터만 추가하는 방식으로 재작업했다. 권한도 기존 UI 게이트(모임장/관리자)를 그대로 유지 — owner-only로 좁히지 않았다.
- **이유**: 이름과 설명은 같은 "모임 기본 정보 수정" 다이얼로그 안에 나란히 있는 한 쌍의 필드다. 같은 다이얼로그의 한 필드는 관리자가 고칠 수 있고 바로 옆 필드는 모임장만 고칠 수 있게 만들면, 사용자 입장에서 이유를 알기 힘든 비일관적 권한 경계가 생긴다. CLAUDE.md의 "동일 기능이 이미 있는지 먼저 검색 후 구현"·"기존 아키텍처 유지" 원칙에 따라, 새 요구사항이 기존 기능의 자연스러운 확장일 때는 새 권한 모델을 만들지 않고 기존 것을 따르는 쪽을 우선한다.
- **일반화**: "OO만 할 수 있어야 한다"는 요구를 받으면, 그 OO 기능과 인접한 기존 기능이 이미 있는지 먼저 찾아보고 권한 모델이 다르면 그 차이가 의도적인지(예: 소유권 승계는 실제로 owner 전용) 먼저 확인할 것 — 이번엔 사용자가 명시한 바 없이 임의로 가정한 권한이었으므로 기존 관례를 따르는 게 안전했다.

---

## Firebase RTDB REST 직접 호출 경로에서는 `.indexOn` 누락이 "경고"가 아니라 조회 자체를 400으로 거부한다 (2026-09-10, 99차 세션)

96차부터 이월된 "채팅 전송은 되는데 목록에 안 뜨는" 버그의 근본 원인이 `orderBy=sentAtMillis` 조회에 대한 `.indexOn` 색인 누락이었음을 99차에 확정했다.

- **오판했던 지점**: Firebase 공식 문서/통념상 "SDK로 orderBy 쓸 때 `.indexOn`이 없으면 콘솔에 경고만 뜨고 쿼리는 동작한다"는 게 일반적으로 맞는 얘기라, 98차 조사 때 이 가능성을 먼저 배제하고 코드 로직 쪽만 팠었다. 하지만 이 프로젝트는 모든 Firebase 접근을 SDK 없이 REST(`HttpClient`/`HttpURLConnection`)로 직접 호출하는 구조라 사정이 달랐다 — REST 경로에서는 색인 없는 `orderBy` 쿼리가 그냥 느리게 도는 게 아니라 HTTP 400으로 거절당했다.
- **왜 못 잡았는지(교훈)**: 쓰기(전송)는 색인과 무관해 항상 성공했고, 읽기(목록 조회) 실패는 코드가 조용히 빈 리스트로 덮어쓰고 있어서(`.getOrDefault(emptyList())`) 겉보기엔 "메시지가 그냥 안 뜬다"는 애매한 증상만 남았다. 98차엔 "전송 실패" 쪽에만 실패 사유 표시 계측을 넣었는데 정작 실패하는 건 읽기 쪽이라 아무 도움이 안 됐고, 읽기 쪽에도 동일한 계측(`Result<List<ChatMessage>>`)을 넣고서야 사용자가 실제 400 에러 본문을 그대로 전달해줘 확정할 수 있었다.
- **일반화된 교훈**: 이 프로젝트에서 앞으로 새 `orderBy` 쿼리를 추가할 땐 `.indexOn`을 항상 같이 추가할 것. 그리고 "쓰기/읽기 중 어느 쪽이 실패하는지" 증상만으로는 구분이 안 될 때, 양쪽 모두에 동일한 수준의 실패 사유 표시 계측을 넣는 게 코드를 더 추측하는 것보다 빠른 진단 경로였다.

---

## 채팅 사진/GIF 첨부: 구현했다가 사용자 요청으로 전면 삭제 (2026-09-10, 99차 세션)

사용자 요청으로 Firebase Storage REST 업로드 기반 사진/GIF 첨부(안드로이드 Coil, 데스크탑 Skia 디코드 표시)를 구현·빌드·배포까지 완료했으나, 곧이어 "사진, gif 보내기 기능 삭제해"라는 요청을 받아 관련 코드를 전부 원복했다.

- **되돌린 이유**: 사용자가 별도 사유 설명 없이 삭제를 요청 — 이 문서엔 되돌렸다는 사실과 범위만 기록한다(다시 요청받으면 이 세션의 구현 방식(Storage REST 업로드+토큰 포함 다운로드 URL을 메시지 필드에 저장)을 그대로 재사용할 수 있다).
- **완전히 제거된 것**: `ChatSyncClient.uploadChatMedia()`, `ChatMessage.mediaUrl` 필드, `sendGroupMessage`/`sendDmMessage`의 `mediaUrl` 파라미터, 안드로이드 `coil-compose`/`coil-gif` 의존성과 `PhoneLockApplication`의 `ImageLoaderFactory`, `firebase-storage.rules`의 `chatMedia/` 규칙. `ChatThreadScreen.kt`의 `chatId`/`ActiveChatTracker`(채팅 알림용으로 같이 도입됨)는 첨부 기능과 무관해 유지.
- **남겨둔 교훈**: 이 세션에 Firebase Storage 활성화 여부를 확인 못 했다(콘솔 접근 권한 없음) — 클라우드 백업 기능(82차)도 같은 사전조건을 요구하는데 여태 활성화됐는지 불명인 채로 방치돼 있다. 사진 첨부를 다시 요청받으면 이 부분부터 확인할 것.

---

## 채팅 알림은 기존 무전기 서비스에 편승, 새 채널은 진동 기본값을 명시적으로 끔 (2026-09-10, 99차 세션)

사용자가 "채팅방 알림 없다면 알림 넣어줘 + 진동 없게 하자"고 요청했을 때, 별도 백그라운드 서비스를 새로 만들지 안드로이드 `WalkieTalkieService`(로그인 중 항상 도는 7초 폴링 포그라운드 서비스)에 얹을지 검토했다.

- **결정**: `WalkieTalkieService`의 기존 폴링 루프에 `pollChatMessages()`를 추가하는 쪽을 택했다 — 이 서비스는 이미 "화면이 꺼져 있어도 짧은 주기로 돌아야 하는" 요구사항(무전기/넛지)과 동일한 조건을 만족하고 있어서, 새 포그라운드 서비스를 또 띄우면 상주 알림이 2개가 되고 배터리 사용량도 이중으로 잡힌다.
- **비용 절감**: 채팅방마다 전체 메시지 200개를 매번 받아오면 낭비라, `ChatSyncClient.peekLatestGroupMessage`/`peekLatestDmMessage`(신규, `limitToLast=1`)로 "최신 메시지 1개만" 가볍게 조회하는 전용 함수를 별도로 뒀다 — `SocialGroupSyncClient`의 넛지 조회와 동일한 절약 패턴.
- **진동 채널**: 이 프로젝트의 기존 알림 채널(루틴 리마인더/스트릭/넛지/무전기 메시지)은 전부 `enableVibration(true)`를 명시적으로 켜야 했다(안드로이드 기본값이 꺼짐이라서) — `chat_message` 채널은 사용자가 명시적으로 "진동 없이"를 요청한 첫 케이스라, 아무것도 안 하는 것(기본값 그대로 둠)만으로 요구사항이 충족된다. 다만 채널 정책상 한 번 배포된 채널 ID는 앱이 코드로 재정의할 수 없으므로(61차/74차에 반복 확인된 제약), 나중에 이 채널의 진동 설정을 바꿔야 할 일이 생기면 새 채널 ID(`chat_message_v2` 등)로 우회해야 한다.
- **중복 알림 방지**: 지금 보고 있는 대화방에 대해서도 백그라운드 알림이 뜨면 어색하다 — `ChatThreadScreen.kt`가 열려있는 동안 전역 `ActiveChatTracker.openChatId`(양 플랫폼, `@Volatile var`)를 채워두고 백그라운드 폴러가 이 값과 대조해서 건너뛰도록 했다. `DisposableEffect`로 진입 시 설정/이탈 시 해제.

---

## 루틴 모드: 모드도 루틴/로그와 동일한 "배열 인덱스 참조" 동기화 패턴 (2026-09-10, 98차 세션)

루틴 모드(96차 설계, 98차 구현) Firebase 동기화 스키마를 정할 때, 기존 `routineLogs`가 로컬 auto-increment id 대신 `routines` 배열 안 인덱스(`routineIndex`)로 부모를 참조하던 패턴(51차, 로컬 id가 기기마다 달라 충돌하기 때문)을 모드에도 그대로 확장했다.

- **결정**: `modes` 배열을 최상위에 추가하고, 각 `routines[i]`에 `modeIndex`(그 루틴이 속한 모드가 `modes` 배열의 몇 번째인지) 필드를 넣는다. 반입 시 모드를 배열 순서대로 새 로컬 id로 재삽입한 뒤, 그 새 id를 `modeIndex`로 찾아 루틴에 연결한다.
- **이유**: 모드의 로컬 id도 루틴/로그와 똑같이 기기마다 다르게 배정되므로(안드로이드 Room auto-increment vs 데스크탑 수동 카운터), 같은 문제가 그대로 재발한다 — 이미 검증된 해결 패턴을 새 필드에도 일관되게 적용하는 게 새로운 방식을 고안하는 것보다 안전하고, 코드 리뷰 시 "왜 이렇게 짰는지" 설명 부담도 없다.
- **모드 삭제 시 루틴 재배정**: 마지막 남은 모드는 삭제할 수 없게 막고(루틴 모드는 항상 최소 1개), 모드를 삭제하면 그 모드의 루틴은 자동으로 남은 첫 모드(sortOrder 최소)로 재배정한다 — 사용자에게 "이 모드의 루틴은 어떻게 할까요" 확인창을 띄우는 대신 자동 처리를 택한 이유는 루틴 자체가 없어지는 게 아니라 다른 모드로 옮겨질 뿐이라 되돌리기 쉬운 결정이기 때문(모드 서브탭만 바꾸면 바로 보임).

---

## 온라인/오프라인 모드: 감지는 실제 네트워크 연결 유무 기준(ConnectivityManager), Firebase 요청 실패 감지 방식은 채택 안 함 (2026-09-10, 98차 세션)

사용자가 "게스트 계정이 네트워크 필요 기능에서 오류→로그아웃되는 버그"를 언급하며 온라인/오프라인 모드 도입을 요청, 감지 방식으로 ①Firebase 요청 실패 시점 감지 ②실제 인터넷 연결 감시(ConnectivityManager) ③둘 다 중 ②를 선택했다(사용자 확인).

- **결정**: 안드로이드는 `ConnectivityManager.NetworkCallback`으로 실시간 연결 상태를 감시하는 `NetworkMonitor` 싱글턴을 앱 시작 시 등록. 데스크탑은 대응하는 OS API가 마땅치 않아, 호출 시점에 8.8.8.8:53으로 짧은 타임아웃(1초) TCP 연결을 시도하는 방식으로 대체(상시 감시 대신 각 화면 진입 시 1회씩만 확인하므로 부담 없음).
- **이유**: Firebase 요청 실패 시점 감지는 "연결은 있지만 Firebase 서버만 일시적으로 응답 안 함" 같은 경우와 "진짜 오프라인"을 구분 못 해 오탐(진짜 오프라인이 아닌데 오프라인으로 잘못 전환)이 생길 수 있다 — 사용자도 연결 유무 기준이 "사용자 입장에서 더 안전(off인데 Firebase만 일시 먹통이어도 오프라인으로 잡힘)"하다고 판단해 확정.
- **게스트는 온/오프라인과 무관하게 항상 로컬 전용**: `isEffectivelyOffline()`이 수동 토글 OR 연결 끊김 OR 게스트(익명) 계정 중 하나라도 true면 오프라인으로 취급하도록 게스트 조건을 포함시켰다 — "게스트는 네트워크 필요 기능만 빼고 전부 로컬로 쓸 수 있게" 요구를 별도 분기 없이 기존 오프라인 판정 로직 안에 자연스럽게 편입.
- **범위 제한(98차엔 4개 화면만 게이트)**: `isEffectivelyOffline()`으로 게이트한 곳은 루틴/캘린더/계산기/그룹설정 4개 화면의 진입 시 동기화 호출뿐 — 소셜/무전기/계정동기화 등 나머지 Firebase 호출부는 이번 세션에 손대지 않았다(각 호출부가 이미 `runCatching`/타임아웃으로 방어돼 있어 크래시는 안 나지만, 오프라인일 때 불필요하게 네트워크 타임아웃을 기다리는 지연은 남아있음 — 다음에 확장 검토).

---

## 도움말 예시 이미지: 실제 화면 캡처 대신 HTML/CSS 재현 + 헤드리스 크롬 렌더링 (2026-09-09, 97차 세션)

97차 세션 안에서 도움말 이미지 방식이 세 번 바뀌었다: ① 이모지 큰 배지 → ② 실제 데스크탑 앱을 화면 캡처(PowerShell `PrintWindow`/전체화면 캡처) → ③ HTML/CSS로 재현한 목업을 헤드리스 크롬으로 렌더링. 최종적으로 ③을 채택했다.

- **결정**: 도움말에 쓰는 "실제 화면 예시 이미지"는 앱을 실제로 캡처하지 않고, HTML/CSS로 앱과 동일한 팔레트·컴포넌트 스타일을 재현한 뒤 `chrome.exe --headless --screenshot`으로 PNG를 뽑는다. 번호 배지는 `position:absolute` CSS로 실제 요소 위 정확한 픽셀 좌표에 겹쳐 그린다.
- **이유**: ② 실제 캡처는 두 가지 문제가 있었다. 첫째, 사용자가 "디자인적으로 별로"라고 명확히 기각했다 — 실제 앱 UI를 그대로 오려붙이면 창 크롬(제목표시줄)·경고 배너 같은 도움말과 무관한 잡음이 함께 찍히고, 프레이밍을 통제할 수 없었다. 둘째, 실제 데이터(로그인 계정 실명, 가입 승인 대기자 아이디 등)가 찍혀서 개인정보가 공개 GitHub 릴리스 자산에 그대로 실릴 위험이 있었다(설정 화면 캡처에서 실제로 발생 — 검게 가려서 회피해야 했음). ③ HTML 재현 방식은 두 문제 모두 원천적으로 없다 — 표본 데이터만 쓰고, 배지 좌표를 코드로 정확히 지정할 수 있어 나중에 스크린샷 위에 원을 덧그리는 것보다 정확도가 높았다(실측: 실제 캡처+수동 좌표 추정 방식은 배지가 버튼을 벗어나는 경우가 잦아 재작업이 필요했던 반면, HTML 방식은 plain 렌더 1회로 좌표를 읽고 배지를 넣으면 대부분 한 번에 맞았음).
- **파생 결정 — 이미지는 두 플랫폼이 동일 파일을 공유**: 안드로이드 `res/drawable`과 데스크탑 `src/main/resources/guide/`에 같은 PNG를 복사해 넣는다(사용자가 "모바일과 데스크탑 둘 다 범용적으로 쓸 수 있게" 명시적으로 요구) — 실제 화면 캡처였다면 플랫폼별로 다른 이미지가 필요했겠지만, 재현 목업은 플랫폼에 종속되지 않는 추상적인 "기능 설명용 화면"이라 한 세트로 충분하다.
- **파생 결정 — 모든 도움말 페이지에 이미지, 텍스트는 보조**: 처음엔 각 탭의 첫 페이지(개요)에만 이미지를 넣었는데, 사용자가 "일부 화면만 수정하는 작은 작업이 아니라 도움말 전체의 구조와 설명 방식을 다시 잡는 큰 개편"이라고 정정 — 32개 섹션 페이지 전부에 그 섹션이 설명하는 화면의 예시 이미지를 붙였다(같은 화면을 설명하는 섹션은 이미지 재사용). 기존 상세 텍스트(불릿)는 삭제하지 않고 이미지 아래에 그대로 남겨, 이미지로 감을 잡고 텍스트로 정확한 규칙을 확인하는 구조가 됐다.

---

## 도움말을 "단일 워크스루" 대신 "짧은 워크스루 + 탭별 상세 도움말"로 분리 (2026-09-09, 97차 세션)

89차에 만든 도움말은 관리/일시정지/공부/루틴/모임/설정을 한 번에 스와이프하는 7페이지짜리 워크스루 하나뿐이었다. 93~96차에 디자인·기능이 계속 바뀌면서(소셜 메신저 확장, 관리 탭 자물쇠 개편 등) 이 7페이지 내용이 실제 화면과 어긋나기 시작했고, 사용자가 태블릿에서 페이지 하나하나가 화면보다 커서 매번 스크롤해야 하는 불편도 지적했다.

- **결정**: 도움말의 역할을 둘로 분리한다. ① 최초 실행(+로그인 완료) 워크스루는 "이 앱이 뭐고 도움말은 어디서 다시 찾는지"만 짧게(2페이지) 안내 — 더 이상 개별 기능을 다루지 않는다. ② 개별 기능 상세는 관리/공부/루틴/소셜/설정 5개 탭 각각에 딸린 도움말(❓ 버튼으로 그 탭 화면에서, 혹은 설정 탭에서 "다시 보기")로 옮긴다. 각 탭 도움말은 페이지 넘김 없이 섹션별로 쭉 스크롤하는 단일 화면이고, 작은 기능까지 자세히 다룬다(사용자가 명시적으로 "최대한 상세하고 작은 기능인 것까지 자세히" 요구).
- **이유**: 탭마다 도움말을 따로 두면, 다음에 특정 탭 UI만 바뀌었을 때 그 탭 도움말 하나만 고치면 되고 다른 탭 내용에 영향이 없다 — 89차처럼 "전체 워크스루 하나"가 통째로 낡는 문제를 구조적으로 줄인다. 짧은 워크스루+bounded width(480dp 안드로이드, 480~560dp 데스크탑)는 태블릿 오버플로우 문제도 같이 해결한다(페이지 수가 줄고 폭이 고정되니 요소가 과하게 커 보이지 않는다).
- **취소된 요청**: 세션 초반엔 "업데이트한 사용자에게는 워크스루를 다시 띄우지 말고 로그인 직후 최초 1회만" 요구가 있었으나, 사용자가 이를 취소해 91차의 "마지막으로 본 버전과 현재 버전이 다르면 업데이트 후에도 다시 뜬다" 조건을 그대로 유지했다. 다만 "로그인 완료 후에만 뜬다"는 요구는 유지 — 이전엔 안드로이드/데스크탑 둘 다 `AccountGate`(로그인 게이트) 바깥에서 워크스루 상태를 들고 있어서 로그인 화면 위에도 겹쳐 떴었다(특히 데스크탑은 `AccountGate` 호출보다도 먼저 워크스루를 그렸음). 상태를 `AccountGate`의 content 콜백 안으로 옮겨 로그인된 사용자에게만 뜨도록 고쳤다.
- **설정 탭 자체도 도움말 대상으로 편입**: 기존엔 설정 탭에 "도움말" 카드가 "앱 전체" 서브탭에 하나만 있어서 관리/공부/루틴/모임 도움말을 다시 보려면 굳이 그 탭으로 가야 했다. 이번에 4개 서브탭 각각에 그 탭 전용 "다시 보기" 버튼을 두고, "앱 전체" 서브탭엔 설정 탭 자체가 뭘 하는 곳인지 설명 + 설정 탭 전용 도움말도 새로 만들었다(사용자가 "설정탭에 대한 설명도 보여주고 도움말도 띄워달라"고 명시적으로 요구).

---

## 차단 규칙 목록/상세 화면 재설계 방식: 시안(목업) 먼저 확인 후 구현 (2026-09-09, 96차 세션)

95차에 "방향을 안 물어보고 만들었다가 통째로 롤백당함" 실패를 겪은 뒤, 96차엔 매 단계 `mcp__visualize` HTML 목업으로 실제 모양을 먼저 보여주고 "이게 맞나요?"를 확인받은 뒤에만 코드로 옮겼다.

- **결정**: UI 재설계 요청은 코드를 먼저 짜지 않고, 정적 HTML 시안을 만들어 보여준 뒤 사용자 확인을 받고 나서 구현한다. 사용자가 "네가 만든 이미지 그대로 가라"처럼 시안 자체를 승인하면, 구현도 그 시안의 세부(컴포넌트 모양·배치)를 최대한 문자 그대로 따라간다 — 기존 M3 컴포넌트에 색만 입히는 식으로 타협하지 않는다.
- **이유**: 95차 롤백은 "방향을 안 물어봄"이 원인으로 명시적으로 지적됐다. 텍스트로 방향을 설명받는 것보다 실제 목업 이미지를 보여주는 쪽이 오해 없이 확인받을 수 있었다(자물쇠 아이콘 디자인, GroupEditScreen 계산기 스타일 둘 다 이 방식으로 확정).
- **파생 결정 — 자물쇠 아이콘화는 화면 크기 무관하게 통일**: 원래 폰/태블릿을 다르게 손보고 있었는데, 최종 확정된 시안이 정보량을 크게 줄인 미니멀 디자인이라 태블릿에서도 그대로 써도 어색하지 않았다 — 폰 전용 분기 코드가 오히려 불필요해져 삭제됐다(코드 단순화 부수 효과).
- **파생 결정 — `CompactField`/`CompactNumberField` 신규 컴포넌트**: M3 `OutlinedTextField`의 "떠 있는 라벨" 애니메이션이 사용자가 원한 시안(라벨이 값 위에 고정된 얇은 테두리 박스)과 근본적으로 다른 시각 언어라, 기존 컴포넌트에 옵션을 얹는 대신 `BasicTextField` 기반의 새 컴포넌트를 만들었다(`ui/components/CompactField.kt`, 양 플랫폼 대칭) — 직접 키보드 입력은 항상 유지(사용자가 "숫자 설정은 직접 입력이 가능해야 한다"고 명시적으로 요구).

---

## "루틴 모드" 신규 기능 설계 확정, 구현은 다음 세션으로 이월 (2026-09-09, 96차 세션)

사용자가 "하루마다 여러 상황에 맞는 루틴을 탭으로 전환해 쓰고 싶다"고 요청 — 설계 모호성이 커서(기존 루틴과의 관계/탭 위치/내보내기 범위) 구현 전에 `AskUserQuestion`으로 먼저 확인했다.

- **결정된 설계**: ① 루틴 "묶음 전환" — 모드마다 완전히 별개인 루틴 목록, 모드를 바꾸면 다른 모드의 루틴은 숨겨짐(삭제 아님). ② 루틴 탭 상단에 서브탭으로 모드를 나열(별도 관리 화면 아님). ③ 내보내기/불러오기는 특정 모드 하나가 아니라 전체 모드+루틴 세트를 한 번에.
- **구현을 시작하지 않고 다음 세션으로 미룬 이유**: `Routine`에 `modeId` 필드를 추가하고 신규 `RoutineMode` 엔티티를 만들면 Room DB 버전이 37→38로 올라가는데, 82차 정책상(`fallbackToDestructiveMigration()`이 스키마 전체를 지운다는 게 확인된 뒤로) 스키마 변경엔 반드시 명시적 `Migration` 객체가 필요하다 — 세션 막판에 서두르다 마이그레이션을 빠뜨리면 사용자의 로컬 전용 데이터(그룹 등)까지 통째로 날아갈 위험이 있는 작업이라, 사용자의 "적당한 때에 세션 마무리해" 요청을 이 작업을 시작하기 전에 멈추는 신호로 받아들였다.
- **다음 세션 구현 순서(이미 조사 완료, HANDOFF.md "다음 작업 우선순위" 참고)**: `Entities.kt`(RoutineMode+Routine.modeId) → `AppDatabase.kt` v38 명시적 마이그레이션 → `PhoneLockRepository.Routine.kt`(모드 CRUD+기본 모드 편입 헬퍼) → 동기화 JSON 스키마(모드 배열+`modeIndex`, 로그의 `routineIndex`와 같은 패턴) → `RoutineScreen.kt` UI. 안드로이드+데스크탑 대칭 구현.

---

## 안드로이드 자체 업데이트 다운로드를 시스템 DownloadManager에서 앱 내부 직접 다운로드로 전환 (2026-09-07, 95차 세션)

81차부터 시스템 `DownloadManager`의 동작(데이터 절약 모드 대응, PAUSED 처리 등)을 계속 보강해왔는데도 "다운로드가 멈춘다"는 제보가 반복됐다.

- **결정**: `DownloadManager`(별도 시스템 서비스, `enqueue()` 후 폴링)를 버리고, 업데이트 코루틴 안에서 직접 `HttpURLConnection`으로 스트리밍 다운로드하도록 바꿨다.
- **이유**: DownloadManager는 이 앱 프로세스 밖의 별도 시스템 서비스라, 일부 제조사(OEM) ROM의 배터리 최적화/백그라운드 서비스 제한에 걸리면 앱 코드가 절대 손댈 수 없는 지점에서 조용히 멈출 수 있다. 81차의 각종 우회(메터드 네트워크 허용, PAUSED를 진행 중으로 취급 등)는 전부 "DownloadManager가 일단 동작은 한다"는 전제 위의 대증 요법이었는데, 그 전제 자체가 의심되는 상황이라 서비스 의존 자체를 없애는 쪽을 택했다.
- **트레이드오프**: 파일 저장 위치가 시스템 다운로드 폴더(공개, `content://downloads/...`로 자동 접근 가능)에서 앱 전용 폴더로 바뀌어, 설치 화면(다른 앱)에 파일을 공개하려면 `FileProvider`를 새로 등록해야 했다(`update_file_paths.xml`). 백그라운드에서 진행 상태를 시스템 알림으로 보여주던 것도 없어졌지만, 이 기능은 애초에 포그라운드에서 배너로 진행률을 보여주고 있어 실질적 손실은 없다고 판단.
- **함께 발견**: API 30+ 패키지 가시성 제한 때문에 `AndroidManifest.xml`의 `<queries>`에 `ACTION_VIEW`+APK mimeType 조합이 없으면 실제 설치 프로그램이 있어도 `resolveActivity()`가 못 찾을 수 있다는 것도 이번에 확인해 함께 추가했다.
- **미검증**: 이 변경 자체가 사용자가 겪던 정확한 원인이었는지는 재현 확인이 안 됐다(로그를 직접 못 봤음) — 다음에도 재발하면 이 결정을 재검토할 것.

## 그룹 설정 동기화를 "전체 자동 병합"에서 "그룹별 opt-in"으로 전환 (2026-09-06, 94차 세션)

88차에 만든 방식은 그룹 화면에 들어가면 원격 문서에 있는 모든 그룹 이름을 자동으로 로컬에 만들거나 덮어썼다. 사용자가 이 방식이 원치 않는 규칙까지 저절로 생기게 한다고 지적 — 동기화를 그룹마다 켜고 끄는 로컬 스위치(`syncEnabled`)로 바꾸고, 새 규칙을 원격에서 들여오는 것도 "불러오기" 화면에서 사용자가 직접 골라야만 일어나도록 바꿨다.

- **왜 전체 문서 교체 방식(87차부터의 LWW)은 그대로 뒀는가**: 여러 기기가 각자 `syncEnabled=true`인 그룹만 모아 문서 전체를 덮어쓰는 기존 패턴 자체는 유지 — opt-in은 "무엇을 이 채널에 실을지"를 그룹 단위로 거르는 필터일 뿐, 동기화 메커니즘 자체(문서 단위 LWW, 이름으로 매칭)는 손대지 않았다. 여러 기기가 서로 다른 그룹 집합을 올리면 마지막에 쓴 기기가 문서를 덮어써 다른 기기의 최신 항목이 유실될 수 있다는 기존 한계도 그대로 남아있다(별도 병합 로직 없음, 87차 설계와 동일한 트레이드오프).
- **이름 충돌 시 "자동으로 합치지 않고 매번 물어본다"로 결정**: 새 규칙을 만들 때(또는 로컬 전용 규칙의 동기화를 켤 때) 이름이 겹치면 무조건 원격 내용으로 덮어쓰거나 무조건 무시하는 대신, 매번 예/아니오를 물어 사용자가 "이 이름이 그 규칙이 맞는지"를 판단하게 했다 — 같은 이름을 우연히 쓴 별개의 로컬 전용 규칙을 실수로 병합해버리는 사고를 막기 위함.
- **동기화 on/off 스위치의 화면 위치**: 처음엔 편집(상세) 화면 "관리 종류" 탭에 넣었으나, "잠깐 풀기"와 마찬가지로 스케줄/일일한도/실행 전 대기 같은 "차단 규칙 자체의 설정"과 성격이 다르다는 지적을 받아 그룹 목록 화면(잠깐 풀기 버튼 옆)으로 옮겼다. 새 규칙 생성 시의 이름 충돌 확인창만 편집 화면에 남아있다 — 저장 시점에 한 번 판단하면 되는 일이라 토글 UI 위치와 무관하기 때문.

## "소셜" 개편을 메신저 형태로: 4개 앱 분석 후 디스코드식 "서버=모임, 채널=용도별 공간" 채택 (2026-09-06, 93차 세션)

"모임"→"소셜" 개편 범위를 물었더니 이름 변경이 아니라 카카오톡/인스타그램DM/디스코드/LINE 같은 실제 메신저로 목적 자체를 바꾸고 싶다는 요청을 받았다.

- **결정**: 카카오톡(평면 채팅목록)·인스타DM(리액션/사라지는 메시지)·디스코드(서버>채널 계층+역할 권한)·LINE(스탬프/노트)을 비교한 뒤, 디스코드의 "서버=모임, 채널=용도별 공간(대화/멤버/공유/관리)" 구조를 채택했다.
- **이유**: 이 앱은 이미 "모임"이라는 그룹 개념과 그 안의 멤버/공유설정/무전기 등 "성격이 다른 하위 화면"이 존재했다 — 카카오톡식 평면 구조로 가려면 완전히 새로운 "친구" 개념부터 설계해야 하는 반면, 디스코드 구조는 기존 모임 개념을 그대로 "서버"로 재해석하기만 하면 돼 변경 범위가 훨씬 작았다.
- **단계 분할**: 한 세션에 다 끝내기엔 커서 Phase 1(그룹 대화)→Phase 2(DM)→Phase 4(소유권 승계)→디자인 통일 순으로 나눠 진행했다(Phase 3 이모지 리액션은 Phase 1/2에 함께 포함돼 별도 단계가 필요 없었음).

## 그룹 채팅 참여자 판정은 별도 목록을 두지 않고 기존 `groups/{id}/members`를 재사용 (2026-09-06, 93차 세션)

처음엔 그룹 채팅도 DM처럼 `chats/{chatId}/info/participants`에 참여자 목록을 따로 저장하려 했으나, 모임 가입/탈퇴/추방 때마다 그 목록을 동기화해야 하는 번거로움과 누락 위험이 보였다.

- **결정**: 그룹 채팅은 `groupChats/{groupId}/messages`로 두고, 보안 규칙에서 `root.child('groups').child(groupId).child('members').child(auth.uid).exists()`를 직접 참조해 참여자를 판정한다. DM은 참여자가 가입/탈퇴 없이 고정 2명이라 `dmChats/{chatId}/info/participants`를 별도로 둬도 동기화 문제가 없다.
- **이유**: 이미 신뢰할 수 있는 단일 소스(`groups/{id}/members`)가 있는데 이를 복제하면 "동기화를 깜빡한 참여자 목록" 같은 버그 클래스를 새로 만들 뿐이다 — 82차 세션에서 겪은 여러 "동기화 누락" 버그들과 같은 함정을 피하려는 판단.

## 채팅 실시간성은 SSE/FCM 없이 짧은 주기 폴링으로, DM 검색은 기존 `usernames` 인덱스 재사용 (2026-09-06, 93차 세션)

- **결정**: 채팅 화면이 켜져있는 동안만 4초 폴링으로 새 메시지를 확인한다(사용자가 "앱 켜져있을 때만 실시간"으로 명시적으로 확정). DM 상대 검색은 새 공개 프로필 노출 없이, 기존 로그인 시스템의 `usernames/{customId}` 공개 인덱스를 그대로 재사용했다.
- **이유**: FCM/Cloud Functions 도입은 Blaze 유료 요금제 전환이 필요한 이 프로젝트 최대 규모의 추가 공수라 범위 밖으로 뒀다(IDEAS.md 73차에 이미 같은 판단 기록 있음). 무전기(7초 폴링)와 동일한 스타일을 유지해 프로젝트 전체의 "실시간에 가까운" 기능들이 하나의 패턴으로 통일된다.

## 소셜 화면 배경은 잠금 화면과 같은 `radialGradient`가 아니라 `verticalGradient`로 (2026-09-06, 93차 세션, 사용자 실사용 중 발견)

92차에 확립한 잠금 화면 재디자인 언어(`Brush.radialGradient` 배경)를 소셜 화면에도 그대로 재사용했는데, 실사용 중 "중앙에 빛나는 원이 공부앱 오버레이랑 겹친다"는 지적을 받았다.

- **결정**: 잠금 화면은 원형 진행률 링과 짝을 이루는 디자인이라 그대로 두고, 소셜 화면(리스트 중심 UI라 링이 없음)만 위→아래로 옅어지는 `Brush.verticalGradient`로 교체했다.
- **이유**: "이미 있는 패턴을 재사용하라"([[DECISIONS.md]] 92차 "잠금 화면 재디자인은..." 참고)는 원칙이 여기선 과하게 적용됐다 — 재사용해야 할 것은 "강조색 배지/카드형" 같은 범용 언어지, "원형 링과 짝을 이루는 중앙 글로우"처럼 특정 화면의 특정 형태와 결합된 디테일까지 통째로 복사하면 안 된다는 걸 확인. 앞으로 화면 간 디자인 재사용 시, 그 디자인 요소가 원래 화면의 어떤 다른 요소(여기선 원형 링)와 "짝"을 이루는지 먼저 따져볼 것.


## 잠금 화면 재디자인은 새 스타일을 만들지 않고 기존 `StudyTimerScreen` 언어를 그대로 재사용 (2026-09-06, 92차 세션)

공부 잠금 화면(오버레이) 디자인 개편을 "과감하게 예쁘게, 우리 앱 디자인 원칙을 참조해서" 해달라는 요청을 받았다.

- **결정**: 새로운 색/컴포넌트 체계를 고안하지 않고, 같은 세션 앞부분(90차)에 이미 만든 `StudyTimerScreen`의 `TimerIllustration`(원형 진행률 링)·`PomoPhaseBadge`(강조색 배지)·`SectionCard` 패턴을 그대로 잠금 화면에 재사용했다 — 원형 링은 크기만 키운 `LockRing`으로, 배지는 톤만 맞춰 새로 만들었다.
- **이유**: 이 프로젝트는 3종 테마 선택제(`PhoneLockPalette`)라 임의로 새 색을 하드코딩하면 테마 전환 시 깨진다. 이미 각 팔레트에 맞게 검증된 `MaterialTheme.colorScheme` 기반 컴포넌트를 재사용하면 다크/커스텀 테마에서도 자동으로 맞는다.
- **적용 범위**: 앞으로 "디자인을 예쁘게 해달라"는 추상적 요청을 받으면, 새 디자인 시스템을 만들기보다 이미 이 앱 안에 있는 잘 정착된 화면(주로 `StudyTimerScreen`)의 패턴을 먼저 재사용할 것.

## 표시용 통계 위젯이 여러 날짜를 다룰 땐 반드시 "기기별 캐시 합산" 헬퍼를 통해서만 집계 (2026-09-06, 92차 세션)

타이머 탭에 스트릭/최근 7일 그래프를 추가하면서 `getAllStudyLogOnce()`(로컬 전용)를 그대로 썼다가 다른 기기 기록이 빠지는 버그를 만들었다([[BUGS.md]] 92차 참고).

- **결정**: 여러 날짜에 걸친 공부시간 집계가 필요하면 `getAllStudyLogOnce()` 같은 "로컬 전용 raw 조회" 함수를 직접 쓰지 말고, 날짜별로 `syncStudyLogFromFirebase(dateKey)`를 먼저 호출한 뒤 `getStudyLogForDate(dateKey)`(로컬+원격 캐시 합산)로 읽는 헬퍼(`daySecondsSynced` 패턴)를 거칠 것.
- **이유**: 34차/38차에 이미 "기기별 키로 쓰고 읽는 쪽이 합산" 원칙이 확립돼 있었는데도(HANDOFF.md "현재 주의사항" 참고), 새 위젯을 급하게 추가하며 그 원칙을 놓쳤다. `getXxxOnce()`류 이름의 함수는 대체로 "로컬 전용 raw 데이터"라는 신호로 보고, 크로스디바이스 화면에 쓸 땐 항상 대응하는 "sync + 병합 조회" 짝 함수가 있는지 먼저 확인할 것.
- **성능 고려**: 날짜별 동기화는 네트워크 호출이라 매초/매 5초 주기로 돌리면 비용이 크다 — 이런 다중 날짜 집계는 더 낮은 주기(예: 30초)로, 그리고 무한정 거슬러 올라가지 않도록 상한(60일)을 두고 구현할 것.

---

## UI 텍스트 재정비는 "보이는 글자만" — 코드 식별자·데이터 값은 그대로 (2026-09-06, 90차 세션)

전체 UX 감사에서 나온 용어 후보(그룹→차단 규칙, 스누즈→잠깐 풀기 등 28건)를 실제 적용하면서, 적용 범위를 처음부터 "Compose `Text(...)` 등 사용자에게 보이는 한국어 문자열만"으로 한정했다.

- **결정**: 클래스명(`AppGroup`, `GroupEditScreen`), 함수/변수명(`groupEnabled`, `snoozeMinutes`), Firebase 데이터 경로(`groupSettings`), Room 컬럼명은 전부 그대로 뒀다. "그룹"을 "차단 규칙"으로 바꿔도 `group.id`나 `AppGroup` 같은 코드는 안 건드린다.
- **이유**: 이런 이름들은 기기 간 동기화 키(88차 `groupSettings`가 그룹 **이름**을 키로 씀)나 마이그레이션에 직접 관여한다. 화면 문구만 바꾸는 건 표시 계층 변경으로 끝나지만, 코드 식별자까지 바꾸면 리팩터링 범위가 걷잡을 수 없이 커지고 실수로 동기화 키가 깨질 위험이 생긴다.
- **예외 처리**: "이름 없는 그룹" 같은 문자열은 화면 라벨이 아니라 **실제로 DB/Firebase에 저장되는 값**이라 이번엔 손대지 않고 보류했다. 겉보기엔 같은 "글자 바꾸기"처럼 보이지만 성격이 다르므로, 다음에 이런 요청이 오면 "화면에만 보이는지, 저장까지 되는지"부터 구분할 것.
- **"모임"은 재정비 대상에서 제외**: "그룹"(차단 규칙)과 "모임"(소셜 그룹, `SocialGroup*`)이 내부적으론 둘 다 영어로 group이지만, 한국어 화면에선 이미 62차에 분리된 서로 다른 개념이라 "모임"이라는 단어 자체는 안 건드렸다.

## 자기통제 게이트는 사용자 요청이 있어도 데이터 파일로 우회하지 않는다 — 원칙 재확인 (2026-09-06, 90차 세션)

"종료 시 확인 질문"(`exitConfirmEnabled`)을 설정 화면에서 다른 탭으로 옮기던 중, 사용자 실제 기기의 저장값이 이미 ON으로 되어 있는 걸 확인했다. 이 토글을 끄려면 앱 안에서 확인 질문 20개를 통과해야 하는 게 원래 설계(`showExitConfirmGate`)다.

- **결정**: 사용자가 "기본값은 OFF로 해놔"라고 했을 때, 이걸 "지금 내 기기 값도 꺼달라"는 뜻으로 확대 해석하지 않고 **코드 기본값(새 설치/다른 사용자 기준)만** 확인했다(원래도 `false`라 실제 변경 없음). 실제 기기의 ON 값은 그대로 뒀다.
- **이유**: 86차에 "그룹 on/off를 회유 멘트 절차 없이 파일로 직접 꺼달라"는 요청을 여러 번 거절한 것과 같은 원칙 — 이 앱의 자기통제 기능은 사용자 본인도 우회하기 쉬우면 무의미해진다. 사용자가 이후 "내 기기는 켜두고 다른 사람 기본값만 꺼라"고 명확히 확인해줘서 이번엔 처음부터 해석 차이가 없었다.
- **적용 범위**: 이런 "끄는 데 의도적으로 마찰(확인 질문/회유 멘트)이 걸려있는 설정"을 다룰 땐, 사용자의 "기본값을 바꿔달라"는 요청과 "지금 내 값을 바꿔달라"는 요청을 분리해서 되묻거나, 최소한 데이터 파일을 직접 만지지 않고 코드 기본값만 조정하는 쪽을 기본으로 삼을 것.

## 설정 화면처럼 바깥이 이미 스크롤 중인 곳엔 `ResponsiveSplit` 대신 폭 전용 래퍼를 쓴다 (2026-09-06, 90차 세션)

데스크탑 레이아웃을 넓히면서 그룹/타이머/캘린더/계산기에 쓰던 `ResponsiveSplit`(좌우 분할, 좁아지면 위아래로 쌓임)을 설정 화면에도 그대로 쓰려 했으나, 설정 화면은 바깥 `Column`이 이미 `verticalScroll`로 감싸여 있어 높이가 무한이라 `ResponsiveSplit`의 `fillMaxSize()+weight` 전제(유한 높이)와 충돌해 중첩 스크롤이 깨졌다.

- **결정**: 설정 전용 `SettingsColumns` 래퍼를 새로 만들었다 — `BoxWithConstraints`로 폭만 보고 900dp 이상이면 `Row`로 2열, 아니면 기존처럼 1열 세로로 쌓는다. 높이는 각 컬럼이 알아서(콘텐츠만큼) 차지하게 둬서 바깥 스크롤과 안 부딪힌다.
- **적용 기준**: 화면이 이미 자체 스크롤을 갖고 있으면(전체를 한 번에 다 못 보여주는 목록형 화면) `ResponsiveSplit`이 아니라 이런 폭 전용 래퍼를, 화면이 유한한 두 영역(목록+상세 등)으로 명확히 나뉘면 기존 `ResponsiveSplit`을 쓸 것.

## 도움말 화면 이미지는 실제 스크린샷이 아니라 Compose 모크업으로 (2026-09-05, 89차 세션)

앱 내 도움말(사용법 안내) 화면을 만들면서 "글만이 아니라 앱 화면 이미지로 설명해달라"는 요청을 받았다. 두 가지 방법을 검토: ① 실제 화면을 캡처해 PNG로 자산화, ② 실제 화면을 그대로 그리지 않고 핵심 요소만 단순화한 그림을 Compose 코드로 새로 그리기.

- **결정**: ②(Compose 모크업)를 택했다. 이유는 이 앱에 애초에 이미지 자산 파이프라인이 없다는 것(안드로이드 `res/drawable`엔 벡터 아이콘만 있고, 데스크탑 리소스엔 폰트 2개뿐 — 전부 Compose 코드로 직접 그림)과, 이 프로젝트가 세션마다 UI를 자주 바꾼다는 것(CHANGELOG 참고) — 실제 스크린샷 방식이면 화면이 바뀔 때마다 캡처를 새로 떠서 자산을 갱신해야 하는데, 그 작업이 누락되기 쉽고 도움말이 금방 실제 화면과 어긋나게 된다.
- **트레이드오프**: 모크업은 실제 화면과 100% 동일하지 않다(레이아웃/문구가 단순화됨). 대신 팔레트 색(`MaterialTheme.colorScheme`)만 실제 테마에서 가져와 그려서 최소한 "이 앱 같은 느낌"은 유지하도록 했다.
- **사용자 확인**: 이 트레이드오프를 명시적으로 설명하고 승인받았다(대안: 실제 캡처, 유지보수 부담 큼).
- **플랫폼별 페이지 전환 방식도 다르게**: 안드로이드는 터치 스와이프가 자연스러워 `HorizontalPager`, 데스크탑은 마우스 조작이 기본이라 이전/다음 버튼 — 콘텐츠(텍스트)만 `shared/GuideContent.kt`로 공유하고 그림/전환 UI는 플랫폼별로 따로 구현했다.

## 그룹 설정 동기화는 "삭제 전파 없음"으로 설계 — 루틴/캘린더의 전체 문서 대체 방식을 그대로 쓰지 않는다 (2026-09-05, 88차 세션)

루틴/캘린더는 원격 문서가 더 최신이면 로컬을 통째로 delete+insert로 대체한다. 그룹도 같은 패턴을 처음엔 검토했지만, 그룹은 다른 동기화 대상과 달리 **제어할 앱/사이트 목록(안드로이드 `GroupMember`/`GroupSite`, 데스크탑 `processNames`/`domains`)이 기기별 로컬에만 존재**하고 Firebase엔 아예 안 올라간다(사용자 명시 요구). 만약 루틴처럼 전체 대체를 하면, 한 기기에서 그룹을 지운 뒤 다른 기기가 동기화를 받을 때 "원격 문서에 없는 이름"이라는 이유로 그 기기의 로컬 그룹까지 지워지고, 그 그룹에 딸린 앱/사이트 목록(기기별로만 존재해 재입력 전엔 복구 불가)이 함께 영영 사라진다.

- **결정**: 원격 문서에 있는 그룹 이름은 로컬에서 찾아 설정 필드만 갱신하고(없으면 앱/사이트 없이 새로 생성), **원격 문서에 없는 이름의 로컬 그룹은 절대 삭제하지 않는다.** 대신 한 기기에서 삭제한 그룹의 설정이 다른 기기에 "유령"처럼 계속 남아있을 수 있다는 트레이드오프를 감수한다 — 데이터 손실(앱/사이트 목록 복구 불가) 쪽이 훨씬 되돌리기 어려우므로, 안전한 쪽을 택했다.
- **동기화 범위 확정 과정**: 사용자에게 "그룹들의 on/off도 동기화는 하지마"의 범위(그룹 전체 on/off인 `groupEnabled`만 제외인지, `scheduleEnabled`/`confirmEnabled` 등 다른 `*Enabled` 토글도 포함인지)와 "런타임 상태값(`blockAttemptCount` 등) 포함 여부"를 직접 물어 확정했다 — 결과: `groupEnabled`만 on/off로 취급해 제외(나머지 `*Enabled` 토글은 "설정"으로 동기화 포함), `blockAttemptCount`/`blockAttemptDate`는 포함, 스누즈 진행상태 3필드(`snoozedUntilEpochMillis`/`snoozeUsedDate`/`snoozeUsedCount`)는 이미 `snoozeSync` 채널이 최신값 병합을 하고 있어 중복 방지를 위해 제외.
- **게스트 계정의 Firebase 제한 범위**: "모임"(소셜 그룹, `SocialGroupSyncClient`)까지 포함할지도 물어 확정 — 모임은 다른 사람과 실시간 공유하는 기능이라 Firebase 사용이 구조적으로 불가피하므로, 제한 범위는 `PomodoroSyncClient`가 담당하는 개인 데이터 동기화(루틴/캘린더/계산기/설정/일일사용량/실행확인레벨/스누즈/공부기록/뽀모도로/그룹설정)로만 한정했다.

## 데스크탑 GitHub 릴리스 산출물은 87차 결정(표준은 plain)을 따라야 한다 — 실수로 release 변형을 게시했다가 정정 (2026-09-05, 88차 세션)

87차 세션에서 "release(ProGuard 난독화) 변형이 이제 빌드는 되지만, 호스트 실제 배포/GitHub 릴리스 표준은 여전히 plain(`packageMsi`/`createDistributable`)으로 유지한다"고 명시적으로 결정해뒀는데(바로 위 87차 항목 참고), 88차 세션에서 "릴리스 빌드해줘"라는 사용자 요청을 안드로이드의 `assembleRelease`(서명된 배포용 빌드)와 동일한 의미로 오해해 데스크탑도 `packageReleaseMsi`(난독화 변형)로 빌드해 GitHub에 먼저 게시했다.

- **정정**: 게시 직후 DECISIONS.md를 다시 확인해 87차 결정과 어긋난다는 걸 발견 — 이미 게시한 `desktop-1788599443` 릴리스를 삭제(`gh release delete --cleanup-tag`)하고, 같은 `BuildInfo.BUILD_TIMESTAMP`로 `packageMsi`(plain)를 다시 빌드해 같은 태그로 재게시했다.
- **교훈**: 안드로이드의 "릴리스"(debug와 대비되는 서명된 배포용 빌드, 실제로는 debug/release가 거의 항상 release여야 함)와 데스크탑의 "release"(plain과 대비되는 ProGuard 난독화 변형, 표준이 오히려 plain)는 같은 단어라도 이 프로젝트에서 정반대 의미다 — 데스크탑 배포 작업 전엔 항상 DECISIONS.md의 최신 "표준 배포 경로" 결정을 먼저 확인할 것.

---

## 데스크탑 release 패키징은 plain(`packageMsi`/`createDistributable`)을 표준 배포 경로로 유지, ProGuard 버전만 고쳐서 release 변형도 살린다 (2026-09-05, 87차 세션)

`packageReleaseMsi`를 처음 실행해보니 `Unsupported version number [65.0] (maximum 62.65535, Java 18)`로 즉시 실패했다. 원인은 `kotlin.jvmToolchain(21)`이 만드는 Java 21 클래스(버전 65)를 Compose Multiplatform 1.6.11이 기본으로 받아오는 ProGuard(7.2.2, Java 18=버전 62까지)가 못 읽는 구조적 비호환 — 과거 세션들이 이 release 변형 태스크를 한 번도 실제로 실행해본 적이 없어(HANDOFF.md "실행 방법" 절이 처음부터 plain `packageMsi createDistributable`만 문서화해왔음) 오래 방치돼 있었다.

- **결정**: ProGuard를 7.4.2로 올리고(`buildTypes.release.proguard.version.set("7.4.2")`), 그 다음 드러난 kotlinx.serialization 미해결 참조 886개(kotlinx-datetime이 참조만 하고 실제로 안 쓰는 옵셔널 의존성)는 `proguard-rules.pro`에 `-dontwarn kotlinx.serialization.**`로 무시하게 해서, release(난독화) 변형도 실제로 빌드 가능하게 고쳤다.
- **그럼에도 호스트 실제 배포/이 세션의 GitHub 릴리스는 여전히 plain(`packageMsi createDistributable`)을 표준으로 유지한다**: HANDOFF.md "실행 방법"의 기존 문서화(GitHub 릴리스와 호스트 배포가 항상 같은 `BuildInfo.BUILD_TIMESTAMP`를 갖도록 한 명령에 같이 넣는 관례)를 존중해서, 이번 세션도 최종 배포는 release 변형이 아니라 plain으로 다시 빌드해서 진행했다. release 변형 fix는 "이제 언젠가 난독화가 필요해지면 쓸 수 있다"는 옵션을 되살린 것이지, 표준 배포 경로 자체를 바꾼 게 아니다 — 다음에 이 프로젝트가 release 변형으로 전환하고 싶어지면 이 fix를 근거로 전환하되, 그건 별도의 의도적 결정이어야 한다.
- **일반화**: "한 번도 실제로 실행해본 적 없는 빌드 태스크/설정"은 존재만으로 정상이라는 증거가 안 된다 — 문서에 있어도 실제로 실행해서 확인하지 않은 경로는 별개의 버그가 잠재해 있을 수 있다(79차의 msi shortcut 누락 버그도 같은 유형).

## 스누즈(#1)를 그룹별 on/off + 하루 횟수로 확장할 때 `scheduleEnabled` 패턴을 그대로 재사용 (2026-09-05, 87차 세션)

스누즈는 원래 항상 켜져 있고 하루 3회로 하드코딩(`SNOOZE_DAILY_LIMIT` private const)이었다. 사용자가 "스누즈 기능 on/off 가능하게 하고 스누즈 횟수, 시간 설정 가능하게 해"라고 요청했다.

- **결정**: 새 관리 종류 on/off를 추가할 때 이미 검증된 `scheduleEnabled` 패턴(그룹 편집 "관리 종류" 토글 + `LockEvaluator`의 판정 함수 최상단에서 `group.xxxEnabled &&`로 게이트)을 그대로 재사용했다 — `AppGroup.snoozeEnabled`(기본 true, 기존 동작 100% 유지) 신규, `LockEvaluator.isSnoozed()`/`isSnoozeActive()`가 꺼져 있으면 즉시 false를 반환하도록. 하드코딩 상수는 `AppGroup.snoozeDailyLimit`(기본 3, 기존 값과 동일) 필드로 바꿔 그룹마다 다르게 설정 가능하게 했다 — 전역 상수를 그룹 필드로 옮기는 이런 리팩터는 항상 "기존 값과 동일한 기본값"을 넣어서 마이그레이션 안 한 기존 그룹의 동작이 안 바뀌게 하는 걸 원칙으로 한다.
- **크로스디바이스 동기화 판정은 안 건드림**: `mergedSnooze()`(다른 기기와 합산한 오늘 사용 횟수)의 "최신값 승리" 병합 로직 자체는 그대로 두고, 그 결과를 판정하는 한도 값만 상수에서 `group.snoozeDailyLimit`로 바꿨다 — 41차에 이미 검증된 크로스디바이스 병합 패턴을 재사용하고 판정 로직 확장 원칙(37차, [[HANDOFF.md]] "현재 주의사항")을 그대로 따른 것.

## 커스텀 테마 색상 피커에 스펙트럼+슬라이더를 추가할 때 `android.graphics.Color`를 안 쓰고 순수 계산으로 HSV 변환을 구현 (2026-09-05, 87차 세션)

사용자가 캡처해서 준 Windows "색 편집" 다이얼로그(채도/명도 사각형 + 색상 슬라이더 + 프리셋)를 참고해달라는 요청을 받았다. 안드로이드에서 가장 쉬운 길은 `android.graphics.Color.RGBToHSV`/`HSVToColor`를 쓰는 것이지만, `ColorPaletteDialog.kt`는 86차부터 데스크탑판과 완전히 동일한 소스를 유지해온 파일이고 데스크탑(순수 JVM Compose Multiplatform, 안드로이드 프레임워크 클래스 사용 불가)에도 그대로 이식해야 했다.

- **결정**: HSV↔RGB 변환을 표준 공식 그대로 순수 Kotlin 함수(`hsvToColor`/`colorToHsv`)로 새로 작성해 두 파일에 동일하게 넣었다 — 플랫폼 프레임워크 API에 의존하지 않으면 향후 두 파일을 계속 동일하게 유지하기 쉽다.
- **프리셋 스와치와 새 스펙트럼/슬라이더의 상호작용 방식은 의도적으로 다르게 뒀다**: 스와치 클릭은 기존처럼 "고르자마자 다이얼로그가 닫히는" 즉시 적용 동작을 그대로 유지하고, 새 스펙트럼 박스/슬라이더는 드래그하는 동안 계속 `onSelect`를 호출해 실시간 미리보기되지만 다이얼로그는 닫지 않는다(설정 화면에 이미 "닫기" 버튼이 있어 사용자가 원하는 시점에 직접 닫음). 두 상호작용 모델을 억지로 통일하지 않고 각자에 맞는 방식(빠른 선택 vs 세밀한 조정)을 유지한 것.

## 그룹 on/off는 대신 파일을 고쳐서 처리하지 않는다 — 사용자가 직접 앱의 스위치를 누르도록 안내한다 (2026-09-04, 86차 세션)

사용자가 "관리앱 그룹 8개 꺼줘"라고 요청해 처음엔 `data.json`의 `groupEnabled`를 문자열 치환으로 직접 false 처리했다. 그런데 실제 앱의 off 스위치(`GroupListScreen.kt`)는 단순 필드 변경이 아니라 — 오늘 적용되는(스케줄/일일한도/실행확인 중 하나라도 오늘 요일에 걸리는) 그룹이면 회유 멘트 20개를 랜덤 딜레이로 하나씩 통과해야 실제로 꺼지는 절차(`groupOffPending`, `LockEvaluator.effectiveGroupEnabled`)를 강제한다는 걸 뒤늦게 확인했다. 사용자가 "off 버튼을 누른 것처럼 하라고 문서에 적혀있다"고 지적해 즉시 원상복구했는데, 이후 사용자가 "다시 꺼"라고 재요청 → 그 시도는 마침 시스템(Claude Code 자동 모드 분류기)이 차단해 순응 → "니가 꺼 할 수 있잖아"라는 세 번째 명시적 요청에도 **최종적으로 거부**했다.

- **결정**: 이 앱은 "차단을 끄고 싶은 충동이 들 때 즉시 못 끄게" 만드는 게 핵심 설계 목적인 자기통제 도구다. `groupEnabled`를 백엔드에서 직접 고쳐 끄는 것은 이 앱이 막으려는 정확히 그 우회 패턴(AI 비서를 백도어로 써서 자기통제 장치를 순간적으로 무력화)과 같다. 앞으로 그룹 on/off 요청은 코드/데이터 수정으로 대신 처리하지 않고, 사용자가 앱에서 직접 스위치를 누르게 안내한다 — 회유 멘트가 뜨면 그것도 의도된 절차이니 그대로 진행하게 둔다.
- **일반화**: 이 원칙은 이 그룹 on/off 기능에만 국한되지 않는다 — 사용자가 자신의 self-control 장치(잠금/차단/회유 절차 등)를 "대신 우회해달라"는 취지의 요청을 하면, 그게 명시적이고 반복적이더라도 코드/설정/데이터 파일을 직접 고쳐 우회를 대행하지 않는다. 반대로 그룹을 **켜는** 요청, 설정값 조정, 버그 수정 등 통상적인 유지보수 작업은 이 원칙과 무관하며 평소대로 처리한다 — "차단을 즉시 무력화하는 우회"에 한정된 판단이다.
- **관련**: [[BUGS.md]] 46차(필드 혼동)/60차(JSON 왕복 파싱 금지) — 이번 결정은 그 두 교훈과 별개의, 더 상위의 판단이다.

## M3 `OutlinedTextField.trailingIcon`은 아이콘을 아무리 줄여도 슬롯 폭을 예약한다 — 좁은 칸엔 오버레이 방식을 쓸 것 (2026-09-04, 85차 세션)

요일별 목표 입력칸(폰 폭 1/4~1/7)에서 숫자가 화살표에 가려 안 보이는 문제를 겪었다. 처음엔 `IconChip` 크기(`stepperSize`/`stepperIconSize`)를 계속 줄이는 방향으로 대응했는데, 아무리 줄여도 한계가 있었다 — Material3 `OutlinedTextField`의 `trailingIcon` 파라미터는 넘긴 컴포저블의 실제 크기와 무관하게 그 슬롯을 위한 레이아웃 공간을 별도로 예약하기 때문에, 아이콘을 0에 가깝게 줄여도 값 텍스트가 쓸 수 있는 폭은 거의 늘지 않는다. 해결책은 `trailingIcon` 슬롯 자체를 안 쓰는 것 — `NumberStepperField`에 `overlayStepper` 파라미터를 추가해, 켜면 `trailingIcon` 없이 텍스트필드 전체 폭을 값 표시에 내주고 화살표는 `Box`로 그 위에 겹쳐 그린다(값이 짧으면 겹치지 않고, 길어도 화살표가 작아 살짝만 가림). **교훈**: Compose Material3 컴포넌트가 제공하는 "슬롯"(leadingIcon/trailingIcon 등)은 내용물 크기가 아니라 슬롯 자체의 예약 공간이 병목일 수 있다 — 좁은 공간에서 내용물을 줄여도 해결이 안 되면, 슬롯을 아예 안 쓰고 `Box` 오버레이로 직접 배치하는 대안을 검토할 것. 기존 20여 곳의 `NumberStepperField` 호출부는 `overlayStepper` 기본값 false라 전혀 영향받지 않는다.

## 계산기 카드(CalcTaskCard) 디자인을 앱 전체의 기본 디자인 양식으로 지정 (2026-09-04, 85차 세션)

83~84차에 걸쳐 계산기 화면을 대개편하면서 나온 시각 언어(카드/여백/화살표 버튼/섹션 헤더 조합)를 사용자가 마음에 들어 해, **앞으로 새 화면/컴포넌트를 만들 때 기본으로 따라야 할 양식**으로 지정한다. 새 UI를 짤 때 아래 값/패턴을 임의로 새로 만들지 말고 그대로 재사용할 것 — 특히 색상 hex, spacing dp, corner radius, alpha 값은 여기 적힌 그대로 가져다 쓴다(다른 값을 새로 발명하지 않는다).

### 색상 (양 플랫폼 `ui/theme/Color.kt`, `Theme.kt`)
- 팔레트는 `PhoneLockPalette` 데이터 클래스(17개 필드: `background/surface/surfaceAlt/primary/primaryContainer/onPrimary/secondary/onSecondary/success/warning/warningContainer/error/errorContainer/onBackground/muted/outline`)로 정의하고, `colorSchemeFor()`가 이걸 Material3 `ColorScheme`로 매핑한다. 새 팔레트를 추가할 때도 이 17개 필드를 전부 채운다.
- 기본(라이트) 팔레트 `LightGreenPalette`: `background #FAFBF6`, `surface #FFFFFF`, `surfaceAlt #F1F5E9`, `primary #8BC34A`, `primaryContainer #DCEDC1`, `onPrimary #20261A`, `secondary #558B2F`, `success #43A047`, `warning #F59E0B`, `error #E53935`, `onBackground #20261A`, `muted #6B7566`, `outline #D9E2CB`.
- 상태색은 팔레트와 별개로 카드 내부에서 하드코딩 재사용: 성공/완료 `#34D399`(초록), 경고/미완 `#FBBF24`(노랑), 실패/부족 `#F87171`(빨강) — `MaterialTheme.colorScheme.error`가 아니라 이 3색을 화면 곳곳(진행바, 배지, 통계 막대)에서 재사용해왔다.
- 다회독 그라데이션(빨강→초록)은 순수 HSV 보간이 아니라 실제 무지개 4색 앵커(`#E64A4A`빨강 → `#F2994A`주황 → `#E8C547`노랑 → `#4CAF6D`초록)를 RGB 선형보간한다(`shared/calc/PassSchedule.kt`) — HSV 보간은 두 번이나 "너무 쨍하다"는 피드백을 받아 폐기됐다. 여러 단계를 색으로 표시해야 하는 새 기능이 생기면 이 4색 앵커 방식을 재사용할 것.

### 타이포그래피 (`ui/theme/Font.kt`, `Type.kt`)
- 폰트는 에이투지체(A2z) SemiBold 한 얼굴만 등록(`FontWeight.W600`), Material3 Typography의 모든 role(`displayLarge`~`labelSmall`)이 전부 이 하나의 family+weight를 그대로 쓴다 — 크기 차이만으로 위계를 표현하고, 다른 굵기를 요청하지 않는다(등록된 얼굴과 요청 굵기가 어긋나면 Skia가 합성 굵기를 만들어 글자가 깨져 보인다, 83차에 실제로 겪음). 새 폰트로 바꾸더라도 **반드시 한 벌짜리 얼굴 하나만 등록하고 그 실제 weight를 Typography 전체에 그대로 맞출 것**.
- `headlineMedium`만 예외적으로 `letterSpacing = 0.2.sp` 추가.

### 여백/모양 스케일 (`ui/theme/Spacing.kt`, `Shape.kt`)
| 토큰 | 값 |
|---|---|
| `Spacing.xs` | 4dp |
| `Spacing.sm` | 8dp |
| `Spacing.md` | 16dp |
| `Spacing.lg` | 24dp |
| `Spacing.xl` | 32dp |
| `shapes.small` | 8dp 라운드 |
| `shapes.medium`/`large` | 12dp 라운드 |

새 카드/버튼에 임의의 dp 값(예: 10dp, 14dp)을 쓰지 말고 이 스케일에서 고를 것.

### 컴포넌트 패턴
- **카드 컨테이너**(`SectionCard`): `Surface(shape = shapes.medium(12dp), color = surfaceVariant, border = BorderStroke(1.dp, outline), tonalElevation = 0.dp)` + 내부 `Column(padding = Spacing.md)`. 강조가 필요하면 `accentColor` 파라미터로 배경을 10% alpha로 물들이고 테두리를 1.5dp+70% alpha로 진하게(계산 결과 카드의 성공/경고 색 등).
- **섹션 헤더 알약(pill)**(`CalcFieldGroupHeader`): 이모지+텍스트를 `labelLarge`로, `background(primary, alpha=0.12f, RoundedCornerShape(50))`, `padding(horizontal=10dp, vertical=4dp)`. 카드 안에서 "이 아래는 어떤 그룹인지" 표시할 때 항상 이 패턴을 쓴다(예: "📊 기본 정보", "🗓️ 기간").
- **화살표/토글 버튼**(`IconChip`): 유니코드 화살표 글리프(▲▼▶) 대신 항상 벡터 아이콘(`Icons.Filled.KeyboardArrowUp/Down/Right`)을 이 컴포넌트로 감싸 쓴다 — 폰트가 기하학 기호 글리프를 지원 안 해도 깨지지 않는다. `size=20dp/iconSize=14dp` 기본(좁은 칸엔 16dp/11dp로 축소 가능), `RoundedCornerShape(6dp)`, 배경 `primary` alpha 0.12(enabled)/0.05(disabled), 아이콘 tint alpha 1.0(enabled)/0.35(disabled).
- **숫자 입력**(`NumberStepperField`): `OutlinedTextField(shape=RoundedCornerShape(12dp))` + 우측 trailing 아이콘 자리에 `IconChip` 위/아래를 3dp 간격 `Column`으로 쌓은 스테퍼. 값은 항상 문자열로 유지(소수/빈 문자열 허용), 스테퍼 버튼이 없어도 되는 자리는 `showStepper=false`.
- **모두 펴기/접기 + 개별 접기**: 리스트 상단에 `Row(spacedBy(xs)) { TextButton(weight=1f) x2 }`("모두 펴기"/"모두 접기"), 카드 헤더 줄엔 `IconChip(KeyboardArrowRight/Down)`으로 개별 접기/펴기 토글. 순서 변경이 필요한 리스트는 카드 왼쪽에 위/아래 `IconChip` 세로 `Column`(`enabled = !isFirst`/`!isLast`)을 붙인다.
- **필드 그룹 사이 구분**: `Spacer(sm) → HorizontalDivider() → Spacer(sm) → CalcFieldGroupHeader(...)` 리듬을 그대로 반복(그룹 마지막 필드 다음엔 `Spacer(md)`로 한 박자 더 띄운 뒤 divider). `HorizontalDivider`는 색/두께를 커스텀하지 않고 Material3 기본(`outlineVariant` 1dp)을 쓴다.

이 패턴들은 이미 `ui/components/SectionCard.kt`, `IconChip.kt`, `NumberStepperField.kt`에 컴포넌트화돼 있으므로, 새 화면을 만들 때 이 컴포넌트들을 그대로 import해서 쓰는 것이 최우선이고, 부득이하게 새로 짜야 한다면 위 값들을 그대로 따른다.

---

## 다회독 상세화: color 문자열 필드는 지우지 않고 하위호환 라벨로 유지 (2026-09-03, 83차 세션)

회독 수/간격을 업무마다 자유 설정하게 하려면 `CalendarTask.color`("red"/"yellow"/"green" 고정 3값)로는 4단계 이상을 표현할 수 없다. 처음엔 `color` 필드를 완전히 대체할지 검토했지만, `applyIncompleteCarryOver`/`setCalendarTaskStatus`의 `task.color == "red"` 판정(연동 진행량을 딱 1회독에서만 반영), `TimetableScreen`/`StudyStatsScreen`/모임 멤버 상세의 색상 렌더링 등 `color` 문자열을 직접 비교·매칭하는 코드가 이미 여러 곳에 퍼져 있어, 필드를 없애면 그 코드를 전부 다시 훑어야 하는 위험이 컸다. 대신 새 필드(`passIndex`/`passTotal`/`passIntervalsCsv`)를 실제 회독 진행/색상(HSV 그라데이션)의 원천으로 두고, `color`는 `legacyColorLabel(passIndex, passTotal)`로 계속 채우되 **passIndex==0은 항상 "red"**로 고정해 기존 "1회독 최초 생성" 판정 코드가 손대지 않고도 그대로 작동하게 했다. 4단계 이상의 중간 회독은 `"pass{N}"` 같은 사람이 안 읽는 라벨을 넣는데, 이 문자열 자체를 UI가 직접 읽는 곳은 이제 없다(모두 새 필드 기반 렌더링으로 교체) — `color`는 사실상 "1회독 여부" 플래그+레거시 호환용 잔재로 남긴 것. **교훈**: 스키마를 확장할 때 기존 필드가 여러 곳에서 암묵적 불변식(예: "red면 항상 첫 회독")으로 쓰이고 있다면, 필드를 완전히 걷어내기보다 그 불변식만 지키도록 남겨두는 쪽이 변경 범위를 훨씬 줄인다.

## `:shared`에 PassSchedule 신규 — 회독 색상/간격 계산을 플랫폼 공용 순수 함수로 (2026-09-03, 83차 세션)

82차에 `CalcEngine.kt`가 이미 `:shared` composite build 모듈로 옮겨져 있어(양 플랫폼 대칭 복제를 없앤 전례), 이번 회독 그라데이션 계산(HSV 보간)·회독 수/간격 파싱·기본값 생성 로직도 처음부터 `:shared/calc/PassSchedule.kt`에 새로 작성했다. 색상은 `Color` 대신 ARGB `Int`를 반환하게 설계했는데, Android/Desktop 양쪽의 Compose `Color(Int)` 생성자가 ARGB Int를 그대로 받기 때문에 shared 모듈이 `androidx.compose.ui.graphics` 의존성 없이도(순수 Kotlin/JVM 모듈 유지) 양쪽에서 바로 쓸 수 있다.

## 캘린더 월 그리드의 태블릿/폰 공용 그리드는 컴포저블 추출, 계산기 입력/결과는 breakpoint 자동 분기 (2026-09-03, 83차 세션)

안드로이드에 데스크탑과 같은 태블릿 좌우분할을 넣을 때 두 화면에서 다른 전략을 썼다. 캘린더는 월 그리드 렌더링(달력 셀 89줄가량)이 상태(연/월/선택일)를 직접 변경(`selectedDate = date` 등)하는 로컬 클로저라, 별도 파일로 추출하지 않고 콜백(`onSelectDate`/`onPrevMonth`/...)을 받는 `CalendarMonthGrid` 컴포저블로 잘라내 태블릿/폰 두 분기가 같은 함수를 호출하게 했다. 계산기는 반대로 기존 "입력/결과/저장됨" 3개 탭 구조를 태블릿에서 "입력+결과 동시 표시(ResponsiveSplit)+저장됨 별도 탭"으로 바꿔야 해서 화면 자체의 최상위 분기(`if (isTabletWidth())`)로 나눴다 — 데스크탑의 `ResponsiveSplit`이 이미 `narrowBreakpoint`(600dp) 밑에서는 자동으로 세로 스택이 되므로, 안드로이드 폰은 이 컴포넌트를 아예 안 거치게 분기해 기존 동작을 100% 보존했다.

## 계산기 업무 입력 날짜/숫자 UI: 웹앱에 참고할 소스가 없어 순수 네이티브로 새로 설계 (2026-09-03, 83차 세션)

사용자가 "웹앱 코드를 따와서 수정하라"고 요청했지만, 실제로 `공부앱/index.html`을 확인해보니 계산기 입력 폼은 브라우저 기본 `<input type="date">`/`<input type="number">`를 그대로 쓰고 있었고 커스텀 미니 캘린더나 스테퍼 버튼은 존재하지 않았다(사용자에게 확인 후 승인받음). 그래서 두 컴포넌트를 순수 네이티브로 새로 설계했다 — `NumberStepperField`는 값이 여전히 자유 텍스트 입력(기존 계산기 필드가 소수/빈 문자열을 그대로 저장하는 방식과 호환)이면서 우측에 ▲▼ 버튼만 추가하는 절충 설계(스테퍼 전용 필드였다면 소수 입력을 막아 회귀가 됐을 것). `DatePickerField`는 안드로이드는 Material3 `DatePickerDialog`(이 앱 최초 사용례)를 쓰지만, Compose Desktop엔 그런 내장 컴포넌트가 없어 `Popup`+`java.time.YearMonth`로 월 그리드를 직접 그리는 커스텀 컴포넌트를 새로 작성했다.

## 공부 중 알림 억제: 시스템 DND 대신 "이 앱 자신의 알림만" 개별 차단 (2026-09-01, 81차 세션)

최초 요청("공부 타이머 작동 중엔 알림이 보이지도 느껴지지도 않게")을 받고 처음엔 `NotificationManager.setInterruptionFilter(INTERRUPTION_FILTER_NONE)`로 시스템 전체 알림을 무음 처리하도록 구현했다. 그런데 이 방식은 (1) 전화·알람을 포함해 다른 모든 앱의 알림까지 건드리고, (2) 알림 자체는 알림창에 그대로 남고 소리/진동/팝업만 억제된다는 한계가 있다는 걸 설명하자, 사용자가 "다른 앱은 건드리지 말고 이 앱(갓생살기) 자신의 알림만 막자"고 범위를 재정의했다 — 시스템 DND 코드/설정 토글을 전부 걷어내고, 이 앱이 스스로 보내는 4종 알림(루틴 리마인더/스트릭/모임 무작위 알림/모임 깨우기·무전기)만 개별적으로 억제하는 `StudyNotificationGate`로 교체했다. 억제 방식은 알림 종류에 따라 둘로 나뉜다: 서버(RTDB)에 안 읽힌 채로 남는 종류(깨우기/무전기)는 그냥 폴링을 건너뛰기만 하면 공부 종료 후 다음 폴링이 자연스럽게 재현하고, 한 번 놓치면 다시 올 계기가 없는 일회성 알람(루틴 리마인더/스트릭/무작위 알림)만 로컬 큐에 쌓았다가 공부 종료 시점에 재발송한다. **교훈**: 사용자가 "느껴지지도 않게"처럼 강한 표현을 썼다고 반드시 시스템 레벨(OS API)로 풀어야 하는 건 아니다 — 실제 의도를 확인하니 앱 범위로 충분했고, 오히려 그쪽이 다른 앱 알림(전화 등)을 건드리지 않는 더 안전한 설계였다.

## 모임 "무작위 알림": 자동 넛지 발송이 아니라 "나에게 알려주고 내가 판단"이 원래 의도였음 (2026-09-01, 81차 세션)

77차에 처음 구현할 때는 "모임에서 처지는 멤버가 있으면 앱이 대신 자동으로 😴 깨우기를 보내준다"로 만들었다. 81차에 사용자가 이 기능을 다시 보고 "그건 내가 설계한 의도와 완전히 다르다 — 원래는 'OO 사용자가 지금 해야 할 일을 안 하고 있다'는 걸 내가 알림으로 받고, 그걸 본 내가 스스로 판단해서 깨우러 가는 것"이라고 정정했다. 자동 발송(`sendSocialGroupNudge` 직접 호출)을 제거하고 정보성 알림(`StudyNotificationGate.showOrQueue`)으로 교체했다. **교훈**: "무작위 알림"이라는 기능 이름과 "누구에게 무엇을 자동으로 보낼지"의 세부 설계는 코드만 봐서는 원래 의도와 다르게 구현됐어도 잘 안 드러난다 — 특히 이런 자동화 기능은 나중에라도 사용자가 실제 사용 흐름을 다시 설명해줄 때 코드와 대조해 확인하는 게 중요하다.

## 데스크탑 사용 중 오버레이: 코너 위젯 제외 결정을 뒤집고 Win32 클릭-통과로 전체화면화 (2026-08-31, 80차 세션)

43차에 "Compose Desktop 창은 클릭까지 가로채서 전체화면으로 덮으면 아래 프로그램을 못 쓰게 된다"는 이유로 데스크탑의 "사용 중 남은 시간" 오버레이만 작은 코너 위젯(160x64dp)으로 명시적으로 남겨뒀었다([[BUGS.md]] 아님, 이 파일 43차 참고). 80차에 사용자가 이 코너 박스를 보고 "안드로이드/브라우저처럼 전체화면이어야 하는데 버그 아니냐"고 문의 — 당시엔 진짜 제약이었지만, 프로젝트가 이미 `jna-platform`을 의존성으로 갖고 있다는 걸 재확인하고 Win32 `WS_EX_TRANSPARENT` 확장 스타일(`GetWindowLong`/`SetWindowLong`, `Main.kt`의 `makeClickThrough()`)로 진짜 클릭-통과 창을 만들 수 있다는 걸 발견해 결정을 뒤집었다. `transparent = true` + `WindowPlacement.Maximized`로 바꾸고 창이 뜨자마자 네이티브 스타일을 걸어, 안드로이드 접근성 오버레이/브라우저 확장의 `pointer-events:none`과 동등한 효과를 얻었다. **교훈**: "이 프로젝트가 이미 가진 의존성/도구로 뭐가 가능한지"는 한 번 막혔다고 영구히 막힌 게 아니라 재검토할 가치가 있다 — JNA는 이미 레지스트리 조작(79차 자동실행 설정)에도 쓰이고 있었는데도 43차 결정 당시엔 이 창 스타일 조작 용도로는 고려되지 않았다.

## 데스크탑 반응형 레이아웃: Row↔Column 전환 + weight 유지 (2026-08-31, 79차 세션)

창이 좁아지면 좌우 분할 화면(타이머/캘린더/계산기)이 뭉개지는 문제를 고치면서, "안드로이드처럼 만들거나 다른 괜찮은 방식"이라는 사용자의 재량을 받았다. 안드로이드용 탭 구조를 그대로 이식하는 대신, `BoxWithConstraints`로 실측 폭을 재서 임계값(760dp) 미만이면 `Row`를 `Column`으로 바꿔치기하는 `ResponsiveSplit` 공용 컴포넌트를 새로 만들었다. 핵심은 두 모드 모두 각 영역이 `weight()`로 "부모로부터 유한한 크기를 받는" 것은 동일하게 유지한다는 점 — Row에서 Column으로 바뀌어도 내부의 `verticalScroll`/`weight(1f)`(캘린더 월그리드 행 등)가 여전히 유효한 부모 높이를 받으므로 별도 리팩터링 없이 그대로 재사용됐다. 안드로이드 탭 구조를 흉내 내는 대안은 각 화면마다 완전히 다른 레이아웃을 새로 짜야 해서 버렸다.

## 테마 커스텀: 배경+포인트 2색에서 나머지 14개 필드를 blend로 자동 계산 (2026-08-31, 79차 세션)

기존 8개 고정 팔레트(`PhoneLockPalette`, 16개 필드)에 사용자가 직접 만드는 "커스텀" 옵션을 추가해달라는 요청. 16개 필드를 전부 사용자가 고르게 하면 조합이 폭발하고 대비가 깨질 위험이 커서, 사용자가 고르는 값은 배경/포인트 2색으로 제한하고 나머지(텍스트/카드/보조색/컨테이너색/테두리 등)는 `buildCustomPalette()`가 배경↔포인트 색의 선형 blend로 자동 계산하도록 했다. 라이트/다크 판정은 배경색의 명도(luminance)로, 텍스트 대비는 각 색의 명도를 보고 흰색/검은색 중 더 대비가 큰 쪽을 골라 보장한다. success/warning/error 3색만은 사용자가 고른 색과 무관하게 기존 8개 팔레트가 공유하는 표준값을 그대로 재사용(고른 색과 충돌 시 의미 전달이 흐려지는 걸 방지).

## "종료 확인 절차" on/off를 같은 절차로 보호 (2026-08-31, 79차 세션)

사용자가 "정말 종료하시겠습니까"(회유 멘트 20개)는 관리(차단) 기능 전용 꼼수 방지 장치이지 앱 전체에 항상 필요한 건 아니라고 판단, 설정에서 켜고 끌 수 있게 해달라고 요청했다. 이 요청을 그대로 구현하면 "끄기 버튼 한 번"으로 전체 보호 장치가 무력화되는 새로운 우회로가 생기므로, 켬→끔 전환 자체를 같은 20문항 절차로 게이트했다(`ExitConfirmScreen`을 `title`/`finalLabel` 파라미터로 일반화해 재사용). 반대로 끔→켬 전환은 보호를 강화하는 방향이라 즉시 허용. 기본값은 사용자 요청대로 꺼짐(off)으로 설정했지만, 신규 설치에만 적용되고 이미 저장된 값은 그대로 유지(다른 설정 기본값 변경과 동일 원칙).

## 데스크탑 자체 업데이트 무한반복: 워치독 인지 종료 신호 누락이 원인 (2026-08-31, 79차 세션)

자체 업데이트(75차) 구현 당시 "설치파일 실행 후 `exitProcess(0)`"만 하면 충분하다고 판단했으나, 이 앱은 자기 자신을 감시/재기동하는 워치독이 있다는 걸 놓쳤다. 트레이 "종료"는 `intentional_exit.flag`를 남겨 워치독의 재기동을 막는데, 업데이트 배너의 종료 경로는 이 표식을 안 남겨서 워치독이 설치 마법사보다 먼저 옛 버전을 되살리고, 그 옛 버전이 새 설치 파일을 다시 잠가버리는 레이스 컨디션이 났다. 교훈: 이 프로젝트에서 "앱을 의도적으로 완전히 죽이는" 코드를 새로 짤 땐 항상 워치독의 존재를 전제하고 종료 표식을 남겨야 한다 — 위 "현재 주의사항"(HANDOFF.md)에도 일반 원칙으로 남겨둠.

---

## 태블릿 릴스/쇼츠 감지: 하단 밴드에 좌우 사이드 레일 밴드 추가 (2026-08-31, 78차 세션)

폰용으로 짠 `containsSelectedKeyword()`가 "선택된 탭 노드는 화면 하단 12% 안에 있다"는 가정으로 작성돼 있었는데, 태블릿에서 Instagram/YouTube가 좌측 세로 내비게이션 레일을 쓰면서 이 가정이 깨져 릴스/쇼츠 차단이 전혀 안 됐다.

- **결정**: 태블릿 여부를 기기 정보로 분기하는 대신, 하단 밴드 조건에 좌/우측 16% 폭의 사이드 레일 밴드를 OR로 추가했다. 폰/태블릿 구분 로직을 따로 두지 않아도 두 레이아웃을 동시에 커버하고, 여전히 `isSelected` + 키워드 매칭이라는 이중 조건이 있어 오탐 위험이 낮다고 판단.
- 대안으로 `LocalConfiguration`류 화면폭 기준 분기도 검토했으나, 이미 44차 세션에서 "태블릿/폰 분기가 필요한 다른 문제"(제목 자동 축소)에 `screenWidthDp >= 600`을 썼던 전례가 있어 일관성 있게 재사용할 수도 있었다 — 다만 이번 문제는 화면폭이 아니라 "내비게이션이 어디 있는가"가 핵심이라, 폭 기준 대신 위치 기준(밴드)으로 접근하는 게 더 직접적이라고 판단해 채택하지 않았다.

---

## 모임 "일정표" 데이터 공유: 새 토글 신설 대신 기존 `shareSchedule`에 편입 (2026-08-31, 78차 세션)

77차엔 계산기 draft 업무(`CalcTask`)가 모임에 전혀 공유되지 않아 "일정표" 탭을 캘린더 데이터로 대체했었다. 78차에 사용자가 "일정표는 진짜 계산기 기반 일정표를 뜻한다"고 정정하면서 계산기 데이터를 실제로 동기화해야 했다.

- **결정**: 별도의 "계산기 공유" 토글을 새로 만들지 않고, 이미 있는 `shareSchedule`(모임 상세 화면에서 캘린더/일정표/통계 탭 전체를 가리는 토글)에 `calcTasks` 필드를 편입시켰다. 화면 UI 관점에서 "일정표" 탭은 이미 `shareSchedule` 하나로 켜고 끄는 같은 섹션 안에 있어서, 별도 토글을 추가하면 사용자에게 "이건 왜 또 따로 켜야 하지"라는 혼란만 준다고 판단.
- RTDB `groups/{id}/stats/{uid}` 노드는 이미 "본인만 전체 쓰기 가능" 블랜킷 규칙이라 새 필드 추가에 규칙 재게시가 필요 없었다 — 77차의 `admins` 도입처럼 매번 규칙 변경이 따라오는 게 아니라는 걸 확인하고 진행.
- 완료 체크(✅, `linkedGoalAchieved`)는 계산기 원본에서도 로컬 캘린더 연동이 있어야 계산되는 파생값이라, 이 값 자체를 동기화하려면 캘린더+계산기 두 데이터를 다시 조인해야 해서 범위가 커진다 — 이번엔 제외하고 목표량 표시까지만 구현(라이브 화면과의 유일한 차이로 남김).

---

## 무전기 TTS 남성 목소리: 실제 다른 배우 음성 대신 피치 조정/한국어 음성 강제 선택 (2026-08-31, 78차 세션)

"남성 목소리를 추가해달라"는 요청에 안드로이드 `TextToSpeech`/데스크탑 SAPI 양쪽 모두 "설치된 음성 중 이름으로 성별을 골라 쓰는" 접근이 기기마다 결과가 다르다는 문제가 있었다(한국어 음성 자체가 기기당 보통 하나뿐).

- **안드로이드**: 이름 기반 Voice 선택 대신 `TextToSpeech.setPitch(0.78f)`로 톤을 낮춰 남성처럼 들리게 했다 — 실제 다른 배우의 목소리가 아니라 같은 엔진 음성의 톤 변화이지만, 어느 기기/엔진에서도 항상 같은 방향(더 낮게)으로 동작한다는 게 장점.
- **데스크탑**: SAPI에 설치된 음성 중 한국어(`ko`) 문화권만 먼저 필터링한 뒤, 그 안에서 남성이 있으면 선택하고 없으면 한국어 여성으로, 한국어 음성 자체가 하나도 없을 때만 시스템 기본값을 쓰도록 했다. 이 과정에서 **기존 코드가 애초에 한국어 음성을 한 번도 명시적으로 선택한 적이 없었다는 잠재 버그를 발견**(사용자가 "지금 보이스는 영어 보이스"라고 지적) — PC의 기본 SAPI 음성이 영어로 설정돼 있으면 한글을 영어 음성으로 읽고 있었던 것. 성별 기능 구현과 함께 이 버그도 같이 고쳤다(BUGS.md 참고).
- 두 플랫폼 다 "설치돼 있으면 쓰고 없으면 조용히 폴백"이라는 원칙을 지켜, 남성 음성이 없는 대부분의 Windows 환경에서도 최소한 한국어 발음이 깨지는 회귀는 없도록 했다.

---

## 워크플로 변경: "깃허브에 올려"=릴리스 게시, 안드로이드 기본 빌드=release (2026-08-31, 78차 세션)

사용자가 두 가지 표준 작업 방식을 명시적으로 확정했다.

- **"깃허브에 올려/커밋해"는 GitHub Release 게시까지 포함**한다. 이전까지는 소스를 `study-planner` 공개 저장소에 커밋/푸시(`sync-public-repo.ps1`)하는 것까지만 했는데, 이러면 desktop/android 자체 업데이트 기능이 참조하는 GitHub Releases 태그가 갱신되지 않아 "업데이트 확인해도 최신 버전"이라고 뜨는 현상이 생겼다 — 사용자가 이를 지적하며 앞으로는 커밋 요청 시 릴리스 게시까지 자동으로 하라고 확정.
- **안드로이드는 기본적으로 `assembleRelease`(release 서명 APK)를 빌드**한다. 그동안 `assembleDebug`를 관행적으로 써왔는데, 사용자가 "왜 자꾸 릴리즈로 서명된 apk는 무시하냐, 앞으로 수정할 때마다 그걸 기준으로 하라"고 명시적으로 정정. `assembleDebug`는 이제 컴파일만 빠르게 확인하고 싶을 때만 쓰고, 실제 배포/게시용 산출물은 항상 release 서명을 쓴다.

---

## 모임 멤버 상세 화면은 라이브 화면 재사용이 아닌 보기전용 별도 작성 (2026-08-30, 77차 세션)

"모임에서 멤버를 클릭하면 내 앱과 같은 탭 구조로 그 사람 데이터가 보이게 해달라"는 요청을 구현할 때, 기존 `RoutineScreen`/`CalendarScreen` 등 라이브 화면에 "보는 대상"(나 vs 멤버) 파라미터를 얹어 재사용할지, 아니면 완전히 새 보기전용 화면을 따로 작성할지 선택해야 했다.

- **결정(사용자 선택)**: 후자(보기전용 별도 작성). 라이브 화면들은 내 실제 데이터를 읽고 **쓰는** 핵심 경로라, "멤버 모드"를 얹으면 모든 편집 진입점(체크박스, 삭제, 색상 변경 등)마다 "지금 내 데이터를 보는 중인가/남의 데이터를 보는 중인가"를 판별해 막아야 한다. 하나라도 놓치면 내가 남의 계정 데이터를 실수로 고치거나, 반대로 멤버 화면에서 뭔가를 눌렀는데 내 데이터가 바뀌는 심각한 사고로 이어질 수 있다.
- 대신 `SocialGroupMemberDetailScreen.kt` 안에 읽기 전용 리프 컴포저블(`MemberRoutineTodayTab` 등)을 새로 작성 — 코드는 일부 중복되지만(`CalendarScreen.kt`의 `stageTextColor`/`TaskChip` 등 순수 렌더링 헬퍼만 `internal`로 열어 재사용), "이 화면에서 절대 쓰기가 일어날 수 없다"는 게 컴파일 타임에 보장된다(애초에 쓰기 함수를 호출하는 코드 자체가 없음).
- **부작용**: `CalendarScreen.kt`의 `MONTHS_KO`/`WEEKDAYS_KO`/`COLOR_LABEL`을 `internal`로 넓히려다 다른 파일(`TimetableScreen.kt` 등)의 동명 `private` top-level 선언과 충돌해 실패 — 대신 멤버 상세 파일 안에 `MEMBER_CAL_` 접두사로 로컬 복제본을 뒀다. 이 프로젝트는 여러 화면 파일이 같은 이름(`MONTHS_KO` 등)의 `private` top-level 상수를 독립적으로 반복 선언하는 관행이 이미 있어(공유 모듈 없음), 새로 화면을 추가할 때도 이 관행을 따르는 게 일관적이다.

---

## RTDB 관리자 권한 모델: `admins` 노드는 별도 규칙 없이 부모 규칙을 상속 (2026-08-30, 77차 세션)

"모임장만 관리자를 승격/해제할 수 있다"를 서버(RTDB 규칙)에서도 강제해야 했다. `groups/{id}/admins/{uid}` 경로에 별도 `.write` 규칙을 추가하는 방법과, 아무 규칙도 안 넣고 부모 `groups/$groupId`의 기존 블랜킷 규칙(모임장만 쓰기 가능)을 그대로 상속시키는 방법 중 후자를 택했다.

- **이유**: RTDB 규칙은 자식 경로에 `.write` 규칙이 없으면 가장 가까운 조상의 규칙을 그대로 물려받는다 — `groups/$groupId` 자체가 이미 "모임장만" 규칙이므로, `admins`에 새 규칙을 안 넣는 것 자체가 "모임장만 admins를 쓸 수 있다"를 정확히 표현한다. 반대로 `info`(이름/코드)와 `members/{uid}`(내쫓기)는 "모임장 또는 admin"이 쓸 수 있어야 해서(admin도 이름 수정/내쫓기 가능, 하지만 admin 임명은 못 함) 부모 규칙과 다른 조건이 필요해 명시적으로 새 규칙을 추가했다 — 이 차이(admins는 상속, info/members는 명시)가 곧 "모임장만 admin을 임명할 수 있고, admin은 admin을 못 만든다"는 요구사항 그 자체다.
- **아직 미배포**: 이 규칙 변경은 `phone-lock-android/firebase-database.rules.json` 파일에만 반영돼 있고 Firebase 콘솔에 재게시되지 않았다 — 재게시 전까지는 모임장 계정만 기존 블랜킷 규칙으로 이름수정/내쫓기가 되고, 승격된 일반 관리자는 권한 거부로 실패한다(HANDOFF.md 다음 우선순위 1번).

---

## 공개 저장소 동기화는 `git worktree`로 분리, 브랜치 전환 없이 진행 (2026-08-30, 77차 세션)

`study-planner`(공개 GitHub 저장소)에 소스만 골라 올리는 스크립트를 만드는 과정에서, 1차 구현(`git checkout clean-main` → `git checkout master -- <path>`)이 아직 커밋 안 한 이 세션의 버그 수정 코드를 통째로 지워버리는 사고를 냈다(자세한 경위는 [[BUGS.md]] 77차 참고). `git checkout master -- <path>`는 master의 "최신 커밋"에서 파일을 가져오는 것이지, 이 프로젝트처럼 "커밋 안 하고 계속 작업"하는 게 일상인 워크플로우에선 항상 최신 작업 트리와 다를 수 있다는 걸 놓쳤다.

- **결정**: `git worktree add C:\build\clean-main-worktree clean-main`으로 완전히 별도의 디렉터리+체크아웃을 만들고, 스크립트는 이 worktree 안에서만 브랜치를 다루며 **메인 작업 디렉터리(`master`)의 브랜치는 절대 전환하지 않는다.** 소스 반입은 robocopy로 현재 디스크 상태(커밋 여부 무관)를 worktree에 복사하는 방식으로 바꿔, "커밋 안 한 파일이 브랜치 전환 때문에 사라지는" 시나리오 자체가 구조적으로 불가능해졌다.
- **위임**: 사용자가 "이것도 네가 알아서 해"라고 승인해, 이후 소스 변경 시마다 확인 없이 이 스크립트를 실행하는 것을 명시적으로 허락받음 — CLAUDE.md의 기본 "git push 금지" 원칙에 대한 좁은 예외로 HANDOFF.md에 기록.

---

## 자체 업데이트 확인 실패를 "최신 버전"과 구분: `Result<T>` + 3상태 sealed class (2026-08-30, 77차 세션)

업데이트 확인이 GitHub API 요청 실패(이번엔 비인증 요청 시간당 60회 한도 초과) 시 "최신 버전입니다"로 오판하는 버그를 고치면서, 단순히 이번 실패 케이스만 패치할지 아니면 향후 어떤 원인의 실패든 구분되게 구조를 바꿀지 선택했다.

- **결정**: `checkLatestAndroidRelease()`/`checkLatestDesktopRelease()`가 `LatestRelease?`(null=실패 또는 없음, 구분 불가) 대신 `Result<LatestRelease?>`(null 자체는 성공+최신, 실패는 별도 `Result.failure`)를 반환하도록 바꾸고, 상위 `Repository`/`PhoneLockRepository`에 `UpdateCheckOutcome`(`Available`/`UpToDate`/`Failed(reason)`) 3상태를 신설했다. 원인 하나(rate limit)만 고치는 대신 "확인 실패"라는 상태 자체를 앱 전체에 존재하게 만든 것 — 다음에 어떤 이유(네트워크 끊김, GitHub 장애, 파싱 오류 등)로 확인이 실패하든 같은 경로로 사용자에게 "확인 실패, 나중에 다시 시도"가 보이지, 다시는 "최신 버전"으로 오판하지 않는다.
- 이 패턴이 처음이 아니다 — 72차에 이미 `PomodoroSyncClient`에서 "빈 응답"과 "네트워크 오류"를 구분 못 해 첫 동기화가 조용히 실패하는 버그를 같은 방식(성공/빈 결과/실패를 명확히 분리)으로 고친 전례가 있다([[DECISIONS.md]] 72차, [[BUGS.md]] 72차 참고) — "실패를 성공처럼 보이는 기본값(null 등)으로 뭉개지 않는다"는 원칙을 이 프로젝트에서 반복 확인.

---

## "모임" 사용자별 비공개는 RTDB 규칙이 아닌 클라이언트 신뢰 기반으로 구현 (2026-08-30, 76차 세션)

"특정 멤버에게만 내 정보를 숨기고 싶다"는 요청을 구현하면서, RTDB 필드 단위 보안 규칙(`auth.uid`를 조건으로 특정 필드를 특정 사용자에게만 안 보이게 막는 것)으로 진짜 서버 강제를 걸지, 아니면 기존 `shareRoutines` 등과 같은 수준(클라이언트가 값을 보고 알아서 "비공개"로 렌더링)으로 갈지 선택해야 했다.

**결정**: 후자(클라이언트 신뢰 기반)로 구현. 이유:
- 기존 `shareRoutines`/`shareStudy`/`shareStreak` 공유 토글도 필드 자체를 아예 안 쓰는 방식(서버 강제에 가까움)이지만, "특정 인물에게만" 조건은 그 값이 한 노드(`stats/{myUid}`)에 여러 명이 동시에 읽는 구조라 필드를 아예 생략할 수 없다 — 결국 `hiddenFromUids`(안 보여줄 uid 목록)를 노드 안에 넣어두고, 각 뷰어가 자기 uid가 그 목록에 있는지 확인하는 수밖에 없다.
- 진짜 서버 강제를 하려면 RTDB 규칙에 `auth.uid`를 조건으로 한 읽기 제한을 걸어야 하는데, 이건 "그룹 멤버 전체가 stats를 읽을 수 있다"는 지금 구조 자체를 손봐야 해서 영향 범위가 크다.
- 이 앱은 서로 아는 소수의 친구/가족이 자발적으로 가입하는 소셜 기능이라, "기술적으로 뚫으려면 뚫을 수 있지만 보통은 안 그런다"는 신뢰 기반 수준이면 충분하다고 판단(무전기 볼륨/재생 로직 등도 같은 수준의 신뢰를 전제).

**적용 범위**: "내 정보를 이 사람에게 숨기기"만 이 방식이고, "이 사람 정보를 내가 안 보기(관심없음)"는 애초에 순수 로컬 설정이라 이 논의와 무관(RTDB에 전송조차 안 함).

---

## 자체 업데이트를 GitHub Releases 기반으로 구현 (2026-08-30, 75차 세션)

사용자가 "APK를 GitHub에 push하고 자동으로 받아오는" 자체 업데이트 기능을 요청. 스토어(Play Store 등) 없이 사이드로드 배포 중인 프로젝트라 자체 구현이 필요했다.

- **왜 GitHub Releases인가**: 이미 공부앱 웹앱을 올려둔 공개 저장소(`studybeultaeon-svg/study-planner`)가 있어 별도 서버/스토리지 없이 재사용 가능하고, Releases API가 토큰 없이(공개 저장소 한정) 조회 가능해 클라이언트에 비밀값을 심을 필요가 없다. private 저장소 + 토큰 내장 방식은 APK/실행파일을 디컴파일하면 토큰이 그대로 노출되므로 배제.
- **태그 규칙으로 버전 판별**: 안드로이드는 이미 `versionCode = System.currentTimeMillis()/1000`(빌드 시각 자동 증가)를 쓰고 있어 그대로 태그(`android-<versionCode>`)로 재사용. 데스크탑엔 그런 자동 증가 값이 없어서(`version = "1.0.0"` 수동 고정), 안드로이드와 같은 원칙(빌드 시각)으로 컴파일 시점에 `BuildInfo.kt`를 생성하는 Gradle 태스크를 신설해 `desktop-<BUILD_TIMESTAMP>` 태그를 매칭했다 — 매 릴리스마다 수동으로 버전을 올릴 필요가 없다는 게 핵심 이유(안드로이드 versionCode 자동 증가와 같은 이유).
- **release 목록 전체를 훑는 이유**: GitHub의 "latest release"는 저장소 전체에서 하나뿐인데, 이 저장소는 안드로이드/데스크탑 릴리스를 번갈아 올리므로 최신 release가 항상 원하는 플랫폼이라는 보장이 없다. 그래서 `/releases`(목록) 전체를 가져와 태그 접두사로 필터링 후 그 안에서 가장 큰 값을 고른다.
- **완전 자동 설치는 불가능, 최종 확인은 항상 사용자**: 안드로이드는 OS 정책상 APK 설치 화면에서 사용자가 직접 "설치" 버튼을 눌러야 하고(`REQUEST_INSTALL_PACKAGES` 권한이 있어도 무인 설치는 불가), 데스크탑은 실행 중인 자기 자신의 파일을 installer가 덮어써야 해서 설치 프로그램을 띄운 뒤 앱을 스스로 종료하고 사용자가 설치 마법사를 마저 진행해야 한다. 두 경우 다 "배너+버튼"까지가 이 기능의 범위이고, 그 이후 마지막 확인은 항상 사용자 몫이다.
- **하루 1회 자동 확인 + 수동 "지금 확인" 버튼 병행**: 처음엔 `dailyResetHour` 기준 하루 1회 자동 확인만 구현했으나, 사용자가 "확인용 버튼뿐 아니라 실제 업데이트를 해주는 버튼도 있어야 한다"고 요청 — 설정 화면에 "지금 확인"(하루 1회 가드를 무시하고 즉시 조회) 버튼을 추가하고, 새 버전이 발견되면 그 자리에 실제 다운로드/설치를 수행하는 배너(자동 확인과 동일한 컴포넌트 재사용)가 나타나도록 구성했다 — 자동/수동 두 경로가 결과를 공유하므로 로직 중복 없음.
- **네트워크 호출은 하루 1회뿐이라 배터리/트래픽 부담이 거의 없음**: 공개 저장소 비인증 API 호출은 IP당 시간당 60회 제한인데, 기기당 하루 1~2회(자동+수동) 수준이라 여유가 크다.

## GitHub CLI 인증은 device-code 웹 인증으로만 수행 (2026-08-30, 75차 세션)

사용자가 "릴리스 생성까지 네가 직접 해달라"고 요청해서 GitHub CLI(`gh`)를 호스트에 설치하고 실제로 릴리스를 게시했다. 이 과정에서 인증이 필요했는데, Claude는 비밀번호/토큰을 직접 입력하거나 저장하는 행위가 금지되어 있다.

- **결정**: `gh auth login --web`(device-code 플로우)만 사용 — Claude는 터미널에 뜨는 일회성 코드와 URL만 사용자에게 전달하고, 사용자가 직접 브라우저에서 GitHub에 로그인해 코드를 입력·승인한다. Claude는 어떤 시점에도 비밀번호나 토큰 값을 보거나 입력하지 않으며, 인증이 끝나면 `gh`가 로컬에 토큰을 안전하게 보관한다.
- 이후 릴리스 생성은 `gh release create`로 위 태그 규칙에 맞춰 자동화했다 — 이 인증은 세션이 끝나도 호스트에 유지되므로, 다음 릴리스부터는 이 인증 단계 없이 바로 릴리스 생성만 하면 된다.

---

## 무전기 설정을 전역(Settings)에서 모임별로 전면 이동 + "깨우기"와 통합 (2026-08-30, 74차 세션)

73차에 만든 무전기는 앱 전체에 하나뿐인 전역 설정(켜짐/모드/볼륨/허용 시간대 1개)이었다. 사용자가 "모임마다 다르게 설정하고 싶다"고 요청 — 예: A 모임은 즉시 재생으로 24시간 받고, B 모임은 업무시간에만 메시지로만 받는 식.

- **결정**: 설정을 `AppPreferences`/`Repository`(로컬)가 아니라 `groups/{groupId}/walkieSettings/{myUid}`(RTDB, 모임×사용자 조합)로 옮겼다 — 로컬에 안 두고 서버에 둔 이유는 이미 모임 자체가 여러 사용자가 공유하는 서버 데이터이고, 다른 기능(모임 공유 설정 등)도 같은 위치에 있어 일관성을 유지하기 위해서다.
- **허용 시간대를 리스트로 확장**: 기존 무전기는 관리앱 그룹처럼 "시작~종료 1개"였는데, 사용자가 "여러 개 추가할 수 있게" 요청해서 `WalkieSchedule(daysMask, startMinute, endMinute)`의 리스트(`GroupWalkieSettings.schedules`)로 바꿨다 — 리스트가 비어있으면 항상 허용, 하나 이상이면 그중 하나라도 지금 시각과 맞으면 허용(OR 판정, 관리앱 그룹의 스케줄+일일한도 OR 판정과 같은 원칙 재사용).
- **"😴 깨우기"와 통합한 이유**: 사용자가 "무전기는 엄연히 깨우기의 확장 버전"이라고 명시적으로 정의 — 그래서 기존에 따로 있던 넛지 버튼과 무전 버튼을 하나의 선택창(`WakeOptionsDialog`)으로 합치고, 그 자리에 텍스트(TTS) 옵션도 새로 추가했다. 이 통합 덕분에 "그냥 깨우기"도 자연스럽게 무전기의 7초 폴링 인프라를 함께 쓰게 돼, 기존 15분 WorkManager보다 훨씬 빨리 오는 부수효과도 얻었다(6번째 CHANGELOG 항목 참고).
- **서비스 자체는 항상 실행, 개별 판단은 폴링마다**: 전역 on/off 스위치가 없어졌으니 `WalkieTalkieService`를 언제 띄울지 애매해질 수 있었는데, "로그인만 돼 있으면 항상 실행하고 폴링마다 메시지가 속한 모임의 설정을 조회해서 처리"로 단순화했다 — 아무 모임도 무전기를 안 켰으면 폴링이 비어서 사실상 유휴 상태이므로 배터리/트래픽 비용이 크지 않다고 판단.

---

## "무전기" 텍스트 메시지(TTS): 사용자 입력을 절대 셸 명령에 이어붙이지 않는다 (2026-08-30, 74차 세션)

74차에 무전기를 "깨우기의 확장"으로 재정의하며 텍스트를 적어 보내면 상대가 TTS로 읽어주는 기능을 추가했다. 안드로이드는 `TextToSpeech` API로 간단했지만, 데스크탑(JVM)엔 내장 TTS가 없어 Windows SAPI를 PowerShell로 호출하는 방식을 택했다.

- **위협 모델**: 이 텍스트는 "다른 모임 멤버"가 보낸, 이 기기 입장에선 신뢰할 수 없는 입력이다. 순진하게 `-Command "...${text}..."` 식으로 문자열을 이어붙이면, 악의적인(또는 계정이 탈취된) 멤버가 따옴표/세미콜론/`$()` 등을 섞어 수신자 PC에서 임의 명령을 실행시키는 커맨드 인젝션이 가능해진다.
- **결정**: 사용자 입력이 전혀 섞이지 않는 고정 스크립트(`speak.ps1`, `param([string]$Text, [int]$Volume)`로 시작)를 앱 데이터 폴더에 한 번만 만들어두고, 실제 문자열은 `ProcessBuilder`의 별도 인자(`-File <경로> -Text <값> -Volume <값>`)로만 전달한다. PowerShell의 `param()` 바인딩은 이 값을 스크립트 코드가 아니라 순수 문자열 데이터로만 취급하므로, 안에 무엇이 들어있어도 명령으로 해석되지 않는다. `ProcessBuilder`도 각 인자를 셸을 거치지 않고 그대로 하나의 프로세스 인자로 전달한다.
- 이 패턴(고정 스크립트 + 파라미터 바인딩으로 데이터 전달, 절대 문자열 결합 금지)은 앞으로 다른 신뢰할 수 없는 입력을 외부 프로세스/셸에 넘겨야 하는 기능이 생기면 그대로 재사용할 것.

---

## 루틴 동기화 알람 누수: 근본 수정 + 일회성 정리 + 방어 코드 3단 대응 (2026-08-30, 74차 세션)

74차 무전기 재설계 배포 후 "앱을 켜자마자 꺼진다"는 크래시가 재현됐다. 처음엔 무전기 서비스의 포그라운드 시작 제약을 의심해 방어 코드를 추가했지만 재발했고, 사용자가 직접 넘겨준 크래시 로그(`PhoneLockApplication.kt`의 전역 크래시 로거로 확보)로 실제 원인이 `AlarmManager` 앱당 예약 알람 500개 한도 초과(`IllegalStateException`)임을 확정했다. 원인은 `syncRoutinesFromFirebase()`가 원격이 더 최신이면 로컬 루틴을 통째로 지우고 새 Room ID로 재삽입하는데, 루틴 알림 예약(`RoutineAlarmScheduler`)이 이 ID를 그대로 `AlarmManager` requestCode로 써서 — 동기화 때마다 옛 ID의 알람은 취소할 방법이 사라지고 계속 쌓이던 것.

- **결정**: 문제를 세 층으로 나눠 대응했다 — ① 재발 방지(근본 수정: 삭제 전에 옛 루틴들의 알람을 먼저 취소), ② 기존 피해 복구(이미 쌓인 알람을 정리하는 일회성 스윕, ID 범위를 훑어 존재 여부와 무관하게 취소 시도 — 존재하지 않는 걸 취소해도 예외가 안 나는 API 특성을 이용), ③ 미지의 재발 대비(한도 초과 자체를 `runCatching`으로 흡수해 앱이 죽지 않게). 하나만으로는 부족했다 — ①만 하면 이미 쌓인 500개는 안 지워지고, ②만 하면 다음 동기화에서 또 쌓이고, ③만 하면 알람 기능 자체가 조용히 안 되는 채로 방치된다.
- **왜 로그가 결정적이었나**: 로그 없이 첫 번째 시도(무전기 서비스 의심)는 완전히 빗나간 추측이었다 — 실제로는 무관한 기능(루틴 동기화)이 원인이었다. 이후 "크래시가 재현되는데 원인을 모르겠다" 상황이 다시 생기면, 추측으로 여러 라운드 반복하기 전에 최대한 빨리 실제 로그(이번엔 `PhoneLockApplication.kt`의 크래시 로거, 또는 `adb logcat`)부터 확보할 것 — 이 원칙은 38차 이전에도 "DebugLog 계측" 사례로 이미 한 번 확인된 바 있다.

---

## "무전기" 실시간성: FCM/Cloud Functions 백엔드 대신 포그라운드 서비스 + 빠른 폴링 (2026-08-29, 73차 세션)

사용자가 "모임 내 강제 음성 메시지"(무전기) 기능을 제안 — 상대가 수락하지 않아도 재생되는 게 핵심이라 실시간성이 중요했다. 조사 결과 이 프로젝트는 서버 코드가 전혀 없고(모든 동기화가 클라이언트→RTDB REST 직접 호출) FCM으로 진짜 즉시 푸시를 하려면 "보내는 쪽"이 서버 자격증명으로 FCM 발송 API를 호출해야 해서, 사실상 Firebase Cloud Functions(서버리스 백엔드) 신규 도입이 필요하다는 걸 사용자에게 설명했다 — Cloud Functions는 무료 Spark 요금제로는 못 쓰고 유료 Blaze 요금제 전환이 필요하다.

- **결정(사용자 선택)**: 백엔드 도입 없이 클라이언트 폴링만으로 최대한 빠르게 타협 — 안드로이드는 새 포그라운드 서비스(`WalkieTalkieService`, 상주 알림 1개, 7초 폴링), 데스크탑은 이미 상시 실행 중인 앱에 조건부 폴링 루프만 추가.
- **왜 기존 "깨우기"(nudge) 인프라에 안 얹었는지**: 안드로이드의 기존 `GroupNudgeWorker`는 WorkManager 최소 주기(15분) 하한 때문에 이미 "즉시 전달"이 아님이 62차부터 알려진 한계였다(IDEAS.md에 FCM 업그레이드가 후속 과제로 남아있었음). 접근성 서비스(`AppMonitorAccessibilityService`)의 2초 tick은 더 빠르지만 접근성이 꺼져 있으면 아예 안 도는 조건부 컴포넌트라, "수신 허용을 켰으면 항상 동작"해야 하는 무전기엔 안 맞았다 — 그래서 독립 포그라운드 서비스를 새로 만들었다(상주 알림이 하나 늘어나는 트레이드오프는 감수).
- 데스크탑은 백엔드도 새 서비스도 필요 없이, `Main.kt`의 기존 30초 루프(`RoutineNotifier`/`SocialGroupNotifier`) 옆에 `walkieReceiveEnabled`가 켜진 동안만 7초, 꺼지면 30초로 돌아가는 조건부 루프를 별도로 추가하는 것으로 충분했다.
- 진짜 실시간(FCM/Cloud Functions)은 IDEAS.md에 후속 과제로 남김 — 사용자가 실사용 후 폴링 지연이 불편하면 재검토.

---

## "무전기" 오디오 포맷: 코덱 없는 표준 WAV (2026-08-29, 73차 세션)

두 플랫폼이 서로 녹음한 클립을 재생해야 하는데(안드로이드가 녹음한 걸 데스크탑이 재생, 그 반대도), 이 프로젝트는 지금까지 오디오 녹음/재생 코드가 전혀 없었고 새 코덱 의존성을 들이고 싶지 않았다.

- **결정**: `MediaRecorder`의 기본 출력(3GP/AMR)은 데스크탑에서 별도 코덱 라이브러리 없인 디코드가 안 돼서 후보에서 제외. 대신 안드로이드는 `AudioRecord`로 원시 PCM을 캡처해 44바이트 WAV 헤더를 직접 붙이고, 데스크탑은 JDK 표준 `javax.sound.sampled`(`TargetDataLine`)로 그대로 WAV를 저장 — 둘 다 플랫폼 기본 API만 쓰고 새 Gradle 의존성이 0개다.
- **스펙**: 8kHz, mono, 16-bit PCM, 최대 10초 — 이 스펙의 10초 클립은 raw ≈160KB, base64 인코딩 후 ≈213KB로, RTDB 노드 크기 제한에 여유 있게 들어가고 가끔 보내는 음성 메모 용도로는 대역폭도 허용 범위라고 판단했다(압축 코덱보다 용량은 크지만, 코덱 없이 양 플랫폼이 대칭 구현을 유지할 수 있는 단순함을 우선).
- **저장**: Firebase Storage(새 상품 도입) 대신 짧은 클립을 base64로 기존 RTDB에 그대로 저장(사용자 선택) — 재생/확인 후 즉시 삭제해서 무한정 쌓이지 않게 한다(nudges의 "최신값 1개만 덮어쓰기"와 달리 리스트 구조라 명시적 삭제가 필요).

---

## "무전기" 강제 재생 수준: 기기 볼륨/무음은 항상 존중, 앱 자체 opt-in 설정으로 보완 (2026-08-29, 73차 세션)

"무전기"라는 개념 자체는 상대 동의 없이 일방적으로 들리게 하는 것이지만, 사용자가 명시적으로 "당연히 볼륨/무음 설정을 존중해야" 한다고 결정 — 알람 앱처럼 `STREAM_ALARM` 등 특수 스트림으로 무음/방해금지 모드를 뚫는 방식은 채택하지 않았다.

- 재생은 일반 미디어 스트림(안드로이드 `STREAM_MUSIC`, 데스크탑 `Clip`+`MASTER_GAIN`)을 쓰고, 기기가 무음/저볼륨이면 그대로 안 들리거나 작게 들린다.
- 대신 수신자를 보호하는 장치를 전부 **앱 자체 설정**으로 옮겼다: 수신 허용 on/off(**기본 off**, 명시적 opt-in — 기존 "모임 공유 설정" 3종 토글은 기본 on인 것과 의도적으로 반대), 볼륨(0~100, 시스템 볼륨 위에 곱해지는 배율), 허용 시간대(둘 다 비우면 "항상 허용"), 그리고 "즉시 재생(무전기)" vs "메시지로 받기(눌러서 재생)" 모드 선택 — 후자를 고르면 강제성 자체가 사라지고 일반 음성 메시지에 가까워진다.
- 허용 시간대 밖이면 모드와 무관하게 무조건 강제 재생을 안 하고 인박스에만 쌓는다(안전장치가 모드 선택보다 우선).

---

## 호스트에서 직접 빌드 (VM 원칙의 예외, 사용자 명시적 요청) (2026-08-29, 73차 세션)

이 프로젝트는 VM_BUILD_HANDOFF.md에 따라 데스크탑/안드로이드 빌드를 호스트와 분리된 VM(`PhoneLockBuildVM`)에서만 하도록 정해져 있었다 — 호스트엔 실제 운영 중인 워치독(`PhoneLockDesktopWatchdog`)과 프로덕션 Firebase 데이터가 있어서, 빌드 검증 중 실수하면 실사용 시스템이나 실제 데이터가 손상될 위험이 있기 때문(과거 두 번 실제로 데이터가 유실된 전적 있음).

- 73차엔 데스크탑 "모임 생성" 버그 수정을 빨리 검증하고 싶다는 사용자 요청으로, 이 원칙을 알려준 뒤에도 사용자가 "여기서 진행해"라고 명시적으로 재확인해서 호스트에서 직접 빌드/배포를 진행했다.
- 빌드 자체는 이번 수정 범위(`createGroup()` 함수 하나 + 신규 기능이지만 기존 기능 로직은 안 건드림)가 크지 않다고 판단해 진행했지만, 데이터 리스크(그룹 생성 테스트가 실제 프로덕션 Firebase에 실제로 쓰임)는 여전히 존재 — 실제 배포 전 워치독 비활성화→프로세스 종료→robocopy(FAILED 0 확인)→해시 비교→재실행→워치독 재활성화의 기존 표준 절차를 그대로 지켜서 리스크를 최소화했다.
- **이 예외가 원칙을 바꾸는 건 아니다** — 다음에 호스트에서 직접 빌드가 필요하면 다시 사용자에게 확인받을 것(HANDOFF.md "현재 주의사항"에도 명시).
- 호스트에 이미 Gradle 8.7 캐시와 jpackage 없는 JDK 21(Android Studio 번들 JBR)이 있었고, jpackage가 필요해서 Temurin JDK 21을 별도로 받아 해결했다 — JDK 25는 jpackage는 있지만 Gradle 8.7 자체가 그 위에서 못 돈다(Kotlin DSL 컴파일러의 버전 문자열 파서가 "25.0.2" 형식을 못 읽음). 자세한 내용은 [[CHANGELOG.md]] 73차 참고.

---

## Google 로그인 → 아이디/비밀번호 로그인 전환 (2026-08-28, 72차 세션)

사용자가 Google 계정 로그인 방식을 빼고 아이디+비밀번호 방식의 일반 로그인/회원가입으로 바꿔달라고 요청.

- **가짜 이메일 변환**: Firebase Authentication은 email/password 방식만 지원하고 임의 문자열 아이디를 지원하지 않는다. 사용자가 입력한 아이디를 `{아이디}@phonelockapp.local`로 변환해 Firebase Auth에 넘기는 방식을 택했다(사용자 승인) — 화면엔 이 가짜 이메일이 절대 노출되지 않고, uid 발급은 그대로 Firebase가 하므로 uid 기반의 기존 동기화 인프라(`AccountSyncClient`/`SocialGroupSyncClient`/`PomodoroSyncClient`, RTDB 스키마 전체)를 전혀 안 건드리고 재사용할 수 있었다.
- **로그인 아이디와 "가입 신청" 아이디 통합**: 68차부터 있던 관리자 승인용 `customId` 신청 단계(로그인 방식과 무관하게 항상 존재)와 이번에 새로 생긴 로그인용 아이디가 서로 다른 개념으로 남으면 사용자가 아이디를 두 번 입력하는 혼란이 생긴다고 판단해 사용자에게 직접 확인 후 하나로 통합 — 실제 계정으로 로그인(게스트 아님)한 경우 로그인 아이디를 그대로 가입 신청 아이디로 프리셋하고 입력칸 자체를 숨긴다(`AuthManager.currentLoginId`, 안드로이드 `IdSetupScreen`의 `presetId`/데스크탑 `UsernameStep`). 게스트 로그인은 여전히 아이디를 직접 입력해야 한다(로그인 시점엔 아이디가 없으므로).
- **google-services 플러그인은 유지**: 안드로이드 `build.gradle.kts`에서 Google Sign-In 전용 의존성(`androidx.credentials`, `googleid`)만 제거하고 `com.google.gms.google-services` 플러그인과 `google-services.json`은 그대로 뒀다 — 이 플러그인은 Google Sign-In이 아니라 FirebaseAuth SDK 자체의 프로젝트 설정(FirebaseOptions) 자동 초기화에 쓰이기 때문에, 빼면 `FirebaseAuth.getInstance()`가 아예 동작하지 않는다.
- **데스크탑은 오히려 단순화**: 기존 Google 로그인은 OAuth PKCE + 로컬 루프백 리다이렉트(RFC 8252) + 브라우저 팝업까지 필요한 무거운 구현이었는데, email/password 방식은 Firebase Identity Toolkit REST(`accounts:signUp`/`accounts:signInWithPassword`)를 직접 호출하는 것만으로 끝나서 브라우저를 열 필요가 없어졌다 — 코드량이 크게 줄었다.
- **후속 조치**: ① Firebase 콘솔의 Authentication → Sign-in method → Email/Password는 같은 세션 후반에 사용자가 직접 활성화 완료. ② 기존에 Google 계정으로 로그인해서 이미 관리자 승인까지 받은 사용자가 새 아이디/비번 방식으로 다시 가입하면 Firebase가 **새 uid**를 발급하므로 예전 프로필/승인 상태와 완전히 분리된 별개 계정이 된다 — 재가입 신청과 관리자 재승인이 다시 필요하다. 데이터 이전(uid 마이그레이션) 기능은 이번엔 구현하지 않았다(63차에 유사한 레거시 마이그레이션 기능을 이미 제거한 전례가 있어, 필요성이 확인되면 별도로 논의 후 구현 권장).
- 자세한 구현 파일 목록은 [[CHANGELOG.md]] 72차 참고.

---

## 첫 동기화 시 "원격 없음"과 "네트워크 오류"를 구분해서 반환할 것 (2026-08-28, 72차 세션)

새 아이디/비번 계정으로 실사용하던 중 루틴/캘린더/계산기가 전혀 동기화되지 않는 걸 발견([[BUGS.md]] 72차 참고) — `PomodoroSyncClient`의 문서 단위 LWW 읽기 함수(`readRoutines`/`readCalendarTasks`/`readCalculator`)가 "RTDB 경로가 아직 없음"(신규 계정의 정상 상태)과 "네트워크/파싱 오류"를 똑같이 `null`로 반환하고 있었고, 그 `null`을 받은 `syncXFromFirebase()`가 곧장 함수를 빠져나가 로컬→원격 push 분기 자체에 도달하지 못했다.

- **결정**: `body == "null"`(RTDB가 빈 경로에 실제로 돌려주는 정상 응답)은 `null`이 아니라 "빈 결과"(모든 타임스탬프 0)로 반환하도록 세 함수를 고쳤다. 진짜 오류(비2xx 상태 코드, 예외)만 여전히 `null`로 남긴다.
- **왜 지금까지 안 드러났는지**: `confirmSync`/`snoozeSync`/`dailyUsage`/`studyLog` 같은 "기기별 키" 방식 동기화는 `null`을 "동기화 값 없음, 로컬만 써라"로 자연스럽게 처리하는 구조라 이 문제가 없다 — 문제는 "문서 전체 LWW 비교 후 필요하면 push"하는 3개 함수에 국한된다. 앞으로 새로운 문서 단위 LWW 동기화 경로를 추가할 때 이 구분을 반드시 처음부터 넣을 것 — "성공했지만 비어있음"과 "실패"를 같은 값으로 뭉개면 첫 동기화가 조용히, 영구히 실패한다.

---

## 관리자 승인 시 기능 범위(권한) 지정 (2026-08-28, 72차 세션)

사용자가 "관리자가 승인할 때 루틴/공부/관리/모임 중 어디까지 허용할지 정할 수 있게 해달라, 단 설정 화면이 복잡해지지 않게"라고 요청.

- **스키마**: `users/{uid}/profile.permissions = {routine, study, manage, social}`(전부 boolean). 필드가 없으면(기존 승인 사용자) 전부 `true`로 취급해 하위호환을 보장한다 — 별도 마이그레이션 스크립트 불필요.
- **UI를 안 늘리는 방법**: 새 화면이나 다이얼로그를 추가하지 않고, 기존 "승인" 버튼 위에 4개 `FilterChip`(루틴/공부/관리/모임, 기본 전부 선택)을 인라인으로 붙이고 승인 시 그 상태를 그대로 같이 기록했다. 이미 승인된 사용자 목록에도 같은 칩을 붙여 클릭 즉시 저장(별도 저장 버튼 없음) — 신규 화면/모달 없이 기존 관리자 패널 레이아웃 안에서 해결.
- **클라이언트 적용 범위**: 각 사용자의 로그인 기기가 승인 확인(`AccountGateScreen`)할 때마다 권한을 로컬에 캐시(`AppPreferences`/`Repository`)하고, `MainActivity`/`MainScreen`이 그 값으로 해당 탭(하단 탭/사이드바 항목)을 아예 숨긴다. 설정 탭은 로그아웃 등을 위해 항상 노출. 이건 "숨기기"이지 서버 측 강제 차단이 아니다 — RTDB 규칙 자체는 각 데이터 경로의 소유자 uid 기준 접근만 통제할 뿐 기능별 세분화된 규칙은 두지 않았다(1인/소수 신뢰 사용자 전제인 이 프로젝트 성격상 UI 레벨 통제로 충분하다고 판단).

---

## 계정 삭제: 자기 계정만 가능, 관리자의 타인 계정 삭제는 넣었다가 뺌 (2026-08-28, 72차 세션)

사용자가 "계정 삭제 버튼을 만들어달라, 관리자 패널에서도 삭제할 수 있게 하거나 본인이 설정에서 삭제하거나"라고 요청 — 처음엔 둘 다 구현했으나, 관리자 쪽은 사용자 판단으로 같은 세션에 다시 제거했다.

- **왜 관리자는 남의 Firebase Auth 계정을 못 지우는가**: Firebase Identity Toolkit의 `accounts:delete`는 요청에 실린 idToken이 가리키는 "그 계정 본인"만 지울 수 있다 — Admin SDK(서비스 계정 자격증명)가 있어야 관리자가 임의 계정을 지울 수 있는데, 이 프로젝트는 Admin SDK를 쓰지 않는다(REST + 사용자 idToken 방식만 사용, [[DECISIONS.md]] 61차 이후 일관). 그래서 처음 구현한 관리자용 "삭제" 버튼은 실제로는 `users/{uid}/profile`+`allowedUsers/{uid}`만 지우고 Firebase Auth 계정 자체는 그대로 남기는, 이름과 실제 동작이 안 맞는 기능이었다.
- **결정**: 사용자에게 이 한계를 설명한 뒤, "그러면 관리자의 타인 계정 삭제 기능은 없애자"는 판단을 받아 버튼과 `deleteUserByAdmin()` 함수를 양 플랫폼에서 완전히 제거했다. 관리자는 기존 "승인취소"(allowedUsers 제거 + status=rejected)만 유지 — 실질적으로 그 사용자의 앱 접근을 완전히 끊는다는 목적은 이것으로 충분히 달성된다.
- **자기 계정 삭제 순서**: RTDB 데이터 삭제(`users/{uid}` 전체, 아직 로그인 상태에서만 가능) → Firebase Auth 계정 삭제. 순서를 반대로 하면 Auth 계정이 먼저 사라져 그 뒤의 RTDB 삭제 요청이 인증을 통과하지 못한다. `allowedUsers/{uid}`는 규칙상 관리자만 지울 수 있어 자기 삭제로는 못 지우지만, 그 uid는 Auth 계정이 없어져 다시는 로그인할 수 없으므로 무해한 흔적으로 남는다.
- **아이디 영구 잠김**: `usernames/{customId}` 선점은 RTDB 규칙상(`!data.exists()`만 허용) 그 누구도(관리자 포함) 지울 수 없다 — 계정을 삭제해도 그 아이디는 본인을 포함해 영원히 재사용 불가. 삭제 확인 다이얼로그에 이 사실을 명시했다.

---

## 화이트리스트를 콘솔 수동 등록 대신 앱 내 "가입 신청 → 관리자 승인" 플로우로 구현 (2026-08-27, 68차 세션)

64차에 `allowedUsers` 화이트리스트를 도입했지만 uid 등록을 매번 Firebase 콘솔에서 수동으로 해야 해서 사용자가 번거로움을 느낌 — 앱 안에서 로그인/게스트 시작 → 아이디+닉네임 신청 → 관리자 승인까지 끝나는 플로우로 바꿔달라는 명시적 요청을 받음.

- **결정**: `allowedUsers` 게이트 메커니즘 자체(그룹/모임/inviteCodes 등 기존 경로가 전부 이걸로 게이트됨)는 손대지 않고, 그 위에 `usernames/{customId}`(아이디 선점, 중복 방지)와 `users/{uid}/profile`(status: pending/approved/rejected) 두 노드만 추가해서 관리자가 앱에서 직접 `allowedUsers`를 써줄 수 있게 규칙만 확장했다. 기존 게이트를 새 메커니즘으로 대체하지 않고 "관리자 쓰기 권한을 얻는 방법"만 콘솔 수동 조작에서 앱 내 조건부 규칙으로 바꾼 셈 — 이미 검증된 구조를 최대한 재사용해 규칙 재설계 리스크를 줄였다.
- **관리자 판별**: 별도의 `isAdmin` 필드 대신 `usernames/BEULTAEON`에 저장된 uid로 판별하기로 함(사용자가 이 아이디를 관리자로 지정해달라고 명시적으로 요청) — 이 노드는 "선점식"이라 한 번 등록되면 다른 uid로 바꿀 수 없으므로, 관리자 계정을 나중에 다른 사람에게 넘기려면 규칙을 직접 고쳐야 한다는 한계가 있음(현재는 1인 관리자 전제로 충분하다고 판단).
- **관리자 자가승인(부트스트랩)**: `usernames/BEULTAEON` 규칙이 `!data.exists()`만 요구해 로그인한 사람이면 누구나 최초 1회 선점 가능한 점을 이용해, 클라이언트가 `customId === "BEULTAEON"`으로 신청하면 그 즉시 스스로를 `allowedUsers`+`profile.status=approved`로 만들도록 구현 — 콘솔 개입 없이 첫 관리자가 자기 자신을 승인하는 문제를 풀었다. 이후 다른 사람이 "BEULTAEON"을 다시 선점하는 건 규칙상 불가능하므로 안전.
- **게스트 로그인 재도입**: 61차 결정으로 로그인 필수 전환하며 옛 "익명인증+fbUser 텍스트" 방식을 63차에 완전히 삭제한 바 있다. 이번 게스트 로그인은 그 삭제된 메커니즘을 되살리는 게 아니라, `GoogleAuthManager`의 uid 세션 체계 안에 Firebase Anonymous Auth를 하나의 로그인 수단으로 편입시킨 새 설계다 — `fbUser` 같은 텍스트 식별자 개념은 다시 들이지 않음.
- **자세한 구현 내용**은 [[CHANGELOG.md]] 68차 참고.

---

## Release keystore를 새로 생성하고 gradle.properties와 분리된 keystore.properties로 관리 (2026-08-27, 67차 세션)

64차에 ProGuard/R8은 켰지만 release 서명 설정 자체가 없어 `assembleRelease` 산출물이 unsigned APK였다. 실기기 설치 검증(64차 우선순위 항목)을 진행하려면 서명이 필수라 사용자에게 "지금 keystore를 새로 만들지" 확인 후 진행.

- **결정**: `phone-lock-android/keystore/release.jks`(PKCS12, alias `phonelock-release`, 유효기간 10000일)를 새로 생성하고, 비밀번호는 `phone-lock-android/keystore.properties`(신규 파일)에 저장, `app/build.gradle.kts`가 이 파일이 있으면 `signingConfigs.release`를 구성해 release 빌드에 자동 서명하도록 연결했다.
- **왜 `gradle.properties`가 아닌 별도 파일인가**: `git ls-files`로 확인해보니 이 저장소의 초기 baseline 커밋(`7d9e448`)이 `phone-lock-android` 전체를 이미 커밋해뒀다는 걸 발견 — HANDOFF에 오래 남아있던 "phone-lock-android는 이 저장소에서 untracked" 서술은 (적어도 지금은) 틀린 정보였다(추적 대상이 아니었던 게 아니라 baseline 커밋 이후 status에 `M`으로만 표시돼서 그렇게 보였을 가능성). `gradle.properties`는 이미 git에 커밋돼 추적 중이라 여기 비밀번호를 적으면 다음 커밋에 그대로 올라간다 — 그래서 새 파일 `keystore.properties`와 `keystore/` 디렉터리를 만들고 루트 `.gitignore`에 명시적으로 추가해 커밋 대상에서 뺐다.
- **keytool 동작 주의**: 최신 JDK의 `keytool -genkeypair`는 기본적으로 PKCS12 형식을 만드는데, 이 형식은 keystore 비밀번호와 별도의 key 비밀번호를 지원하지 않는다(`-keypass`를 줘도 무시되고 storepass가 그대로 쓰임) — `keystore.properties`의 `keyPassword`를 `storePassword`와 동일한 값으로 맞춰둔 이유.
- **사용자 책임**: 이 keystore를 잃어버리면 이후 release 빌드는 다른 서명이 되어 기존에 설치된 앱 위에 업데이트로 덮어쓸 수 없게 된다(제거 후 재설치만 가능, 데이터 유실). OneDrive 안에 있어 어느 정도 백업은 되지만, 사용자가 별도로 안전한 곳(비밀번호 관리자 등)에 `keystore/release.jks` + `keystore.properties` 내용을 백업해두는 게 좋다.
- 자세한 빌드 검증 과정은 [[BUGS.md]]/[[CHANGELOG.md]] 67차 참고.

---

## 데스크탑 jpackage용 JDK를 Android Studio 번들 JBR과 별도로 확보 (2026-08-27, 66차 세션)

데스크탑 `createDistributable`(Compose Desktop이 내부적으로 jpackage 호출)을 빌드하려면 Gradle 자신이 구동되는 JDK에 `jpackage.exe`가 있어야 하는데, 이 환경엔 Android Studio 번들 JBR(JDK 21, jpackage 없음)과 시스템 JDK 25(Gradle 8.7과 비호환) 둘뿐이라 안드로이드 컴파일은 되지만 데스크탑 패키징은 안 되는 상황이었다.

- **결정**: 사용자 승인을 받아 Eclipse Temurin(공식 OpenJDK 배포판) JDK 21을 GitHub 릴리즈에서 직접 받아 `C:\build\jdk-temurin21`에 압축 해제해두고, 데스크탑 빌드 시에만 이 경로를 `JAVA_HOME`으로 지정하는 방식을 택했다. 시스템 JDK를 바꾸거나 PATH를 건드리지 않고, 이 프로젝트의 데스크탑 빌드에만 국한된 로컬 도구로 격리.
- **이유**: Android Studio JBR은 애초에 IDE 내장용이라 jpackage를 포함하지 않고, 별도로 jpackage만 뽑아 쓰는 방법은 없다(JDK 배포 전체가 필요). 시스템 JDK 25는 Gradle 8.7이 지원하는 최신 버전(21)보다 훨씬 앞서 있어 Gradle 데몬 구동 자체가 실패한다. 안드로이드 컴파일(`compileDebugKotlin`)은 JBR(21)만으로 충분해서 이번에 새로 건드리지 않았다.
- **앞으로**: 데스크탑 배포가 필요한 세션에서 `C:\build\jdk-temurin21\jdk-21.0.12.1+1`이 남아있는지 먼저 확인하고 없으면 다시 받을 것. [[HANDOFF.md]]/[[CHANGELOG.md]] 66차 참고.

---

## 무단 배포/코드 복제 방지: "완전 차단 불가" 전제 하에 백엔드 잠금(Firebase 규칙+화이트리스트)을 최우선, App Check는 보류 (2026-08-27, 64차 세션)

APK를 지인에게 직접 전달하는 방식으로 배포할 예정인데, 받은 사람이 파일을 재배포하거나 코드를 뜯어 비슷한 앱을 만들어 파는 걸 걱정한다는 요청 — 기술적으로 가능한 것과 불가능한 것을 먼저 구분해서 설명하고 진행했다.

- **"APK 파일 자체의 재배포"는 기술적으로 막을 방법이 없다는 걸 먼저 명시**: 파일을 상대방에게 준 순간 그 바이트는 상대방 손에 있고, 어떤 DRM을 걸어도 사이드로딩 방식에서는 복사 자체를 막지 못한다 — 대신 "복사된 앱이 계속 쓸모 있는지"(백엔드 접근권)를 통제하는 쪽으로 방향을 잡았다. 이 구분(파일 자체 보호 vs 백엔드 보호)을 사용자에게 먼저 설명하고 동의를 구한 뒤 작업 범위를 정함.
- **Firebase RTDB 보안 규칙 강화 + `allowedUsers` 화이트리스트를 최우선으로 선택한 이유**: 이 앱은 Google 로그인 uid를 경로로 쓰는 구조(61차)라, 로그인 여부만 확인하는 지금의 열린 규칙에서는 앱이 어떻게 유출되든 로그인만 하면 남의 데이터를 읽고 쓸 수 있는 게 근본 위험이었다. `allowedUsers/{uid}` 화이트리스트를 전체 트리 조건에 추가하면, 앱을 손에 넣은 사람이 구글 로그인에 성공해도 사전에 등록 안 된 계정이면 아무 데이터도 못 읽고/못 쓰게 된다 — "앱 유출 자체"는 못 막아도 "유출된 앱이 정상 작동하는 것"은 막을 수 있다. 규칙 파일(`phone-lock-android/firebase-database.rules.json`)만 작성했고 **실제 Firebase 콘솔 적용과 `allowedUsers` 등록은 사용자가 직접 해야 함**(Claude에게 콘솔 접근 권한 없음).
- **"모임" 기능의 교차 사용자 쓰기 패턴 때문에 규칙이 단순 소유자 검사로 안 끝남**: `SocialGroupSyncClient.kt`를 다시 읽어보니 `deleteGroup()`이 모임장 권한으로 다른 멤버의 `users/{다른uid}/socialGroupIds/{groupId}`를 지우고, `sendNudge()`는 멤버 아무나가 다른 멤버의 `nudges/{targetUid}`에 쓴다 — 순수 "자기 uid만" 규칙으로는 이 두 케이스가 막혀서 기능이 깨진다. `socialGroupIds/{groupId}`엔 "본인 또는 그 그룹 모임장"이라는 예외를, `nudges/{targetUid}`엔 "쓰는 사람이 그 모임의 멤버이기만 하면"이라는 별도 조건을 달아 해결 — RTDB 규칙은 "경로상 어느 한 단계라도 `.write`가 true면 허용"이라는 계단식 평가를 이용해, 상위(`groups/{groupId}`)에서는 모임장 전용으로 잠그고 하위 경로에서만 개별 예외를 열었다.
- **ProGuard/R8은 켰지만 이번 세션에 컴파일 검증은 못 함**: `isMinifyEnabled = false → true`로 바꾸고 최소한의 `proguard-rules.pro`(Firebase Auth/Credential Manager/Room 엔티티 keep)를 추가했는데, 이 세션엔 Android 빌드 도구가 없어 실제 `assembleRelease`가 성공하는지 확인 못 했다 — 다음 배포 전에 반드시 한 번 릴리즈 빌드+실기기 설치까지 확인할 것을 [[HANDOFF.md]]에 남김. R8이 리플렉션 기반 코드를 잘못 지우면 런타임에만 터지고 컴파일은 성공할 수 있어 특히 주의가 필요.
- **Firebase App Check(Play Integrity)는 이번 세션에서 보류(사용자 동의)**: 재서명된 클론 앱이 백엔드를 못 쓰게 막는 유일한 조치라 원래는 하고 싶었지만, `PomodoroSyncClient.kt`가 공용 HTTP 헬퍼 없이 15곳 넘게 각자 `HttpURLConnection`을 만드는 구조라 손이 많이 가고(각 지점에 토큰 헤더를 개별로 붙여야 함), 무엇보다 **데스크탑 앱은 애초에 대상이 아니다**(Play Integrity는 안드로이드 전용, 데스크탑용 attestation은 이 프로젝트 규모에 비해 과한 별도 작업). 컴파일 확인이 불가능한 이번 세션에 15곳+를 손으로 고쳐서 지금 정상 동작 중인 뽀모도로/실행확인/스누즈 동기화를 깨뜨릴 위험을 감수하지 않기로 하고, 설계만 [[IDEAS.md]]에 남겨 다음 빌드 가능한 세션으로 미뤘다.

---

## Firebase 접속 정보 하드코딩 + 마이그레이션/`fbUser` 완전 삭제: "여러 사용자가 쓰는 앱" 전제로 정리 (2026-08-27, 63차 세션)

- **"Firebase 연결 설정"(Database URL/Web API Key 수동 입력 칸)을 프로젝트 고정 상수로 하드코딩**: 이 값들은 애초에 "사용자가 자기 프로젝트를 골라 넣는" 설정이 아니라 이 앱이 접속할 단 하나의 고정 프로젝트(`study-fc3bf`)를 가리키는 값이었다 — 매번 두 기기(안드로이드/데스크탑)에 같은 값을 정확히 입력해야만 동기화가 되는 게 UX 마찰이자 오입력 위험이었다. 데스크탑 `Models.kt`의 `DEFAULT_FB_DATABASE_URL`/`DEFAULT_FB_API_KEY`, 안드로이드 `AppPreferences.kt`의 동일 이름 상수로 하드코딩하고 설정 화면 UI는 제거. 안드로이드 값은 `google-services.json`의 실제 등록된 Android 앱 API Key를 그대로 썼고, 데스크탑 값은 61차에 실제로 로그인 성공까지 검증됐던 기존 사용자 설정값을 그대로 상수화했다 — 두 플랫폼이 Firebase Console에 서로 다른 "앱"으로 등록돼 있어 API Key 값 자체는 다르지만, 같은 프로젝트(같은 `databaseUrl`)를 가리키므로 무방하다.
- **"기존 사용자 데이터 가져오기" 마이그레이션 기능을 완전히 삭제(사용자 명시적 요청, "이 앱은 여러 사용자가 이용하는 걸 가정")**: 61차에 로그인 필수 전환을 하면서 "예전 익명인증 시절 데이터를 새 로그인 계정으로 옮기는" 1회성 마이그레이션 버튼을 만들어뒀는데, 이 기능은 본질적으로 "이 기기를 쓰던 사용자는 한 명"이라는 전제 위에 서 있었다(레거시 텍스트 아이디 하나 → 지금 로그인한 uid 하나로 복사). 사용자가 이 앱을 여러 사람이 각자 쓰는 걸 전제로 하겠다고 명시하면서, UI(버튼/확인 다이얼로그/"예전 사용자 ID" 입력칸)와 구현 함수(`migrateLegacyDataToAccount`, 양 플랫폼)를 전부 삭제했다.
- **`fbUser` 필드/파라미터를 프로젝트 전체에서 완전 삭제 — 삭제 전 "이미 죽은 코드인지" 반드시 확인하고 진행**: 마이그레이션 삭제 작업 중 `PomodoroSyncClient.resolveIdentity(apiKey, fallbackUser)`를 다시 읽어보니, 함수 본문이 `GoogleAuthManager.currentUid`만 반환하고 `fallbackUser` 인자는 아예 참조하지 않고 있었다 — 61차에 로그인 필수로 전환하면서 이미 사실상 죽은 파라미터가 되어 있었는데 그동안 시그니처만 남아있었던 것. 삭제 전에 이 사실을 코드로 직접 확인(모든 호출부가 `resolveIdentity`를 거치고, 그 함수가 두 번째 인자를 안 쓴다는 걸 grep+읽기로 검증)한 뒤에 진행 — 짐작만으로 "안 쓰겠지"라며 지우지 않고, 실제로 안 쓰인다는 걸 확인한 뒤 지운 순서가 중요하다. 결과적으로 판정 로직(어떤 값을 반환하는지) 자체는 전혀 안 바뀌었고, 순수하게 죽은 매개변수 배관만 걷어낸 셈이라 위험도가 낮았다.
- **참고**: 61차 DECISIONS엔 "RTDB 보안 규칙 강화는 마이그레이션 기능이 열린 규칙에 의존하니 미룬다"는 결정이 있었는데, 63차에 그 마이그레이션 기능 자체가 삭제됐으므로 이 유예 사유는 더 이상 유효하지 않다 — 다음에 규칙 강화를 검토할 때 이 점을 참고할 것([[HANDOFF.md]] "다음 작업 우선순위"에도 반영).

---

## "모임"(소셜 그룹) 기능: 이름 분리, `groups/` 최상위 스키마, 키별 쓰기, 캐싱 없는 실시간 조회, Room 미사용, WorkManager 폴링 방식 깨우기 (2026-08-26, 62차 세션)

- **이름을 "그룹" 대신 "모임"으로 분리**: 기존 "관리" 탭의 앱/사이트 차단 대상이 이미 `Group`/`AppGroup`이라는 이름과 UI 문구 "그룹"을 쓰고 있어서, 새 소셜 기능을 그대로 "그룹"이라 부르면 화면에서 두 개념이 헷갈린다. 사용자에게 확인받아 새 탭/코드 모두 "모임"(`SocialGroup*`)으로 명명하고 완전히 별개 데이터 모델로 구현했다 — 기존 `GroupListScreen`/`GroupEditScreen`/`AppGroup` 등은 전혀 건드리지 않음.
- **Firebase 경로를 `users/{uid}/...` 밑이 아니라 최상위 `groups/{groupId}/...`에 둠**: 이 프로젝트의 기존 동기화 데이터(캘린더/계산기/루틴/스누즈 등)는 전부 "내 계정의 내 데이터"라 `users/{uid}` 밑이 자연스러웠지만, 모임은 애초에 여러 uid가 공유하는 데이터라 특정 한 사용자 경로 밑에 두면 소유권 모델이 꼬인다. 참여 코드(`inviteCodes/{code}: groupId`)로 역인덱스를 따로 둔 이유도 같은 맥락 — RTDB는 키를 모르면 자식을 조회할 수 없어서, "코드만 아는 사람이 그룹을 찾을 방법"이 별도로 필요했다.
- **멤버 목록/내 모임 목록을 배열이 아니라 키별(map) 쓰기로 설계**: [[DECISIONS.md]]에 이미 있는 34차 교훈("여러 기기가 각자 추가하는 데이터는 문서 전체 LWW 대신 기기별/항목별 키에만 쓰고 합산")을 그대로 적용 — `groups/{id}/members/{uid}`, `users/{uid}/socialGroupIds/{groupId}` 둘 다 개별 키 쓰기라, 여러 사용자(또는 한 사용자의 여러 기기)가 동시에 그룹에 가입해도 서로의 항목을 덮어쓰지 않는다. 반면 `stats/{uid}`는 그 uid 소유 기기만 쓰는 노드라 병합 로직(`mergedSnooze`류) 자체가 필요 없다고 판단해 안 만들었다.
- **모임 목록/멤버/통계는 로컬에 캐싱·영속화하지 않고 화면 진입 시마다 Firebase에서 직접 읽음**: 캘린더/계산기/루틴처럼 "로컬 우선 + 동기화"(LWW 비교) 구조를 그대로 복제하면 병합 로직이 새로 필요해지고, 애초에 다인원 실시간 데이터라 로컬 캐싱의 이득도 적다. 대신 로컬엔 "내가 켠 공유 항목"과 "마지막으로 본 넛지 시각"만 남긴다 — Room DB 스키마도 전혀 손대지 않음(버전은 27 그대로, 마이그레이션 리스크 회피).
- **"깨우기"는 FCM 대신 Firebase RTDB 신호 + 기존 로컬 알림 인프라 재사용(사용자 승인)**: 진짜 실시간 푸시를 하려면 FCM 서버 키/토큰 관리/수신 채널을 새로 구축해야 해서 이번 범위에 비해 과했다. 대신 `groups/{id}/nudges/{targetUid}`에 최신값 1건만 쓰고(과거 `confirmSync`/`snoozeSync`와 동일한 "최신값 비교" 패턴), 받는 쪽은 이미 있는 주기적 폴링 지점에 얹었다 — 데스크탑은 `Main.kt`의 기존 30초 tick 루프, 안드로이드는 `AccessibilityWatchdogWorker`와 나란한 새 `PeriodicWorkRequestBuilder`(WorkManager 하한 15분). **트레이드오프**: 안드로이드는 최대 15분 지연이 발생할 수 있어 "즉시 깨우기"로는 부족함 — 사용자에게 사전 고지했고, 진짜 실시간이 필요해지면 FCM 도입을 [[IDEAS.md]]에 후속 과제로 남김.

---

## Google 로그인 동기화 아키텍처: 안드로이드는 Firebase Auth SDK, 데스크탑은 자체 루프백 OAuth + `resolveIdentity()` 캡슐화 (2026-08-25~26, 61차 세션)

- **안드로이드는 Firebase Auth SDK를 새로 도입, 데스크탑은 SDK 없이 직접 구현**: 이 프로젝트는 원래 Firebase RTDB를 순수 REST(익명 인증 토큰 직접 관리)로만 써왔는데, 안드로이드는 Credential Manager+`firebase-auth` SDK를 새로 추가하는 게 훨씬 안전하다고 판단했다(토큰 자동 갱신/세션 영속화를 SDK가 대신 해줌, 직접 구현하면 실수하기 쉬운 영역). 반면 데스크탑(Kotlin/Compose Desktop)엔 그런 SDK가 없어 표준 "설치된 앱" OAuth 흐름(PKCE + 로컬 루프백 리다이렉트, RFC 8252)을 `com.sun.net.httpserver.HttpServer`로 직접 구현했다 — 이미 `LocalApiServer.kt`가 같은 클래스로 로컬 API 서버를 띄우고 있어 새 의존성 없이 재사용 가능했다. 세션(uid/email/refreshToken)은 SDK가 없으니 `data.json`과 같은 디렉터리의 `google_auth.json`에 직접 저장.
- **기존 REST 함수들과의 통합은 `resolveIdentity()` 하나로 캡슐화, 각 함수 본문은 최소 변경**: `PomodoroSyncClient`의 15개 이상 함수가 전부 `val token = ensureIdToken(apiKey) ?: return ...` 패턴을 반복하고 있었는데, 이 한 줄을 `val (token, user) = resolveIdentity(apiKey, user) ?: return ...`로만 바꿔서(반환하는 `user`가 기존 파라미터명을 그대로 shadow) 나머지 URL 문자열 등 함수 본문은 전혀 안 건드렸다. 함수 시그니처(`databaseUrl`/`apiKey`/`user` 파라미터)도 그대로 유지해 `Repository.kt`/`PhoneLockRepository.kt`의 15개+ 호출부는 단 한 줄도 안 고쳤다 — 새 기능을 기존 아키텍처에 최대한 안 건드리고 얹는 방식을 택함.
- **기존 "익명 인증 + 사용자ID 텍스트" 동기화 방식을 완전히 삭제하고 로그인 필수로 전환(사용자 명시적 요청)**: 처음엔 로그인 여부와 무관하게 동작하는 하위호환 fallback으로 설계했으나("로그인 안 한 지인은 기존 방식 그대로"), 사용자가 "이제 기존 방식은 없어져야 하지 않겠어"라고 요청해 `TokenCache`/`ensureIdToken`/`signInAnonymously`/`refreshIdToken`(양 플랫폼)을 전부 삭제하고 로그인 필수로 확정. **트레이드오프를 사전에 명시하고 사용자 승인을 받음**: 이 저장소 밖의 별도 웹앱 "공부앱"이 여전히 옛 텍스트 경로(`users/{텍스트}/pomodoro`)에 뽀모도로 상태를 쓰기 때문에, 그 앱이 Google 로그인으로 전환되기 전까지 "뽀모도로 휴식 시 자동 해제" 연동은 끊긴다 — 별도 프로젝트라 이 저장소에서 고칠 수 없는 부분이라 사전에 확인받고 진행.
- **RTDB 보안 규칙 강화(auth.uid 기준으로 제한)는 이번 범위에서 제외**: 로그인 없이도 익명 인증 토큰만 있으면 아무 `users/*` 경로나 읽고 쓸 수 있는 열린 규칙이 여전히 유지된다. 마이그레이션(기존 데이터를 uid 경로로 복사) 기능이 바로 이 열린 규칙을 이용해 동작하므로(로그인한 사용자가 자기 uid가 아닌 예전 경로를 읽어와야 함), 규칙을 먼저 잠그면 마이그레이션 자체가 막힌다 — 그래서 규칙 강화는 사용자들이 충분히 이전을 마친 뒤의 후속 작업으로 미룸.

---

## `data.json` 등 앱 저장 파일은 절대 PowerShell JSON 왕복 파싱으로 수정하지 않는다 (2026-08-23, 60차 세션)

- **"그룹 모두 꺼줘" 요청을 처리하며 `%APPDATA%\PhoneLockDesktop\data.json`을 PowerShell `ConvertFrom-Json`/`ConvertTo-Json`으로 왕복 편집했다가 153KB 파일이 1.2KB로 유실되는 사고가 났다** — Windows PowerShell 5.1의 JSON 왕복이 중첩 배열(특히 원소 1개짜리 배열의 스칼라화)을 안정적으로 보존하지 못하는 게 원인. `JsonStore.kt`가 파싱 실패 시 전체를 빈 `AppData()`로 대체하는 구조라 일부만 깨져도 앱에선 데이터 전체가 초기화된 것처럼 보일 수 있다. 자세한 경위는 [[BUGS.md]] 60차 참고.
- **재발 방지 원칙**: 이 프로젝트의 저장 파일(`data.json`, 안드로이드 SharedPreferences export 등)에서 특정 필드 값만 바꿔야 할 때는 **원본 텍스트를 파싱하지 않고 정확한 문자열/정규식 치환만 사용**한다(치환 전후 개수·파일 크기 변화를 확인해 검증). 구조 전체를 다시 쓰는 재직렬화는 하지 않는다. 편집 전엔 항상 백업을 먼저 만든다(이번엔 다행히 편집 직전 백업이 있어 즉시 복구 가능했다).

---

## 루틴 스트릭 알림: "정각 일치" 비교를 "목표 시각 이후 && 오늘 미발송"으로, 발송 시각은 고정 정각에서 랜덤으로 (2026-08-21, 58차 세션)

- **데스크탑 `RoutineNotifier.tick()`이 `now.hour == dailyResetHour && now.minute == 0`처럼 "정확히 그 순간"만 비교하던 걸 `!now.isBefore(target) && lastStreakNotifyDate != todayKey`(이상 비교)로 바꿨다**: 30초 주기 틱 루프에서 "정각==지금 분" 같은 정확한 일치 비교는 그 1분 창을 놓치거나(틱 타이밍 드리프트) 그 순간 앱이 꺼져 있으면 그날은 영영 못 울리는 구조였다. 안드로이드는 애초에 `AlarmManager` 예약이라 이 문제가 없었는데, 데스크탑만 이 취약점이 있었던 것 — 앞으로 데스크탑에서 "특정 시각에 한 번" 발생해야 하는 기능을 tick 루프로 짤 땐 항상 부등호 비교(지났는지)를 쓰고 "오늘 이미 처리했는지" 별도 플래그로 중복을 막을 것, 정확한 일치 비교는 쓰지 않는다.
- **발송 시각을 `dailyResetHour` 정각 고정에서 매일 완전 랜덤 시각으로 변경(사용자 요청)**: 예측 가능한 정각 발송이 무시되기 쉽다는 이유. 안드로이드 `RoutineAlarmScheduler.scheduleStreakCheck()`에서 `hour` 파라미터를 아예 없애고 `(0..23).random()`/`(0..59).random()`으로 다음 트리거를 정하는 방식으로 바꿨다(다음 알림도 발송 시점에 다시 랜덤으로 재계산). 데스크탑도 `randomTimeAfter(now)`로 대칭 구현.
- **스트릭 0 지속을 "끊김 여부"(Boolean) 하나가 아니라 "며칠째 0인지"(Int, `zeroStreakDays`)로 확장**: 응원(1~2일)→조롱(3~6일)→팩폭(7일+) 3단계 메시지 강도를 위해 새 영속 필드가 필요했다. 안드로이드는 `AppPreferences`(SharedPreferences) 하나만 고치면 끝이지만, 데스크탑은 `Models.kt`(AppData 필드)+`Repository.kt`(프로퍼티)+`JsonStore.kt`(parse+save 둘 다) 4곳을 전부 고쳐야 하는 구조라는 걸 다시 확인(HANDOFF "두 저장 경로 항상 같이 확인" 원칙, 36차/54차에 이미 겪은 유형).

---

## 스누즈 예외는 LockEvaluator의 기존 "기간지정 우선" 패턴을 재사용, 관리 종류 우선순위는 "잠금 판정을 확인 판정보다 먼저 검사"로 구현 (2026-08-14, 55차 세션)

- **스누즈 중 편집 자유화는 새 조건을 설계하지 않고 `isCurrentlyRestricting()`/`evaluate()`가 이미 쓰던 `!isForceEnabled(group) && isSnoozed(group)` 예외를 `detectWeakeningEdit()`에도 그대로 복제**: 판정 함수마다 스누즈/기간지정 상호작용을 각자 다르게 짜면 나중에 어긋나기 쉬우므로, "기간 지정이 스누즈보다 우선한다"는 39~41차 원칙을 그대로 재사용했다. 삭제 버튼은 이미 `isCurrentlyRestricting()`을 직접 호출해서 별도 수정이 필요 없었음 — 편집 저장 경로(`detectWeakeningEdit`)만 예외가 빠져 있던 것.
- **스케줄/한도/실행확인 우선순위는 새 판정 함수를 만들지 않고 두 기존 호출의 순서만 재배치**: `evaluate()`(스케줄→한도 순으로 이미 내부 정렬됨)와 `isConfirmActiveNow()`는 원래도 서로 독립적인 판정이라, "잠금 조건이면 확인창 없이 곧바로 차단"이라는 요구는 로직 자체가 아니라 호출 순서 문제였다. 안드로이드 2곳(`tickInternal`/`checkSitesInternal`)과 데스크탑 2곳(`EnforcementService.decide`/`SiteEnforcement.check`+`tick`) 모두 같은 재배치를 대칭 적용 — 새 판정 로직을 추가하면 4곳이 서로 다르게 구현될 위험이 있어 순서만 바꾸는 최소 변경을 택함.

## 로컬 영속화(JsonStore/Room)와 Firebase 동기화 양쪽에 저장되는 모델은 필드 추가 시 두 경로를 항상 함께 검토 (2026-08-14, 54차 세션)

- **문제의 본질**: `Routine`처럼 "로컬 파일에도 저장하고 Firebase에도 동기화하는" 모델에 새 필드를 추가하면, Repository/Firebase 직렬화 함수(`routinesToJsonArrays` 등)만 고치고 로컬 저장 파일(`JsonStore.kt` save()/parse())은 빠뜨리기 쉽다 — 36차의 `pomodoroModeEnabled`와 54차의 `Routine.icon`/`notifyEnabled`/`startDate`/`endDate` 둘 다 같은 실수였다. 두 경로가 서로 다른 파일에 각자 필드 목록을 나열하는 구조라 컴파일러가 누락을 잡아주지 않는다.
- **54차에 새로 드러난 함정 — LWW 타임스탬프가 "우연히 정확히 같아지면" 버그가 스스로 안 고쳐진다**: `pushRoutinesToFirebase()`는 push 시점에 로컬 `routinesTs`도 원격과 동일한 값으로 맞춰 쓴다. 로컬 파일 저장에서 필드 하나가 빠져 있어도 타임스탬프는 정확히 같으므로, 재시작 후 `syncRoutinesFromFirebase()`의 LWW 비교(`result.ts > data.routinesTs`/`data.routinesTs > result.ts`)가 둘 다 거짓이 되어 영구히 재동기화가 안 일어난다 — 코드를 고쳐 배포해도 "이미 망가진 상태로 저장된" 기존 로컬 데이터는 저절로 복구되지 않는다는 뜻. 이번엔 앱 종료 후 로컬 `routinesTs`를 0으로 수동 리셋해서 강제로 원격 재수신을 유도했다(52차 `resetSyncTimestamps()`와 같은 종류의 처방).
- **앞으로의 원칙**: (1) 로컬+원격 양쪽에 저장되는 모델에 필드를 추가할 땐 직렬화 함수 목록을 grep해서 저장/복원 두 지점 모두 확인할 것(캘린더/계산기도 같은 구조라 동일 위험). (2) "push할 때 로컬 타임스탬프도 원격과 맞춰 쓰는" 패턴(`pushXxxToFirebase()`류)을 쓰는 모든 기능은 이런 필드 누락이 생기면 자동 감지가 안 된다는 걸 알고, 필드 추가 후엔 실제로 재시작해서 값이 살아남는지 한 번 확인하는 습관을 들일 것. [[BUGS.md]] 54차 참고.

## 계산기 데이터 복구 + 테마 전수 확장(위젯/브라우저 확장) + 루틴 알림/내보내기 핵심 결정들 (2026-08-14, 53차 세션)

- **계산기 데이터 복구는 "타임스탬프 강제 갱신으로 정상 push 경로를 재사용"하는 방식을 택함**: 손상된 Firebase 문서를 REST PATCH로 직접 덮어쓸 수도 있었지만, 앱이 이미 갖고 있는 push 로직(스키마 직렬화, 폴더 트리 재구성 등)을 그대로 타면 수작업으로 JSON을 구성하다 스키마를 놓칠 위험이 없다 — 데스크탑 로컬 `calcSavedTs`/`calcFolderTs`만 강제로 최신화해 다음 동기화 시 "로컬이 더 최신"으로 판정되게 만들었다. 다만 `pushCalcTasksAndSaved()`가 `savedTs` 필드까지는 갱신하지 않는 걸 뒤늦게 발견해 그 필드 하나만 REST PATCH로 별도 보정 — 앞으로 비슷한 유실 복구 시 "앱의 기존 쓰기 경로를 최대한 재사용하되, 부분 갱신 함수가 모든 관련 타임스탬프 필드를 다 건드리는지 직접 검증할 것"을 교훈으로 남김.
- **테마 하드코딩 버그는 "secondaryContainerColor에 리터럴 Color를 직접 넘긴 곳"이 공통 패턴이었음**: 49차에 라이트+그린 전환 시 앱 전체 accent를 찾아 바꿨다고 기록했지만, `InterstitialScreen`/`WatchAndWaitScreen`의 `secondaryContainerColor` 파라미터로 개별 화면이 넘기는 리터럴은 grep 패턴(`0xFF8BC34A`)에 걸리지 않고 넘어갔던 것으로 보인다 — 앞으로 "테마 색이 하드코딩 없이 전부 따라가는지" 검증할 땐 팔레트 하나(예: primary hex)로 grep하는 것만으로는 부족하고, `MaterialTheme.colorScheme`을 거치지 않는 모든 `Color(0xFF...)` 리터럴을 훑어야 한다는 걸 확인.
- **RemoteViews(홈 위젯)는 Compose 테마를 못 타므로 테마별 드로어블을 미리 만들어두고 런타임에 리소스 ID만 스위칭**: Jetpack Glance 없이 classic RemoteViews로 만든 50차 설계를 그대로 유지하면서(새 Gradle 의존성 회피 원칙 계속 적용), 배경/아이템행/체크아이콘 각각 3세트(테마별) XML을 만들고 `AppPreferences.themeMode`를 읽어 `setInt(id, "setBackgroundResource", resId)`로 갈아끼우는 방식을 택함 — RemoteViews가 `setBackgroundColor`로 색만 바꾸면 둥근 모서리/테두리(shape drawable의 나머지 속성)를 잃어버리기 때문에 색상 하나만 넘기는 API 대신 리소스 자체를 스위칭했다.
- **브라우저 확장 테마 연동은 새 로컬 API 엔드포인트(`/theme`) + CSS 커스텀 프로퍼티 조합으로, 팔레트 정의는 JS에 별도 복제**: 확장은 Kotlin 코드를 공유할 수 없는 완전히 다른 런타임이라 `THEME_PALETTES`를 `theme.js`에 그대로 다시 적어야 했다(Color.kt/PhoneLockPalette와 값만 동일하게 유지) — `overlay-status`가 이미 쓰던 "content script가 임의 페이지 출처에서 호출하므로 openCors" 패턴을 그대로 재사용해 새 조사 없이 안전하게 열었다. `:root`에 앱 기본 테마(라이트+그린) 값을 기본으로 박아둬서, 데스크탑 앱과 통신 전/실패 시에도 완전히 깨진 화면 대신 합리적인 기본 테마가 보이게 했다.
- **루틴 알림을 헤드업(플로팅)으로 바꾸려고 기존 채널의 importance를 코드로 못 올린다는 안드로이드 제약을 새 채널 ID 발급으로 우회**: `NotificationChannel`은 한 번 만들어지면 이후 코드에서 `importance`를 재정의해도 이미 설치된 기기엔 반영이 안 된다(사용자가 시스템 설정에서 직접 바꿔야 함) — `routine_reminder`→`routine_reminder_v2`로 새 채널 ID를 발급해 새 기본값(HIGH+진동)을 강제 적용했다. 이 프로젝트에서 알림 채널 설정을 다시 바꿔야 할 일이 생기면 이 패턴(채널 ID 버전업)을 우선 검토할 것.
- **루틴 내보내기/가져오기는 그룹 백업과 별도 스키마 대신 기존 Firebase 동기화 문서 스키마를 그대로 재사용**: 새 파일 포맷을 설계하는 대신 이미 있는 `routinesToJson`/`routinesFromJson`(51차, 배열 인덱스 참조 방식)을 로컬 파일 입출력에도 그대로 연결 — 스키마가 하나뿐이라 유지보수 부담이 줄고, 가져온 파일이 마치 방금 다른 기기에서 동기화된 것처럼 자연스럽게 Firebase에도 재푸시된다.
- **테마 5종 추가는 새 팔레트 구조 없이 기존 `PhoneLockPalette` 데이터클래스에 값만 채워 넣는 방식**: 50차에 이미 `ThemeMode`+`PhoneLockPalette`+`paletteFor()` 구조로 재구조화돼 있어서, 사용자가 요청한 "예쁠 것 같은 테마"를 위해 라이트 3종(라벤더/민트/로즈)과 다크 2종(미드나잇/포레스트)의 hex 값만 새로 고르고 `THEME_DISPLAY_NAMES`(신규, 표시 이름 순서 목록)로 UI가 순회하게 했다 — 구조 변경 없이 값만 추가하는 확장이 가능하다는 게 50차 리팩터링의 설계 의도가 실제로 들어맞은 사례. onPrimary/onAccent는 각 팔레트의 primary 밝기에 따라 명도 대비가 되는 쪽(밝은 파스텔 계열은 어두운 텍스트, 진한 틸/핑크 계열은 흰 텍스트)으로 개별 판단했다.
- **테마 선택 UI가 항목 수 증가로 넘칠 때는 FlowRow로 통일해서 해결(다시 확인)**: 50차 요일 피커/계산기 연동 버튼에 이어 이번에도 "고정 개수를 가정한 Row"가 항목이 늘자 잘리는 문제였다 — 항목이 가변(테마도 나중에 더 늘 수 있음)이라 이번에도 `FlowRow` 선택, `weight` 균등분할은 고려하지 않았다(칩 너비가 다 달라 균등분할이 안 어울림). 안드로이드만 신고됐지만 데스크탑도 향후 창을 좁히면 같은 문제가 재현될 걸 예상해 함께 통일.
- **태블릿 분기 기준은 44차에 확립한 `screenWidthDp >= 600`을 계속 재사용**: 새 기준을 만들지 않고 `InterstitialScreen.kt`의 태블릿 판정 그대로 `StudyStatsScreen.kt`/`RoutineScreen.kt`에도 적용 — 이 프로젝트에서 "태블릿이냐 폰이냐"를 판단할 일이 생기면 항상 이 상수를 재사용할 것.

---

## 루틴 순서/복사/아이콘/알림/기간 + 동기화 버그 대응 핵심 결정들 (2026-08-14, 52차 세션)

- **루틴 순서 변경은 드래그 대신 기존 ▲/▼ 버튼 패턴**: 계산기 업무/저장됨/폴더, 캘린더 일정 등 이 프로젝트의 모든 순서변경 UI가 이미 ▲/▼ 버튼으로 통일돼 있고(진짜 드래그 제스처는 프로젝트 어디에도 없음), 루틴 백엔드에도 48차에 이미 같은 방식의 `moveRoutineOrder()`가 있었지만 UI 연결만 안 된 상태였다. 사용자에게 확인받아 기존 패턴을 그대로 따름(진짜 드래그는 새 제스처 인프라를 안드로이드/데스크탑 양쪽에 처음 도입해야 해서 리스크·작업량 모두 큼).
- **순서 버튼은 시간대 미지정 루틴에만 표시**: "오늘" 탭은 시간대 지정 루틴을 항상 시간순으로 먼저 보여주므로, 그 항목들의 순서를 수동으로 바꿔도 화면이 즉시 시간순으로 재정렬되어 버튼을 눌러도 효과가 안 보인다. 그래서 ▲/▼는 시간대 없는(순서가 곧 사용자 의도인) 루틴에만 붙였다 — `moveRoutineOrder`(전역 sortOrder 인접 스왑)를 그대로 쓰면 시간대 지정 루틴과 뒤섞여 의도치 않은 자리와 스왑될 수 있어, 화면에 실제로 보이는 "시간대 없는 루틴" 부분집합 안에서 이웃을 찾아 스왑하는 `swapRoutineOrder(idA, idB)`를 새로 추가했다. 나중에 "시간대 지정 루틴도 순서를 직접 바꾸고 싶다"는 요청이 오면, 시간순 자동정렬을 포기하고 sortOrder 전면 전환 vs 같은 시각끼리만 순서 변경 중 선택이 필요(사용자에게 물어봤으나 이번엔 "없던 일로" 결정, 현재 방식 유지).
- **그룹/루틴 복사는 진행 중 상태를 초기화**: 그룹 복사 시 스누즈/기간강화/차단시도횟수/회유절차진행상태를 리셋하고 멤버·사이트는 그대로 복제(`copyGroup`). "복사"는 설정 재사용이 목적이지 진행 중이던 제한 상태까지 복제할 이유가 없다고 판단.
- **루틴 신규 필드(아이콘/알림/기간) 4개를 한 번에 추가해 Room 버전업을 1회로 묶음**: `icon`/`notifyEnabled`/`startDate`/`endDate`를 각 기능마다 따로 추가하면 `fallbackToDestructiveMigration()`이 매번 로컬 DB를 통째로 지운다 — 이번에 4개 요청을 한 세션에서 순서대로 처리하기로 했으므로, 스키마 변경은 한 번(version 26→27)으로 몰아서 destructive migration 횟수를 최소화했다. **그런데 이 버전업 자체가 심각한 동기화 버그를 드러냄** — 아래 항목 참고.
- **데스크탑 루틴 알림은 새 TrayIcon 대신 기존 Tray의 TrayState.sendNotification() 재사용**: Compose Desktop의 `Tray` composable이 이미 트레이 아이콘을 하나 띄우고 있어서, 알림용으로 `java.awt.SystemTray`에 별도 `TrayIcon`을 추가하면 트레이에 아이콘이 두 개로 보이는 눈에 띄는 문제가 생긴다. 대신 `Tray(state = trayState, ...)`로 `TrayState`를 넘겨받아 `DesktopNotifier` 싱글턴에 등록해두고, Composable이 아닌 곳(`RoutineNotifier.tick()`)에서도 같은 트레이 아이콘으로 풍선 알림을 띄우게 했다.
- **안드로이드 루틴 알림은 정확한 예약(`setExactAndAllowWhileIdle`) 대신 `setAndAllowWhileIdle`**: 습관 리마인드는 몇 분 늦어도 치명적이지 않은 용도라, Android 12+에서 별도 권한(`SCHEDULE_EXACT_ALARM`)과 사용자 승인 플로우가 필요한 정확한 알람 대신 권한 불필요한 부정확 알람을 선택 — 매니페스트에 새 권한을 안 늘려도 된다.
- **Room 버전을 올릴 땐 SharedPreferences에 저장된 동기화 타임스탬프도 같이 고려해야 함(52차 발견, 중요)**: `fallbackToDestructiveMigration()`은 Room(SQLite)만 지우고 `AppPreferences`(SharedPreferences)는 안 건드린다. 그런데 캘린더/루틴/계산기의 "마지막 동기화 시각"(`calendarTs`/`routinesTs`/`calcTasksTs`/`calcSavedTs`/`calcFolderTs`/`calcFolderOrderTs`)이 전부 SharedPreferences에 있어서, DB가 지워져도 이 값들은 그대로 남는다 — 다음 실행의 LWW 비교(`원격 ts > 로컬 ts`)가 "로컬이 이미 최신"으로 오판해 텅 빈 로컬 상태를 그대로 방치(최악의 경우엔 로컬이 원격보다 timestamp가 앞서 있어 빈 데이터를 원격에 push해버릴 위험도 있었음, 이번엔 실제로 그렇게까진 안 갔음). **해결책은 "DB가 지워졌다"는 신호(`PreMigrationBackup.backupIfVersionChanged()`가 non-null 반환)를 받으면 그 즉시 관련 타임스탬프를 전부 0으로 리셋하는 것**(`AppPreferences.resetSyncTimestamps()`, `MainActivity.onCreate`에서 호출) — 이러면 다음 동기화가 무조건 "로컬이 원격보다 오래됨"으로 판정돼 안전하게 다시 받아온다. **앞으로 Room 버전을 올릴 때마다 이 리셋이 자동으로 같이 실행되므로 재발 안 함** — 새로운 SharedPreferences 기반 동기화 타임스탬프를 추가할 땐 `resetSyncTimestamps()`에도 같이 추가할 것.
- **그룹(차단 대상 목록)은 Firebase 동기화 대상이 아니라서 DB 초기화로 실제 유실됨 — 별도 복구 경로 신설**: 기존 "설정/그룹 내보내기·가져오기" 기능은 사용자가 사전에 수동으로 내보내둔 파일이 있어야 하는데, 이번 사고는 사용자가 그 백업을 미리 만들어두지 않은 상태였다. 대신 `PreMigrationBackup`이 DB 초기화 직전 자동으로 남기는 raw SQLite 덤프(`{table: [row,...]}`, 기존 내보내기와는 다른 포맷)를 그대로 활용하는 `PhoneLockRepository.restoreGroupsFromBackup()`을 신설 — SQLite BOOLEAN이 0/1 정수로 저장되는 것만 변환하면 나머지 필드는 그대로 매핑 가능했다. 원본 그룹 id를 유지해서 `group_member`/`usage_record`/`confirm_escalation`/`confirm_counter`의 groupId 참조가 깨지지 않게 했다. 설정 화면에 "⚠ 그룹 데이터 복구" 카드로 노출(백업 파일이 있을 때만 표시).

---

## 루틴앱 v2 개편 핵심 결정들 (2026-08-13, 50차 세션)

49차까지의 v1을 실사용 관점에서 다듬으며 확정한 결정들.

- **개별 방어권 설정 대신 전역 "주 2회" 고정**: v1은 루틴별로 방어권 종류(주/월)와 횟수를 편집 화면에서 직접 입력받았는데, "일정 하나만 못해도 스트릭이 깨지는" 걱정을 해소하려면 사용자가 매번 이 값을 튜닝하게 두는 것보다 합리적인 기본값 하나로 고정하는 게 낫다고 판단 — 주 2회로 고정하고 편집 UI 자체를 제거했다. 나중에 "이 루틴은 방어권 없이 빡세게" 같은 요구가 나오면 그때 예외 옵션을 다시 추가하는 방향으로 남겨둠.
- **Compose 리스트 재할당이 구조적으로 동일하면 리컴포지션을 스킵한다**: 데스크탑 루틴 체크박스가 "다른 탭 갔다와야 반영"됐던 버그의 원인 — `RoutineLog` 토글 후 `mutableStateOf<List<Routine>>`을 새 리스트로 재할당해도, 값 자체(Routine)는 안 바뀌었으니 Compose가 "달라진 게 없다"고 보고 리컴포지션을 건너뛴다. 이미 그룹 탭에서 같은 유형의 버그를 겪어 도입했던 `refreshTick: Int`(항상 증가)를 `key(refreshTick){...}`으로 감싸는 패턴을 그대로 재사용해 해결 — 앞으로 "체크박스/토글 상태가 즉시 안 바뀌는데 다른 화면 갔다 오면 맞다" 유형의 버그를 보면 이 패턴부터 의심할 것.
- **가변폭 리스트를 좁은 화면에 맞출 땐 스크롤보다 줄바꿈(FlowRow)이나 균등분할(weight)을 우선 검토**: 안드로이드 요일 피커(금/토 안 보임)와 계산기 연동 업무 선택 버튼(항목 많으면 안 보임) 둘 다 처음 `horizontalScroll`로 구현했다가 "여전히 안 보인다"는 재현을 받았다. 요일처럼 항목 수가 고정(7개)이면 `weight(1f)` 균등분할로 스크롤 자체를 없애고, 계산기 업무처럼 항목 수가 가변이면 `FlowRow`로 여러 줄 자동 줄바꿈하는 쪽이 이 사용자에게는 스크롤보다 훨씬 신뢰할 수 있는 해법이었다 — 앞으로 비슷한 "다 안 보인다" 류 버그는 스크롤 추가보다 이 두 대안을 먼저 검토할 것.
- **8단계 회독 간격은 에빙하우스 망각곡선/spacing effect 기반 지수 증가로 확정**: 처음엔 1,1,3,7,14,30,60일로 짰다가 사용자가 과학적 근거를 재검토해달라고 요청 — 연속된 1일 간격(백→빨강, 빨강→주황)이 spacing effect(간격이 매번 늘어나야 장기기억에 효과적)를 어겨 1,3,7,14,30,60,120일(매 단계 ~2~2.3배)로 재조정했다. SuperMemo/Anki의 ease factor(2~2.5배)와 같은 원리.
- **회독 색상 재배정은 재라벨링이지 데이터 마이그레이션이 아님**: 8색으로 확장하면서 `color="red"`가 이제 1회독이 아니라 2회독을 뜻하게 되는 등 매핑 자체가 바뀌었다. 저장된 `CalendarTask.color` 문자열은 손대지 않고 `COLOR_LABEL` 매핑 테이블만 바꿨기 때문에, 과거에 만든 일정은 이번 변경 이후 다른 회독 번호로 "보이게" 된다 — 데이터 손상이 아니라 의도된 재라벨링이며, 사용자에게 이 트레이드오프를 명시적으로 알리고 진행 승인을 받았다.
- **계산기-캘린더 연동 트리거 색을 웹앱의 "red" 대신 "white"로 적응**: 원본 웹앱은 4단계 체계라 1회독이 "red"였고 연동 차감 트리거가 그 색을 기준으로 했다. 이번 8단계 체계에선 1회독이 "white"로 이동했으므로, 이식한 `setCalendarTaskStatus`의 연동 트리거 조건도 `color == "white"`로 맞춰 옮겼다 — 원본 로직의 "표현 의도"(1회독 완료 시 차감)를 따르는 게 원본 코드의 리터럴 색상값을 그대로 옮기는 것보다 맞다고 판단.
- **루틴 Firebase 동기화는 배열 인덱스 참조로 ID 충돌 회피**: 안드로이드는 Room `@PrimaryKey(autoGenerate=true)`, 데스크탑은 수동 카운터라 같은 루틴이 기기마다 다른 로컬 ID를 가질 수 있다. `RoutineLog`가 로컬 ID로 부모 루틴을 참조하는 기존 구조를 그대로 Firebase에 올리면 다른 기기에서 잘못된 루틴에 로그가 매핑될 위험이 있어, Firebase 문서에서는 루틴을 배열로 직렬화하고 로그는 "몇 번째 루틴인지"(인덱스)로 참조하도록 설계(`routinesToJson`/`routinesFromJson`) — pull 시 로컬 ID를 순서대로 새로 배정하며 로그를 재연결한다. 캘린더/계산기와 같은 "전체 문서 LWW" 패턴 위에 이 인덱스 참조만 추가한 것.
- **안드로이드 홈 위젯은 Jetpack Glance 대신 classic RemoteViews**: Glance를 쓰면 신규 Gradle 의존성이 필요해 빌드 툴체인이 불안정한 이 프로젝트 세션 환경([[project_build_toolchain_missing]] 참고)에서 리스크가 커진다고 판단 — 이미 프로젝트에 익숙한 `AppWidgetProvider`/`RemoteViewsService`/`RemoteViewsFactory`/`PendingIntent` 조합으로 구현했다.
- **테마를 3종 선택제로**: 49차에 라이트+그린으로 완전히 갈아엎었던 걸 사용자가 "이전 다크+파랑, 그리고 맨 처음 화이트+오렌지도 선택지로 남겨달라"고 요청 — `Color.kt`를 `ThemeMode` 상수+`PhoneLockPalette` 데이터클래스+팔레트 3개 val로 재구조화해 세 톤을 동등하게 전환 가능한 구조로 바꿨다. 앞으로 새 테마가 또 필요해지면 팔레트 하나만 추가하면 됨.

## 루틴 편집은 별도 화면이 아니라 다이얼로그로, 라이트+그린 정확한 톤은 "밝은 연두"로 확정 (2026-08-13, 49차 세션)

48차 데이터 계층 위에 화면을 얹으면서 두 가지를 결정했다.

- **루틴 편집을 그룹 편집처럼 좌우 분할 전체 화면이 아니라 `AlertDialog`로**: `GroupEditScreen`은 필드가 많고(스케줄/일일한도/실행확인 3개 섹션 각각 요일마스크+시간대) 그룹 목록과 나란히 두고 비교하며 편집할 이유가 있어 좌우 분할이 맞았다. 루틴은 필드가 5개(제목/시간대/요일/스트릭 추적/방어권)뿐이라 같은 구조를 그대로 가져오면 화면 전환 비용만 늘어난다고 판단 — 다이얼로그로 오늘/일과표/습관 어느 탭에서나 같은 방식으로 추가/수정할 수 있게 했다.
- **그린 톤은 사용자에게 후보 3개(포레스트/에메랄드/세이지)를 제시했으나 전부 기각, "더 밝은 연두"로 재요청받아 확정**: 47차에 "정확한 그린 톤은 착수 시 후보 제시"로 남겨뒀던 결정을 이번에 마무리 — Primary `#8BC34A`(Material Light Green 500 계열), 배경은 순백 대신 옅은 그린 틴트 오프화이트 `#FAFBF6`. 이 밝기의 초록 위에 흰 텍스트를 올리면 대비가 나빠서(Material 가이드도 Light Green 500 위엔 어두운 텍스트를 권장), `onPrimary`를 흰색 대신 짙은 그린빛 다크 톤(`#20261A`)으로 잡았다 — 이 다크 톤은 동시에 `onBackground`(본문 텍스트)로도 재사용해서 색 종류를 늘리지 않았다.
- **테마 전환 시 `MaterialTheme.colorScheme`을 안 쓰는 하드코딩된 accent도 같이 찾아 바꿈**: 차단/실행확인 화면과 사용 중 오버레이의 강조색(옛 파란 `#4F8EF7`, 35차에 주황에서 교체됐던 값)은 원시 `Color(0xFF...)` 리터럴로 박혀 있어 `Theme.kt`만 바꿔선 자동으로 안 따라온다 — grep으로 전부 찾아 새 Primary로 통일했다(안드로이드/데스크탑/브라우저 확장 `overlay.js` 포함). 다만 브라우저 확장 자체의 다크 팔레트(`confirm.html`/`blocked.html`/`onboarding.html`, 35차 통일)는 "앱 전체"를 네이티브 Compose 앱 기준으로 좁게 해석해 이번 범위에서 제외 — 확장 전체를 라이트로 바꾸는 건 다크 CSS 변수 체계를 통째로 다시 설계해야 하는 별도 규모의 작업이라 별도 요청이 필요하다고 판단.
- **전수 색상 감사는 하지 않음**: 캘린더/통계 화면에 흩어진 회독 단계색(빨강/주황/노랑/초록)이나 요일 강조색(일요일 빨강/토요일 파랑) 같은 의미론적(semantic) 색상은 다크 배경이든 라이트 배경이든 대체로 무난히 읽혀서 이번엔 손대지 않았다 — 발견된 유일한 실제 대비 문제(캘린더 "오늘" 배지의 흰 텍스트가 밝은 초록 배경 위에서 잘 안 보이던 것)만 `onPrimary`로 고쳤다. 나머지는 실사용 후 눈에 띄는 문제가 있으면 그때 개별적으로 수정하는 쪽이 낫다고 판단(CLAUDE.md "변경 범위 최소화" 원칙).

## 루틴앱 스트릭 방어권을 저장 카운터가 아니라 매번 재계산하는 순수 함수로 구현 (2026-08-13, 48차 세션)
47차 설계는 방어권을 "주기당 허용 횟수를 두고 주기가 바뀔 때마다 소모 카운터를 리셋"한다고 표현했고, 이는 스누즈(41차)의 `snoozeUsedDate`/`snoozeUsedCount`처럼 그룹/루틴에 영구 저장되는 필드를 암시하는 것처럼 읽힐 수 있었다. 실제 구현(`RoutineEngine.currentStreak`)은 그런 필드를 두지 않았다.

- **스누즈와 다른 점**: 스누즈는 "지금 이 순간 즉시 실행되는 우회 동작"이라 하루가 끝나기 전에 실시간으로 몇 번 썼는지 알아야 하므로 영구 카운터가 필수다. 반면 스트릭 방어권은 "과거 기록을 훑어 지금 스트릭이 몇인지 계산"하는 순수 조회 동작이라, 매번 오늘부터 과거로 걸어가면서 그 시점의 주기(주/월) 경계마다 로컬 변수를 리셋하며 세면 충분하다 — 이미 있는 `RoutineLog`만으로 방어권 소모 여부를 그때그때 재구성할 수 있어 별도 저장이 불필요하다.
- **TimetableScreen/StudyStatsScreen과 같은 "파생 뷰" 철학을 그대로 유지**: 이 프로젝트는 이미 "원본 데이터(CalcTask/CalendarTask)로부터 매번 다시 계산해서 보여주는" 읽기 전용 화면 패턴을 여러 번 써왔다(29~30차). 방어권 있는 스트릭도 저장된 파생 상태(defenseUsedCount 등) 없이 항상 원본 로그로부터 재계산하면, "리셋을 언제 트리거할지"(그룹 자동 재활성화의 `applyDailyGroupResetIfNeeded()`처럼 매 tick 또는 날짜 전환 시점에 호출해야 하는 문제)라는 별도 고민 자체가 생기지 않는다 — 코드가 더 단순해지고 버그 표면도 줄어든다.
- **트레이드오프**: 스트릭이 아주 길어지면(예: 10년) 매번 최대 3650일을 순회하는 비용이 있다(기존 StudyStatsScreen 스트릭 계산도 동일한 3650일 상한을 쓰고 있어 새 패턴이 아님). 화면에 표시할 때만 호출되는 값이라 이 정도 비용은 무시 가능하다고 판단했다.

## 루틴앱 v1 설계: Routine 하나로 체크리스트/습관/일과표 통합, 방어권 있는 스트릭, 앱 전체 라이트+그린 재단장 (2026-08-13, 47차 세션)
45차에 통합만 결정되고 미착수 상태였던 루틴앱의 v1 설계를 대화로 확정. 사용자가 처음 요청한 세 기능(반복 할 일 체크리스트/습관 트래커+스트릭/시간대별 일과표)이 데이터 모델 관점에서 겹치는 것을 발견해 통합했고, 마이루틴·Streaks·Loop Habit Tracker 조사 결과를 참고해 스트릭 방어권 개념을 추가했다.

- **세 기능을 별개 화면/모델이 아니라 `Routine` 하나 + 플래그로 통합**: "체크리스트"와 "습관"은 `trackStreak` 여부 차이일 뿐이고, "시간대별 일과표"는 `timeSlot`이 있는 루틴들을 시간순으로 그리는 파생 뷰(별도 저장 안 함, 기존 `TimetableScreen`이 계산기 draft를 파생시키는 패턴과 동일)로 설계했다. 데이터 중복 없이 화면 전환만으로 세 요구를 충족.
- **스트릭 계산은 기존 `StudyStatsScreen`의 "지정 요일만 카운트, 지정일 미체크 시 즉시 끊김" 로직을 그대로 재사용하되, "방어권"을 추가**: 순수 카운트 방식은 하루만 놓쳐도 스트릭이 0으로 리셋돼 가혹하다는 문제(Loop Habit Tracker 조사에서 착안)를 사용자가 지적, "주 1회/월 3회처럼 봐주는 횟수"를 요청했다. `defenseType`(NONE/WEEKLY/MONTHLY)+`defenseCount`로 주기당 허용 횟수를 두고, 주기가 바뀔 때마다 소모 카운터를 리셋 — 방어권을 쓴 날은 "지정 안 된 날"과 동일하게 취급(스트릭 유지, 증가는 안 함). 기존 스누즈의 "하루 3회 제한, 날짜 바뀌면 리셋" 패턴(41차)과 구조적으로 동일해서 새 전략을 설계하지 않고 주기만 하루→주/달로 확장한 것.
- **앱 전체를 라이트+그린으로 재단장하기로 결정, 다크+파랑(28차/35차)을 뒤집음**: "마이루틴을 따라가고 하얀 바탕에 초록 포인트로 가자"는 요청에 적용 범위(루틴 섹션만 vs 앱 전체)를 먼저 확인 — 사용자가 앱 전체 재단장을 선택했다. 정확한 그린 톤은 미확정(착수 시 후보 제시하기로 함), 배경은 순백 대신 오프화이트 계열 추천만 해둔 상태.
- **관리앱(그룹 차단) 기능과의 통합은 이번 설계에서 명시적으로 배제**: 앞선 대화에서 "관리앱 대개편(루틴앱 중심으로 그룹/통계를 서브탭으로 편입)" 방향이 잠깐 논의됐으나, 실제 설계 요청 시 사용자가 "현재 관리앱 요소는 우선 배제하고"로 범위를 좁혔다 — 루틴앱은 독립적으로 완성하고, 관리앱과의 연동(예: 루틴 완료가 차단 해제 조건에 들어가는지 등)은 별도 논의 대상으로 남겨둠.
- **v1 스코프에서 명시적으로 뺀 것**: 배지/스탬프 게이미피케이션(마이루틴/Habitica 참고했으나 과설계 우려로 제외), "주 N회" 같은 비-요일 기반 반복 규칙(Loop Habit Tracker에 있으나 이번엔 요일마스크만), 몸무게/컨디션 등 수치 기록+그래프, 위젯/워치 연동. 전부 [[IDEAS.md]]에 기록.
- **이번 세션은 코드 변경 없음**: 순수 설계 논의만 진행, 다음 세션에서 `Routine`/`RoutineLog` 데이터 모델부터 착수 예정 — [[HANDOFF.md]] "다음 작업 우선순위" 참고.

---

## 앱 아이콘: 안드로이드/데스크탑 exe/트레이 3곳이 같은 108x108 픽셀 좌표를 공유하도록 설계 (2026-08-13, 46차 세션)
프로젝트 개명(45차)에 맞춰 앱 아이콘도 새로 만들어달라는 요청 — 기존 안드로이드 벡터 자물쇠 아이콘 좌표계(`ic_launcher_foreground.xml`, 108x108 뷰포트)를 그대로 재사용하는 걸 기준으로 삼았다.

- **좌표 소스를 하나로 못박고 세 곳에 각자 다른 방식으로 이식**: 픽셀아트 사각형 목록(9dp 그리드) 하나를 안드로이드 `ic_launcher_background.xml`/`ic_launcher_foreground.xml`(Android Vector Drawable XML), 데스크탑 exe 아이콘(PowerShell `System.Drawing`으로 직접 렌더링해 다중 해상도 `.ico` 생성, `packaging/generate_icon.ps1`), 데스크탑 트레이/창 아이콘(Compose `Painter` 서브클래스, `ui/PixelSunriseIcon.kt`) 세 군데에 각 플랫폼 API에 맞는 방식으로 옮겼다 — 렌더링 방식은 다르지만 좌표 리스트 자체는 세 파일에서 동일하게 유지해서, 나중에 색을 바꾸거나 모양을 조정할 때 세 곳을 항상 같이 고쳐야 함을 명시.
- **`.ico` 없이 Compose Painter만으로는 안 됨**: jpackage가 만드는 실제 실행파일(exe) 아이콘은 Windows 리소스로 빌드타임에 박히는 것이라 Compose 런타임 아이콘(Tray/Window)과는 별개 경로 — `build.gradle.kts`의 `windows.iconFile`에 `.ico` 파일을 지정해야 작업표시줄/탐색기에서 보이는 아이콘이 바뀐다. `.ico`는 PNG 프레임을 담는 표준 ICONDIR 포맷을 PowerShell에서 직접 바이트 단위로 조립해 생성(외부 도구 없이).
- **작업표시줄 아이콘 캐시는 exe 교체와 별개 문제**: exe의 임베드 아이콘이 바뀌어도 Windows가 파일 경로 기준으로 아이콘을 캐싱해서 탐색기/작업표시줄엔 옛 아이콘이 남을 수 있다 — `IconCache.db`/`iconcache_*.db`/`thumbcache_*.db` 삭제 후 `explorer.exe` 재시작이 표준 해결책.

---

## 프로젝트를 "갓생살기종합세트"로 개명 + 루틴앱 통합 결정, 알람앱은 분리 유지 (2026-08-13, 45차 세션)
사용자가 관리앱+공부앱이 이미 한 코드베이스/앱으로 합쳐져 쓰이는 것에 대해 "분리가 나을지 계속 합쳐 쓰는 게 나을지" 질문 → 이어서 "그럼 알람앱도 합칠까"(별도 저장소) → 최종적으로 "루틴앱을 새로 만들 건데 이것도 이 프로젝트에 포함시키자, 프로젝트 이름을 갓생살기종합세트로 바꿔줘"로 결론.

- **판단 기준은 "기능적 결합도"이지 "주제가 비슷한가"가 아님**: 관리앱↔공부앱은 공부 타이머의 로컬 상태를 `LockEvaluator`가 직접 읽어서 공부 잠금을 거는 실제 코드 의존성이 있어 합쳐 쓰는 게 합리적이라고 판단(분리 시 Firebase 등 외부 채널로 재우회해야 해서 지연/복잡도만 늘어남). 알람앱은 패키지(`com.wakealarm`)·Room DB·도메인(강제 기상 미션)이 완전히 독립적이고 관리앱/공부앱과 공유하는 데이터나 로직이 전혀 없어 "자기관리"라는 주제만 겹칠 뿐 결합도가 0이라 분리 유지로 판단. 루틴앱은 아직 기능이 정해지지 않았지만, 사용자가 이 프로젝트에 통합하기로 먼저 결정.
- **루틴앱은 계획 단계일 뿐 착수 아님**: 이번 세션에서 코드는 전혀 건드리지 않았다. 이름 변경과 "앞으로 이 프로젝트에 포함시킨다"는 방향만 정한 것이며, 루틴앱의 구체적 기능·공부앱/관리앱과의 연동 방식(예: 루틴 완료 여부가 공부 잠금 해제 조건에 들어가는지 등)은 다음 세션에서 사용자에게 먼저 확인해야 한다.
- **문서 체계는 그대로 유지**: 프로젝트명만 바뀌었고 HANDOFF/CHANGELOG/DECISIONS/BUGS/IDEAS 5종 문서 체계와 관리앱 루트 위치는 변경하지 않았다 — 루틴앱이 실제로 착수되면 이 저장소 안에 하위 모듈로 추가할지 여부도 그때 다시 결정.

---

## 사용 중 오버레이: 타이머 텍스트도 배경과 같은 alpha를 공유하는 "오버레이 일부"로 설계 (2026-08-12, 43차 세션)
"타이머 또한 오버레이의 일부로 판단하여 평상시엔 투명하다가 레벨이 오르면 오를수록 불투명해지는 시스템으로" 요청 — 기존엔 배경(경고색 오버레이)만 레벨에 따라 알파가 오르고, 타이머 숫자는 항상 완전 불투명한 파란색(`#4F8EF7`)으로 고정돼 있었다.

- **별도 상태 없이 배경과 동일한 alpha 값을 텍스트 색상에도 그대로 적용**: 새 상태 변수나 애니메이션 커브를 추가하지 않고, 이미 계산돼 있던 `alpha`(0~255, 배경 `argb(alpha, 251,246,240)`에 쓰이던 값)를 타이머 텍스트 색상(`argb(alpha, 0x4F,0x8E,0xF7)`)에도 그대로 재사용했다 — 배경과 타이머가 항상 같은 투명도로 움직이므로 "타이머가 오버레이의 일부"라는 요구를 정확히 만족하면서, 판정 로직이나 알파 계산 공식(`OVERLAY_BASE_ALPHA`~`OVERLAY_MAX_ALPHA`, `overlayLevelStepsToMax` 기반)은 전혀 건드리지 않았다. 브라우저 확장(`overlay.js`)도 동일 패턴(`rgba(79,142,247,opacity)`).
- **제목/문구 텍스트는 축소가 아니라 완전 삭제**: "멘트를 아예 삭제하고 타이머만 남겨놔"라는 요청이 명확해서, 폰트를 줄이거나 숨기는 대신 View/DOM 엘리먼트 자체를 생성하지 않도록 했다(안드로이드는 `title`/`quote` TextView 생성 코드 삭제, 브라우저 확장은 해당 `div` 생성 코드 삭제) — 나중에 되살릴 여지를 남기기보다 요청 그대로 단순화.
- **데스크탑 코너 위젯은 범위 밖으로 명시적으로 확인 후 제외**: `UsageOverlayContent.kt`는 애초에 "무시당하면서 살기" 문구가 없는 별개 UI(작은 코너 창, 전체화면 불가라 다른 이유로 이미 축소된 디자인)라 이번 변경과 무관 — 사용자에게 "이건 그대로 둬도 되는지" 확인 요청 없이 먼저 안내했고, 사용자도 "꺾일 일 없으니 놔둬도 될 것 같다"고 확인.

## 텍스트 자동 축소(shrink-to-fit)는 "무엇을 막으려는지"에 맞춰 maxLines를 고를 것 (2026-08-12, 43차 세션)
"제목이 길어지면서 줄바꿈되는 게 가독성을 떨어뜨린다"는 요청에 처음엔 `maxLines=2`+`onTextLayout`의 `didOverflowHeight`로 "2줄을 넘칠 때만" 축소하는 로직을 짰다. 배포 후 사용자가 "바뀐 게 없다"고 지적 — 원인은 대부분의 랜덤 문구가 정확히 2줄에 들어가서 애초에 `didOverflowHeight`가 한 번도 `true`가 안 된 것이었다.

- **"줄바꿈 자체"가 문제일 땐 `maxLines=1`+`didOverflowWidth`가 맞는 조건**: `maxLines=N`+`didOverflowHeight`는 "N+1번째 줄이 생기는 것"만 막는다. "짧아도 2줄로 꺾이는 모양 자체가 보기 싫다"는 요구는 처음부터 한 줄을 기준으로 판정해야 한다 — `maxLines=1`+`softWrap=false`로 강제로 한 줄만 그리게 하고, 그 한 줄이 폭을 넘치면(`didOverflowWidth`) 줄이는 방식으로 바꿔서 해결했다. 최소 배율(55%)을 둬서 극단적으로 긴 문구도 읽을 수 없을 만큼 작아지지는 않게 했다.
- **교훈**: shrink-to-fit류 로직을 짤 때 "몇 줄까지 허용할지"와 "무엇을 트리거로 축소를 시작할지"를 분리해서 생각할 것 — 허용 줄 수를 늘리면 그만큼 축소가 늦게 발동한다.

---

## 조롱조 문구 강도 시스템: "절대 수치" 기준 5단계 + title/quote 필드 재배치 (2026-08-12, 42차 세션)
사용자가 여러 차례에 걸쳐 뽑은 조롱조 문구 124개를 "실행확인은 레벨이 오를수록, 스케줄/일일한도는 클릭(시도) 횟수가 많을수록 독해지게" 로테이션시켜달라고 요청. 두 가지 설계 포인트가 있었다.

- **강도 기준을 "오버레이 밝기 설정과 무관한 절대 수치"로 못박음**: 사용자가 먼저 "이건 오버레이 최고 밝기까지 재확인 횟수(`overlayLevelStepsToMax`)와는 별개로 절대적인 수치로 정하는 게 좋겠다"고 명시했다. `overlayLevelStepsToMax`는 그룹마다 다르게 설정되는 순수 표시값(오버레이 알파 계산용)인데, 이걸 문구 강도 기준으로 같이 쓰면 그룹 설정에 따라 같은 레벨에서도 다른 문구가 나와 일관성이 깨진다. 그래서 `confirmQuoteTier(level)`은 `Repository.getCurrentLevel()`이 반환하는 원시 레벨(재확인할 때마다 1씩 오르는 절대값)만 보고 고정 임계값(0~1/2~3/4~5/6~7/8+)으로 판정한다.
- **잠김(스케줄/일일한도) 화면은 "오늘 이 그룹을 열려던 횟수"라는 새 카운터 신설**: 실행확인처럼 참고할 기존 "레벨" 개념이 없어서, 스누즈(#1)의 `snoozeUsedDate`/`snoozeUsedCount`(하루 단위 리셋)와 정확히 같은 패턴으로 `Group.blockAttemptDate`/`blockAttemptCount`를 신설, `EnforcementService.decide()`/`SiteEnforcement.check()`/`tick()`이 잠금 판정이 난 바로 그 순간(`onBlock` 호출 직전)에 1 증가시킨다. STUDY_LOCK/REELS/SHORTS 차단은 "그룹을 열려는 시도"라는 개념 자체가 안 맞아(그룹이 아니라 공부 잠금/키워드 감지 기반) 강도 없이 항상 순한 맛으로 남겨뒀다 — 무리하게 억지 카운터를 만들지 않고 범위를 명확히 좁힌 것.
- **title/quote 필드를 뒤늦게 스왑**: 처음 구현 땐 화면의 작은 부제(quote, 원래 "의무에 따라 행동하세요" 한 줄)에 로테이션을 걸었는데, 사용자가 바꿔달라던 건 큰 제목(title, "무시당하면서 살기, 무시하면서 살기")이었다. 사용자 지적을 받고 title에 로테이션 문구를 걸도록 전부 바꾸고, 이제 중복이 된 quote 줄은 삭제했다(한 화면에 두 개의 조롱 문구가 동시에 뜰 필요가 없음). 안드로이드 "사용 중 남은시간" 오버레이(`AppMonitorAccessibilityService`)와 브라우저 확장 `overlay.js`는 확인/차단 게이트 화면이 아니라 별개의 상시 표시 위젯이라 이번 범위에서 제외 — 필요해지면 별도 요청으로 처리.

## 그룹 자동 재활성화는 스누즈 카운터와 같은 "날짜 비교" 패턴으로 하루 1회만 적용 (2026-08-11, 42차 세션)
"초기화 시간이 지나면 꺼둔 그룹도 다시 켜지게" 요청 — 매 tick(2초)마다 무조건 켜면 사용자가 오늘 중에 다시 끈 그룹까지 계속 강제로 켜지는 문제가 생긴다.

- **`lastGroupAutoResetDate` 하나로 "오늘 이미 적용했는지" 추적**: `effectiveDate(dailyResetHour)` 기준 오늘 날짜와 마지막 적용 날짜가 다를 때만 `groupEnabled=false`인 그룹을 전부 켜고, 그 즉시 `lastGroupAutoResetDate`를 오늘로 갱신한다. 날짜가 같으면(오늘 이미 한 번 적용했으면) 아무것도 안 해서, 사용자가 초기화 이후 다시 그룹을 끄면 다음 초기화 시각 전까지는 그대로 꺼진 채 유지된다.
- **매 tick 시작 지점에서 호출**: 별도 백그라운드 스케줄러를 새로 만들지 않고 기존 2초 주기 감시 루프(`EnforcementService.tick()`/`AppMonitorAccessibilityService.tick()`) 맨 앞에 얹었다 — 이미 항상 돌고 있는 루프라 지연이 최대 2초로 충분히 짧고, 새 코루틴/스레드를 추가로 관리할 필요가 없다.
## 스누즈(#1) 크로스디바이스 동기화는 confirmSync 병합 패턴 재사용 + UI/판정 경로 분리 (2026-08-11, 41차 세션)
39차에 만든 스누즈가 그룹 자체(`AppGroup`/`Group`)를 애초에 Firebase로 올리지 않는 구조라 기기별 로컬 전용이었던 걸 사용자가 지적, "바로 동기화 추가"를 선택해 41차에 구현.

- **새 상태를 추가하지 않고 기존 confirmSync 패턴을 그대로 재사용**: 실행확인 레벨 동기화(`confirmSync`, "최신값 승리" — `lastConfirmedAtEpochMillis`가 더 큰 쪽 채택)와 스누즈("종료 시각이 더 미래인 쪽 채택")는 본질적으로 같은 모양의 문제라, 새로운 동기화 전략을 설계하지 않고 그대로 복제했다(`Firebase 경로만 users/{user}/snoozeSync/{그룹명}`으로 분리). 그룹은 이름으로 매칭(기기마다 그룹ID가 다름)하는 것도 동일.
- **하루 3회 한도는 "합산"이 아니라 "병합값 기준 재판정"**: 기기별로 각자 3회씩 총 6회가 가능해지면 스누즈의 "회유 절차 생략 예외"라는 취지(39차 DECISIONS 참고)가 무력화된다. 그렇다고 진짜 합산(사용시간처럼 기기별 키에 쓰고 읽는 쪽이 더함)을 하면 로직이 복잡해지므로, confirmSync와 같은 "최신값 승리" 병합값(사용 날짜+횟수)을 기준으로 판정해 사실상 하루 3회를 기기 통틀어 지키게 했다 — 완벽한 합산은 아니지만(예: 두 기기가 정확히 동시에 스누즈하면 레이스 가능) 기존 confirmSync도 같은 특성을 갖고 있어 일관된 트레이드오프로 판단.
- **UI 표시용 함수와 판정 게이트용 함수를 분리**: `isSnoozeActive()`(그룹 목록 배지, `snoozeRemainingToday()`)는 Compose 리컴포지션마다 직접 호출되는 자리라 여기서 네트워크 I/O를 타면 UI가 버벅인다. 반면 실제 판정(`isConfirmActiveNow`/`evaluate`/`isCurrentlyRestricting`)은 EnforcementService/AppMonitorAccessibilityService의 백그라운드 tick에서만 불린다. 그래서 판정 경로의 private `isSnoozed()`만 동기화된 값(`repository.syncedSnoozeUntil()`)을 보게 하고, UI용 `isSnoozeActive()`는 로컬 값만 보게 남겨뒀다 — 병합 결과가 판정 시점에 로컬로 저장(persist)되므로, UI는 다음 재조회 때 자연히 따라잡는다(즉시 반영은 아님, confirmSync/escalation 동기화와 같은 지연 특성).
- **안드로이드 `isConfirmActiveNow()`를 `suspend fun`으로 변경**: 동기화된 스누즈 확인이 suspend 함수(`syncedSnoozeUntil`)가 되면서 어쩔 수 없이 필요했다. 호출부 3곳(AppMonitorAccessibilityService의 tickInternal/checkSitesInternal/refreshUsageOverlay)이 이미 전부 suspend 함수 안이라 안전하게 바꿀 수 있음을 먼저 확인한 뒤 진행했다.

---

## 스누즈(#1)를 LockEvaluator에 직접 추가하기로 결정 — 세부 규칙을 사용자가 먼저 확정 (2026-08-10, 39차 세션)
38차엔 "판정 로직 핵심 게이트를 확장해야 한다"는 이유로 보류했던 기능. 39차에 사용자가 "진행시켜"라고 요청했을 때, 곧바로 코드를 짜지 않고 먼저 세부 규칙(스누즈 최대 시간, 하루 몇 번까지)을 질문으로 확정한 뒤 구현했다 — 이유는 이 기능이 "회유 절차를 생략하는 합법적 우회 경로"라서, 규칙 없이 구현하면 사실상 실행확인 전체를 무력화하는 백도어가 될 수 있기 때문.

- **결정된 규칙**: 스누즈 시간은 그룹마다 다르게 설정 가능(`Group.snoozeMinutes`, 기본 30분), 하루 3회까지 제한(`snoozeUsedDate`/`snoozeUsedCount`, 일일 한도와 같은 `dailyResetHour` 기준 "오늘"로 리셋).
- **구현 위치는 `isPomodoroUnlocked()`와 완전히 동일한 패턴**: `LockEvaluator`에 `isSnoozed()`를 추가하고 `evaluate()`/`isCurrentlyRestricting()`/`isConfirmActiveNow()` 최상단에서 판정을 우회하되, `isGroupActive()`/`effectiveGroupEnabled()`(그룹 켜짐/꺼짐 자체)는 건드리지 않았다 — 이미 24차부터 뽀모도로 휴식 시간 임시 해제가 정확히 이 패턴(원격 신호로 판정만 일시 우회, 영구 상태는 그대로)을 쓰고 있어서, 새 개념을 만들지 않고 검증된 패턴을 재사용할 수 있었다.
- **왜 `ConfirmationGate.kt`는 안 건드렸는가**: 스누즈는 "그룹 전체를 일시적으로 판정에서 빼는" 기능이라 `LockEvaluator`(그룹 잠금 판정)의 영역이지, "재확인까지 대기시간이 얼마나 늘어나는지"를 계산하는 `ConfirmationGate.kt`(escalation 레벨 판정)의 영역이 아니다 — 둘은 서로 다른 책임이라 스누즈 구현이 이 파일에 손댈 이유 자체가 없었다.

## 기간 지정 자동 강화(#7)는 groupEnabled만 강제로 덮어쓰고, 스누즈보다 우선하도록 결정 (2026-08-10, 39차 세션)
사용자가 스누즈와 함께 진행을 요청한 두 번째 기능. 시험기간처럼 미리 설정해두는 "이 기간엔 무조건 보호 켜짐"과, 즉흥적으로 쓰는 스누즈가 같은 세션에 함께 들어오면서 두 기능이 충돌할 때 어느 쪽이 이겨야 하는지 결정이 필요했다.

- **구현 범위를 "groupEnabled 강제 true"로 최소화**: 처음엔 "이 기간엔 무조건 잠금"까지 고려했으나, 그러면 사용자가 설정해둔 시간대/일일한도 자체를 무시하고 하루 종일 잠기는 훨씬 강한 동작이 되어버린다. 대신 `isGroupActive()`가 `forceEnabledFrom~Until` 범위 안에서 `groupEnabled` 값과 무관하게 true를 반환하도록만 했다 — 이후의 시간대/한도 판정은 평소와 똑같이 흘러간다. "스위치를 꺼도 무시된다"만 보장하고, 나머지는 그룹 자체 설정을 그대로 존중하는 쪽이 사용자의 기존 설정을 덜 놀라게 한다고 판단했다.
- **기간 지정 강화가 스누즈보다 우선**: `evaluate()` 등에서 `isForceEnabled(group, now) || !isSnoozed(group)`처럼, 강제 기간 안에서는 스누즈 여부를 아예 확인하지 않고 통과시킨다(스누즈가 걸려 있어도 무시). 시험기간처럼 나중에 후회할 즉흥적 판단(스누즈)을 막기 위해 미리 설정해두는 기능이라는 목적을 생각하면, 그 즉흥적 판단이 이 안전장치를 그냥 뚫고 지나가면 기능 자체의 존재 의미가 없어지기 때문. 반대로 강제 기간이 아닐 때는 기존처럼 스누즈가 정상 작동한다.
- **UI에 "지금 강제 켜짐 중" 배지는 추가하지 않음**: 그룹 목록의 켜짐/꺼짐 스위치는 사용자가 마지막으로 둔 상태 그대로 보이고, 실제 판정만 강제로 켜진 것으로 취급된다. 배지를 추가하면 더 명확하겠지만 이번 세션 범위를 최소화하려 보류했다 — 사용자가 헷갈려하면 다음에 추가 검토.

## 전문가 종합분석 보고서의 신규 기능 구현 범위: 판정 로직 인접 영역은 보류 (2026-08-10, 38차 세션)
사용자 요청으로 대규모 코드/기능 분석 보고서를 작성한 뒤 "보고서를 따라 앱 개선 진행시켜"라는 후속 요청을 받았다. 보고서엔 30개 신규 기능이 있었지만 전부를 기계적으로 구현하지 않고, 매 기능마다 착수 전 사용자에게 진행 여부를 확인하며 9개만 구현했다 — 그 경계를 어떻게 그었는지가 이 결정의 핵심.

- **구현한 9개의 공통점**: 전부 `LockEvaluator.evaluate()`/`ConfirmationGate.kt`(잠금 여부·재확인 판정)를 직접 확장하지 않고, ① 이미 있는 데이터를 다시 보여주기만 하거나(요약 위젯, 완료율 비교), ② "원본은 그대로, 호출부에서 부가 기능"이라는 37차에 확립된 패턴을 재사용하거나(재확인 카운터), ③ 판정과 무관한 부가 UX(백업/복원, CSV, DND, 이상패턴 경고)였다.
- **의도적으로 구현 안 한 것 — 그룹 스누즈(#1)/완화 시 PIN(#17)**: 둘 다 `LockEvaluator`의 핵심 게이트(`isGroupActive`/`evaluate`) 자체를 확장해야 한다. 이 파일은 "사용자가 스스로 규칙을 약화시키는 걸 막기 위한" `detectWeakeningEdit()`이라는 방대한 방어 로직 그 자체를 담고 있어서, HANDOFF.md의 "절대 손대지 말 것" 원칙과 정신적으로 같은 영역이라고 판단했다. 특히 스누즈는 "회유 절차를 생략한 임시 해제 버튼"이라 앱의 핵심 목적(자기통제)과 정면 충돌할 여지가 있어 사용자와 먼저 설계를 확정해야 한다.
- **의도적으로 구현 안 한 것 — 접근성 이벤트 디바운싱(#14)**: 보고서 자체가 "실기기 프로파일링 후 스로틀 값 결정"을 전제로 달았다. 해당 코드(`AppMonitorAccessibilityService.onAccessibilityEvent`)의 현재 주석들은 "넷플릭스 버그"처럼 실기기에서만 드러난 과거 이슈를 겪으며 조정된 흔적이 뚜렷해서, 빌드조차 못 하는 이번 세션 환경에서 임의로 스로틀을 추가하는 건 검증 없는 회귀 위험이 크다고 판단했다.
- **판정 로직에 인접했지만 회색지대였던 케이스 — `mergedEscalation()`의 락 안 블로킹 I/O**: 3절 버그 수정 중 `peerUsageSeconds`(순수 사용시간 읽기)는 고쳤지만 `mergedEscalation()`(실행확인 레벨을 계산하는 함수, escalation 판정과 밀접)은 같은 유형의 버그였음에도 손대지 않고 남겨뒀다. 성능/동시성 버그 수정이라 해도 escalation 계산 함수의 락/캐싱 타이밍을 바꾸는 건 판정 결과에 미세하게 영향을 줄 수 있어, 보수적으로 범위를 좁혔다.

## 재확인 카운터(#32)를 로컬 전용으로 설계 — Firebase 동기화 대상에서 제외 (2026-08-10, 38차 세션)
"하루에 재확인 화면을 몇 번 마주쳤는지" 카운터를 추가하면서, `dailyUsage`/`studyLog`처럼 크로스디바이스로 합산할지 검토했으나 로컬 전용(기기별로 따로 관리)으로 결정했다. 이유: ① 이 카운터는 "이 기기에서 이 순간 유혹에 몇 번 굴복했는지"라는 순간적 자기관찰용 지표라, 여러 기기를 합산하면 오히려 어느 기기에서 무슨 일이 있었는지 알기 어려워진다. ② 34차에 확립된 "기기별 키에 쓰고 읽는 쪽이 합산" 패턴을 굳이 쓰지 않아도 되는 값이라 새 Firebase 스키마 없이 순수 로컬 데이터로 남겨 범위를 최소화했다. 필요하면 나중에 `dailyUsage`와 같은 패턴으로 확장 가능(현재 구조를 갈아엎지 않고 그대로 얹을 수 있음).

## 오래된 데이터 정리(#21)를 자동이 아니라 수동 버튼으로 구현 (2026-08-10, 38차 세션)
보고서 원안은 "설정에서 N개월 이상 지난 기록 자동 삭제(기본 12개월, off 가능)"였으나, 실제로는 캘린더의 기존 "🧹 정리" 버튼과 동일하게 사용자가 직접 눌러야 실행되는 수동 트리거로 구현했다. 데이터를 되돌릴 수 없게 영구 삭제하는 동작을 사용자 인지 없이 백그라운드에서 자동 실행하는 건, 이 세션의 안전 원칙(파일 삭제 등 되돌리기 어려운 작업은 사용자가 명시적으로 트리거하게 한다)과 프로젝트의 기존 UX 관례(캘린더 정리도 수동) 둘 다에 맞지 않는다고 판단했다.

## 이상 사용 패턴 경고(#34)를 시스템 알림이 아니라 화면 내 배너로 구현 (2026-08-10, 38차 세션)
보고서 원안은 OS 알림(안드로이드 알림/데스크탑 트레이 알림)이었으나, 통계 화면을 열었을 때만 보이는 배너로 범위를 좁혔다. 시스템 알림으로 구현하려면 하루 1회 트리거 지점을 모니터링 tick 루프(`EnforcementService`/`AppMonitorAccessibilityService`, 둘 다 이번 세션에 이미 다른 버그를 수정한 민감한 파일)나 별도 백그라운드 스케줄러에 새로 추가해야 하는데, 컴파일조차 못 하는 이번 세션에 모니터링 루프를 추가로 건드리는 리스크를 감수하고 싶지 않았다. 화면 내 배너는 기존 통계 조회 흐름에 얹기만 하면 되어 리스크가 훨씬 낮다.

## 크로스디바이스 공부 잠금 강제 적용: 미러 표시를 실제 잠금으로 격상 + 신선도 컷오프 재사용 (2026-08-10, 37차 세션)
34차에 만든 "타이머 크로스디바이스 미러"(다른 기기 상태를 화면에 보여만 주고 잠금엔 관여 안 함)를 사용자가 "다른 기기에서 타이머 실행하면 이 기기도 잠기게 해달라"고 요청 — 표시에서 실제 강제(enforcement)로 범위가 넓어졌다.

- **왜 24차 "로컬이 source of truth" 원칙과 충돌 안 하는가**: 24차 결정은 "이 기기 자신의 타이머 상태(초 단위 경과시간 등)는 로컬이 유일한 진실"이라는 뜻이지, "다른 기기의 상태를 절대 참고하지 말라"는 뜻이 아니다. `LockEvaluator.isPomodoroUnlockActive()`가 이미 24차부터 원격 신호(`PomodoroSyncClient.isBreakActive()`)로 그룹을 임시 "해제"하고 있었던 것과 정확히 대칭 — 이번엔 반대 방향(원격 신호로 "잠금")으로 같은 패턴을 재사용했을 뿐이다.
- **허용 목록은 여전히 로컬**: 잠금을 거는 조건만 원격 신호로 넓혔고, 잠긴 상태에서 무엇을 허용할지(`studyLockAllowedApps`/`studyLockAllowedSites`)는 24차 결정대로 각 기기의 로컬 값을 그대로 쓴다. 원격 기기의 허용 목록을 가져오지 않는다 — 플랫폼마다 허용 항목 형식이 다르기도 하고(exe명 vs 패키지명), 애초에 "이 기기에서 뭘 허용할지"는 이 기기 사용자가 정할 문제라고 판단.
- **정지/전환 버튼은 원격 잠금일 때 숨긴다(제어는 안 함)**: 19차 `remoteCommand` 신뢰성 문제로 24차에 원격 제어 자체를 없앤 전례를 다시 반복하지 않기 위해, 이번에도 "표시(그리고 이번엔 강제 잠금까지)는 하되 제어는 안 함" 원칙을 지켰다. 로컬에 실행 중인 타이머가 없는 상태에서 정지 버튼을 눌러봐야 아무 효과가 없으므로(`Repository.timerStop()`이 `getTimerRun() == null`이면 조용히 no-op), 버튼을 활성 상태로 보여주면 "눌러도 안 먹는 버그"처럼 보일 것 — 아예 숨기고 "다른 기기에서 실행 중, 그 기기에서 조작하라"는 안내로 대체했다.
- **20분 신선도 컷오프를 반드시 재사용해야 하는 이유**: 34차의 미러 표시는 신호가 오래돼도 "화면이 좀 이상하게 보이는" 정도의 위험이지만, 이번엔 그 신호로 진짜 잠금을 걸기 때문에 위험도가 다르다 — 신호를 올리던 기기가 정지 버튼 없이 앱을 완전히 꺼버리면 Firebase엔 `timerActive:true`가 유령처럼 영원히 남을 수 있고, 컷오프가 없으면 이 기기가 그 순간부터 영구히 잠기는 심각한 사고로 이어진다. 34차에 이미 검증해둔 `REMOTE_STUDY_SIGNAL_STALE_MS`(20분, `remoteUpdatedAtMillis()` 기준) 컷오프를 그대로 재사용해서 이 위험을 막았다 — 새 원격 "강제 동작"을 추가할 때는 항상 이 안전장치를 먼저 고려할 것.
- **안드로이드에서 뒤늦게 발견한 함정**: `StudyLockActivity`의 `isStillActive` 콜백이 원래 `{ repository.isStudyLockActive() }`(로컬만 확인)였는데, 이대로 두면 원격 신호로 뜬 잠금 화면이 "로컬 타이머가 없다"는 이유로 뜨자마자 매초 `onInactive()`를 호출해 즉시 닫혀버리는(깜빡임) 문제가 생길 뻔했다 — `checkStudyLock()`을 고치면서 잠금을 "거는" 조건은 넓혔지만 "유지하는" 조건(`isStillActive`)을 같이 안 넓히면 반쪽짜리 구현이 된다는 걸 구현 중간에 발견해 함께 수정(콜백을 suspend로 바꿔 원격 신호도 폴링).

## 재확인 유예시간(쿨다운) 크로스디바이스 공유: `ConfirmationGate.kt` 원본 유지 + 호출부 병합 패턴 (2026-08-10, 37차 세션)
사용자가 "한쪽에서 전자 버튼을 누르면 같은 이름의 그룹을 가진 다른 기기도 같은 시간대를 가지고 활동을 진행할 수 있게" 요청 — 실행확인을 통과한 뒤의 유예시간(재확인까지 남은 시간)을 그룹 이름 기준으로 기기 간 공유해달라는 것.

- **`ConfirmationGate.kt`(양 플랫폼)는 절대 수정하지 않는다는 기존 원칙과 정면으로 부딪히는 요청**: 이 파일은 과거 큰 삽질 끝에 "재확인/escalation 판정 로직에는 절대 손대지 말 것"이 확립된 파일이다([[표시값과 판정 로직 분리 원칙]]). 요청을 곧이곧대로 구현하려면 이 파일의 쿨다운 판정 자체를 크로스디바이스로 확장해야 할 것처럼 보이지만, 실제로는 원본을 그대로 두고 **호출부에서 로컬 값과 동기화 값을 비교**하는 것만으로 충분했다.
- **이미 있던 재료를 그대로 재사용**: `confirmSync`(12차 세션에 도입된 실행확인 레벨 동기화)가 이미 `lastConfirmedAtEpochMillis`를 그룹 이름 기준으로 Firebase에 최신값 승리 방식으로 동기화하고 있었고, 이 값은 `recordConfirm()`이 `ConfirmationGate.markConfirmed()`와 정확히 같은 시각에 기록한다 — 즉 "다른 기기가 마지막으로 확인한 시각"이 이미 동기화되고 있었을 뿐, 그걸 유예시간 계산에 쓰는 코드가 없었을 뿐이다. `Repository.syncedLastConfirmedAtEpochMillis(group)`을 신설해 기존 `mergedEscalation()`의 계산 결과를 그대로 노출하고, 호출부에 `effectiveRemainingCooldownSeconds()`(로컬 `ConfirmationGate.remainingCooldownSeconds()`와 동기화 값 기반 계산 중 `maxOf`)를 추가해서 끝냈다.
- **"판정 로직을 절대 건드리지 말라"는 규칙이 걸린 파일을 확장해야 할 때의 일반 패턴으로 남긴다**: 원본 파일은 정말로 원본 그대로 두고, 그 파일이 이미 노출하는 값(또는 이미 동기화되고 있는 관련 값)을 호출부에서 조합해 원하는 동작을 얻을 수 있는지 먼저 찾아본다. 이번엔 마침 `confirmSync`가 필요한 데이터를 이미 갖고 있어서 새 Firebase 스키마조차 필요 없었다.
- **부수 효과**: 오버레이의 "남은시간" 표시도 이 값을 그대로 쓰므로(`overlayStatusFor`/`overlayStatus`가 `effectiveRemainingCooldownSeconds()` 사용), 두 기기가 같은 종료 시각을 기준으로 카운트다운을 보여주게 되어 "같은 타이머 시간대를 공유"한다는 사용자 요청을 부가 구현 없이 함께 충족했다.

## 앱 전체 강조색을 주황(#FF9800)에서 파란 accent(#4F8EF7)로 통일 (2026-08-08, 35차 세션)
"후자"(그만두기) 버튼과 오버레이 남은시간 숫자에 안드로이드/데스크탑/브라우저 확장 9곳에서 공통으로 쓰던 주황을, 공부앱(index.html)의 `--accent`와 동일한 파란색으로 전면 교체. 예전엔 "위험/포기 액션"이라는 의미로 주황을 의도적으로 골랐던 것으로 보이나, 사용자가 실사용 중 파란 계열이 전체 다크 팔레트와 더 어울린다고 판단해 요청 — 별도 "경고색"을 두지 않고 텍스트 강조는 전부 accent 하나로 통일하는 쪽으로 방향을 바꿨다. 버튼 텍스트 색은 `Theme.kt`의 `onPrimary = Background` 관례(Primary와 정확히 같은 hex를 쓰면 Compose M3가 자동으로 onPrimary를 골라 어두운 텍스트가 나옴)에 맞춰 밝은 텍스트에서 `#0f1117`로 함께 조정 — 브라우저 확장은 이 자동 매핑이 없어 수동으로 같은 값을 맞춰줬다.

## 캘린더 "미완료(X)" 시 다음날 자동 복사 — "완료(O)"의 자동 다음회독 생성과 대칭 패턴 (2026-08-08, 35차 세션)
사용자가 "업무 미완료하면 다음 날에 똑같이 복사되게 해줘"라고 요청 — 트리거 시점(자정 배치 없음, 미완료 버튼 클릭 시 즉시)과 원본 처리(그대로 남김, 미완료 기록 유지)를 먼저 확인한 뒤, 기존에 있던 "완료(O) → `applyCalendarAutoSchedule`로 다음 회독 자동 생성 / 되돌리면 `revertCalendarAutoSchedule`로 제거" 패턴을 그대로 미러링해서 구현했다(`applyIncompleteCarryOver`/`revertIncompleteCarryOver`). 새 개념을 만들지 않고 이미 검증된 토글 규칙(같은 상태 다시 누르면 취소, 부작용도 같이 되돌림)을 재사용한 것이 핵심 — 회독 색 변경(`CALENDAR_SCHEDULE`) 없이 같은 이름/색으로 정확히 +1일에만 복사한다는 점만 다르다.

## 공부 기록(StudyLogEntry) 크로스디바이스 동기화: dailyUsage와 같은 "기기별 키" 패턴 (2026-08-07, 34차 세션)
24차 결정("타이머는 기기별 로컬, Firebase는 신호만")을 사용자가 재차 확인 후 "공부기록만은 반드시 동기화되어야 한다"고 명시적으로 요청 — 기록 자체(완료된 공부 시간의 누적 목록)는 신호가 아니라 실제 데이터라 이번엔 부가 채널이 아니라 진짜 동기화 대상으로 격상했다.

- **왜 캘린더/계산기처럼 문서 전체 LWW가 아닌가**: `studyLog`는 여러 기기가 각자 독립적으로 계속 추가(append)하는 로그라, 문서 단위 LWW(마지막에 쓴 기기가 전체를 덮어씀)를 쓰면 다른 기기가 그 사이 추가한 항목이 통째로 사라진다. 대신 `dailyUsage/{날짜}/{그룹}/{기기}`에서 이미 검증된 "기기별로 자기 키에만 쓰고, 읽는 쪽이 합산" 패턴을 그대로 재사용 — `users/{user}/studyLog/{날짜}/{기기}`에 그 기기가 그날 기록한 전체 배열을 통째로 덮어쓴다(항목 단위 append가 아니라 그 기기의 "그날 전체"를 매번 다시 씀 — 구현이 단순하고 하루 안의 기록 개수가 많지 않아 부담 없음). 동시 쓰기 경쟁이 없다(각자 다른 키).
- **로컬 DB/데이터파일엔 다른 기기 기록을 병합해서 쓰지 않는다**: 대신 Repository에 `remoteStudyLogCache`(날짜→다른 기기 기록)라는 표시 전용 캐시를 따로 두고, `getTodayStudyLog()`/`getStudyLogForDate()`가 "로컬 기록 + 캐시"를 합쳐서 반환한다. 재동기화를 아무리 반복해도 로컬 저장소엔 중복이 안 쌓인다 — 캐시를 덮어쓰기만 하면 되기 때문. 로컬 DB에 원격 항목을 실제로 merge-insert하는 방식은 "이미 병합된 원격 항목"과 "새로 온 원격 항목"을 구분할 방법이 없어 재동기화마다 중복이 쌓이는 문제가 있어 피했다.
- **동기화 시점**: 타이머 탭은 5초마다(포그라운드에 떠 있는 동안 계속), 캘린더 날짜 상세는 그 날짜 패널이 열려 있는 동안 5초마다. 처음엔 캘린더 쪽을 "패널 진입 시 1회만" 동기화하도록 만들었다가, 실사용 테스트 중 "패널을 이미 열어둔 채로 다른 기기에서 막 기록한 게 안 보인다"는 문제가 재현되어 5초 주기로 바꿨다 — 진입 시 1회만으로는 화면을 계속 켜놓고 보는 사용 패턴에서 새 기록이 반영 안 되는 사각지대가 있었다.
- **실측 디버깅**: 데스크탑 로컬 데이터파일(`%APPDATA%\PhoneLockDesktop\data.json`)에서 Firebase 설정값을 직접 읽어 REST API로 이 계정의 실제 DB를 조회해, 데스크탑 쓰기가 실제로 성공했음을 먼저 확인한 뒤 안드로이드 쪽 문제로 범위를 좁혔다 — 코드만 보고 추측하지 않고 실제 서버 상태를 직접 확인하는 게 크로스디바이스 버그 디버깅에 효과적이었다.

## 타이머 크로스디바이스 "미러" 표시: 실시간 숫자는 보여주되 원격 제어는 하지 않음 (2026-08-07, 34차 세션)
24차 결정으로 타이머 실행 상태(경과 초)는 기기별 로컬이라 다른 기기 화면엔 안 보였는데, 사용자가 "데스크탑에서 2분이면 모바일도 2분으로 보여야 한다"고 명시적으로 요청 — 이 기기 타이머가 꺼져 있을 때, 다른 기기가 재고 있으면 시작 버튼 없이 메인 화면 자체가 자동으로 "실행 중" 상태(phase 배지·업무 이름·큰 숫자)를 그대로 미러링해서 보여주도록 바꿨다.

- **push 페이로드 확장**: 기존 `users/{user}/pomodoro`는 `timerActive`/`breakActive`/`phaseEndAt`/`mode`만 있었는데(크로스디바이스 잠금 해제 신호용, 24차), 미러 표시에 필요한 `phaseStartedAt`/`taskName`을 추가해서 다른 기기가 이 값으로 로컬에서 경과/남은 시간을 자체 계산할 수 있게 했다.
- **정지/전환 버튼은 원격 제어하지 않는다**: 19차 세션의 `pomodoro/remoteCommand`(원격 명령 채널)가 "공부앱이 그 명령을 구독하고 있어야만 실제로 반영되는" 비동기 왕복 구조라 신뢰성 문제(무반응 버그 5건)로 이어졌던 걸 24차에서 아예 제거했었다 — 같은 실수를 반복하지 않기 위해, 미러링 중엔 정지/전환 버튼 자체를 숨기고 "다른 기기에서 실행 중입니다 — 정지·전환은 그 기기에서 해주세요" 안내만 보여준다. 표시는 실시간에 가깝게(3초 폴링), 제어는 항상 그 세션을 시작한 기기에서만.
- **오래된 신호 방어(20분 컷오프)**: 다른 기기가 타이머를 켠 채로 정지 없이 앱을 완전히 꺼버리면 Firebase엔 "실행 중"이라는 마지막 값이 그대로 남는다. `_ts`(push 시각)를 함께 저장해 읽는 쪽에서 20분 넘게 갱신이 없으면 신호를 무시하도록 해서, 미러가 유령 상태로 계속 "실행 중"으로 보이는 걸 방지했다.

## 공부 잠금 허용 프로그램 실행: 이름만으로 찾는 3단계 폴백 (2026-08-07, 33차 세션)
`StudyLockScreen`의 허용 프로그램 버튼이 사용자가 등록한 이름("chrome.exe" 같은 맨 파일명)만으로 실제 실행파일을 찾아 켜야 하는데, JVM `ProcessBuilder(appName)`은 PATH 환경변수만 검색해서 대부분 실패했다(`CreateProcess error=2`). 1차로 `cmd /c start`(Windows 탐색기와 같은 검색 방식)로 바꿔봤지만 ① 매번 콘솔 창이 잠깐 뜨고 ② 그래도 못 찾는 앱(Discord류 Electron 설치형, App Paths 레지스트리에 등록 안 됨)은 창만 남긴 채 조용히 실패하는데 `cmd.exe` 프로세스 자체는 정상 종료라 우리 쪽 로그엔 "성공"으로 잘못 찍혔다(사용자가 실기기에서 재현).

- **최종 해결책은 순서가 있는 3단계 이름 해석**: ① 레지스트리 `HKCU`/`HKLM` `App Paths`(Chrome/Edge 등이 설치 시 등록하는 곳, `Advapi32Util`로 조회) → ② 시작 메뉴 바로가기(`%APPDATA%`/`%ProgramData%`의 `Microsoft\Windows\Start Menu\Programs`를 재귀 검색해 파일명이 일치하는 `.lnk`, Discord류가 여기 있음) → ③ 둘 다 못 찾으면 예전처럼 `ProcessBuilder(appName)`(PATH 검색)를 시도. 실행은 못 찾으면 정직하게 실패 토스트를 띄운다(예전 `cmd` 방식처럼 거짓 성공을 내지 않음).
- **`.lnk`는 `Desktop.getDesktop().open()`이 아니라 `explorer.exe`로 열게 함**: `.lnk`를 resolve해도 JDK의 `Desktop.open()`은 "Unsupported URI content"로 실패했다(실기기 재현) — Windows 바로가기 해석은 JDK가 공식 지원하는 URI 콘텐츠 타입이 아닌 것으로 보인다. `ProcessBuilder("explorer.exe", lnkPath)`로 대신 열면 탐색기가 바로가기를 해석해서 대상 실행파일을 띄워준다(창 깜빡임 없음).
- **JNA는 이미 프로젝트 의존성**: `ForegroundWindowWatcher.kt` 등에서 이미 `jna-platform`을 쓰고 있어 새 의존성 추가 없이 `Advapi32Util`로 레지스트리 조회가 가능했다.
- **적용 범위는 데스크탑뿐**: 안드로이드는 애초에 패키지명 기준 `Intent`로 앱을 실행하므로 이런 "맨 이름으로 실행파일 찾기" 문제 자체가 없다.

## UI를 웹앱과 맞출 때는 스크린샷 추측 대신 `index.html` 소스(CSS/JS)를 먼저 확인 (2026-08-07, 32차 세션)
계산기 결과 카드 디자인을 사용자가 보내준 스크린샷만 보고 여러 차례 추측했다가("카드 전체가 상태색으로 물듦" → "판정 배너가 2줄" → "필요 행이 amber" 등) 계속 틀려서 사용자가 반복 정정해야 했다. 결국 사용자가 "웹앱을 직접 읽어서 구현할 수도 있는 거였냐"고 지적한 뒤에야 `공부앱/index.html`의 실제 CSS 클래스(`.result-block`/`.dday-badge`/`.progress-bar-fill`/`.pace-table`/`.result-verdict` 등)와 그 값을 만드는 JS 템플릿(`calculate()` 함수)을 grep해서 정확한 값을 확인하는 방식으로 전환했고, 그 뒤로는 타이머(`renderTimer()`, `.pomo-*`/`.timer-*`/`.study-log-*` 클래스)와 캘린더(`.day-cell`/`.task-chip`/`.task-name-modal`/`.modal-btn` 클래스)도 처음부터 이 방식으로 진행해 정정 없이 한 번에 맞았다.
- **원칙**: 관리앱 네이티브 UI를 웹앱과 시각적으로 맞출 때는 스크린샷을 역추론 재료로 쓰지 말고, `공부앱/index.html`에서 관련 CSS 클래스명과 그 클래스를 만드는 JS 함수를 먼저 Grep해서 정확한 색상 hex값·padding·border-radius·텍스트 구조를 확인한다. CSS 변수(`--accent`/`--green`/`--yellow`/`--red`/`--muted`/`--border`/`--surface`)는 관리앱 테마(`ui/theme/Color.kt`)에 이미 거의 1:1 대응값이 있다.
- **부가 발견**: 계산기 결과 카드에 필요했던 "기간 내 X 소화 가능"/"마감 N일 초과" 표시는 `CalcEngine.CalcResult`에 이미 `totalCapacity`/`finishDiffDays` 필드로 존재했는데 UI에서 그동안 안 쓰고 있었을 뿐이었다 — "새 데이터가 필요하다"고 판단하기 전에 기존 모델 필드부터 확인할 것. 메모리 [[feedback_gwanrieob_ui_reference_source_first]]로 기록해뒀다.

## 계산기 데스크탑 레이아웃을 28차의 3탭 구조에서 웹앱 사이드바 구조로 되돌림 + 폴더 목록 자동 보정 (2026-08-07, 31차 세션)
28차 UI 개편으로 바뀐 데스크탑 계산기(입력/결과/저장됨 3개 동등 탭 + 저장됨 좌우 분할 폴더 탐색기)를 실사용해보니 세 가지 문제가 드러남 — ① 계산 버튼을 누르면 크래시, ② 저장 항목의 폴더 표시는 되는데 좌측 폴더 목록엔 안 뜸, ③ 결과가 별도 탭 뒤에 숨어 있어 계산 직후 결과를 못 찾음.

- **레이아웃을 28차 이전 방향(웹앱 사이드바 구조)으로 되돌림**: "결과가 탭 뒤에 숨는다"는 문제의 근본 원인이 애초에 "입력/결과/저장됨을 동등한 탭 3개로 만든 것" 자체였다고 판단 — 웹앱(`공부앱/index.html`)은 처음부터 왼쪽 좁은 사이드바(업무 입력/저장됨 서브탭)+오른쪽 넓은 칸(결과, 서브탭이 아니라 항상 보이는 영역)의 좌:우 2:8 구조였다. 28차에 이걸 "카드 그리드로 보기 좋게"라는 목적으로 3탭 구조로 바꿨던 게 오히려 웹앱 UX와 어긋나는 회귀였다고 결론짓고, 원래 웹앱 구조로 복귀했다. 왼쪽 폭이 좁아진 만큼 입력 카드는 2열 그리드 대신 1열로, 저장됨은 좌우 분할 폴더 탐색기 대신 안드로이드판과 동일한 재귀 폴더 트리(세로 나열)로 함께 되돌렸다 — 결과적으로 데스크탑과 안드로이드의 "저장됨" 구현이 다시 대칭을 이룸(28차 이전 상태로 복귀). 안드로이드는 이번 변경 대상이 아니다(사용자가 안드로이드 쪽은 이미 문제없다고 확인).
- **폴더 목록 자동 보정(`healCalcFolderPaths`) 추가**: 저장 항목이 폴더 이름은 표시하는데 좌측 폴더 목록엔 그 폴더가 없는 문제의 원인은, 웹앱에서 만들어진 기존 Firebase 데이터가 항목별 `folderPath`만 갖고 폴더 트리 자체(`savedFolderTree`)는 비어있는 채로 동기화된 경우였다(웹앱 자체도 이 시나리오를 감안해 `rebuildFolderTreeFromItems`로 자동 복구하는 로직을 갖고 있음). 네이티브 쪽엔 이 보정이 없었기 때문에, `syncCalculatorFromFirebase()` 실행 시 저장 항목이 참조하는 폴더 경로(및 조상 경로)가 로컬 폴더 목록에 없으면 자동으로 채워 넣고 Firebase에도 다시 푸시하는 `healCalcFolderPaths()`를 양 플랫폼 Repository에 추가했다. 매번 동기화할 때마다 검사하므로 이후에 같은 이유로 폴더가 다시 누락돼도 계산기 탭을 열 때마다 스스로 복구된다.
- **`filterIsInstance<Pair<A, B>>`의 제네릭 타입 소거 함정은 코드 패턴 자체를 지양**: 크래시 원인이 "Kotlin은 제네릭 타입 인자를 런타임에 소거하므로 `is Pair<X, SpecificType>` 같은 검사는 실제로 `is Pair<*, *>`만 확인한다"는 점이었다. 앞으로 sealed class로 분기해야 하는 리스트를 거를 땐 `filterIsInstance<Pair<_, SpecificSubtype>>()` 같은 패턴을 쓰지 말고, `mapNotNull { if (it.second is X) ... else null }`처럼 `is` 체크를 직접 하는 패턴을 표준으로 삼는다(안드로이드판 `CalcResultTab`은 애초에 `when` 분기라 이 함정이 없었음 — 데스크탑판만 최적화하려다 생긴 버그).

## 5단계(통계) 네이티브 재구현: 새 데이터 모델 없이 캘린더 전체 집계, 계산기 연동 계속 제외 (2026-08-07, 30차 세션)
4단계(일정표) 완료 후 사용자가 실기기 검증보다 5단계 착수를 먼저 택함. 23차에서 확정한 5단계 로드맵(타이머→캘린더→계산기→일정표→통계)의 마지막 단계.

- **데이터 출처는 캘린더 일정 전체(`getAllCalendarTasks`)이지 계산기/일정표와는 무관**: 웹앱 `renderStats()`가 `calTasks` 전체(날짜 무관, 회독 색상/완료 상태만 있는 캘린더 항목)를 집계하는 것과 동일 — 일정표(4단계)가 계산기 draft를 파생했던 것과 달리, 통계는 캘린더를 파생한다. 두 파생 뷰가 서로 다른 원본 데이터를 가리키는 것은 웹앱 구조 자체가 그렇기 때문(계산기 업무는 진행률/페이스 계산용, 캘린더 일정은 완료 체크 기반 회독 진행용 — 별개 개념).
- **저장소에 "전체 조회" 함수를 새로 추가**: 기존 `getCalendarTasks(dateKey)`/`getCalendarTasksInRange(from, to)`는 특정 날짜(범위)만 조회했는데, 통계는 날짜 제한 없이 전 기간 일정이 필요해 `getAllCalendarTasks()`(데스크탑, `data.calendarTasks.toList()`)/`getAllCalendarTasksOnce()`(안드로이드, 기존 Room DAO의 `getAllOnce()` 재사용 — Firebase 동기화용으로 이미 있던 쿼리를 통계에도 그대로 씀)를 추가했다. 새 DAO 쿼리를 만들지 않고 기존 걸 재사용한 것은 CLAUDE.md "중복 코드를 만들지 않는다" 원칙을 따른 것.
- **연속 완료일(스트릭) 계산 로직을 웹앱 그대로 이식**: "일정이 없는 날은 스트릭을 끊지 않고 건너뛴다(중립), 일정이 있는데 전부 완료가 아니면 중단"이라는 웹앱의 미묘한 규칙(단순 "매일 완료해야 유지"가 아님)을 그대로 옮겼다 — 재설계하면 사용자가 이미 웹앱에서 익숙해진 스트릭 값과 어긋날 위험이 있다고 판단.
- **캘린더 연동(`linkedCalc`) 계속 제외**: 3~4단계에서 이미 제외한 범위를 그대로 유지 — 이번 통계도 계산기 결과와는 무관하게 캘린더만으로 완결된다.
- **플랫폼별 UI 차이는 화면 폭 대응뿐**: 30일 막대그래프 아래 날짜 숫자를 안드로이드는 5일 간격(+오늘)만 표시하고 데스크탑은 매일 표시 — 로직/데이터는 동일, 좁은 화면에서 30개 라벨이 겹치는 문제만 해결한 순수 UI 조정이라 별도 설계 결정이라 할 것도 없지만, 이 화면 하나만 플랫폼 코드가 살짝 갈리는 이유를 남겨둔다.
- **범위**: 이로써 23차 세션에서 확정한 공부앱 네이티브 재구현 5단계 로드맵(타이머/뽀모도로→캘린더→계산기→일정표→통계) 전체가 코드/빌드 기준으로 완료됐다. 남은 것은 전 단계 통틀어 실기기 검증뿐 — [[HANDOFF.md]] 참고.

## 4단계(일정표) 네이티브 재구현: draft 업무 기준 읽기 전용 뷰, 플랫폼별 뷰 형태 유지 (2026-08-07, 29차 세션)
3단계(계산기) 완료 후 사용자가 실기기 검증보다 4단계 착수를 먼저 택함. 일정표는 웹앱에서도 자체 데이터가 없는 "파생 뷰"라 새 데이터 모델이 필요 없었다.

- **데이터 출처는 계산기의 draft 업무 목록(`repository.getCalcTasks()`)이지 저장됨(`getCalcSaved()`) 목록이 아니다**: 웹앱 `renderTimetable()`이 `loadCalcFromStorage().tasks`(입력 폼의 현재 draft)를 읽는 것과 동일 — 사용자가 계산기 "입력" 탭에 채워둔 값이 바로 일정표에 반영되어야 계산기와 일정표가 같은 화면의 다른 관점처럼 동작한다. 저장됨 목록을 쓰면 "계산 후 저장"까지 해야만 일정표에 나타나는 부자연스러운 흐름이 된다.
- **캘린더 연동(`linkedCalc`/`progressStep` 완료 체크마크)은 이번에도 제외**: 3단계에서 이미 뺀 기능이고 별도 데이터 모델 추가가 필요해 범위가 커진다 — [[3단계(계산기) 네이티브 재구현]]과 같은 판단 유지.
- **데스크탑/안드로이드가 서로 다른 뷰 형태를 유지**: 웹앱이 `isMobile()`(`window.innerWidth <= 640`)로 데스크탑=주간 테이블, 모바일=일 단위 리스트+◀▶ 날짜 이동으로 분기하던 것을, 네이티브에서는 플랫폼 자체가 이미 그 구분을 대신하므로 그대로 각 플랫폼에 고정 이식(런타임 분기 불필요). 데스크탑은 이번 주(일~토) 고정 테이블 하나만 두고 주 이동 네비게이션은 만들지 않았다 — 웹앱 데스크탑 뷰도 원래 주 이동이 없었다.
- **읽기 전용**: 업무 이름/요일별 목표량 수정은 계산기 탭에서만 하고, 일정표는 그 결과를 보여주기만 한다 — 웹앱도 동일 구조라 새로 설계할 필요가 없었다.
- **범위**: 일정표 뷰만 — 통계는 5단계. 상세 구현은 [[HANDOFF.md]] "현재 중요한 파일" 참고.

## 관리앱 UI 전면 개편: 앱 전체 다크 테마 + 플랫폼별 네비게이션 분화 + 데스크탑 좌우 분할 레이아웃 (2026-08-07, 28차 세션)
실기기 1차 검증 중 사용자가 UI에 대해 세 차례에 걸쳐 피드백을 줌: ① 탭이 6개나 나란히 있어 산만하다, ② 다크 테마 적용 범위를 물었을 때 "앱 전체"로 확정, 데스크탑 최적화 방향은 "왼쪽 사이드바"로 확정(AskUserQuestion), ③ 재배포 후에도 "데스크탑만의 UI가 없다"며 재차 피드백 — 결국 "가로 꽉 채우고 세로로 쭉 내리는 게 아니라 가로/세로 역할을 나눠서 보기 좋게"라는 구체적 요구로 확정됨.

- **테마 범위는 관리앱 전체(그룹/통계/설정 포함)**: 공부앱 탭만 다크로 바꾸는 절충안도 제시했지만 사용자가 일관성을 택함 — `Color.kt`/`Theme.kt`(양 플랫폼)를 공부앱(`공부앱/index.html`)의 다크 팔레트로 완전 교체(`lightColorScheme`→`darkColorScheme`), 기존 따뜻한 라이트 톤(주황/베이지) 폐기.
- **네비게이션은 플랫폼별로 다른 형태를 택함**: 데스크탑은 왼쪽 `NavigationRail`(관리앱/공부앱/설정) + 섹션 내부 `TabRow` 서브탭, 안드로이드는 기존처럼 하단 `NavigationBar`(3탭으로 축소) + 섹션 내부 `TabRow` 서브탭. 모바일까지 사이드바로 바꾸지 않은 이유는 사용자가 "데스크탑 전용 최적화"를 명시적으로 요청했고, 하단 탭이 모바일 관례에 더 맞기 때문.
- **데스크탑 화면 배경이 흰색으로 보이는 버그의 교훈**: `MaterialTheme`은 색상 팔레트만 정의할 뿐 캔버스를 실제로 칠하지 않는다 — 최상위 `Window` 콘텐츠를 `Surface(color = background)`로 감싸지 않으면, 개별 화면이 덮지 않는 여백은 OS 기본 배경(흰색)이 그대로 비친다. 전체화면 인터스티셜 창들(`WatchAndWaitScreen`)은 이미 `Surface { }`로 감싸고 있어 이 문제가 없었던 것과 대조적 — Compose Desktop에서 새 최상위 창을 만들 때마다 반드시 확인할 항목으로 기억해둘 것.
- **데스크탑 3개 화면(타이머/캘린더/계산기)을 좌우 분할로 재작성, 모바일은 그대로 유지**: "데스크탑만의 UI"라는 요구가 막연해서 재확인 없이 구체화가 필요했는데, 사용자가 "가로 세로 역할과 구분을 딱딱 나눠서"라고 명확히 함 — 단순히 여백/폭 조정이 아니라 실제 레이아웃 구조(마스터-디테일, 좌우 패널)를 요구한 것으로 해석. 타이머(왼쪽 타이머 본체/오른쪽 설정+기록), 캘린더(왼쪽 월 그리드/오른쪽 날짜 상세), 계산기(입력·결과는 2열 카드 그리드, 저장됨은 파일탐색기 스타일 폴더 좌측 패널+항목 우측 패널)로 재작성. 안드로이드는 손대지 않음 — 모바일 폭에서는 좌우 분할이 오히려 부적합.
- **계산기 "저장됨" 탭을 재귀 중첩 트리에서 폴더 탐색기 좌우 분할로 바꾼 이유**: 기존 구조는 모든 하위 폴더를 펼쳐야만 그 안의 항목이 보였는데, 이게 "저장됨에 아무것도 안 보인다"는 사용자 착시(혹은 실제 누락)를 유발했을 가능성이 있다고 판단 — "전체" 뷰를 기본 선택지로 둬서 폴더 위치와 무관하게 모든 항목을 한 번에 볼 수 있게 함으로써 재발을 원천 차단.

## 3단계(계산기) 네이티브 재구현: 폴더 트리 포함 전체 이식, Firebase 3구간 독립 LWW (2026-08-07, 27차 세션)
2단계(캘린더) 완료 후 착수. 착수 전 사용자와 확인한 두 가지: ① "저장됨" 목록의 폴더 트리(하위 폴더 생성/이름변경/삭제/순서변경, 드래그 대신 ▲▼ 버튼)까지 웹앱과 동일하게 전부 구현 — 이번에도 범위를 좁히자는 제안(단순 목록만)이 있었지만 사용자가 폴더 기능 자체를 실사용 중이라 전체 구현을 택함, ② 캘린더 연동(`linkedCalc`/`progressStep` 채우는 UI)은 2단계와 마찬가지로 이번에도 제외.

- **Firebase 경로/스키마를 웹앱과 완전히 동일하게 유지**: `users/{user}/calculator`에 `{tasks, tasksTs, saved, savedTs, savedFolderTree, savedFolderTreeTs, savedFolderOrder, savedFolderOrderTs}`를 그대로 읽고 쓴다. 캘린더처럼 기존 웹앱 데이터를 마이그레이션 없이 그대로 이어받기 위함.
- **동기화는 캘린더의 "전체 문서 단일 LWW"보다 한 단계 더 세분화 — draft(tasks)/저장됨(saved)/폴더 트리(savedFolderTree+savedFolderOrder) 3구간을 각각 독립 타임스탬프로 비교**: 웹앱 자체가 이미 이 세 구간을 별도 `.update()` 호출과 별도 `_ts` 필드로 관리하고 있어서(예: 폴더만 옮겨도 `savedFolderOrderTs`만 갱신되고 `tasksTs`는 그대로), 캘린더처럼 하나의 `_ts`로 뭉뚱그리면 웹앱 쪽이 이미 하고 있는 "부분 갱신"과 충돌해 한쪽의 최신 변경이 다른 쪽의 오래된 값에 조용히 덮이는 문제가 생긴다. 다만 각 구간 내부는 캘린더와 동일하게 "화면 진입 시 1회 pull + 변경 직후 push"로 단순화했다(웹앱처럼 실시간 구독은 안 함) — 이 정도 단순화는 계산기 특성상(달력처럼 하루 종일 안 보고 있어도 되는 데이터) 문제 없다고 판단.
- **Android는 `HttpURLConnection`이 PATCH를 직접 지원하지 않아 `POST + X-HTTP-Method-Override: PATCH` 헤더로 우회**: Firebase의 `.update()`(부분 갱신)에 대응하는 REST 메서드는 PATCH인데, Android `HttpURLConnection`은 허용 메서드 목록에 PATCH가 없어 `setRequestMethod("PATCH")` 자체가 예외를 던진다. Firebase REST API가 공식 지원하는 이 헤더 우회 방식을 사용 — 데스크탑은 `java.net.http.HttpClient`가 임의 메서드를 허용해 `.method("PATCH", ...)`를 직접 쓸 수 있어 이 문제가 없다(플랫폼별 HTTP 클라이언트 차이에 따른 자연스러운 비대칭, 캘린더의 "id+sortOrder vs dateKey+ordinal"과 같은 종류의 차이).
- **폴더 트리의 로컬 표현을 웹앱의 중첩 객체(`savedFolderTree`) 대신 평평한 경로 리스트로 단순화**: Kotlin 데이터클래스로 임의 깊이 중첩 객체를 표현하려면 재귀 타입이 필요해 번거롭고, 필요한 연산(존재 확인/하위 목록 조회/삭제/이름변경)은 전부 경로 리스트만으로 충분하다. Firebase에 쓸 때는 `buildFolderTree()`/읽을 때는 `flattenFolderTree()`로 웹앱의 중첩 객체 스키마와 상호 변환한다 — 로컬 표현만 다를 뿐 Firebase에 올라가는 실제 데이터는 웹앱과 100% 동일하다.
- **계산 로직(`CalcEngine`)은 별도 순수 함수 모듈로 분리**: 웹앱 `calculate()`/`simulateFinish()`/`calcRequiredPace()`/`countDaysInRange()`를 `LocalDate` 기반으로 그대로 이식하되(요일 인덱스는 JS `getDay()`와 맞추기 위해 0=일~6=토로 변환), UI나 Repository에 흩어놓지 않고 입출력이 명확한 순수 함수로 뒀다 — 타이머/캘린더보다 계산 로직 자체가 복잡해서(페이스 역산, 10년 시뮬레이션 등) 테스트/검증 가능성을 위해 분리하는 게 맞다고 판단.
- **범위**: 계산기 기능만 — 일정표/통계는 4~5단계 이후. 상세 구현은 [[HANDOFF.md]] "현재 중요한 파일" 참고.

## 2단계(캘린더) 네이티브 재구현: Firebase 전체 문서 LWW로 기존 웹앱 데이터 그대로 이어받기 (2026-08-07, 25차 세션)
1단계(타이머) 완료 후 착수. 사용자와 확인한 두 가지: ① 캘린더 데이터는 Firebase로 기기 간(안드로이드/데스크탑) 동기화하고 기존 웹앱과 같은 경로를 써서 기존 데이터를 그대로 이어받는다(24차의 "허용 프로그램/사이트는 기기별 로컬"과는 다른 선택 — 캘린더는 개인 할 일 목록이라 기기별로 다르면 오히려 불편하다는 판단), ② 타이머 탭의 자유 텍스트 업무 입력을 "오늘 캘린더 일정" 드롭다운으로 교체(웹앱도 원래 이 방식).

- **Firebase 경로/스키마를 웹앱과 완전히 동일하게 유지**: `users/{user}/calendar`에 `{ tasks: {dateKey: [...]}, _ts }` 전체 문서를 그대로 읽고 쓴다. 새 경로를 만들지 않은 이유는 사용자가 이미 웹앱에 입력해둔 캘린더 데이터가 있고, 같은 경로를 쓰면 마이그레이션 없이 자동으로 이어받기 때문. 동기화 방식은 1단계에서 확립한 "전체 문서 단위 LWW"(`_ts` 비교, 로컬/원격 중 최신이 승리) 그대로 재사용 — 캘린더는 타이머와 달리 크로스디바이스 실시간성이 중요하지 않아(하루 종일 봐도 되는 일정) 페이즈 전환마다 push하는 타이머와 달리 "화면 진입 시 1회 pull + 각 변경 직후 push"로 충분하다고 판단.
- **웹앱 색상 키와 실제 표시색이 어긋나는 것을 그대로 유지**: `color` 필드는 `red`=1회독(실제로는 회색 표시), `orange`=2회독(실제로는 빨강), `yellow`=3회독(노랑), `green`=4회독(초록)이다. 이름을 정리하고 싶은 유혹이 있었지만, 기존 Firebase 데이터의 값과 정확히 일치해야 마이그레이션 없이 호환되므로 웹앱 코드 그대로(어색해 보여도) 이식했다.
- **완료 시 자동 다음 회독 생성 로직도 그대로 이식**: `red→orange +1일`, `orange→yellow +3일`, `yellow→green +7일`(green은 종단), `nextDays` 필드로 사용자가 간격을 오버라이드하면 다음 단계에도 그 값이 전파된다. 취소(완료 해제) 시 아직 손대지 않은 자동생성 다음-회독 항목을 되돌리는 로직(`revertCalendarAutoSchedule`)도 웹앱 `undoMarkStatus`를 그대로 따랐다.
- **순서 관리 방식이 플랫폼별로 다름**: 웹앱은 배열 순서=표시 순서였다. 데스크탑은 원본처럼 통짜 리스트(`AppData.calendarTasks: MutableList`) 안에서 같은 날짜 항목들의 상대 위치로 순서를 표현하고 `dateKey+ordinal`(정수 인덱스)로 조작한다. 안드로이드는 Room을 쓰므로 배열 개념이 없어 `sortOrder: Int` 필드를 추가해 같은 날짜 안에서의 순번을 저장한다 — 두 플랫폼의 Repository 함수 시그니처가 다르다(데스크탑은 `dateKey+ordinal`, 안드로이드는 `CalendarTask` 객체+Room의 `id`를 직접 사용). 이는 각 플랫폼의 기존 데이터 저장 방식(JSON 통짜 파일 vs Room)을 그대로 따른 자연스러운 차이로, 통일하려고 추상화 레이어를 새로 만들지 않았다(CLAUDE.md "불필요한 리팩터링 금지" 원칙).
- **"할당량 연동 추가"(계산기 연동) 섹션은 이번 단계에서 제외**: 계산기(3단계) 자체가 아직 없어 연동할 대상이 없다. `linkedCalc`/`progressStep` 필드는 데이터 모델에 남겨뒀지만(향후 호환용, 항상 null) UI로 설정하는 경로는 만들지 않았다. 계산기 구현 시 이 필드들을 채우는 UI만 추가하면 되도록 필드 이름/의미를 웹앱과 동일하게 유지했다.
- **범위**: 캘린더 기능만 — 계산기/일정표/통계는 3단계 이후. 상세 구현은 [[HANDOFF.md]] "현재 중요한 파일" 참고.

## 1단계(타이머/뽀모도로) 네이티브 재구현: 크로스디바이스는 Firebase 유지, 같은 기기 판정은 로컬 직접 읽기 (2026-08-07, 24차 세션)
23차에서 확정된 재구현 방침의 1단계 착수. 착수 전 사용자와 두 가지를 확인: ① 공부앱 웹 버전은 장기적으로 완전히 네이티브가 대체(당장은 손대지 않음), ② 단계 순서는 타이머/뽀모도로 → 캘린더 → 계산기 → 일정표 → 통계.

- **핵심 발견**: 탐색 결과, 기존 구조는 "다른 기기에 신호를 주는" 크로스디바이스 동기화뿐 아니라 **같은 기기 안에서의 즉시 잠금 판정조차** Firebase RTDB 왕복(`PomodoroSyncClient.isStudyTimerActive()` 등, 5초 캐시)에 의존하고 있었다. 원본 상태는 웹앱(브라우저 탭/WebView)이 타이머가 바뀔 때마다 Firebase에 write한 것이라, 웹뷰가 백그라운드로 밀려 JS가 스로틀되거나 탭이 닫히면 이 write 자체가 멈췄다. 정지/전환 버튼도 `evaluateJavascript` 또는 `pomodoro/remoteCommand` 노드를 거치는 비동기 왕복이라 실패해도 신호가 없었다 — 이게 23차까지 계측을 추가해도 못 고쳤던 근본 원인.
- **결정**: 타이머 상태(phase/mode/phaseStartedAt/phaseEndAt/cycleCount/breakExtraUsed)를 각 플랫폼 로컬(안드로이드 `AppPreferences`+Room `StudyLogEntry`, 데스크탑 `data.json`의 `TimerRunState`)에 직접 저장하는 것을 유일한 source of truth로 삼는다. **같은 기기 자신의 공부 잠금 판정/제어**(`AppMonitorAccessibilityService`/`EnforcementService`의 `checkStudyLock`, `StudyLockActivity`/`StudyLockScreen`의 정지·전환 버튼)는 이제 이 로컬 상태를 동기 직접 읽고 쓴다 — 네트워크도, 별도 프로세스(웹뷰)도 거치지 않는다.
- **Firebase는 부가 채널로 남김**: 페이즈가 바뀔 때마다(매초 아님) 새 함수 `PomodoroSyncClient.pushLocalStudyStatus()`(양 플랫폼 대칭)로 `users/{user}/pomodoro`에 기존과 같은 스키마로 write한다. `isBreakActive`/`currentPhaseEndAt`/`isStudyTimerActive`/`isPomodoroMode` 같은 읽기 함수는 그대로 남겨뒀지만, 호출부의 의미가 "이 기기 자신의 판정"에서 "**다른 기기**가 지금 공부/휴식 중인지"로 바뀌었다(`LockEvaluator.isPomodoroUnlockActive`가 유일하게 아직 이걸 호출 — `pomodoroUnlockEnabled` 그룹의 크로스디바이스 자동 해제 기능은 그대로 유지하기 위해 의도적으로 남김).
- **제거**: `pomodoro/remoteCommand` 채널(`sendRemoteCommand`/`subscribeRemoteCommand`) 전체, 안드로이드 `StudyAppScreen`(WebView 임베드)+`StudyAppWebViewHolder`, 데스크탑 "공부앱 열기(브라우저)" 탭 — 전부 로컬 제어로 대체되어 불필요해짐.
- **허용 프로그램/사이트 목록도 로컬로 이전**: 기존엔 데스크탑에 이 입력 UI 자체가 없었고(웹앱 "타이머" 탭 → Firebase `studyLockConfig`에만 있었음), 안드로이드 허용 사이트도 같은 Firebase 값을 읽기만 했다. 이번에 데스크탑엔 `StudyTimerScreen`에, 안드로이드엔 `StudyLockAppsScreen`에 각각 네이티브 입력 필드를 추가했다. **트레이드오프**: 예전엔 한 번 입력하면 두 기기가 자동으로 공유됐지만, 이제는 각 기기에서 따로 입력해야 한다(허용 프로그램은 애초에 플랫폼마다 형식이 달라 공유 불가능했고, 허용 사이트만 공유가 가능했었음) — 로컬 우선이라는 이번 결정의 자연스러운 귀결로 판단해 그대로 감수했다. 필요해지면 2단계 이후에 사이트 목록만 Firebase로 재동기화하는 걸 재검토할 수 있다.
- **타이머 로직 자체는 웹앱 원본 그대로 이식**(재설계 아님): wall-clock 기반 계산, 공부→휴식은 시간을 다 채워야 전환·휴식→공부는 언제든, "5분만 더" 1회 유예. 웹앱 최신 버전 기준(유예 후 자동 강제전환은 이미 제거된 상태)을 따랐다.
- **범위**: 타이머/뽀모도로 기능만 — 캘린더/계산기/일정표/통계, 기존 Firebase `studyLog` 데이터 마이그레이션은 2단계 이후로 미뤘다. 지금은 캘린더가 없어 업무 이름을 자유 텍스트로 입력한다(2단계에서 캘린더 연동으로 교체 예정).

## 공부앱을 웹/웹뷰가 아닌 완전 네이티브로 재구현하기로 결정 (2026-08-07, 23차 세션) — 다음 세션 최우선 착수 항목
22차에서 사용자가 아이디어로 제안했던 것([[IDEAS.md]]에 있었음)을, 23차 세션에서 데스크탑 정지/전환·허용앱 실행 버튼에 계측(로그+화면 배너)까지 추가하고도 공부 잠금 버그 5건이 전혀 해결되지 않자 사용자가 **확정된 방침**으로 격상했다. 더 이상 "아이디어"가 아니라 다음 세션이 가장 먼저 시작해야 하는 작업.

- **결정**: 공부앱(현재 `index.html` 단일 HTML/JS SPA)의 타이머 관련 기능을 웹뷰 임베드/Firebase 폴링·원격명령 방식이 아니라, 안드로이드/데스크탑 앱에 직접 통합된 네이티브 코드로 다시 구현한다.
- **왜**: 현재 아키텍처(공부앱은 별개 웹앱, 관리앱은 그 Firebase 상태를 읽기 전용 폴링 + `remoteCommand` 채널로 명령만 남김)는 근본적으로 "공부앱이 어딘가에 열려서 정상적으로 실행 중이어야만" 작동한다. 웹뷰가 백그라운드로 밀리면 JS 타이머/리스너가 스로틀링되고, Firebase 왕복 자체가 실패해도 관리앱 쪽엔 원인이 안 남았다(23차에서 로그를 붙였지만 그건 "원격 명령이 왜 실패하는지"를 보여줄 뿐, 애초에 웹뷰 스로틀링·별개 프로세스라는 구조적 문제 자체를 없애지는 못한다). 5건의 버그(정지/전환 무반응, 허용앱 무반응, Alt-Tab 우회, 잠금화면 안 닫힘, 허용앱 선택 화면 문제)가 전부 이 "관리앱과 공부앱이 별개 프로세스/웹뷰로 분리되어 있고 명령이 비동기 왕복을 거쳐야 한다"는 같은 구조적 원인에서 파생된 것으로 사용자가 판단.
- **범위**: 공부앱 전체(캘린더/계산기/시간표/통계 등)가 아니라 **타이머 기능만** 우선 네이티브로 이식하는 단계적 접근을 [[IDEAS.md]]에서 이미 제안한 바 있음 — 착수 시 이 범위 좁히기부터 사용자와 재확인할 것.
- **영향**: 착수하면 `PomodoroSyncClient.kt`(양쪽)의 원격 명령/폴링 로직, `StudyLockActivity`/`StudyLockScreen`/`StudyAppScreen`(WebView 임베드) 등 공부 잠금 관련 코드 전반이 재설계 대상이 됨. 착수 전 반드시 사용자와 상세 범위(어떤 기능부터, 공부앱 웹 버전은 유지할지)를 합의할 것 — 아직 구체적 설계는 없음.

## 공부 잠금 원격 제어 (2026-08-06, 19차 세션) — "관리앱은 읽기 전용" 원칙의 의도적 예외
공부 잠금 화면(전체화면)에서 타이머를 직접 정지하거나 뽀모도로 휴식으로 전환할 수 있게 해달라는 요청을 받아, `users/{user}/pomodoro/remoteCommand`(action, ts)를 신설해 데스크탑/안드로이드가 여기에 **쓰도록** 허용했다. 이는 "공부앱 연동은 항상 읽기 전용 폴링"이라는 기존 원칙(바로 아래 항목 참고)에 대한 의도적 예외다.

- **왜 이 방법뿐인가**: 타이머 로직(시작/정지/페이즈 전환, 뽀모도로 게이팅 규칙)은 전부 공부앱 JS에만 존재하고, 관리앱은 그 상태를 읽기만 하는 완전히 별개의 앱이다. 관리앱이 직접 `timerRun`을 조작할 방법이 없으므로, "명령을 남기면 공부앱이 실행"하는 방식이 유일한 선택지다.
- **한계(사용자에게 사전 설명 후 진행 동의 받음)**: 공부앱이 브라우저 탭이나(안드로이드는 WebView) 어딘가에 열려 있어야만 명령이 실제로 반영된다. 안 열려 있으면 Firebase에 명령이 대기하다가 다음에 공부앱이 열릴 때 처리된다(공부앱이 `remoteCommand.ts`를 최초 구독 시점에도 확인하기 때문). 안드로이드는 WebView에 직접 JS를 실행하는 방식(즉시 반영되지만 그 WebView가 열려있어야 하는 건 마찬가지)도 검토했으나, 데스크탑과 구현을 통일하기 위해 Firebase 경유 방식으로 통일했다.
- **기존 규칙을 우회하지 않음**: 원격 명령은 공부앱의 기존 함수(`timerStop()`/`timerSwitchPhase()`)를 그대로 호출하는 방식이라, "공부 시간을 다 채워야 휴식 전환 가능" 같은 기존 게이팅 로직이 원격 명령에도 동일하게 적용된다(전환 조건이 안 맞으면 조용히 무시됨 — 관리앱 쪽엔 실시간 `phaseEndAt`이 없어 버튼을 미리 숨기지 못하므로, 버튼 자체는 항상 보이되 안내 문구로 조건을 알려준다).
- **`mode` 필드 추가**: `pomodoro.mode`("plain"/"pomodoro")를 새로 push해서, 관리앱이 "휴식으로 전환" 버튼을 보여줄지 판단한다(일반 스톱워치 모드는 휴식 개념이 없음).

## 공부 잠금 (2026-08-06, 15차 세션)
14차 세션에서 "뽀모도로 작동 중 제한"으로 요청되었던 기능을 사용자와 논의해 다음과 같이 확정했다.

- **적용 범위**: 처음엔 "타이머 실행 중이면 공부/휴식 구분 없이 항상"으로 확인받아 구현했으나, 실제 동작을 보고 나서 사용자가 "공부 페이즈일 때만"으로 정정 요청해 변경했다(같은 세션 내). 최종적으로 공부앱 타이머가 "공부" 페이즈로 진행 중일 때만(일반 모드는 항상 공부로 취급, 뽀모도로 휴식 페이즈는 제외) 적용된다. 기존 `pomodoroUnlockEnabled`(휴식 중 특정 그룹 임시 **해제**)와는 방향이 반대이고 완전히 독립적인 기능이라, 그룹 시스템에 얹지 않고 전역(그룹 무관) 기능으로 구현했다 — 특정 그룹을 잠그는 게 아니라 "허용 목록 외 전부"를 잠근다.
- **데스크탑은 진짜 전체화면 차단이 가능**: 처음엔 `IDEAS.md`의 "진짜 클릭-통과 오버레이"(JNA로 `WS_EX_LAYERED|WS_EX_TRANSPARENT`) 작업이 필요하다고 판단했으나, 기존 `BlockScreen`이 이미 `Maximized`+`undecorated`+`alwaysOnTop` `Window`로 완전 차단(클릭 통과 불필요, 오히려 다 막는 게 목적)을 구현하고 있다는 걸 코드 확인 후 발견 — 같은 패턴을 그대로 재사용해 JNA 없이 구현했다. "코너 소형 위젯" 제약은 `UsageOverlayContent`(정보용, 클릭 통과 필요)에만 해당하고 차단용 화면과는 무관하다.
- **안드로이드는 진짜 차단 불가, 감지 후 재차단(베스트 에포트)**: 기기 소유자(device owner)/키오스크 모드 없이는 접근성 서비스로 다른 앱의 실행 자체를 막을 수 없다. 사용자에게 이 제약을 설명하고 "허용 안 된 앱이 뜨면 감지해서 잠금 화면으로 되돌리는" 방식(사용자가 예시로 든 "열품타" 앱과 유사한 방식)으로 진행하기로 합의했다.
- **허용 목록 지정 방식이 플랫폼마다 다르다**: 데스크탑은 공부앱 웹페이지가 설치된 프로그램 목록에 접근할 수 없으므로 사용자가 실행파일명(예: `chrome.exe`)을 직접 입력(공부앱 타이머 탭, Firebase `studyLockConfig` 노드로 동기화). 안드로이드는 네이티브 앱이 설치 앱 목록을 조회할 수 있으므로 목록에서 체크박스로 선택(안드로이드 앱 내부 설정, 기기 로컬 저장 — 이 목록은 같은 기기에서만 쓰이므로 Firebase 동기화 불필요).
- Firebase `pomodoro` 노드에 `timerActive` 필드를 추가했다(기존엔 `breakActive`/`phaseEndAt`만 있어 뽀모도로 "휴식"만 표현 가능했고 "타이머가 켜져 있다" 자체를 표현할 방법이 없었음).

### 허용 사이트 추가 (2026-08-06, 16차 세션)
허용 프로그램(exe명/패키지명)만으로는 브라우저 자체를 허용해버리면 그 안의 모든 사이트가 뚫리는 문제가 있어, "허용 사이트"(도메인)를 별도로 추가했다. 허용 프로그램은 플랫폼마다 형식이 달라 공유할 수 없지만(exe명 vs 패키지명), 도메인은 플랫폼 무관이라 데스크탑·안드로이드가 **하나의 Firebase 값을 공유**한다(공부앱 타이머 탭에서 한 번만 설정). 도메인 매칭 알고리즘은 플랫폼별로 기존 사이트 차단 로직과 일관성을 맞췄다 — 데스크탑은 기존 그룹 사이트 차단(`Repository.findGroupsForDomain`)과 같은 suffix 매칭(`host == d || host.endsWith(".$d")`, 확장이 넘겨주는 깨끗한 hostname 기준), 안드로이드는 기존 그룹 사이트 차단(`findGroupSitesForAddress`)과 같은 주소창 텍스트 substring 매칭(접근성 서비스로 읽은 원문 텍스트라 정밀한 hostname 파싱이 항상 가능하진 않음). 새 `LockReason.STUDY_LOCK` 값을 추가해 그룹과 무관하게 이 사유로 차단되었음을 구분한다.

## 문서 관리를 단일 HANDOFF.md에서 5종 문서 체계로 전환 (2026-08-05)
기존엔 `HANDOFF.md` 하나에 세션별 상세 작업 기록을 계속 누적하는 방식을 썼는데, 726줄/130KB까지 비대해져서 매 세션 시작마다 전체를 읽어야 하는 비효율이 커졌다. 역할별로 `HANDOFF.md`(현재 상태만)/`CHANGELOG.md`(누적 이력)/`DECISIONS.md`(설계 결정)/`BUGS.md`(버그)/`IDEAS.md`(아이디어) 5개 문서로 분리하기로 사용자가 결정. `HANDOFF.md`는 항상 "다음 세션이 2~3분 안에 이해할 수 있는" 분량으로 유지하고, 오래된 세션 상세는 `CHANGELOG.md`로만 이관한다. 참고로 공부앱(별개 프로젝트)은 이 변경과 무관하게 기존 단일 HANDOFF.md 방식을 그대로 유지한다.

## 표시값(UI)과 판정 로직(보안)은 항상 분리한다
사용자가 "오버레이 불투명도가 갑자기 오르는 게 불안정해 보인다"고 요청했을 때, 처음엔 재확인 게이트(`ConfirmationGate`) 자체의 쿨다운/연속성 판정을 바꿔서 해결하려 했다가 실제 확인 절차까지 사라지는 부작용이 생겨 전부 원복했다(2026-07-30, 3차 세션). 최종 해법은 오버레이가 "보여주는" 값에만 캡을 씌우는 것(`OverlayLevelRatchet`)이고, 재확인/escalation 판정 로직은 전혀 건드리지 않았다.
**원칙**: 표시(오버레이 알파, UI 문구 등)와 판정(언제 잠글지/언제 재확인을 요구할지)은 항상 별개의 축으로 취급한다. 표시 요청을 판정 로직 변경으로 확대 해석하지 말 것. 애매하면 먼저 "이렇게 하면 판정 로직도 바뀌는데 괜찮은지" 확인부터 받는다.

## 시간대/일일한도는 항상 OR로만 판정 (AND 융합 옵션 삭제)
2026-07-30(6차 세션)에 "시간대+일일한도를 AND로 융합"하는 옵션(`requireAllConditions`)을 완전히 삭제했다. 관리 종류(스케줄/일일한도/실행확인)를 각각 독립 on/off + 독립 적용시간대로 재구성하면서, 조건을 겹쳐서 요구하는 기능 자체를 없애는 게 더 단순하고 사용자 의도(개별 관리 종류를 각자 원하는 시간대에만 적용)에 맞다고 판단.

## "관리 종류"에 스케줄 토글을 포함하되, 우회 방지 로직을 함께 확장
스케줄 on/off를 그룹 편집 화면의 "관리 종류" 토글에도 노출할지 애매했다 — 원래 스케줄 on/off는 그룹 목록 화면 스위치에만 있었고 거기엔 끄기 시도 시 회유 멘트 절차(`scheduleOffPending`)가 걸려 있었는데, 편집 화면 저장 로직(`detectWeakeningEdit`)은 "스케줄 자체를 끄는 행위"를 검사하지 않아 편집 화면에 그냥 토글을 추가하면 회유 절차를 우회하는 새 경로가 생긴다. 사용자 확인 후 "토글 추가"로 결정하고, 대신 `detectWeakeningEdit`에 "오늘 요일 제한이 실제로 걸려있는 도중에 이 화면에서 스케줄을 바로 끔"을 약화 항목으로 추가해 우회를 막았다(2026-07-30, 6차 세션). 이후 일일한도/실행확인의 적용시간대·요일마스크에도 동일 원칙(그 기능을 통째로 끄는 것과 같은 효과의 우회는 전부 같은 수준으로 방어)을 일관 적용했다.

## 그룹 전체 on/off(`groupEnabled`)와 스케줄 on/off(`scheduleEnabled`)를 분리
원래 그룹 목록 화면의 마스터 스위치가 `scheduleEnabled` 하나로 "그룹 전체 on/off"와 "스케줄 관리 종류 on/off"를 겸하고 있었다(설계 결함). `groupEnabled`(그룹 전체, 회유 멘트 절차 포함)를 신설해 분리하고, `scheduleEnabled`는 스케줄 관리 종류 전용으로 축소했다(2026-08-04, 9차 세션). 참고로 별도 필드 `enabled`는 5차 세션에 "통계 탭 표시 필터 전용, 차단 로직과 무관"으로 이미 확정되어 있었으므로 재사용하지 않고 새 필드를 만들었다. 안드로이드가 이 결론과 달리 `LockEvaluator`에서 `group.enabled`도 체크하고 있던 플랫폼 간 불일치도 이 기회에 데스크탑과 통일했다.

## 데스크탑 네이티브 오버레이는 전체화면이 아닌 코너 소형 위젯
Compose Desktop `Window`는 `transparent=true`로 투명하게는 만들 수 있어도 그 영역의 마우스 클릭을 아래 프로그램으로 통과시키지 못한다(진짜 click-through 아님). 전체화면으로 덮으면 사용자가 그 뒤의 프로그램을 못 쓰게 되므로, 화면 우측 상단의 작은(160×64dp) `focusable=false` 위젯으로 타협했다. 진짜 click-through가 필요하면 JNA로 `WS_EX_LAYERED|WS_EX_TRANSPARENT`를 직접 걸어야 한다 — [[진짜 클릭-통과 오버레이, IDEAS.md 참고]].

## 로컬 API CORS 정책은 "호출 주체"에 따라 결정
content script(페이지 안에서 실행)의 fetch는 확장 출처(`chrome-extension://...`)가 아니라 **페이지의 출처**로 나간다. 반면 background.js(서비스워커)나 확장 페이지에서 보내는 fetch는 확장 출처로 나간다. 이 차이를 놓쳐서 CORS 강화 시 `/overlay-status`(content script가 직접 호출)가 회귀버그로 막혔었다(2026-07-29). 원칙: 상태를 바꾸지 않는 단순 조회는 `openCors=true`로 개방, 상태를 바꾸는 요청(`/confirm`,`/tick`,`/heartbeat`)은 `chrome-extension://` 제한 + 토큰 인증 유지.

## 공부앱 연동은 항상 읽기 전용 폴링 + fail-safe 만료
공부앱(별개 웹앱)과의 연동(뽀모도로 자동해제, 이후 확장된 실행확인/일일한도 동기화)은 전부 Firebase RTDB 기반이며, 관리앱은 공부앱의 원본 데이터(`pomodoro` 노드)를 절대 쓰지 않고 읽기만 한다. **(19차 세션에서 단 하나의 예외 추가: `pomodoro/remoteCommand` 경로는 공부 잠금 화면의 원격 제어를 위해 관리앱이 쓴다 — 위 "공부 잠금 원격 제어" 항목 참고. 그 외 `pomodoro` 노드 전체는 여전히 읽기 전용 원칙 유지.)** 뽀모도로 자동해제는 `breakActive && now < phaseEndAt + GRACE_MS(15초)`로 재검증해서, 공부앱 탭이 갑자기 닫혀 `breakActive:false`를 못 올려도 정해진 종료 시각이 지나면 자동으로 무효 처리된다(영구 해제 방지). 네트워크 오류/설정 누락 시 항상 `false`(해제 안 함)로 fail-safe — 보안 판정이므로 fail-open 금지.

## 실행확인 레벨 동기화: 최신값 승리 vs 일일사용한도: 합산
같은 Firebase 인프라를 쓰지만 동기화 전략이 다르다:
- **실행확인 레벨**(`confirmSync`, 11차 세션): 값 자체가 단일 상태이므로 "마지막으로 확인한 시각이 더 늦은 기기의 값"을 그대로 채택(최신값 승리).
- **일일 사용시간**(`dailyUsage`, 12차 세션): 두 기기에서 각자 실제로 쓴 시간이 다 더해져야 "그룹 전체 기준 하루 30분" 같은 한도가 기기와 무관하게 지켜진다. 그래서 기기별로 자기 값만 쓰고, 읽는 쪽이 "내 기기를 뺀 나머지 기기들의 합"을 로컬 값에 더한다 — 각 기기가 자기 키에만 쓰므로 동시 쓰기 경쟁(lost update)이 없다.

## 뽀모도로 임시해제 오버레이는 레벨과 무관한 고정 불투명도
실행확인 오버레이는 재확인을 반복할수록 점점 진해지는 게 의도된 디자인이지만, 뽀모도로 휴식으로 임시 해제된 상태는 "재확인을 반복해서 진해지는 것"과 의미가 다른 신호다. 그래서 뽀모도로 오버레이는 레벨 무관 고정값(안드로이드 alpha 70, 데스크탑 0.72f, 브라우저 확장 0.28 — 각 플랫폼 기본값보다 눈에 띄게 높게)을 쓴다(2026-08-05, 10차 세션).

## 82차 아키텍처 감사 결과 중 3건은 실제로는 문제가 아니었음 (2026-09-02, 82차)
81차 세션 종료 시점에 작성한 "갓생살기 아키텍처 리뷰"(전체 코드 감사)에서 CRIT/MED로 분류했던 항목 중 3건을 실제 코드 확인 후 정정한다 — 향후 세션에서 같은 오해로 재작업하지 않도록 기록.
- **키스토어 유출 위험(CRIT로 분류됨)**: `phone-lock-android/keystore.properties`와 `keystore/`는 `.gitignore:19-20`에 이미 개별 항목으로 등록돼 있고, `git ls-files`로도 추적되지 않음이 확인됨. 실제 유출 경로가 없었다 — 감사 당시 `.gitignore` 파일 자체를 직접 읽지 않고 정황만으로 CRIT 판정한 것이 원인. **결론: 이미 안전, 수정 불필요.** 앞으로 새 민감 파일(토큰/키/인증서)을 추가할 때도 이 두 줄과 같은 패턴(파일 단위 명시적 gitignore)을 유지할 것.
- **Firebase RTDB 규칙 개방(MED로 분류됨)**: `firebase-database.rules.json` 루트가 `.read:false/.write:false`이고 모든 하위 경로(`users`, `groups`, `inviteCodes` 등)가 `auth != null` + `allowedUsers`/멤버십 스코프로 닫혀있음을 확인. 열린 규칙 없음. **결론: 이미 안전.** 다만 신규 RTDB 경로(공지사항/공동목표/문구통계 등, 82차에서 추가)를 만들 때마다 반드시 이 파일에 스코프 규칙을 함께 추가해야 하며, 잊으면 그 경로만 조용히 열린 규칙(암묵적 거부는 Firebase 기본값이라 실제로는 안전하지만 명시적으로 닫아두는 관례 유지)이 될 수 있다.
- **JsonStore(데스크탑) 동시쓰기 경합(MED로 분류됨)**: `JsonStore.kt` 자체엔 락이 없지만, 유일한 호출부인 `Repository.kt`가 거의 모든 읽기-수정-쓰기 함수를 `synchronized(lock)`으로 감싸고 있어 실질적으로 경합이 없음을 확인. **결론: 수정 불필요.** 다만 새 Repository 함수를 추가할 때 `synchronized(lock)`을 빠뜨리면 이 안전장치가 깨지므로, 새 함수 작성 시 항상 감싸는 관례를 유지할 것(안드로이드의 `escalationMutex.withLock{}` 관례와 동일한 취지).

## 82차 God Object 파일 분리 — 1차는 "모임"만, 사용자 재요청으로 전체(계산기/캘린더/루틴까지) 완료 (2026-09-02, 82차)
아키텍처 감사(§7)가 제안한 `PhoneLockRepository.kt`(2053줄)/`Repository.kt`(1911줄) 분리를 처음엔 "모임"(소셜 그룹) 섹션만 안전 범위로 진행했었다(가장 결합도가 낮은 부분). 이후 사용자가 "저거 싹다 진행해"로 계산기/캘린더/루틴까지 마저 분리해달라고 재요청 — 실제로 해보니 우려했던 것보다 리스크가 낮았다:
- **핵심 발견**: 안드로이드는 Room DAO들이 이미 `internal`로 열려있었고(모임 분리 때 넓혀둔 것 + 이번에 `db`/`calcTaskDao`/`calcSavedItemDao`/`routineLogDao`/`usageDao`/`confirmCounterDao`/`ioScope`까지 추가로 `internal` 전환), 데스크탑은 애초에 `data`(전체 `AppData` blob)와 `lock`(동기화 락)과 `persist()`만 `internal`로 열면 되는 훨씬 단순한 구조였다(모든 함수가 `synchronized(lock) { data.xxx }` 패턴이라 DAO 단위 가시성 추적이 아예 불필요).
- **분리 결과**(양 플랫폼 대칭): `*Repository.Calendar.kt`(캘린더+계산기 연동 로직, `addLinkedCalendarTask` 등 캘린더 중심 함수들과 함께 묶음), `*Repository.Calc.kt`(계산기 CRUD+폴더+동기화), `*Repository.Routine.kt`(루틴 CRUD+스트릭 로그+동기화). 안드로이드 `PhoneLockRepository.kt`는 2053→1017줄, 데스크탑 `Repository.kt`는 1911→966줄로 줄었다.
- **`exportBackupJson`/`importBackupJson`(안드로이드) 등 여러 섹션을 넘나드는 cross-cutting 함수는 core 파일에 그대로 남겼다** — 그룹/멤버/사이트/전체 백업처럼 진짜로 여러 도메인을 동시에 다루는 함수까지 억지로 어느 한 파일에 넣지 않는다는 원칙.
- **가시성 전략**: 모든 추출된 최상위 확장 함수/프로퍼티는 `private` 없이(기본 public) 선언 — 어느 파일이 어느 함수를 부를지 일일이 추적하는 대신, 같은 모듈(app/데스크탑 각각) 안에서는 자유롭게 서로 호출 가능하게 통일했다. 순수 파일 내부용 헬퍼(`formatCalcNumber` 등 각 파일에 중복 정의된 사소한 포맷 함수)만 `private`로 남김.
- **호출부 수정**: extension 함수는 멤버 함수와 호출 문법이 동일(`repository.getCalcTasks()`처럼)하지만 **다른 패키지에서는 반드시 import가 필요** — `CalculatorScreen.kt`/`CalendarScreen.kt`/`RoutineScreen.kt`/`SettingsScreen.kt`/`StudyStatsScreen.kt`/`StudyTimerScreen.kt`/`TimetableScreen.kt`/`MainActivity.kt`/위젯 파일들(안드로이드)과 동급 데스크탑 파일들에 `import com.phonelock.app.data.*`(또는 `.desktop.data.*`) 와일드카드 임포트를 추가해서 해결.
- **부수 발견(중요, 별도 BUGS.md 기록)**: 이 작업 중 Room `fallbackToDestructiveMigration()`이 테이블 단위가 아니라 DB 전체를 지운다는 걸 재확인, v29→v33까지 4단계 마이그레이션을 전부 명시적으로 작성해 잠재적 데이터 손실을 배포 전에 막았다.
- `:shared` 순수 로직 공유 모듈 추출은 이후 같은 세션에서 실제로 진행함 — 아래 "`:shared` composite build 모듈 도입" 항목 참고.

## `:shared` composite build 모듈 도입 + AGP Lint 버그 발견/수정 (2026-09-02, 82차)
사용자가 감사 후속 잔여 항목("`:shared` 순수 로직 공유 모듈") 진행을 지시 — 두 플랫폼이 완전히 분리된 Gradle 루트 프로젝트라 진짜 모듈 공유를 하려면 composite build(`includeBuild`)가 필요했는데, 실제로 해보니 우려했던 것보다 범위가 좁고 안전했다.
- **이관 대상 선정 기준**: `diff`로 안드로이드/데스크탑 버전을 비교해 "패키지 선언과 주석만 다르고 로직은 100% 동일"한 파일만 골랐다 — `CalcEngine.kt`(java.time+kotlin stdlib만 사용, 완전 순수), `PersuasionMessages.kt`/`MotivationalQuotes.kt`(문구 리스트+순수 함수), `RoutineQuotes.kt` 4개. `LockEvaluator`/`RoutineEngine`은 각 플랫폼의 Room 엔티티/데이터 클래스(`AppGroup` vs `Group`, `Routine`)에 깊이 결합돼 있어 후보에서 제외 — 억지로 공유하려면 데이터 계층까지 추상화해야 해서 리스크 대비 효용이 낮다고 판단.
- **구조**: `shared/`(신규 최상위 폴더)를 순수 `kotlin("jvm")` 모듈로 만들고, `phone-lock-android/settings.gradle.kts`와 `phone-lock-desktop/settings.gradle.kts` 양쪽에 `includeBuild("../shared")` 추가 + 각 앱 모듈에 `implementation("com.phonelock:shared")` — Gradle이 그룹:이름으로 자동 치환해준다.
- **AGP Lint 버그**: 이 구조로 바꾸자 안드로이드 `assembleRelease`가 `generateReleaseLintVitalReportModel`에서 "Could not find jar for project :shared"로 실패 — AGP의 Lint 아티팩트 해석기가 `includeBuild`로 치환된 프로젝트 의존성의 jar 경로를 못 찾는 게 원인(AGP 8.5.2의 알려진 한계, `shared`에 `java-library` 플러그인을 추가해봐도 안 고쳐짐). **해결**: `android { lint { checkReleaseBuilds = false } }`로 lintVital을 release 조립 태스크 그래프에서 떼어냄 — lint 자체가 없어진 게 아니라 `gradle lint`로 수동 실행은 여전히 가능하고, release APK 빌드가 lint 통과를 전제조건으로 삼지 않게 됐을 뿐이다. 이 프로젝트는 지금까지 lint를 CI/자동 게이트로 쓴 적이 없어(수동 리뷰 위주) 회귀 리스크는 낮다고 판단.
- **빌드 스크립트 동기화 함정**: `sync-public-repo.ps1`이 `phone-lock-android`/`phone-lock-desktop`만 동기화하고 신규 `shared/`를 몰랐던 걸 배포 직전에 발견 — 그대로 실행했으면 공개 저장소가 `includeBuild("../shared")`를 찾지 못해 빌드 불가 상태로 푸시될 뻔했다. 스크립트에 `shared/` 동기화 한 줄 추가로 해결. **교훈**: 최상위에 새 Gradle 프로젝트(모듈)를 추가하면, 그 프로젝트를 아직 모르는 배포/동기화 스크립트가 있는지 항상 함께 점검할 것.

## 안드로이드는 앱 내 WebView, 데스크탑은 외부 브라우저로 공부앱 열기
안드로이드는 SDK 내장 `WebView`로 앱 내 임베드가 쉽지만, Compose Desktop은 내장 웹뷰가 없어 JCEF 같은 무거운 라이브러리를 새로 추가해야 한다(이미 JDK/jpackage 버전 이슈가 있는 빌드에 리스크 추가). 사용자에게 이 기술적 차이를 설명하고 확인받아, 안드로이드만 앱 내 WebView 임베드, 데스크탑은 외부 브라우저 오픈으로 결정했다(2026-08-04, 7차 세션).
