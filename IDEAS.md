# IDEAS

나중에 구현할 아이디어 모음.

---

- **[최우선, 88차 세션 사용자 지정] 앱 설명서(사용법 안내) 추가**: 지금은 앱 안에 사용법을 설명하는 화면/문서가 전혀 없다 — 그룹/루틴/공부/모임 등 기능이 이미 많이 쌓여서 신규 사용자(또는 오랜만에 켜는 본인)가 헤매기 쉬운 단계. 형태(앱 내 도움말 화면/온보딩 투어/별도 문서·웹페이지 등)와 범위(전체 기능 vs 핵심 기능만)부터 사용자와 먼저 논의 필요.
- **[최우선, 88차 세션 사용자 지정] UI 대개편**: 28차(다크+파랑)→49~50차(라이트+그린 등 3~4종 테마)로 색상/테마는 여러 차례 손봤지만 화면 레이아웃·컴포넌트 구조 자체의 전면 재설계는 아직 없었다 — 92차(공부 잠금 화면)/93차(소셜 섹션 전체)에 부분적으로 이 방향의 재디자인이 진행됐지만, 루틴/관리/설정 등 나머지 섹션은 아직 손대지 않았다. 어느 화면부터 손볼지, 어떤 스타일/레퍼런스를 지향할지 범위와 방향을 사용자와 먼저 합의해야 착수 가능.

- **데스크탑 "자동 백업 (Firebase)" 설정을 제대로 재설계**(85차, 사용자 요청으로 UI만 제거): 로그인+Firebase Storage 활성화 등 전제조건이 많아 실사용 검증이 안 된 상태로 노출돼 있었다 — 설정 > 공통 탭에서 UI(SectionCard "자동 백업 (Firebase)")만 제거했고, `cloudBackupEnabled`/`lastCloudBackupResult`/`uploadCloudBackupNow()`/`CloudBackupClient.kt`/`runDailyMaintenanceIfNeeded()`의 자동 백업 분기는 그대로 남겨뒀다(안드로이드는 그대로 유지, 요청 범위가 데스크탑 한정이었음). 나중에 다시 노출할 때는 [[IDEAS.md]] "Firebase Storage 버킷 이름 실제 확인 및 클라우드 백업 실사용 검증"(82차) 항목도 함께 확인할 것.

- **호스트 실제 배포 절차를 `sync-public-repo.ps1`처럼 스크립트 하나로 묶기**(87차): 지금은 watchdog 비활성화→프로세스 종료→robocopy(빌드 산출물→2곳)→해시 비교→재실행→watchdog 재활성화(데스크탑)+release 빌드→3곳 복사·해시 비교(안드로이드)를 매번 여러 PowerShell 명령으로 수동 실행한다 — 명령 순서가 많아 실수 여지가 있다. 78차 신설 `sync-public-repo.ps1`와 같은 패턴으로 `deploy-desktop.ps1`/`deploy-android.ps1`을 만들어두면 다음부터는 한 번의 호출로 끝낼 수 있음. `Disable/Enable-ScheduledTask`가 매번 자동 모드 분류기에 걸린다는 점([[HANDOFF.md]] 87차 참고)도 스크립트화 여부와 무관하게 남는 제약.
- **데스크탑 release(ProGuard 난독화) 변형을 표준 배포 경로로 전환할지 검토**(87차): 이번에 release 빌드 자체는 고쳤지만([[BUGS.md]]/[[DECISIONS.md]] 87차), 표준은 여전히 plain으로 유지했다 — 실제로 난독화가 필요해지면(코드 보호 목적 등) 전환을 검토할 것. 전환 시 HANDOFF.md "실행 방법" 절의 명령을 `packageReleaseMsi createReleaseDistributable`로 바꿔야 한다.
- **"미래의 나에게"를 음성 메시지로 확장**(82차): 이번엔 텍스트만 구현(무전기 녹음 UI 재사용은 리스크 대비 효용이 낮다고 판단해 범위 축소) — 나중에 필요하면 `WakeOptionsDialog`의 녹음 흐름을 자기 자신 대상으로 확장해 그룹의 `selfMessageAudioRef`에 저장하는 방식 검토.
- **회유 멘트 성공률 통계를 브라우저 확장에도 적용**(82차): 안드로이드/데스크탑만 구현, `confirm.js`(브라우저 확장)는 범위 밖으로 뒀다 — 사이트 차단도 포함해서 통계를 보고 싶으면 추가 검토.
- **BlockActivity(스케줄/일일한도 차단 화면)에도 "미래의 나에게" 표시**(82차): "진행" 버튼이 장식용이라 회유 멘트 성공률 로깅은 의미 없어 제외했지만, 예약 메시지 자체는 정보성이라 표시만 하는 건 검토 여지 있음.
- **Firebase Storage 버킷 이름 실제 확인 및 클라우드 백업 실사용 검증**(82차): `<projectId>.appspot.com`으로 추정만 하고 콘솔에서 확인 안 함 — 실패 시 가장 먼저 의심할 지점. [[HANDOFF.md]] 82차 참고.
- **보안 체크리스트를 VM_BUILD_HANDOFF.md에 추가**(82차 감사 TOP20 16위, 사용자가 이번엔 보류 선택): §15 대응 절차를 문서화하는 항목 — 유닛테스트(TOP20 20위)만 이번에 진행하고 이건 명시적으로 건너뜀. 나중에 요청받으면 진행.
- **`LockEvaluator`/`RoutineEngine`도 `:shared`로 이관**(82차, `:shared` 모듈 도입 후 남은 부분): `CalcEngine`/문구류 4개 파일은 82차에 `:shared`로 이관 완료([[DECISIONS.md]] "`:shared` composite build 모듈 도입" 참고)했지만, `LockEvaluator`/`RoutineEngine`은 각 플랫폼의 Room 엔티티/데이터 클래스(`AppGroup` vs `Group`, `Routine`)에 깊이 결합돼 있어 제외했다 — 이관하려면 데이터 계층을 인터페이스로 추상화하는 별도 설계가 필요, 82차에 신설한 `LockEvaluatorTest`(양 플랫폼 대칭)가 회귀 안전망이 돼줄 것.

- **캘린더에 실제 공휴일(설날/추석 등) 표시**(79차, 사용자가 착수 직전 취소): 공공데이터포털(data.go.kr) "한국천문연구원_특일 정보" API 연동으로 논의가 진행됐었음 — 인증키 발급(무료, 회원가입만 하면 즉시 자동승인)이 번거롭다는 이유로 사용자가 중단 지시. 나중에 다시 요청받으면: 공공API 연동 / 고정 연도표 하드코딩(매년 갱신 필요) / 사용자 직접 입력 3가지 방식 중 공공API를 이미 선택했었다는 점 참고.
- **안드로이드에도 "앱 종료 확인 절차" on/off 개념 도입 검토**(79차): 데스크탑엔 트레이 "종료"가 있어 이 개념이 있지만, 안드로이드는 앱을 완전히 "종료"하는 사용자 흐름 자체가 없어(백그라운드로 가거나 강제종료만 있음) 이번엔 데스크탑 전용으로 구현했다 — 필요성이 생기면 재검토.

- **무전기 남성 목소리를 실제 다른 배우 음성으로**(78차): 지금은 안드로이드 피치 조정/데스크탑 설치된 한국어 음성 탐색이라 "톤만 다른 같은 목소리"이거나, 데스크탑은 남성 한국어 음성이 애초에 설치 안 된 PC가 대부분이라 사실상 항상 여성으로 폴백된다. 실제로 다른 배우 목소리가 필요하면 별도 TTS 엔진(예: 클라우드 TTS API, 추가 비용/네트워크 의존 발생)이나 사용자가 직접 Windows에 다른 언어팩/음성을 설치하도록 안내하는 방법을 검토할 것 — 우선순위는 낮음(사용자가 톤 차이로 충분하다고 판단하면 불필요).
- **빌드 스크립트(`build.gradle.kts`/`settings.gradle.kts`) 변경사항이 AndroidBuilds 사본에 자동 반영되도록**(78차): 지금은 `robocopy /MIR`가 `src/`만 대상이라 빌드 스크립트 자체가 바뀌면 수동으로 별도 복사해야 한다는 걸 78차에 발견([[BUGS.md]] 78차 참고) — 매번 잊기 쉬운 지점이라, 소스 동기화 시 루트 `.gradle.kts` 파일들도 함께 확인/복사하는 걸 표준 절차 문서([[HANDOFF.md]] "실행 방법")에 명시하거나, 아예 동기화 스크립트 자체에 포함시키는 것 검토.

- ~~모임장 탈퇴 시 소유권 승계~~ → 93차 세션에서 구현. "👥 멤버 관리" 다이얼로그에 "👑 승계" 버튼(모임장이 다른 멤버를 수동으로 새 모임장 지정, 본인은 자동으로 관리자가 됨) — "탈퇴 시 자동 승계"까지는 범위에 안 넣었으니 필요하면 별도로 검토.
- **모임 멤버 상세 탭에 실사용 피드백 반영**(77차): 이번에 추가한 읽기전용 탭(루틴/공부/관리)은 실기기 검증 전이라, 사용자가 써보고 "이 정보도 보고 싶다"/"이건 안 보여줘도 된다" 같은 피드백이 나오면 범위 조정 검토.
- **무작위 알림 발신 빈도/시간대 사용자 설정**(77차): 지금은 스트릭 알림과 같은 패턴으로 "하루 중 무작위 시각 1회"만 있고 빈도나 제외 시간대(수면 시간 등) 설정이 없다 — 실사용해보고 너무 자주/엉뚱한 시각에 온다는 피드백이 있으면 설정 옵션 추가 검토.
- **자체 업데이트 배너에 릴리스노트 표시**(75차, 1단계 범위 밖): 지금은 "새 버전이 있습니다"만 뜨고 무엇이 바뀌었는지는 안 보여준다 — GitHub 릴리스의 `body`(현재 `--notes`로 채우는 값)를 API 응답에서 함께 읽어와 배너에 짧게 보여주는 것 검토 가능.
- **데스크탑 자체 업데이트를 Windows 서비스로 등록된 경우까지 매끄럽게**(75차): 지금 구현은 일반 실행 상태만 고려했다 — `packaging/README.md`의 WinSW 서비스 등록 경로로 설치된 경우, installer 실행 전에 서비스를 먼저 stop해야 파일 교체가 될 수 있음. 서비스로 쓰는 사용자가 생기면 그때 보완.

- **Firebase App Check(Play Integrity) 연동(64차 세션, 설계만 확정·구현 보류)**: 재서명/재조립된 클론 앱이 이 프로젝트의 Firebase 백엔드를 계속 이용하지 못하게 막는 조치. 안드로이드에 App Check SDK(Play Integrity provider) 추가 후, `PomodoroSyncClient.kt`/`SocialGroupSyncClient.kt`의 모든 `HttpURLConnection` 생성 지점(`PomodoroSyncClient`만 15곳+)에 `FirebaseAppCheck.getInstance().getAppCheckToken(false).await().token`을 `X-Firebase-AppCheck` 헤더로 붙여야 함. Firebase 콘솔에서 처음엔 "모니터링(unenforced)" 모드로 켜서 정상 요청이 거부되지 않는지 확인한 뒤 "적용(enforced)"으로 전환하는 순서를 권장(잘못 설정하면 실제 사용자까지 차단될 위험). **데스크탑 앱은 이 조치의 보호 대상이 아님**(Play Integrity가 안드로이드 전용이라 구조적 한계, 데스크탑을 커버하려면 자체 attestation 서버가 필요해 별도 규모의 작업) — 브라우저 확장도 마찬가지로 대상 밖. 착수 전 반드시 Android 빌드 도구가 있는 환경에서 각 수정 지점을 실제로 컴파일+실기기 테스트할 것(뽀모도로/실행확인/스누즈 동기화가 깨지면 파급이 큼). 배경은 [[DECISIONS.md]] 64차 참고.

- **"소셜" 대화/DM 후속 아이디어**(93차, 1단계 범위 밖으로 의도적 축소): 텍스트+이모지 리액션만 구현했다 — 이미지/음성 메시지, 메시지 삭제/수정, 200개 초과 메시지 페이지네이션(지금은 최근 200개만 조회), DM을 아무나 걸 수 있어 발신자 차단/음소거가 없는 점(무전기 IDEAS.md 73차 항목과 같은 한계) 모두 실사용 피드백 보고 검토.
- **"모임" 깨우기/무전기를 진짜 실시간 푸시(FCM)로 업그레이드**(62차 깨우기, 73차 무전기도 같은 한계 승계, 의도적으로 범위 밖): 지금은 둘 다 RTDB 신호+폴링이라 안드로이드는 지연이 생긴다(깨우기는 WorkManager 하한 15분, 무전기는 새 포그라운드 서비스로 7초까지 줄였지만 여전히 폴링). FCM으로 진짜 즉시 전달을 하려면 이 프로젝트에 아예 없는 백엔드(Firebase Cloud Functions, 유료 Blaze 요금제 전환 필요)를 새로 도입해야 하는 큰 작업이라 미룸 — 실사용해보고 지연이 계속 불편하면 검토([[DECISIONS.md]] 73차 참고).
- **"무전기" 발신자별 차단/음소거**(73차, 1단계 범위 밖): 지금은 수신 허용을 켜면 모임 내 누구든 나에게 보낼 수 있다 — 특정 멤버만 음소거/차단하는 기능은 없음.
- **"무전기" 재생 이력(로컬 저장, 다시 듣기)**(73차, 1단계 범위 밖): 지금은 재생(또는 확인) 즉시 RTDB에서 삭제해서 다시 들을 수 없다 — 필요해지면 로컬에 최근 N개 캐싱하는 것 검토.
- **"무전기" 클립 길이/음질 사용자 설정**(73차, 1단계 범위 밖): 지금은 8kHz mono 16-bit·최대 10초로 고정(용량/단순함 우선, [[DECISIONS.md]] 73차 참고) — 실사용해보고 너무 짧거나 음질이 아쉬우면 설정 가능하게 확장.
- **"모임" 멤버 관리 기능 확장**(62차, 1단계 범위 밖으로 의도적 축소): 지금은 owner의 모임 전체 삭제/멤버 각자 나가기만 있고, "특정 멤버 강제 퇴장(kick)", 모임 이름 수정, 모임 설명/공지 같은 관리 기능은 없음. 실사용 피드백 보고 필요하면 추가.
- **"모임" 상세페이지에 오늘 캘린더 일정 공유 추가**(62차): 최초 요청엔 "오늘 일정표는 어떤지"도 있었으나 1단계에선 루틴/공부시간·진행률/스트릭 3항목만으로 범위를 좁혔다(사용자 확인). 필요하면 같은 공유-토글 패턴으로 캘린더 일정도 추가 가능.
- **Firebase RTDB 보안 규칙을 `auth.uid` 기준으로 강화**(61차부터 미룸, 62차 "모임" 데이터도 같은 리스크 승계): 지금은 로그인 여부만 확인하는 열린 규칙이라, 로그인한 임의 계정이 다른 사람의 `users/*` 경로나 `groups/*` 경로를 읽고 쓸 수 있다. 마이그레이션 기능이 이 열린 규칙에 의존하므로 사용자들이 이전을 충분히 마친 뒤 처리.
- **안드로이드 루틴 알림 백그라운드 주기적 동기화**(56차 세션 논의, 보류): 현재는 앱을 켤 때(`MainActivity.onCreate`)만 Firebase에서 최신 루틴을 끌어와 알림을 재예약한다 — 다른 기기에서 루틴을 바꾼 뒤 이 폰 앱을 아예 안 켜면 여전히 옛 알림 예약 그대로다. `WorkManager` 주기적 작업으로 백그라운드에서도 동기화하면 이 경우까지 커버할 수 있지만, 배터리 소모/구현 복잡도 트레이드오프가 있어 이번엔 범위에서 제외(사용자 확인). 실사용해보고 여전히 자주 불편하면 검토.
- **진짜 클릭-통과 오버레이**: `ForegroundWindowWatcher.kt`의 JNA 인프라를 확장해서 `SetWindowLong`으로 `WS_EX_LAYERED|WS_EX_TRANSPARENT`를 걸면, 데스크탑 오버레이도 안드로이드처럼 전체화면으로 덮으면서 클릭은 아래로 통과시킬 수 있음. 지금은 리스크/공수 대비 과하다고 판단해 코너 위젯으로 타협([[DECISIONS.md]] 참고) — 사용자가 전체화면 스타일을 원하면 검토.
- **오버레이 판정 로직 공통화**: `SiteEnforcement.overlayStatus()`와 `EnforcementService.overlayStatusFor()`가 로직이 거의 같음(그룹의 `confirmEnabled` + 쿨다운 + 레벨). 그룹 조회 방식(`findGroupsForDomain` vs `findGroupsForProcess`)이 달라 지금은 각자 유지 중 — 세 번째 유사 케이스가 생기면 공용 함수로 추출.
- **CORS 정책 문서화**: 로컬 API 엔드포인트별로 "누가 호출하는지(content script/서비스워커/확장페이지) → 어떤 CORS를 써야 하는지" 표를 코드 주석이나 별도 메모로 남기면, 새 엔드포인트 추가 시 회귀 예방 가능.
- **릴스/쇼츠 감지 데스크탑 이식**: 현재 안드로이드(접근성 트리)/브라우저 확장(URL 패턴)에만 있고 데스크탑 네이티브 앱엔 없음. 윈도우 타이틀 기반 등으로 이식 가능하지만 보류 중.
- **추가 동기화 후보** (12차 세션 끝에 논의): ~~① 그룹 설정 자체(한도/스케줄/토글) 동기화~~ → **88차 세션에서 구현 완료**(제어할 앱/사이트·groupEnabled·스누즈 진행상태 제외, [[HANDOFF.md]]/[[CHANGELOG.md]]/[[DECISIONS.md]] 88차 참고). ② 전역 설정(`dailyResetHour`/`blockReels`/`blockShorts`) — confirmSync와 같은 "최신값 승리" 패턴으로 간단히 가능, 아직 미구현. ③ 회유 멘트 진행 상태 — 가치 낮다고 판단.
- **스케줄 시간 초 단위 설정**: 현재 HH:mm만 지원, 필요하면 초 단위까지 확장.
- ~~공부앱을 웹/웹뷰가 아니라 완전 네이티브로 재구현~~ → **1~5단계(타이머/뽀모도로·캘린더·계산기·일정표·통계) 전체 완료(30차 세션)**. 남은 것은 실기기 검증뿐. [[DECISIONS.md]] "5단계(통계) 네이티브 재구현" 및 [[HANDOFF.md]] 다음 작업 우선순위 참고.
- ~~공부 잠금 원격 제어를 최소 상태만 동기화하는 방식으로 재설계~~ → 24차 세션의 네이티브 재구현으로 `pomodoro/remoteCommand` 채널 자체가 제거되어 해소됨.
- **허용 사이트 목록 재동기화 검토**: 1단계에서 허용 프로그램/사이트 입력을 로컬로 이전하면서, 예전엔 한 값을 공유하던 "허용 사이트"(도메인, 플랫폼 무관)도 이제 기기마다 따로 입력해야 한다. 실사용해보고 불편하면 사이트 목록만 Firebase로 재동기화하는 것 검토([[DECISIONS.md]] "1단계(타이머/뽀모도로) 네이티브 재구현" 트레이드오프 항목 참고).
- ~~그룹/통계 화면도 데스크탑 좌우 분할 검토~~ → 34차 세션에서 구현. 그룹은 왼쪽 목록/오른쪽 편집 폼, 통계는 왼쪽 그룹별 요약/오른쪽 선택한 그룹 상세로 재작성. [[DECISIONS.md]]/[[HANDOFF.md]] 참고.
- ~~안드로이드 계산기 "저장됨" 탭도 폴더 탐색기 좌우 분할 검토~~ → 31차 세션에서 데스크탑 계산기 자체가 좌(2):우(8) 사이드바 구조로 재작성되며 폴더 탐색기 좌우 분할이 사라지고 다시 재귀 트리(안드로이드판과 동일)로 돌아감 — 이 아이디어의 전제가 없어져 폐기.
- ~~캘린더 날짜 상세에 그날 타이머 기록 표시~~ → 34차 세션에서 구현(`getStudyLogForDate()` 신규). 처음엔 별도 섹션으로 붙였다가 사용자 요청으로 날짜 옆 총시간+업무 이름 옆 개별 시간으로 재배치. [[CHANGELOG.md]] 34차 참고.
- ~~웹앱에 있던 애니메이션 이식~~ → 34차 세션에서 구현. phase 배지 dot pulse, 계산기 결과 카드/저장됨 항목 fadeIn+slideUp 진입 애니메이션. [[CHANGELOG.md]] 34차 참고.
- ~~저장됨 업무 폴더 지정 시 뜨는 폴더 트리를 웹앱과 동일한 모양으로~~ → 34차 세션에서 구현. `DropdownMenu`로 버튼 옆에 뜨는 팝오버, 현재 폴더 accent 강조. [[CHANGELOG.md]] 34차 참고.
- ~~데스크탑 일정표에 지난주/다음주 이동 네비게이션 추가~~ → 34차 세션에서 구현(`weekOffset` 상태). [[CHANGELOG.md]] 34차 참고.
- ~~"타이머" 탭 이름을 "시간 측정"으로 변경~~ → 34차 세션에서 구현.
- **타이머 크로스디바이스 미러에 원격 정지/전환 제어 추가 검토** (34차 세션): 지금은 다른 기기가 재고 있는 걸 실시간으로 보여주기만 하고 정지/전환은 그 기기에서만 가능함(19차 `remoteCommand` 문제 재발을 피하려고 의도적으로 뺌 — [[DECISIONS.md]] "타이머 크로스디바이스 '미러' 표시" 참고). 사용자가 실사용해보고 불편하면, 이번엔 네이티브 로컬 타이머끼리라 웹뷰 스로틀링 문제가 없으므로 원격 명령 방식을 다시 검토할 수 있음(단, 두 기기가 동시에 정지를 시도하는 경쟁 상황 등 설계 필요).
- **`전문가_종합분석_보고서_2026-08-10.md`(38차 세션)의 미구현 항목들**: 9개(#11/#31/#32/#19/#21/#20/#13/#34, [[CHANGELOG.md]] 38차 참고)는 구현 완료. 나머지 중 실제로 다시 검토할 가치가 있는 것만 아래에 남김(전체 30개 목록은 보고서 원본 참고):
  - **그룹 일시정지(스누즈, #1)/완화 시 PIN(#17)**: `LockEvaluator` 핵심 게이트를 직접 확장해야 해서 이번엔 보류(38차 [[DECISIONS.md]] 참고). 스누즈는 특히 "회유 절차 생략 임시 해제"라 앱 목적과 충돌 여지 있음 — 사용자가 정말 필요하다고 판단하면 먼저 세부 설계(스누즈 최대 시간, 하루 몇 번까지 등)부터 논의할 것.
  - **안드로이드 접근성 이벤트 디바운싱(#14, 성능)**: 실기기 프로파일링 없이는 스로틀 값을 정하면 안 됨 — 실기기 검증 세션에서 배터리/반응성 실측 후 재검토.
  - **현재 화면 앱/사이트 원클릭 그룹 추가(#2)**: 트레이 메뉴(데스크탑)/알림 액션(안드로이드) 통합이 필요해 범위가 중간 이상. 실사용 중 그룹 생성이 번거롭다는 피드백이 나오면 착수.
  - **큰 글씨/고대비 모드(#27), TalkBack 라벨 보강(#28)**: 전체 화면 전수 점검이 필요한 큰 작업. 접근성이 필요한 실제 사용자가 있을 때 우선순위 재검토.
  - **루틴앱 v1에서 제외한 것들(47차 세션 설계, [[DECISIONS.md]] 참고)**: 배지/스탬프 게이미피케이션, "주 N회"류 비-요일 기반 반복 규칙(Loop Habit Tracker 참고), 몸무게/컨디션 등 수치 기록+그래프, 위젯/워치 연동. 루틴앱 v1 완성 후 실사용 피드백 보고 재검토.
- **루틴앱 ↔ 관리앱(그룹 차단) 연동**(47차 세션): 설계 논의 중 "루틴앱 중심으로 관리앱을 대개편" 방향이 잠깐 나왔으나 최종 설계에서는 배제(범위 축소). 루틴 완료 여부를 차단 해제 조건에 넣는다든지 하는 아이디어는 루틴앱 v1이 완성된 뒤 별도로 다시 논의할 것.
- **로컬 휴리스틱 그룹 추천(#22)**: 외부 API 없이 순수 로컬 통계로 구현 가능해 리스크는 낮지만, "미차단 앱 중 사용 시간이 긴 것"을 판단하려면 지금 추적 안 하는 데이터(그룹에 없는 앱의 사용 시간)가 필요해 범위가 생각보다 큼 — 착수 전 데이터 수집 범위부터 재설계 필요.
- **브라우저 확장을 라이트+그린으로 전환**(49차 세션): 네이티브 앱(안드로이드/데스크탑)은 49차에 라이트+그린으로 전환됐지만, 확장의 `confirm.html`/`blocked.html`/`onboarding.html`은 35차에 통일한 다크 팔레트 그대로 남아있다 — 의도적으로 이번 범위에서 제외(다크 CSS 변수 체계를 통째로 다시 설계해야 하는 별도 작업). 네이티브 앱과 확장의 톤이 어긋나 보이면 다음에 진행.
- **루틴 화면 색상 전수 감사**(49차 세션): 새 라이트+그린 테마 적용 후 캘린더 "오늘" 배지(흰 텍스트 대비 문제)만 고쳤고, 나머지 화면(통계 막대그래프, 회독 단계색, 일정표 요일 강조색 등)에 흩어진 하드코딩 색상은 손대지 않았다 — 실사용해보고 눈에 띄게 안 어울리는 게 있으면 개별적으로 조정.
- ~~루틴 상징 아이콘 선택 기능~~ → 52차 세션에서 구현. `Routine.icon`(이모지 문자열) 필드 + 편집 다이얼로그 텍스트필드/quick-pick 12개 팔레트, 오늘 탭/통계/안드로이드 홈위젯에 표시.
- ~~루틴 알림 설정 기능~~ → 52차 세션에서 구현. 안드로이드는 `AlarmManager.setAndAllowWhileIdle`(특수 권한 불필요)+부팅 재예약, 데스크탑은 30초 주기 틱 비교+`TrayState.sendNotification`(기존 트레이 아이콘 재사용). [[DECISIONS.md]] 52차 참고.
- ~~스트릭 기반 응원/비판/조롱 알림~~ → 52차 세션에서 구현. 설정 화면 전역 토글, 매일 초기화 시각에 어제 스트릭을 계산해 끊겼으면 비판 톤/쌓이면 응원 톤(`RoutineQuotes.kt`, 42차 `MotivationalQuotes.kt`와는 별개의 작은 세트).
- ~~루틴 기간 설정 기능~~ → 52차 세션에서 구현. `Routine.startDate`/`endDate`(yyyy-MM-dd, 선택) 추가, `RoutineEngine.isScheduledOn`이 daysMask와 함께 기간도 검사. Room DB version 26→27(아이콘/알림/기간 필드를 한 번에 묶어 버전업 1회로 최소화).
- **데스크탑 `data.json` 외부 편집 시 실행 중인 앱에 자동 반영(hot-reload)**(60차 세션에서 문제로 발견): `Repository`가 시작 시 1회만 파일을 읽어 메모리에 캐싱하는 구조라, 앱이 실행 중일 때 파일을 외부에서 직접 고쳐도 반영되려면 반드시 프로세스 재시작이 필요하다는 게 이번에 드러남(파일은 정상인데 화면엔 옛 상태가 계속 보이는 혼란 발생, [[BUGS.md]] 60차 참고). 파일 변경 감지(watch) 또는 "설정 새로고침" 수동 버튼 중 하나를 추가하면 재발 방지 가능 — 우선순위는 낮음(외부 편집 자체가 드문 케이스).
