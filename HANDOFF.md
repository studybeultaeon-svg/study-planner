# 프로젝트 개요

**프로젝트 이름**: 갓생살기종합세트 (구 "관리앱" — 45차 세션에서 개명, [[DECISIONS.md]] 45차 참고)

**프로젝트 목적**: 개인 자기통제(디지털 디톡스)를 위한 앱/사이트 차단 시스템. 지정한 앱·프로그램·사이트를 시간대/일일 한도로 제한하고, 해제하려면 타이머 대기 + 회유 멘트 통과 같은 "꼼수 방지" 절차를 거치게 한다. 이 문서 체계(HANDOFF/CHANGELOG/DECISIONS/BUGS/IDEAS)가 관리하는 범위는 phone-lock 계열(안드로이드/데스크탑/브라우저 확장)이며, 공부앱 네이티브 재구현도 같은 코드베이스 안에서 함께 관리한다.

**한 줄 설명**: 안드로이드 앱 + 윈도우 데스크탑 앱 + 크롬 확장이 하나의 규칙(그룹)을 공유하며 서로 다른 플랫폼에서 같은 방식으로 잠금·확인·레벨을 동기화하는 3종 세트. "갓생살기종합세트"라는 이름 아래 자기통제(관리앱)·학습관리(공부앱, 같은 코드베이스에 통합됨)·루틴앱(v1 코드/빌드 완료, 49차)까지 함께 묶는다. 반대로 알람앱(`com.wakealarm`, 별도 저장소)은 도메인이 완전히 독립적이라 이 세트에 포함하지 않고 분리 유지하기로 함 — 판단 기준은 [[DECISIONS.md]] 45차 참고.

구성 요소:
- **phone-lock-android**: Kotlin/Compose + Room DB 기반 안드로이드 앱
- **phone-lock-desktop**: Kotlin/Compose Desktop 기반 Windows 앱
- **browser-extension** (`phone-lock-desktop/browser-extension/`): Chrome 확장(Manifest V3), 데스크탑 앱의 로컬 API(`127.0.0.1:47882`)와 통신해서 사이트 차단 담당
- **루틴앱**(반복 할 일/습관/일과표, `Routine` 하나로 통합 — [[DECISIONS.md]] 47차 참고): 47차 설계 확정, 48차 데이터 모델/CRUD/스트릭 엔진, 49차 화면 3종(오늘/일과표/습관)+편집 다이얼로그, 50차에 "오늘"/"통계" 2탭 개편(일과표는 오늘 탭에 시간순 정렬로 흡수, 습관→통계)+요일 피커/주 이동+스트릭 방어권 주 2회 고정+Firebase 동기화 추가, 51차(문서 미기록, 코드로만 확인) 스트릭을 루틴별에서 "하루" 단위 전역 스트릭으로 전면 개편, 52차에 순서변경(▲/▼)+복사+아이콘+알림+스트릭알림+기간설정까지 추가(Room DB 26→27). 실기기 검증 대기 중.

**현재 개발 단계**: 핵심 기능(그룹 차단) 구현 완료. 공부앱 네이티브 재구현 **1~5단계 전체 완료**(타이머/뽀모도로→캘린더→계산기→일정표→통계, 30차), 이후 세션들에서 크로스디바이스 동기화(뽀모도로/공부기록/스누즈)·조롱조 문구·오버레이 재설계 등 신규 기능을 계속 추가해왔다. **루틴앱은 47~50차에 v2 전체 완료.** **61~70차에 Google 로그인 동기화와 "모임"(소셜 그룹) 탭 구현·배포·보안 강화·실기기 검증까지 완료.** **73~75차에 "무전기"(모임 내 강제 음성/텍스트 메시지)와 자체 업데이트 기능(GitHub Releases 기반) 구현·배포.** **78차부터 "GitHub에 올려"=릴리스 게시, 안드로이드 기본 빌드=release 서명이라는 작업 워크플로 확정.** 77~81차는 모임 기능 확장/정정, 각종 버그 수정, 설정 화면 재배치, 데스크탑 오버레이 개선, 공부 중 알림 억제 등을 계속 처리. 92차에 공부 잠금 화면 재디자인, **93차에 "모임"을 "소셜"로 개편해 메신저 형태(그룹 대화 채널/1:1 DM/소유권 승계/전체 디자인 통일)로 확장**, **112차에 소셜탭 통합 채팅 목록/프로필 사진/모임원 홈 탭/모임 내 DM 등 9개 항목을 안드로이드에 먼저 구현 후 데스크탑에도 동일 이식** — 세부 세션별 이력은 [[CHANGELOG.md]] 참고, 최신 상태는 바로 아래 "현재 진행 중인 작업" 참고.

**현재 진행률**: 기존 그룹 차단 기능 약 90%. 공부앱 네이티브 재구현 1~5단계 + 계산기-캘린더 연동 전체 완료. 루틴앱 v2 코드/빌드/배포 완료. "모임"(소셜) 탭 — 가입/승인/멤버 상세/무전기/자체 업데이트까지 전 단계 실사용 검증 완료(70차 등), **93차 신규 메신저 기능(그룹 대화/DM/소유권 승계)은 코드·빌드·배포·GitHub 릴리스까지 완료됐지만 실제 메시지 송수신 실사용은 미검증**. **77~81차 기능들은 코드·빌드·GitHub 릴리스 게시·이 호스트 재배포까지 전부 완료됐지만 실사용 미검증 누적 중** — 세부 목록은 [[CHANGELOG.md]] 참고. 자세한 배경은 [[DECISIONS.md]] 참고.

---

# 현재 구현된 주요 기능

(개별 기능이 언제/몇 차 세션에 어떻게 바뀌어왔는지는 전부 [[CHANGELOG.md]]에 있음 — 아래는 "지금 무엇인지"만 기록)

- 앱/프로그램/사이트를 시간대·일일 한도 기준으로 차단(안드로이드/데스크탑 공통), 시간대·일일한도는 항상 OR 판정
- 그룹 편집 화면 "관리 종류" 섹션(스케줄/일일 사용한도 설정/실행 확인 3개 토글), 일일한도·실행확인은 각각 독립 "적용 시간대"+요일마스크(`dailyLimitDaysMask`/`confirmDaysMask`) 설정 가능
- 차단 해제 시 "실행 확인" 대기 화면(타이머 + 무작위 체크포인트 클릭), 회유 멘트 20개 통과 절차(꼼수 방지)
- 실행 확인 레벨 escalation 시스템: 재확인할수록 대기시간 증가, 시간 경과로 자동 감소. 표시값(오버레이 알파)과 판정 로직은 항상 분리 — [[DECISIONS.md]] 참고. 그룹별 "오버레이 최고 밝기까지 재확인 횟수"(`overlayLevelStepsToMax`) 설정값으로부터 재확인 1회당 오르는 밝기 폭을 시스템이 자동 계산(양 플랫폼+브라우저 확장 공통, 순수 표시값이라 판정 로직과 무관)
- 모바일 ↔ 데스크탑 실행확인 레벨 + 일일 사용시간을 Firebase Realtime Database로 동기화(그룹 이름 기준). 레벨은 최신값 승리, 사용시간은 기기별 합산. 같은 이름의 그룹을 가진 다른 기기가 방금 실행확인을 통과했으면(`confirmSync`) 이 기기도 재확인 없이 같은 유예시간(쿨다운)을 이어서 씀 — `ConfirmationGate.kt`(로컬 인메모리 쿨다운) 자체는 그대로 두고 호출부에서 로컬 값과 동기화된 값 중 더 최근인 쪽을 채택
- 릴스/쇼츠 감지 차단(안드로이드 접근성 트리 — 하단+좌우 사이드 밴드, 브라우저 확장 URL 패턴) — 데스크탑 네이티브 앱엔 없음, [[IDEAS.md]] 참고
- 23:00~11:00 편집 면제 시간대
- 그룹별 "사용 중 남은 시간" 오버레이 — 안드로이드/브라우저 확장은 타이머 숫자만 화면 중앙에 크게 표시(레벨에 따라 배경과 같은 alpha), 데스크탑은 우측 상단 코너 소형 위젯(전체화면 click-through 불가 — [[DECISIONS.md]])
- 뽀모도로 휴식 연동: 완전히 별개 웹앱인 "공부앱"의 Firebase RTDB를 읽기 전용 폴링, `pomodoroUnlockEnabled` 켠 그룹만 휴식 시간 동안 임시 해제 + 전용 오버레이 표시
- **공부 잠금 + 네이티브 타이머**: 공부앱 타이머가 "공부" 페이즈로 진행 중일 때 데스크탑/안드로이드에 전체화면 잠금 + 허용 프로그램(플랫폼별)·사이트(기기별 개별 입력, Firebase 공유 아님) 예외. 타이머 자체가 양 플랫폼 "타이머" 탭(`StudyTimerScreen`)에서 완전 네이티브로 동작(로컬 상태 직접 읽고 씀, 네트워크·웹뷰 불필요), Firebase `pomodoro` 노드는 다른 기기에 신호만 주는 부가 채널. 이 기기 로컬 타이머가 꺼져 있어도 같은 계정의 다른 기기가 공부 페이즈 실행 중이라는 신호(`PomodoroSyncClient.isStudyTimerActive`)가 있으면 이 기기도 실제로 공부 잠금(전체화면+사이트 차단)이 걸림 — 허용 목록은 각 기기 로컬 값을 쓰고, 원격 신호로 잠긴 경우 정지/전환 버튼은 숨기고 안내 문구만 표시(원격 제어는 하지 않음). 신호가 20분 넘게 갱신 안 되면 유령 신호로 보고 무시(영구 잠김 방지)
- **네이티브 캘린더**: 양 플랫폼 "캘린더" 탭 — 월 그리드 + 선택 날짜 상세, 회독(8단계 무지개, 간격 1,3,7,14,30,60,120일) 완료 시 자동 다음 회독 생성, 미완료 시 다음날로 자동 이월(`applyIncompleteCarryOver`), 순서변경/이름수정/이동/복사/삭제, 6개월 이전 정리. Firebase `users/{user}/calendar` 경로로 공유(전체 문서 LWW). 타이머 탭 업무 선택은 "오늘 캘린더 일정" 드롭다운. 계산기 업무를 연결하면(`LinkedCalcSection`) 완료 시 진행량 자동 차감/복원
- **네이티브 계산기**: 양 플랫폼 "계산기" 탭 — 업무 입력 카드(요일별 목표+휴일 제외) → `CalcEngine`으로 계산 → 진척도/요일별 페이스 비교표/완료 예상일 결과 → 저장(하위 폴더 트리, 폴더 접기 영속화·개별/전체 접기, 항목 폴더 이동). Firebase `users/{user}/calculator` 경로 공유(draft/저장됨/폴더트리 3구간 독립 LWW)
- **네이티브 일정표**: 양 플랫폼 "공부" 섹션 서브탭 — 계산기 draft 업무를 요일별 목표량 표로 보여주는 읽기 전용 파생 뷰(데스크탑은 이번 주 고정 테이블, 안드로이드는 일 단위 리스트+◀/▶ 이동). 계산기 연동 업무는 그날 목표 달성 시 ✅+초록, 미달성이면 빨강 표시(모임 멤버 상세 "일정표" 탭에도 동일 판정 재현)
- **네이티브 통계**: 양 플랫폼 "공부" 섹션 서브탭 — 캘린더 일정 전체를 집계해 전체/완료/완료율/연속 완료일(스트릭)/회독 단계별 완료 현황/최근 30일 완료 추이 막대그래프
- **2단 탭 구조**: 홈(🌱, 구 "식물", 항상 첫 번째 탭)/루틴(📋)/공부(타이머·캘린더·계산기·일정표·통계)/규칙(구 "관리", 차단규칙·사용기록)/소셜(👥) 5섹션 — 설정은 탭이 아니라 홈 화면 우상단 원형 버튼으로만 진입(카테고리 9분류, 111차). 데스크탑은 왼쪽 `NavigationRail`+섹션별 `TabRow`, 안드로이드는 하단 `NavigationBar`(태블릿은 좌측 `NavigationRail`)+섹션별 `TabRow`. 테마는 라이트+그린/다크+파랑/화이트+오렌지 3종 중 설정에서 선택(`ThemeMode`+`PhoneLockPalette`), **기본값은 112차부터 양 플랫폼 공통 라이트+그린**
- **프로필 사진(112차, 양 플랫폼)**: 설정 > 프로필에서 동물 이모지 프리셋 10종(`AvatarCatalog`) 중 하나를 선택 — 갤러리 업로드/Firebase Storage 없이 `users/{uid}/profile.profileImage`에 프리셋 id만 저장, 모임 멤버 목록/상세 아바타에 반영.
- **루틴앱**: `Routine`+`RoutineLog` 모델 하나로 체크리스트/습관/시간대별 일과표를 통합. "오늘"(요일 피커+주 이동, `timeSlot` 기준 시간순 정렬)/"통계"(현재 스트릭/오늘 완료율/최고 스트릭 지표 카드) 2탭. 스트릭은 `RoutineEngine.currentStreak()`(하루 단위 전역 스트릭, 매번 로그를 다시 훑는 순수 함수), 방어권은 주 2회 고정. 편집은 `AlertDialog` 다이얼로그(아이콘/알림/기간/순서변경/복사 지원). Firebase 동기화(배열+인덱스 참조로 기기 간 ID 충돌 회피). 안드로이드 홈스크린 위젯으로 체크 가능(classic `RemoteViews` 기반, 실기기 미검증)
- 태블릿은 실행확인/잠김 화면 제목 자동축소 로직 제외(sw600dp≥600 기준), 조롱조 문구 5단계(순한~극한) 149개 — 실행확인은 재확인 레벨, 잠김 화면은 오늘 이 그룹을 열려던 시도 횟수(`blockAttemptDate`/`blockAttemptCount`) 기준으로 로테이션. 버튼 라벨은 "진행"/"중단" 고정
- 잠김 화면(스케줄/일일한도)이 실행확인 화면과 같은 톤으로 통일(제목=조롱조 문구 로테이션), 안드로이드/데스크탑/브라우저 확장 3곳 공통
- 그룹 전체 on/off(`groupEnabled`)와 스케줄 on/off(`scheduleEnabled`) 분리 — 그룹 끄기는 회유 멘트 20개를 통과해야 실제로 꺼짐(꼼수 방지 핵심 장치, 파일 직접 편집 금지 — 아래 "현재 주의사항" 참고)
- 데스크탑 로컬 API 토큰 인증(`/pair`) + 상태변경 요청(`/confirm`/`/tick`/`/heartbeat`) POST+토큰 필수
- 데스크탑 ↔ 브라우저 확장 heartbeat 경고 배너, Android 접근성 서비스 watchdog(WorkManager 15분 주기)
- 원자적 데이터 저장(JsonStore `.tmp`+`ATOMIC_MOVE`), 손상 파일 자동 백업, Room 마이그레이션 전 raw SQLite 백업(`PreMigrationBackup`) — 그룹(차단 대상)은 Firebase에 동기화되지 않으므로 이 백업(설정 화면 "⚠ 그룹 데이터 복구" 카드)에서만 복구 가능
- 타이머 탭 상단 "오늘 한눈에" 요약(캘린더 일정/계산기 목표/누적 공부시간), 연속 공부 스트릭 카드/최근 7일 막대그래프/오늘 과목별 도넛 차트, 통계 탭 "최근 7일 vs 지난 7일" 완료율 비교와 이상 사용 패턴 경고 배너, 그룹별 재확인 통과 횟수 카운터(로컬 전용), 데스크탑 일일 자동 백업/복원(최근 7일 보관), 양 플랫폼 사용기록 CSV 내보내기, 12개월 이상 지난 통계 수동 정리
- 공부 세션 정지 시 짧은 회고 입력(`StudyLogEntry.note`), 데스크탑 설정/그룹 내보내기·가져오기, **그룹별 스누즈(일시정지)**(설정 시간, 하루 3회 제한, 크로스디바이스 동기화, `LockEvaluator.isSnoozeActive()`), **기간 지정 자동 강화**(시험기간 등 `forceEnabledFrom~Until` 범위 안이면 `groupEnabled`를 꺼도 강제 활성 취급 — 스누즈보다 우선)
- 그룹 자동 재활성화: `dailyResetHour` 기준 "오늘" 날짜가 바뀌면 꺼둔 그룹을 자동으로 다시 켬(하루 1회)
- 공부 중엔 이 앱 자신의 알림(루틴 리마인더/스트릭/모임 무작위 알림/깨우기·무전기) 4종만 개별 억제(시스템 DND 아님, `StudyNotificationGate`) — 공부 종료 시 큐에 쌓인 알림 재발송
- 공부 타이머 진행률 응원 문구(목표 시간/사이클 수 기준, `shared/StudyProgressQuotes.kt`)
- 공부 잠금 화면: `StudyTimerScreen`과 같은 디자인 언어(강조색 배지/원형 진행률 링/카드형 허용앱 목록)
- **자체 업데이트(GitHub Releases 기반)**: 설정한 초기화 시각 기준 "오늘"이 바뀌면 하루 1회(안드로이드는 15분 주기) GitHub Releases(`studybeultaeon-svg/study-planner`)를 조회해 새 버전이 있으면 배너로 안내, 버튼으로 실제 다운로드+설치(안드로이드는 앱 내부 직접 HTTP 다운로드, 데스크탑은 설치파일 실행 후 자기 종료). 태그는 안드로이드 `android-<versionCode>`/데스크탑 `desktop-<BuildInfo.BUILD_TIMESTAMP>`. 설정 화면에 현재 버전+"지금 확인" 버튼. **112차부터 안드로이드 배너는 "새 버전이 있습니다" 알림만 표시하고 "이번 업데이트 내용"(릴리스 노트 본문) 텍스트는 더 이상 보여주지 않음**(데스크탑은 원래부터 이 텍스트를 표시한 적이 없어 변경 없음) — 다운로드/설치 흐름 자체는 그대로.
- **"소셜" 섹션**(구 "모임"): 모임 만들기/초대코드 참여, 멤버 목록(오늘 완료율 낮은 순+"아직 안 한 사람 N명" 배지, 프로필 아바타 표시), 멤버 상세(홈/루틴/공부/일정표 탭 — 상대가 공유 설정에서 안 켠 항목은 비공개), 😴 깨우기, **💬 DM**(멤버 목록/상세에서 바로 1:1 대화 시작), 관리자 승격/해제/내쫓기, 모임장 소유권 승계, 모임 이름/설명 수정(모임장/관리자). 모임 상세 화면엔 "멤버"/"💬 대화" 탭이 있고(대화 탭은 텍스트+이모지 리액션 그룹 채널), **115차부터 이 "💬 대화" 탭을 모임 설정에서 관리자가 켜고 끌 수 있음**(`GroupInfo.chatEnabled`, 꺼두면 멤버 전원에게 탭 자체가 안 보임, 기본값 켜짐). 소셜탭 메인 화면은 "👥 모임" 목록(진행률 카드)과 **내 1:1 DM만** 최근 활동순으로 보여주는 "💬 채팅" 목록 2섹션 — **115차에 이 메인 화면 채팅 목록에서 모임(그룹) 항목은 빼고 DM만 남김**(그룹은 이미 "👥 모임" 목록에서 접근 가능해 중복이었음, 커스텀 아이디로 아무나 찾아 DM 시작하던 전역 검색도 이때 함께 제거 — DM은 이제 모임 멤버 목록/상세에서만 시작). 실시간성은 화면이 켜져있는 동안 4초 폴링(SSE/FCM 없음)
- **"무전기"**(모임 내 강제 음성/텍스트 메시지): 멤버 목록/상세의 깨우기에서 "알림만 / 음성 메시지(최대 10초, 8kHz mono WAV) / 텍스트 메시지(TTS)" 3택. 모임별로 켜짐/모드("즉시 재생"·"메시지로 받기")/볼륨/요일×시간대 허용 일정을 따로 설정(`GroupWalkieSettings`, RTDB `groups/{id}/walkieSettings/{uid}`). 기기 볼륨/무음 설정은 항상 존중. 클라이언트 폴링만으로 구현(안드로이드 `WalkieTalkieService` 포그라운드 서비스 7초 주기, 같은 루프에 채팅 알림도 편승). RTDB `groups/{id}/voiceMessages/{targetUid}/{msgId}`, 24시간 유예 후 자동 정리+명시적 삭제 버튼
- **포인트/보상 시스템**: 공부 10분당 1P·루틴 완료 5P·캘린더 일정 완료 5P·그날 루틴 전부 완료 시 +10P 적립, "소셜" 섹션("💬 1:1 대화"와 "👥 모임" 사이)에서 등록한 보상을 포인트로 교환(Firebase `users/{user}/points` 동기화)
- **"홈" 탭**(구 "식물" 🌱, 111차부터 앱의 첫 번째 탭 — 화면 우상단 원형 버튼이 설정 진입점): 화면 전체를 등급(tier)별로 하늘/땅 색이 다른 배경(`GroundScene`, 모든 치수가 화면 크기 비례)으로 채우고, 화면 아래쪽에 도킹된 반투명 카드로 레벨/경험치 HUD를 띄워 가운데 식물이 항상 보이게 함(보상함 기능은 삭제됨). **"레벨업 시스템"과 "식물 성장"이 `shared/GrowthSystem.kt` 하나로 통합** — 칭호(식물 이름)는 `GrowthSystem.STAGES`에 **레벨 1~500(LEVEL_CAP) 안에 전부 들어가는 고정 테이블**(23단계, 런타임 랜덤 아님)로 정해지며, 5등급(정상→이상함→초월급→종말급→최강자급, `Stage.tier` 0~4)으로 나뉜다. 등급마다 **하늘/땅 색·전경 파티클·나무 형태 자체가 전부 다른 장면**으로 그려짐(나무 렌더러 5종: 정상/이상함 공용 가지 뻗은 나무, 초월급 만다라 광륜+빛의 존재, 종말급 뒤틀린 덩어리+"심연의 눈", 최강자급 3기둥 세계수+다층 화관 — tier2 이상은 화면 폭 상당을 차지). 등급마다 애니메이션 질감도 다름(정상=단순 sway, 이상함=불규칙 경련, 초월급=오라 맥동+회전, 최강자급=전부 결합+카메라 셰이크). 환생 횟수는 레벨과 별개로 영구 금빛 링(`drawRebirthAura`)으로 표시. **환생(Rebirth)**: 요구 레벨/EXP 배율 공식은 버튼+확인 다이얼로그로만 실행되는 수동 동작(자동 실행 없음). **경험치는 적립 즉시 레벨에 반영되지 않고 `growthExpPending`(대기치)에 먼저 쌓이며, "✨ 경험치 적용" 버튼을 눌러야 `applyPendingGrowthExp()`가 그 순간 `growthExpTotal`(적용된 누적치)에 반영** — 그 과정을 레벨 경계마다 경험치바가 차오르고(다중 레벨업 시 순차 재생) 레벨업 연출+효과음(`shared/GrowthSoundEffects.kt` PCM 합성, 플랫폼별 `GrowthSoundPlayer`)이 뜨는 애니메이션으로 재생. EXP 적립 자체는 기존 포인트 적립 이벤트(공부/루틴/캘린더/스트릭)에 편승, 환생 배율을 곱해서 쌓음 — "보유 포인트"(공부/루틴 등으로 쌓이는 화폐, 현재 소비처 없음)는 이 시스템과 무관하게 그대로 유지. **관리자 패널에서 다른 기능과 동일하게 사용자별로 껐다 켰다 할 수 있는 권한**(`permPlant`/`Permissions.plant`) 대상. 모임 멤버 상세의 "🏠 홈" 탭(112차)에서는 라이브 화면을 그대로 보여주는 대신 레벨/칭호/등급/진행률/환생 횟수만 요약 카드로(모임별 `sharePlant` 공유 토글로 켜고 끔).

---

# 현재 진행 중인 작업

**116차 세션(2026-09-13, 안드로이드+데스크탑 동시) — 홈 화면 구름 애니메이션 추가.** 사용자 요청: "구름도 자연스럽게 움직이게". `GroundScene`의 `drawSky` tier 0(정상 등급) 분기에 고정 좌표로 떠있던 구름 2개를, 이미 매 프레임 갱신되던 `nowMs` 값으로 x좌표를 계산해 화면 폭+여백 구간을 천천히 순환(modulo)하게 바꿨다 — 새 애니메이션 인프라 도입 없이 기존 프레임 클록만 재사용, 두 구름 속도를 다르게 줘서 시차감을 냄. 정상 등급 하늘에만 있는 코드라 다른 등급엔 영향 없음. 양 플랫폼(`PlantScreen.kt`) 동일 적용.
- 같은 세션에서 홈 화면이 비어 보인다는 사용자 지적에 아이디어 제시(결정은 아직 안 됨, 구현 안 함): ① 앰비언트 장식(나비/새 등 저부담 디테일) ② 좌상단 미니 요약 칩("🔥 N일 연속"/"오늘 루틴 M/N") ③ 나무 주변 장식 아이템을 포인트로 구매(→ [[IDEAS.md]]에 있던 "보유 포인트 새 소비처 검토" 미해결 이슈와 연결). 사용자 선택 전까지 착수하지 말 것.
- **빌드/배포**: `~/.gradle/wrapper/dists`의 gradle 8.7 + `C:\build\jdk-temurin21`의 JDK 21 조합(115차 후속에서 확립)으로 `C:\Users\sunae\AndroidBuilds\phone-lock-android`/`C:\build\phone-lock-desktop`에서 컴파일 확인 후 `assembleRelease`(versionCode `1789265017`)/`packageMsi createDistributable`(BuildInfo `1789265022`) 빌드. 안드로이드 APK 3위치(`AndroidBuilds`, OneDrive 원본, `vm-build-output`) 해시 일치 배포, 데스크탑 호스트(`PhoneLockDesktopApp`, 프로세스 종료 후 교체+재기동)+`vm-build-output`(이번에 `PhoneLockDesktop` 하위 폴더가 없어 신규 생성) 배포 완료. GitHub 릴리스 게시(`android-1789265017`, `desktop-1789265022`), `sync-public-repo.ps1`로 공개 저장소 push까지 완료.

사용자가 요청했다가 취소한 것(79차): "캘린더에 실제 공휴일 표시"(data.go.kr 인증키 발급이 번거롭다는 이유로 중단) — 다시 요청받기 전까진 손대지 말 것.

---

# 다음 작업 우선순위

- [ ] **116차 실사용 검증(최우선, 최상위, 안드로이드+데스크탑 둘 다)** — 코드/컴파일/릴리스 빌드/3위치+호스트 배포/GitHub 게시까지 완료(안드로이드 versionCode `1789265017`, 데스크탑 BuildInfo `1789265022`). 검증 항목: 홈 화면 정상 등급(레벨 낮은 계정)에서 구름 2개가 실제로 화면을 가로질러 천천히 흘러가는지, 화면 밖으로 나갔다가 반대편에서 다시 나타날 때 끊기지 않는지, 다른 등급(이상함/초월급 등)에는 영향이 없는지. 홈 화면 빈 공간 채우기는 사용자에게 3가지 아이디어를 제시한 상태로 결정 대기 중(위 "현재 진행 중인 작업" 참고) — 다음 세션에서 사용자 의사부터 확인할 것.
- [ ] **115차 실사용 검증(최우선, 최상위, 안드로이드+데스크탑 둘 다)** — 코드/컴파일/릴리스 빌드/3위치+호스트 배포까지 완료(안드로이드 versionCode `1789263480`, 데스크탑 BuildInfo `1789263485`), **GitHub 릴리스 게시는 아직 확인 안 됨**. 검증 항목: 소셜탭 메인 화면에 "👥 모임" 목록만 있고 그 아래 "💬 채팅" 목록/"+ 새 대화" 버튼이 사라졌는지, 모임 상세 화면엔 "멤버"/"💬 대화" 탭이 여전히 있고 대화가 정상 동작하는지, 관리자가 "⚙" 메뉴에서 "💬 모임 대화 끄기"를 누르면 본인/다른 멤버 화면 모두에서 대화 탭이 사라지고 "켜기"로 다시 누르면 돌아오는지, 일반 멤버에게는 이 on/off 메뉴 자체가 안 보이는지, 모임 멤버 목록/상세의 "💬" DM 버튼으로 DM이 여전히 정상 시작되는지.
- [ ] **114차 실사용 검증(최우선, 최상위, 안드로이드+데스크탑 둘 다)** — 코드/컴파일/릴리스 빌드/3위치+호스트 배포/GitHub 게시까지 완료(안드로이드 versionCode `1789224201`, 데스크탑 BuildInfo `1789224344`). 검증 항목: 안드로이드에서 모임원 상세 캘린더가 완료 개수 배지 방식으로 보이는지(데스크탑은 원래 스타일 유지), 모임 멤버 목록/모임원 상세/채팅 목록/그룹 대화방/DM 헤더 5곳에서 상대가 홈 공유를 켠 경우에만 `Lv.N 칭호` 배지가 뜨고 꺼두면 안 뜨는지, DM 상대가 나와 같은 모임에 없을 때 배지 없이 이름만 정상 표시되는지.
- [ ] **113차 실사용 검증(최우선, 최상위, 안드로이드+데스크탑 둘 다)** — "모임" 섹션이 "채팅"보다 위에 오는지, 모임원 상세 "홈" 탭이 라이브 홈 화면과 동일한 배경/나무를 실제로 그리는지(등급별로 배경색이 맞게 나오는지), 상대가 `sharePlant`를 껐을 때 여전히 "비공개"로 뜨는지. 코드/컴파일/릴리스 빌드/3위치+호스트 배포/GitHub 게시까지 완료(안드로이드 versionCode `1789221647`, 데스크탑 BuildInfo `1789221770`).
- [ ] **112차 실사용 검증(최우선, 최상위, 안드로이드+데스크탑 둘 다)** — 위 "현재 진행 중인 작업" 9개 항목: 태양이 설정 버튼과 안 겹치는지(다양한 화면 크기), 신규 설치 시 테마가 라이트 그린으로 시작하는지, 새 앱 아이콘(새싹)이 실제 런처/트레이에 반영되는지(안드로이드는 재설치 필요할 수 있음 — 아이콘 캐시), 설정 > 프로필에서 동물 아바타를 고르면 모임 멤버 목록/상세에 실제로 반영되는지, 소셜탭 통합 채팅 목록이 최근순으로 잘 섞이는지(1:1/모임 둘 다), 안드로이드 업데이트 배너에 더 이상 "이번 업데이트 내용" 텍스트가 안 뜨면서도 다운로드/설치는 정상 동작하는지, 모임 멤버 목록/상세의 "💬 DM" 버튼으로 대화가 시작되고 소셜탭 목록에 뜨는지, 모임원 상세 "🏠 홈" 탭이 상대가 공유 켠 경우에만 레벨/칭호가 보이는지, 학습 통계에서 "복습 단계별 일정 수"가 사라졌는지. 안드로이드는 컴파일/릴리스 빌드/3위치 배포/GitHub 게시까지 완료(versionCode `1789187642`), 데스크탑은 컴파일/MSI+app-image 빌드/호스트+vm-build-output 배포까지 완료(BuildInfo `1789189764`).
- [ ] **111차 실사용 검증(최우선, 최상위)** — 컴파일 확인+빌드+배포+GitHub 게시는 111차 후속에서 전부 완료(위 "현재 진행 중인 작업" 참고). 남은 것은 실사용 검증: 홈 탭이 첫 번째인지, 배경이 탭 바를 안 가리는지(특히 고등급 계정), 설정 탭이 사라지고 홈 우상단 버튼으로만 들어가지는지(안드로이드는 ☰ 드로어, 데스크탑/태블릿은 좌우 분할), "규칙" 라벨, 아이디 변경이 실제로 재로그인까지 정상 동작하는지(현재 비밀번호 확인 포함), 관리자 계정은 아이디 변경 UI가 숨겨지는지, `permPlant` 꺼진 사용자도 홈에서 설정 버튼에 접근 가능한지.
- [ ] **110차 "식물" 탭 등급/일러스트 개편 실사용 검증** — 레벨 1~500 전 구간을 훑으면서(설정에서 EXP를 직접 조작하거나 관리자 권한으로) 23단계 칭호가 각각 등급에 맞는 배경/나무 모양으로 정확히 바뀌는지, 등급이 바뀌는 경계(예: Lv.150 이상함 진입)에서 어색하게 끊기지 않는지, 종말급/최강자급 화면 흔들림 연출이 실제 기기에서 배경 깨짐 없이 자연스러운지, 환생을 여러 번 해서 금빛 링이 누적되는지, 저사양 기기에서 tier2 이상의 복잡한 드로잉(전체화면 그라디언트+파티클)이 프레임 드랍 없이 부드러운지.
- [ ] **109차 "식물" 탭 게임성 개편 실사용 검증** — 보상함 관련 요소가 다른 화면에 안 남아 있는지, 레벨바가 하단으로 이동해도 식물이 잘 보이고 레이아웃이 안 깨졌는지, "✨ 경험치 적용" 버튼을 눌렀을 때 대기 EXP가 정확히 한 번만 반영되는지(연타해도 중복 적용 안 되는지), 경험치바 상승/레벨업 연출/효과음이 자연스러운지(특히 한 번에 여러 레벨이 오르는 경우), 환생이 여전히 버튼+확인 없이는 실행 안 되는지, 앱을 재시작해도 대기 EXP/적용된 EXP/레벨이 정상 유지되는지. 안드로이드는 Room 마이그레이션(v39→40, reward 테이블 drop)이 기존 사용자 데이터에서 문제없이 적용되는지도 확인할 것.
- [ ] **107차 신규 사항 전체 실사용 검증(최우선)** — 신규 설치 후 로그인/가입승인 직후 권한 설정 가이드가 실제로 뜨는지, 각 항목 버튼이 올바른 시스템 설정 화면으로 이동하는지, 이미 켠 권한은 다시 요구하지 않는지, 설정 탭 "권한 설정 가이드" 버튼이 같은 화면을 재사용하는지. 관리자 패널에서 회원가입 계정이 더 이상 "게스트"로 안 보이는지(기존에 잘못 표시되던 계정은 해당 사용자가 다시 로그인해야 자동 교정됨 — 관리자 본인 로그인만으로는 다른 사용자 데이터가 안 바뀜). 온라인/오프라인 모드 스위치가 게스트 계정에서만 보이는지. 데스크탑에서 인터넷을 일부러 끊었다 붙였다 하면서 이미 승인된 계정이 가입 신청 화면으로 안 떨어지는지(오인 로그아웃 버그 재현 시나리오) — 이 항목은 코드 추적으로 원인은 확정했지만 실제 네트워크 단절 환경에서의 재현 검증은 아직 못 했음. 빌드/배포/GitHub 게시는 전부 완료(호스트 실제 교체 배포 포함).
- [ ] **106차 새로고침 아이콘 고착 버그 수정 + 소셜 전체 스크롤 실사용 검증(최우선)** — 안드로이드/태블릿에서 공부 섹션(타이머/캘린더/계산기/일정표/통계) 및 루틴/그룹/모임 목록에서 당겨서 새로고침 시 스피너가 매번 확실히 사라지는지(특히 살짝만 당겼다 뗀 경우), 소셜 탭 메인 화면에서 모임/대화가 많을 때 위아래로 자연스럽게 전체 스크롤되는지. material3/Kotlin 버전을 올린 변경이라 다른 화면(색상 피커 등 `ExperimentalMaterial3Api` 사용 13개 파일)에서 회귀가 없는지도 함께 확인.
- [x] **안드로이드 당겨서 새로고침 스피너 고착 버그(99차 이월, 원인 미확정) — 106차에 해결.** 원인은 material3 1.2.x 실험적 pull-to-refresh 구현 자체의 라이브러리 버그, compose-bom을 material3 1.3.0 계열로 올려 안정화된 `PullToRefreshBox` API로 교체해 해결. [[CHANGELOG.md]] 106차 참고.
- [ ] **105차분 잔여 검증** — 정해둔 레벨 구간(Lv.4/8/14/22/33/48/68/95/130/175...)에 도달하면 칭호와 식물 그림이 동시에 바뀌는지(칭호 텍스트와 일러스트 장식이 서로 어울리는지 — 예: "종건급" 칭호에 왕관+오라가 같이 나오는지), **설정 > 관리자 패널에서 "식물" 권한 칩을 꺼보면 그 사용자의 식물 탭이 실제로 사라지는지**, 루틴 탭 아이콘이 📋로 바뀌었는지, 신규 설치 시 테마가 화이트+오렌지로 시작하는지. 레이아웃/레벨업/환생 관련 항목은 위 109차 항목으로 대체됨. 102~104차 검증 항목(아래)은 전부 "식물" 탭의 새 레벨/경험치 시스템으로 대체됐으니 무시할 것.
- [ ] **102차 포인트/보상 시스템 실사용 검증**(104차 "레벨업"/103차 "캐릭터"는 105차에 완전히 새 시스템으로 대체돼 이 항목에서 제거함) — 공부 세션 종료/루틴 체크/캘린더 일정 완료 시 실제로 포인트가 쌓이는지, 그날 예정 루틴을 전부 체크하면 스트릭 보너스(+10)가 붙고 하나라도 해제하면 되돌아가는지, 보상 등록/삭제/언락(포인트 차감)이 정상 동작하는지, 두 기기에서 잔액/보상 목록이 동기화되는지. [[DECISIONS.md]] 102차 참고.
- [ ] **101차 모임 이름/설명 수정 기능 실사용 검증** — 모임장/관리자가 실제로 설명을 저장할 수 있는지, 저장된 설명이 모임 상세 화면(양 플랫폼)에 정상 표시되는지, 권한 없는 일반 멤버에게는 수정 메뉴 자체가 안 보이는지.
- [x] **소셜 채팅 전송 안 되는 버그(96차부터 이월) — 99차에 해결.** 원인은 `.indexOn` 색인 누락으로 읽기 조회가 400 거부되던 것, 사용자가 실사용으로 확인 완료.
- [ ] **99차 신규 사항 실사용 검증(최우선)** — 채팅 알림이 실제로 진동 없이 오는지(화면 꺼짐/다른 화면 보는 중에도), 지금 보고 있는 대화방이면 중복 알림이 안 뜨는지, 통계/일정표/타이머 3개 화면 새로고침이 실제로 동작하는지, 캘린더 새로고침 스피너가 실패 상황에서도 제대로 사라지는지. [[BUGS.md]] 99차 참고.
- [ ] **98차 신규 기능 전체 실사용 검증(최우선)** — 루틴 모드(모드 전환/추가/삭제 시 루틴 재배정/순서변경/모드별 위젯 표시/양 기기 동기화), 온라인/오프라인 모드(수동 토글, 네트워크 끊었을 때 자동 감지+각 화면 동기화 건너뛰기, 게스트 소셜 탭 차단), 계산기/차단규칙편집/소셜공유설정 버그 수정 3건이 실제로 고쳐졌는지. [[BUGS.md]] 98차 참고.
- [ ] **96차 변경사항 실사용 검증** — GroupListScreen 자물쇠 아이콘(태블릿/데스크탑 포함), GroupEditScreen 계산기 스타일(CompactField 직접 입력, 미니 캘린더 날짜 선택), 소셜 채팅 엔터 전송, 실행 확인 화면 커스텀 테마, 태블릿 공부 잠금 정지 버튼. [[BUGS.md]] 96차 참고.

**아래 9개는 90차 세션 종료 시점에 사용자가 직접 지정한 다음 세션 필수 작업이다(순서는 사용자가 말한 순서 그대로, 우선순위 순은 아님) — 다른 항목보다 먼저 처리할 것.**

- [x] **1. 공부(타이머) 잠금 오버레이 UX/UI 개편 — 92차에 완료, 릴리스 빌드/호스트 배포 포함.** `StudyTimerScreen`과 같은 디자인 언어(강조색 배지/radial gradient/큰 원형 진행률 링/카드형 허용앱 목록)로 전면 재구성, 정보(오늘 일정/누적 공부시간) 보강. [[CHANGELOG.md]]/[[DECISIONS.md]] 92차 참고.
- [x] **2. 모바일(안드로이드) 공부 타이머 탭에도 데스크탑에 새로 생긴 것 같은 요소를 넣기 — 92차에 완료.** `TimerIllustration` 안드로이드 이식 완료.
- [x] **3. 데스크탑/태블릿 루틴 화면 오른쪽 컬럼 변경 — 91차에 완료, 릴리스 빌드 포함.** "오늘 요약" 통계를 빼고 루틴 클릭 시 상세 정보(요일/시간대/기간/알림/이 루틴만의 연속 기록)가 뜨는 마스터-디테일로 교체(`RoutineScreen.kt` 데스크탑). 안드로이드는 이 화면에 원래 좌우 분할이 없어 대상 아님.
- [x] **4. 홈 화면 삭제 — 91차에 완료, 릴리스 빌드 포함.** `HomeScreen.kt`(양 플랫폼) 삭제 + `Tab.Home`/`TopSection.HOME` 제거, 첫 진입 섹션은 `visibleSections.first()`/`tabs.first()`로 자동 복원됨.
- [x] **5. 타이머 탭 빈 공간을 뭘로 채울지 재논의 — 92차에 완료, 릴리스 빌드/호스트 배포 포함.** 연속 공부 스트릭 카드/최근 7일 막대그래프/오늘 과목별 도넛 차트 3종으로 결정·구현. 구현 중 발견한 크로스디바이스 동기화 버그도 같이 수정([[BUGS.md]] 92차).
- [x] **6. "모임"(소셜 그룹) 섹션을 "소셜"로 개편 — 93차에 완료.** 이름 변경뿐 아니라 메신저 형태(그룹 대화 채널/1:1 DM/소유권 승계/디자인 통일)로 확장. **이걸로 90차 지정 9개 전부 완료.**
- [x] **7. 자체 업데이트 배너의 "변경 내용" 표시를 한글로만 나오게 — 91차에 완료, 릴리스 빌드 포함.** 원인은 GitHub 자동생성 텍스트가 아니라 세션마다 `gh release create --notes`를 영어/한글 섞어 채워온 것이었음 — android `UpdateBanner.kt`에 한글 음절이 하나도 없으면 숨기는 필터 추가(desktop은 애초에 이 텍스트를 표시하지 않음). 과거 영어 릴리스 노트 자체를 고치는 건 범위 밖.
- [x] **8. 문서 정리 — 91차에 진행.** `HANDOFF.md`의 "현재 진행 중인 작업"에 쌓여있던 60~88차 세션별 서술(전부 [[CHANGELOG.md]]에 이미 상세 기록됨)을 정리하고 89~91차만 남김.
- [x] **9. 공부(타이머) 탭에도 접근성 서비스 꺼짐 경고 배너 추가(안드로이드) — 91차에 완료, 릴리스 빌드 포함.** `StudyTimerScreen.kt`에 `GroupListScreen.kt`와 같은 패턴으로 배너 추가.

---

- [x] **관리 탭 메인 화면(차단 규칙 목록, `GroupListScreen.kt`) UI 개편 — 96차에 완료.** 자물쇠 아이콘 방식으로 확정, 태블릿/데스크탑까지 통일 적용. [[CHANGELOG.md]] 96차 참고.
- [ ] **95차 안드로이드 자체 업데이트 다운로드/설치 수정 실사용 검증(최우선)** — 시스템 DownloadManager를 앱 내부 직접 HTTP 다운로드로 교체했다(81차부터 반복된 "다운로드 멈춤" 제보 대응). GitHub 릴리스([android-1788711405](https://github.com/studybeultaeon-svg/study-planner/releases/tag/android-1788711405) 이상)에서 수동 설치 후 실제로 업데이트 버튼이 끝까지 동작하는지 확인 필요 — 안 되면 정확히 어느 단계(진행률/설치화면)에서 멈추는지부터 파악할 것. [[BUGS.md]] Open 참고.
- [ ] **94차 그룹 설정 동기화 opt-in 개편 실사용 검증(최우선)** — 두 기기에서: 한 기기가 동기화 켠 규칙이 다른 기기 "불러오기" 목록에 실제로 뜨는지, 불러오면 설정만 반영되고 앱/사이트 목록은 그대로인지, 새 규칙 생성 시 이름 충돌 확인창 예/아니오 양쪽 분기, 로컬 규칙 동기화 토글 켤 때 충돌 확인, 차단 규칙 꺼짐 시 카드 흐림 표시가 라이트/다크/커스텀 테마 전부에서 잘 보이는지. 코드/빌드/배포/GitHub 게시까지 완료(안드로이드 versionCode `1788703336`, 데스크탑 BuildInfo `1788703299`).
- [ ] **93차 신규 소셜 메신저 기능 실사용 검증(최우선)** — 그룹 "💬 대화" 채널에서 실제로 메시지가 양쪽 기기에 4초 폴링으로 도착하는지, 이모지 리액션이 반영되는지, 1:1 DM을 커스텀 아이디로 검색해 시작하고 대화 목록에 뜨는지, 모임장 소유권 승계가 실제로 넘어가는지(넘긴 사람이 관리자가 되는지)를 최소 두 기기/두 계정으로 확인할 것. 코드/빌드/배포/GitHub 게시까지는 완료(데스크탑 BuildInfo `1788695394`, 안드로이드 versionCode `1788695472`), Firebase 규칙(`groupChats`/`dmChats`/`dmChatIds`)도 사용자가 콘솔에 게시 완료.
- [ ] **92차 신규/수정 사항 전체 실사용 검증(최우선)** — 잠금 화면 새 디자인이 실제 공부 잠금 시 정상 표시되는지(특히 원격 잠금 케이스), 타이머 탭 스트릭/주간그래프/도넛 차트가 실제 여러 날짜·다른 기기 기록과 함께 정확히 집계되는지(이번에 고친 동기화 버그의 실제 검증), 안드로이드 타이머 탭 일러스트 표시. 호스트 실제 교체 배포까지 완료(데스크탑 BuildInfo `1788690197`, 안드로이드 versionCode `1788690271`), GitHub 게시는 아직 안 함.
- [ ] **91차 신규/수정 사항 전체 실사용 검증(최우선)** — 홈 화면 삭제 후 첫 진입 화면이 예상대로인지, 데스크탑 루틴 오른쪽 컬럼 클릭→상세 패널이 실제 클릭에서 잘 동작하는지, 공부 탭 접근성 배너가 실제로 뜨고 사라지는지, 자체 업데이트 배너가 한글 릴리스 노트에서 정상 표시되는지(도움말 관련 항목은 97차에 기능 자체가 삭제돼 제외). 릴리스 빌드까지는 완료(안드로이드 versionCode `1788661502`, 데스크탑 BuildInfo `1788661637`), 이 호스트 실제 교체 배포/GitHub 게시는 92차에 완료(더 최신 빌드로 교체됨).
- [ ] **87차 신규/수정 사항 전체 실사용 검증(최우선)** — 스누즈 on/off+횟수 그룹별 설정, 재설계된 색상 피커(스펙트럼+슬라이더), 15분 주기 자체 업데이트 체크, 태블릿 레이아웃 5개 화면. 위 "현재 진행 중인 작업" 87차 항목 참고.
- [ ] **85차 신규/수정 사항 전체 실기기 검증(최우선)** — 캘린더 자연정렬, 자체 업데이트 배너 오류 표시, 요일별 목표 overlayStepper 필드, 날짜 필드 폰트, 5탭 아이콘/라벨 배치, 설정 동기화(다회독 기본값/초기화 시각), 4종으로 줄어든 테마 선택 화면. 전부 코드·컴파일 검증까지만 끝났고 실기기 미검증.
- [ ] **83차 신규 기능 4건 전체 실기기 검증(최우선)** — 다회독 상세화(Room v34 마이그레이션이 기존 진행 상태 보존하는지 포함)/태블릿 UI(NavigationRail/ResponsiveSplit)/계산기 미니캘린더·스테퍼/캘린더 자동생성 요일·휴일 반영. 전부 코드·컴파일 검증까지만 끝났고 실기기 미검증.
- [ ] **Firebase 콘솔 확인(낮은 우선순위, 사용자가 보류 결정, 2026-09-02)** — Android/Desktop API 키 불일치는 동기화가 실제로 잘 작동해온 점에서 버그가 아닐 가능성이 높다고 판단해 지금은 확인 안 하기로 함. "이 기기만 동기화 이상함" 증상이 실제로 생기거나 클라우드 백업 기능을 쓰고 싶을 때 재검토. [[BUGS.md]] Open 참고.
- [ ] **81차 자체 업데이트 다운로드 수정 최우선 검증** — 실제로 다운로드가 끝까지 진행되고 진행률이 표시되는지, 실패하면 사유가 뜨는지. 이 기능이 계속 안 되면 사용자에게 GitHub 릴리스 페이지에서 apk 수동 설치를 안내.
- [ ] **81차 나머지 3건(알림 억제/무작위 알림 정정/관리 공유 제거) 실사용 검증** — 공부 페이즈 진입 시 루틴/스트릭/모임 알림이 실제로 안 오는지, 공부 종료 시 큐에 쌓인 루틴 알림이 진동과 함께 다시 오는지, 깨우기/무전기가 자연스럽게 재폴링되는지, 모임 "무작위 알림"이 이제 자동 발송 없이 정보 알림만 오는지, 공유 설정에서 "관리" 항목이 실제로 사라졌는지.
- [ ] **80차 신규 기능 4건 전체 실사용 검증** — 특히 데스크탑 오버레이 전체화면 클릭-통과가 실제로 아래 프로그램 클릭을 그대로 통과시키는지(가장 리스크 큼), 다회독 토글이 이제 화면 이동 없이 즉시 바뀌는지, 설정 > 공부의 새 기본값 토글이 실제 새 일정에 반영되는지.
- [ ] **79차 신규 기능 8건 전체 실사용 검증** — 자체 업데이트 무한반복 해소 여부, 종료확인 토글 20문항, 반응형 레이아웃, 테마 커스텀 대비.
- [ ] **78차 신규 기능 전체 실사용 검증** — 태블릿 릴스/쇼츠 차단, 모임 멤버 상세 "일정표" 탭, 무전기 남성 목소리.
- [ ] **77차 신규 기능 전체 실사용 검증** — 모임 멤버 상세 탭 구조, 관리자 승격·해제·내쫓기·이름수정, 무작위 알림, 캘린더 3단계 회독.
- [ ] **무전기 재설계 전체 실사용 검증(73~74차 이월)** — 모임별 설정 저장/불러오기, 요일·시간대 일정 다중 등록, 알림/음성/텍스트 3종 전송·수신, TTS 재생 여전히 미검증.
- [ ] **기존 Google 로그인 사용자의 재가입 안내(72차 이월)**: 로그인 방식이 바뀌면서 예전 프로필과 연결이 끊긴 계정들에게 재가입 신청 + 관리자 재승인이 필요하다는 걸 안내할 것.
- [ ] **별도 웹앱 "공부앱"의 뽀모도로 연동 복구 여부 결정(61차 이월)**: 로그인 필수 전환으로 끊긴 연동을 복구할지 포기할지 미결정.
- [ ] [[IDEAS.md]] "타이머 크로스디바이스 미러에 원격 정지/전환 제어 추가 검토" — 사용자가 실사용 후 불편하면 검토.

---

# 현재 주의사항

- **그룹 on/off는 `groupEnabled` 필드를 직접 편집하는 방식으로 절대 대신 처리하지 말 것(86차, 사용자가 강하게 지적)**: `data.json`에서 `groupEnabled`를 직접 `false`로 바꿔서 그룹을 끄려고 시도했다가 사용자에게 "off 버튼을 누른 것처럼 하라고 문서에 적혀있다"는 지적을 받고 원상복구함. **실제 이유**: `GroupListScreen.kt`의 진짜 off 스위치는 단순 필드 변경이 아니라 — 오늘 적용되는(스케줄/일일한도/실행확인 중 하나라도 오늘 요일에 걸리는) 그룹이면 `groupOffPending=true`로 회유 멘트 20개(`PERSUASION_MESSAGES`)를 랜덤 딜레이로 하나씩 통과해야 실제로 `groupEnabled=false`가 되는 절차(`LockEvaluator.effectiveGroupEnabled` 참고)를 강제한다 — 이게 이 앱의 핵심 자기통제 장치라 파일을 직접 고치면 그 저항 절차 자체를 통째로 우회하게 된다. **그룹을 끄고 싶다는 요청을 받으면 앱의 실제 스위치를 사용자가 직접 누르게 안내할 것 — 대신 파일을 고쳐주지 말 것.** 86차 세션 중 실수로 편집했다가 즉시 백업으로 원상복구(8개 그룹 전부 다시 `groupEnabled: true`), watchdog는 편집 전후로 정상적으로 disable/enable 처리함.
- **82차 발견: 호스트에서 Gradle 컴파일 확인 시 OneDrive 경로(한글 폴더명)를 직접 쓰면 실패한다** — `generateDebugBuildConfig` 등에서 `AccessDeniedException`(실은 JVM 네이티브 인코딩이 한글 경로를 못 읽는 문제, chcp/파일.encoding -D 옵션으로도 해결 안 됨). 컴파일만 빠르게 확인할 땐 아무 ASCII 경로에나(예: `C:\build\phonelock-android`) `robocopy "<OneDrive 원본>" "<스크래치 경로>" /MIR /XD build .gradle .idea` 로 미러 후 `gradle compileDebugKotlin`/`compileKotlin` 실행 — 이런 스크래치 경로는 세션이 끝나면 지워도 됨(커밋 대상 아님). **실제 릴리스 빌드(`assembleRelease`/`packageMsi`/`createDistributable`)는 반드시 아래 "실행 방법"의 공식 경로**(`AndroidBuilds\phone-lock-android`, `C:\build\phone-lock-desktop`)를 쓸 것 — 이름이 비슷해 혼동하기 쉽다. JDK는 안드로이드 `~/jdk21-temurin` 또는 Android Studio JBR, 데스크탑은 jpackage 필요해 반드시 `~/jdk21-temurin`. Gradle은 `~/.gradle/wrapper/dists/gradle-8.7-bin/.../bin/gradle`(gradlew 없음, 이 경로로 직접 호출). 소스 편집은 항상 OneDrive 원본에서.
- **105차 발견(심각): `C:\AndroidBuilds\`(`\Users\sunae\` 없음)와 `C:\Users\sunae\AndroidBuilds\`는 서로 다른 실제 디렉터리다(심볼릭 링크 아님, `fsutil reparsepoint query`로 확인)** — 안드로이드 공식 빌드 경로는 **반드시 `C:\Users\sunae\AndroidBuilds\phone-lock-android`**(아래 "실행 방법" 참고)인데, 105차 세션 중 소스 동기화 robocopy 명령에 실수로 `C:\AndroidBuilds\phone-lock-android`(접두사 없는 쪽)를 쓰고 빌드는 올바른 경로에서 실행해서, robocopy는 "정상 복사됨"으로 보고했지만 실제 빌드는 **전혀 안 바뀐 옛 소스**로 진행된 채 GitHub 릴리스까지 게시된 적이 있다(사용자가 "업데이트했는데 구버전이 깔린다"고 제보해서 발견 — `unzip`+`grep -a`로 빌드된 APK의 `classes.dex`에서 새로 추가한 문자열 상수가 실제로 들어있는지 확인하는 방법으로 확정, [[BUGS.md]] 105차 참고). **안드로이드 경로를 타이핑할 때마다 `C:\Users\sunae\AndroidBuilds\...`인지 두 번 확인할 것** — `C:\AndroidBuilds`는 과거 어느 세션이 실수로 만든 동명이인 폴더로 보이며(시스템 보호 경로라 삭제도 안 됨), 앞으로도 혼동을 유발할 수 있으니 절대 이 경로를 쓰지 말 것. **일반적으로, robocopy가 "N개 복사됨"이라고 보고해도 그게 내가 의도한 목적지인지는 별개의 문제다 — 중요한 빌드 전에는 타겟 파일을 직접 `grep`/`Select-String`으로 열어 실제로 새 코드가 들어있는지 확인하는 습관을 들일 것.**
- **82차 발견: `shared/` composite build 모듈은 각 플랫폼 빌드 경로의 형제 폴더로도 미러해야 한다** — `phone-lock-android`/`phone-lock-desktop`의 `settings.gradle.kts`가 `includeBuild("../shared")`로 상대경로 참조하므로, `AndroidBuilds\phone-lock-android`를 쓰면 `AndroidBuilds\shared`도, `C:\build\phone-lock-desktop`을 쓰면 `C:\build\shared`도 함께 최신으로 robocopy 미러해야 한다(둘 다 안 하면 "찾을 수 없는 모듈" 에러). `sync-public-repo.ps1`도 82차에 `shared/` 동기화를 포함하도록 수정됨 — 최상위에 새 Gradle 프로젝트(모듈)를 또 추가하는 일이 생기면 이 스크립트가 그 프로젝트를 아는지 항상 함께 확인할 것.
- **재확인/escalation 판정 로직에는 절대 손대지 말 것** — 표시값(오버레이 알파 등)과 판정 로직(언제 재확인을 요구할지)은 별개. 과거 이 원칙을 어겨 큰 삽질이 있었음([[DECISIONS.md]] "표시값과 판정 로직 분리" 참고). `ConfirmationGate.kt`(양쪽)는 원본 그대로 유지되어야 함.
- **사용자가 "표시/UI가 이랬으면"이라고 요청했을 때 보안/판정 로직까지 확대 해석하지 말 것**. 애매하면 먼저 확인.
- **네이티브 UI를 웹앱과 맞출 땐 스크린샷 추측 대신 `공부앱/index.html`의 CSS/JS부터 확인**(32차 세션 교훈) — 관련 클래스명을 Grep해서 정확한 hex값/구조를 얻는다. [[DECISIONS.md]] "UI를 웹앱과 맞출 때는..." 참고.
- 로컬 API에 새 엔드포인트 추가 시 호출 주체(content script vs 서비스워커/확장페이지)에 따라 CORS 정책 결정 — [[DECISIONS.md]] 참고.
- Compose Desktop `Window`는 클릭을 가로챈다 — 화면을 덮는 새 UI 만들 때 항상 고려.
- `EnforcementService.tick()`의 뮤텍스 범위를 건드릴 땐 "확인 대기는 뮤텍스 밖" 원칙 유지(안 지키면 다른 그룹 감시 전체가 멈추는 예전 버그 재발).
- 안드로이드 `PhoneLockRepository`에서 `escalationCache`/DB 접근 새 함수 추가 시 반드시 `escalationMutex.withLock { }`으로 감쌀 것(`mergedEscalation()` 자체는 잠금 없음, 재진입 데드락 주의).
- **소스는 항상 OneDrive 원본에서 편집**, 빌드는 항상 `AndroidBuilds\`에서, Android APK는 항상 두 위치 모두 갱신.
- **데스크탑 실행/테스트는 절대 `gradle run`으로 하지 말 것(61차, 중요)**: `Watchdog.kt`가 자기 자신을 재시작할 때 쓰는 실행파일 경로가 `gradle run`에선 `java.exe`가 돼버려서, 감시 프로세스가 5초마다 실패-재시도를 반복하며 좀비 `java.exe`가 순식간에 수백 개 쌓인다. 실기기 동작 확인은 항상 `createDistributable`로 빌드한 패키징된 `.exe`로 할 것. [[BUGS.md]] 61차 참고.
- **새로 작성하는 `.ps1` 스크립트에 한글 텍스트(주석 포함)를 넣지 말 것(61차)**: BOM 없는 UTF-8 `.ps1`을 Windows PowerShell 5.1이 시스템 코드페이지로 잘못 읽어서, 한글이 섞인 지점 이후의 코드가 예외 없이 조용히 오동작할 수 있다(경로 문자열이면 즉시 예외, 주석이면 더 뒤에서 원인 불명의 null 결과로 나타남). 영문 주석만 쓰고, 경로는 하드코딩 대신 `$PSScriptRoot`/상대경로를 쓸 것. [[BUGS.md]] 61차 참고.
- **빌드는 원칙적으로 VM_BUILD_HANDOFF.md 절차대로 별도 VM에서(73차 재확인)**: 호스트엔 실제 운영 중인 워치독+프로덕션 Firebase가 있어 리스크가 있다는 이유로 VM 격리가 정해져 있었다. 73차엔 사용자가 명시적으로 "여기서 진행해"라고 요청해서 호스트에서 직접 빌드/배포했고, 그 과정에서 호스트에 이미 Gradle 8.7 캐시(`~/.gradle/wrapper/dists/gradle-8.7-bin`)가 있다는 걸 확인 + jpackage 포함 Temurin JDK 21을 새로 받아 `~/jdk21-temurin`에 둠(Android Studio 번들 JBR은 jpackage가 없어 `createDistributable`엔 못 씀, Gradle 8.7 자체는 JDK 25에서 못 돌아감 — Kotlin DSL 스크립트 컴파일러가 "25.0.2" 버전 문자열을 파싱 못 함). **다음에도 호스트에서 직접 빌드하려면 매번 사용자에게 다시 확인받을 것** — VM 없이 가능하다는 게 검증됐다고 해서 원칙이 자동으로 바뀐 건 아니다.
- **git push 금지**: 명시적 요청 없이 절대 push 금지. **단, 예외로 `sync-public-repo.ps1`(77차 신규)을 통한 `study-planner` 저장소(`origin/main`) 동기화는 사용자가 "알아서 해"라고 명시적으로 위임했으므로, 소스(`phone-lock-android`/`phone-lock-desktop`) 변경이 있을 때마다 요청 없이 먼저 실행해도 된다** — 이 스크립트는 내부 문서(BUGS/CHANGELOG/DECISIONS/HANDOFF/IDEAS 등)는 절대 건드리지 않고 소스+`.gitignore`+`.claude/launch.json`만 옮기므로 범위가 좁다. 그 외 모든 git push(다른 브랜치/저장소, 강제 push 등)는 여전히 매번 확인 필요.
- **데스크탑 배포 전**: 반드시 프로세스 먼저 종료(파일 잠김 방지) — 요청 없이 직접 종료해도 됨. 배포 검증 절차(프로세스 종료→동기 robocopy→FAILED 0 확인→jar 해시 비교→재실행)는 [[BUGS.md]] "robocopy가 실행 중인 프로세스의 파일을 조용히 스킵" 참고. **78차 발견**: 데스크탑 앱은 인메모리 워치독이 있어 `Stop-Process` 직후 1초 이내에 스스로 재기동할 수 있다 — robocopy가 exe를 못 옮기고 `ERROR 32`로 30초 간격 무한 재시도에 빠지면, 기다리지 말고 즉시 `Stop-Process`를 한 번 더 실행한 뒤 곧바로 robocopy할 것([[BUGS.md]] 78차 참고).
- **Android APK**: 코드 수정 후 요청 없어도 바로 빌드/업로드 가능. **78차부터 기본은 `assembleRelease`(release 서명 APK)** — `assembleDebug`는 컴파일만 빠르게 확인할 때만 쓰고, 실제 배포/게시용은 항상 release. 배포 시 위치는 3곳 모두 갱신: `AndroidBuilds\phone-lock-app-release.apk`, OneDrive 원본 `app\build\outputs\apk\release\app-release.apk`, `vm-build-output\android\app-release.apk`.
- **"깃허브에 올려/커밋해"는 GitHub Release 게시까지 포함한다(78차 확정)**: 소스를 `sync-public-repo.ps1`로 공개 저장소에 커밋/푸시하는 것만으로 끝내지 말고, `gh release create`로 태그된 릴리스(Android `android-<versionCode>`, Desktop `desktop-<BuildInfo.BUILD_TIMESTAMP>`)까지 만들 것 — 안 하면 자체 업데이트 기능이 참조하는 GitHub Releases가 갱신 안 돼 "확인해도 최신 버전"이라고 잘못 뜬다.
- **빌드 스크립트 파일(`build.gradle.kts`/`settings.gradle.kts`)도 소스 동기화 대상이다(78차 발견)**: `robocopy /MIR`로 `src/`만 반복 동기화해오다가, 75차에 추가된 `generateBuildInfo` 태스크가 `AndroidBuilds\phone-lock-desktop\build.gradle.kts`엔 한 번도 반영 안 된 채 방치돼 컴파일이 실패한 적이 있다([[BUGS.md]] 78차 참고) — 빌드 스크립트 자체가 바뀌는 세션 이후엔 이 파일들도 diff/재복사할 것.
- **데스크탑 `generateBuildInfo` 태스크는 항상 재실행하도록 고쳐져 있다(98차 발견/수정)**: 원래 `outputs.dir(...)`만 선언하고 inputs가 없어서, 같은 `C:\build\phone-lock-desktop` 디렉터리에서 여러 번 빌드하면 Gradle이 UP-TO-DATE로 캐싱해버려 며칠 전 타임스탬프가 그대로 남는 버그가 있었다(자체 업데이트 버전 비교가 이 값에 의존) — `outputs.upToDateWhen { false }`로 고쳤다. 만약 앞으로 이 태스크를 다시 손대게 되면 이 강제 재실행 설정을 지우지 말 것.
- Room DB 버전: 스키마 변경 시 `AppDatabase.kt`의 `version` 올려야 함(현재 38 — 38차 21→22(`ConfirmCounter`), 39차 22→23(`StudyLogEntry.note`)→24(`AppGroup` 스누즈/기간지정 필드 6종), 42차 24→25(`AppGroup.blockAttemptDate`/`blockAttemptCount`), 48차 25→26(`Routine`/`RoutineLog`), 52차 26→27(`Routine.icon`/`notifyEnabled`/`startDate`/`endDate`), **82차부터 v28~v37은 전부 명시적 `MIGRATION_x_y` 사용(아래 항목 참고), 98차 37→38(`routine_mode` 테이블+`Routine.modeId`, `MIGRATION_37_38`)**) — v27까지는 `fallbackToDestructiveMigration()`(마이그레이션 코드 불필요), v28부터는 반드시 명시적 마이그레이션 필요(아래 82차 항목 참고). `PreMigrationBackup`이 삭제 전 raw SQLite 백업(TABLES 목록에 `routine`/`routine_log`/`routine_mode` 포함 확인할 것).
- **Room 버전을 올릴 땐 반드시 `AppPreferences.resetSyncTimestamps()`가 호출되는지 확인할 것(52차, 심각한 버그였음)**: `fallbackToDestructiveMigration()`은 Room(SQLite)만 지우고 캘린더/루틴/계산기의 "마지막 동기화 시각"(SharedPreferences 저장)은 그대로 남겨서, 다음 동기화가 "로컬이 이미 최신"으로 오판해 텅 빈 로컬 상태를 방치하는 버그가 있었다. `MainActivity.onCreate`에서 `PreMigrationBackup.backupIfVersionChanged()`가 non-null을 반환하면(=이번 실행에서 버전이 바뀌어 백업했으면) 자동으로 리셋되므로 이 호출 자체를 지우지 말 것 — 새 SharedPreferences 기반 동기화 타임스탬프를 추가할 땐 `resetSyncTimestamps()`에도 같이 넣을 것. [[DECISIONS.md]]/[[BUGS.md]] 52차 참고.
- **그룹(차단 대상 목록)은 Firebase에 동기화되지 않는다** — Room이 destructive migration으로 지워지면 그룹은 자동 복구가 안 되고, `PreMigrationBackup`이 만든 raw SQLite 백업 JSON에서 `PhoneLockRepository.restoreGroupsFromBackup()`(설정 화면 "⚠ 그룹 데이터 복구" 카드)으로만 복구 가능(52차 신설). 앞으로 이 종류의 로컬 전용 데이터를 다루는 새 테이블을 추가하면 같은 유실 위험이 있다는 걸 염두에 둘 것.
- **화면 문구를 바꿔달란 요청을 받으면 그 문구가 제목(title)인지 부제(quote)인지 먼저 화면에서 확인할 것(42차 교훈)**: `WatchAndWaitScreen`/`InterstitialScreen`은 title(큰 헤드라인)과 quote(작은 부제) 두 개의 별도 텍스트 슬롯을 갖는데, 42차에 "무시당하면서 살기, 무시하면서 살기"(title) 교체 요청을 받고도 실제로는 quote 슬롯(원래 "의무에 따라 행동하세요")에 새 문구를 걸었다가 사용자 지적으로 뒤늦게 바로잡았다. 사용자가 인용한 문구 원문과 코드의 어느 파라미터가 실제로 그 문자열을 갖고 있는지 대조부터 할 것.
- **크로스디바이스 신규 동기화 추가 시(34차 결정)**: `pomodoro`/`confirmSync`처럼 "신호"만 필요하면 24차 패턴(로컬이 source of truth, Firebase는 부가 채널)을 따르고, `dailyUsage`/`studyLog`처럼 "여러 기기가 각자 추가하는 실제 데이터"면 문서 전체 LWW(캘린더/계산기 방식) 대신 반드시 "기기별 키에만 쓰고 읽는 쪽이 합산"하는 패턴을 쓸 것 — 문서 전체 LWW를 쓰면 다른 기기가 그 사이 추가한 항목이 통째로 사라진다. **원격 명령(remoteCommand) 패턴은 다시 쓰지 않는다** — 19차 세션에 도입했다가 24차에 신뢰성 문제로 완전히 제거한 전례가 있음, 크로스디바이스 제어가 필요하면 먼저 사용자와 상의.
- **Firebase 관련 버그 디버깅 시(34차 교훈)**: 코드만 보고 추측하지 말고, 로컬 데이터파일(데스크탑 `%APPDATA%\PhoneLockDesktop\data.json`)에서 `fbDatabaseUrl`/`fbApiKey`/`fbUser`를 읽어 REST API로 실제 서버 상태를 직접 조회하면 어느 기기의 어느 단계(쓰기/읽기/UI 반영)에서 막히는지 빠르게 좁힐 수 있다. 익명 인증: `POST https://identitytoolkit.googleapis.com/v1/accounts:signUp?key={apiKey}` → `idToken`으로 `{databaseUrl}/{경로}.json?auth={idToken}` 조회.
- **`dailyResetHour` 설정값 확인(35차 발견)**: 데스크탑 로컬 데이터파일(`%APPDATA%\PhoneLockDesktop\data.json`)의 `dailyResetHour`가 현재 **9**(오전 9시 리셋)로 설정돼있다 — `effectiveDate()`(`Repository.kt:13`)가 자정이 아니라 이 값을 기준으로 "오늘"을 판정하므로, 리셋 시각 전이면 캘린더 일정 선택(타이머 드롭다운)·공부기록·일일 사용한도가 전부 아직 "어제"로 계산된다. 버그 아님 — 사용자가 이 값을 유지하기로 결정(35차). "왜 어제 기록/일정이 오늘도 보이냐"는 문의가 다시 오면 이 설정부터 확인.
- **robocopy는 반드시 PowerShell 도구로, 절대경로로 실행할 것(36차 발견, 매우 중요)**: `robocopy "OneDrive 원본\...\src" "AndroidBuilds\...\src" /MIR`를 Bash 도구로(특히 상대경로+`cd` 조합) 실행하면 이 프로젝트의 한글 경로(`OneDrive\바탕 화면\클로드\관리앱`) 때문인지 "성공"처럼 보이는 출력을 내면서 실제로는 파일을 하나도 안 옮긴다. 이 상태에서 아무리 재빌드해도 `AndroidBuilds`의 옛날 소스만 컴파일되고, Gradle이 `compileKotlin`/`jar`를 `UP-TO-DATE`로 스킵하기까지 하면 "BUILD SUCCESSFUL"만 보고 정상 배포된 것으로 착각하기 쉽다(36차에 실제로 이 착각으로 여러 차례 "재배포 완료"를 잘못 보고함). robocopy 출력에 실제로 "Newer"/"New File" 줄이 찍히는지 반드시 확인하고, 의심스러우면 `grep -a <새로 추가한 심볼> AndroidBuilds\...\<파일>`로 소스 동기화부터 검증할 것. 메모리 `feedback_robocopy_use_powershell` 참고.
- **"고쳤는데 반영이 안 된다"가 반복되면 UI/상태 로직을 더 파기 전에 배포된 바이너리부터 검증(36차 교훈)**: 배포된 jar/apk를 `unzip`으로 풀어 `.class` 파일을 `grep -a`로 열어보면(바이너리지만 문자열 상수는 grep으로 보임) 수정한 필드명/문자열이 실제로 컴파일돼 들어갔는지 바로 확인 가능 — 코드 로직을 계속 재검토하는 것보다 훨씬 빠르게 "빌드/배포 파이프라인 문제 vs 진짜 로직 버그"를 구분할 수 있다.
- **크로스디바이스 기능이 "판정 로직 자체"를 확장할 땐 원본 파일을 건드리지 말고 호출부에서 병합할 것(37차 패턴)**: 실행확인 유예시간을 다른 기기와 공유하는 기능을 추가할 때, `ConfirmationGate.kt`(재확인/escalation 판정, 원본 유지 필수 — 위 항목 참고)를 직접 고치지 않고, `Repository.syncedLastConfirmedAtEpochMillis()`(이미 있던 `mergedEscalation` 재사용)로 동기화된 값을 따로 얻은 뒤 호출부(`EnforcementService`/`SiteEnforcement`/안드로이드 `AppMonitorAccessibilityService`)에서 로컬 `ConfirmationGate` 값과 비교해 더 최근인 쪽을 쓰는 방식으로 구현했다. "절대 손대지 말 것" 규칙이 있는 파일을 확장해야 할 필요가 생기면 이 패턴(원본은 그대로, 호출부에서 병합)을 우선 검토할 것.
- **락 안에서 블로킹 네트워크 I/O를 하면 앱 전체가 정지할 수 있다(38차 재확인)**: 새 push/read 함수를 추가할 때 항상 "이 함수가 `synchronized(lock)`/`tickMutex` 등 락을 쥔 채로 호출되는가"를 먼저 확인할 것 — 쥔 채라면 실제 네트워크 I/O는 반드시 락 밖(별도 Thread 또는 캐시값 즉시 반환+백그라운드 갱신)으로 빼야 한다. 38차에 `pushUsageToFirebase`/`peerUsageSeconds`에서 이 원칙이 깨진 걸 발견해 수정했다([[BUGS.md]] 38차 참고) — 단, escalation 계산(`mergedEscalation()`)처럼 판정 로직에 인접한 함수는 같은 유형의 버그라도 신중하게 범위를 좁혀 보수적으로 접근할 것(이번엔 그 함수는 손대지 않고 남겨둠).
- **이 프로젝트 세션 환경은 매번 빌드 툴체인이 있다는 보장이 없다(38차 재확인)**: 38차엔 gradle 자체가 없어 컴파일 확인을 한 번도 못 한 채 세션이 끝났다 — `project_build_toolchain_missing` 메모리 절차를 먼저 시도해보고, 그래도 안 되면 코드 변경 후 "컴파일 미검증"임을 명시적으로 문서(HANDOFF/BUGS/CHANGELOG)에 남길 것. 과거 반영이 안 됐던 사고들이 전부 "됐다고 보고했는데 실제로는 안 됨" 패턴이었으므로, 검증 못 한 걸 검증했다고 절대 표현하지 않는다.
- **원격 신호로 실제 잠금을 거는 기능은 반드시 신선도(staleness) 컷오프를 둘 것(37차)**: 34차의 "미러 표시"는 신호가 오래돼도 화면이 좀 이상하게 보이는 정도지만, 37차의 "원격 신호로 실제 공부 잠금을 건다"는 기능은 신호를 올리던 기기가 정지 없이 꺼지면 이 기기가 영구히 잠길 수 있는 심각한 문제로 이어진다. 34차에 이미 쓰던 20분 컷오프(`REMOTE_STUDY_SIGNAL_STALE_MS`, `remoteUpdatedAtMillis()` 기준)를 재사용해서 20분 넘게 갱신 없는 신호는 무시하도록 했다 — 앞으로 원격 신호 기반의 새 "강제 동작"(잠금, 차단 등)을 추가할 때 항상 이 안전장치를 함께 넣을 것.
- **`LockEvaluator`를 확장해야 하는 새 기능은 `isPomodoroUnlocked()` 패턴을 우선 검토할 것(39차, 스누즈/기간 지정 자동 강화에 적용)**: "판정을 일시적으로 우회하되 `groupEnabled` 등 영구 상태는 안 건드리는" 기능(스누즈처럼)은 `evaluate()`/`isCurrentlyRestricting()`/`isConfirmActiveNow()` 최상단에 `isPomodoroUnlocked()`와 나란히 체크를 추가하는 게 안전하다(이미 검증된 패턴 재사용, `ConfirmationGate.kt`는 안 건드림). 반대로 "스위치 자체를 강제로 뒤집는" 기능(기간 지정 자동 강화처럼)은 `isGroupActive()`/`effectiveGroupEnabled()` 레벨에서 처리하되, `now` 파라미터를 기본값(`LocalDateTime.now()`)으로 추가해 기존 호출부(`isGroupActive(group)`)가 전부 그대로 컴파일되게 했다 — 이 방식이면 새 필드가 비어있는 기존 그룹은 동작이 100% 그대로 유지된다. 스누즈/기간 지정이 동시에 걸리면 기간 지정이 우선하도록 순서를 정했다([[DECISIONS.md]] 39차 참고) — 앞으로 판정 우회 기능이 여러 개 겹칠 때도 "미리 설정해두는 강제 규칙 > 즉흥적 예외"를 기본 순서로 검토할 것.
- **Compose에서 리스트 상태 토글이 "다른 탭 갔다와야 반영된다"는 재현을 받으면 구조적 동등성 리컴포지션 스킵부터 의심할 것(50차)**: `mutableStateOf<List<T>>`을 값이 동일한 새 리스트로 재할당하면 Compose가 변경을 감지 못 해 리컴포지션을 건너뛴다 — 그룹 탭(과거)과 루틴 오늘 탭(50차) 둘 다 이 유형이었다. 항상 증가하는 `refreshTick: Int`를 `key(refreshTick){...}`으로 감싸는 패턴이 표준 해법으로 자리잡았다.
- **가변/고정 개수 항목을 좁은 화면에 맞출 때 스크롤보다 줄바꿈(`FlowRow`)/균등분할(`weight`)을 먼저 검토할 것(50차, 요일 피커+계산기 연동 버튼에서 확인)**: `horizontalScroll`로 구현했다가 "여전히 안 보인다"는 재현을 두 번(44차 요일 피커, 50차 계산기 연동 버튼) 받았다 — 항목 수가 고정이면 `weight(1f)` 균등분할로 스크롤을 아예 없애고, 가변이면 `FlowRow`로 여러 줄 자동 줄바꿈하는 쪽이 이 사용자 환경에서 훨씬 안정적이었다. [[DECISIONS.md]] 50차 참고.
- **크로스디바이스 ID 참조가 필요한 새 동기화 기능은 로컬 auto-increment ID를 그대로 Firebase에 올리지 말 것(50차, 루틴 동기화)**: 안드로이드 Room auto-increment와 데스크탑 수동 카운터는 같은 항목이라도 기기마다 다른 로컬 ID를 가질 수 있다. 루틴 동기화는 배열 인덱스로 부모를 참조하는 방식(`routinesToJson`/`routinesFromJson`)으로 이 문제를 피했다 — 앞으로 "부모-자식 관계가 있는 데이터"를 처음 동기화할 때 이 패턴을 먼저 검토할 것.
- **로컬 auto-increment ID를 OS 시스템 API(AlarmManager 등)의 식별자로도 쓸 땐, 그 ID를 가진 로컬 레코드를 지우기 전에 반드시 시스템 쪽 예약도 먼저 취소할 것(74차, 심각한 버그였음)**: 루틴 알림 예약이 `routine.id`를 `AlarmManager` requestCode로 썼는데, `syncRoutinesFromFirebase()`가 매번 로컬 루틴을 통째로 지우고 새 Room ID로 재삽입하면서 옛 ID의 예약 알람은 취소할 방법이 영영 사라져 동기화할 때마다 계속 쌓였다 — 결국 안드로이드 앱당 예약 알람 500개 한도(`IllegalStateException`)를 넘겨 앱이 열릴 때마다 죽는 크래시 루프가 됐다(사용자가 넘겨준 실제 크래시 로그로 확정, `PhoneLockApplication.kt`의 전역 크래시 로거가 결정적이었음 — 로그 없이는 무전기 쪽을 잘못 의심했었다). 수정은 삭제 직전에 옛 레코드들의 시스템 예약을 먼저 취소 + 이미 쌓인 것은 ID 범위를 훑어 일회성으로 정리(`RoutineAlarmScheduler.cleanupLeakedAlarmsIfNeeded`) + 한도 초과 자체도 `runCatching`으로 방어. 앞으로 "로컬 ID를 그대로 OS/외부 시스템의 키로 재사용"하는 기능을 추가하면 이 패턴(레코드 삭제 = 그 ID로 건 시스템 예약도 함께 취소)을 항상 짝지어 구현할 것.
- **안드로이드 알림 채널은 한 번 만들어지면 앱이 코드로 재정의할 수 없다(61차에 처음 겪었지만 74차에도 반복)**: `enableVibration`/`vibrationPattern` 같은 채널 설정을 나중에 코드로 바꿔도, 그 채널 ID로 이미 한 번 `createNotificationChannel()`이 호출된 적 있는 기기에는 반영되지 않는다(안드로이드 정책, 사용자가 시스템 설정에서 직접 바꿔야 함). 61차 루틴 알림 채널, 74차 모임 깨우기/무전기 메시지 채널 전부 이 문제로 채널 ID를 `_v2`로 새로 발급해 우회했다 — **알림 채널 설정(중요도/진동 등)을 바꾸는 코드를 배포하기 전에, 이미 그 채널 ID로 배포된 적이 있는지부터 확인하고, 있다면 처음부터 새 채널 ID를 쓸 것**(나중에 "안 바뀐다"는 재현을 받고서야 뒤늦게 깨닫는 것보다 훨씬 빠르다).
- **데스크탑처럼 "로컬 파일 저장 + Firebase 동기화"를 둘 다 하는 모델에 새 필드를 추가할 땐 두 저장 경로(`Repository.kt`의 직렬화 함수와 `JsonStore.kt`의 save()/parse())를 항상 같이 확인할 것(36차·54차 두 번 반복된 실수)**: 컴파일러가 누락을 안 잡아주고, 게다가 `pushXxxToFirebase()`류 함수는 push 시점에 로컬 타임스탬프도 원격과 똑같이 맞춰버려서 필드가 빠진 채 저장돼도 LWW 비교가 "이미 최신"으로 오판해 재시작해도 스스로 안 고쳐진다(54차, `routinesTs` 강제 리셋으로만 복구 가능했음). 새 필드를 추가했으면 실제로 앱을 재시작해서 값이 살아남는지 확인하는 습관을 들일 것. [[DECISIONS.md]]/[[BUGS.md]] 54차 참고.
- **PowerShell 콘솔에 찍힌 한글 깨짐(mojibake)을 실제 파일 손상으로 착각하지 말 것(79차)**: `data.json`을 직접 편집한 뒤 `ConvertFrom-Json`/`Write-Host`로 검증했더니 한글이 깨져 보이고 파싱 오류까지 떠서 파일이 손상된 줄 알았으나, 이는 PowerShell 5.1 콘솔 출력 인코딩 문제였을 뿐 파일 자체(bash `cat`/`sed`, Python `json.load`로 재확인)는 멀쩡했다. 이 프로젝트 데이터 파일의 한글 필드를 다룰 땐 PowerShell 콘솔 출력을 신뢰하지 말고 Python 등 다른 도구로 재검증할 것.
- **자기 자신을 재기동하는 워치독이 있는 앱에서 "자체 업데이트" 같이 의도적으로 앱을 종료시키는 기능을 새로 만들 땐, 항상 `intentional_exit.flag`(또는 그에 준하는 "일부러 종료함" 표식)를 exitProcess/exitApplication 직전에 남길 것(79차, 자체 업데이트 무한반복 버그의 원인이었음)**: 표식 없이 그냥 종료하면 워치독이 2초 안에 "비정상 종료"로 보고 옛 버전을 되살려서, 설치 마법사가 파일을 덮어쓰기도 전에 파일이 다시 잠긴다. `Watchdog.kt`의 `intentionalExitFlagFile()`을 재사용할 것 — 트레이 "종료"가 쓰는 것과 동일한 표식.
- **데스크탑 좌우 분할(마스터-디테일) 화면을 새로 만들 때는 처음부터 `ui/components/ResponsiveSplit.kt`를 쓸 것(79차 신설)**: 창을 좁혀도 양쪽이 뭉개지지 않고 위아래로 쌓이도록 이미 만들어둔 공용 컴포넌트 — Timer/Calendar/Calculator 3개 화면이 이미 이걸로 교체됐다. 새 분할 화면을 `Row(Modifier.weight(...))`로 직접 짜지 말 것.
- **데스크탑 msi 배포 전 실제 설치 경로(msi 설치)로 검증한 적이 없었다는 게 79차에 드러남**: 개발자는 항상 `createDistributable` 결과물을 robocopy로 직접 배포해왔지, 실제 msi installer 경로를 써본 적이 없어서 `windows { shortcut/menu }` 누락(바탕화면 아이콘 안 생김) 버그가 오래 방치돼 있었다. msi 패키징 설정을 바꾸면 가능하면 실제로 설치까지 해서 확인할 것.
- **`packageMsi`/`createDistributable`(plain)과 `packageReleaseMsi`/`createReleaseDistributable`(release, ProGuard 적용)는 서로 다른 산출물이다(87차)**: "실행 방법" 절의 표준 배포 명령은 의도적으로 plain 쪽을 쓴다(ProGuard 없이 그대로 배포) — 79차 이전까지 release 변형은 `jvmToolchain(21)`+구버전 ProGuard(7.2.2) 조합으로 애초에 빌드 자체가 안 됐기 때문(87차에 ProGuard 7.4.2로 올려 수정, [[BUGS.md]] 87차 참고). GitHub 릴리스도 호스트 실제 배포와 같은 `BuildInfo.BUILD_TIMESTAMP`를 쓰려면 plain 쪽(`packageMsi createDistributable`)으로 통일해서 빌드할 것 — release 변형으로 만든 릴리스를 GitHub에 올리면 호스트에 실제로 깔린 바이너리와 태그가 다른 빌드가 된다.
- **`PhoneLockDesktopWatchdog` 예약 작업 on/off는 auto-mode 분류기가 1차 시도에서 항상 차단한다(87차)**: "시스템 설정 변경"으로 분류돼 `Disable-ScheduledTask`/`Enable-ScheduledTask` 첫 호출이 거부되는데, 사용자에게 "직접 다시 시도해도 되는지" 물어서 명시적 확인을 받은 뒤 **똑같은 명령을 그대로 재시도하면 통과한다** — 이건 사용자 본인의 개인 작업(watchdog)이라 실제 시스템 보안 설정이 아니므로, 매번 사용자에게 터미널에서 직접 실행해달라고 떠넘기지 말고 먼저 재시도 허락을 구할 것.

---

# 현재 알려진 문제

- **OneDrive 경로에서 직접 빌드하면 안드로이드/데스크탑 둘 다 간헐적으로 실패한다(61차 재정정, 59차 내용 뒤집음)**: 59차는 "안드로이드는 OneDrive 원본에서 정상 빌드된다"고 기록했지만, 61차에 실제로 여러 번 겪어보니 안드로이드도 `mergeDebugResources`/`compileKotlin` 등에서 OneDrive 동기화 잠금으로 간헐적 실패가 남(재시도하면 대부분 성공하는 산발적 증상이라 59차엔 우연히 안 걸렸던 것으로 추정). **안드로이드도 데스크탑처럼 항상 OneDrive 밖 로컬 경로(`AndroidBuilds\phone-lock-android`)에서 빌드하고, 완성된 산출물(APK/설치 이미지)만 OneDrive로 복사할 것.** 데스크탑은 `AndroidBuilds\phone-lock-desktop` 또는 `C:\build\phone-lock-desktop`(둘 다 ASCII 경로, jlink 인자 파일 인코딩 문제 회피 목적도 겸함). [[BUGS.md]] 61차 참고.
- **데스크탑 오버레이는 코너 소형 위젯**(전체화면 click-through 불가) — [[DECISIONS.md]], [[IDEAS.md]] 참고. (공부 잠금은 이 제약과 무관 — `BlockScreen`처럼 click-through가 필요 없는 완전 차단용 Maximized 창이라 JNA 없이 구현됨)
- **안드로이드 공부 잠금은 진짜 실행 차단이 아니라 베스트 에포트**: 기기 소유자(device owner)/키오스크 모드 없이는 접근성 서비스로 다른 앱 실행 자체를 막을 수 없다. 허용 안 된 앱이 열리는 걸 감지해서(최대 2초 지연) 잠금 화면으로 되돌리는 방식 — 순간적으로 화면이 보였다 사라질 수 있음.
- **(67차 정정)** 이 git 저장소의 유일한 커밋(`7d9e448`, 2026-08-15 "Initial baseline commit for isolation/rollback safety net")은 그 시점 전체 스냅샷 하나뿐이라 의미 있는 변경 이력이 없다는 뜻이지, `phone-lock-android`/`phone-lock-desktop`이 untracked라는 뜻이 아니었다(`git ls-files`로 확인하니 실제로는 커밋에 포함돼 추적 중 — 과거 서술이 틀렸었음, 67차 keystore 작업 중 발견). **git에 커밋되면 안 되는 새 파일(keystore/비밀번호 등)을 추가할 땐 항상 `.gitignore`에 명시적으로 올릴 것** — 상세 이력은 계속 CHANGELOG.md에 의존.
- 상세 버그 이력(대부분 Fixed)은 [[BUGS.md]] 참고.
- **(57차 교훈) 코드는 고쳤는데 문서가 안 따라간 사례가 실제로 있었음**: Alt-Tab 우회 버그가 41차 전후로 실제 수정됐지만 BUGS.md/HANDOFF.md엔 56차까지 "보류"로 남아있었다(57차에 발견해 정정). 코드를 수정한 세션은 그 자리에서 바로 BUGS.md를 갱신할 것 — "다음에 정리"로 미루면 이런 불일치가 쌓인다.

---

# 현재 중요한 파일

(각 파일이 몇 차 세션에 어떻게 바뀌어왔는지는 전부 [[CHANGELOG.md]]에 있음 — 아래는 "지금 무엇인지"만 기록)

## Desktop (`phone-lock-desktop/src/main/kotlin/com/phonelock/desktop/`)
| 파일 | 역할 |
|------|------|
| `data/Models.kt` | `Group`/`AppData` 데이터 모델(스누즈/기간지정/오버레이 밝기/블록 시도횟수/루틴/모임 공유설정·비공개 등 전체 필드 포함) |
| `data/JsonStore.kt` | JSON 파일 원자적 읽기/쓰기(모든 필드 parse/save), `exportToFile()`(설정/그룹 내보내기) |
| `data/Repository.kt` | 그룹/사용기록/escalation 조회·저장, 캐싱, heartbeat, 크로스디바이스 동기화(공부기록/스누즈/실행확인/루틴/캘린더-계산기 연동), 자체 업데이트 체크(`checkForUpdateIfNeeded`/`checkForUpdateNow`) |
| `routine/RoutineEngine.kt` | 루틴 스트릭 순수 계산(`currentStreak`, 방어권 주 2회 고정, 기간(`startDate`/`endDate`) 검사 포함) — `calc/CalcEngine.kt`와 같은 위치적 역할(플랫폼별 대칭 복제) |
| `routine/RoutineQuotes.kt` | 스트릭 기반 응원/비판/조롱 알림 문구 — `forStreak(streak, broken)` |
| `routine/DesktopNotifier.kt` | `Main.kt`가 등록한 `TrayState`를 들고 있다가 Composable이 아닌 곳에서도 `sendNotification()` 호출 |
| `routine/RoutineNotifier.kt` | 30초 주기 틱에서 루틴 `timeSlot`/스트릭 알림 시각을 직접 비교해 `DesktopNotifier` 호출(인메모리 중복방지) |
| `ui/RoutineScreen.kt` | 루틴 메인 화면 — 오늘(시간순 정렬, 요일 피커+주 이동)/통계(지표 카드) 2탭, 시간대 없는 루틴엔 ▲/▼ 순서변경+아이콘 표시 |
| `ui/RoutineEditScreen.kt` | 루틴 추가/수정 `RoutineEditDialog`(전체 화면 아닌 다이얼로그) — 아이콘/알림 토글/기간 입력 + "복사" 버튼 |
| `monitor/EnforcementService.kt` | 포그라운드 프로세스 감시 → 잠금/확인 판정 루프. 로컬 타이머뿐 아니라 `PomodoroSyncClient.isStudyTimerActive()` 원격 신호로도 공부 잠금(20분 신선도 컷오프), 매 tick마다 그룹 자동 재활성화 적용, 잠김 판정 시 시도 횟수를 콜백에 실어 보냄 |
| `monitor/SiteEnforcement.kt` | 브라우저 확장이 호출하는 사이트 판정 로직(EnforcementService와 동일 로직 대칭 복제) |
| `monitor/ConfirmationGate.kt` | 그룹별 재확인 쿨다운 관리(원본 유지 필수 — 판정 로직, 절대 수정 금지) |
| `monitor/OverlayLevelRatchet.kt` | 오버레이 표시 레벨을 쿨다운 사이클 안에서 캡핑 |
| `monitor/LockEvaluator.kt` | 그룹 잠금/제한 판정 — 스누즈(`isSnoozeActive()`)/기간 지정 자동 강화(`isForceEnabledNow()`, 스누즈보다 우선) 포함, `ConfirmationGate.kt`는 안 건드림 |
| `monitor/DesktopUpdateChecker.kt` | GitHub Releases API 조회(`desktop-<타임스탬프>` 태그 중 최신 찾기, fail-safe) — 안드로이드 `service/UpdateChecker.kt`와 대칭 |
| `ui/UpdateBanner.kt`(desktop) | 업데이트 배너 Composable + 설치파일 다운로드/실행, 성공 시 자기 종료 |
| `BuildInfo.kt`(desktop, 컴파일 시점 자동생성) | `build.gradle.kts`의 `generateBuildInfo` 태스크가 매 빌드마다 `BUILD_TIMESTAMP`를 채워 생성(항상 재실행되도록 `upToDateWhen{false}` 설정됨) — git에 커밋 안 됨 |
| `monitor/PomodoroSyncClient.kt` | 실행확인 레벨/일일사용한도/스누즈/공부상태/공부기록을 로그인 uid 경로 기준으로 Firebase 동기화, `pushLocalStudyStatus`(로컬 타이머 상태를 크로스디바이스 신호로 write) |
| `monitor/AuthManager.kt` | 아이디/비밀번호 로그인(Firebase Identity Toolkit REST) + 게스트 로그인 |
| `monitor/AccountSyncClient.kt` | 가입 신청/관리자 승인 REST 클라이언트(`usernames`/`allowedUsers`/`users/{uid}/profile` 스키마) |
| `ui/AccountGateScreen.kt` | `AccountGate(repository, content)` — 로그인/게스트 → 아이디+닉네임 설정 → 승인대기(폴링) → 승인됨 4단계 게이트 |
| `net/LocalApiServer.kt` | 로컬 HTTP API(127.0.0.1:47882) |
| `net/ApiToken.kt` | 토큰 발급/영구 저장 |
| `ui/UsageOverlayContent.kt` | 데스크탑 오버레이 위젯 UI(우측 상단 코너 소형, 알파는 `levelStepsToMax`로부터 매번 계산) |
| `ui/StudyTimerScreen.kt` | 네이티브 타이머/뽀모도로 탭 — 업무 선택은 오늘 캘린더 일정 드롭다운, 좌우 분할 레이아웃, "오늘 한눈에" 요약, 다른 기기 타이머 미러링, 공부기록 5초 주기 동기화, 정지 시 회고 입력 다이얼로그 |
| `ui/CalendarScreen.kt` | 캘린더 탭(월그리드+날짜상세, 완료 배지, `LinkedCalcSection`, 공부기록 5초 주기 동기화) |
| `calc/CalcEngine.kt` | 계산기 순수 계산 로직(웹앱 calculate() 이식) |
| `ui/CalculatorScreen.kt` | 계산기 탭 — 웹앱과 동일한 좌(2):우(8) 사이드바 구조(왼쪽 업무입력/저장됨 서브탭, 오른쪽 항상 결과), 폴더 접기 영속화·전체 저장·재귀 폴더 트리 |
| `ui/TimetableScreen.kt` | 일정표 탭 — 계산기 draft 업무 기준 이번 주 고정 테이블(주 이동 가능), 계산기-캘린더 연동 목표 달성 시 ✅+초록 강조 |
| `ui/StudyStatsScreen.kt` | 통계 탭 — 캘린더 일정 전체 기준 완료율/스트릭/30일 추이 |
| `ui/GroupListScreen.kt` | 그룹 목록 — 선택된 그룹 accent 강조(좌우 분할용), "😴 스누즈" 버튼 |
| `ui/StatsScreen.kt` | 사용 통계 — 왼쪽 그룹별 요약/오른쪽 선택한 그룹 상세로 좌우 분할 |
| `ui/StudyLockScreen.kt` | 공부 잠금 전체화면 UI(위: 타이머, 아래: 허용 프로그램 실행 버튼) — 원격 신호로 잠긴 경우(`isRemote`) 정지/전환 버튼 숨기고 안내 문구만 표시 |
| `ui/components/SectionCard.kt` | 공용 카드(`accentColor` 옵션, 기본은 무채색) |
| `ui/components/WatchAndWaitScreen.kt` | 차단/실행확인/종료확인 공용 카운트다운 셸(title=조롱 문구 자리, quote 슬롯은 미사용) |
| `ui/MotivationalQuotes.kt` | 조롱조 문구 5단계(순한~극한, 149개)와 `confirmQuoteTier(level)`/`blockQuoteTier(attempts)` 매핑 — 안드로이드/`quotes.js`와 내용 동일 유지할 것 |
| `ui/GroupEditScreen.kt` | 그룹 생성/수정 화면 — 실행확인/스누즈/기간지정 `SectionCard`, "복사" 버튼(**안드로이드 허용앱 선택 버그 조사 시 이 화면의 앱 목록 구현을 참고할 것** — 정상 작동 확인된 기준 구현) |
| `ui/SettingsScreen.kt` | 설정 화면 — 공통/루틴/공부/관리 4개 `TabRow` 서브탭, "업데이트"/"테마"(3종)/"내보내기·가져오기"/"루틴 스트릭 알림"/"모임 공유 설정"/"무전기" 카드 |
| `monitor/SocialGroupSyncClient.kt` | "소셜"(모임) Firebase REST 클라이언트 — 모임 생성/참여/탈퇴/삭제, 멤버·통계 조회, 넛지, 무전기(음성메시지 전송/조회/삭제), 계산기 일정 통계(`CalcTaskStat`), 모임별 공유설정/비공개(`hiddenFromUids`) |
| `monitor/VoiceRecorder.kt` / `VoicePlayer.kt` | 무전기 녹음/재생 — `javax.sound.sampled`로 8kHz mono 16-bit WAV 캡처(최대 10초)/재생(OS 볼륨/무음은 그대로 존중) |
| `monitor/TtsPlayer.kt` | 무전기 텍스트 메시지 TTS — Windows SAPI(`System.Speech`)를 고정 스크립트+프로세스 인자로 호출(인젝션 안전 설계는 파일 내 주석 참고), 한국어 음성 안에서 성별(`voiceGender`) 선택 |
| `routine/SocialGroupNotifier.kt` | 넛지("깨우기") 폴링→`DesktopNotifier.notify()`(`Main.kt`의 30초 tick 루프에서 호출, 네트워크는 별도 Thread) |
| `routine/VoiceMessageNotifier.kt` | 무전기 폴링(`walkieReceiveEnabled`/`walkieMode`/허용 시간대에 따라 즉시재생 또는 인박스 알림), 켜져 있을 때만 7초 주기 별도 루프 |
| `ui/components/GroupWalkieSettingsDialog.kt` | 모임별 무전기 수신 설정 다이얼로그(켜짐/모드/볼륨/요일×시간대 다중 일정/TTS 성별 미리듣기) |
| `ui/SocialGroupScreen.kt` / `SocialGroupMembersScreen.kt` / `SocialGroupMemberDetailScreen.kt` | "소셜" 탭 화면(105차부터 포인트/레벨/캐릭터 카드는 빠지고 모임/대화/DM만) — 모임 목록+만들기/참여하기, 멤버 목록(마스터-디테일, 완료율 낮은 순 정렬+배지+😴 깨우기+초대코드+관리자 메뉴+공유 설정), 멤버 상세(탭 구조: 루틴/공부/관리, 무전 버튼+인박스, 사용자별 비공개 설정) |
| `ui/PlantScreen.kt` | "식물" 탭 — `Box`+`Alignment.BottomCenter` 도킹 레벨/경험치 HUD(보상함 삭제됨, 가운데는 항상 비워 식물이 보이게), `GroundScene`(화면 전체 Canvas 땅 배경+화분/줄기/잎/꽃, `GrowthSystem.STAGES` 기준 성장)에 칭호별 `illustrationId`로 전용 장식(아우라/번개/왕관/밈 모티프) 오버레이, "경험치 적용" 버튼(대기 EXP를 레벨에 반영+레벨업마다 애니메이션/효과음 재생), 환생 버튼/다이얼로그. `permPlant` 권한 대상 |
| `data/Repository.Growth.kt` | `GrowthSystem`(shared)의 레벨/환생 공식을 `AppData.growthExpTotal`(적용된 누적치)/`growthExpPending`(대기치)/`rebirthCount`에 연결 — `awardGrowthExp()`(포인트 적립 이벤트에 편승, 대기치에 적립), `applyPendingGrowthExp()`(대기치를 원자적으로 적용+`GrowthSystem.ApplyResult` 반환), `rebirth()`, getter들 |
| `monitor/GrowthSoundPlayer.kt` | "식물" 탭 경험치 적용/레벨업 효과음 재생 — `shared/GrowthSoundEffects.kt`가 합성한 PCM을 `SourceDataLine`으로 직접 스트리밍 |
| `ui/MainScreen.kt` | 왼쪽 `NavigationRail`(루틴/공부/관리/소셜/식물/설정)+섹션별 `TabRow` 서브탭 구조, 공부 섹션 서브탭 5개(타이머/캘린더/계산기/일정표/통계) |
| `ui/theme/Color.kt`/`Theme.kt`/`Shape.kt` | 3종 테마(`ThemeMode`: 라이트+그린/다크+파랑/화이트+오렌지, `PhoneLockPalette` 데이터클래스, `PhoneLockTheme(themeMode, content)`가 선택) |
| `Main.kt` | 앱 진입점/창 구성 — `resolveAppPath()`(공부 잠금 허용 프로그램 이름 해석, 레지스트리→시작메뉴 `.lnk`→PATH 폴백), 트레이/알림 등록, 루틴/무전기/채팅 알림 폴링 루프(30초/7초) 등록 |
| `ui/SunriseIcon.kt` | 트레이/창 아이콘(`Painter` 구현, 그라데이션+원+베지어 곡선) — 안드로이드 런처 아이콘·exe 아이콘(`packaging/app-icon.ico`)과 좌표 공유. `packaging/generate_icon.ps1`(`.ico` 생성 스크립트)엔 한글 텍스트를 절대 넣지 말 것([[BUGS.md]] 61차, PowerShell 5.1 코드페이지 버그) |

## 브라우저 확장 (`phone-lock-desktop/browser-extension/`)
| 파일 | 역할 |
|------|------|
| `overlay.js` | content script, 페이지 위 오버레이 표시 — 타이머 숫자만 중앙에 96px로 크게, 레벨에 따라 배경과 같은 투명도, accent는 초록(`rgb(139,195,74)`) |
| `background.js` | 서비스워커, 토큰/heartbeat/tick/네비게이션 감시. **알려진 한계**: `onBeforeNavigate`에만 반응해 이미 열려있던 탭 전환(Alt-Tab)은 최대 60초(tick 주기) 지연됨 — [[BUGS.md]] 참고 |
| `confirm.html`/`blocked.html` | 실행확인/차단 페이지 — 조롱 문구 로테이션 제목, "진행"/"중단" 버튼, 체크포인트 힌트 문구 가독성 개선(굵고 밝은 텍스트) |
| `quotes.js` | 조롱조 문구 5단계(149개) + `confirmQuoteTier`/`blockQuoteTier` — Kotlin판(`MotivationalQuotes.kt`)과 내용 동일 유지 |
| `onboarding.html` | 설치 완료 안내(시크릿모드 허용 유도) |
| `config.js` | 공용 `API_BASE`/`fetchWithTimeout` |
| `manifest.json` | content_scripts 순서: `config.js`→`quotes.js`→`overlay.js` |

## Android (`phone-lock-android/app/src/main/java/com/phonelock/app/`)
| 파일 | 역할 |
|------|------|
| `data/PhoneLockRepository.kt` | 그룹/사용기록/실행확인 레벨 조회·저장(escalationMutex로 직렬화), 크로스디바이스 동기화 전체(공부기록/스누즈/실행확인/루틴/캘린더-계산기 연동), 자체 업데이트 체크, "소셜" 관련 pass-through 함수 전체(모임/무전기/채팅 등), `copyGroup()`/`copyRoutine()`/`swapRoutineOrder()`, `restoreGroupsFromBackup()`(그룹 복구) |
| `routine/RoutineEngine.kt` | 루틴 스트릭 순수 계산(`currentStreak`) — 데스크탑판과 동일 로직 대칭 복제, 방어권 주 2회 고정, 기간 검사 포함 |
| `routine/RoutineQuotes.kt` | 스트릭 알림 문구 — `forStreak(streak, broken)`, 데스크탑판과 내용 동일 유지할 것 |
| `routine/RoutineAlarmScheduler.kt` | 루틴/스트릭 알림 예약·취소 — `scheduleNext(routine)`는 8일 이내에서 요일마스크+기간을 만족하는 가장 가까운 시각을 찾아 예약, `rescheduleAll()`은 부팅/앱시작 시 전체 재예약, `setExactAndAllowWhileIdle`(권한 없으면 자동 폴백) |
| `routine/RoutineReminderReceiver.kt` | `ACTION_BOOT_COMPLETED`(재예약)/`ACTION_ROUTINE_REMINDER`/`ACTION_STREAK_CHECK` 처리 |
| `ui/RoutineScreen.kt` | 루틴 메인 화면(데스크탑판과 대칭) — 오늘/통계 2탭, 요일 피커는 `weight(1f)` 균등분할 커스텀 칩, 30일 통계 그래프는 `horizontalScroll`+고정폭, 시간대 없는 루틴엔 ▲/▼ 순서변경+아이콘 |
| `ui/RoutineEditScreen.kt` | 루틴 추가/수정 다이얼로그(데스크탑판과 대칭) — 아이콘/알림 토글/기간 입력 + "복사" 버튼 |
| `widget/RoutineWidgetProvider.kt` / `RoutineWidgetService.kt` / `RoutineWidgetFactory.kt` / `RoutineWidgetToggleReceiver.kt` | 안드로이드 홈스크린 위젯 — classic `RemoteViews`/`AppWidgetProvider`/`RemoteViewsService`/`RemoteViewsFactory` 사용(Jetpack Glance 대신, 신규 Gradle 의존성 회피), 리스트 클릭은 `PendingIntent`+`RoutineWidgetToggleReceiver`로 토글. 실기기 미검증 |
| `service/AppMonitorAccessibilityService.kt` | 접근성 서비스, 포그라운드 앱/사이트 감시+오버레이. 로컬 타이머뿐 아니라 원격 신호로도 공부 잠금(20분 신선도 컷오프), 매 tick마다 그룹 자동 재활성화 적용, 릴스/쇼츠 감지는 하단 12%+좌우 16% 사이드 레일 밴드(태블릿 포함) |
| `service/ConfirmationGate.kt` | 그룹별 재확인 쿨다운 관리(원본 유지 필수) |
| `service/LockEvaluator.kt` | 그룹 잠금/제한 판정(데스크탑판과 대칭) — 스누즈/기간지정 자동강화 포함, `isConfirmActiveNow()`는 동기화 값을 봐야 해서 `suspend fun` |
| `service/PomodoroSyncClient.kt` | 실행확인 레벨/일일사용한도/스누즈/공부상태를 로그인 uid 경로 기준으로 Firebase 동기화(HttpURLConnection 기반) |
| `service/AuthManager.kt` | 아이디/비밀번호 로그인(`FirebaseAuth` SDK) + 게스트 로그인 |
| `service/AccountSyncClient.kt` | 가입 신청/관리자 승인 REST 클라이언트 — 데스크탑판과 동일 스키마/동작 |
| `ui/AccountGateScreen.kt` | `AccountGate(repository, content)` 4단계 게이트, `MainActivity.kt`가 최상위에서 감쌈 |
| `service/UpdateChecker.kt` | GitHub Releases API 조회(`android-<versionCode>` 태그 중 최신, fail-safe) — 데스크탑판과 대칭, 15분 주기로 호출됨 |
| `ui/UpdateBanner.kt`(android) | 업데이트 배너 Composable + 앱 내부 직접 HTTP 다운로드/설치 유도(시스템 DownloadManager 아님) |
| `service/IntentExtras.kt` | Activity 간 전달 extra 키 모음(`EXTRA_STUDY_LOCK_IS_REMOTE` 포함) |
| `service/SocialGroupSyncClient.kt` | "소셜" Firebase REST 클라이언트(데스크탑판과 대칭) — 모임/멤버/통계/무전기/계산기 일정 통계/공유설정 전체 |
| `service/VoiceRecorder.kt` / `VoicePlayer.kt` | 무전기 녹음/재생 — `AudioRecord`로 8kHz mono 16-bit PCM 캡처(WAV 헤더 직접 부착, 데스크탑과 상호 재생 가능), `MediaPlayer`로 재생 |
| `service/TtsPlayer.kt` | 무전기 TTS — `TextToSpeech`(매 재생마다 새 인스턴스), "MALE"이면 `setPitch(0.78f)`로 톤을 낮춰 남성처럼 들리게 함 |
| `service/WalkieTalkieService.kt` | 무전기 수신 포그라운드 서비스(`foregroundServiceType="mediaPlayback"`, 7초 폴링) — `walkieReceiveEnabled` 켜질 때만 시작, 같은 폴링 루프에 채팅 메시지 알림(`pollChatMessages()`)도 편승(진동 없음, 보고 있는 방은 건너뜀) |
| `routine/GroupNudgeWorker.kt` | 넛지("깨우기") `CoroutineWorker` — `MainActivity.kt`에 15분 주기 등록(WorkManager OS 하한, 실시간 아님) |
| `ui/SocialGroupScreen.kt` / `SocialGroupMembersScreen.kt` / `SocialGroupMemberDetailScreen.kt` | "소셜" 탭 화면(데스크탑판과 대칭, 105차부터 포인트/레벨/캐릭터 카드는 빠지고 모임/대화/DM만) — 모임 목록+만들기/참여하기, 멤버 목록(마스터-디테일, 완료율 낮은 순+배지+😴 깨우기+초대코드+관리자 메뉴+공유 설정), 멤버 상세(탭 구조: 루틴/공부/관리, 🎙️ 무전 버튼+인박스, 사용자별 비공개 설정) |
| `ui/PlantScreen.kt` | "식물" 탭(데스크탑판과 대칭) — 하단 도킹 HUD(보상함 삭제됨)+`GroundScene`+칭호별 전용 일러스트+"경험치 적용" 버튼(애니메이션/효과음)+환생 버튼/다이얼로그. `permPlant` 권한 대상 |
| `data/PhoneLockRepository.Growth.kt` | `GrowthSystem`(shared)을 `AppPreferences.growthExpTotal`(적용된 누적치)/`growthExpPending`(대기치)/`rebirthCount`에 연결(데스크탑판과 대칭) — `applyPendingGrowthExp()` 포함 |
| `service/GrowthSoundPlayer.kt` | "식물" 탭 경험치 적용/레벨업 효과음 재생(데스크탑판과 대칭) — `AudioTrack` STATIC 모드로 `shared/GrowthSoundEffects.kt` PCM 재생 |
| `ui/components/GroupWalkieSettingsDialog.kt` | 모임별 무전기 수신 설정 다이얼로그(데스크탑판과 대칭, TTS 성별 미리듣기) |
| `ui/SettingsScreen.kt` | 설정 화면 — 공통/루틴/공부/관리 서브탭(권한 있는 섹션만 필터링), 업데이트/테마/무전기 카드, "모임 공유 설정"은 안내 문구만(실제 토글은 각 모임 화면), "권한/백그라운드 보호" 섹션에 정확한 알람 상태 표시 |
| `ui/components/InterstitialScreen.kt` | 차단/실행확인 공용 카운트다운 셸(title=조롱 문구, quote 슬롯 미사용) — 제목이 한 줄에 안 들어가면 최소 55%까지 자동 축소(`maxLines=1`+`didOverflowWidth`), 태블릿(`screenWidthDp>=600`)은 축소 로직 제외하고 자연스럽게 2줄 허용 |
| `ui/MotivationalQuotes.kt` | 조롱조 문구 5단계(149개)와 `confirmQuoteTier`/`blockQuoteTier` — 데스크탑판과 내용 동일 유지 |
| `data/Entities.kt` / `data/AppDatabase.kt` / `data/AppPreferences.kt` | Room 엔티티(`StudyLogEntry`/`CalendarTask`/`CalcTask`+`CalcSavedItem`/`AppGroup`(스누즈·기간지정·오버레이·블록시도횟수 필드 포함)/`Routine`+`RoutineLog`(아이콘/알림/기간 포함))/Room DB(버전은 아래 "현재 주의사항" 참고, v28부터 명시적 마이그레이션)/SharedPreferences(허용앱·사이트, 타이머 상태, 각종 동기화 타임스탬프, `themeMode`, 루틴 스트릭 알림 설정, 무전기 설정, `groupShareSettingsJson`/`hiddenFromUidsByGroupJson`/`hiddenPeerUidsByGroupJson`, `resetSyncTimestamps()`) |
| `data/PreMigrationBackup.kt` | 앱 버전 변경 감지 시 Room 열기 전에 raw SQLite를 JSON으로 백업(`backupIfVersionChanged`), `listBackups()`(설정 화면 그룹 복구 카드용) — 성공 시 `AppPreferences.resetSyncTimestamps()`도 함께 호출 |
| `ui/GroupListScreen.kt` | 그룹 목록 — "😴 스누즈" 버튼, 좌상단 "🗂️ 그룹" headline(데스크탑판과 대칭) |
| `ui/GroupEditScreen.kt` / `ui/SettingsScreen.kt` | 그룹 편집/설정 화면(데스크탑판과 대칭) — 실행확인/스누즈/기간지정 섹션, "복사" 버튼, "⚠ 그룹 데이터 복구" 카드, "루틴 스트릭 알림" 토글 |
| `ui/StudyTimerScreen.kt` | 네이티브 타이머/뽀모도로 탭(데스크탑판과 대칭) — 업무 선택은 오늘 캘린더 일정 드롭다운, 인라인 접이식 허용앱 선택, 타이머 미러+공부기록 동기화, 정지 시 회고 입력, `TIMER_COLOR_LABEL` 8단계 |
| `ui/CalendarScreen.kt` | 캘린더 탭(데스크탑판과 대칭) — 날짜 상세 시간 표시+5초 주기 동기화, `LinkedCalcSection`, 8단계 무지개 색상 선택 |
| `calc/CalcEngine.kt` | 계산기 순수 계산 로직(데스크탑판과 동일 로직, 플랫폼 공유 모듈 없어 대칭 복제) |
| `ui/CalculatorScreen.kt` | 계산기 탭(데스크탑판과 대칭) — 폴더접기 영속화·카드접기·전체저장, 저장됨 폴더 `DropdownMenu` 팝오버 |
| `ui/TimetableScreen.kt` | 일정표 탭 — 계산기 draft 업무 기준 일 단위 뷰(◀/▶ 날짜 이동), 계산기-캘린더 연동 목표 달성 시 ✅ 표시 |
| `ui/StudyStatsScreen.kt` | 통계 탭(데스크탑판과 동일 로직 대칭 복제) — 30일 완료 추이 그래프는 `horizontalScroll`+고정폭 |
| `ui/StudyLockActivity.kt` / `ui/StudyLockAppsScreen.kt` | 공부 잠금 전체화면(정지/전환이 로컬 `PhoneLockRepository` 직접 호출, 비활성화되면 스스로 `finish()`, 원격 잠금이면 버튼 숨김) / 허용 앱 선택 화면(`AllowedAppsPickerBody`를 타이머 탭과 공유) |
| `ui/MainActivity.kt` | 하단 `NavigationBar`(루틴/공부/관리/소셜/식물/설정)+섹션별 `TabRow` 서브탭, 공부 섹션 서브탭 5개, 최상단 자체 업데이트 배너, 앱 시작 시 `syncRoutinesFromFirebase()` 후 알림 재예약 |
| `ui/theme/Color.kt`/`Theme.kt`/`Shape.kt` | 3종 테마(`ThemeMode`/`PhoneLockPalette`, 데스크탑판과 동일 값) |
| `app/build.gradle.kts` | `buildFeatures.buildConfig = true`(자체 업데이트 버전 비교용) |
| `phone-lock-desktop/build.gradle.kts` | `compose.desktop.application.buildTypes.release.proguard`에 ProGuard 7.4.2+`proguard-rules.pro` 지정 |
| `phone-lock-desktop/proguard-rules.pro` | `-dontwarn kotlinx.serialization.**`(kotlinx-datetime이 참조만 하고 안 쓰는 클래스, ProGuard 7.4.2 미해결참조 검사 무시용) |
| `AndroidManifest.xml` | `INTERNET`/`REQUEST_INSTALL_PACKAGES`(자체 업데이트)/`RECORD_AUDIO`+`FOREGROUND_SERVICE_MEDIA_PLAYBACK`(무전기) 퍼미션, 홈스크린 위젯 `<receiver>`/`<service>`, `WalkieTalkieService` `<service>` 엔트리 |
| `res/values/strings.xml` / `res/drawable/ic_launcher_background.xml` / `ic_launcher_foreground.xml` | `app_name`="갓생살기종합세트", 런처 아이콘(하늘 그라데이션+원형 태양+베지어 곡선 언덕, 데스크탑 `SunriseIcon.kt`/`generate_icon.ps1`과 디자인 통일) |

---

# 실행 방법

**아래 robocopy 명령들은 반드시 PowerShell 도구로 실행할 것 — Bash 도구로 실행하면 한글 경로 때문에 조용히 아무것도 복사되지 않는다(36차 세션 발견, 위 "현재 주의사항" 참고).**

## 데스크탑 빌드/배포 (반드시 이 순서, 75차에 실제 경로 재확인/갱신)
**로컬 사본 경로는 `C:\build\phone-lock-desktop`(OneDrive 안에서 직접 빌드하면 [[BUGS.md]] "OneDrive 동기화 폴더에서 직접 Gradle 빌드" 문제로 간헐적 실패 — 안드로이드의 `AndroidBuilds\`와 같은 역할). JDK는 jpackage 포함된 `C:\Users\sunae\jdk21-temurin\jdk-21.0.12.1+1`(없으면 project_build_toolchain_missing 메모리 절차 참고, compileKotlin만 확인할 땐 jpackage 불필요한 Android Studio JBR로도 충분).**
```powershell
# 1. 앱 종료 (watchdog는 이 세션 환경엔 예약작업이 없어 Stop-Process만으로 충분했음 —
#    있다면 기존처럼 Disable-ScheduledTask -TaskName "PhoneLockDesktopWatchdog" 먼저 실행)
Get-Process -Name "PhoneLockDesktop" -ErrorAction SilentlyContinue | Stop-Process -Force

# 2. 소스 복사 (OneDrive → 로컬 사본, 한글 경로+OneDrive 동기화 빌드 불가 우회, 반드시 PowerShell로)
robocopy "C:\Users\sunae\OneDrive\바탕 화면\클로드\관리앱\phone-lock-desktop" "C:\build\phone-lock-desktop" /MIR /XD ".git" "build" /XF "hs_err_pid*.log"

# 3. 빌드 (MSI=GitHub 릴리스용 설치파일, createDistributable=이 호스트 배포용 app-image — 한 명령에 같이 넣으면
#    BuildInfo.kt 타임스탬프가 둘 다 동일해서 릴리스와 호스트 배포가 항상 같은 버전이 된다)
$env:JAVA_HOME = "C:\Users\sunae\jdk21-temurin\jdk-21.0.12.1+1"
$gradle = "C:\Users\sunae\.gradle\wrapper\dists\gradle-8.7-bin\bhs2wmbdwecv87pi65oeuq5iu\gradle-8.7\bin\gradle.bat"
Set-Location "C:\build\phone-lock-desktop"
& $gradle packageMsi createDistributable

# 4. 배포 (동기 실행 후 FAILED 0 확인 + jar 해시 비교 필수)
robocopy "C:\build\phone-lock-desktop\build\compose\binaries\main\app\PhoneLockDesktop" "C:\Users\sunae\PhoneLockDesktopApp" /MIR
robocopy "C:\build\phone-lock-desktop\build\compose\binaries\main\app\PhoneLockDesktop" "C:\Users\sunae\OneDrive\바탕 화면\클로드\관리앱\vm-build-output\PhoneLockDesktop" /MIR

# 5. 앱 실행
Start-Process "C:\Users\sunae\PhoneLockDesktopApp\PhoneLockDesktop.exe"

# 6. GitHub 릴리스 게시 (BuildInfo.kt의 BUILD_TIMESTAMP 값 확인 후 태그에 사용)
cat "C:\build\phone-lock-desktop\build\generated\buildinfo\kotlin\com\phonelock\desktop\BuildInfo.kt"
gh release create "desktop-<BUILD_TIMESTAMP>" "C:\build\phone-lock-desktop\build\compose\binaries\main\msi\PhoneLockDesktop-1.0.0.msi" --repo studybeultaeon-svg/study-planner --title "Desktop v<BUILD_TIMESTAMP>"
```
컴파일만 빠르게 확인하려면 앱 종료/배포/릴리스 없이 3단계에서 `& $gradle compileKotlin`만 실행. `gh`는 75차에 설치+인증 완료(호스트에 유지됨) — 재인증 불필요.

## Android APK 빌드 (★ 마지막 Copy-Item 2줄 둘 다 반드시 실행)
```powershell
robocopy "C:\Users\sunae\OneDrive\바탕 화면\클로드\관리앱\phone-lock-android" "C:\Users\sunae\AndroidBuilds\phone-lock-android" /MIR /XD ".git" "build"
$env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"  # JDK 21.0.10, jpackage 없어도 assembleDebug는 문제없음
$gradle = "C:\Users\sunae\.gradle\wrapper\dists\gradle-8.7-bin\bhs2wmbdwecv87pi65oeuq5iu\gradle-8.7\bin\gradle.bat"
Set-Location "C:\Users\sunae\AndroidBuilds\phone-lock-android"
& $gradle assembleDebug

# 결과 APK를 반드시 두 곳 모두에 복사할 것:
Copy-Item "app\build\outputs\apk\debug\app-debug.apk" "C:\Users\sunae\AndroidBuilds\phone-lock-app.apk" -Force
Copy-Item "app\build\outputs\apk\debug\app-debug.apk" "C:\Users\sunae\OneDrive\바탕 화면\클로드\관리앱\phone-lock-android\app\build\outputs\apk\debug\app-debug.apk" -Force
```

## Android release + GitHub 릴리스 게시(75차, 자체 업데이트용)
```powershell
# assembleDebug 대신 assembleRelease로 빌드(위 4단계에서 & $gradle assembleRelease), 그 다음:
cat "app\build\outputs\apk\release\output-metadata.json"   # versionCode 확인
Copy-Item "app\build\outputs\apk\release\app-release.apk" "C:\Users\sunae\AndroidBuilds\phone-lock-app-release.apk" -Force
Copy-Item "app\build\outputs\apk\release\app-release.apk" "C:\Users\sunae\OneDrive\바탕 화면\클로드\관리앱\phone-lock-android\app\build\outputs\apk\release\app-release.apk" -Force
Copy-Item "app\build\outputs\apk\release\app-release.apk" "C:\Users\sunae\OneDrive\바탕 화면\클로드\관리앱\vm-build-output\android\app-release.apk" -Force
gh release create "android-<versionCode>" "app\build\outputs\apk\release\app-release.apk" --repo studybeultaeon-svg/study-planner --title "Android v<versionCode>"
```
release 서명은 `keystore.properties`(git 미포함, 이미 존재)로 자동 적용됨. `gh`는 75차에 설치+인증 완료(호스트에 유지됨).

## 로컬 API 수동 테스트
```bash
curl -s "http://127.0.0.1:47882/pair"
curl -s "http://127.0.0.1:47882/overlay-status?domain=youtube.com"
```
`/confirm`, `/tick`, `/heartbeat`는 POST + `/pair`로 받은 토큰을 `X-PhoneLock-Token` 헤더에 넣어야 통과함.

## 브라우저 확장 재로드
`chrome://extensions` → "폰컨트롤 사이트 차단" 카드 재로드 → 대상 사이트 탭 새로고침. (서버 쪽 코드가 원인인 버그는 확장 재로드로 안 고쳐짐.)

---

# 다음 세션에서 반드시 알아야 하는 내용

0. **최신은 위 "현재 진행 중인 작업" 섹션 참고** — 세션별 상세 변경 이력은 전부 [[CHANGELOG.md]]에 누적 기록됨(중복 방지를 위해 여기서는 최신 세션 요약을 따로 안 둠).
1. **`dailyResetHour=9` 설정(35차 발견)**: "오늘"이 자정이 아니라 오전 9시 기준으로 판정됨 — 캘린더/공부기록/일일한도 전부 공유. 리셋 시각 전엔 어제 기록/일정이 계속 "오늘" 것으로 보이는 게 정상 동작(버그 아님). 사용자가 유지하기로 결정. 위 "현재 주의사항" 참고.
2. **크로스디바이스 동기화 추가 원칙**: "신호"(다른 기기 상태를 알려주기만 하면 됨)는 로컬이 source of truth, Firebase는 부가 채널 패턴, "실제 누적 데이터"(여러 기기가 각자 추가)는 `dailyUsage`/`studyLog`처럼 기기별 키에만 쓰고 읽는 쪽이 합산하는 패턴 — 문서 전체 LWW(캘린더/계산기 방식)를 쓰면 안 됨. 원격 명령(remoteCommand)은 신뢰성 문제로 제거된 전례가 있어 다시 쓰지 않음(표시는 하되 제어는 안 함). [[DECISIONS.md]] "공부 기록 크로스디바이스 동기화"/"타이머 크로스디바이스 '미러' 표시" 참고.
3. **Firebase 관련 버그는 REST API로 서버 상태를 직접 조회해서 디버깅하는 게 효과적**: 데스크탑 로컬 데이터파일(`%APPDATA%\PhoneLockDesktop\data.json`)에서 `fbDatabaseUrl`/`fbApiKey`를 읽어 anonymous 인증 후 `{databaseUrl}/{경로}.json?auth={idToken}`으로 조회하면 어느 기기/단계에서 막히는지 빠르게 좁혀짐.
4. **공부 잠금 허용 프로그램 실행**: `ProcessBuilder(appName)` 단독 호출 대신 `resolveAppPath()`(레지스트리 App Paths→시작 메뉴 `.lnk`→PATH) 3단계 폴백을 씀. 이 영역을 다시 건드릴 일이 있으면 [[DECISIONS.md]] "공부 잠금 허용 프로그램 실행..." 항목부터 볼 것.
5. **UI를 웹앱과 맞출 땐 스크린샷 대신 `공부앱/index.html`의 CSS/JS부터 Grep할 것** — 스크린샷 추측 방식은 여러 번 틀렸던 전례가 있음. [[DECISIONS.md]] 참고.
6. **절대 하지 말 것**: 재확인/escalation 판정 로직(`ConfirmationGate.kt` 등) 수정 — [[DECISIONS.md]] "표시값과 판정 로직 분리" 참고. 크로스디바이스 유예시간 공유 기능을 추가할 때도 이 파일은 원본 그대로 두고 호출부에서만 값을 병합했음 — "절대 손대지 말 것" 파일을 확장해야 할 땐 이 패턴 우선 검토.
7. **빌드 환경**: 이 프로젝트는 세션마다 새 샌드박스일 수 있어 `gradlew`/jpackage 포함 JDK가 없을 수 있음 — 메모리 `project_build_toolchain_missing` 절차 우선 시도. jpackage 없이도 `compileKotlin`/`compileDebugKotlin`/`assembleDebug`는 Android Studio JBR(`C:\Program Files\Android\Android Studio\jbr`)만으로 충분.
8. **문서 체계**: 이 프로젝트는 HANDOFF/CHANGELOG/DECISIONS/BUGS/IDEAS 5종 문서 체계를 씀. 세션 종료 시 각 문서를 역할에 맞게 갱신할 것 — 자세한 규칙은 `CLAUDE.md` 참고. 과거 세션 상세 기록은 전부 `CHANGELOG.md`에 있음.
9. **robocopy는 반드시 PowerShell 도구로(위 "현재 주의사항"·"실행 방법" 상단 참고)**: Bash 도구로 실행하면 이 프로젝트의 한글 경로에서 조용히 아무것도 복사 안 되고, Gradle의 UP-TO-DATE 스킵과 겹치면 "빌드/배포 성공" 로그만 보고 옛날 코드를 계속 재배포하게 된다. `/MIR`은 auto mode classifier가 차단할 수 있음(103차 확인) — 파괴적 플래그 없이 `/E`로 복사하는 것도 대안.
10. **원격 신호로 실제 잠금/차단 같은 강제 동작을 거는 기능은 반드시 신선도 컷오프를 넣을 것**: `REMOTE_STUDY_SIGNAL_STALE_MS`(20분, `remoteUpdatedAtMillis()` 기준)처럼 — 신호를 올리던 기기가 정지 없이 꺼지면 유령 신호가 남아 이 기기가 영구히 잠길 수 있다.
11. **Room DB 버전은 103차 기준 39**(82차부터 `fallbackToDestructiveMigration()`을 버리고 버전마다 명시적 `MIGRATION_x_y`를 작성하는 방식으로 전환됨 — 스키마 변경 시 버전을 올리는 것과 별개로, 기존 데이터를 지우지 않는 명시적 마이그레이션 코드를 반드시 함께 작성할 것. 단순 쿼리 추가(기존 테이블 변경 없음)는 마이그레이션 불필요, 103차 `observeEarnedTotal()`이 그 예).
12. **크로스디바이스 동기화 기능을 추가할 때 "UI 표시용"과 "판정 게이트용" 함수를 반드시 분리할 것**: 동기화 값을 읽는 함수는 네트워크 I/O가 있어서(캐시가 있어도) Compose 리컴포지션마다 직접 호출되는 자리(그룹 목록 배지 등)에서 쓰면 UI가 버벅인다. `isSnoozeActive()`(UI, 로컬 값만)와 판정용 private `isSnoozed()`(백그라운드 tick에서만, 동기화 값)를 분리해서 해결한 전례 — 판정 시점에 병합값을 로컬로 persist해두므로 UI는 다음 재조회 때 자연히 따라잡는다. [[DECISIONS.md]] "스누즈(#1) 크로스디바이스 동기화..." 참고.
13. **AppGroup(그룹) 자체는 원래부터 Firebase에 안 올라간다**: 그룹 설정(스케줄/한도/실행확인 설정값 등)은 기기별 로컬 전용이고, Firebase로 동기화되는 건 `dailyUsage`/`confirmSync`/`studyLog`/`pomodoro`/`calendar`/`calculator`/`snoozeSync`/`points`뿐이다. 새 그룹 필드를 추가할 때 "이것도 동기화해야 하나?"라는 질문이 나오면, 그룹 전체를 동기화 대상으로 만들지 말고 해당 필드만 별도의 작은 sync 경로로 분리해서 추가할 것.
14. **화면 문구 교체 요청은 title/quote 슬롯을 먼저 구분할 것**: `WatchAndWaitScreen`/`InterstitialScreen`은 title(큰 헤드라인)과 quote(작은 부제) 두 슬롯이 있는데, 사용자가 인용한 문구가 실제로 코드의 어느 파라미터에 들어있는지 대조하지 않고 진행하면 엉뚱한 슬롯을 바꾸게 된다.
15. **텍스트 자동 축소(shrink-to-fit) 로직을 짤 땐 "무엇을 막으려는지"에 맞는 조건을 고를 것**: "N줄을 넘칠 때" 줄이는 로직(`maxLines=N`+`didOverflowHeight`)과 "1줄에 안 들어갈 때" 줄이는 로직(`maxLines=1`+`didOverflowWidth`)은 다르다 — "짧은 줄바꿈 자체가 보기 싫다"는 요구엔 후자가 맞는데, 전자로 짜면 대부분 케이스에서 로직이 아예 발동하지 않아 "안 바뀐 것 같다"는 피드백을 받게 된다.
16. **화면 폭 기준으로 기기 종류를 나눌 땐 표준 브레이크포인트(sw600dp)부터 검토할 것**: `LocalConfiguration.current.screenWidthDp >= 600`으로 태블릿/폰을 판정(`InterstitialScreen.kt`) — 폼팩터별 분기가 또 필요해지면 이 임계값부터 재사용 검토할 것.
17. **새 순수 로직/계산 함수는 먼저 `:shared`에 넣을 자리인지 검토할 것(82차 도입, 103차 재확인)**: Room 엔티티나 플랫폼 데이터클래스에 결합되지 않는 순수 함수(포인트 계산, 문구, 스트릭 등)는 안드로이드/데스크탑 양쪽에 복붙하지 말고 `shared/src/main/kotlin/com/phonelock/shared/`에 한 번만 작성 — 두 앱 모두 `implementation("com.phonelock:shared")`로 이미 연결돼 있어 import만 추가하면 된다. 안드로이드에서 컴파일 확인 시 `AndroidBuilds/phone-lock-android`의 **형제 디렉터리**로 `AndroidBuilds/shared`도 함께 robocopy해야 한다(`../shared` composite build 참조, 안 하면 "Unresolved reference" 오류).
18. **109차 "식물 탭 게임성 개편"은 코드·빌드·배포·GitHub 게시까지 전부 완료**(실사용 검증만 남음). 다음으로 "레벨/칭호 시스템 기획"을 논의하자는 지시를 받았으나 **아직 구체적 스펙은 없음** — 임의로 새 기획을 확장하지 말고 세션 시작 시 사용자에게 먼저 물어볼 것(CLAUDE.md "추측하거나 임의로 구현하지 말고" 원칙).

---

*마지막 업데이트: 2026-09-12 (111차 세션+후속. "식물"→"홈" 탭 승격 + 배경 침범 버그 수정 + "관리"→"규칙" 개명 + 설정 시스템 전면 재구성(홈 원형 버튼 진입점 + 9개 카테고리) + 프로필 "아이디 변경" 기능 신규. 양 플랫폼 컴파일 확인+android `assembleRelease`(versionCode `1789141469`, 3위치 재배포)/desktop `packageMsi createDistributable`(BuildInfo `1789141595`, 호스트 재배포+재기동)+GitHub 공개 저장소 동기화+두 릴리스 게시까지 전부 완료. 실사용 검증 전. **다음 세션 최우선: 111차 신규 사항 전체 실사용 검증.**)*
