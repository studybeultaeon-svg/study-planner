# 프로젝트 개요

**프로젝트 이름**: 갓생살기종합세트 (구 "관리앱" — 45차 세션에서 개명, [[DECISIONS.md]] 45차 참고)

**프로젝트 목적**: 개인 자기통제(디지털 디톡스)를 위한 앱/사이트 차단 시스템. 지정한 앱·프로그램·사이트를 시간대/일일 한도로 제한하고, 해제하려면 타이머 대기 + 회유 멘트 통과 같은 "꼼수 방지" 절차를 거치게 한다. 이 문서 체계(HANDOFF/CHANGELOG/DECISIONS/BUGS/IDEAS)가 관리하는 범위는 phone-lock 계열(안드로이드/데스크탑/브라우저 확장)이며, 공부앱 네이티브 재구현도 같은 코드베이스 안에서 함께 관리한다.

**한 줄 설명**: 안드로이드 앱 + 윈도우 데스크탑 앱 + 크롬 확장이 하나의 규칙(그룹)을 공유하며 서로 다른 플랫폼에서 같은 방식으로 잠금·확인·레벨을 동기화하는 3종 세트. "갓생살기종합세트"라는 이름 아래 자기통제(관리앱)·학습관리(공부앱, 같은 코드베이스에 통합됨)·루틴앱(v1 코드/빌드 완료, 49차)까지 함께 묶는다. 반대로 알람앱(`com.wakealarm`, 별도 저장소)은 도메인이 완전히 독립적이라 이 세트에 포함하지 않고 분리 유지하기로 함 — 판단 기준은 [[DECISIONS.md]] 45차 참고.

구성 요소:
- **phone-lock-android**: Kotlin/Compose + Room DB 기반 안드로이드 앱
- **phone-lock-desktop**: Kotlin/Compose Desktop 기반 Windows 앱
- **browser-extension** (`phone-lock-desktop/browser-extension/`): Chrome 확장(Manifest V3), 데스크탑 앱의 로컬 API(`127.0.0.1:47882`)와 통신해서 사이트 차단 담당
- **루틴앱**(반복 할 일/습관/일과표, `Routine` 하나로 통합 — [[DECISIONS.md]] 47차 참고): 47차 설계 확정, 48차 데이터 모델/CRUD/스트릭 엔진, 49차 화면 3종(오늘/일과표/습관)+편집 다이얼로그, 50차에 "오늘"/"통계" 2탭 개편(일과표는 오늘 탭에 시간순 정렬로 흡수, 습관→통계)+요일 피커/주 이동+스트릭 방어권 주 2회 고정+Firebase 동기화 추가, 51차(문서 미기록, 코드로만 확인) 스트릭을 루틴별에서 "하루" 단위 전역 스트릭으로 전면 개편, 52차에 순서변경(▲/▼)+복사+아이콘+알림+스트릭알림+기간설정까지 추가(Room DB 26→27). 실기기 검증 대기 중.

**현재 개발 단계**: 핵심 기능(그룹 차단) 구현 완료. 공부앱 네이티브 재구현 **1~5단계 전체 완료**(타이머/뽀모도로→캘린더→계산기→일정표→통계, 30차), 이후 세션들에서 크로스디바이스 동기화(뽀모도로/공부기록/스누즈)·조롱조 문구·오버레이 재설계 등 신규 기능을 계속 추가해왔다. **루틴앱은 47~50차에 v2 전체 완료.** **61~70차에 Google 로그인 동기화와 "모임"(소셜 그룹) 탭 구현·배포·보안 강화·실기기 검증까지 완료.** **73~75차에 "무전기"(모임 내 강제 음성/텍스트 메시지)와 자체 업데이트 기능(GitHub Releases 기반) 구현·배포.** **78차부터 "GitHub에 올려"=릴리스 게시, 안드로이드 기본 빌드=release 서명이라는 작업 워크플로 확정.** 77~81차는 모임 기능 확장/정정, 각종 버그 수정, 설정 화면 재배치, 데스크탑 오버레이 개선, 공부 중 알림 억제 등을 계속 처리. 92차에 공부 잠금 화면 재디자인, **93차에 "모임"을 "소셜"로 개편해 메신저 형태(그룹 대화 채널/1:1 DM/소유권 승계/전체 디자인 통일)로 확장** — 세부 세션별 이력은 [[CHANGELOG.md]] 참고, 최신 상태는 바로 아래 "현재 진행 중인 작업" 참고.

**현재 진행률**: 기존 그룹 차단 기능 약 90%. 공부앱 네이티브 재구현 1~5단계 + 계산기-캘린더 연동 전체 완료. 루틴앱 v2 코드/빌드/배포 완료. "모임"(소셜) 탭 — 가입/승인/멤버 상세/무전기/자체 업데이트까지 전 단계 실사용 검증 완료(70차 등), **93차 신규 메신저 기능(그룹 대화/DM/소유권 승계)은 코드·빌드·배포·GitHub 릴리스까지 완료됐지만 실제 메시지 송수신 실사용은 미검증**. **77~81차 기능들은 코드·빌드·GitHub 릴리스 게시·이 호스트 재배포까지 전부 완료됐지만 실사용 미검증 누적 중** — 세부 목록은 [[CHANGELOG.md]] 참고. 자세한 배경은 [[DECISIONS.md]] 참고.

---

# 현재 구현된 주요 기능

- 앱/프로그램/사이트를 시간대·일일 한도 기준으로 차단(안드로이드/데스크탑 공통), 시간대·일일한도는 항상 OR 판정
- 그룹 편집 화면 "관리 종류" 섹션(스케줄/일일 사용한도 설정/실행 확인 3개 토글), 일일한도·실행확인은 각각 독립 "적용 시간대"+요일마스크(`dailyLimitDaysMask`/`confirmDaysMask`) 설정 가능
- 차단 해제 시 "실행 확인" 대기 화면(타이머 + 무작위 체크포인트 클릭), 회유 멘트 20개 통과 절차(꼼수 방지)
- 실행 확인 레벨 escalation 시스템: 재확인할수록 대기시간 증가, 시간 경과로 자동 감소. 표시값(오버레이 알파)과 판정 로직은 항상 분리 — [[DECISIONS.md]] 참고. **37차**: 그룹별 "오버레이 최고 밝기까지 재확인 횟수"(`overlayLevelStepsToMax`) 설정 추가 — 이 값으로부터 한 번 재확인할 때마다 오르는 밝기 폭을 시스템이 자동 계산(양 플랫폼+브라우저 확장 3곳 모두 적용, 순수 표시값이라 판정 로직과 무관)
- 모바일 ↔ 데스크탑 실행확인 레벨 + 일일 사용시간을 Firebase Realtime Database로 동기화(그룹 이름 기준). 레벨은 최신값 승리, 사용시간은 기기별 합산 — 자세한 설계는 [[DECISIONS.md]]. **37차**: 같은 이름의 그룹을 가진 다른 기기가 방금 실행확인을 통과했으면(`confirmSync`의 `lastConfirmedAtEpochMillis`) 이 기기도 재확인 없이 같은 유예시간(쿨다운)을 이어서 쓰도록 확장 — `ConfirmationGate.kt`(로컬 인메모리 쿨다운) 자체는 그대로 두고, 호출부(`EnforcementService`/`SiteEnforcement`/안드로이드 `AppMonitorAccessibilityService`)에서 로컬 값과 동기화된 값 중 더 최근인 쪽을 쓰도록 확장
- 릴스/쇼츠 감지 차단(안드로이드 접근성 트리, 브라우저 확장 URL 패턴) — 데스크탑 네이티브 앱엔 없음, [[IDEAS.md]] 참고
- 23:00~11:00 편집 면제 시간대
- 그룹별 "사용 중 남은 시간" 오버레이(안드로이드 접근성 오버레이, 브라우저 확장 content script DOM 삽입, 데스크탑 우측 상단 소형 위젯 — 코너 위젯인 이유는 [[DECISIONS.md]])
- 뽀모도로 휴식 연동: 완전히 별개 웹앱인 "공부앱"의 Firebase RTDB를 읽기 전용 폴링, `pomodoroUnlockEnabled` 켠 그룹만 휴식 시간 동안 임시 해제 + 전용 오버레이 표시
- **공부 잠금 + 네이티브 타이머(2026-08-06~07, 15~16·19·24차 세션)**: 공부앱 타이머가 "공부" 페이즈로 진행 중일 때(휴식 중엔 아님) 데스크탑/안드로이드에 전체화면 잠금 + 허용 프로그램(플랫폼별)·사이트(각 기기에 개별 입력, 24차부터 Firebase 공유 아님) 예외. 24차 세션부터 타이머 자체가 네이티브(양 플랫폼 "타이머" 탭, `StudyTimerScreen`)로 동작 — 잠금 판정/정지·전환 버튼 모두 로컬 상태를 직접 읽고 쓰며(네트워크·웹뷰 불필요), Firebase `pomodoro` 노드는 다른 기기에 신호를 주는 부가 채널로만 남음. **37차**: 이 기기 로컬 타이머가 꺼져 있어도 같은 계정의 다른 기기가 공부 페이즈 실행 중이라는 신호(`PomodoroSyncClient.isStudyTimerActive`)가 있으면 이 기기도 실제로 공부 잠금(전체화면+사이트 차단)이 걸리도록 확장(기존엔 34차의 "표시만 하는 미러"였음) — 허용 목록은 여전히 각 기기 로컬 값을 쓰고, 원격 신호로 잠긴 경우 정지/전환 버튼은 숨기고 안내 문구만 표시(원격 제어는 하지 않음, 19→24차 원칙 유지). 신호가 20분 넘게 갱신 안 되면 유령 신호로 보고 무시(영구 잠김 방지 안전장치). 자세한 설계는 [[DECISIONS.md]] "1단계(타이머/뽀모도로) 네이티브 재구현", "공부앱을 웹/웹뷰가 아닌 완전 네이티브로 재구현", "크로스디바이스 공부 잠금 강제 적용"(37차) 참고
- **네이티브 캘린더(2026-08-07, 25차 세션)**: 양 플랫폼 "캘린더" 탭(`CalendarScreen`) — 월 그리드 + 선택 날짜 상세, 회독(1~4단계 색상) 완료 시 자동 다음 회독 생성(웹앱 로직 그대로), 순서변경/이름수정/이동/복사/삭제, 6개월 이전 정리. Firebase `users/{user}/calendar` 경로로 기존 웹앱과 동일 스키마 공유(전체 문서 LWW) — 24차의 "허용 프로그램/사이트는 기기별 로컬"과 달리 캘린더는 기기 간 동기화 대상. 타이머 탭의 "업무 이름" 자유 텍스트가 "오늘 캘린더 일정" 드롭다운으로 교체됨. "할당량 연동"(계산기 연동)은 3단계 이후로 제외. 자세한 설계는 [[DECISIONS.md]] "2단계(캘린더) 네이티브 재구현" 참고
- **네이티브 계산기(2026-08-07, 27차 세션)**: 양 플랫폼 "계산기" 탭(`CalculatorScreen`, 입력/결과/저장됨 3개 서브탭) — 업무 입력 카드(요일별 목표+휴일 제외) → `CalcEngine`(웹앱 calculate()/simulateFinish()/calcRequiredPace() 그대로 이식)로 계산 → 진척도/요일별 페이스 비교표/완료 예상일 결과 → 저장(사용자 요청으로 폴더 트리 포함 웹앱과 동일 기능: 하위 폴더 생성/이름변경/삭제/순서변경, 항목 폴더 이동). Firebase `users/{user}/calculator` 경로를 기존 웹앱과 동일 스키마로 공유하되, draft(`tasks`)/저장됨(`saved`)/폴더 트리(`savedFolderTree`+`savedFolderOrder`) 3구간을 독립 LWW 타임스탬프로 각각 비교(진입 시 1회 동기화, 웹앱처럼 실시간 구독은 아님 — 캘린더와 같은 단순화). 캘린더 연동(`linkedCalc`/`progressStep`)은 이번에도 제외(사용자 확인). 자세한 설계는 [[DECISIONS.md]] "3단계(계산기) 네이티브 재구현" 참고
- **2단 탭 구조 + 데스크탑 좌우 분할 레이아웃(2026-08-07, 28차 세션)**: 그룹/통계/타이머/캘린더/계산기/설정이 나란히 있던 구조를 "관리앱"(그룹/통계)·"공부앱"(타이머/캘린더/계산기)·설정 3섹션으로 재편(49차에 "루틴" 섹션 추가돼 4섹션, **50차에 순서를 루틴→공부→관리→설정으로 재배열 + "공부앱"/"관리앱" 라벨에서 "앱" 제거해 "공부"/"관리"로 축약**). 데스크탑은 왼쪽 `NavigationRail` 사이드바+섹션 내부 `TabRow` 서브탭, 안드로이드는 하단 `NavigationBar`+섹션 내부 `TabRow` 서브탭(`MainActivity.kt`의 `ManageSection`/`StudySection`). 이어서 데스크탑 최상위 창에 배경 `Surface`가 없어 여백이 흰색으로 비치던 버그 수정(`Main.kt`), 데스크탑 타이머/캘린더/계산기 3개 화면을 좌우 분할(마스터-디테일) 레이아웃으로 재작성. 안드로이드는 이 레이아웃 변경 대상 아님(세로 레이아웃 유지). **테마는 28차 당시 다크+파랑 → 49차 라이트+그린 전면 교체 → 50차에 라이트+그린/다크+파랑/화이트+오렌지 3종 선택제로 확장**(설정 화면에서 전환, 아래 루틴앱 항목 참고) — 자세한 판단 근거는 [[DECISIONS.md]] "관리앱 UI 전면 개편"(28차)/49~50차 참고.
- **루틴앱 v1→v2: 반복 할 일/습관/일과표 통합 + 오늘/통계 2탭 개편 + Firebase 동기화 + 앱 전체 테마 3종 선택제(2026-08-13, 47~50차 세션)**: `Routine`(제목/시간대/요일마스크/스트릭 추적)+`RoutineLog` 모델 하나로 "체크리스트"/"습관"/"시간대별 일과표" 세 요구를 통합(플래그·파생 뷰 차이일 뿐 별도 저장 없음). 양 플랫폼 "루틴" 섹션은 49차엔 오늘/일과표/습관 3탭이었으나 **50차에 오늘/통계 2탭으로 개편** — 일과표는 별도 탭 없이 오늘 탭 루틴 목록이 항상 `timeSlot` 기준 시간순으로 보이는 것으로 흡수, 습관 탭은 "현재 스트릭/오늘 완료/오늘 완료율/최고 스트릭" 지표 카드 중심의 통계 탭으로 교체. 오늘 탭엔 **50차**에 일~토 요일 피커(안드로이드는 스크롤 대신 `weight(1f)` 균등분할 칩으로 확정 — [[BUGS.md]] 50차 참고)+주 단위 이동(◀/▶)도 추가돼 과거/미래 날짜를 조회 가능(스트릭 계산은 항상 실제 오늘 기준 유지). 편집은 `GroupEditScreen`과 달리 `AlertDialog` 기반 다이얼로그이며, **50차에 방어권 종류/횟수를 직접 입력하던 UI를 제거하고 항상 "주 2회"로 고정**(안내 문구만 남김). 스트릭은 `RoutineEngine.currentStreak()`(양 플랫폼 대칭 복제, `CalcEngine.kt`와 같은 패턴)이 매번 로그를 다시 훑어 계산하는 순수 함수. **49차에 앱 전체 테마를 다크+파랑(28차/35차)에서 라이트+그린(Primary `#8BC34A`, 배경 `#FAFBF6`)으로 뒤집었고, 50차엔 이를 3종 선택제(라이트+그린/다크+파랑/화이트+오렌지)로 확장**(설정 `SectionCard`, `ThemeMode`+`PhoneLockPalette` 구조) — `MaterialTheme`을 안 쓰는 하드코딩 accent(차단/실행확인 화면, 오버레이 타이머 텍스트, 브라우저 확장 `overlay.js`)는 49차 교체분(옛 파랑→초록) 그대로 유지, 브라우저 확장 자체의 다크 CSS 팔레트는 범위 밖. **50차에 루틴 Firebase 동기화 신규 추가**(47~49차엔 로컬 전용) — 안드로이드 Room auto-increment ID와 데스크탑 수동 카운터 간 충돌을 피하려 루틴을 배열로, 로그는 배열 인덱스로 부모 루틴을 참조하는 방식(`routinesToJson`/`routinesFromJson`), 캘린더/계산기와 같은 전체 문서 LWW. 자세한 설계는 [[DECISIONS.md]] 47~50차 참고, 상세 내용은 [[CHANGELOG.md]] 48~50차 참고. **50차 신규 기능 전부 실기기 미검증.**
- **공부앱 계산기↔캘린더 연동 기능 이식 + 안드로이드 홈스크린 위젯(2026-08-13, 50차 세션)**: 원본 웹앱(`공부앱/index.html`)엔 있었지만 25~30차 네이티브 재구현 때 명시적으로 제외됐던 "캘린더 일정에 계산기 업무를 연결하면 완료 시 진행량이 자동 차감/복원되는" 연동을 이번에 이식 — `Repository`/`PhoneLockRepository`에 `linkedProgressAmount`/`addLinkedCalendarTask`/`isLinkedGoalAchieved`/`adjustLinkedCalcProgress` 신규, `CalendarScreen.kt` 날짜 상세에 `LinkedCalcSection`(업무 선택은 `FlowRow`로 오버플로우 방지), `TimetableScreen.kt`가 목표 달성 일정에 ✅ 표시. 8단계 체계 전환으로 연동 트리거 색을 웹앱의 "red"(1회독)에서 "white"(현재 1회독)로 조정 — [[DECISIONS.md]] 50차 참고. **회독을 4단계→8단계 무지개(하양~보라)로 확장, 간격은 처음 1,1,3,7,14,30,60일로 짰다가 에빙하우스 망각곡선/spacing effect 검토 후 1,3,7,14,30,60,120일(매 단계 ~2~2.3배)로 재조정** — 저장된 `color` 값 자체는 안 바뀌고 라벨 매핑만 바뀌어 과거 일정은 다른 회독 번호로 보이게 됨(의도된 재라벨링, 사용자 승인). 데스크탑 캘린더 월그리드에도 안드로이드처럼 날짜 칸 완료 배지(초록/노랑/빨강) 추가. **안드로이드 홈스크린 위젯 신규**(루틴 목록을 홈 화면에서 체크) — 새 Gradle 의존성 없이 기존 `RemoteViews`/`AppWidgetProvider`/`RemoteViewsService` 인프라로 구현. 전부 실기기 미검증.
- **네이티브 일정표(2026-08-07, 29차 세션)**: 양 플랫폼 "공부앱" 섹션에 4번째 서브탭 `TimetableScreen` 추가 — 계산기 draft 업무(`repository.getCalcTasks()`)를 요일별 목표량 표로 보여주는 읽기 전용 파생 뷰. 데스크탑은 웹앱과 동일하게 이번 주(일~토) 고정 테이블(업무×요일+합계 행), 안드로이드는 일 단위 리스트+◀/▶ 날짜 이동(웹앱 모바일 뷰와 동일). 51차에 계산기 연동(`linkedCalc` 완료 체크, 그날 목표 달성 시 ✅+초록, 오늘인데 미달성이면 빨강) 이식 완료. **79차**: 모임 멤버 상세의 "일정표" 탭(78차 신규)에도 이 빨강/초록 판정을 동일하게 재현 — `ScheduleStat`에 `linkedCalc`/`progressStep` 동기화 필드 추가, 양 플랫폼 빌드/배포/GitHub 릴리스 게시까지 완료.
- **네이티브 통계(2026-08-07, 30차 세션)**: 양 플랫폼 "공부앱" 섹션에 5번째(마지막) 서브탭 `StudyStatsScreen` 추가 — 캘린더 일정 전체(`repository.getAllCalendarTasks()`/`getAllCalendarTasksOnce()`)를 집계해 전체/완료/완료율/연속 완료일(스트릭)/회독 단계별 완료 현황/최근 30일 완료 추이 막대그래프로 보여주는 읽기 전용 파생 뷰. 계산기 연동(`linkedCalc`)은 3~4단계와 마찬가지로 제외. 이로써 공부앱 네이티브 재구현 5단계 로드맵(타이머→캘린더→계산기→일정표→통계) 전체 완료. 자세한 설계는 [[DECISIONS.md]] "5단계(통계) 네이티브 재구현" 참고.
- **계산기 버그 3건 수정(2026-08-07, 31차 세션)**: ① 데스크탑 계산 결과 크래시(제네릭 타입 소거로 인한 `filterIsInstance` 오작동 → `ClassCastException`) 수정, ② 저장됨 항목의 폴더가 목록에 안 뜨는 문제(레거시 웹앱 데이터가 `savedFolderTree` 없이 항목별 `folderPath`만 가진 경우) — 양 플랫폼 `Repository`/`PhoneLockRepository`에 `healCalcFolderPaths()` 자동 보정 추가, ③ 데스크탑 계산기 레이아웃을 28차의 입력/결과/저장됨 동등 3탭 구조에서 웹앱과 동일한 좌(2):우(8) 사이드바 구조(왼쪽 업무입력/저장됨 서브탭, 오른쪽엔 항상 결과)로 재작성, 저장됨도 안드로이드판과 같은 재귀 폴더 트리로 회귀. 자세한 원인은 [[CHANGELOG.md]] 31차 세션 참고.
- **계산기 기능 보강 + 웹앱 소스 기반 전면 UI/색상 재검토(2026-08-07, 32차 세션)**: 계산기에 폴더 접기 영속화(`calcFolderCollapsed`)·카드 개별 접기·모두 펴기/접기·전체 저장·입력 하단 버튼 고정 추가, 안드로이드 타이머 탭엔 인라인 허용 앱 선택 섹션 신규(`StudyLockAppsScreen`의 `AllowedAppsPickerBody` 재사용). 이어서 사용자가 웹앱과 나란히 비교하며 지적 → 계산기 결과 카드/타이머/캘린더 3개 화면을 `공부앱/index.html`의 실제 CSS/JS를 직접 확인해 전면 재구현(스크린샷 추측 방식은 여러 번 틀려서 폐기 — [[DECISIONS.md]] "UI를 웹앱과 맞출 때는..." 참고). 타이머 "오늘 캘린더 일정" 드롭다운이 선택이 안 바뀌던 버그를 `ExposedDropdownMenuBox`로 교체해 수정. 자세한 내용은 [[CHANGELOG.md]] 32차 세션 참고.
- **32차 신규 버그 4건 수정(2026-08-07, 33차 세션)**: 데스크탑 공부 잠금 허용 프로그램이 `ProcessBuilder`의 PATH 전용 검색 한계로 실행 안 되던 문제를 레지스트리 App Paths→시작 메뉴 바로가기(.lnk)→PATH 3단계 폴백(`resolveAppPath()`)으로 해결(자세한 배경은 [[DECISIONS.md]] "공부 잠금 허용 프로그램 실행..." 참고), 공부 기록이 "오늘의 공부 기록"에 안 남던 건 저장 로직이 아니라 잠금 화면 경로에서 타이머 탭 UI 상태가 안 갱신되던 표시 버그였음(tick 루프에 `todayLog` 재조회 추가), 안드로이드 허용 앱 선택창이 안 뜨던 건 `AllowedAppsPickerBody`의 중첩 `LazyColumn`+`weight()` 조합이 무한 높이 부모(`verticalScroll`) 안에서 렌더링이 깨지던 문제였음(`weight()` 제거, `heightIn(max)`만 사용), 계산기 결과 카드 비율은 고정 2열 그리드를 웹앱과 동일한 `LazyVerticalGrid(GridCells.Adaptive(minSize=320.dp))`로 교체해 해결. 자세한 내용은 [[CHANGELOG.md]] 33차 세션 참고.
- **32차 아이디어 6건 전부 구현 + 타이머/공부기록 크로스디바이스 동기화(2026-08-07, 34차 세션)**: 탭 이름 변경, 데스크탑 일정표 주 이동(`weekOffset`), 캘린더 날짜 상세 타이머 기록(날짜 옆 총시간+업무별 개별 시간), 저장됨 폴더 팝오버(`DropdownMenu`), 웹앱 애니메이션(phase 배지 pulse, 카드 fadeIn+slideUp), 관리앱 좌우 분할(그룹/통계) — 6건 전부 완료. 이어서 사용자 요청으로 **타이머 크로스디바이스 미러**(이 기기 타이머가 꺼져 있을 때 다른 기기가 재고 있으면 시작 없이 메인 화면이 자동으로 실시간 상태를 보여줌, 제어는 제외 — `pomodoro` push에 `phaseStartedAt`/`taskName` 추가 + 20분 초과 시 무시하는 `remoteUpdatedAtMillis()`)와 **공부 기록(StudyLogEntry) 진짜 동기화**(`users/{user}/studyLog/{날짜}/{기기}`, dailyUsage와 같은 기기별 키 패턴, 로컬엔 병합 안 하고 표시용 캐시로만 합산)를 신규 구현. 캘린더 날짜 상세가 "진입 시 1회만 동기화"하던 버그(패널을 계속 열어두면 갱신 안 됨)를 실제 Firebase 서버 상태 REST API 직접 조회로 디버깅해 발견, 5초 주기로 수정. 사용자가 안드로이드 실기기에서 공부기록 동기화 정상 작동 확인. 자세한 설계는 [[DECISIONS.md]] "공부 기록 크로스디바이스 동기화"/"타이머 크로스디바이스 '미러' 표시" 참고, 상세 내용은 [[CHANGELOG.md]] 34차 세션 참고.
- **Alt-Tab 버그 원인 확정 + 캘린더 미완료 자동복사 + 확장 다크테마 통일 + accent 색상 교체(2026-08-08, 35차 세션)**: ① Alt-Tab 사이트 차단 우회 버그 — `background.js`가 `onBeforeNavigate`(새 네비게이션)에만 반응해서 이미 열려있던 탭으로 Alt-Tab만 하면 최대 60초(1분 tick 폴링 주기)까지 실제로 이용 가능하다는 원인을 코드로 확정, 수정(탭/창 포커스 리스너 추가)은 사용자 요청으로 보류. ② 캘린더 "미완료(X)" 시 다음날로 같은 업무 자동 복사 — 기존 "완료(O)→다음 회독 자동 생성" 패턴과 대칭되는 `applyIncompleteCarryOver`/`revertIncompleteCarryOver` 추가(원본은 그대로 남김). ③ 브라우저 확장 `confirm.html`/`blocked.html`/`onboarding.html`이 28차 다크테마 개편에서 빠져있던 걸 공부앱과 동일한 팔레트로 통일. ④ "후자" 버튼/오버레이 강조색을 주황(`#FF9800`)에서 파란 accent(`#4F8EF7`)로 앱 전체 9개 파일 교체(사용자 요청). ⑤ "어제 공부기록/캘린더 일정이 오늘도 보인다"는 문의에 `dailyResetHour=9`(오전 9시 리셋) 설정 때문임을 로컬 데이터파일로 확인해 설명 — 코드 변경 없음, 사용자가 유지 결정. 자세한 설계는 [[DECISIONS.md]] "앱 전체 강조색을..."/"캘린더 '미완료(X)' 시..." 참고, 상세 내용은 [[CHANGELOG.md]] 35차 세션 참고.
- **"모임" 탭(2026-08-26, 62차 구현 → 63차 실제 빌드/배포 완료, 실기기 검증만 남음)**: 관리↔설정 탭 사이 신규 탭 — 기존 앱차단 "그룹"과 헷갈리지 않도록 이름을 "모임"으로 분리. 모임 만들기/초대코드 참여, 멤버 목록(오늘 완료율 낮은 순 정렬+"오늘 아직 안 한 사람 N명" 배지), 멤버 탭하면 상세페이지(루틴 체크리스트/오늘 공부시간·진행률/스트릭, 상대가 설정에서 안 켠 항목은 "비공개"), 😴 깨우기 버튼(RTDB 신호+로컬 알림, 안드로이드는 WorkManager 15분 하한이라 즉시 도착은 아님). 설정에 "모임 공유 설정"(루틴/공부/스트릭 각각 토글, 기본 on) 신규. Firebase `groups/{id}/...`+`inviteCodes/{code}`+`users/{uid}/socialGroupIds` 스키마, Room DB는 안 건드림(로컬 저장은 SharedPreferences/JSON 몇 필드뿐). 자세한 설계는 [[DECISIONS.md]] 62차, 파일 목록은 아래 "현재 중요한 파일" 참고.
- **"소셜" 메신저 확장(2026-09-06, 93차 / 채팅 버그 99차 해결+알림 신규 추가)**: "모임" 탭 라벨을 "소셜"로 바꾸고 디스코드식 "서버=모임, 채널=용도별 공간" 구조로 확장 — 모임 상세에 "💬 대화" 채널(텍스트+이모지 리액션, `groupChats/{groupId}/messages`, 참여자 판정은 기존 `groups/{id}/members` 재사용) 신규, "소셜" 탭 진입 화면에 1:1 DM 목록+전역 검색(`dmChats/{chatId}`, 상대는 기존 `usernames/{customId}` 인덱스로 검색) 신규, "👥 멤버 관리"에 모임장 소유권 승계("👑 승계") 신규. 실시간성은 화면이 켜져있는 동안 4초 폴링(SSE/FCM 없음, IDEAS.md 73차와 같은 트레이드오프). 소셜 관련 화면 전체(목록/멤버/멤버상세/채팅) 디자인을 92차 잠금 화면 언어(강조색 배지/카드형)로 통일, 배경은 잠금 화면과 달리 `verticalGradient`(원형 글로우는 잠금 화면 전용으로 남김 — 사용자 지적으로 정정, [[DECISIONS.md]] 93차 참고). **96차에 발견된 "채팅 메시지가 목록에 안 뜨는" 버그는 99차에 `.indexOn` 색인 누락으로 확정·해결(사용자 실사용 확인 완료).** **99차에 채팅 알림 신규**(진동 없음, 무전기 서비스 7초 폴링에 편승, 지금 보는 대화방은 중복 알림 제외) — 사진/GIF 첨부는 같은 세션에 시도했다가 요청으로 되돌려 현재 없음. 자세한 설계는 [[DECISIONS.md]] 93차/99차, 상세 내용은 [[CHANGELOG.md]] 93차/99차 참고.
- **자체 업데이트 기능(GitHub Releases 기반, 2026-08-30, 75차, 양 플랫폼 코드/빌드/실제 배포/GitHub 릴리스 게시까지 완료, 사용자 확인함)**: 설정탭 초기화 시각(`dailyResetHour`) 기준 "오늘"이 바뀌면 하루 1회 GitHub Releases(`studybeultaeon-svg/study-planner`, 공부앱 웹앱과 같은 공개 저장소)를 조회해 새 버전이 있으면 배너("업데이트를 진행하세요")를 띄우고, 버튼을 누르면 실제 다운로드+설치까지 수행한다(안드로이드는 `DownloadManager`+설치 인텐트, 데스크탑은 설치파일 실행 후 자기 종료 — 최종 "설치" 확인은 OS 정책상 항상 사용자 몫). 태그 규칙은 안드로이드 `android-<versionCode>`/데스크탑 `desktop-<BuildInfo.BUILD_TIMESTAMP>`(데스크탑도 안드로이드 versionCode처럼 컴파일 시점에 빌드 시각을 자동 기록하는 Gradle 태스크 신설). 설정 화면에도 "업데이트" 섹션(현재 버전 표시+"지금 확인" 버튼+발견 시 실제 설치 버튼) 추가. `gh` CLI를 호스트에 설치해 device-code 웹 인증(비밀번호/토큰은 Claude가 직접 다루지 않음)으로 `studybeultaeon-svg` 계정 인증 완료, 이후 릴리스 게시까지 실제로 수행 — 자세한 설계는 [[DECISIONS.md]] 75차 참고, 구현 파일은 아래 "현재 중요한 파일" 참고.
- **"무전기"(모임 내 강제 음성/텍스트 메시지, 2026-08-29~30, 73~74차, 양 플랫폼 빌드/배포 완료, 실사용 미검증)**: "😴 깨우기"의 확장이라는 개념으로 통합됨(74차) — 멤버 목록/상세 화면에서 깨우기를 누르면 "알림만 보내기 / 음성 메시지 녹음(최대 10초, 8kHz mono WAV) / 텍스트 메시지(TTS로 읽어줌)" 3택 선택창(`WakeOptionsDialog`, 양 플랫폼 공용)이 뜬다. **설정은 74차에 전역(Settings 탭)에서 모임별로 전면 이동** — 각 모임 화면의 ⚙ 무전기 버튼에서 그 모임만의 켜짐/모드("즉시 재생"·"메시지로 받기")/볼륨/요일×시간대 허용 일정(여러 개 등록 가능, 하나라도 맞으면 허용)을 따로 설정한다(`GroupWalkieSettings`+`WalkieSchedule`, RTDB `groups/{id}/walkieSettings/{uid}`). 기기 볼륨/무음 설정은 항상 존중(특수 스트림으로 뚫지 않음). FCM/Cloud Functions 백엔드 없이 클라이언트 폴링만으로 구현(안드로이드는 `WalkieTalkieService` 포그라운드 서비스 7초 폴링 — 74차부터 로그인만 돼있으면 항상 실행되고 모임별 설정을 매번 조회, "그냥 깨우기" 넛지도 같은 7초 주기에 포함시켜 기존 15분 WorkManager보다 훨씬 빠르게 옴; 데스크탑은 기존 30초 루프 옆의 7초 루프) — 실시간성 한계는 사용자와 사전 협의된 트레이드오프. RTDB `groups/{id}/voiceMessages/{targetUid}/{msgId}`(오디오 또는 `textMessage` 필드) 스키마, 재생 후 즉시 삭제 대신 24시간 유예 후 자동 정리 + 명시적 삭제 버튼(74차). TTS는 안드로이드 `TextToSpeech`, 데스크탑은 Windows SAPI를 PowerShell로 호출하되 사용자 입력을 스크립트에 절대 이어붙이지 않는 인젝션 안전 설계([[DECISIONS.md]] 74차 참고). **안드로이드는 RECORD_AUDIO 런타임 권한 요청 플로우가 73차에 이 기능으로 처음 생김.** 자세한 설계는 [[DECISIONS.md]] 73~74차 참고, 구현 파일 목록은 아래 "현재 중요한 파일" 참고. **74차 재설계분 전체(모임별 설정/다중 일정/TTS) 실사용 미검증 — 다음 세션 우선순위.**
- **Google 로그인 전용 동기화로 전면 정리(2026-08-27, 63차)**: 설정 화면의 "Firebase 연결 설정"(Database URL/Web API Key 수동 입력 칸)을 완전히 제거하고 프로젝트 고정값으로 하드코딩(`DEFAULT_FB_DATABASE_URL`/`DEFAULT_FB_API_KEY`, 데스크탑 `Models.kt`·안드로이드 `AppPreferences.kt`) — 사용자는 이제 Google 로그인 버튼만 누르면 됨. 이 과정에서 "예전 사용자 ID" 입력칸과 "기존 데이터 가져오기" 마이그레이션 기능(+`migrateLegacyDataToAccount` 함수)도 함께 삭제, 이어서 `fbUser` 필드/파라미터 자체가 61차 로그인 전환 이후 `PomodoroSyncClient`의 모든 함수 내부에서 실제로는 전혀 안 읽히던 죽은 코드였음을 발견해 프로젝트 전체(양 플랫폼, 함수 시그니처 20여 개 포함)에서 완전히 제거 — 이 앱이 여러 사용자가 각자 쓰는 걸 전제한다는 사용자 판단에 따른 정리. 자세한 배경은 [[DECISIONS.md]] 63차 참고.
- 잠김 화면(스케줄/일일한도)이 실행확인 화면과 같은 톤으로 통일(제목=조롱조 문구 로테이션, "진행"/"중단" 버튼), 안드로이드/데스크탑/브라우저 확장 3곳 공통
- 그룹 전체 on/off(`groupEnabled`)와 스케줄 on/off(`scheduleEnabled`) 분리, `enabled`는 통계 탭 필터 전용
- 데스크탑 로컬 API 토큰 인증(`/pair`) + 상태변경 요청(`/confirm`/`/tick`/`/heartbeat`) POST+토큰 필수
- 데스크탑 ↔ 브라우저 확장 heartbeat 경고 배너, Android 접근성 서비스 watchdog(WorkManager 15분 주기)
- 원자적 데이터 저장(JsonStore `.tmp`+`ATOMIC_MOVE`), 손상 파일 자동 백업, Room 마이그레이션 전 raw SQLite 백업(`PreMigrationBackup`)
- **(2026-08-10, 38차, 39차에 컴파일 확인 완료)** 타이머 탭 상단 "오늘 한눈에" 요약(캘린더 일정/계산기 목표/누적 공부시간), 통계 탭 "최근 7일 vs 지난 7일 완료율" 비교와 "이상 사용 패턴" 경고 배너(7일 평균 대비 1.5배), 그룹별 "재확인 통과 횟수" 카운터(로컬 전용, `ConfirmationGate.kt` 미변경), 데스크탑 일일 자동 백업/복원(최근 7일 보관), 양 플랫폼 사용기록 CSV 내보내기, 설정에서 12개월 이상 지난 통계 수동 정리, 안드로이드 공부 잠금 중 방해금지 모드 자동 적용(**81차에 시스템 DND 방식은 폐기, 아래 81차 항목의 이 앱 자체 알림 억제로 대체됨**). 전부 판정 로직 미변경 — 자세한 목록은 [[CHANGELOG.md]] 38차 참고.
- **(2026-08-10, 39차, 실기기 미검증)** 공부 세션 정지 시 짧은 회고 입력(`StudyLogEntry.note`), 데스크탑 설정/그룹 내보내기·가져오기(안드로이드는 기존부터 있었음), **그룹 일시정지(스누즈)**(그룹별 설정 시간, 하루 3회 제한, `LockEvaluator.isSnoozeActive()`가 `isPomodoroUnlocked()`와 동일한 패턴으로 판정을 일시 우회), **기간 지정 자동 강화**(시험기간 등, `forceEnabledFrom~Until` 범위 안이면 `groupEnabled` 스위치를 꺼도 `LockEvaluator.isGroupActive()`가 켜진 것으로 강제 취급 — 스누즈보다 우선). 스누즈/기간 지정은 이번에 처음으로 `LockEvaluator` 핵심 판정 함수를 확장한 기능이라 다른 기능들과 달리 실기기 검증 우선순위가 높음 — [[DECISIONS.md]]/[[CHANGELOG.md]] 39차 참고.
- **스누즈 크로스디바이스 동기화(2026-08-11, 41차, 실기기 미검증)**: 39차의 스누즈가 그룹 자체를 Firebase로 안 올리는 구조상 기기별 로컬 전용이었던 걸 41차에 동기화 추가. `confirmSync`와 동일한 "최신값(종료시각) 승리" 병합 패턴으로 `users/{user}/snoozeSync/{그룹명}` 신설, 하루 3회 한도도 병합값 기준으로 재판정(기기 나눠 써도 총 3회). 그룹 목록 배지(`isSnoozeActive`)는 Compose 리컴포지션 자리라 여전히 로컬 값만 즉시 조회 — 판정 게이트(`isConfirmActiveNow`/`evaluate`/`isCurrentlyRestricting`)만 동기화된 값을 씀. 안드로이드 `isConfirmActiveNow()`가 이 때문에 `suspend fun`으로 바뀜(호출부 전부 이미 suspend라 안전). 양 플랫폼 컴파일 확인 + APK/jar 빌드 산출물에서 `snoozeSync` 문자열 실제 포함 확인 + 안드로이드 APK 두 위치 갱신 + 데스크탑 표준 절차로 재배포·재실행까지 완료. 자세한 설계는 [[DECISIONS.md]] "스누즈(#1) 크로스디바이스 동기화..." 참고, 상세 내용은 [[CHANGELOG.md]] 41차 참고.
- **그룹 자동 재활성화 + 버튼 문구 고정 + 조롱조 문구 강도별 로테이션(2026-08-12, 42차, 실기기 미검증)**: ① `dailyResetHour` 기준 "오늘" 날짜가 바뀌면 꺼둔 그룹(`groupEnabled=false`)을 자동으로 다시 켜는 `applyDailyGroupResetIfNeeded()` 신규(`AppData.lastGroupAutoResetDate`로 하루 1회만 적용, 매 tick 시작 지점에서 호출). ② 실행확인/잠김 화면 버튼 라벨 "전자"/"후자" → "진행"/"중단"으로 고정(동작은 그대로). ③ 실행확인/잠김 화면 제목("무시당하면서 살기, 무시하면서 살기" 자리)에 조롱조 문구 124개(순한~극한 5단계)를 강도별로 로테이션 — 실행확인은 재확인 레벨(절대 수치, `overlayLevelStepsToMax`와 무관), 잠김(스케줄/일일한도)은 오늘 이 그룹을 열려던 시도 횟수(`Group.blockAttemptDate`/`blockAttemptCount` 신규, 스누즈 카운터와 동일 패턴)가 기준. `MotivationalQuotes.kt`(양 플랫폼)/`quotes.js` 신규 구성. 자세한 설계는 [[DECISIONS.md]] 42차 참고, 상세 내용은 [[CHANGELOG.md]] 42차 참고.
- **사용 중 오버레이 재설계 + 실행확인 가독성 개선(2026-08-12, 43차, 안드로이드만 실배포)**: ① 사용 중 남은시간 오버레이("무시당하면서 살기..." 제목+문구)에서 문구를 완전히 삭제하고 타이머 숫자만 화면 정중앙에 훨씬 크게(안드로이드 32sp→64sp, 브라우저 확장 32px→96px) 표시, 타이머 텍스트 색상도 배경과 같은 alpha 값을 적용해 오버레이의 일부로 취급(레벨 0일 땐 거의 안 보이다가 재확인 레벨이 오를수록 배경과 함께 또렷해짐) — 안드로이드 `AppMonitorAccessibilityService.kt`/브라우저 확장 `overlay.js` 양쪽 적용, 데스크탑 코너 위젯(`UsageOverlayContent.kt`)은 애초에 문구가 없던 구조라 그대로 둠(사용자 확인). ② 실행확인/잠김 화면의 체크포인트 힌트 문구("이게 의무입니까?" 등)를 작고 흐린 텍스트에서 굵고 밝은 텍스트로 가독성 개선 — 안드로이드 `InterstitialScreen.kt`, 데스크탑 `WatchAndWaitScreen.kt`, 브라우저 확장 `confirm.html` 3곳 모두 적용(데스크탑은 아직 재빌드/재배포 안 함). ③ 제목 문구가 길어지면 2줄로 꺾여 가독성이 떨어지던 걸 안드로이드에 자동 축소 로직으로 해결(`InterstitialScreen.kt`) — 처음엔 `maxLines=2`+`didOverflowHeight` 조합으로 구현했다가 "2줄에 정확히 들어가는 대부분의 케이스에선 애초에 안 줄어든다"는 사용자 지적으로 `maxLines=1`+`didOverflowWidth`(한 줄에 안 들어가면 무조건 축소, 최소 55%)로 재작업. 데스크탑은 창 폭이 넓어 줄바꿈 위험이 낮다고 판단해 적용 안 함(사용자 확인). 안드로이드 APK 두 위치 재빌드/배포 2회(1차 축소 로직 포함 → 2차 축소 로직 수정본) 완료.
- **태블릿 제목 축소 제외 + 조롱조 문구 25개 추가(2026-08-12, 44차, 안드로이드만 실배포)**: ① 43차의 제목 자동 축소(`maxLines=1`+`didOverflowWidth`)가 화면 폭 넓은 태블릿에도 똑같이 적용돼 있었는데, 태블릿은 폰과 달리 한 줄로 욱여넣을 필요가 없다는 지적을 받고 `LocalConfiguration.current.screenWidthDp >= 600`(표준 sw600dp 기준)으로 분기 — 태블릿이면 데스크탑 `WatchAndWaitScreen.kt`와 동일하게 축소 없이 `Text(title, style=...)`만 호출해 필요하면 자연스럽게 2줄로 감싸지게 함(`InterstitialScreen.kt`). ② 42차에 만든 5단계(순한~극한) 조롱조 문구에 각 단계 5개씩 총 25개 추가(124→149개) — `MotivationalQuotes.kt`(안드로이드/데스크탑) + `quotes.js`(브라우저 확장) 3곳 내용 동일하게 유지. 안드로이드는 두 변경 모두 포함해 APK 두 위치 재빌드/배포 완료. 데스크탑·브라우저 확장은 문구 소스 파일만 갱신됐고 아직 재빌드/재배포·재로드 안 함(43차부터 밀린 데스크탑 재배포와 함께 처리 권장).
- **공부 중 알림 억제 + 모임 "무작위 알림" 동작 정정 + 자체 업데이트 다운로드 멈춤 수정(2026-09-01, 81차, 실사용 미검증)**: 공부 페이즈 중엔 이 앱 자신의 알림(루틴 리마인더/스트릭/모임 무작위 알림/모임 깨우기·무전기) 4종만 개별 억제하고(시스템 DND 아님), 공부가 끝나면 다시 알림 — 신규 `StudyNotificationGate`(`service/StudyNotificationGate.kt`, 안드로이드만). 깨우기/무전기는 서버(RTDB)에 안 읽힌 채로 남으므로 폴링만 건너뛰면 공부 종료 후 자연 재현되고, 루틴 리마인더/스트릭/무작위 알림은 일회성 알람이라 로컬 큐(SharedPreferences)에 쌓았다가 공부 종료 시 재발송(진동 포함). 모임 "무작위 알림"은 77차 최초 구현이 자동으로 넛지를 대신 보내버렸는데, 사용자가 "내가 알림을 받고 내가 직접 판단해서 깨우는 게 원래 의도"라고 정정 — `checkAndNotifySlackingMembers`로 교체, 자동 발송(`sendSocialGroupNudge`) 제거(안드로이드만). 모임 정보 공유 설정에서 "작동 중인 관리(차단) 그룹" 항목도 완전 제거(멤버 상세 "🗂️ 관리" 탭 전체 포함, **양 플랫폼**). 배포 직후 사용자가 자체 업데이트 다운로드가 "다운로드 중"에서 멈추는 걸 직접 겪어 `UpdateBanner.kt`도 추가 수정(안드로이드만, [[BUGS.md]] 81차 참고). 자세한 설계는 [[DECISIONS.md]] 81차 참고.
- **공부 타이머 진행률 응원 문구**: 목표(시간 또는 사이클 수) 설정 시 진행률에 따라 응원 문구 표시(양 플랫폼, `shared/StudyProgressQuotes.kt`).
- **모임 이름/설명 수정**: 모임장/관리자가 "✏️ 모임 이름/코드 수정" 다이얼로그에서 이름과 함께 설명(선택, 여러 줄)을 수정 가능, 설명은 모임 상세 화면 이름 아래에 표시(양 플랫폼, `SocialGroupSyncClient.updateGroupName`).
- **포인트/보상 시스템**: 공부 10분당 1P·루틴 완료 5P·캘린더 일정 완료 5P·그날 루틴 전부 완료 시 +10P 적립, "루틴" 섹션 "🎁 포인트" 탭에서 사용자가 등록한 보상을 포인트로 교환(양 플랫폼, Firebase `users/{user}/points` 동기화).
- **캐릭터/식물 키우기**: "🎁 포인트" 탭 맨 위에서 누적 획득 포인트(보상 교환으로 줄지 않음) 기준 8단계 식물이 자람(🌰→🌱→🌿→🪴→🌳→🌷→🌺→🌻, `shared/CharacterGrowth.kt`).

---

# 현재 진행 중인 작업

**103차 세션(2026-09-10) — 캐릭터/식물 키우기(게이미피케이션 2차) 신규.**
[[IDEAS.md]] "최우선 후보"(게이미피케이션) 백로그 2번째 항목. 사용자에게 성장 기준(누적 획득 포인트/현재 잔액/누적 공부시간)과 단계 수(3/5/8)를 먼저 확인받아 "누적 획득 포인트·8단계"로 확정. `:shared`에 순수 로직 신규(`CharacterGrowth.kt`, 8단계 임계값+판정 함수), 성장 기준값은 포인트 잔액이 아니라 ledger 양수 delta만 합산한 "누적 획득 포인트"(보상 교환으로 줄지 않음). UI는 "🎁 포인트" 탭 맨 위에 `CharacterGrowthCard` 신규(이모지+단계명+진행바), 새 탭 대신 기존 구조에 얹음(102차와 같은 패턴). 일러스트는 Canvas 드로잉 대신 이모지 텍스트로 구현(코드량 절약, 기존 이모지 UI 관례와 일치). Room 마이그레이션 불필요(기존 `points_ledger`에 쿼리만 추가). 자세한 설계는 [[DECISIONS.md]] 103차 참고. 양 플랫폼 컴파일+릴리스 빌드+호스트 배포(안드로이드 APK 3위치/데스크탑 앱 2위치)+GitHub 릴리스 게시(안드로이드 `android-1789032989`, 데스크탑 `desktop-1789032819`)+공개 저장소 push까지 완료. **실사용 검증은 안 됨.**

(89~102차의 완료된 작업 요약은 전부 [[CHANGELOG.md]]에 세션별로 이미 상세 기록돼 있어 여기서는 제거함 — 과거 세션 내용이 필요하면 CHANGELOG.md에서 해당 회차를 찾을 것)

사용자가 요청했다가 취소한 것(79차): "캘린더에 실제 공휴일 표시"(data.go.kr 인증키 발급이 번거롭다는 이유로 중단) — 다시 요청받기 전까진 손대지 말 것.

---

# 다음 작업 우선순위

- [ ] **103차 캐릭터/식물 키우기 실사용 검증(최우선)** — 포인트가 쌓일 때 실제로 단계가 올라가는지, 보상을 교환해도 캐릭터 단계가 줄지 않는지, 8단계(4000P)까지 도달하면 "최종 단계 도달!"로 정상 표시되는지, 두 기기에서 같은 단계로 보이는지(포인트 동기화에 얹혀가므로 별도 동기화 코드는 없음). [[DECISIONS.md]] 103차 참고.
- [ ] **102차 포인트/보상 시스템 실사용 검증(최우선)** — 공부 세션 종료/루틴 체크/캘린더 일정 완료 시 실제로 포인트가 쌓이는지, 그날 예정 루틴을 전부 체크하면 스트릭 보너스(+10P)가 붙고 하나라도 해제하면 되돌아가는지, 보상 등록/삭제/언락(포인트 차감)이 정상 동작하는지, 두 기기에서 잔액/보상 목록이 동기화되는지. [[DECISIONS.md]] 102차 참고.
- [ ] **101차 모임 이름/설명 수정 기능 실사용 검증** — 모임장/관리자가 실제로 설명을 저장할 수 있는지, 저장된 설명이 모임 상세 화면(양 플랫폼)에 정상 표시되는지, 권한 없는 일반 멤버에게는 수정 메뉴 자체가 안 보이는지.
- [x] **소셜 채팅 전송 안 되는 버그(96차부터 이월) — 99차에 해결.** 원인은 `.indexOn` 색인 누락으로 읽기 조회가 400 거부되던 것, 사용자가 실사용으로 확인 완료.
- [ ] **99차 신규 사항 실사용 검증(최우선)** — 채팅 알림이 실제로 진동 없이 오는지(화면 꺼짐/다른 화면 보는 중에도), 지금 보고 있는 대화방이면 중복 알림이 안 뜨는지, 통계/일정표/타이머 3개 화면 새로고침이 실제로 동작하는지, 캘린더 새로고침 스피너가 실패 상황에서도 제대로 사라지는지. [[BUGS.md]] 99차 참고.
- [ ] **안드로이드 당겨서 새로고침이 스크롤과 겹치는 문제(99차 이월)** — 원인을 못 좁힘, 재현 시 정확히 어느 화면/어느 상황(목록 맨 위 vs 중간)에서 안 되는지부터 파악할 것. 근본 해결은 Material3 pull-to-refresh를 실험적 API에서 안정 API로 옮기는 것(Compose 라이브러리 버전 업그레이드 필요, 다른 화면 영향 커서 신중히 검토).
- [ ] **98차 신규 기능 전체 실사용 검증(최우선)** — 루틴 모드(모드 전환/추가/삭제 시 루틴 재배정/순서변경/모드별 위젯 표시/양 기기 동기화), 온라인/오프라인 모드(수동 토글, 네트워크 끊었을 때 자동 감지+각 화면 동기화 건너뛰기, 게스트 소셜 탭 차단), 계산기/차단규칙편집/소셜공유설정 버그 수정 3건이 실제로 고쳐졌는지. [[BUGS.md]] 98차 참고.
- [ ] **96차 변경사항 실사용 검증** — GroupListScreen 자물쇠 아이콘(태블릿/데스크탑 포함), GroupEditScreen 계산기 스타일(CompactField 직접 입력, 미니 캘린더 날짜 선택), 소셜 채팅 엔터 전송, 실행 확인 화면 커스텀 테마, 태블릿 공부 잠금 정지 버튼. [[BUGS.md]] 96차 참고.

**아래 9개는 90차 세션 종료 시점에 사용자가 직접 지정한 다음 세션 필수 작업이다(순서는 사용자가 말한 순서 그대로, 우선순위 순은 아님) — 다른 항목보다 먼저 처리할 것.**

- [x] **1. 공부(타이머) 잠금 오버레이 UX/UI 개편 — 92차에 완료, 릴리스 빌드/호스트 배포 포함.** `StudyTimerScreen`과 같은 디자인 언어(강조색 배지/radial gradient/큰 원형 진행률 링/카드형 허용앱 목록)로 전면 재구성, 정보(오늘 일정/누적 공부시간) 보강. [[CHANGELOG.md]]/[[DECISIONS.md]] 92차 참고.
- [x] **2. 모바일(안드로이드) 공부 타이머 탭에도 데스크탑에 새로 생긴 것 같은 요소를 넣기 — 92차에 완료.** `TimerIllustration` 안드로이드 이식 완료.
- [x] **3. 데스크탑/태블릿 루틴 화면 오른쪽 컬럼 변경 — 91차에 완료, 릴리스 빌드 포함.** "오늘 요약" 통계를 빼고 루틴 클릭 시 상세 정보(요일/시간대/기간/알림/이 루틴만의 연속 기록)가 뜨는 마스터-디테일로 교체(`RoutineScreen.kt` 데스크탑). 안드로이드는 이 화면에 원래 좌우 분할이 없어 대상 아님.
- [x] **4. 홈 화면 삭제 — 91차에 완료, 릴리스 빌드 포함.** `HomeScreen.kt`(양 플랫폼) 삭제 + `Tab.Home`/`TopSection.HOME` 제거, 첫 진입 섹션은 `visibleSections.first()`/`tabs.first()`로 자동 복원됨.
- [x] **5. 타이머 탭 빈 공간을 뭘로 채울지 재논의 — 92차에 완료, 릴리스 빌드/호스트 배포 포함.** 연속 공부 스트릭 카드/최근 7일 막대그래프/오늘 과목별 도넛 차트 3종으로 결정·구현. 구현 중 발견한 크로스디바이스 동기화 버그도 같이 수정([[BUGS.md]] 92차).
- [x] **6. "모임"(소셜 그룹) 섹션을 "소셜"로 개편 — 93차에 완료.** 이름 변경뿐 아니라 메신저 형태(그룹 대화 채널/1:1 DM/소유권 승계/디자인 통일)로 확장. **이걸로 90차 지정 9개 전부 완료.**
- [x] **7. 자체 업데이트 배너의 "변경 내용" 표시를 한글로만 나오게 — 91차에 완료, 릴리스 빌드 포함.** 원인은 GitHub 자동생성 텍스트가 아니라 세션마다 `gh release create --notes`를 영어/한글 섞어 채워온 것이었음 — android `UpdateBanner.kt`에 한글 음절이 하나도 없으면 숨기는 필터 추가(desktop은 애초에 이 텍스트를 표시하지 않음). 과거 영어 릴리스 노트 자체를 고치는 건 범위 밖.
- [x] **8. 문서 정리 — 91차에 진행.** `HANDOFF.md`의 "현재 진행 중인 작업"에 쌓여있던 60~88차 세션별 서술(전부 [[CHANGELOG.md]]에 이미 상세 기록됨)을 정리하고 89~91차만 남김.
- [x] **9. 공부(타이머) 탭에도 접근성 서비스 꺼짐 경고 배너 추가(안드로이드) — 91차에 완료, 릴리스 빌드 포함.** `StudyTimerScreen.kt`에 `GroupListScreen.kt`와 같은 패턴으로 배너 추가.

---

- [x] **관리 탭 메인 화면(차단 규칙 목록, `GroupListScreen.kt`) UI 개편 — 96차에 완료.** 자물쇠 아이콘 방식으로 확정, 태블릿/데스크탑까지 통일 적용. [[CHANGELOG.md]] 96차 참고.
- [ ] **95차 안드로이드 자체 업데이트 다운로드/설치 수정 실사용 검증(최우선)** — 시스템 DownloadManager를 앱 내부 직접 HTTP 다운로드로 교체했다(81차부터 반복된 "다운로드 멈춤" 제보 대응). GitHub 릴리스([android-1788711405](https://github.com/studybeultaeon-svg/study-planner/releases/tag/android-1788711405) 이상)에서 수동 설치 후 실제로 업데이트 버튼이 끝까지 동작하는지 확인 필요 — 안 되면 정확히 어느 단계(진행률/설치화면)에서 멈추는지부터 파악할 것. [[BUGS.md]] Open 참고.
- [ ] **94차 그룹 설정 동기화 opt-in 개편 실사용 검증(최우선)** — 두 기기에서: 한 기기가 동기화 켠 규칙이 다른 기기 "불러오기" 목록에 실제로 뜨는지, 불러오면 설정만 반영되고 앱/사이트 목록은 그대로인지, 새 규칙 생성 시 이름 충돌 확인창 예/아니오 양쪽 분기, 로컬 규칙 동기화 토글 켤 때 충돌 확인, 차단 규칙 꺼짐 시 카드 흐림 표시가 라이트/다크/커스텀 테마 전부에서 잘 보이는지. 코드/빌드/배포/GitHub 게시까지 완료(안드로이드 versionCode `1788703336`, 데스크탑 BuildInfo `1788703299`).
- [ ] **93차 신규 소셜 메신저 기능 실사용 검증(최우선)** — 그룹 "💬 대화" 채널에서 실제로 메시지가 양쪽 기기에 4초 폴링으로 도착하는지, 이모지 리액션이 반영되는지, 1:1 DM을 커스텀 아이디로 검색해 시작하고 대화 목록에 뜨는지, 모임장 소유권 승계가 실제로 넘어가는지(넘긴 사람이 관리자가 되는지)를 최소 두 기기/두 계정으로 확인할 것. 코드/빌드/배포/GitHub 게시까지는 완료(데스크탑 BuildInfo `1788695394`, 안드로이드 versionCode `1788695472`), Firebase 규칙(`groupChats`/`dmChats`/`dmChatIds`)도 사용자가 콘솔에 게시 완료.
- [ ] **92차 신규/수정 사항 전체 실사용 검증(최우선)** — 잠금 화면 새 디자인이 실제 공부 잠금 시 정상 표시되는지(특히 원격 잠금 케이스), 타이머 탭 스트릭/주간그래프/도넛 차트가 실제 여러 날짜·다른 기기 기록과 함께 정확히 집계되는지(이번에 고친 동기화 버그의 실제 검증), 안드로이드 타이머 탭 일러스트 표시. 호스트 실제 교체 배포까지 완료(데스크탑 BuildInfo `1788690197`, 안드로이드 versionCode `1788690271`), GitHub 게시는 아직 안 함.
- [ ] **91차 신규/수정 사항 전체 실사용 검증(최우선)** — 홈 화면 삭제 후 첫 진입 화면이 예상대로인지, 데스크탑 루틴 오른쪽 컬럼 클릭→상세 패널이 실제 클릭에서 잘 동작하는지, 공부 탭 접근성 배너가 실제로 뜨고 사라지는지, 자체 업데이트 배너가 한글 릴리스 노트에서 정상 표시되는지(도움말 관련 항목은 97차에 기능 자체가 삭제돼 제외). 릴리스 빌드까지는 완료(안드로이드 versionCode `1788661502`, 데스크탑 BuildInfo `1788661637`), 이 호스트 실제 교체 배포/GitHub 게시는 92차에 완료(더 최신 빌드로 교체됨).
- [ ] **87차 신규/수정 사항 전체 실사용 검증(최우선)** — 스누즈 on/off+횟수 그룹별 설정, 재설계된 색상 피커(스펙트럼+슬라이더), 15분 주기 자체 업데이트 체크, 태블릿 레이아웃 5개 화면. 위 "현재 진행 중인 작업" 87차 항목 참고.
- [ ] **85차 신규/수정 사항 전체 실기기 검증(최우선)** — 캘린더 자연정렬, 자체 업데이트 배너 오류 표시, 요일별 목표 overlayStepper 필드, 날짜 필드 폰트, 5탭 아이콘/라벨 배치, 설정 동기화(다회독 기본값/초기화 시각), 4종으로 줄어든 테마 선택 화면. 전부 코드·컴파일 검증까지만 끝났고 실기기 미검증.
- [ ] **83차 신규 기능 4건 전체 실기기 검증(최우선)** — 다회독 상세화(Room v34 마이그레이션이 기존 진행 상태 보존하는지 포함)/태블릿 UI(NavigationRail/ResponsiveSplit)/계산기 미니캘린더·스테퍼/캘린더 자동생성 요일·휴일 반영. 전부 코드·컴파일 검증까지만 끝났고 실기기 미검증.
- [ ] **Firebase 콘솔 확인(낮은 우선순위, 사용자가 보류 결정, 2026-09-02)** — Android/Desktop API 키 불일치는 동기화가 실제로 잘 작동해온 점에서 버그가 아닐 가능성이 높다고 판단해 지금은 확인 안 하기로 함. "이 기기만 동기화 이상함" 증상이 실제로 생기거나 클라우드 백업 기능을 쓰고 싶을 때 재검토. [[BUGS.md]] Open 참고.
- [ ] **81차 자체 업데이트 다운로드 수정 최우선 검증** — 실제로 다운로드가 끝까지 진행되고 진행률이 표시되는지, 실패하면 사유가 뜨는지. 이 기능이 계속 안 되면 사용자에게 GitHub 릴리스 페이지에서 apk 수동 설치를 안내.
- [ ] **81차 나머지 3건(알림 억제/무작위 알림 정정/관리 공유 제거) 실사용 검증** — 공부 페이즈 진입 시 루틴/스트릭/모임 알림이 실제로 안 오는지, 공부 종료 시 큐에 쌓인 루틴 알림이 진동과 함께 다시 오는지, 깨우기/무전기가 자연스럽게 재폴링되는지, 모임 "무작위 알림"이 이제 자동 발송 없이 정보 알림만 오는지, 공유 설정에서 "관리" 항목이 실제로 사라졌는지.
- [ ] **80차 신규 기능 4건 전체 실사용 검증** — 특히 데스크탑 오버레이 전체화면 클릭-통과가 실제로 아래 프로그램 클릭을 그대로 통과시키는지(가장 리스크 큼), 다회독 토글이 이제 화면 이동 없이 즉시 바뀌는지, 설정 > 공부의 새 기본값 토글이 실제 새 일정에 반영되는지.
- [ ] **79차 신규 기능 8건 전체 실사용 검증** — 자체 업데이트 무한반복 해소 여부, 종료확인 토글 20문항, 반응형 레이아웃, 테마 커스텀 대비.
- [ ] **78차 신규 기능 전체 실사용 검증** — 태블릿 릴스/쇼츠 차단, 모임 멤버 상세 "일정표" 탭, 무전기 남성 목소리.
- [ ] **77차 신규 기능 전체 실사용 검증** — 모임 멤버 상세 탭 구조, 관리자 승격·해제·내쫓기·이름수정, 무작위 알림, 캘린더 3단계 회독.
- [ ] **무전기 재설계 전체 실사용 검증(73~74차 이월)** — 모임별 설정 저장/불러오기, 요일·시간대 일정 다중 등록, 알림/음성/텍스트 3종 전송·수신, TTS 재생 여전히 미검증.
- [ ] **기존 Google 로그인 사용자의 재가입 안내(72차 이월)**: 로그인 방식이 바뀌면서 예전 프로필과 연결이 끊긴 계정들에게 재가입 신청 + 관리자 재승인이 필요하다는 걸 안내할 것.
- [ ] **별도 웹앱 "공부앱"의 뽀모도로 연동 복구 여부 결정(61차 이월)**: 로그인 필수 전환으로 끊긴 연동을 복구할지 포기할지 미결정.
- [ ] [[IDEAS.md]] "타이머 크로스디바이스 미러에 원격 정지/전환 제어 추가 검토" — 사용자가 실사용 후 불편하면 검토.

---

# 현재 주의사항

- **그룹 on/off는 `groupEnabled` 필드를 직접 편집하는 방식으로 절대 대신 처리하지 말 것(86차, 사용자가 강하게 지적)**: `data.json`에서 `groupEnabled`를 직접 `false`로 바꿔서 그룹을 끄려고 시도했다가 사용자에게 "off 버튼을 누른 것처럼 하라고 문서에 적혀있다"는 지적을 받고 원상복구함. **실제 이유**: `GroupListScreen.kt`의 진짜 off 스위치는 단순 필드 변경이 아니라 — 오늘 적용되는(스케줄/일일한도/실행확인 중 하나라도 오늘 요일에 걸리는) 그룹이면 `groupOffPending=true`로 회유 멘트 20개(`PERSUASION_MESSAGES`)를 랜덤 딜레이로 하나씩 통과해야 실제로 `groupEnabled=false`가 되는 절차(`LockEvaluator.effectiveGroupEnabled` 참고)를 강제한다 — 이게 이 앱의 핵심 자기통제 장치라 파일을 직접 고치면 그 저항 절차 자체를 통째로 우회하게 된다. **그룹을 끄고 싶다는 요청을 받으면 앱의 실제 스위치를 사용자가 직접 누르게 안내할 것 — 대신 파일을 고쳐주지 말 것.** 86차 세션 중 실수로 편집했다가 즉시 백업으로 원상복구(8개 그룹 전부 다시 `groupEnabled: true`), watchdog는 편집 전후로 정상적으로 disable/enable 처리함.
- **82차 발견: 호스트에서 Gradle 컴파일 확인 시 OneDrive 경로(한글 폴더명)를 직접 쓰면 실패한다** — `generateDebugBuildConfig` 등에서 `AccessDeniedException`(실은 JVM 네이티브 인코딩이 한글 경로를 못 읽는 문제, chcp/파일.encoding -D 옵션으로도 해결 안 됨). 컴파일만 빠르게 확인할 땐 아무 ASCII 경로에나(예: `C:\build\phonelock-android`) `robocopy "<OneDrive 원본>" "<스크래치 경로>" /MIR /XD build .gradle .idea` 로 미러 후 `gradle compileDebugKotlin`/`compileKotlin` 실행 — 이런 스크래치 경로는 세션이 끝나면 지워도 됨(커밋 대상 아님). **실제 릴리스 빌드(`assembleRelease`/`packageMsi`/`createDistributable`)는 반드시 아래 "실행 방법"의 공식 경로**(`AndroidBuilds\phone-lock-android`, `C:\build\phone-lock-desktop`)를 쓸 것 — 이름이 비슷해 혼동하기 쉽다. JDK는 안드로이드 `~/jdk21-temurin` 또는 Android Studio JBR, 데스크탑은 jpackage 필요해 반드시 `~/jdk21-temurin`. Gradle은 `~/.gradle/wrapper/dists/gradle-8.7-bin/.../bin/gradle`(gradlew 없음, 이 경로로 직접 호출). 소스 편집은 항상 OneDrive 원본에서.
- **82차 발견: `shared/` composite build 모듈은 각 플랫폼 빌드 경로의 형제 폴더로도 미러해야 한다** — `phone-lock-android`/`phone-lock-desktop`의 `settings.gradle.kts`가 `includeBuild("../shared")`로 상대경로 참조하므로, `AndroidBuilds\phone-lock-android`를 쓰면 `AndroidBuilds\shared`도, `C:\build\phone-lock-desktop`을 쓰면 `C:\build\shared`도 함께 최신으로 robocopy 미러해야 한다(둘 다 안 하면 "찾을 수 없는 모듈" 에러). `sync-public-repo.ps1`도 82차에 `shared/` 동기화를 포함하도록 수정됨 — 최상위에 새 Gradle 프로젝트(모듈)를 또 추가하는 일이 생기면 이 스크립트가 그 프로젝트를 아는지 항상 함께 확인할 것.
- **재확인/escalation 판정 로직에는 절대 손대지 말 것** — 표시값(오버레이 알파 등)과 판정 로직(언제 재확인을 요구할지)은 별개. 과거 이 원칙을 어겨 큰 삽질이 있었음([[DECISIONS.md]] "표시값과 판정 로직 분리" 참고). `ConfirmationGate.kt`(양쪽)는 원본 그대로 유지되어야 함.
- **사용자가 "표시/UI가 이랬으면"이라고 요청했을 때 보안/판정 로직까지 확대 해석하지 말 것**. 애매하면 먼저 확인.
- **네이티브 UI를 웹앱과 맞출 땐 스크린샷 추측 대신 `공부앱/index.html`의 CSS/JS부터 확인**(32차 세션 교훈) — 관련 클래스명을 Grep해서 정확한 hex값/구조를 얻는다. [[DECISIONS.md]] "UI를 웹앱과 맞출 때는..." 참고.
- 로컬 API에 새 엔드포인트 추가 시 호출 주체(content script vs 서비스워커/확장페이지)에 따라 CORS 정책 결정 — [[DECISIONS.md]] 참고.
- Compose Desktop `Window`는 클릭을 가로챈다 — 화면을 덮는 새 UI 만들 때 항상 고려.
- `EnforcementService.tick()`의 뮤텍스 범위를 건드릴 땐 "확인 대기는 뮤텍스 밖" 원칙 유지(안 지키면 다른 그룹 감시 전체가 멈추는 예전 버그 재발).
- 안드로이드 `PhoneLockRepository`에서 `escalationCache`/DB 접근 새 함수 추가 시 반드시 `escalationMutex.withLock { }`으로 감쌀 것(`mergedEscalation()` 자체는 잠금 없음, 재진입 데드락 주의).
- **소스는 항상 OneDrive 원본에서 편집**, 빌드는 항상 `AndroidBuilds\`에서, Android APK는 항상 두 위치 모두 갱신.
- **데스크탑 실행/테스트는 절대 `gradle run`으로 하지 말 것(61차, 중요)**: `Watchdog.kt`가 자기 자신을 재시작할 때 쓰는 실행파일 경로가 `gradle run`에선 `java.exe`가 돼버려서, 감시 프로세스가 5초마다 실패-재시도를 반복하며 좀비 `java.exe`가 순식간에 수백 개 쌓인다. 실기기 동작 확인은 항상 `createDistributable`로 빌드한 패키징된 `.exe`로 할 것. [[BUGS.md]] 61차 참고.
- **새로 작성하는 `.ps1` 스크립트에 한글 텍스트(주석 포함)를 넣지 말 것(61차)**: BOM 없는 UTF-8 `.ps1`을 Windows PowerShell 5.1이 시스템 코드페이지로 잘못 읽어서, 한글이 섞인 지점 이후의 코드가 예외 없이 조용히 오동작할 수 있다(경로 문자열이면 즉시 예외, 주석이면 더 뒤에서 원인 불명의 null 결과로 나타남). 영문 주석만 쓰고, 경로는 하드코딩 대신 `$PSScriptRoot`/상대경로를 쓸 것. [[BUGS.md]] 61차 참고.
- **빌드는 원칙적으로 VM_BUILD_HANDOFF.md 절차대로 별도 VM에서(73차 재확인)**: 호스트엔 실제 운영 중인 워치독+프로덕션 Firebase가 있어 리스크가 있다는 이유로 VM 격리가 정해져 있었다. 73차엔 사용자가 명시적으로 "여기서 진행해"라고 요청해서 호스트에서 직접 빌드/배포했고, 그 과정에서 호스트에 이미 Gradle 8.7 캐시(`~/.gradle/wrapper/dists/gradle-8.7-bin`)가 있다는 걸 확인 + jpackage 포함 Temurin JDK 21을 새로 받아 `~/jdk21-temurin`에 둠(Android Studio 번들 JBR은 jpackage가 없어 `createDistributable`엔 못 씀, Gradle 8.7 자체는 JDK 25에서 못 돌아감 — Kotlin DSL 스크립트 컴파일러가 "25.0.2" 버전 문자열을 파싱 못 함). **다음에도 호스트에서 직접 빌드하려면 매번 사용자에게 다시 확인받을 것** — VM 없이 가능하다는 게 검증됐다고 해서 원칙이 자동으로 바뀐 건 아니다.
- **git push 금지**: 명시적 요청 없이 절대 push 금지. **단, 예외로 `sync-public-repo.ps1`(77차 신규)을 통한 `study-planner` 저장소(`origin/main`) 동기화는 사용자가 "알아서 해"라고 명시적으로 위임했으므로, 소스(`phone-lock-android`/`phone-lock-desktop`) 변경이 있을 때마다 요청 없이 먼저 실행해도 된다** — 이 스크립트는 내부 문서(BUGS/CHANGELOG/DECISIONS/HANDOFF/IDEAS 등)는 절대 건드리지 않고 소스+`.gitignore`+`.claude/launch.json`만 옮기므로 범위가 좁다. 그 외 모든 git push(다른 브랜치/저장소, 강제 push 등)는 여전히 매번 확인 필요.
- **데스크탑 배포 전**: 반드시 프로세스 먼저 종료(파일 잠김 방지) — 요청 없이 직접 종료해도 됨. 배포 검증 절차(프로세스 종료→동기 robocopy→FAILED 0 확인→jar 해시 비교→재실행)는 [[BUGS.md]] "robocopy가 실행 중인 프로세스의 파일을 조용히 스킵" 참고. **78차 발견**: 데스크탑 앱은 인메모리 워치독이 있어 `Stop-Process` 직후 1초 이내에 스스로 재기동할 수 있다 — robocopy가 exe를 못 옮기고 `ERROR 32`로 30초 간격 무한 재시도에 빠지면, 기다리지 말고 즉시 `Stop-Process`를 한 번 더 실행한 뒤 곧바로 robocopy할 것([[BUGS.md]] 78차 참고).
- **Android APK**: 코드 수정 후 요청 없어도 바로 빌드/업로드 가능. **78차부터 기본은 `assembleRelease`(release 서명 APK)** — `assembleDebug`는 컴파일만 빠르게 확인할 때만 쓰고, 실제 배포/게시용은 항상 release. 배포 시 위치는 3곳 모두 갱신: `AndroidBuilds\phone-lock-app-release.apk`, OneDrive 원본 `app\build\outputs\apk\release\app-release.apk`, `vm-build-output\android\app-release.apk`.
- **"깃허브에 올려/커밋해"는 GitHub Release 게시까지 포함한다(78차 확정)**: 소스를 `sync-public-repo.ps1`로 공개 저장소에 커밋/푸시하는 것만으로 끝내지 말고, `gh release create`로 태그된 릴리스(Android `android-<versionCode>`, Desktop `desktop-<BuildInfo.BUILD_TIMESTAMP>`)까지 만들 것 — 안 하면 자체 업데이트 기능이 참조하는 GitHub Releases가 갱신 안 돼 "확인해도 최신 버전"이라고 잘못 뜬다.
- **빌드 스크립트 파일(`build.gradle.kts`/`settings.gradle.kts`)도 소스 동기화 대상이다(78차 발견)**: `robocopy /MIR`로 `src/`만 반복 동기화해오다가, 75차에 추가된 `generateBuildInfo` 태스크가 `AndroidBuilds\phone-lock-desktop\build.gradle.kts`엔 한 번도 반영 안 된 채 방치돼 컴파일이 실패한 적이 있다([[BUGS.md]] 78차 참고) — 빌드 스크립트 자체가 바뀌는 세션 이후엔 이 파일들도 diff/재복사할 것.
- **데스크탑 `generateBuildInfo` 태스크는 항상 재실행하도록 고쳐져 있다(98차 발견/수정)**: 원래 `outputs.dir(...)`만 선언하고 inputs가 없어서, 같은 `C:\build\phone-lock-desktop` 디렉터리에서 여러 번 빌드하면 Gradle이 UP-TO-DATE로 캐싱해버려 며칠 전 타임스탬프가 그대로 남는 버그가 있었다(자체 업데이트 버전 비교가 이 값에 의존) — `outputs.upToDateWhen { false }`로 고쳤다. 만약 앞으로 이 태스크를 다시 손대게 되면 이 강제 재실행 설정을 지우지 말 것.
- Room DB 버전: 스키마 변경 시 `AppDatabase.kt`의 `version` 올려야 함(현재 38 — 38차 21→22(`ConfirmCounter`), 39차 22→23(`StudyLogEntry.note`)→24(`AppGroup` 스누즈/기간지정 필드 6종), 42차 24→25(`AppGroup.blockAttemptDate`/`blockAttemptCount`), 48차 25→26(`Routine`/`RoutineLog`), 52차 26→27(`Routine.icon`/`notifyEnabled`/`startDate`/`endDate`), **82차부터 v28~v37은 전부 명시적 `MIGRATION_x_y` 사용(아래 항목 참고), 98차 37→38(`routine_mode` 테이블+`Routine.modeId`, `MIGRATION_37_38`)**) — v27까지는 `fallbackToDestructiveMigration()`(마이그레이션 코드 불필요), v28부터는 반드시 명시적 마이그레이션 필요(아래 82차 항목 참고). `PreMigrationBackup`이 삭제 전 raw SQLite 백업(TABLES 목록에 `routine`/`routine_log`/`routine_mode` 포함 확인할 것).
- **Room 버전을 올릴 땐 반드시 `AppPreferences.resetSyncTimestamps()`가 호출되는지 확인할 것(52차, 심각한 버그였음)**: `fallbackToDestructiveMigration()`은 Room(SQLite)만 지우고 캘린더/루틴/계산기의 "마지막 동기화 시각"(SharedPreferences 저장)은 그대로 남겨서, 다음 동기화가 "로컬이 이미 최신"으로 오판해 텅 빈 로컬 상태를 방치하는 버그가 있었다. `MainActivity.onCreate`에서 `PreMigrationBackup.backupIfVersionChanged()`가 non-null을 반환하면(=이번 실행에서 버전이 바뀌어 백업했으면) 자동으로 리셋되므로 이 호출 자체를 지우지 말 것 — 새 SharedPreferences 기반 동기화 타임스탬프를 추가할 땐 `resetSyncTimestamps()`에도 같이 넣을 것. [[DECISIONS.md]]/[[BUGS.md]] 52차 참고.
- **그룹(차단 대상 목록)은 Firebase에 동기화되지 않는다** — Room이 destructive migration으로 지워지면 그룹은 자동 복구가 안 되고, `PreMigrationBackup`이 만든 raw SQLite 백업 JSON에서 `PhoneLockRepository.restoreGroupsFromBackup()`(설정 화면 "⚠ 그룹 데이터 복구" 카드)으로만 복구 가능(52차 신설). 앞으로 이 종류의 로컬 전용 데이터를 다루는 새 테이블을 추가하면 같은 유실 위험이 있다는 걸 염두에 둘 것.
- **화면 문구를 바꿔달란 요청을 받으면 그 문구가 제목(title)인지 부제(quote)인지 먼저 화면에서 확인할 것(42차 교훈)**: `WatchAndWaitScreen`/`InterstitialScreen`은 title(큰 헤드라인)과 quote(작은 부제) 두 개의 별도 텍스트 슬롯을 갖는데, 42차에 "무시당하면서 살기, 무시하면서 살기"(title) 교체 요청을 받고도 실제로는 quote 슬롯(원래 "의무에 따라 행동하세요")에 새 문구를 걸었다가 사용자 지적으로 뒤늦게 바로잡았다. 사용자가 인용한 문구 원문과 코드의 어느 파라미터가 실제로 그 문자열을 갖고 있는지 대조부터 할 것.
- **크로스디바이스 신규 동기화 추가 시(34차 결정)**: `pomodoro`/`confirmSync`처럼 "신호"만 필요하면 24차 패턴(로컬이 source of truth, Firebase는 부가 채널)을 따르고, `dailyUsage`/`studyLog`처럼 "여러 기기가 각자 추가하는 실제 데이터"면 문서 전체 LWW(캘린더/계산기 방식) 대신 반드시 "기기별 키에만 쓰고 읽는 쪽이 합산"하는 패턴을 쓸 것 — 문서 전체 LWW를 쓰면 다른 기기가 그 사이 추가한 항목이 통째로 사라진다. **원격 명령(remoteCommand) 패턴은 다시 쓰지 않는다** — 19차 세션에 도입했다가 24차에 신뢰성 문제로 완전히 제거한 전례가 있음, 크로스디바이스 제어가 필요하면 먼저 사용자와 상의.
- **Firebase 관련 버그 디버깅 시(34차 교훈)**: 코드만 보고 추측하지 말고, 로컬 데이터파일(데스크탑 `%APPDATA%\PhoneLockDesktop\data.json`)에서 `fbDatabaseUrl`/`fbApiKey`/`fbUser`를 읽어 REST API로 실제 서버 상태를 직접 조회하면 어느 기기의 어느 단계(쓰기/읽기/UI 반영)에서 막히는지 빠르게 좁힐 수 있다. 익명 인증: `POST https://identitytoolkit.googleapis.com/v1/accounts:signUp?key={apiKey}` → `idToken`으로 `{databaseUrl}/{경로}.json?auth={idToken}` 조회.
- **`dailyResetHour` 설정값 확인(35차 발견)**: 데스크탑 로컬 데이터파일(`%APPDATA%\PhoneLockDesktop\data.json`)의 `dailyResetHour`가 현재 **9**(오전 9시 리셋)로 설정돼있다 — `effectiveDate()`(`Repository.kt:13`)가 자정이 아니라 이 값을 기준으로 "오늘"을 판정하므로, 리셋 시각 전이면 캘린더 일정 선택(타이머 드롭다운)·공부기록·일일 사용한도가 전부 아직 "어제"로 계산된다. 버그 아님 — 사용자가 이 값을 유지하기로 결정(35차). "왜 어제 기록/일정이 오늘도 보이냐"는 문의가 다시 오면 이 설정부터 확인.
- **robocopy는 반드시 PowerShell 도구로, 절대경로로 실행할 것(36차 발견, 매우 중요)**: `robocopy "OneDrive 원본\...\src" "AndroidBuilds\...\src" /MIR`를 Bash 도구로(특히 상대경로+`cd` 조합) 실행하면 이 프로젝트의 한글 경로(`OneDrive\바탕 화면\클로드\관리앱`) 때문인지 "성공"처럼 보이는 출력을 내면서 실제로는 파일을 하나도 안 옮긴다. 이 상태에서 아무리 재빌드해도 `AndroidBuilds`의 옛날 소스만 컴파일되고, Gradle이 `compileKotlin`/`jar`를 `UP-TO-DATE`로 스킵하기까지 하면 "BUILD SUCCESSFUL"만 보고 정상 배포된 것으로 착각하기 쉽다(36차에 실제로 이 착각으로 여러 차례 "재배포 완료"를 잘못 보고함). robocopy 출력에 실제로 "Newer"/"New File" 줄이 찍히는지 반드시 확인하고, 의심스러우면 `grep -a <새로 추가한 심볼> AndroidBuilds\...\<파일>`로 소스 동기화부터 검증할 것. 메모리 `feedback_robocopy_use_powershell` 참고.
- **"고쳤는데 반영이 안 된다"가 반복되면 UI/상태 로직을 더 파기 전에 배포된 바이너리부터 검증(36차 교훈)**: 배포된 jar/apk를 `unzip`으로 풀어 `.class` 파일을 `grep -a`로 열어보면(바이너리지만 문자열 상수는 grep으로 보임) 수정한 필드명/문자열이 실제로 컴파일돼 들어갔는지 바로 확인 가능 — 코드 로직을 계속 재검토하는 것보다 훨씬 빠르게 "빌드/배포 파이프라인 문제 vs 진짜 로직 버그"를 구분할 수 있다.
- **크로스디바이스 기능이 "판정 로직 자체"를 확장할 땐 원본 파일을 건드리지 말고 호출부에서 병합할 것(37차 패턴)**: 실행확인 유예시간을 다른 기기와 공유하는 기능을 추가할 때, `ConfirmationGate.kt`(재확인/escalation 판정, 원본 유지 필수 — 위 항목 참고)를 직접 고치지 않고, `Repository.syncedLastConfirmedAtEpochMillis()`(이미 있던 `mergedEscalation` 재사용)로 동기화된 값을 따로 얻은 뒤 호출부(`EnforcementService`/`SiteEnforcement`/안드로이드 `AppMonitorAccessibilityService`)에서 로컬 `ConfirmationGate` 값과 비교해 더 최근인 쪽을 쓰는 방식으로 구현했다. "절대 손대지 말 것" 규칙이 있는 파일을 확장해야 할 필요가 생기면 이 패턴(원본은 그대로, 호출부에서 병합)을 우선 검토할 것.
- **락 안에서 블로킹 네트워크 I/O를 하면 앱 전체가 정지할 수 있다(38차 재확인)**: 새 push/read 함수를 추가할 때 항상 "이 함수가 `synchronized(lock)`/`tickMutex` 등 락을 쥔 채로 호출되는가"를 먼저 확인할 것 — 쥔 채라면 실제 네트워크 I/O는 반드시 락 밖(별도 Thread 또는 캐시값 즉시 반환+백그라운드 갱신)으로 빼야 한다. 38차에 `pushUsageToFirebase`/`peerUsageSeconds`에서 이 원칙이 깨진 걸 발견해 수정했다([[BUGS.md]] 38차 참고) — 단, escalation 계산(`mergedEscalation()`)처럼 판정 로직에 인접한 함수는 같은 유형의 버그라도 신중하게 범위를 좁혀 보수적으로 접근할 것(이번엔 그 함수는 손대지 않고 남겨둠).
- **이 프로젝트 세션 환경은 매번 빌드 툴체인이 있다는 보장이 없다(38차 재확인)**: 38차엔 gradle 자체가 없어 컴파일 확인을 한 번도 못 한 채 세션이 끝났다 — `project_build_toolchain_missing` 메모리 절차를 먼저 시도해보고, 그래도 안 되면 코드 변경 후 "컴파일 미검증"임을 명시적으로 문서(HANDOFF/BUGS/CHANGELOG)에 남길 것. 과거 반영이 안 됐던 사고들이 전부 "됐다고 보고했는데 실제로는 안 됨" 패턴이었으므로, 검증 못 한 걸 검증했다고 절대 표현하지 않는다.
- **원격 신호로 실제 잠금을 거는 기능은 반드시 신선도(staleness) 컷오프를 둘 것(37차)**: 34차의 "미러 표시"는 신호가 오래돼도 화면이 좀 이상하게 보이는 정도지만, 37차의 "원격 신호로 실제 공부 잠금을 건다"는 기능은 신호를 올리던 기기가 정지 없이 꺼지면 이 기기가 영구히 잠길 수 있는 심각한 문제로 이어진다. 34차에 이미 쓰던 20분 컷오프(`REMOTE_STUDY_SIGNAL_STALE_MS`, `remoteUpdatedAtMillis()` 기준)를 재사용해서 20분 넘게 갱신 없는 신호는 무시하도록 했다 — 앞으로 원격 신호 기반의 새 "강제 동작"(잠금, 차단 등)을 추가할 때 항상 이 안전장치를 함께 넣을 것.
- **`LockEvaluator`를 확장해야 하는 새 기능은 `isPomodoroUnlocked()` 패턴을 우선 검토할 것(39차, 스누즈/기간 지정 자동 강화에 적용)**: "판정을 일시적으로 우회하되 `groupEnabled` 등 영구 상태는 안 건드리는" 기능(스누즈처럼)은 `evaluate()`/`isCurrentlyRestricting()`/`isConfirmActiveNow()` 최상단에 `isPomodoroUnlocked()`와 나란히 체크를 추가하는 게 안전하다(이미 검증된 패턴 재사용, `ConfirmationGate.kt`는 안 건드림). 반대로 "스위치 자체를 강제로 뒤집는" 기능(기간 지정 자동 강화처럼)은 `isGroupActive()`/`effectiveGroupEnabled()` 레벨에서 처리하되, `now` 파라미터를 기본값(`LocalDateTime.now()`)으로 추가해 기존 호출부(`isGroupActive(group)`)가 전부 그대로 컴파일되게 했다 — 이 방식이면 새 필드가 비어있는 기존 그룹은 동작이 100% 그대로 유지된다. 스누즈/기간 지정이 동시에 걸리면 기간 지정이 우선하도록 순서를 정했다([[DECISIONS.md]] 39차 참고) — 앞으로 판정 우회 기능이 여러 개 겹칠 때도 "미리 설정해두는 강제 규칙 > 즉흥적 예외"를 기본 순서로 검토할 것.
- **Compose에서 리스트 상태 토글이 "다른 탭 갔다와야 반영된다"는 재현을 받으면 구조적 동등성 리컴포지션 스킵부터 의심할 것(50차)**: `mutableStateOf<List<T>>`을 값이 동일한 새 리스트로 재할당하면 Compose가 변경을 감지 못 해 리컴포지션을 건너뛴다 — 그룹 탭(과거)과 루틴 오늘 탭(50차) 둘 다 이 유형이었다. 항상 증가하는 `refreshTick: Int`를 `key(refreshTick){...}`으로 감싸는 패턴이 표준 해법으로 자리잡았다.
- **가변/고정 개수 항목을 좁은 화면에 맞출 때 스크롤보다 줄바꿈(`FlowRow`)/균등분할(`weight`)을 먼저 검토할 것(50차, 요일 피커+계산기 연동 버튼에서 확인)**: `horizontalScroll`로 구현했다가 "여전히 안 보인다"는 재현을 두 번(44차 요일 피커, 50차 계산기 연동 버튼) 받았다 — 항목 수가 고정이면 `weight(1f)` 균등분할로 스크롤을 아예 없애고, 가변이면 `FlowRow`로 여러 줄 자동 줄바꿈하는 쪽이 이 사용자 환경에서 훨씬 안정적이었다. [[DECISIONS.md]] 50차 참고.
- **크로스디바이스 ID 참조가 필요한 새 동기화 기능은 로컬 auto-increment ID를 그대로 Firebase에 올리지 말 것(50차, 루틴 동기화)**: 안드로이드 Room auto-increment와 데스크탑 수동 카운터는 같은 항목이라도 기기마다 다른 로컬 ID를 가질 수 있다. 루틴 동기화는 배열 인덱스로 부모를 참조하는 방식(`routinesToJson`/`routinesFromJson`)으로 이 문제를 피했다 — 앞으로 "부모-자식 관계가 있는 데이터"를 처음 동기화할 때 이 패턴을 먼저 검토할 것.
- **로컬 auto-increment ID를 OS 시스템 API(AlarmManager 등)의 식별자로도 쓸 땐, 그 ID를 가진 로컬 레코드를 지우기 전에 반드시 시스템 쪽 예약도 먼저 취소할 것(74차, 심각한 버그였음)**: 루틴 알림 예약이 `routine.id`를 `AlarmManager` requestCode로 썼는데, `syncRoutinesFromFirebase()`가 매번 로컬 루틴을 통째로 지우고 새 Room ID로 재삽입하면서 옛 ID의 예약 알람은 취소할 방법이 영영 사라져 동기화할 때마다 계속 쌓였다 — 결국 안드로이드 앱당 예약 알람 500개 한도(`IllegalStateException`)를 넘겨 앱이 열릴 때마다 죽는 크래시 루프가 됐다(사용자가 넘겨준 실제 크래시 로그로 확정, `PhoneLockApplication.kt`의 전역 크래시 로거가 결정적이었음 — 로그 없이는 무전기 쪽을 잘못 의심했었다). 수정은 삭제 직전에 옛 레코드들의 시스템 예약을 먼저 취소 + 이미 쌓인 것은 ID 범위를 훑어 일회성으로 정리(`RoutineAlarmScheduler.cleanupLeakedAlarmsIfNeeded`) + 한도 초과 자체도 `runCatching`으로 방어. 앞으로 "로컬 ID를 그대로 OS/외부 시스템의 키로 재사용"하는 기능을 추가하면 이 패턴(레코드 삭제 = 그 ID로 건 시스템 예약도 함께 취소)을 항상 짝지어 구현할 것.
- **안드로이드 알림 채널은 한 번 만들어지면 앱이 코드로 재정의할 수 없다(61차에 처음 겪었지만 74차에도 반복)**: `enableVibration`/`vibrationPattern` 같은 채널 설정을 나중에 코드로 바꿔도, 그 채널 ID로 이미 한 번 `createNotificationChannel()`이 호출된 적 있는 기기에는 반영되지 않는다(안드로이드 정책, 사용자가 시스템 설정에서 직접 바꿔야 함). 61차 루틴 알림 채널, 74차 모임 깨우기/무전기 메시지 채널 전부 이 문제로 채널 ID를 `_v2`로 새로 발급해 우회했다 — **알림 채널 설정(중요도/진동 등)을 바꾸는 코드를 배포하기 전에, 이미 그 채널 ID로 배포된 적이 있는지부터 확인하고, 있다면 처음부터 새 채널 ID를 쓸 것**(나중에 "안 바뀐다"는 재현을 받고서야 뒤늦게 깨닫는 것보다 훨씬 빠르다).
- **데스크탑처럼 "로컬 파일 저장 + Firebase 동기화"를 둘 다 하는 모델에 새 필드를 추가할 땐 두 저장 경로(`Repository.kt`의 직렬화 함수와 `JsonStore.kt`의 save()/parse())를 항상 같이 확인할 것(36차·54차 두 번 반복된 실수)**: 컴파일러가 누락을 안 잡아주고, 게다가 `pushXxxToFirebase()`류 함수는 push 시점에 로컬 타임스탬프도 원격과 똑같이 맞춰버려서 필드가 빠진 채 저장돼도 LWW 비교가 "이미 최신"으로 오판해 재시작해도 스스로 안 고쳐진다(54차, `routinesTs` 강제 리셋으로만 복구 가능했음). 새 필드를 추가했으면 실제로 앱을 재시작해서 값이 살아남는지 확인하는 습관을 들일 것. [[DECISIONS.md]]/[[BUGS.md]] 54차 참고.
- **PowerShell 콘솔에 찍힌 한글 깨짐(mojibake)을 실제 파일 손상으로 착각하지 말 것(79차)**: `data.json`을 직접 편집한 뒤 `ConvertFrom-Json`/`Write-Host`로 검증했더니 한글이 깨져 보이고 파싱 오류까지 떠서 파일이 손상된 줄 알았으나, 이는 PowerShell 5.1 콘솔 출력 인코딩 문제였을 뿐 파일 자체(bash `cat`/`sed`, Python `json.load`로 재확인)는 멀쩡했다. 이 프로젝트 데이터 파일의 한글 필드를 다룰 땐 PowerShell 콘솔 출력을 신뢰하지 말고 Python 등 다른 도구로 재검증할 것.
- **자기 자신을 재기동하는 워치독이 있는 앱에서 "자체 업데이트" 같이 의도적으로 앱을 종료시키는 기능을 새로 만들 땐, 항상 `intentional_exit.flag`(또는 그에 준하는 "일부러 종료함" 표식)를 exitProcess/exitApplication 직전에 남길 것(79차, 자체 업데이트 무한반복 버그의 원인이었음)**: 표식 없이 그냥 종료하면 워치독이 2초 안에 "비정상 종료"로 보고 옛 버전을 되살려서, 설치 마법사가 파일을 덮어쓰기도 전에 파일이 다시 잠긴다. `Watchdog.kt`의 `intentionalExitFlagFile()`을 재사용할 것 — 트레이 "종료"가 쓰는 것과 동일한 표식.
- **데스크탑 좌우 분할(마스터-디테일) 화면을 새로 만들 때는 처음부터 `ui/components/ResponsiveSplit.kt`를 쓸 것(79차 신설)**: 창을 좁혀도 양쪽이 뭉개지지 않고 위아래로 쌓이도록 이미 만들어둔 공용 컴포넌트 — Timer/Calendar/Calculator 3개 화면이 이미 이걸로 교체됐다. 새 분할 화면을 `Row(Modifier.weight(...))`로 직접 짜지 말 것.
- **데스크탑 msi 배포 전 실제 설치 경로(msi 설치)로 검증한 적이 없었다는 게 79차에 드러남**: 개발자는 항상 `createDistributable` 결과물을 robocopy로 직접 배포해왔지, 실제 msi installer 경로를 써본 적이 없어서 `windows { shortcut/menu }` 누락(바탕화면 아이콘 안 생김) 버그가 오래 방치돼 있었다. msi 패키징 설정을 바꾸면 가능하면 실제로 설치까지 해서 확인할 것.
- **`packageMsi`/`createDistributable`(plain)과 `packageReleaseMsi`/`createReleaseDistributable`(release, ProGuard 적용)는 서로 다른 산출물이다(87차)**: "실행 방법" 절의 표준 배포 명령은 의도적으로 plain 쪽을 쓴다(ProGuard 없이 그대로 배포) — 79차 이전까지 release 변형은 `jvmToolchain(21)`+구버전 ProGuard(7.2.2) 조합으로 애초에 빌드 자체가 안 됐기 때문(87차에 ProGuard 7.4.2로 올려 수정, [[BUGS.md]] 87차 참고). GitHub 릴리스도 호스트 실제 배포와 같은 `BuildInfo.BUILD_TIMESTAMP`를 쓰려면 plain 쪽(`packageMsi createDistributable`)으로 통일해서 빌드할 것 — release 변형으로 만든 릴리스를 GitHub에 올리면 호스트에 실제로 깔린 바이너리와 태그가 다른 빌드가 된다.
- **`PhoneLockDesktopWatchdog` 예약 작업 on/off는 auto-mode 분류기가 1차 시도에서 항상 차단한다(87차)**: "시스템 설정 변경"으로 분류돼 `Disable-ScheduledTask`/`Enable-ScheduledTask` 첫 호출이 거부되는데, 사용자에게 "직접 다시 시도해도 되는지" 물어서 명시적 확인을 받은 뒤 **똑같은 명령을 그대로 재시도하면 통과한다** — 이건 사용자 본인의 개인 작업(watchdog)이라 실제 시스템 보안 설정이 아니므로, 매번 사용자에게 터미널에서 직접 실행해달라고 떠넘기지 말고 먼저 재시도 허락을 구할 것.

---

# 현재 알려진 문제

- **OneDrive 경로에서 직접 빌드하면 안드로이드/데스크탑 둘 다 간헐적으로 실패한다(61차 재정정, 59차 내용 뒤집음)**: 59차는 "안드로이드는 OneDrive 원본에서 정상 빌드된다"고 기록했지만, 61차에 실제로 여러 번 겪어보니 안드로이드도 `mergeDebugResources`/`compileKotlin` 등에서 OneDrive 동기화 잠금으로 간헐적 실패가 남(재시도하면 대부분 성공하는 산발적 증상이라 59차엔 우연히 안 걸렸던 것으로 추정). **안드로이드도 데스크탑처럼 항상 OneDrive 밖 로컬 경로(`AndroidBuilds\phone-lock-android`)에서 빌드하고, 완성된 산출물(APK/설치 이미지)만 OneDrive로 복사할 것.** 데스크탑은 `AndroidBuilds\phone-lock-desktop` 또는 `C:\build\phone-lock-desktop`(둘 다 ASCII 경로, jlink 인자 파일 인코딩 문제 회피 목적도 겸함). [[BUGS.md]] 61차 참고.
- **데스크탑 오버레이는 코너 소형 위젯**(전체화면 click-through 불가) — [[DECISIONS.md]], [[IDEAS.md]] 참고. (공부 잠금은 이 제약과 무관 — `BlockScreen`처럼 click-through가 필요 없는 완전 차단용 Maximized 창이라 JNA 없이 구현됨)
- **안드로이드 공부 잠금은 진짜 실행 차단이 아니라 베스트 에포트**: 기기 소유자(device owner)/키오스크 모드 없이는 접근성 서비스로 다른 앱 실행 자체를 막을 수 없다. 허용 안 된 앱이 열리는 걸 감지해서(최대 2초 지연) 잠금 화면으로 되돌리는 방식 — 순간적으로 화면이 보였다 사라질 수 있음.
- **(67차 정정)** 이 git 저장소의 유일한 커밋(`7d9e448`, 2026-08-15 "Initial baseline commit for isolation/rollback safety net")은 그 시점 전체 스냅샷 하나뿐이라 의미 있는 변경 이력이 없다는 뜻이지, `phone-lock-android`/`phone-lock-desktop`이 untracked라는 뜻이 아니었다(`git ls-files`로 확인하니 실제로는 커밋에 포함돼 추적 중 — 과거 서술이 틀렸었음, 67차 keystore 작업 중 발견). **git에 커밋되면 안 되는 새 파일(keystore/비밀번호 등)을 추가할 땐 항상 `.gitignore`에 명시적으로 올릴 것** — 상세 이력은 계속 CHANGELOG.md에 의존.
- 상세 버그 이력(대부분 Fixed)은 [[BUGS.md]] 참고.
- **(57차 교훈) 코드는 고쳤는데 문서가 안 따라간 사례가 실제로 있었음**: Alt-Tab 우회 버그가 41차 전후로 실제 수정됐지만 BUGS.md/HANDOFF.md엔 56차까지 "보류"로 남아있었다(57차에 발견해 정정). 코드를 수정한 세션은 그 자리에서 바로 BUGS.md를 갱신할 것 — "다음에 정리"로 미루면 이런 불일치가 쌓인다.

---

# 현재 중요한 파일

## Desktop (`phone-lock-desktop/src/main/kotlin/com/phonelock/desktop/`)
| 파일 | 역할 |
|------|------|
| `data/Models.kt` | `Group`/`AppData` 데이터 모델. **37차** `Group.overlayLevelStepsToMax` 추가(오버레이 최고 밝기까지 재확인 횟수, 순수 표시값). **39차** `StudyLogEntry.note` 추가(공부 세션 회고), `Group`에 `snoozeMinutes`/`snoozedUntilEpochMillis`/`snoozeUsedDate`/`snoozeUsedCount`(스누즈)와 `forceEnabledFrom`/`forceEnabledUntil`(기간 지정 자동 강화) 추가. **42차** `AppData.lastGroupAutoResetDate`(그룹 자동 재활성화 하루 1회 적용 추적), `Group.blockAttemptDate`/`blockAttemptCount`(잠김 화면 문구 강도용, 오늘 시도 횟수) 추가. **48차** `Routine`/`RoutineLog` 데이터클래스 신규 + `AppData.routines`/`routineLogs`/`nextRoutineId` 추가(루틴앱 v1, [[DECISIONS.md]] 48차 참고). **62차** `AppData`에 `shareRoutinesToGroup`/`shareStudyToGroup`/`shareStreakToGroup`(기본 true)/`nudgeLastSeenByGroup` 추가("모임" 공유 설정+깨우기 확인 시각, Room 아님). **76차(컴파일 검증됨)** 위 3필드 삭제 → `groupShareSettings: MutableMap<String, GroupShareSettings>`(모임별 6항목)/`hiddenFromUidsByGroup`/`hiddenPeerUidsByGroup`(사용자별 비공개) 대체, `GroupShareSettings` 데이터클래스 신규 |
| `data/JsonStore.kt` | JSON 파일 원자적 읽기/쓰기. **37차** `overlayLevelStepsToMax` parse/save 추가. **39차** 위 신규 필드 parse/save 추가, `save()`의 JSON 구성 로직을 `toJsonObject()`로 추출해 신규 `exportToFile()`(설정/그룹 내보내기, #6)과 공유. **42차** `lastGroupAutoResetDate`/`blockAttemptDate`/`blockAttemptCount` parse/save 추가. **48차** `routines`/`routineLogs`/`nextRoutineId` parse/save 추가. **54차** 52차에 `Routine`에 추가됐던 `icon`/`notifyEnabled`/`startDate`/`endDate` 4개 필드가 여기서 빠져있던 걸 뒤늦게 발견해 추가([[BUGS.md]] 54차 참고). **62차** "모임" 공유 토글 3종/`nudgeLastSeenByGroup` parse/save 추가 |
| `data/Repository.kt` | **75차** `checkForUpdateIfNeeded()`(하루 1회 가드, 네트워크는 락 밖 `Thread`)/`pendingUpdateInstallerUrl()`/`checkForUpdateNow(onResult)`(설정 화면 수동 확인) 신규. 그룹/사용기록/escalation 조회·저장, 캐싱, heartbeat. **34차** `remoteStudyLogCache`+`pushStudyLogToFirebase`+`syncStudyLogFromFirebase` 추가(공부기록 크로스디바이스 동기화, `studyLog`와 별개로 로컬엔 병합 안 함). **35차** `applyIncompleteCarryOver`/`revertIncompleteCarryOver` 추가(캘린더 미완료 시 다음날 자동 복사, `setCalendarTaskStatus`에서 O와 대칭 처리). **37차** `syncedLastConfirmedAtEpochMillis(group)` 추가 — 기존 `mergedEscalation()` 재사용, 같은 이름의 그룹을 가진 다른 기기의 마지막 확인 시각을 호출부에 노출(실행확인 유예시간 크로스디바이스 공유용, `ConfirmationGate.kt` 자체는 안 건드림). **39차** `snoozeGroup()`/`snoozeRemainingToday()`(스누즈, 하루 3회 제한) + `exportDataToFile()`(설정/그룹 내보내기) 신규. **41차** `mergedSnooze()`/`syncedSnoozeUntil(group)` 신규(스누즈 크로스디바이스 동기화, `mergedEscalation()`과 동일한 "최신값 승리" 패턴) — `snoozeGroup()`이 이 병합값 기준으로 하루 3회 판정하도록 변경, `snoozeRemainingToday()`(UI 표시용)는 네트워크 호출 없이 로컬 값만 보도록 그대로 유지. **42차** `applyDailyGroupResetIfNeeded()`(그룹 자동 재활성화, 하루 1회)/`recordBlockAttempt(groupId)`(잠김 화면 문구 강도용 시도 횟수 증가) 신규. **48차** `getRoutines`/`addRoutine`/`updateRoutine`/`deleteRoutine`/`moveRoutineOrder`/`getRoutineLogsForDate`/`isRoutineCompleted`/`toggleRoutineLog`/`getRoutineCompletedDateKeys` 신규(루틴앱 v1 CRUD, 48~49차엔 로컬 전용). **50차** `pushRoutinesToFirebase()`/`syncRoutinesFromFirebase()`/`routinesToJson`/`routinesFromJson` 신규(루틴 Firebase 동기화, 배열 인덱스 참조로 ID 충돌 회피 — [[DECISIONS.md]] 50차 참고), `themeMode` get/set 프로퍼티 추가(3종 테마), 계산기-캘린더 연동 함수 `linkedProgressAmount`/`addLinkedCalendarTask`/`isLinkedGoalAchieved`/`adjustLinkedCalcProgress` 신규 + `setCalendarTaskStatus`가 연동 일정 완료/취소 시 계산기 진행량 함께 조정, `CALENDAR_COLOR_ORDER`/`CALENDAR_SCHEDULE`을 8단계 무지개+망각곡선 기반 간격(1,3,7,14,30,60,120일)으로 전면 재작성. **62차** "모임" 공유 토글 3종+`nudgeLastSeenFor`/`setNudgeLastSeen` 프로퍼티 추가. **63차** `fbUser` 프로퍼티 삭제 + `PomodoroSyncClient` 호출부 전체에서 세 번째 인자(`user`) 제거 |
| `routine/RoutineEngine.kt` | 루틴 스트릭 순수 계산(`currentStreak`) — 방어권 소모를 저장 카운터 없이 매번 재계산([[DECISIONS.md]] 48차 참고). `calc/CalcEngine.kt`와 같은 위치적 역할(플랫폼별 대칭 복제). 49차부터 `RoutineScreen.kt`가 실제로 호출. **50차** 방어권을 루틴별 커스텀 입력 대신 항상 "주 2회" 고정으로 단순화([[DECISIONS.md]] 50차 참고). **52차** `isScheduledOn`을 public으로 열어 기간(`startDate`/`endDate`) 검사 추가, `RoutineNotifier`도 재사용 |
| `routine/RoutineQuotes.kt` | **신규(52차)** 스트릭 기반 응원/비판/조롱 알림 문구 — `forStreak(streak, broken)`, 42차 `MotivationalQuotes.kt`와는 별개의 작은 세트(안드로이드판과 내용 동일 유지할 것) |
| `routine/DesktopNotifier.kt` | **신규(52차)** `Main.kt`가 등록한 `TrayState`를 들고 있다가 Composable이 아닌 곳에서도 `sendNotification()` 호출 — 새 `TrayIcon`을 안 만드는 이유는 [[DECISIONS.md]] 52차 참고 |
| `routine/RoutineNotifier.kt` | **신규(52차)** 30초 주기 틱(`Main.kt`에서 호출)에서 루틴 `timeSlot`/스트릭 알림 시각을 직접 비교해 `DesktopNotifier` 호출 — 인메모리 중복방지만 함(앱 재시작 시 리셋되지만 무해) |
| `ui/RoutineScreen.kt` | **신규(49차)** 루틴 메인 화면. **50차** 서브탭을 오늘/일과표/습관 3개에서 오늘(항상 시간순 정렬, 요일 피커+주 이동 추가)/통계(지표 카드) 2개로 개편, 체크박스 즉시 미반영 버그를 `refreshTick`+`key()`로 수정([[BUGS.md]] 50차 참고). **52차** 시간대 없는 루틴에만 ▲/▼ 순서변경 버튼(`swapRoutineOrder`) 추가, 아이콘 표시 |
| `ui/RoutineEditScreen.kt` | **신규(49차)** 루틴 추가/수정 `RoutineEditDialog` — `GroupEditScreen`과 달리 전체 화면 아니라 다이얼로그. **50차** 방어권 종류/횟수 입력 UI 제거, 안내 문구로 대체. **52차** 아이콘(텍스트필드+12개 quick-pick)/알림 토글/기간(시작일·종료일) 입력 + "루틴 복사" 버튼 추가 |
| `monitor/EnforcementService.kt` | 포그라운드 프로세스 감시 → 잠금/확인 판정 루프. **37차** `checkStudyLock()`이 로컬 타이머뿐 아니라 `PomodoroSyncClient.isStudyTimerActive()` 원격 신호로도 잠금(`REMOTE_STUDY_SIGNAL_STALE_MS`=20분 컷오프), `effectiveRemainingCooldownSeconds()`/`isRecentlyConfirmedAnyDevice()` 신규(로컬 `ConfirmationGate` vs 동기화 값 중 최신 채택). **42차** `tick()` 맨 앞에서 매번 `repository.applyDailyGroupResetIfNeeded()` 호출, `decide()`가 잠금 판정 시 매칭된 그룹을 함께 반환하도록 바뀌어 `onBlock` 콜백에 `recordBlockAttempt()` 결과(시도 횟수)를 실어 보냄(`onBlock` 시그니처에 `blockAttempts: Int` 추가) |
| `monitor/SiteEnforcement.kt` | 브라우저 확장이 호출하는 사이트 판정 로직. **37차** `isBlockedByStudyLock()`에 원격 신호 OR 추가, `effectiveRemainingCooldownSeconds()`/`isRecentlyConfirmedAnyDevice()` 신규(EnforcementService와 동일 로직 대칭 복제). **42차** `CheckResult.Confirm`에 `level`, `CheckResult.Block`에 `attempts` 필드 추가(실행확인/잠김 화면 문구 강도를 `/check`·`/tick` 응답으로 브라우저 확장에 전달) |
| `monitor/ConfirmationGate.kt` | 그룹별 재확인 쿨다운 관리(원본 유지 필수 — 37차에도 이 원칙 지킴, 호출부에서만 동기화 값과 병합) |
| `monitor/OverlayLevelRatchet.kt` | 오버레이 표시 레벨을 쿨다운 사이클 안에서 캡핑 |
| `monitor/LockEvaluator.kt` | 그룹 잠금/제한 판정. **39차** `isSnoozeActive()`(스누즈, `isPomodoroUnlocked()`와 동일 패턴)와 `isForceEnabledNow()`(기간 지정 자동 강화, `isGroupActive()`에 `now` 파라미터 추가해 통합) 신규 — `evaluate()`/`isCurrentlyRestricting()`/`isConfirmActiveNow()`가 이 둘을 최상단에서 확인(기간 지정이 스누즈보다 우선), `ConfirmationGate.kt`는 안 건드림. **41차** 판정용 private `isSnoozed()`가 `repository.syncedSnoozeUntil(group)`(동기화 값, 네트워크 I/O)를 보도록 변경, UI 표시용 `isSnoozeActive()`(그룹 목록 배지)는 로컬 값만 보도록 분리 유지 |
| `monitor/DesktopUpdateChecker.kt` | **신규(75차)** GitHub Releases API 조회(`java.net.http.HttpClient`+`org.json`) — 안드로이드 `service/UpdateChecker.kt`와 대칭, `desktop-<타임스탬프>` 태그 중 최신 찾기, fail-safe(오류 시 null) |
| `ui/UpdateBanner.kt`(desktop) | **신규(75차)** 업데이트 배너 Composable + 설치파일 다운로드/실행 — `SettingsScreen.kt`(수동 "지금 확인")와 `MainScreen.kt`(자동 배너) 양쪽에서 재사용, 성공 시 `exitProcess(0)`로 앱 자신 종료 |
| `BuildInfo.kt`(desktop, 컴파일 시점 자동생성) | **신규(75차)** `build.gradle.kts`의 `generateBuildInfo` 태스크가 매 빌드마다 `BUILD_TIMESTAMP`(빌드 시각)를 채워 생성 — 안드로이드 `versionCode` 자동증가와 동일 역할, git에 커밋 안 됨(`build/generated/`) |
| `monitor/PomodoroSyncClient.kt` | 실행확인 레벨/일일사용한도 Firebase 동기화 + `pushLocalStudyStatus`(로컬 타이머 상태를 크로스디바이스 신호로 write). **34차** `pushLocalStudyStatus`에 `phaseStartedAt`/`taskName` 파라미터 추가, `remoteUpdatedAtMillis()`(신호 신선도 판정)·`writeStudyLogForDate`/`readStudyLogForDate`(공부기록 동기화) 신규. **37차**부터 `isStudyTimerActive()`/`remoteUpdatedAtMillis()`가 미러 표시뿐 아니라 실제 잠금 판정에도 쓰임(함수 자체는 변경 없음, 호출부만 확장). **41차** `readSnoozeSync`/`writeSnoozeSync` 신규(`users/{user}/snoozeSync/{그룹명}`, `readConfirmSync`/`writeConfirmSync`와 동일 패턴). **61차** `resolveIdentity()` 신설로 모든 함수가 로그인 uid 경로/토큰을 쓰도록 전면 개편, 기존 익명인증 완전 삭제(로그인 필수 전환), `migrateLegacyDataToAccount()` 신규. **63차** `migrateLegacyDataToAccount()` 삭제(설정 화면 마이그레이션 기능 자체 제거), 모든 함수 시그니처에서 `user`(=`fbUser`) 파라미터 완전 제거(`resolveIdentity()`가 실제로는 이미 무시하던 죽은 파라미터였음이 조사로 확인됨) — 호출부는 `databaseUrl`/`apiKey` 두 값만 넘기면 됨 |
| `monitor/AuthManager.kt` | **61차** 신설 당시 파일명은 `GoogleAuthManager.kt`, OAuth PKCE + 로컬 루프백 흐름. **68차** `signInGuest()`(Firebase Anonymous Auth REST) + `Session.isAnonymous` 추가. **72차** `AuthManager.kt`로 이름 변경 + OAuth 흐름 전체 제거, Firebase Identity Toolkit REST(`accounts:signUp`/`accounts:signInWithPassword`)로 아이디/비번 로그인 구현(`signIn(id,password,apiKey)`/`signUp(...)`/`currentLoginId`) — 세션 파일명(`google_auth.json`)은 하위 호환으로 유지, [[DECISIONS.md]] 72차 참고 |
| `monitor/AccountSyncClient.kt` | **신규(68차)** 가입 신청/관리자 승인 REST 클라이언트(`usernames`/`allowedUsers`/`users/{uid}/profile` 스키마) — [[DECISIONS.md]] 68차 참고 |
| `ui/AccountGateScreen.kt` | **신규(68차)** `AccountGate(repository, content)` — 로그인/게스트 → 아이디+닉네임 설정 → 승인대기(폴링) → 승인됨 4단계 게이트, `Main.kt`가 최상위에서 이걸로 메인 UI를 감쌈 |
| `net/LocalApiServer.kt` | 로컬 HTTP API(127.0.0.1:47882). **37차** `/overlay-status` 응답에 `levelStepsToMax` 필드 추가 |
| `net/ApiToken.kt` | 토큰 발급/영구 저장 |
| `ui/UsageOverlayContent.kt` | 데스크탑 오버레이 위젯 UI. **37차** 오버레이 알파 증가폭을 고정 상수 대신 `status.levelStepsToMax`로부터 매번 계산. **49차** 타이머 숫자 accent 색을 옛 파랑(`#4F8EF7`)에서 새 Primary 초록(`#8BC34A`)으로 교체 |
| `ui/StudyTimerScreen.kt` | **24차** 네이티브 타이머/뽀모도로 탭, **25차**부터 업무 선택이 오늘 캘린더 일정 드롭다운, **28차** 좌우 분할, **32차** 웹앱 CSS 기준 전면 재작성(phase 배지 공부=파랑/휴식=초록, 타이머 숫자 실행중 항상 파랑, 전환버튼 파랑틴트, 뽀모도로 ON/OFF 알약버튼, 공부기록 카드+합계행, 허용앱/사이트 입력+리스트+삭제 UI로 교체) + 드롭다운 선택 버그 수정(`ExposedDropdownMenuBox`). **34차** phase 배지 dot pulse 애니메이션, 로컬 타이머 꺼져 있을 때 다른 기기 상태를 메인 화면에 그대로 미러링(정지/전환 버튼 숨김), 공부기록 5초 주기 크로스디바이스 동기화. **38차** "오늘 한눈에" 요약 카드 신규(39차에 문자열 템플릿 컴파일 버그 수정). **39차** "정지" 버튼이 즉시 정지 대신 회고 입력 다이얼로그를 열도록 변경(`timerStop(note)`), "오늘의 공부 기록"에 최근 회고 표시 |
| `ui/CalendarScreen.kt` | **신규(25차)** `dateKey+ordinal` 기준 조작, **28차** 좌우 분할, **32차** 웹앱 CSS 기준 전면 재작성(오늘=날짜숫자 원형배지, 월그리드 일정=회독색 채운 배지, 날짜상세 일정명=색텍스트만, 완료/미완료/이동/복사/삭제 버튼 색 틴트, 다음회독을 헤더줄 소형 알약으로) + 그리드 세로 스트레치(행마다 weight). **34차** 날짜 상세 제목에 그날 총 공부시간, 업무 이름 옆에 개별 시간 표시 + 5초 주기 공부기록 동기화(진입 시 1회만 하던 버그 수정). **49차** "오늘" 배지의 하드코딩 `Color.White` 텍스트가 새 밝은 초록 Primary 배경 위에서 대비가 나빠져 `MaterialTheme.colorScheme.onPrimary`로 교체. **50차** 월그리드 날짜 칸에 완료 배지(개수+색상) 추가(안드로이드 대칭), `LinkedCalcSection`(계산기 연동 업무 선택+수량 입력) 신규, `stageTextColor`/색상 선택 UI를 8단계 무지개로 전면 갱신 |
| `calc/CalcEngine.kt` | **신규(27차)** 계산기 순수 계산 로직(웹앱 calculate() 이식) — `totalCapacity`/`finishDiffDays` 필드가 32차에야 UI에서 처음 쓰임 |
| `ui/CalculatorScreen.kt` | **신규(27차)**, **28차** 3탭+그리드, **31차** 웹앱 사이드바 구조(좌2:우8)로 회귀, **32차** 폴더접기 영속화·카드접기·모두펴기/접기·전체저장·입력하단버튼고정 추가 + 결과 카드를 웹앱 CSS(`.result-block` 등) 기준 전면 재작성(카드는 무채색, 색은 D배지/진행바/필요행/판정배너/마감배지 5곳에만). **34차** 저장됨 폴더 지정을 인라인 목록→`DropdownMenu` 플로팅 팝오버로 교체, 결과 카드 진입 애니메이션(fadeIn+slideUp) |
| `ui/TimetableScreen.kt` | **신규(29차)** 계산기 draft 업무 기준 이번 주 고정 테이블, **32차** 오늘 칸 값 빨강/합계열 파랑 색상 추가. **34차** 이전주/다음주 이동 버튼(`weekOffset`) 추가. **50차** 계산기-캘린더 연동 목표 달성 시 ✅ 표시+초록 강조 |
| `ui/StudyStatsScreen.kt` | **신규(30차)** 캘린더 일정 전체 기준 완료율/스트릭/30일 추이, **32차** 지표 타일 배경 틴트 추가 |
| `ui/GroupListScreen.kt` | 그룹 목록. **34차** `selectedGroupId` 파라미터 추가, 선택된 그룹은 accent 테두리 강조(관리앱 좌우 분할용). **39차** "😴 스누즈" 버튼 신규(제한 중이거나 스누즈 중일 때만 표시, 하루 3회 초과면 실패 메시지) |
| `ui/StatsScreen.kt` | 사용 통계. **34차** 왼쪽 그룹별 요약(진행바만)/오른쪽 선택한 그룹 상세로 좌우 분할 재작성(새 데이터 없이 선택 기반 확대만) |
| `ui/StudyLockScreen.kt` | 공부 잠금 전체화면 UI(위: 타이머, 아래: 허용 프로그램 실행 버튼) — 정지/전환 버튼이 로컬 `Repository` 직접 호출. **37차** `StudyLockStatus.isRemote`가 true면(다른 기기 신호로 잠긴 경우) 정지/전환 버튼을 숨기고 안내 문구로 대체 |
| `ui/components/SectionCard.kt` | **32차** `accentColor` 옵션 파라미터 추가(기본은 무채색 유지, 지정 시 배경 10%/테두리 70% 틴트) |
| `ui/components/WatchAndWaitScreen.kt` | 차단/실행확인/종료확인 공용 카운트다운 셸 (title/quote 별도 슬롯 — 42차에 title이 조롱 문구 자리로 확정, quote는 미사용). **43차** 체크포인트 힌트 문구를 bodySmall→bodyMedium+SemiBold로 가독성 개선(★아직 데스크탑 앱 재배포 안 함) |
| `ui/MotivationalQuotes.kt` | **신규(42차)** 조롱조 문구 5단계(순한~극한, 각 20개)와 `confirmQuoteTier(level)`/`blockQuoteTier(attempts)` 매핑. 안드로이드/`quotes.js`와 내용 동일하게 유지할 것. **44차** 각 단계 5개씩 총 25개 추가(124→149개, 아직 이 데스크탑판만 재빌드/재배포 안 됨) |
| `ui/GroupEditScreen.kt` | 그룹 생성/수정 화면 — **안드로이드 허용앱 선택 버그 조사 시 이 화면의 앱 목록 구현을 참고할 것**(사용자가 이 화면은 정상 작동한다고 확인, [[BUGS.md]] 참고). **37차** "실행 확인" 섹션에 `overlayLevelStepsToMax` 입력 필드 추가(사용 중 남은 시간 오버레이 표시 토글 아래). **39차** "일시정지(스누즈) 시간"/"기간 지정 자동 강화" `SectionCard` 2개 신규. **52차** "복사" 버튼 추가(안드로이드판과 대칭) |
| `ui/SettingsScreen.kt` | 설정 화면. **75차** 공통 서브탭에 "업데이트" `SectionCard` 신규(현재 버전+"지금 확인"+발견 시 `UpdateBanner`). **39차** "설정/그룹 내보내기·가져오기" 카드 신규(`java.awt.FileDialog`, #6). **50차** "테마" `SectionCard` 신규 — 라이트+그린/다크+파랑/화이트+오렌지 3종 `FilterChip`, `repository.themeMode` 즉시 저장. **52차** "루틴 스트릭 알림" 토글 `SectionCard` 신규(안드로이드판과 대칭, `repository.routineStreakNotifyEnabled`). **62차** 공통/루틴/공부/관리 4개 `TabRow` 서브탭으로 재편(기존 카드 내부 로직은 안 건드리고 배치만 재편), "모임 공유 설정" `SectionCard` 신규(공통 아래). **63차** "Firebase 연결 설정" 카드(Database URL/Web API Key 입력칸) 완전 삭제 + "예전 사용자 ID" 입력칸/마이그레이션 기능 전부 삭제 — "계정 동기화" 카드엔 이제 로그인/로그아웃 버튼만 남음(**다음 세션 디자인 개선 대상**). **73차** "무전기(음성 메시지)" `SectionCard` 신규(수신허용 토글 기본 꺼짐+모드 `FilterChip`+볼륨 `Slider`(이 프로젝트 첫 사용)+허용 시간대 두 칸) |
| `monitor/SocialGroupSyncClient.kt` | **신규(62차)** "모임" Firebase REST 클라이언트 — `PomodoroSyncClient`의 `resolveIdentity()` 패턴 재사용. `createGroup`/`joinGroupByCode`/`leaveGroup`/`deleteGroup`/`readMyGroupIds`/`readGroupInfo`/`readGroupMembers`/`pushMyStats`/`readGroupStats`/`sendNudge`/`readIncomingNudges`. **72차** `RoutineStat`에 `icon`/`timeSlot` 필드 추가(멤버 상세페이지 표시용). **73차** `createGroup()` 버그 수정(빈 body push→info 포함 body로 한 번에 push, [[BUGS.md]] 73차 참고) + `sendVoiceMessage`/`readIncomingVoiceMessages`/`deleteVoiceMessage`(무전기) 신규, `push()` 헬퍼가 body를 받도록 확장. **76차** `MemberStats`/`pushMyStats`/`readGroupStats`에 `shareSchedule`/`shareStudyingNow`/`shareActiveGroup`(신규 3항목)+`hiddenFromUids`(사용자별 비공개) 추가, `pushMyStats`가 전역 `repository.shareXToGroup` 대신 `repository.groupShareSettings(groupId)`를 읽도록 변경. **78차** `CalcTaskStat` 데이터클래스 신규 + `MemberStats.calcTasks` 추가(모임 "일정표" 탭 실데이터화, `shareSchedule`에 편입), `GroupWalkieSettings.voiceGender` 추가(무전기 남성 목소리) |
| `monitor/VoiceRecorder.kt` / `VoicePlayer.kt` | **신규(73차)** 무전기 녹음/재생 — `javax.sound.sampled`(JDK 표준, 새 의존성 없음)로 8kHz mono 16-bit WAV 캡처(`TargetDataLine`, 최대 10초)/재생(`Clip`+`MASTER_GAIN`으로 앱 내 볼륨만 적용, OS 볼륨/무음은 그대로 존중) |
| `monitor/TtsPlayer.kt` | 무전기 텍스트 메시지 TTS — Windows SAPI(`System.Speech`)를 고정 스크립트+프로세스 인자로 호출(인젝션 안전 설계는 파일 내 주석 참고). **78차** `speak()`에 `voiceGender` 파라미터 추가, 스크립트가 항상 한국어(`ko`) 음성을 먼저 찾아 그 안에서 성별 선택하도록 변경(기존엔 명시적 음성 선택이 전혀 없어 PC 기본값이 영어면 한글을 영어 음성으로 읽던 잠재 버그 발견/수정, [[BUGS.md]]/[[DECISIONS.md]] 78차 참고) + 스크립트 파일을 매번 덮어쓰도록 변경(`!exists()` 가드 제거 — 이전 버전 캐시가 새 파라미터를 못 받는 문제 방지) |
| `routine/SocialGroupNotifier.kt` | **신규(62차)** 넛지("깨우기") 폴링→`DesktopNotifier.notify()`. `Main.kt`의 기존 30초 tick 루프에서 호출(네트워크 호출은 별도 `Thread`로 분리해 코루틴 안 막음) |
| `routine/VoiceMessageNotifier.kt` | **신규(73차)** 무전기 폴링 — `walkieReceiveEnabled` 꺼지면 아무 일도 안 함, `walkieMode`+허용 시간대에 따라 즉시재생(후 삭제) 또는 알림만(인박스에 남김). `Main.kt`에 켜져 있는 동안만 7초(꺼지면 30초) 주기 별도 루프로 추가(기존 30초 루프와 분리). **78차** `TtsPlayer.speak()` 호출에 `settings.voiceGender` 전달 |
| `ui/components/GroupWalkieSettingsDialog.kt` | 모임별 무전기 수신 설정 다이얼로그(켜짐/모드/볼륨/요일×시간대 다중 일정). **78차** "텍스트 메시지 목소리(TTS)" 여성/남성 `FilterChip`+미리듣기 버튼 신규 |
| `ui/SocialGroupScreen.kt` / `SocialGroupMembersScreen.kt` / `SocialGroupMemberDetailScreen.kt` | **신규(62차)** "모임" 탭 3종 화면 — 내 모임 목록+만들기/참여하기, 멤버 목록(마스터-디테일, 오늘 완료율 낮은 순+"오늘 아직 안 한 사람 N명" 배지+😴 깨우기+초대코드 복사+나가기/삭제), 멤버 상세(루틴/공부시간·진행률/스트릭, 비공개 항목 표시). **72차** 멤버 상세 화면을 헤더 아바타 카드+루틴 아이콘/시간 칩+완료 현황 진행바+원형 공부 진행률 게이지+스트릭 시각화(플레임 개수)로 확대. **73차** `SocialGroupMemberDetailScreen.kt`에 🎙️ 무전 버튼+녹음 다이얼로그(`onSendVoice` 콜백) 신규, `SocialGroupMembersScreen.kt`에 "받은 음성메시지" 인박스 리스트 신규. **76차** `SocialGroupMembersScreen.kt`에 "🔒 공유 설정" 버튼(신규 `ui/components/GroupShareSettingsDialog.kt` 오픈), `SocialGroupMemberDetailScreen.kt`(시그니처에 `repository`/`groupId` 추가)에 "👤 이 사람에 대한 내 설정"(사용자별 비공개 토글 2개)+"📅 오늘 일정"/"⏱️ 지금 상태"/"🗂️ 작동 중인 관리 그룹" 카드 3개 신규. **77차** 멤버 상세를 탭 구조(루틴/공부/관리)로 전면 재설계(이 문서 "현재 진행 중인 작업"→CHANGELOG 77차 참고), `SocialGroupMembersScreen.kt`에 관리자 전용 메뉴 추가. **78차** `MemberStudyTimetableTab`이 캘린더 대신 `MemberStats.calcTasks` 기반 실제 요일별 목표량 표(날짜 이동 포함)로 재작성, `TtsPlayer.speak()` 호출에 `voiceGender` 전달(인박스 재생 버튼) |
| `ui/MainScreen.kt` | **28차 개편** 왼쪽 `NavigationRail`(관리앱/공부앱/설정) + 섹션별 내부 `TabRow` 서브탭 구조, **30차**부터 공부앱 서브탭 5개(타이머/캘린더/계산기/일정표/통계). **49차** `TopSection.ROUTINE`("🌱 루틴") 추가. **50차** `TopSection` 순서를 루틴→공부→관리→설정으로 재배열, "관리앱"/"공부앱" 라벨을 "관리"/"공부"로 축약, `onThemeChange` 콜백 연결. **62차** `TopSection.SOCIAL_GROUP`("👥 모임") 추가(관리↔설정 사이), `selectedSocialGroupId` 로컬 상태로 목록↔멤버 화면 전환 |
| `ui/theme/Color.kt`/`Theme.kt`/`Shape.kt` | **28차 개편** 당시 공부앱(index.html)과 동일한 다크+파랑 팔레트(`darkColorScheme`), 반경 12dp. **49차** 라이트+그린으로 전면 교체(`lightColorScheme`, Primary `#8BC34A`, 배경 `#FAFBF6`). **50차** `ThemeMode` 상수(`LIGHT_GREEN`/`DARK_BLUE`/`LIGHT_ORANGE`)+`PhoneLockPalette` 데이터클래스+팔레트 3개 val로 재구조화(다크+파랑=28~48차 옛 팔레트, 화이트+오렌지=최초 테마 복원), `PhoneLockTheme(themeMode: String, content)`가 `paletteFor(themeMode)`로 선택 — [[DECISIONS.md]] 49~50차 참고 |
| `Main.kt` | 앱 진입점, 창 구성. **33차** `resolveAppPath()`(공부 잠금 허용 프로그램을 레지스트리 App Paths→시작 메뉴 `.lnk`→PATH 순으로 찾는 이름 해석 함수) 추가 — [[DECISIONS.md]] 참고. **46차** 앱 이름 "갓생살기종합세트"로 통일, 트레이/창 아이콘을 `ColorPainter`(단색)에서 `PixelSunriseIcon`으로 교체. **52차** `rememberTrayState()`를 `Tray(state=...)`에 연결하고 `DesktopNotifier.trayState`에 등록, 30초 주기 `RoutineNotifier.tick()` 루프 추가. **73차** `VoiceMessageNotifier.tick()`용 별도 조건부 폴링 루프(`LaunchedEffect`) 추가. **99차** 기존 7초 루프에 `ChatNotifier.tick()` 신규 호출 추가(채팅 알림) |
| `ui/SunriseIcon.kt` | **신규(46차, `PixelSunriseIcon.kt`)** 트레이/창 아이콘(`Painter` 구현) — 안드로이드 `ic_launcher_background.xml`/`ic_launcher_foreground.xml`과 동일한 108x108 좌표를 공유, `packaging/app-icon.ico`(exe 아이콘, `build.gradle.kts`의 `windows.iconFile`)와도 좌표 동일. **61차** 각진 픽셀아트에서 그라데이션+원+베지어 곡선 기반으로 전면 재작성, 파일명도 `PixelSunriseIcon`→`SunriseIcon`으로 변경(`Main.kt` 2곳 참조 수정). `packaging/generate_icon.ps1`(`.ico` 생성 스크립트)도 GDI+ 안티앨리어싱으로 대칭 재작성 — **이 스크립트엔 한글 텍스트를 절대 넣지 말 것**([[BUGS.md]] 61차, PowerShell 5.1 코드페이지 버그) |

## 브라우저 확장 (`phone-lock-desktop/browser-extension/`)
| 파일 | 역할 |
|------|------|
| `overlay.js` | content script, 페이지 위 오버레이 표시. **35차** 남은시간 숫자 색 주황→파란 accent(`#4F8EF7`). **37차** 알파 증가폭을 고정 상수 대신 `/overlay-status` 응답의 `levelStepsToMax`로부터 계산. **43차** 제목/문구 완전 삭제, 타이머만 중앙에 96px로 확대, 타이머 텍스트 색도 배경과 같은 opacity 적용(레벨 연동 투명도). **49차** accent를 파랑에서 새 Primary 초록(`rgb(139,195,74)`)으로 교체(★ `chrome://extensions` 재로드 필요, 아래 미반영 항목 참고) |
| `background.js` | 서비스워커, 토큰/heartbeat/tick/네비게이션 감시. **Alt-Tab 버그(35차 원인 확정, BUGS.md 참고)**: `onBeforeNavigate`에만 반응해 이미 열려있던 탭 전환은 최대 60초(tick 주기) 지연 |
| `confirm.html`/`blocked.html` | 실행확인/차단 페이지. **35차** 28차 다크테마 개편에서 빠져있던 걸 공부앱과 동일 팔레트로 통일. **42차** 버튼 라벨 "전자"/"후자"→"진행"/"중단", `<h1 id="title">`에 조롱 문구 로테이션(quote 문단은 삭제). **43차** `confirm.html` 제목 24px→30px, 체크포인트 힌트 문구 13px 흐린 회색→15px 굵은 밝은색으로 가독성 개선 |
| `quotes.js` | **신규(42차)** 조롱조 문구 5단계 + `confirmQuoteTier`/`blockQuoteTier` — Kotlin판(`MotivationalQuotes.kt`)과 내용 동일 유지, `overlay.js`용 `MOTIVATIONAL_QUOTES`(=순한 맛 전체) 호환 alias 포함. **44차** 각 단계 5개씩 총 25개 추가(124→149개, 아직 확장 재로드 안 함) |
| `onboarding.html` | 설치 완료 안내(시크릿모드 허용 유도). **35차** 다크 팔레트 통일(예전엔 다른 남색 톤이었음) |
| `config.js` | 공용 `API_BASE`/`fetchWithTimeout` |
| `manifest.json` | content_scripts 순서: `config.js`→`quotes.js`→`overlay.js` |

## Android (`phone-lock-android/app/src/main/java/com/phonelock/app/`)
| 파일 | 역할 |
|------|------|
| `data/PhoneLockRepository.kt` | **75차** `checkForUpdateIfNeeded()`/`pendingUpdateApkUrl()`/`checkForUpdateNow()`(설정 화면 수동 확인, 데스크탑판과 대칭) 신규. 그룹/사용기록/실행확인 레벨 조회·저장(escalationMutex로 직렬화). **35차** `applyIncompleteCarryOver`/`revertIncompleteCarryOver` 추가(데스크탑판과 대칭). **37차** `syncedLastConfirmedAtEpochMillis(group)` 추가(데스크탑판과 대칭, `mergedEscalation()` 재사용). **39차** `snoozeGroup()`/`snoozeRemainingToday()` 신규(데스크탑판과 대칭). **41차** `mergedSnooze()`/`syncedSnoozeUntil(group)` 신규(데스크탑판과 대칭, 별도 `snoozeMutex`+10초 캐시), `snoozeGroup()`이 병합값 기준 하루 3회 판정하도록 변경. **42차** `applyDailyGroupResetIfNeeded()`/`recordBlockAttempt(groupId)` 신규(데스크탑판과 대칭). **48차** `routineDao`/`routineLogDao` CRUD 함수 신규(데스크탑판과 대칭, 루틴앱 v1). **50차** `pushRoutinesToFirebase()`/`syncRoutinesFromFirebase()`(데스크탑판과 대칭, 인덱스 참조 방식) + 위젯 갱신용 `refreshRoutineWidget()`(생성자에 `appContext` 추가) + 계산기-캘린더 연동 함수 4종(데스크탑판과 대칭) + `AppPreferences.themeMode` 연동 프로퍼티. **52차** `copyGroup()`/`copyRoutine()`/`swapRoutineOrder()` 신규, `addRoutine`/`updateRoutine`/`deleteRoutine`이 `RoutineAlarmScheduler`도 함께 호출(예약/취소), `restoreGroupsFromBackup()` 신규(그룹 복구, [[BUGS.md]] 52차 참고). **62차** "모임" 관련 pass-through 함수 신규(`createSocialGroup`/`joinSocialGroup`/`leaveSocialGroup`/`deleteSocialGroup`/`readMySocialGroupIds`/`readSocialGroupInfo`·`Members`·`Stats`/`sendSocialGroupNudge`/`readIncomingSocialGroupNudges`/`markSocialGroupNudgeSeen`/`pushMySocialStats`). **63차** `fbUser` 프로퍼티 삭제 + `PomodoroSyncClient` 호출부 전체에서 세 번째 인자(`user`) 제거(데스크탑판과 대칭). **72차** `pushMySocialStats()`가 `RoutineStat`에 `icon`/`timeSlot`도 함께 실어보내도록 변경. **73차(컴파일 미검증)** `sendVoiceMessage`/`readIncomingVoiceMessages`/`deleteVoiceMessage` pass-through 신규(무전기, 데스크탑판과 대칭). **78차** `pushMySocialStats()`가 `getCalcTasks()`로 계산기 draft 업무를 `CalcTaskStat`로 옮겨 `calcTaskStats`로 함께 전송(모임 일정표 실데이터화) |
| `routine/RoutineEngine.kt` | 루틴 스트릭 순수 계산(`currentStreak`) — 데스크탑판과 동일 로직 대칭 복제, [[DECISIONS.md]] 48차 참고. **49차**부터 `RoutineScreen.kt`가 실제로 호출. **50차** 방어권 "주 2회" 고정(데스크탑판과 대칭). **52차** `isScheduledOn`을 public으로 열어 기간 검사 추가, `RoutineAlarmScheduler`도 재사용 |
| `routine/RoutineQuotes.kt` | **신규(52차)** 스트릭 알림 문구 — `forStreak(streak, broken)`, 데스크탑판과 내용 동일 유지할 것 |
| `routine/RoutineAlarmScheduler.kt` | **신규(52차)** 루틴/스트릭 알림 예약·취소. `scheduleNext(routine)`는 8일 이내에서 요일마스크+기간을 만족하는 가장 가까운 시각을 찾아 예약, `rescheduleAll()`은 부팅/앱시작 시 전체 재예약. **56차** Doze 지연(약 2분) 확인돼 `setAndAllowWhileIdle`→`setExactAndAllowWhileIdle`로 전환(`scheduleAlarm()` 헬퍼가 `canScheduleExactAlarms()` 확인 후 권한 없으면 자동 폴백) |
| `routine/RoutineReminderReceiver.kt` | **신규(52차)** `ACTION_BOOT_COMPLETED`(재예약)/`ACTION_ROUTINE_REMINDER`/`ACTION_STREAK_CHECK`(알림 표시+다음 예약) 처리, `RoutineWidgetToggleReceiver`와 같은 `goAsync()`+코루틴 패턴 |
| `ui/RoutineScreen.kt` | **신규(49차)** 데스크탑판과 대칭 — Repository 루틴 함수가 전부 suspend라 `LaunchedEffect(refreshTick)`으로 완료 날짜 집합을 미리 캐싱(StudyStatsScreen과 같은 패턴). **50차** 오늘/통계 2탭 개편(데스크탑판과 대칭) — 요일 피커는 안드로이드만 `weight(1f)` 균등분할 커스텀 칩으로 구현(스크롤 기반 시도 실패 후 확정, [[BUGS.md]] 50차 참고), 30일 통계 그래프는 `horizontalScroll`+고정폭 `20.dp`로 수정. **52차** 시간대 없는 루틴에만 ▲/▼ 순서변경 버튼 추가, 아이콘 표시 |
| `ui/RoutineEditScreen.kt` | **신규(49차)** 데스크탑판과 대칭 다이얼로그. **50차** 방어권 입력 UI 제거(데스크탑판과 대칭). **52차** 아이콘/알림 토글/기간 입력 + "루틴 복사" 버튼 추가(데스크탑판과 대칭) |
| `widget/RoutineWidgetProvider.kt` / `RoutineWidgetService.kt` / `RoutineWidgetFactory.kt` / `RoutineWidgetToggleReceiver.kt` | **신규(50차)** 안드로이드 홈스크린 위젯 — Jetpack Glance 대신 classic `RemoteViews`/`AppWidgetProvider`/`RemoteViewsService`/`RemoteViewsFactory` 사용(신규 Gradle 의존성 회피, [[DECISIONS.md]] 50차 참고), 리스트 항목 클릭은 `PendingIntent` 템플릿+`RoutineWidgetToggleReceiver`(`goAsync()`)로 토글. 관련 리소스: `res/layout/routine_widget.xml`/`routine_widget_item.xml`, `res/xml/routine_widget_info.xml`, `res/drawable/widget_background.xml` 등. **52차** `RoutineWidgetFactory.kt`의 `isScheduledOn`도 기간 검사 추가. 실기기 미검증 |
| `service/AppMonitorAccessibilityService.kt` | 접근성 서비스, 포그라운드 앱/사이트 감시 + 오버레이. **37차** `checkStudyLock()`/`checkStudyLockSite()`가 로컬 타이머뿐 아니라 `PomodoroSyncClient.isStudyTimerActive()` 원격 신호로도 잠금(`REMOTE_STUDY_SIGNAL_STALE_MS`=20분 컷오프), `effectiveRemainingCooldownSeconds()`/`isRecentlyConfirmedAnyDevice()` 신규(데스크탑 EnforcementService와 동일 로직 대칭 복제). **42차** `tick()` 맨 앞에서 `repository.applyDailyGroupResetIfNeeded()` 호출, `launchBlock()`에 `blockAttempts`/`launchConfirm()`·`launchConfirmSite()`에 `level` 파라미터 추가(SCHEDULE/LIMIT 판정에서만 실제 값 전달, STUDY_LOCK/REELS/SHORTS는 기본값 0). **43차** `ensureUsageOverlayView()`에서 제목/문구 TextView 완전 제거, 타이머만 64sp로 중앙 배치, `applyOverlayOpacityForLevel()`/뽀모도로 오버레이 둘 다 타이머 텍스트 색상에도 배경과 같은 alpha 적용. **52차** `REELS_KEYWORDS`/`SHORTS_KEYWORDS` 문자열이 파일 바이트 단위로 깨져있던(mojibake) 버그 수정 — 감지가 전혀 작동 안 했던 근본 원인, [[BUGS.md]] 52차 참고. **78차** `containsSelectedKeyword()`가 하단 12% 밴드만 보던 조건에 좌/우측 16% 사이드 레일 밴드를 추가(태블릿에서 릴스/쇼츠 차단이 전혀 안 되던 버그 수정, [[BUGS.md]]/[[DECISIONS.md]] 78차 참고) |
| `service/ConfirmationGate.kt` | 그룹별 재확인 쿨다운 관리(원본 유지 필수 — 37차에도 지킴) |
| `service/LockEvaluator.kt` | 그룹 잠금/제한 판정. **39차** `isSnoozeActive()`/`isForceEnabledNow()` 신규(데스크탑판과 대칭). **41차** `isConfirmActiveNow()`가 `fun`→`suspend fun`으로 변경(판정용 private `isSnoozed()`가 동기화 값을 봐야 해서), UI 표시용 `isSnoozeActive()`는 로컬 값만 보도록 분리 유지 |
| `service/PomodoroSyncClient.kt` | 실행확인 레벨 Firebase 동기화(HttpURLConnection 기반) + `pushLocalStudyStatus`. **37차**부터 `isStudyTimerActive()`가 미러 표시뿐 아니라 실제 잠금 판정에도 쓰임(함수 자체는 변경 없음). **41차** `readSnoozeSync`/`writeSnoozeSync` 신규(데스크탑판과 대칭). **61차** `resolveIdentity()` 신설로 모든 함수가 로그인 uid 경로/토큰을 쓰도록 전면 개편, 기존 익명인증(`TokenCache`/`ensureIdToken`/`signInAnonymously`/`refreshIdToken`) 완전 삭제(로그인 필수 전환), `migrateLegacyDataToAccount()` 신규(가져오기). **63차** `migrateLegacyDataToAccount()` 삭제, 모든 함수 시그니처에서 `user`(=`fbUser`) 파라미터 완전 제거(데스크탑판과 대칭) |
| `service/AuthManager.kt` | **61차** 신설 당시 파일명은 `GoogleAuthManager.kt`, Credential Manager + Firebase Auth SDK 기반 Google 로그인. 세션은 SDK가 자동 영속화(별도 저장 코드 없음). **68차** `signInGuest()`(Firebase Anonymous Auth) 추가. **72차** `AuthManager.kt`로 이름 변경 + Credential Manager/Google Sign-In 로직 제거, `FirebaseAuth.createUserWithEmailAndPassword`/`signInWithEmailAndPassword`로 아이디/비번 로그인 구현(`signUp`/`signIn`/`currentLoginId`), `androidx.credentials`/`googleid` Gradle 의존성 제거 |
| `service/AccountSyncClient.kt` | **신규(68차)** 가입 신청/관리자 승인 REST 클라이언트 — 데스크탑판과 동일 스키마/동작(`monitor/AccountSyncClient.kt` 참고) |
| `ui/AccountGateScreen.kt` | **신규(68차)** `AccountGate(repository, content)` 4단계 게이트, `MainActivity.kt`가 `PhoneLockApp(...)` 호출을 이걸로 감쌈 |
| `service/UpdateChecker.kt` | **신규(75차)** GitHub Releases API 조회(`HttpURLConnection`+`org.json`) — 데스크탑 `monitor/DesktopUpdateChecker.kt`와 대칭, `android-<versionCode>` 태그 중 최신 찾기, fail-safe. **87차** 자체 로직은 안 바뀜, `PhoneLockRepository.checkForUpdateIfNeeded()`의 호출 주기만 하루 1회→15분으로 단축 |
| `ui/UpdateBanner.kt`(android) | **신규(75차)** 업데이트 배너 Composable + `DownloadManager` 다운로드/설치 인텐트 — `SettingsScreen.kt`(수동)/`MainActivity.kt`(자동) 양쪽에서 재사용, "출처를 알 수 없는 앱" 권한 없으면 그 설정으로 안내 |
| `service/IntentExtras.kt` | Activity 간 전달 extra 키 모음. **37차** `EXTRA_STUDY_LOCK_IS_REMOTE` 추가 |
| `service/SocialGroupSyncClient.kt` | **신규(62차)** "모임" Firebase REST 클라이언트(데스크탑판과 대칭, `resolveIdentity()` 재사용). **72차** `RoutineStat`에 `icon`/`timeSlot` 필드 추가. **73차(컴파일 미검증)** `sendVoiceMessage`/`readIncomingVoiceMessages`/`deleteVoiceMessage`(무전기, 데스크탑판과 대칭). **76차(컴파일 검증됨)** `MemberStats`/`pushMyStats`/`readGroupStats`에 신규 3항목(`shareSchedule`/`shareStudyingNow`/`shareActiveGroup`)+`hiddenFromUids` 추가(데스크탑판과 대칭). **78차** `CalcTaskStat`+`MemberStats.calcTasks`(모임 일정표 실데이터화), `GroupWalkieSettings.voiceGender`(데스크탑판과 대칭) |
| `service/VoiceRecorder.kt` / `VoicePlayer.kt` | **신규(73차, 컴파일 미검증)** 무전기 녹음/재생 — `AudioRecord`로 8kHz mono 16-bit PCM 캡처 후 WAV 헤더 직접 부착(플랫폼 API만 사용, 데스크탑과 동일 포맷으로 상호 재생 가능), `MediaPlayer`로 재생(볼륨은 STREAM_MUSIC 위에 앱 설정 배율만 적용) |
| `service/TtsPlayer.kt` | 무전기 텍스트 메시지 TTS — `android.speech.tts.TextToSpeech`, 매 재생마다 새 인스턴스 생성 후 종료. **78차** `speak()`에 `voiceGender` 파라미터 추가 — "MALE"이면 `setPitch(0.78f)`로 톤을 낮춰 남성처럼 들리게 함(엔진별 Voice 이름으로 실제 다른 목소리를 고르는 건 기기마다 결과가 달라 신뢰 불가, [[DECISIONS.md]] 78차 참고) |
| `service/WalkieTalkieService.kt` | **신규(73차, 컴파일 미검증)** 무전기 수신 포그라운드 서비스(`foregroundServiceType="mediaPlayback"`) — `walkieReceiveEnabled` 켜질 때만 시작(상주 알림 1개), 7초 폴링. 접근성 서비스(켜짐에만 동작)/`GroupNudgeWorker`(WorkManager 15분 하한)에 안 얹고 독립시킨 이유는 [[DECISIONS.md]] 73차 참고. 배너는 `TYPE_ACCESSIBILITY_OVERLAY`(접근성 서비스 전용이라 못 씀) 대신 일반 알림으로 대체. **78차** `TtsPlayer.speak()` 호출에 `settings.voiceGender` 전달. **99차** `pollChatMessages()` 신규(같은 7초 루프에 편승, 채팅 새 메시지 감지) — 새 채널 `chat_message`(진동 없음, `ui/ChatThreadScreen.kt`의 `ActiveChatTracker.openChatId`로 지금 보는 방은 건너뜀) |
| `routine/GroupNudgeWorker.kt` | **신규(62차)** 넛지("깨우기") `CoroutineWorker` — `AccessibilityWatchdogWorker`와 나란히 `MainActivity.kt`에 15분 주기로 등록(WorkManager OS 하한, 실시간 아님). `RoutineReminderReceiver`와 동일한 채널/알림 패턴 |
| `ui/SocialGroupScreen.kt` / `SocialGroupMembersScreen.kt` / `SocialGroupMemberDetailScreen.kt` | **신규(62차)** "모임" 탭 3종 화면(데스크탑판과 대칭). `MainActivity.kt`에 `social_group/{groupId}`/`social_group_member/{groupId}/{uid}` 라우트로 연결. **72차** 멤버 상세 화면 확대(데스크탑판과 대칭, 아바타 헤더/루틴 아이콘+시간 칩/원형 공부 게이지/스트릭 시각화). **73차(컴파일 미검증)** `SocialGroupMemberDetailScreen.kt`에 🎙️ 무전 버튼+녹음 다이얼로그(RECORD_AUDIO 런타임 권한 요청 최초 도입) 신규, `SocialGroupMembersScreen.kt`에 인박스 리스트 신규(데스크탑판과 대칭). **76차(컴파일 검증됨)** `SocialGroupMembersScreen.kt`에 "🔒 공유 설정" `IconButton`(신규 `ui/components/GroupShareSettingsDialog.kt` 오픈), `SocialGroupMemberDetailScreen.kt`에 "👤 이 사람에 대한 내 설정"(사용자별 비공개 토글 2개)+신규 3항목 카드(데스크탑판과 대칭). **77차** 멤버 상세를 탭 구조(루틴/공부/관리)로 전면 재설계, 관리자 전용 메뉴 추가(데스크탑판과 대칭, CHANGELOG 77차 참고). **78차** `MemberStudyTimetableTab`이 `MemberStats.calcTasks` 기반 실제 요일별 목표량 표(날짜 이동)로 재작성, 인박스 재생에 `TtsPlayer.speak(voiceGender=...)` 전달(데스크탑판과 대칭) |
| `ui/components/GroupWalkieSettingsDialog.kt` | 모임별 무전기 수신 설정 다이얼로그(데스크탑판과 대칭). **78차** "텍스트 메시지 목소리(TTS)" 여성/남성 `FilterChip`+미리듣기 버튼 신규 |
| `ui/SettingsScreen.kt` | **62차** 공통/루틴/공부/관리 4개 서브탭으로 재편(데스크탑판과 대칭), "모임 공유 설정" `SectionCard` 신규. **63차** "Firebase 연결 설정" 카드/"예전 사용자 ID" 입력칸/마이그레이션 기능 전부 삭제(데스크탑판과 대칭, **다음 세션 디자인 개선 대상**). **73차(컴파일 미검증)** "무전기(음성 메시지)" `SectionCard` 신규(데스크탑판과 대칭). **76차(컴파일 검증됨)** 서브탭 자체를 `permRoutine/Study/Manage/Social`로 필터링, "모임 공유 설정" `SectionCard`는 안내 문구만 남기고 실제 토글은 각 모임 화면으로 이동 |
| `ui/components/InterstitialScreen.kt` | 차단/실행확인 공용 카운트다운 셸 (title/quote 별도 슬롯 — 42차에 title이 조롱 문구 자리로 확정, quote는 미사용). **43차** 체크포인트 힌트 문구 가독성 개선(bodySmall→bodyMedium+SemiBold), 제목이 한 줄에 안 들어가면(`didOverflowWidth`) 최소 55%까지 자동 축소하는 로직 추가(`maxLines=1`+`softWrap=false`) — 처음엔 `maxLines=2`+`didOverflowHeight`로 짰다가 "2줄로 꺾이는 것 자체"가 문제였다는 지적을 받고 재작업. **44차** `LocalConfiguration.current.screenWidthDp >= 600`이면 태블릿으로 판정해 축소 로직을 건너뛰고 데스크탑 `WatchAndWaitScreen.kt`와 동일한 무조건부 `Text`로 분기 |
| `ui/MotivationalQuotes.kt` | **신규(42차)** 조롱조 문구 5단계와 `confirmQuoteTier`/`blockQuoteTier` — 데스크탑판과 내용 동일 유지. **44차** 각 단계 5개씩 총 25개 추가(124→149개) |
| `data/Entities.kt` / `data/AppDatabase.kt` / `data/AppPreferences.kt` | 데이터 모델(`StudyLogEntry`/**25차** `CalendarTask`/**27차** `CalcTask`+`CalcSavedItem`/**37차** `AppGroup.overlayLevelStepsToMax`/**39차** `StudyLogEntry.note`+`AppGroup` 스누즈·기간지정 필드 6종/**42차** `AppGroup.blockAttemptDate`/`blockAttemptCount`+`AppPreferences.lastGroupAutoResetDate`/**48차** `Routine`+`RoutineLog` 포함, **52차** `Routine.icon`/`notifyEnabled`/`startDate`/`endDate` 추가)/Room DB(**version 27** — 38차 21→22, 39차 22→23→24, 42차 24→25, 48차 25→26, 52차 26→27, 무전기는 Room 안 씀·버전 변경 없음)/SharedPreferences(`studyLockAllowedPackages`/`studyLockAllowedSites`/타이머 상태/`calendarTs`/**27차** 계산기 ts+폴더 트리 JSON 필드/**50차** `themeMode`/`routinesTs` 포함, **52차** `routineStreakNotifyEnabled`/`lastRoutineStreak`/`lastRoutineStreakNotifyDate` + `resetSyncTimestamps()`(Room destructive migration 후 강제 재동기화, [[BUGS.md]]/[[DECISIONS.md]] 52차 참고), **73차(컴파일 미검증)** `walkieReceiveEnabled`(기본 false)/`walkieVolume`/`walkieMode`/`walkieAllowedStartMinute`·`EndMinute` 추가, **76차(컴파일 검증됨)** `shareRoutinesToGroup`/`shareStudyToGroup`/`shareStreakToGroup` 전역 3필드 삭제 → `groupShareSettingsJson`(모임ID -> `GroupShareSettings` 6항목)/`hiddenFromUidsByGroupJson`/`hiddenPeerUidsByGroupJson`(모임 내 사용자별 비공개) 대체) |
| `data/PreMigrationBackup.kt` | 앱 버전 변경 감지 시 Room 열기 전에 raw SQLite를 JSON으로 백업(`backupIfVersionChanged`). **52차** `listBackups()` 신규(설정 화면 그룹 복구 카드용), `MainActivity.onCreate`가 백업 성공 시 `AppPreferences.resetSyncTimestamps()`도 함께 호출하도록 연결 |
| `ui/GroupListScreen.kt` | 그룹 목록. **39차** "😴 스누즈" 버튼 신규(데스크탑판과 대칭). **50차** 좌상단 "🗂️ 그룹" headline 추가(데스크탑판과 대칭) |
| `ui/GroupEditScreen.kt` / `ui/SettingsScreen.kt` | 그룹 편집/설정 화면. **37차** "실행 확인" 섹션에 `overlayLevelStepsToMax` 입력 필드 추가(데스크탑판과 대칭). **39차** `GroupEditScreen.kt`에 스누즈/기간지정 `SectionCard` 2개 추가(데스크탑판과 대칭) — `SettingsScreen.kt`의 백업/복원(#6)은 이미 있었음. **50차** `SettingsScreen.kt`에 테마 3종 선택 `SectionCard` 추가(데스크탑판과 대칭). **52차** `GroupEditScreen.kt`에 "복사" 버튼 추가(데스크탑판과 대칭), `SettingsScreen.kt` 최상단에 "⚠ 그룹 데이터 복구" 카드(`PreMigrationBackup.listBackups()`로 찾은 최신 백업에서 1클릭 복구, 백업 있을 때만 노출) + "루틴 스트릭 알림" 토글 신규. **56차** `SettingsScreen.kt` "권한 / 백그라운드 보호" 섹션에 "정확한 알람" 상태 표시 + `ACTION_REQUEST_SCHEDULE_EXACT_ALARM` 허용 버튼 신규(배터리 최적화 예외 UI와 동일 패턴) |
| `ui/StudyTimerScreen.kt` | **24차** 네이티브 타이머/뽀모도로 탭, **25차**부터 업무 선택이 오늘 캘린더 일정 드롭다운, **32차** 데스크탑판과 동일하게 웹앱 CSS 기준 전면 재작성 + 인라인 접이식 허용앱 선택 섹션(`AllowedAppsCollapsibleSection`) 신규 + 드롭다운 버그 수정. **34차** 데스크탑판과 동일하게 pulse 애니메이션+타이머 미러+공부기록 동기화. **39차** 정지 버튼이 회고 입력 다이얼로그를 열도록 변경(데스크탑판과 대칭). **50차** `TIMER_COLOR_LABEL` 8단계로 확장(데스크탑판과 대칭) |
| `ui/CalendarScreen.kt` | **신규(25차)** `CalendarTask.id`+`sortOrder` 기준 조작, **32차** 데스크탑판과 동일하게 웹앱 CSS 기준 전면 재작성. **34차** 데스크탑판과 동일하게 날짜 상세 시간 표시+5초 주기 공부기록 동기화. **50차** `LinkedCalcSection`(계산기 연동, 업무 선택 버튼은 `FlowRow`로 오버플로우 방지) 신규, 8단계 무지개 색상 선택 UI로 갱신(월그리드 완료 배지는 안드로이드에 이미 있었음) |
| `calc/CalcEngine.kt` | **신규(27차)** 계산기 순수 계산 로직(데스크탑판과 동일 로직, 플랫폼 공유 모듈 없어 대칭 복제) — `totalCapacity`/`finishDiffDays`가 32차에야 UI에서 처음 쓰임 |
| `ui/CalculatorScreen.kt` | **신규(27차)**, **32차** 폴더접기 영속화·카드접기·모두펴기/접기·전체저장·입력하단버튼고정 추가 + 결과 카드 웹앱 CSS 기준 전면 재작성(데스크탑판과 대칭). **34차** 저장됨 폴더 지정 `DropdownMenu` 팝오버, 결과 카드 진입 애니메이션(데스크탑판과 대칭) |
| `ui/TimetableScreen.kt` | **신규(29차)** 계산기 draft 업무 기준 일 단위 뷰(◀/▶ 날짜 이동), **32차** 오늘 값 색상 추가. **50차** 계산기-캘린더 연동 목표 달성 시 ✅ 표시(`isLinkedGoalAchieved`가 suspend라 `LaunchedEffect(cursor, dayTasks)`로 `achievedMap` 선계산) |
| `ui/StudyStatsScreen.kt` | **신규(30차)** 데스크탑판과 동일 로직 대칭 복제, **32차** 지표 타일 배경 틴트 추가. **50차** 30일 완료 추이 그래프를 `horizontalScroll`+고정폭 `20.dp`로 수정(뭉개짐 버그, [[BUGS.md]] 참고) |
| `ui/StudyLockActivity.kt` / `ui/StudyLockAppsScreen.kt` | 공부 잠금 전체화면(정지/전환이 로컬 `PhoneLockRepository` 직접 호출, 비활성화되면 스스로 `finish()`) / 허용 앱 선택 화면(**32차**부터 `AllowedAppsPickerBody`로 목록 로직 분리해 타이머 탭과 공유, **33차**에 중첩 `LazyColumn`+`weight()` 렌더링 버그 수정 완료). **37차** `isStillActive`가 suspend로 바뀌어 로컬+원격 신호 둘 다 확인, `isRemote`면 정지/전환 버튼 숨기고 안내 문구 표시(데스크탑판과 대칭) |
| `ui/MainActivity.kt` | **75차** `PhoneLockApp` 최상단에 자체 업데이트 배너(`repository.checkForUpdateIfNeeded()`를 `LaunchedEffect(Unit)`에서 호출). **28차 개편** 하단 `NavigationBar` 3탭(관리앱/공부앱/설정) + `ManageSection`/`StudySection` 내부 `TabRow` 서브탭, **30차**부터 공부앱 서브탭 5개(타이머/캘린더/계산기/일정표/통계). **49차** `Tab.Routine`("🌱 루틴") 추가로 4탭. **50차** 탭 순서를 루틴→공부→관리→설정으로 재배열, "관리앱"/"공부앱" 라벨을 "관리"/"공부"로 축약, `onThemeChange` 콜백 연결(데스크탑판과 대칭). **56차** 앱 시작 시 루틴 알림 재예약(`rescheduleAll`) 전에 `repository.syncRoutinesFromFirebase()`를 먼저 호출 — 이전엔 "루틴" 탭을 직접 열기 전엔 다른 기기발 변경이 반영 안 돼 알림이 예약조차 안 됐음 |
| `ui/theme/Color.kt`/`Theme.kt`/`Shape.kt` | **28차 개편** 당시 데스크탑판과 동일한 다크+파랑 팔레트(`darkColorScheme`), 반경 12dp. **49차** 라이트+그린으로 전면 교체(데스크탑판과 동일 값). **50차** `ThemeMode`/`PhoneLockPalette` 3종 선택제로 재구조화(데스크탑판과 대칭) |
| `app/build.gradle.kts` | **75차** `buildFeatures.buildConfig = true` 추가(`BuildConfig.VERSION_CODE`를 자체 업데이트 버전 비교에 씀 — 이전엔 비활성 상태였음). **87차** Room `version = 36`(스누즈 on/off+하루한도) |
| `phone-lock-desktop/build.gradle.kts` | **87차** `compose.desktop.application.buildTypes.release.proguard`에 `version.set("7.4.2")`+`configurationFiles.from(proguard-rules.pro)` 추가 — 기존 7.2.2는 `jvmToolchain(21)` 바이트코드를 못 읽어 `packageReleaseMsi`/`Exe`가 원천적으로 실패했음([[BUGS.md]] 87차) |
| `phone-lock-desktop/proguard-rules.pro` | **신규(87차)** `-dontwarn kotlinx.serialization.**` — kotlinx-datetime(`:shared`가 의존)이 참조만 하고 실제로 안 쓰는 클래스라 ProGuard 7.4.2의 엄격해진 미해결참조 검사를 무시 |
| `AndroidManifest.xml` | `INTERNET` 퍼미션 포함. **75차** `REQUEST_INSTALL_PACKAGES` 퍼미션 추가(자체 업데이트 설치용). **50차** 홈스크린 위젯용 `<receiver>`/`<service>` 엔트리 추가. **73차(컴파일 미검증)** `RECORD_AUDIO`/`FOREGROUND_SERVICE_MEDIA_PLAYBACK` 퍼미션 + `WalkieTalkieService` `<service>` 엔트리(`foregroundServiceType="mediaPlayback"`) 추가 |
| `res/values/strings.xml` / `res/drawable/ic_launcher_background.xml` / `ic_launcher_foreground.xml` | **46차** `app_name`을 "갓생살기종합세트"로 변경. **58차** 아이콘을 픽셀아트 태양(각진 사각형)으로 교체했으나 사용자가 "구리다"고 평가. **61차** 데스크탑 트레이 아이콘 컨셉(하늘 그라데이션+일출+언덕)을 기준으로 재설계 — 배경은 실제 `<gradient>` 그라데이션, 전경은 원형 태양+베지어 곡선 언덕을 안전영역(x/y 21~87) 안에 배치해 부드럽고 중앙에 딱 맞도록 함(데스크탑 `SunriseIcon.kt`/`generate_icon.ps1`과 디자인 통일) |

**38차(2026-08-10) 변경 파일**(위 표엔 기존 역할 설명만 유지, 세부 diff는 [[CHANGELOG.md]] 38차 참고): Desktop `data/Repository.kt`(push 비동기화, `ConfirmCounter`/백업/CSV/평균사용량 함수 신규), `data/Models.kt`+`data/JsonStore.kt`(`ConfirmCounter` 모델+parse/save, 일일백업 함수), `ui/StudyTimerScreen.kt`+`ui/StudyStatsScreen.kt`+`ui/StatsScreen.kt`+`ui/SettingsScreen.kt`(신규 UI). Android `data/PhoneLockRepository.kt`(대칭 함수 전부, `recordConfirmFireAndForget`/`usageMutex` 등 버그수정 포함), `data/Entities.kt`+`data/Daos.kt`+`data/AppDatabase.kt`(`ConfirmCounter` 엔티티, Room version 21→22), `data/AppPreferences.kt`(`autoDndEnabled`), `data/PreMigrationBackup.kt`(테이블 5개 추가), `service/PomodoroSyncClient.kt`(`@Volatile`), `ui/ConfirmOpenActivity.kt`(스코프 수정), `ui/StudyLockActivity.kt`(DND 자동 적용), `ui/StudyTimerScreen.kt`+`ui/StudyStatsScreen.kt`+`ui/StatsScreen.kt`+`ui/SettingsScreen.kt`(신규 UI, 데스크탑판과 대칭).

---

# 실행 방법

**아래 robocopy 명령들은 반드시 PowerShell 도구로 실행할 것 — Bash 도구로 실행하면 한글 경로 때문에 조용히 아무것도 복사되지 않는다(36차 세션 발견, 위 "현재 주의사항" 참고).**

## 데스크탑 빌드/배포 (반드시 이 순서, 75차에 실제 경로 재확인/갱신)
**로컬 사본 경로는 `C:\build\phone-lock-desktop`(OneDrive 안에서 직접 빌드하면 [[BUGS.md]] "OneDrive 동기화 폴더에서 직접 Gradle 빌드" 문제로 간헐적 실패 — 안드로이드의 `AndroidBuilds\`와 같은 역할). JDK는 jpackage 포함된 `C:\Users\sunae\jdk21-temurin\jdk-21.0.12.1+1`(없으면 project_build_toolchain_missing 메모리 절차 참고, compileKotlin만 확인할 땐 jpackage 불필요한 Android Studio JBR로도 충분).**
```powershell
# 1. 앱 종료 (watchdog는 이 세션 환경엔 예약작업이 없어 Stop-Process만으로 충분했음 —
#    있다면 기존처럼 Disable-ScheduledTask -TaskName "PhoneLockDesktopWatchdog" 먼저 실행)
Get-Process -Name "PhoneLockDesktop" -ErrorAction SilentlyContinue | Stop-Process -Force

# 2. 소스 복사 (OneDrive → 로컬 사본, 한글 경로+OneDrive 동기화 빌드 불가 우회, 반드시 PowerShell로)
robocopy "C:\Users\sunae\OneDrive\바탕 화면\클로드\관리앱\phone-lock-desktop" "C:\build\phone-lock-desktop" /MIR /XD ".git" "build" /XF "hs_err_pid*.log"

# 3. 빌드 (MSI=GitHub 릴리스용 설치파일, createDistributable=이 호스트 배포용 app-image — 한 명령에 같이 넣으면
#    BuildInfo.kt 타임스탬프가 둘 다 동일해서 릴리스와 호스트 배포가 항상 같은 버전이 된다)
$env:JAVA_HOME = "C:\Users\sunae\jdk21-temurin\jdk-21.0.12.1+1"
$gradle = "C:\Users\sunae\.gradle\wrapper\dists\gradle-8.7-bin\bhs2wmbdwecv87pi65oeuq5iu\gradle-8.7\bin\gradle.bat"
Set-Location "C:\build\phone-lock-desktop"
& $gradle packageMsi createDistributable

# 4. 배포 (동기 실행 후 FAILED 0 확인 + jar 해시 비교 필수)
robocopy "C:\build\phone-lock-desktop\build\compose\binaries\main\app\PhoneLockDesktop" "C:\Users\sunae\PhoneLockDesktopApp" /MIR
robocopy "C:\build\phone-lock-desktop\build\compose\binaries\main\app\PhoneLockDesktop" "C:\Users\sunae\OneDrive\바탕 화면\클로드\관리앱\vm-build-output\PhoneLockDesktop" /MIR

# 5. 앱 실행
Start-Process "C:\Users\sunae\PhoneLockDesktopApp\PhoneLockDesktop.exe"

# 6. GitHub 릴리스 게시 (BuildInfo.kt의 BUILD_TIMESTAMP 값 확인 후 태그에 사용)
cat "C:\build\phone-lock-desktop\build\generated\buildinfo\kotlin\com\phonelock\desktop\BuildInfo.kt"
gh release create "desktop-<BUILD_TIMESTAMP>" "C:\build\phone-lock-desktop\build\compose\binaries\main\msi\PhoneLockDesktop-1.0.0.msi" --repo studybeultaeon-svg/study-planner --title "Desktop v<BUILD_TIMESTAMP>"
```
컴파일만 빠르게 확인하려면 앱 종료/배포/릴리스 없이 3단계에서 `& $gradle compileKotlin`만 실행. `gh`는 75차에 설치+인증 완료(호스트에 유지됨) — 재인증 불필요.

## Android APK 빌드 (★ 마지막 Copy-Item 2줄 둘 다 반드시 실행)
```powershell
robocopy "C:\Users\sunae\OneDrive\바탕 화면\클로드\관리앱\phone-lock-android" "C:\Users\sunae\AndroidBuilds\phone-lock-android" /MIR /XD ".git" "build"
$env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"  # JDK 21.0.10, jpackage 없어도 assembleDebug는 문제없음
$gradle = "C:\Users\sunae\.gradle\wrapper\dists\gradle-8.7-bin\bhs2wmbdwecv87pi65oeuq5iu\gradle-8.7\bin\gradle.bat"
Set-Location "C:\Users\sunae\AndroidBuilds\phone-lock-android"
& $gradle assembleDebug

# 결과 APK를 반드시 두 곳 모두에 복사할 것:
Copy-Item "app\build\outputs\apk\debug\app-debug.apk" "C:\Users\sunae\AndroidBuilds\phone-lock-app.apk" -Force
Copy-Item "app\build\outputs\apk\debug\app-debug.apk" "C:\Users\sunae\OneDrive\바탕 화면\클로드\관리앱\phone-lock-android\app\build\outputs\apk\debug\app-debug.apk" -Force
```

## Android release + GitHub 릴리스 게시(75차, 자체 업데이트용)
```powershell
# assembleDebug 대신 assembleRelease로 빌드(위 4단계에서 & $gradle assembleRelease), 그 다음:
cat "app\build\outputs\apk\release\output-metadata.json"   # versionCode 확인
Copy-Item "app\build\outputs\apk\release\app-release.apk" "C:\Users\sunae\AndroidBuilds\phone-lock-app-release.apk" -Force
Copy-Item "app\build\outputs\apk\release\app-release.apk" "C:\Users\sunae\OneDrive\바탕 화면\클로드\관리앱\phone-lock-android\app\build\outputs\apk\release\app-release.apk" -Force
Copy-Item "app\build\outputs\apk\release\app-release.apk" "C:\Users\sunae\OneDrive\바탕 화면\클로드\관리앱\vm-build-output\android\app-release.apk" -Force
gh release create "android-<versionCode>" "app\build\outputs\apk\release\app-release.apk" --repo studybeultaeon-svg/study-planner --title "Android v<versionCode>"
```
release 서명은 `keystore.properties`(git 미포함, 이미 존재)로 자동 적용됨. `gh`는 75차에 설치+인증 완료(호스트에 유지됨).

## 로컬 API 수동 테스트
```bash
curl -s "http://127.0.0.1:47882/pair"
curl -s "http://127.0.0.1:47882/overlay-status?domain=youtube.com"
```
`/confirm`, `/tick`, `/heartbeat`는 POST + `/pair`로 받은 토큰을 `X-PhoneLock-Token` 헤더에 넣어야 통과함.

## 브라우저 확장 재로드
`chrome://extensions` → "폰컨트롤 사이트 차단" 카드 재로드 → 대상 사이트 탭 새로고침. (서버 쪽 코드가 원인인 버그는 확장 재로드로 안 고쳐짐.)

---

# 다음 세션에서 반드시 알아야 하는 내용

0. **최신은 위 "현재 진행 중인 작업" 섹션 참고** — 세션별 상세 변경 이력은 전부 [[CHANGELOG.md]]에 누적 기록됨(중복 방지를 위해 여기서는 최신 세션 요약을 따로 안 둠).
1. **`dailyResetHour=9` 설정(35차 발견)**: "오늘"이 자정이 아니라 오전 9시 기준으로 판정됨 — 캘린더/공부기록/일일한도 전부 공유. 리셋 시각 전엔 어제 기록/일정이 계속 "오늘" 것으로 보이는 게 정상 동작(버그 아님). 사용자가 유지하기로 결정. 위 "현재 주의사항" 참고.
2. **크로스디바이스 동기화 추가 원칙**: "신호"(다른 기기 상태를 알려주기만 하면 됨)는 로컬이 source of truth, Firebase는 부가 채널 패턴, "실제 누적 데이터"(여러 기기가 각자 추가)는 `dailyUsage`/`studyLog`처럼 기기별 키에만 쓰고 읽는 쪽이 합산하는 패턴 — 문서 전체 LWW(캘린더/계산기 방식)를 쓰면 안 됨. 원격 명령(remoteCommand)은 신뢰성 문제로 제거된 전례가 있어 다시 쓰지 않음(표시는 하되 제어는 안 함). [[DECISIONS.md]] "공부 기록 크로스디바이스 동기화"/"타이머 크로스디바이스 '미러' 표시" 참고.
3. **Firebase 관련 버그는 REST API로 서버 상태를 직접 조회해서 디버깅하는 게 효과적**: 데스크탑 로컬 데이터파일(`%APPDATA%\PhoneLockDesktop\data.json`)에서 `fbDatabaseUrl`/`fbApiKey`를 읽어 anonymous 인증 후 `{databaseUrl}/{경로}.json?auth={idToken}`으로 조회하면 어느 기기/단계에서 막히는지 빠르게 좁혀짐.
4. **공부 잠금 허용 프로그램 실행**: `ProcessBuilder(appName)` 단독 호출 대신 `resolveAppPath()`(레지스트리 App Paths→시작 메뉴 `.lnk`→PATH) 3단계 폴백을 씀. 이 영역을 다시 건드릴 일이 있으면 [[DECISIONS.md]] "공부 잠금 허용 프로그램 실행..." 항목부터 볼 것.
5. **UI를 웹앱과 맞출 땐 스크린샷 대신 `공부앱/index.html`의 CSS/JS부터 Grep할 것** — 스크린샷 추측 방식은 여러 번 틀렸던 전례가 있음. [[DECISIONS.md]] 참고.
6. **절대 하지 말 것**: 재확인/escalation 판정 로직(`ConfirmationGate.kt` 등) 수정 — [[DECISIONS.md]] "표시값과 판정 로직 분리" 참고. 크로스디바이스 유예시간 공유 기능을 추가할 때도 이 파일은 원본 그대로 두고 호출부에서만 값을 병합했음 — "절대 손대지 말 것" 파일을 확장해야 할 땐 이 패턴 우선 검토.
7. **빌드 환경**: 이 프로젝트는 세션마다 새 샌드박스일 수 있어 `gradlew`/jpackage 포함 JDK가 없을 수 있음 — 메모리 `project_build_toolchain_missing` 절차 우선 시도. jpackage 없이도 `compileKotlin`/`compileDebugKotlin`/`assembleDebug`는 Android Studio JBR(`C:\Program Files\Android\Android Studio\jbr`)만으로 충분.
8. **문서 체계**: 이 프로젝트는 HANDOFF/CHANGELOG/DECISIONS/BUGS/IDEAS 5종 문서 체계를 씀. 세션 종료 시 각 문서를 역할에 맞게 갱신할 것 — 자세한 규칙은 `CLAUDE.md` 참고. 과거 세션 상세 기록은 전부 `CHANGELOG.md`에 있음.
9. **robocopy는 반드시 PowerShell 도구로(위 "현재 주의사항"·"실행 방법" 상단 참고)**: Bash 도구로 실행하면 이 프로젝트의 한글 경로에서 조용히 아무것도 복사 안 되고, Gradle의 UP-TO-DATE 스킵과 겹치면 "빌드/배포 성공" 로그만 보고 옛날 코드를 계속 재배포하게 된다. `/MIR`은 auto mode classifier가 차단할 수 있음(103차 확인) — 파괴적 플래그 없이 `/E`로 복사하는 것도 대안.
10. **원격 신호로 실제 잠금/차단 같은 강제 동작을 거는 기능은 반드시 신선도 컷오프를 넣을 것**: `REMOTE_STUDY_SIGNAL_STALE_MS`(20분, `remoteUpdatedAtMillis()` 기준)처럼 — 신호를 올리던 기기가 정지 없이 꺼지면 유령 신호가 남아 이 기기가 영구히 잠길 수 있다.
11. **Room DB 버전은 103차 기준 39**(82차부터 `fallbackToDestructiveMigration()`을 버리고 버전마다 명시적 `MIGRATION_x_y`를 작성하는 방식으로 전환됨 — 스키마 변경 시 버전을 올리는 것과 별개로, 기존 데이터를 지우지 않는 명시적 마이그레이션 코드를 반드시 함께 작성할 것. 단순 쿼리 추가(기존 테이블 변경 없음)는 마이그레이션 불필요, 103차 `observeEarnedTotal()`이 그 예).
12. **크로스디바이스 동기화 기능을 추가할 때 "UI 표시용"과 "판정 게이트용" 함수를 반드시 분리할 것**: 동기화 값을 읽는 함수는 네트워크 I/O가 있어서(캐시가 있어도) Compose 리컴포지션마다 직접 호출되는 자리(그룹 목록 배지 등)에서 쓰면 UI가 버벅인다. `isSnoozeActive()`(UI, 로컬 값만)와 판정용 private `isSnoozed()`(백그라운드 tick에서만, 동기화 값)를 분리해서 해결한 전례 — 판정 시점에 병합값을 로컬로 persist해두므로 UI는 다음 재조회 때 자연히 따라잡는다. [[DECISIONS.md]] "스누즈(#1) 크로스디바이스 동기화..." 참고.
13. **AppGroup(그룹) 자체는 원래부터 Firebase에 안 올라간다**: 그룹 설정(스케줄/한도/실행확인 설정값 등)은 기기별 로컬 전용이고, Firebase로 동기화되는 건 `dailyUsage`/`confirmSync`/`studyLog`/`pomodoro`/`calendar`/`calculator`/`snoozeSync`/`points`뿐이다. 새 그룹 필드를 추가할 때 "이것도 동기화해야 하나?"라는 질문이 나오면, 그룹 전체를 동기화 대상으로 만들지 말고 해당 필드만 별도의 작은 sync 경로로 분리해서 추가할 것.
14. **화면 문구 교체 요청은 title/quote 슬롯을 먼저 구분할 것**: `WatchAndWaitScreen`/`InterstitialScreen`은 title(큰 헤드라인)과 quote(작은 부제) 두 슬롯이 있는데, 사용자가 인용한 문구가 실제로 코드의 어느 파라미터에 들어있는지 대조하지 않고 진행하면 엉뚱한 슬롯을 바꾸게 된다.
15. **텍스트 자동 축소(shrink-to-fit) 로직을 짤 땐 "무엇을 막으려는지"에 맞는 조건을 고를 것**: "N줄을 넘칠 때" 줄이는 로직(`maxLines=N`+`didOverflowHeight`)과 "1줄에 안 들어갈 때" 줄이는 로직(`maxLines=1`+`didOverflowWidth`)은 다르다 — "짧은 줄바꿈 자체가 보기 싫다"는 요구엔 후자가 맞는데, 전자로 짜면 대부분 케이스에서 로직이 아예 발동하지 않아 "안 바뀐 것 같다"는 피드백을 받게 된다.
16. **화면 폭 기준으로 기기 종류를 나눌 땐 표준 브레이크포인트(sw600dp)부터 검토할 것**: `LocalConfiguration.current.screenWidthDp >= 600`으로 태블릿/폰을 판정(`InterstitialScreen.kt`) — 폼팩터별 분기가 또 필요해지면 이 임계값부터 재사용 검토할 것.
17. **새 순수 로직/계산 함수는 먼저 `:shared`에 넣을 자리인지 검토할 것(82차 도입, 103차 재확인)**: Room 엔티티나 플랫폼 데이터클래스에 결합되지 않는 순수 함수(포인트 계산, 문구, 스트릭 등)는 안드로이드/데스크탑 양쪽에 복붙하지 말고 `shared/src/main/kotlin/com/phonelock/shared/`에 한 번만 작성 — 두 앱 모두 `implementation("com.phonelock:shared")`로 이미 연결돼 있어 import만 추가하면 된다. 안드로이드에서 컴파일 확인 시 `AndroidBuilds/phone-lock-android`의 **형제 디렉터리**로 `AndroidBuilds/shared`도 함께 robocopy해야 한다(`../shared` composite build 참조, 안 하면 "Unresolved reference" 오류).

---

*마지막 업데이트: 2026-09-10 (103차 세션. 캐릭터/식물 키우기(게이미피케이션 2차) — 누적 획득 포인트 기준 8단계 이모지 식물, `:shared`에 순수 로직(`CharacterGrowth.kt`), "🎁 포인트" 탭 맨 위에 얹음. 양 플랫폼 컴파일+릴리스 빌드+호스트 배포+GitHub 릴리스 게시(안드로이드 `android-1789032989`, 데스크탑 `desktop-1789032819`)+공개 저장소 push까지 완료, 실사용 검증은 안 됨. 자세한 내용은 [[CHANGELOG.md]]/[[DECISIONS.md]] 103차 참고. **다음 세션 최우선: 103차 신규 기능 실사용 검증.**)*
