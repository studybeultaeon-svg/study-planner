# 프로젝트 개요

**프로젝트 이름**: 갓생살기종합세트 (구 "관리앱" — 45차 세션에서 개명, [[DECISIONS.md]] 45차 참고)

**프로젝트 목적**: 개인 자기통제(디지털 디톡스)를 위한 앱/사이트 차단 시스템. 지정한 앱·프로그램·사이트를 시간대/일일 한도로 제한하고, 해제하려면 타이머 대기 + 회유 멘트 통과 같은 "꼼수 방지" 절차를 거치게 한다. 이 문서 체계(HANDOFF/CHANGELOG/DECISIONS/BUGS/IDEAS)가 관리하는 범위는 phone-lock 계열(안드로이드/데스크탑/브라우저 확장)이며, 공부앱 네이티브 재구현도 같은 코드베이스 안에서 함께 관리한다.

**한 줄 설명**: 안드로이드 앱 + 윈도우 데스크탑 앱 + 크롬 확장이 하나의 규칙(그룹)을 공유하며 서로 다른 플랫폼에서 같은 방식으로 잠금·확인·레벨을 동기화하는 3종 세트. "갓생살기종합세트"라는 이름 아래 자기통제(관리앱)·학습관리(공부앱, 같은 코드베이스에 통합됨)·루틴앱(v1 코드/빌드 완료, 49차)까지 함께 묶는다. 반대로 알람앱(`com.wakealarm`, 별도 저장소)은 도메인이 완전히 독립적이라 이 세트에 포함하지 않고 분리 유지하기로 함 — 판단 기준은 [[DECISIONS.md]] 45차 참고.

구성 요소:
- **phone-lock-android**: Kotlin/Compose + Room DB 기반 안드로이드 앱
- **phone-lock-desktop**: Kotlin/Compose Desktop 기반 Windows 앱
- **browser-extension** (`phone-lock-desktop/browser-extension/`): Chrome 확장(Manifest V3), 데스크탑 앱의 로컬 API(`127.0.0.1:47882`)와 통신해서 사이트 차단 담당
- **루틴앱**(반복 할 일/습관/일과표, `Routine` 하나로 통합 — [[DECISIONS.md]] 47차 참고): 47차 설계 확정, 48차 데이터 모델/CRUD/스트릭 엔진, 49차 화면 3종(오늘/일과표/습관)+편집 다이얼로그, 50차에 "오늘"/"통계" 2탭 개편(일과표는 오늘 탭에 시간순 정렬로 흡수, 습관→통계)+요일 피커/주 이동+스트릭 방어권 주 2회 고정+Firebase 동기화 추가, 51차(문서 미기록, 코드로만 확인) 스트릭을 루틴별에서 "하루" 단위 전역 스트릭으로 전면 개편, 52차에 순서변경(▲/▼)+복사+아이콘+알림+스트릭알림+기간설정까지 추가(Room DB 26→27). 실기기 검증 대기 중.

**현재 개발 단계**: 핵심 기능(그룹 차단) 구현 완료. 공부앱 네이티브 재구현 **1~5단계 전체 완료**(타이머/뽀모도로→캘린더→계산기→일정표→통계, 30차), 이후 세션들에서 크로스디바이스 동기화(뽀모도로/공부기록/스누즈)·조롱조 문구·오버레이 재설계 등 신규 기능을 계속 추가해왔다(세션별 상세 이력은 [[CHANGELOG.md]] 참고). **루틴앱은 47차 설계 확정 → 48차 데이터 모델+CRUD+스트릭 엔진 → 49차 화면 3종+테마 전환 → 50차 2탭 개편(오늘/통계)+요일피커+Firebase 동기화까지 완료** — 코드/빌드 기준 v2 로드맵 전체 완료. **50차에 계산기↔캘린더 연동 기능(웹앱에서 누락됐던 핵심 기능) 이식, 8단계 무지개 회독(망각곡선 기반 간격), 테마 3종 선택제, 안드로이드 홈스크린 위젯도 함께 완료.** **실기기 검증은 프로젝트 전체에 걸쳐 거의 이뤄지지 않은 상태**(34차 크로스디바이스 동기화와 36차 토글 유지 수정만 사용자가 직접 확인) — 아래 "다음 작업 우선순위" 참고.

**현재 진행률**: 기존 그룹 차단 기능 약 90%(실기기 검증 대기 중). 공부앱 네이티브 재구현 1~5단계 전체 완료 + 50차에 계산기-캘린더 연동 이식. 루틴앱은 코드/빌드 기준 100%(50차에 v2 개편+Firebase 동기화까지 완료), 실기기 검증만 남음. 세부 세션별 변경 이력은 [[CHANGELOG.md]] 참고.

---

# 현재 구현된 주요 기능

- 앱/프로그램/사이트를 시간대·일일 한도 기준으로 차단(안드로이드/데스크탑 공통), 시간대·일일한도는 항상 OR 판정
- 그룹 편집 화면 "관리 종류" 섹션(스케줄/일일 사용한도 설정/실행 확인 3개 토글), 일일한도·실행확인은 각각 독립 "적용 시간대"+요일마스크(`dailyLimitDaysMask`/`confirmDaysMask`) 설정 가능
- 차단 해제 시 "실행 확인" 대기 화면(타이머 + 무작위 체크포인트 클릭), 회유 멘트 20개 통과 절차(꼼수 방지)
- 실행 확인 레벨 escalation 시스템: 재확인할수록 대기시간 증가, 시간 경과로 자동 감소. 표시값(오버레이 알파)과 판정 로직은 항상 분리 — [[DECISIONS.md]] 참고. **37차**: 그룹별 "오버레이 최고 밝기까지 재확인 횟수"(`overlayLevelStepsToMax`) 설정 추가 — 이 값으로부터 한 번 재확인할 때마다 오르는 밝기 폭을 시스템이 자동 계산(양 플랫폼+브라우저 확장 3곳 모두 적용, 순수 표시값이라 판정 로직과 무관)
- 모바일 ↔ 데스크탑 실행확인 레벨 + 일일 사용시간을 Firebase Realtime Database로 동기화(그룹 이름 기준). 레벨은 최신값 승리, 사용시간은 기기별 합산 — 자세한 설계는 [[DECISIONS.md]]. **37차**: 같은 이름의 그룹을 가진 다른 기기가 방금 실행확인을 통과했으면(`confirmSync`의 `lastConfirmedAtEpochMillis`) 이 기기도 재확인 없이 같은 유예시간(쿨다운)을 이어서 쓰도록 확장 — `ConfirmationGate.kt`(로컬 인메모리 쿨다운) 자체는 그대로 두고, 호출부(`EnforcementService`/`SiteEnforcement`/안드로이드 `AppMonitorAccessibilityService`)에서 로컬 값과 동기화된 값 중 더 최근인 쪽을 쓰도록 확장
- 릴스/쇼츠 감지 차단(안드로이드 접근성 트리, 브라우저 확장 URL 패턴) — 데스크탑 네이티브 앱엔 없음, [[IDEAS.md]] 참고
- 23:00~11:00 편집 면제 시간대
- 그룹별 "사용 중 남은 시간" 오버레이(안드로이드 접근성 오버레이, 브라우저 확장 content script DOM 삽입, 데스크탑 우측 상단 소형 위젯 — 코너 위젯인 이유는 [[DECISIONS.md]])
- 뽀모도로 휴식 연동: 완전히 별개 웹앱인 "공부앱"의 Firebase RTDB를 읽기 전용 폴링, `pomodoroUnlockEnabled` 켠 그룹만 휴식 시간 동안 임시 해제 + 전용 오버레이 표시
- **공부 잠금 + 네이티브 타이머(2026-08-06~07, 15~16·19·24차 세션)**: 공부앱 타이머가 "공부" 페이즈로 진행 중일 때(휴식 중엔 아님) 데스크탑/안드로이드에 전체화면 잠금 + 허용 프로그램(플랫폼별)·사이트(각 기기에 개별 입력, 24차부터 Firebase 공유 아님) 예외. 24차 세션부터 타이머 자체가 네이티브(양 플랫폼 "타이머" 탭, `StudyTimerScreen`)로 동작 — 잠금 판정/정지·전환 버튼 모두 로컬 상태를 직접 읽고 쓰며(네트워크·웹뷰 불필요), Firebase `pomodoro` 노드는 다른 기기에 신호를 주는 부가 채널로만 남음. **37차**: 이 기기 로컬 타이머가 꺼져 있어도 같은 계정의 다른 기기가 공부 페이즈 실행 중이라는 신호(`PomodoroSyncClient.isStudyTimerActive`)가 있으면 이 기기도 실제로 공부 잠금(전체화면+사이트 차단)이 걸리도록 확장(기존엔 34차의 "표시만 하는 미러"였음) — 허용 목록은 여전히 각 기기 로컬 값을 쓰고, 원격 신호로 잠긴 경우 정지/전환 버튼은 숨기고 안내 문구만 표시(원격 제어는 하지 않음, 19→24차 원칙 유지). 신호가 20분 넘게 갱신 안 되면 유령 신호로 보고 무시(영구 잠김 방지 안전장치). 자세한 설계는 [[DECISIONS.md]] "1단계(타이머/뽀모도로) 네이티브 재구현", "공부앱을 웹/웹뷰가 아닌 완전 네이티브로 재구현", "크로스디바이스 공부 잠금 강제 적용"(37차) 참고
- **네이티브 캘린더(2026-08-07, 25차 세션)**: 양 플랫폼 "캘린더" 탭(`CalendarScreen`) — 월 그리드 + 선택 날짜 상세, 회독(1~4단계 색상) 완료 시 자동 다음 회독 생성(웹앱 로직 그대로), 순서변경/이름수정/이동/복사/삭제, 6개월 이전 정리. Firebase `users/{user}/calendar` 경로로 기존 웹앱과 동일 스키마 공유(전체 문서 LWW) — 24차의 "허용 프로그램/사이트는 기기별 로컬"과 달리 캘린더는 기기 간 동기화 대상. 타이머 탭의 "업무 이름" 자유 텍스트가 "오늘 캘린더 일정" 드롭다운으로 교체됨. "할당량 연동"(계산기 연동)은 3단계 이후로 제외. 자세한 설계는 [[DECISIONS.md]] "2단계(캘린더) 네이티브 재구현" 참고
- **네이티브 계산기(2026-08-07, 27차 세션)**: 양 플랫폼 "계산기" 탭(`CalculatorScreen`, 입력/결과/저장됨 3개 서브탭) — 업무 입력 카드(요일별 목표+휴일 제외) → `CalcEngine`(웹앱 calculate()/simulateFinish()/calcRequiredPace() 그대로 이식)로 계산 → 진척도/요일별 페이스 비교표/완료 예상일 결과 → 저장(사용자 요청으로 폴더 트리 포함 웹앱과 동일 기능: 하위 폴더 생성/이름변경/삭제/순서변경, 항목 폴더 이동). Firebase `users/{user}/calculator` 경로를 기존 웹앱과 동일 스키마로 공유하되, draft(`tasks`)/저장됨(`saved`)/폴더 트리(`savedFolderTree`+`savedFolderOrder`) 3구간을 독립 LWW 타임스탬프로 각각 비교(진입 시 1회 동기화, 웹앱처럼 실시간 구독은 아님 — 캘린더와 같은 단순화). 캘린더 연동(`linkedCalc`/`progressStep`)은 이번에도 제외(사용자 확인). 자세한 설계는 [[DECISIONS.md]] "3단계(계산기) 네이티브 재구현" 참고
- **2단 탭 구조 + 데스크탑 좌우 분할 레이아웃(2026-08-07, 28차 세션)**: 그룹/통계/타이머/캘린더/계산기/설정이 나란히 있던 구조를 "관리앱"(그룹/통계)·"공부앱"(타이머/캘린더/계산기)·설정 3섹션으로 재편(49차에 "루틴" 섹션 추가돼 4섹션, **50차에 순서를 루틴→공부→관리→설정으로 재배열 + "공부앱"/"관리앱" 라벨에서 "앱" 제거해 "공부"/"관리"로 축약**). 데스크탑은 왼쪽 `NavigationRail` 사이드바+섹션 내부 `TabRow` 서브탭, 안드로이드는 하단 `NavigationBar`+섹션 내부 `TabRow` 서브탭(`MainActivity.kt`의 `ManageSection`/`StudySection`). 이어서 데스크탑 최상위 창에 배경 `Surface`가 없어 여백이 흰색으로 비치던 버그 수정(`Main.kt`), 데스크탑 타이머/캘린더/계산기 3개 화면을 좌우 분할(마스터-디테일) 레이아웃으로 재작성. 안드로이드는 이 레이아웃 변경 대상 아님(세로 레이아웃 유지). **테마는 28차 당시 다크+파랑 → 49차 라이트+그린 전면 교체 → 50차에 라이트+그린/다크+파랑/화이트+오렌지 3종 선택제로 확장**(설정 화면에서 전환, 아래 루틴앱 항목 참고) — 자세한 판단 근거는 [[DECISIONS.md]] "관리앱 UI 전면 개편"(28차)/49~50차 참고.
- **루틴앱 v1→v2: 반복 할 일/습관/일과표 통합 + 오늘/통계 2탭 개편 + Firebase 동기화 + 앱 전체 테마 3종 선택제(2026-08-13, 47~50차 세션)**: `Routine`(제목/시간대/요일마스크/스트릭 추적)+`RoutineLog` 모델 하나로 "체크리스트"/"습관"/"시간대별 일과표" 세 요구를 통합(플래그·파생 뷰 차이일 뿐 별도 저장 없음). 양 플랫폼 "루틴" 섹션은 49차엔 오늘/일과표/습관 3탭이었으나 **50차에 오늘/통계 2탭으로 개편** — 일과표는 별도 탭 없이 오늘 탭 루틴 목록이 항상 `timeSlot` 기준 시간순으로 보이는 것으로 흡수, 습관 탭은 "현재 스트릭/오늘 완료/오늘 완료율/최고 스트릭" 지표 카드 중심의 통계 탭으로 교체. 오늘 탭엔 **50차**에 일~토 요일 피커(안드로이드는 스크롤 대신 `weight(1f)` 균등분할 칩으로 확정 — [[BUGS.md]] 50차 참고)+주 단위 이동(◀/▶)도 추가돼 과거/미래 날짜를 조회 가능(스트릭 계산은 항상 실제 오늘 기준 유지). 편집은 `GroupEditScreen`과 달리 `AlertDialog` 기반 다이얼로그이며, **50차에 방어권 종류/횟수를 직접 입력하던 UI를 제거하고 항상 "주 2회"로 고정**(안내 문구만 남김). 스트릭은 `RoutineEngine.currentStreak()`(양 플랫폼 대칭 복제, `CalcEngine.kt`와 같은 패턴)이 매번 로그를 다시 훑어 계산하는 순수 함수. **49차에 앱 전체 테마를 다크+파랑(28차/35차)에서 라이트+그린(Primary `#8BC34A`, 배경 `#FAFBF6`)으로 뒤집었고, 50차엔 이를 3종 선택제(라이트+그린/다크+파랑/화이트+오렌지)로 확장**(설정 `SectionCard`, `ThemeMode`+`PhoneLockPalette` 구조) — `MaterialTheme`을 안 쓰는 하드코딩 accent(차단/실행확인 화면, 오버레이 타이머 텍스트, 브라우저 확장 `overlay.js`)는 49차 교체분(옛 파랑→초록) 그대로 유지, 브라우저 확장 자체의 다크 CSS 팔레트는 범위 밖. **50차에 루틴 Firebase 동기화 신규 추가**(47~49차엔 로컬 전용) — 안드로이드 Room auto-increment ID와 데스크탑 수동 카운터 간 충돌을 피하려 루틴을 배열로, 로그는 배열 인덱스로 부모 루틴을 참조하는 방식(`routinesToJson`/`routinesFromJson`), 캘린더/계산기와 같은 전체 문서 LWW. 자세한 설계는 [[DECISIONS.md]] 47~50차 참고, 상세 내용은 [[CHANGELOG.md]] 48~50차 참고. **50차 신규 기능 전부 실기기 미검증.**
- **공부앱 계산기↔캘린더 연동 기능 이식 + 안드로이드 홈스크린 위젯(2026-08-13, 50차 세션)**: 원본 웹앱(`공부앱/index.html`)엔 있었지만 25~30차 네이티브 재구현 때 명시적으로 제외됐던 "캘린더 일정에 계산기 업무를 연결하면 완료 시 진행량이 자동 차감/복원되는" 연동을 이번에 이식 — `Repository`/`PhoneLockRepository`에 `linkedProgressAmount`/`addLinkedCalendarTask`/`isLinkedGoalAchieved`/`adjustLinkedCalcProgress` 신규, `CalendarScreen.kt` 날짜 상세에 `LinkedCalcSection`(업무 선택은 `FlowRow`로 오버플로우 방지), `TimetableScreen.kt`가 목표 달성 일정에 ✅ 표시. 8단계 체계 전환으로 연동 트리거 색을 웹앱의 "red"(1회독)에서 "white"(현재 1회독)로 조정 — [[DECISIONS.md]] 50차 참고. **회독을 4단계→8단계 무지개(하양~보라)로 확장, 간격은 처음 1,1,3,7,14,30,60일로 짰다가 에빙하우스 망각곡선/spacing effect 검토 후 1,3,7,14,30,60,120일(매 단계 ~2~2.3배)로 재조정** — 저장된 `color` 값 자체는 안 바뀌고 라벨 매핑만 바뀌어 과거 일정은 다른 회독 번호로 보이게 됨(의도된 재라벨링, 사용자 승인). 데스크탑 캘린더 월그리드에도 안드로이드처럼 날짜 칸 완료 배지(초록/노랑/빨강) 추가. **안드로이드 홈스크린 위젯 신규**(루틴 목록을 홈 화면에서 체크) — 새 Gradle 의존성 없이 기존 `RemoteViews`/`AppWidgetProvider`/`RemoteViewsService` 인프라로 구현. 전부 실기기 미검증.
- **네이티브 일정표(2026-08-07, 29차 세션)**: 양 플랫폼 "공부앱" 섹션에 4번째 서브탭 `TimetableScreen` 추가 — 계산기 draft 업무(`repository.getCalcTasks()`)를 요일별 목표량 표로 보여주는 읽기 전용 파생 뷰. 데스크탑은 웹앱과 동일하게 이번 주(일~토) 고정 테이블(업무×요일+합계 행), 안드로이드는 일 단위 리스트+◀/▶ 날짜 이동(웹앱 모바일 뷰와 동일). 캘린더 연동(`linkedCalc` 완료 체크마크)은 3단계와 마찬가지로 제외. 자세한 설계는 [[DECISIONS.md]] "4단계(일정표) 네이티브 재구현" 참고.
- **네이티브 통계(2026-08-07, 30차 세션)**: 양 플랫폼 "공부앱" 섹션에 5번째(마지막) 서브탭 `StudyStatsScreen` 추가 — 캘린더 일정 전체(`repository.getAllCalendarTasks()`/`getAllCalendarTasksOnce()`)를 집계해 전체/완료/완료율/연속 완료일(스트릭)/회독 단계별 완료 현황/최근 30일 완료 추이 막대그래프로 보여주는 읽기 전용 파생 뷰. 계산기 연동(`linkedCalc`)은 3~4단계와 마찬가지로 제외. 이로써 공부앱 네이티브 재구현 5단계 로드맵(타이머→캘린더→계산기→일정표→통계) 전체 완료. 자세한 설계는 [[DECISIONS.md]] "5단계(통계) 네이티브 재구현" 참고.
- **계산기 버그 3건 수정(2026-08-07, 31차 세션)**: ① 데스크탑 계산 결과 크래시(제네릭 타입 소거로 인한 `filterIsInstance` 오작동 → `ClassCastException`) 수정, ② 저장됨 항목의 폴더가 목록에 안 뜨는 문제(레거시 웹앱 데이터가 `savedFolderTree` 없이 항목별 `folderPath`만 가진 경우) — 양 플랫폼 `Repository`/`PhoneLockRepository`에 `healCalcFolderPaths()` 자동 보정 추가, ③ 데스크탑 계산기 레이아웃을 28차의 입력/결과/저장됨 동등 3탭 구조에서 웹앱과 동일한 좌(2):우(8) 사이드바 구조(왼쪽 업무입력/저장됨 서브탭, 오른쪽엔 항상 결과)로 재작성, 저장됨도 안드로이드판과 같은 재귀 폴더 트리로 회귀. 자세한 원인은 [[CHANGELOG.md]] 31차 세션 참고.
- **계산기 기능 보강 + 웹앱 소스 기반 전면 UI/색상 재검토(2026-08-07, 32차 세션)**: 계산기에 폴더 접기 영속화(`calcFolderCollapsed`)·카드 개별 접기·모두 펴기/접기·전체 저장·입력 하단 버튼 고정 추가, 안드로이드 타이머 탭엔 인라인 허용 앱 선택 섹션 신규(`StudyLockAppsScreen`의 `AllowedAppsPickerBody` 재사용). 이어서 사용자가 웹앱과 나란히 비교하며 지적 → 계산기 결과 카드/타이머/캘린더 3개 화면을 `공부앱/index.html`의 실제 CSS/JS를 직접 확인해 전면 재구현(스크린샷 추측 방식은 여러 번 틀려서 폐기 — [[DECISIONS.md]] "UI를 웹앱과 맞출 때는..." 참고). 타이머 "오늘 캘린더 일정" 드롭다운이 선택이 안 바뀌던 버그를 `ExposedDropdownMenuBox`로 교체해 수정. 자세한 내용은 [[CHANGELOG.md]] 32차 세션 참고.
- **32차 신규 버그 4건 수정(2026-08-07, 33차 세션)**: 데스크탑 공부 잠금 허용 프로그램이 `ProcessBuilder`의 PATH 전용 검색 한계로 실행 안 되던 문제를 레지스트리 App Paths→시작 메뉴 바로가기(.lnk)→PATH 3단계 폴백(`resolveAppPath()`)으로 해결(자세한 배경은 [[DECISIONS.md]] "공부 잠금 허용 프로그램 실행..." 참고), 공부 기록이 "오늘의 공부 기록"에 안 남던 건 저장 로직이 아니라 잠금 화면 경로에서 타이머 탭 UI 상태가 안 갱신되던 표시 버그였음(tick 루프에 `todayLog` 재조회 추가), 안드로이드 허용 앱 선택창이 안 뜨던 건 `AllowedAppsPickerBody`의 중첩 `LazyColumn`+`weight()` 조합이 무한 높이 부모(`verticalScroll`) 안에서 렌더링이 깨지던 문제였음(`weight()` 제거, `heightIn(max)`만 사용), 계산기 결과 카드 비율은 고정 2열 그리드를 웹앱과 동일한 `LazyVerticalGrid(GridCells.Adaptive(minSize=320.dp))`로 교체해 해결. 자세한 내용은 [[CHANGELOG.md]] 33차 세션 참고.
- **32차 아이디어 6건 전부 구현 + 타이머/공부기록 크로스디바이스 동기화(2026-08-07, 34차 세션)**: 탭 이름 변경, 데스크탑 일정표 주 이동(`weekOffset`), 캘린더 날짜 상세 타이머 기록(날짜 옆 총시간+업무별 개별 시간), 저장됨 폴더 팝오버(`DropdownMenu`), 웹앱 애니메이션(phase 배지 pulse, 카드 fadeIn+slideUp), 관리앱 좌우 분할(그룹/통계) — 6건 전부 완료. 이어서 사용자 요청으로 **타이머 크로스디바이스 미러**(이 기기 타이머가 꺼져 있을 때 다른 기기가 재고 있으면 시작 없이 메인 화면이 자동으로 실시간 상태를 보여줌, 제어는 제외 — `pomodoro` push에 `phaseStartedAt`/`taskName` 추가 + 20분 초과 시 무시하는 `remoteUpdatedAtMillis()`)와 **공부 기록(StudyLogEntry) 진짜 동기화**(`users/{user}/studyLog/{날짜}/{기기}`, dailyUsage와 같은 기기별 키 패턴, 로컬엔 병합 안 하고 표시용 캐시로만 합산)를 신규 구현. 캘린더 날짜 상세가 "진입 시 1회만 동기화"하던 버그(패널을 계속 열어두면 갱신 안 됨)를 실제 Firebase 서버 상태 REST API 직접 조회로 디버깅해 발견, 5초 주기로 수정. 사용자가 안드로이드 실기기에서 공부기록 동기화 정상 작동 확인. 자세한 설계는 [[DECISIONS.md]] "공부 기록 크로스디바이스 동기화"/"타이머 크로스디바이스 '미러' 표시" 참고, 상세 내용은 [[CHANGELOG.md]] 34차 세션 참고.
- **Alt-Tab 버그 원인 확정 + 캘린더 미완료 자동복사 + 확장 다크테마 통일 + accent 색상 교체(2026-08-08, 35차 세션)**: ① Alt-Tab 사이트 차단 우회 버그 — `background.js`가 `onBeforeNavigate`(새 네비게이션)에만 반응해서 이미 열려있던 탭으로 Alt-Tab만 하면 최대 60초(1분 tick 폴링 주기)까지 실제로 이용 가능하다는 원인을 코드로 확정, 수정(탭/창 포커스 리스너 추가)은 사용자 요청으로 보류. ② 캘린더 "미완료(X)" 시 다음날로 같은 업무 자동 복사 — 기존 "완료(O)→다음 회독 자동 생성" 패턴과 대칭되는 `applyIncompleteCarryOver`/`revertIncompleteCarryOver` 추가(원본은 그대로 남김). ③ 브라우저 확장 `confirm.html`/`blocked.html`/`onboarding.html`이 28차 다크테마 개편에서 빠져있던 걸 공부앱과 동일한 팔레트로 통일. ④ "후자" 버튼/오버레이 강조색을 주황(`#FF9800`)에서 파란 accent(`#4F8EF7`)로 앱 전체 9개 파일 교체(사용자 요청). ⑤ "어제 공부기록/캘린더 일정이 오늘도 보인다"는 문의에 `dailyResetHour=9`(오전 9시 리셋) 설정 때문임을 로컬 데이터파일로 확인해 설명 — 코드 변경 없음, 사용자가 유지 결정. 자세한 설계는 [[DECISIONS.md]] "앱 전체 강조색을..."/"캘린더 '미완료(X)' 시..." 참고, 상세 내용은 [[CHANGELOG.md]] 35차 세션 참고.
- 잠김 화면(스케줄/일일한도)이 실행확인 화면과 같은 톤으로 통일(제목=조롱조 문구 로테이션, "진행"/"중단" 버튼), 안드로이드/데스크탑/브라우저 확장 3곳 공통
- 그룹 전체 on/off(`groupEnabled`)와 스케줄 on/off(`scheduleEnabled`) 분리, `enabled`는 통계 탭 필터 전용
- 데스크탑 로컬 API 토큰 인증(`/pair`) + 상태변경 요청(`/confirm`/`/tick`/`/heartbeat`) POST+토큰 필수
- 데스크탑 ↔ 브라우저 확장 heartbeat 경고 배너, Android 접근성 서비스 watchdog(WorkManager 15분 주기)
- 원자적 데이터 저장(JsonStore `.tmp`+`ATOMIC_MOVE`), 손상 파일 자동 백업, Room 마이그레이션 전 raw SQLite 백업(`PreMigrationBackup`)
- **(2026-08-10, 38차, 39차에 컴파일 확인 완료)** 타이머 탭 상단 "오늘 한눈에" 요약(캘린더 일정/계산기 목표/누적 공부시간), 통계 탭 "최근 7일 vs 지난 7일 완료율" 비교와 "이상 사용 패턴" 경고 배너(7일 평균 대비 1.5배), 그룹별 "재확인 통과 횟수" 카운터(로컬 전용, `ConfirmationGate.kt` 미변경), 데스크탑 일일 자동 백업/복원(최근 7일 보관), 양 플랫폼 사용기록 CSV 내보내기, 설정에서 12개월 이상 지난 통계 수동 정리, 안드로이드 공부 잠금 중 방해금지 모드 자동 적용. 전부 판정 로직 미변경 — 자세한 목록은 [[CHANGELOG.md]] 38차 참고.
- **(2026-08-10, 39차, 실기기 미검증)** 공부 세션 정지 시 짧은 회고 입력(`StudyLogEntry.note`), 데스크탑 설정/그룹 내보내기·가져오기(안드로이드는 기존부터 있었음), **그룹 일시정지(스누즈)**(그룹별 설정 시간, 하루 3회 제한, `LockEvaluator.isSnoozeActive()`가 `isPomodoroUnlocked()`와 동일한 패턴으로 판정을 일시 우회), **기간 지정 자동 강화**(시험기간 등, `forceEnabledFrom~Until` 범위 안이면 `groupEnabled` 스위치를 꺼도 `LockEvaluator.isGroupActive()`가 켜진 것으로 강제 취급 — 스누즈보다 우선). 스누즈/기간 지정은 이번에 처음으로 `LockEvaluator` 핵심 판정 함수를 확장한 기능이라 다른 기능들과 달리 실기기 검증 우선순위가 높음 — [[DECISIONS.md]]/[[CHANGELOG.md]] 39차 참고.
- **스누즈 크로스디바이스 동기화(2026-08-11, 41차, 실기기 미검증)**: 39차의 스누즈가 그룹 자체를 Firebase로 안 올리는 구조상 기기별 로컬 전용이었던 걸 41차에 동기화 추가. `confirmSync`와 동일한 "최신값(종료시각) 승리" 병합 패턴으로 `users/{user}/snoozeSync/{그룹명}` 신설, 하루 3회 한도도 병합값 기준으로 재판정(기기 나눠 써도 총 3회). 그룹 목록 배지(`isSnoozeActive`)는 Compose 리컴포지션 자리라 여전히 로컬 값만 즉시 조회 — 판정 게이트(`isConfirmActiveNow`/`evaluate`/`isCurrentlyRestricting`)만 동기화된 값을 씀. 안드로이드 `isConfirmActiveNow()`가 이 때문에 `suspend fun`으로 바뀜(호출부 전부 이미 suspend라 안전). 양 플랫폼 컴파일 확인 + APK/jar 빌드 산출물에서 `snoozeSync` 문자열 실제 포함 확인 + 안드로이드 APK 두 위치 갱신 + 데스크탑 표준 절차로 재배포·재실행까지 완료. 자세한 설계는 [[DECISIONS.md]] "스누즈(#1) 크로스디바이스 동기화..." 참고, 상세 내용은 [[CHANGELOG.md]] 41차 참고.
- **그룹 자동 재활성화 + 버튼 문구 고정 + 조롱조 문구 강도별 로테이션(2026-08-12, 42차, 실기기 미검증)**: ① `dailyResetHour` 기준 "오늘" 날짜가 바뀌면 꺼둔 그룹(`groupEnabled=false`)을 자동으로 다시 켜는 `applyDailyGroupResetIfNeeded()` 신규(`AppData.lastGroupAutoResetDate`로 하루 1회만 적용, 매 tick 시작 지점에서 호출). ② 실행확인/잠김 화면 버튼 라벨 "전자"/"후자" → "진행"/"중단"으로 고정(동작은 그대로). ③ 실행확인/잠김 화면 제목("무시당하면서 살기, 무시하면서 살기" 자리)에 조롱조 문구 124개(순한~극한 5단계)를 강도별로 로테이션 — 실행확인은 재확인 레벨(절대 수치, `overlayLevelStepsToMax`와 무관), 잠김(스케줄/일일한도)은 오늘 이 그룹을 열려던 시도 횟수(`Group.blockAttemptDate`/`blockAttemptCount` 신규, 스누즈 카운터와 동일 패턴)가 기준. `MotivationalQuotes.kt`(양 플랫폼)/`quotes.js` 신규 구성. 자세한 설계는 [[DECISIONS.md]] 42차 참고, 상세 내용은 [[CHANGELOG.md]] 42차 참고.
- **사용 중 오버레이 재설계 + 실행확인 가독성 개선(2026-08-12, 43차, 안드로이드만 실배포)**: ① 사용 중 남은시간 오버레이("무시당하면서 살기..." 제목+문구)에서 문구를 완전히 삭제하고 타이머 숫자만 화면 정중앙에 훨씬 크게(안드로이드 32sp→64sp, 브라우저 확장 32px→96px) 표시, 타이머 텍스트 색상도 배경과 같은 alpha 값을 적용해 오버레이의 일부로 취급(레벨 0일 땐 거의 안 보이다가 재확인 레벨이 오를수록 배경과 함께 또렷해짐) — 안드로이드 `AppMonitorAccessibilityService.kt`/브라우저 확장 `overlay.js` 양쪽 적용, 데스크탑 코너 위젯(`UsageOverlayContent.kt`)은 애초에 문구가 없던 구조라 그대로 둠(사용자 확인). ② 실행확인/잠김 화면의 체크포인트 힌트 문구("이게 의무입니까?" 등)를 작고 흐린 텍스트에서 굵고 밝은 텍스트로 가독성 개선 — 안드로이드 `InterstitialScreen.kt`, 데스크탑 `WatchAndWaitScreen.kt`, 브라우저 확장 `confirm.html` 3곳 모두 적용(데스크탑은 아직 재빌드/재배포 안 함). ③ 제목 문구가 길어지면 2줄로 꺾여 가독성이 떨어지던 걸 안드로이드에 자동 축소 로직으로 해결(`InterstitialScreen.kt`) — 처음엔 `maxLines=2`+`didOverflowHeight` 조합으로 구현했다가 "2줄에 정확히 들어가는 대부분의 케이스에선 애초에 안 줄어든다"는 사용자 지적으로 `maxLines=1`+`didOverflowWidth`(한 줄에 안 들어가면 무조건 축소, 최소 55%)로 재작업. 데스크탑은 창 폭이 넓어 줄바꿈 위험이 낮다고 판단해 적용 안 함(사용자 확인). 안드로이드 APK 두 위치 재빌드/배포 2회(1차 축소 로직 포함 → 2차 축소 로직 수정본) 완료.
- **태블릿 제목 축소 제외 + 조롱조 문구 25개 추가(2026-08-12, 44차, 안드로이드만 실배포)**: ① 43차의 제목 자동 축소(`maxLines=1`+`didOverflowWidth`)가 화면 폭 넓은 태블릿에도 똑같이 적용돼 있었는데, 태블릿은 폰과 달리 한 줄로 욱여넣을 필요가 없다는 지적을 받고 `LocalConfiguration.current.screenWidthDp >= 600`(표준 sw600dp 기준)으로 분기 — 태블릿이면 데스크탑 `WatchAndWaitScreen.kt`와 동일하게 축소 없이 `Text(title, style=...)`만 호출해 필요하면 자연스럽게 2줄로 감싸지게 함(`InterstitialScreen.kt`). ② 42차에 만든 5단계(순한~극한) 조롱조 문구에 각 단계 5개씩 총 25개 추가(124→149개) — `MotivationalQuotes.kt`(안드로이드/데스크탑) + `quotes.js`(브라우저 확장) 3곳 내용 동일하게 유지. 안드로이드는 두 변경 모두 포함해 APK 두 위치 재빌드/배포 완료. 데스크탑·브라우저 확장은 문구 소스 파일만 갱신됐고 아직 재빌드/재배포·재로드 안 함(43차부터 밀린 데스크탑 재배포와 함께 처리 권장).

---

# 현재 진행 중인 작업

- **56차 세션 완료**([[CHANGELOG.md]]/[[BUGS.md]] 56차 참고): 사용자 보고 "안드로이드 루틴 알림이 2분 정도 늦게 온다"에서 시작 — ① `RoutineAlarmScheduler.kt`가 52차부터 쓰던 부정확 알람(`setAndAllowWhileIdle`)을 `setExactAndAllowWhileIdle`로 전환(권한 없으면 자동 폴백), `SCHEDULE_EXACT_ALARM` 매니페스트 권한 + 설정 화면 허용 버튼 신규. ② 사용자의 추가 요청("다른 원인도 있는지 파악")으로 점검하다 별개 버그 발견 — 다른 기기에서 만들거나 수정한 루틴은 `MainActivity.onCreate`의 앱 실행 시 재예약이 로컬 DB만 봐서 "루틴" 탭을 직접 열기 전엔 알림이 아예 예약 안 됐음(지연이 아니라 누락), `syncRoutinesFromFirebase()`를 `rescheduleAll()` 앞에 추가해 수정. 양 빌드 성공 확인 후 안드로이드 APK 두 위치 갱신. **둘 다 실기기 미검증**.
- **55차 세션 완료**([[CHANGELOG.md]]/[[DECISIONS.md]] 55차 참고): 독립된 두 요청 — ① **스누즈 진행 중 그룹 설정 변경 자유화**(`LockEvaluator.detectWeakeningEdit()`에 스누즈 예외 추가, 양 플랫폼), ② **스케줄 => 일일 한도 => 실행 확인 우선순위 정렬**(겹치는 관리 종류가 있을 때 잠금 판정을 확인창 판정보다 먼저 검사하도록 안드로이드 2곳/데스크탑 2곳 호출 순서 재배치). 양 플랫폼 컴파일 확인 + 안드로이드 APK 두 위치 갱신 + 데스크탑 표준 배포 절차 완료. **둘 다 실기기 미검증** — 아래 "다음 작업 우선순위" 최상단 참고.
- **54차 세션 완료**([[CHANGELOG.md]]/[[DECISIONS.md]]/[[BUGS.md]] 54차 참고): **데스크탑 루틴 아이콘 재시작 시 소실 버그 수정(사용자 직접 확인 완료)** — 원인은 52차에 추가된 `Routine.icon`/`notifyEnabled`/`startDate`/`endDate` 4개 필드가 로컬 저장 파일(`JsonStore.kt`)에 빠져 있었던 것 + LWW 타임스탬프가 push 시점에 원격과 정확히 같아져서 재시작해도 자동 재동기화가 안 되는 함정. `JsonStore.kt` 수정 + 이미 손상된 로컬 데이터는 `routinesTs` 강제 리셋으로 복구. 이 세션은 드물게 실기기(사용자 데스크탑) 검증까지 완료됨.
- **53차 세션 완료**([[CHANGELOG.md]]/[[DECISIONS.md]]/[[BUGS.md]] 53차 참고): **계산기 저장 항목 34개 Firebase 유실 복구(사용자 직접 확인 완료)**, 회유 절차 20개 조롱조 전환, 조롱조 문구 25개 추가(149→174), 테마 하드코딩 버그 전수 수정(실행확인/차단 "중단" 버튼, 사용 중 오버레이 4곳), 안드로이드 홈위젯 테마 연동, **브라우저 확장 프로그램 테마 연동 신규**(`/theme` API+`theme.js`), 루틴 알림 플로팅(헤드업)+진동 추가(새 채널 v2), 루틴 파일 내보내기/가져오기 신규(양 플랫폼), **테마 3종→8종 확장**(라벤더/민트/로즈/미드나잇/포레스트 신규), 안드로이드 테마 선택 UI `FlowRow` 개편(8종 찌부러짐 수정), 태블릿 통계 탭 30일 그래프 레이아웃 수정(공부앱/루틴앱) — 계산기 복구 외 전부 실기기 미검증.
- **52차 세션 완료**([[CHANGELOG.md]]/[[DECISIONS.md]]/[[BUGS.md]] 52차 참고): 릴스/쇼츠 감지 mojibake 버그 수정, 루틴 순서변경(▲/▼, 시간대 없는 루틴만)+복사, 그룹 복사, 루틴 아이콘/알림/스트릭알림/기간설정(Room 26→27) — 코드/빌드 기준 완료, 실기기 미검증. 이 DB 버전업이 유발한 동기화 버그(캘린더/루틴/계산기 동기화 중단+그룹 유실)는 53차에 계산기까지 포함해 복구 확인 완료.
---

# 다음 작업 우선순위

- [ ] **최우선(56차): 이번 세션 신규 기능 실기기 검증** — ① 설정 화면에서 "정확한 알람 허용하기"를 누른 뒤 루틴 알림이 지연 없이 정시(±몇 초)에 오는지(허용 안 한 상태에선 예전처럼 늦게 오는 게 정상, 폴백 확인용으로 대조). ② 데스크탑(또는 다른 폰)에서 루틴을 새로 만들거나 시간대/알림을 수정한 뒤, 이 폰에서 "루틴" 탭은 열지 않고 앱만 켰다 껐을 때도 그 루틴의 알림이 정상적으로 오는지(수정 전엔 안 왔음).
- [ ] **최우선(55차): 이번 세션 신규 기능 실기기 검증** — ① 그룹을 스누즈한 상태에서 그룹 편집 화면에서 실행확인/일일한도를 약화시키는 방향으로 저장(또는 삭제)했을 때 회유 멘트 20개 없이 곧바로 적용되는지, 스누즈가 끝난 뒤엔 다시 회유 절차가 걸리는지, 기간 지정 자동 강화가 걸려있으면 스누즈 중이어도 회유 절차가 그대로 유지되는지(우선순위 확인). ② 한 그룹에 스케줄·일일한도·실행확인을 모두 켜두고 스케줄 차단 시간대이면서 동시에 실행확인 대상인 상황을 만들어 확인창 없이 곧바로 차단 화면이 뜨는지(안드로이드 앱/사이트, 데스크탑 프로그램/브라우저 확장 전부), 일일한도 초과 상태에서도 마찬가지인지, 반대로 관리 종류가 하나만 겹칠 땐 기존처럼 정상 동작하는지.
- [ ] **최우선(53차): 이번 세션 신규 기능 실기기 검증** — ① 안드로이드 홈위젯 테마 연동(테마 바꾸면 위젯 배경/글자색/체크아이콘도 즉시 바뀌는지, 8종 전부), ② 테마 버그 수정 확인(실행확인/차단 화면 "중단" 버튼과 사용 중 오버레이 타이머가 어떤 테마에서도 각 테마 색을 따라가는지, 안드로이드 접근성 오버레이 포함), ③ 브라우저 확장 테마 연동(`chrome://extensions` 재로드 후 confirm.html/blocked.html/onboarding.html/사용 중 오버레이가 데스크탑에서 고른 테마를 따라가는지, 데스크탑 앱 꺼져 있을 때 라이트+그린 기본값으로 안전하게 보이는지), ④ 루틴 알림이 실제로 화면 위 플로팅(헤드업)으로 뜨고 진동이 오는지(새 채널이라 기존 알림 설정과 무관하게 기본 ON이어야 함), ⑤ 루틴 내보내기/가져오기 파일이 정상 저장되고 가져오기 후 루틴/체크/Firebase 동기화가 꼬이지 않는지, ⑥ 새로 추가된 조롱조 문구 25개(174개 중)가 로테이션에 실제로 섞여 나오는지, ⑦ 회유 절차 20단계가 조롱조 톤으로 바뀐 채 정상 작동하는지(그룹/루틴 설정 약화·삭제 시), ⑧ **신규 테마 5종**(라벤더/민트/로즈/미드나잇/포레스트)이 실제로 선택·저장·재시작 후 유지되고 색상 대비가 실사용에서 괜찮은지, ⑨ 안드로이드 설정 화면 테마 선택이 8종 다 줄바꿈돼서 잘리지 않고 보이는지, ⑩ **태블릿 실기기**에서 공부앱/루틴앱 통계 탭 30일 그래프가 스크롤 없이 폭 전체를 채우는지(태블릿 실기기가 없으면 큰 화면 에뮬레이터로라도 확인).
- [ ] **최우선(52차, 부분 해결): 그룹 데이터 복구 결과 확인** — 캘린더/계산기는 53차에 재동기화 확인 완료(계산기는 원격 데이터 자체 유실이라 별도 복구 작업 필요했음, [[BUGS.md]] 53차 참고). **루틴 재동기화 확인은 아직 안 됨**, **그룹은 여전히 미확인** — 사용자 폰에서 "⚠ 그룹 데이터 복구"(설정 화면 최상단) 버튼을 눌러봤는지, 눌렀다면 그룹이 정상 복원됐는지(개수/설정값/멤버 앱·사이트), 복원된 그룹으로 실제 차단이 다시 작동하는지 확인. 카드 자체가 안 보인다면(백업 파일을 못 찾은 경우) 복구 경로를 다시 잡아야 함.
- [ ] **신규(52차) 실기기 검증**: 릴스/쇼츠 재감지 여부(수정 후 미검증), 루틴 ▲/▼ 순서변경(시간대 없는 루틴에서만 버튼이 뜨는지, 눌렀을 때 실제로 순서가 바뀌는지), 루틴/그룹 복사(설정값·멤버가 그대로 복제되는지), 루틴 아이콘(quick-pick+직접입력 모두 저장되는지), 루틴 알림(안드로이드 `AlarmManager` 예약이 실제로 울리는지, 데스크탑 트레이 풍선 알림이 뜨는지, 부팅 후에도 재예약되는지), 스트릭 알림(설정 토글 켜면 다음 초기화 시각에 실제로 오는지), 루틴 기간 설정(시작일/종료일 밖이면 오늘 탭에서 실제로 빠지는지).
- [ ] **신규 아이디어(50차 세션 종료 후 요청)**: 4개(아이콘/알림/스트릭알림/기간설정) 전부 52차에 구현 완료 — 위 실기기 검증 항목으로 이동. [[IDEAS.md]] 참고.
- [ ] **신규(50차) 실기기 검증**: ① 안드로이드 홈스크린 위젯 — 실제로 홈 화면에 추가되는지, 리스트 항목 클릭 시 체크가 토글되고 앱 내 상태와 즉시 일치하는지. ② 루틴 Firebase 동기화 — 기기 A에서 루틴 추가/체크 → 기기 B에도 반영되는지, 서로 다른 로컬 ID 체계(안드로이드 Room auto-increment vs 데스크탑 카운터)에서 로그-루틴 매핑이 꼬이지 않는지. ③ 계산기↔캘린더 연동 — 일정에 계산기 업무를 연결 후 완료 체크 시 계산기 진행량이 실제로 차감되는지, 취소 시 복원되는지, 8단계 체계에서 트리거 색(white=1회독)이 의도대로 동작하는지. ④ 8단계 무지개 회독 — 완료 시 다음 회독이 올바른 순서/색상으로 생성되는지, 간격(1→3→7→14→30→60→120일)이 맞는지. ⑤ 테마 3종(라이트+그린/다크+파랑/화이트+오렌지) 전환이 설정에서 즉시 반영되고 재시작 후에도 유지되는지.
- [ ] **루틴 오늘/통계 탭 개편 실기기 검증**(50차): 오늘 탭 요일 피커로 과거/미래 날짜를 조회할 때 그 날짜 기준 체크 상태가 맞는지(안드로이드 `weight` 균등분할 칩이 실제로 7개 다 보이는지), 주 이동(◀/▶)이 정상 동작하는지, 통계 탭 지표(현재 스트릭/오늘 완료/완료율/최고 스트릭)가 정확한지, 방어권 주 2회 고정이 실제로 하루 놓쳐도 스트릭을 안 끊는지(단, 주 3회째부터는 끊기는지).
- [ ] **라이트+그린 테마 실사용 확인**(49차): 새 배경/포인트 색이 실제로 보기 좋은지, 특히 밝은 연두 Primary 위 텍스트 대비가 화면 전반에서 괜찮은지 — 캘린더 "오늘" 배지 외에 눈에 띄는 대비 문제가 있으면 [[IDEAS.md]] "루틴 화면 색상 전수 감사" 참고해서 개별 수정.
- [ ] **브라우저 확장 재로드 필요(누적 미반영분)**: 44차 `quotes.js` 문구 25개, 49차 `overlay.js` accent 색(파랑→초록), **53차 신규**(`theme.js`/`confirm.html`/`blocked.html`/`onboarding.html`/`overlay.js` 테마 연동, `quotes.js` 문구 25개 추가분) — 전부 `chrome://extensions`에서 확장 재로드해야 반영됨(빌드는 불필요).
- **참고(46차)**: 현재 데스크탑 그룹 "게임"(id 11)/"제어"(id 16)는 사용자 요청으로 `groupEnabled=false`(꺼짐) 상태 — 차단이 안 걸리는 게 정상이니 버그로 착각하지 말 것. `dailyResetHour` 지나면 [[BUGS.md]]/42차 자동 재활성화 로직으로 다시 켜질 수 있음.
- [ ] **신규(44차) 확인**: ① 태블릿(화면 폭 600dp 이상 기기)에서 실행확인/잠김 화면 제목이 축소 없이 필요하면 2줄로 자연스럽게 표시되는지, 반대로 폰(600dp 미만)에서는 여전히 한 줄로 축소되는지. ② 새로 추가된 25개 문구가 각 강도(순한~극한)에서 실제로 로테이션에 섞여 나오는지.
- [ ] **신규(43차) 확인**: ① 사용 중 오버레이에 문구 없이 타이머만 중앙에 크게 뜨는지(안드로이드 접근성 오버레이+브라우저 확장, 확장은 `chrome://extensions` 재로드 필요), 레벨이 오를수록 타이머 숫자도 배경과 함께 또렷해지는지. ② 실행확인 화면에서 긴 제목이 한 줄로 축소돼 표시되는지(안드로이드).
- [ ] **신규(42차) 실기기 검증**: ① 그룹 자동 재활성화 — `dailyResetHour` 설정한 시각이 지나면 꺼둔 그룹이 실제로 자동으로 켜지는지, 초기화 이후 다시 끈 그룹은 그날 안엔 계속 꺼진 채 유지되는지. ② 실행확인 화면 진행할수록(재확인을 반복할수록) 제목 문구가 더 독한 톤으로 바뀌는지, 시간이 지나 레벨이 자연 차감되면 다시 순해지는지. ③ 스케줄/일일한도로 잠긴 화면을 반복해서 열려고 시도할수록(같은 날 안에서) 문구가 더 독해지는지. ④ 버튼 라벨 "진행"/"중단"이 데스크탑·안드로이드·브라우저 확장 3곳 전부 정상 표시되는지.
- [ ] **스누즈(#1)/기간 지정 자동 강화(#7) 기능적 실기기 검증** — `LockEvaluator` 판정 확장이라 우선순위 높음(39차 구현, 40차 데스크탑 화면 노출 확인, 41차 스누즈 크로스디바이스 동기화 추가). ① 스누즈: 그룹 목록의 "😴 스누즈" 버튼이 하루 3회까지만 눌리는지, 스누즈 중엔 스케줄/한도/실행확인이 실제로 전부 우회되는지, 스누즈 시간이 지나면 자동으로 원래대로 돌아오는지, **41차 신규**: 데스크탑에서 스누즈하면 안드로이드의 같은 이름 그룹도 실제로 풀리는지, 두 기기 합쳐 하루 3회를 넘길 수 없는지. ② 기간 지정 자동 강화: 그룹 편집에서 시작일/종료일을 오늘이 포함되게 설정 → 그룹 스위치를 꺼도 실제로는 계속 제한이 걸리는지, 범위 밖 날짜에선 평소처럼 스위치를 따르는지, 스누즈 중에 강제 기간이 시작되면 스누즈가 무시되고 즉시 다시 제한되는지. 안드로이드는 41차에 새 APK로 재빌드됐으나 실기기 재설치 여부는 미확인.
- [ ] **신규(39차) 부가 기능 실기기 확인**: 공부 세션 정지 시 회고 입력 다이얼로그가 뜨는지·"오늘의 공부 기록"에 회고가 표시되는지, 데스크탑 설정 화면의 "설정/그룹 내보내기·가져오기"가 실제로 파일을 저장/불러오는지(안드로이드는 기존부터 있던 기능이라 우선순위 낮음).
- [ ] **최우선 후보: 1~5단계 전체 실기기 검증**(28~34차 UI/기능/버그수정/크로스디바이스 동기화 변경사항 포함) — 여러 세션째 미뤄지고 있는 항목: ① 계산기 계산/저장/폴더 트리 정상 동작, ② 통계 지표/스트릭/회독별 현황/30일 그래프 정확성, ③ 관리앱/공부앱 탭 전환, 타이머 일정 선택 유지, 좌우 분할 레이아웃(34차 신규: 그룹/통계도 포함) — **테마는 49차에 다크+파랑→라이트+그린으로 바뀌었으니 "배경이 어두운지"가 아니라 새 라이트+그린 배경이 실제로 잘 보이는지로 확인**, ④ 일정표 데스크탑 주간표/안드로이드 날짜 이동(34차 신규: 데스크탑 주 이동 버튼), ⑤ 캘린더 일정 추가→타이머 드롭다운 반영, 공부 잠금 진입/해제, 허용 프로그램·사이트, 뽀모도로 크로스디바이스, 캘린더 자동 다음회독, Firebase 동기화, ⑥ 32차 세션 계산기 결과 카드/타이머/캘린더 색상·레이아웃이 웹앱과 실제로 맞는지, ⑦ 33차 수정분: 안드로이드 허용 앱 선택창이 실기기에서도 뜨는지, 계산기 카드 그리드가 창 크기에 따라 열 개수가 바뀌는지, ⑧ **34차 신규 — 이미 안드로이드에서 부분 확인된 것 외**: 데스크탑에서 타이머 미러/공부기록 동기화 실사용 확인, 캘린더 날짜 상세 폴더 팝오버·애니메이션이 실기기/실행 파일에서 자연스러운지.
- [ ] **신규 최우선 후보: 37차 크로스디바이스 신규 기능 2건 실기기 2대(또는 데스크탑+안드로이드) 검증** — 코드/빌드만 확인됐고 실사용 검증은 전혀 안 됨: ① 기기 A에서 공부 타이머(공부 페이즈) 실행 → 기기 B(로컬 타이머 꺼진 상태)도 자동으로 전체화면 공부 잠금이 걸리는지, B의 정지/전환 버튼이 숨겨지고 안내 문구가 뜨는지, A에서 타이머를 끄면 B의 잠금도 다음 tick(최대 2초) 안에 풀리는지. ② 기기 A에서 실행확인 "전자"를 눌러 통과 → 같은 이름의 그룹을 쓰는 기기 B가 그 그룹 프로그램/사이트를 실행했을 때 재확인창 없이 바로 쓸 수 있는지, B의 오버레이 남은시간이 A와 같은 종료 시각을 기준으로 표시되는지. 그룹별 새 설정 "오버레이 최고 밝기까지 재확인 횟수"도 값을 바꿔가며 오버레이 밝기가 실제로 그 횟수에 맞춰 변하는지 함께 확인.
- [ ] **Alt-Tab 사이트 차단 우회 버그 실제 수정**: 원인은 35차에 확정됨(브라우저 확장이 새 네비게이션에만 반응, 이미 열려있던 탭 전환은 최대 60초 지연) — 해결 방향(`chrome.tabs.onActivated`/`chrome.windows.onFocusChanged` 즉시 재검사 리스너 추가)도 확인했으나 사용자가 이번엔 문서화만 하고 수정은 보류하기로 결정. [[BUGS.md]] Open 참고.
- [ ] 오래된 세션(6~13차)의 개별 검증 항목(일일한도 Firebase 합산, 실행확인 레벨 동기화, 뽀모도로 오버레이, `groupEnabled`/`scheduleEnabled` 분리 등)은 위 "실기기 검증"에 포괄됨 — 상세 항목이 다시 필요하면 `CHANGELOG.md`에서 해당 차수 검색.
- [ ] Android `POST_NOTIFICATIONS` 실기기 권한 요청 플로우 확인.
- [ ] [[IDEAS.md]] "타이머 크로스디바이스 미러에 원격 정지/전환 제어 추가 검토"(34차 신규 아이디어) — 37차에 잠금 자체는 원격 신호로 걸리게 됐지만 정지/전환 "제어"는 여전히 하지 않음(의도적), 사용자가 실사용 후 불편하면 검토.

---

# 현재 주의사항

- **재확인/escalation 판정 로직에는 절대 손대지 말 것** — 표시값(오버레이 알파 등)과 판정 로직(언제 재확인을 요구할지)은 별개. 과거 이 원칙을 어겨 큰 삽질이 있었음([[DECISIONS.md]] "표시값과 판정 로직 분리" 참고). `ConfirmationGate.kt`(양쪽)는 원본 그대로 유지되어야 함.
- **사용자가 "표시/UI가 이랬으면"이라고 요청했을 때 보안/판정 로직까지 확대 해석하지 말 것**. 애매하면 먼저 확인.
- **네이티브 UI를 웹앱과 맞출 땐 스크린샷 추측 대신 `공부앱/index.html`의 CSS/JS부터 확인**(32차 세션 교훈) — 관련 클래스명을 Grep해서 정확한 hex값/구조를 얻는다. [[DECISIONS.md]] "UI를 웹앱과 맞출 때는..." 참고.
- 로컬 API에 새 엔드포인트 추가 시 호출 주체(content script vs 서비스워커/확장페이지)에 따라 CORS 정책 결정 — [[DECISIONS.md]] 참고.
- Compose Desktop `Window`는 클릭을 가로챈다 — 화면을 덮는 새 UI 만들 때 항상 고려.
- `EnforcementService.tick()`의 뮤텍스 범위를 건드릴 땐 "확인 대기는 뮤텍스 밖" 원칙 유지(안 지키면 다른 그룹 감시 전체가 멈추는 예전 버그 재발).
- 안드로이드 `PhoneLockRepository`에서 `escalationCache`/DB 접근 새 함수 추가 시 반드시 `escalationMutex.withLock { }`으로 감쌀 것(`mergedEscalation()` 자체는 잠금 없음, 재진입 데드락 주의).
- **소스는 항상 OneDrive 원본에서 편집**, 빌드는 항상 `AndroidBuilds\`에서, Android APK는 항상 두 위치 모두 갱신.
- **git push 금지**: 명시적 요청 없이 절대 push 금지.
- **데스크탑 배포 전**: 반드시 프로세스 먼저 종료(파일 잠김 방지) — 요청 없이 직접 종료해도 됨. 배포 검증 절차(프로세스 종료→동기 robocopy→FAILED 0 확인→jar 해시 비교→재실행)는 [[BUGS.md]] "robocopy가 실행 중인 프로세스의 파일을 조용히 스킵" 참고.
- **Android APK**: 코드 수정 후 요청 없어도 바로 빌드/업로드 가능, 두 위치 모두 갱신할 것.
- Room DB 버전: 스키마 변경 시 `AppDatabase.kt`의 `version` 올려야 함(현재 27 — 38차 21→22(`ConfirmCounter`), 39차 22→23(`StudyLogEntry.note`)→24(`AppGroup` 스누즈/기간지정 필드 6종), 42차 24→25(`AppGroup.blockAttemptDate`/`blockAttemptCount`), 48차 25→26(`Routine`/`RoutineLog`), 52차 26→27(`Routine.icon`/`notifyEnabled`/`startDate`/`endDate`)) — `fallbackToDestructiveMigration()`이라 마이그레이션 코드 불필요, `PreMigrationBackup`이 삭제 전 raw SQLite 백업(TABLES 목록에 `routine`/`routine_log` 포함 확인할 것).
- **Room 버전을 올릴 땐 반드시 `AppPreferences.resetSyncTimestamps()`가 호출되는지 확인할 것(52차, 심각한 버그였음)**: `fallbackToDestructiveMigration()`은 Room(SQLite)만 지우고 캘린더/루틴/계산기의 "마지막 동기화 시각"(SharedPreferences 저장)은 그대로 남겨서, 다음 동기화가 "로컬이 이미 최신"으로 오판해 텅 빈 로컬 상태를 방치하는 버그가 있었다. `MainActivity.onCreate`에서 `PreMigrationBackup.backupIfVersionChanged()`가 non-null을 반환하면(=이번 실행에서 버전이 바뀌어 백업했으면) 자동으로 리셋되므로 이 호출 자체를 지우지 말 것 — 새 SharedPreferences 기반 동기화 타임스탬프를 추가할 땐 `resetSyncTimestamps()`에도 같이 넣을 것. [[DECISIONS.md]]/[[BUGS.md]] 52차 참고.
- **그룹(차단 대상 목록)은 Firebase에 동기화되지 않는다** — Room이 destructive migration으로 지워지면 그룹은 자동 복구가 안 되고, `PreMigrationBackup`이 만든 raw SQLite 백업 JSON에서 `PhoneLockRepository.restoreGroupsFromBackup()`(설정 화면 "⚠ 그룹 데이터 복구" 카드)으로만 복구 가능(52차 신설). 앞으로 이 종류의 로컬 전용 데이터를 다루는 새 테이블을 추가하면 같은 유실 위험이 있다는 걸 염두에 둘 것.
- **화면 문구를 바꿔달란 요청을 받으면 그 문구가 제목(title)인지 부제(quote)인지 먼저 화면에서 확인할 것(42차 교훈)**: `WatchAndWaitScreen`/`InterstitialScreen`은 title(큰 헤드라인)과 quote(작은 부제) 두 개의 별도 텍스트 슬롯을 갖는데, 42차에 "무시당하면서 살기, 무시하면서 살기"(title) 교체 요청을 받고도 실제로는 quote 슬롯(원래 "의무에 따라 행동하세요")에 새 문구를 걸었다가 사용자 지적으로 뒤늦게 바로잡았다. 사용자가 인용한 문구 원문과 코드의 어느 파라미터가 실제로 그 문자열을 갖고 있는지 대조부터 할 것.
- **크로스디바이스 신규 동기화 추가 시(34차 결정)**: `pomodoro`/`confirmSync`처럼 "신호"만 필요하면 24차 패턴(로컬이 source of truth, Firebase는 부가 채널)을 따르고, `dailyUsage`/`studyLog`처럼 "여러 기기가 각자 추가하는 실제 데이터"면 문서 전체 LWW(캘린더/계산기 방식) 대신 반드시 "기기별 키에만 쓰고 읽는 쪽이 합산"하는 패턴을 쓸 것 — 문서 전체 LWW를 쓰면 다른 기기가 그 사이 추가한 항목이 통째로 사라진다. **원격 명령(remoteCommand) 패턴은 다시 쓰지 않는다** — 19차 세션에 도입했다가 24차에 신뢰성 문제로 완전히 제거한 전례가 있음, 크로스디바이스 제어가 필요하면 먼저 사용자와 상의.
- **Firebase 관련 버그 디버깅 시(34차 교훈)**: 코드만 보고 추측하지 말고, 로컬 데이터파일(데스크탑 `%APPDATA%\PhoneLockDesktop\data.json`)에서 `fbDatabaseUrl`/`fbApiKey`/`fbUser`를 읽어 REST API로 실제 서버 상태를 직접 조회하면 어느 기기의 어느 단계(쓰기/읽기/UI 반영)에서 막히는지 빠르게 좁힐 수 있다. 익명 인증: `POST https://identitytoolkit.googleapis.com/v1/accounts:signUp?key={apiKey}` → `idToken`으로 `{databaseUrl}/{경로}.json?auth={idToken}` 조회.
- **`dailyResetHour` 설정값 확인(35차 발견)**: 데스크탑 로컬 데이터파일(`%APPDATA%\PhoneLockDesktop\data.json`)의 `dailyResetHour`가 현재 **9**(오전 9시 리셋)로 설정돼있다 — `effectiveDate()`(`Repository.kt:13`)가 자정이 아니라 이 값을 기준으로 "오늘"을 판정하므로, 리셋 시각 전이면 캘린더 일정 선택(타이머 드롭다운)·공부기록·일일 사용한도가 전부 아직 "어제"로 계산된다. 버그 아님 — 사용자가 이 값을 유지하기로 결정(35차). "왜 어제 기록/일정이 오늘도 보이냐"는 문의가 다시 오면 이 설정부터 확인.
- **robocopy는 반드시 PowerShell 도구로, 절대경로로 실행할 것(36차 발견, 매우 중요)**: `robocopy "OneDrive 원본\...\src" "AndroidBuilds\...\src" /MIR`를 Bash 도구로(특히 상대경로+`cd` 조합) 실행하면 이 프로젝트의 한글 경로(`OneDrive\바탕 화면\클로드\관리앱`) 때문인지 "성공"처럼 보이는 출력을 내면서 실제로는 파일을 하나도 안 옮긴다. 이 상태에서 아무리 재빌드해도 `AndroidBuilds`의 옛날 소스만 컴파일되고, Gradle이 `compileKotlin`/`jar`를 `UP-TO-DATE`로 스킵하기까지 하면 "BUILD SUCCESSFUL"만 보고 정상 배포된 것으로 착각하기 쉽다(36차에 실제로 이 착각으로 여러 차례 "재배포 완료"를 잘못 보고함). robocopy 출력에 실제로 "Newer"/"New File" 줄이 찍히는지 반드시 확인하고, 의심스러우면 `grep -a <새로 추가한 심볼> AndroidBuilds\...\<파일>`로 소스 동기화부터 검증할 것. 메모리 `feedback_robocopy_use_powershell` 참고.
- **"고쳤는데 반영이 안 된다"가 반복되면 UI/상태 로직을 더 파기 전에 배포된 바이너리부터 검증(36차 교훈)**: 배포된 jar/apk를 `unzip`으로 풀어 `.class` 파일을 `grep -a`로 열어보면(바이너리지만 문자열 상수는 grep으로 보임) 수정한 필드명/문자열이 실제로 컴파일돼 들어갔는지 바로 확인 가능 — 코드 로직을 계속 재검토하는 것보다 훨씬 빠르게 "빌드/배포 파이프라인 문제 vs 진짜 로직 버그"를 구분할 수 있다.
- **크로스디바이스 기능이 "판정 로직 자체"를 확장할 땐 원본 파일을 건드리지 말고 호출부에서 병합할 것(37차 패턴)**: 실행확인 유예시간을 다른 기기와 공유하는 기능을 추가할 때, `ConfirmationGate.kt`(재확인/escalation 판정, 원본 유지 필수 — 위 항목 참고)를 직접 고치지 않고, `Repository.syncedLastConfirmedAtEpochMillis()`(이미 있던 `mergedEscalation` 재사용)로 동기화된 값을 따로 얻은 뒤 호출부(`EnforcementService`/`SiteEnforcement`/안드로이드 `AppMonitorAccessibilityService`)에서 로컬 `ConfirmationGate` 값과 비교해 더 최근인 쪽을 쓰는 방식으로 구현했다. "절대 손대지 말 것" 규칙이 있는 파일을 확장해야 할 필요가 생기면 이 패턴(원본은 그대로, 호출부에서 병합)을 우선 검토할 것.
- **락 안에서 블로킹 네트워크 I/O를 하면 앱 전체가 정지할 수 있다(38차 재확인)**: 새 push/read 함수를 추가할 때 항상 "이 함수가 `synchronized(lock)`/`tickMutex` 등 락을 쥔 채로 호출되는가"를 먼저 확인할 것 — 쥔 채라면 실제 네트워크 I/O는 반드시 락 밖(별도 Thread 또는 캐시값 즉시 반환+백그라운드 갱신)으로 빼야 한다. 38차에 `pushUsageToFirebase`/`peerUsageSeconds`에서 이 원칙이 깨진 걸 발견해 수정했다([[BUGS.md]] 38차 참고) — 단, escalation 계산(`mergedEscalation()`)처럼 판정 로직에 인접한 함수는 같은 유형의 버그라도 신중하게 범위를 좁혀 보수적으로 접근할 것(이번엔 그 함수는 손대지 않고 남겨둠).
- **이 프로젝트 세션 환경은 매번 빌드 툴체인이 있다는 보장이 없다(38차 재확인)**: 38차엔 gradle 자체가 없어 컴파일 확인을 한 번도 못 한 채 세션이 끝났다 — `project_build_toolchain_missing` 메모리 절차를 먼저 시도해보고, 그래도 안 되면 코드 변경 후 "컴파일 미검증"임을 명시적으로 문서(HANDOFF/BUGS/CHANGELOG)에 남길 것. 과거 반영이 안 됐던 사고들이 전부 "됐다고 보고했는데 실제로는 안 됨" 패턴이었으므로, 검증 못 한 걸 검증했다고 절대 표현하지 않는다.
- **원격 신호로 실제 잠금을 거는 기능은 반드시 신선도(staleness) 컷오프를 둘 것(37차)**: 34차의 "미러 표시"는 신호가 오래돼도 화면이 좀 이상하게 보이는 정도지만, 37차의 "원격 신호로 실제 공부 잠금을 건다"는 기능은 신호를 올리던 기기가 정지 없이 꺼지면 이 기기가 영구히 잠길 수 있는 심각한 문제로 이어진다. 34차에 이미 쓰던 20분 컷오프(`REMOTE_STUDY_SIGNAL_STALE_MS`, `remoteUpdatedAtMillis()` 기준)를 재사용해서 20분 넘게 갱신 없는 신호는 무시하도록 했다 — 앞으로 원격 신호 기반의 새 "강제 동작"(잠금, 차단 등)을 추가할 때 항상 이 안전장치를 함께 넣을 것.
- **`LockEvaluator`를 확장해야 하는 새 기능은 `isPomodoroUnlocked()` 패턴을 우선 검토할 것(39차, 스누즈/기간 지정 자동 강화에 적용)**: "판정을 일시적으로 우회하되 `groupEnabled` 등 영구 상태는 안 건드리는" 기능(스누즈처럼)은 `evaluate()`/`isCurrentlyRestricting()`/`isConfirmActiveNow()` 최상단에 `isPomodoroUnlocked()`와 나란히 체크를 추가하는 게 안전하다(이미 검증된 패턴 재사용, `ConfirmationGate.kt`는 안 건드림). 반대로 "스위치 자체를 강제로 뒤집는" 기능(기간 지정 자동 강화처럼)은 `isGroupActive()`/`effectiveGroupEnabled()` 레벨에서 처리하되, `now` 파라미터를 기본값(`LocalDateTime.now()`)으로 추가해 기존 호출부(`isGroupActive(group)`)가 전부 그대로 컴파일되게 했다 — 이 방식이면 새 필드가 비어있는 기존 그룹은 동작이 100% 그대로 유지된다. 스누즈/기간 지정이 동시에 걸리면 기간 지정이 우선하도록 순서를 정했다([[DECISIONS.md]] 39차 참고) — 앞으로 판정 우회 기능이 여러 개 겹칠 때도 "미리 설정해두는 강제 규칙 > 즉흥적 예외"를 기본 순서로 검토할 것.
- **Compose에서 리스트 상태 토글이 "다른 탭 갔다와야 반영된다"는 재현을 받으면 구조적 동등성 리컴포지션 스킵부터 의심할 것(50차)**: `mutableStateOf<List<T>>`을 값이 동일한 새 리스트로 재할당하면 Compose가 변경을 감지 못 해 리컴포지션을 건너뛴다 — 그룹 탭(과거)과 루틴 오늘 탭(50차) 둘 다 이 유형이었다. 항상 증가하는 `refreshTick: Int`를 `key(refreshTick){...}`으로 감싸는 패턴이 표준 해법으로 자리잡았다.
- **가변/고정 개수 항목을 좁은 화면에 맞출 때 스크롤보다 줄바꿈(`FlowRow`)/균등분할(`weight`)을 먼저 검토할 것(50차, 요일 피커+계산기 연동 버튼에서 확인)**: `horizontalScroll`로 구현했다가 "여전히 안 보인다"는 재현을 두 번(44차 요일 피커, 50차 계산기 연동 버튼) 받았다 — 항목 수가 고정이면 `weight(1f)` 균등분할로 스크롤을 아예 없애고, 가변이면 `FlowRow`로 여러 줄 자동 줄바꿈하는 쪽이 이 사용자 환경에서 훨씬 안정적이었다. [[DECISIONS.md]] 50차 참고.
- **크로스디바이스 ID 참조가 필요한 새 동기화 기능은 로컬 auto-increment ID를 그대로 Firebase에 올리지 말 것(50차, 루틴 동기화)**: 안드로이드 Room auto-increment와 데스크탑 수동 카운터는 같은 항목이라도 기기마다 다른 로컬 ID를 가질 수 있다. 루틴 동기화는 배열 인덱스로 부모를 참조하는 방식(`routinesToJson`/`routinesFromJson`)으로 이 문제를 피했다 — 앞으로 "부모-자식 관계가 있는 데이터"를 처음 동기화할 때 이 패턴을 먼저 검토할 것.
- **데스크탑처럼 "로컬 파일 저장 + Firebase 동기화"를 둘 다 하는 모델에 새 필드를 추가할 땐 두 저장 경로(`Repository.kt`의 직렬화 함수와 `JsonStore.kt`의 save()/parse())를 항상 같이 확인할 것(36차·54차 두 번 반복된 실수)**: 컴파일러가 누락을 안 잡아주고, 게다가 `pushXxxToFirebase()`류 함수는 push 시점에 로컬 타임스탬프도 원격과 똑같이 맞춰버려서 필드가 빠진 채 저장돼도 LWW 비교가 "이미 최신"으로 오판해 재시작해도 스스로 안 고쳐진다(54차, `routinesTs` 강제 리셋으로만 복구 가능했음). 새 필드를 추가했으면 실제로 앱을 재시작해서 값이 살아남는지 확인하는 습관을 들일 것. [[DECISIONS.md]]/[[BUGS.md]] 54차 참고.

---

# 현재 알려진 문제

- **OneDrive 경로 빌드 불가**(안드로이드/데스크탑 공통): 한국어 경로 인코딩 문제로 Gradle이 `OneDrive\바탕 화면\클로드\` 안에서 직접 빌드 못함 — 항상 `AndroidBuilds\`로 robocopy 후 그곳에서 빌드.
- **데스크탑 오버레이는 코너 소형 위젯**(전체화면 click-through 불가) — [[DECISIONS.md]], [[IDEAS.md]] 참고. (공부 잠금은 이 제약과 무관 — `BlockScreen`처럼 click-through가 필요 없는 완전 차단용 Maximized 창이라 JNA 없이 구현됨)
- **안드로이드 공부 잠금은 진짜 실행 차단이 아니라 베스트 에포트**: 기기 소유자(device owner)/키오스크 모드 없이는 접근성 서비스로 다른 앱 실행 자체를 막을 수 없다. 허용 안 된 앱이 열리는 걸 감지해서(최대 2초 지연) 잠금 화면으로 되돌리는 방식 — 순간적으로 화면이 보였다 사라질 수 있음.
- 이 git 저장소(`관리앱` 상위 폴더)의 추적 커밋 이력은 phone-lock 프로젝트와 무관(캘린더/공부앱 것). `phone-lock-android`/`phone-lock-desktop`은 이 저장소에서 untracked — 변경 이력은 CHANGELOG.md에만 의존.
- 상세 버그 이력(대부분 Fixed)은 [[BUGS.md]] 참고.

---

# 현재 중요한 파일

## Desktop (`phone-lock-desktop/src/main/kotlin/com/phonelock/desktop/`)
| 파일 | 역할 |
|------|------|
| `data/Models.kt` | `Group`/`AppData` 데이터 모델. **37차** `Group.overlayLevelStepsToMax` 추가(오버레이 최고 밝기까지 재확인 횟수, 순수 표시값). **39차** `StudyLogEntry.note` 추가(공부 세션 회고), `Group`에 `snoozeMinutes`/`snoozedUntilEpochMillis`/`snoozeUsedDate`/`snoozeUsedCount`(스누즈)와 `forceEnabledFrom`/`forceEnabledUntil`(기간 지정 자동 강화) 추가. **42차** `AppData.lastGroupAutoResetDate`(그룹 자동 재활성화 하루 1회 적용 추적), `Group.blockAttemptDate`/`blockAttemptCount`(잠김 화면 문구 강도용, 오늘 시도 횟수) 추가. **48차** `Routine`/`RoutineLog` 데이터클래스 신규 + `AppData.routines`/`routineLogs`/`nextRoutineId` 추가(루틴앱 v1, [[DECISIONS.md]] 48차 참고) |
| `data/JsonStore.kt` | JSON 파일 원자적 읽기/쓰기. **37차** `overlayLevelStepsToMax` parse/save 추가. **39차** 위 신규 필드 parse/save 추가, `save()`의 JSON 구성 로직을 `toJsonObject()`로 추출해 신규 `exportToFile()`(설정/그룹 내보내기, #6)과 공유. **42차** `lastGroupAutoResetDate`/`blockAttemptDate`/`blockAttemptCount` parse/save 추가. **48차** `routines`/`routineLogs`/`nextRoutineId` parse/save 추가. **54차** 52차에 `Routine`에 추가됐던 `icon`/`notifyEnabled`/`startDate`/`endDate` 4개 필드가 여기서 빠져있던 걸 뒤늦게 발견해 추가([[BUGS.md]] 54차 참고) |
| `data/Repository.kt` | 그룹/사용기록/escalation 조회·저장, 캐싱, heartbeat. **34차** `remoteStudyLogCache`+`pushStudyLogToFirebase`+`syncStudyLogFromFirebase` 추가(공부기록 크로스디바이스 동기화, `studyLog`와 별개로 로컬엔 병합 안 함). **35차** `applyIncompleteCarryOver`/`revertIncompleteCarryOver` 추가(캘린더 미완료 시 다음날 자동 복사, `setCalendarTaskStatus`에서 O와 대칭 처리). **37차** `syncedLastConfirmedAtEpochMillis(group)` 추가 — 기존 `mergedEscalation()` 재사용, 같은 이름의 그룹을 가진 다른 기기의 마지막 확인 시각을 호출부에 노출(실행확인 유예시간 크로스디바이스 공유용, `ConfirmationGate.kt` 자체는 안 건드림). **39차** `snoozeGroup()`/`snoozeRemainingToday()`(스누즈, 하루 3회 제한) + `exportDataToFile()`(설정/그룹 내보내기) 신규. **41차** `mergedSnooze()`/`syncedSnoozeUntil(group)` 신규(스누즈 크로스디바이스 동기화, `mergedEscalation()`과 동일한 "최신값 승리" 패턴) — `snoozeGroup()`이 이 병합값 기준으로 하루 3회 판정하도록 변경, `snoozeRemainingToday()`(UI 표시용)는 네트워크 호출 없이 로컬 값만 보도록 그대로 유지. **42차** `applyDailyGroupResetIfNeeded()`(그룹 자동 재활성화, 하루 1회)/`recordBlockAttempt(groupId)`(잠김 화면 문구 강도용 시도 횟수 증가) 신규. **48차** `getRoutines`/`addRoutine`/`updateRoutine`/`deleteRoutine`/`moveRoutineOrder`/`getRoutineLogsForDate`/`isRoutineCompleted`/`toggleRoutineLog`/`getRoutineCompletedDateKeys` 신규(루틴앱 v1 CRUD, 48~49차엔 로컬 전용). **50차** `pushRoutinesToFirebase()`/`syncRoutinesFromFirebase()`/`routinesToJson`/`routinesFromJson` 신규(루틴 Firebase 동기화, 배열 인덱스 참조로 ID 충돌 회피 — [[DECISIONS.md]] 50차 참고), `themeMode` get/set 프로퍼티 추가(3종 테마), 계산기-캘린더 연동 함수 `linkedProgressAmount`/`addLinkedCalendarTask`/`isLinkedGoalAchieved`/`adjustLinkedCalcProgress` 신규 + `setCalendarTaskStatus`가 연동 일정 완료/취소 시 계산기 진행량 함께 조정, `CALENDAR_COLOR_ORDER`/`CALENDAR_SCHEDULE`을 8단계 무지개+망각곡선 기반 간격(1,3,7,14,30,60,120일)으로 전면 재작성 |
| `routine/RoutineEngine.kt` | 루틴 스트릭 순수 계산(`currentStreak`) — 방어권 소모를 저장 카운터 없이 매번 재계산([[DECISIONS.md]] 48차 참고). `calc/CalcEngine.kt`와 같은 위치적 역할(플랫폼별 대칭 복제). 49차부터 `RoutineScreen.kt`가 실제로 호출. **50차** 방어권을 루틴별 커스텀 입력 대신 항상 "주 2회" 고정으로 단순화([[DECISIONS.md]] 50차 참고). **52차** `isScheduledOn`을 public으로 열어 기간(`startDate`/`endDate`) 검사 추가, `RoutineNotifier`도 재사용 |
| `routine/RoutineQuotes.kt` | **신규(52차)** 스트릭 기반 응원/비판/조롱 알림 문구 — `forStreak(streak, broken)`, 42차 `MotivationalQuotes.kt`와는 별개의 작은 세트(안드로이드판과 내용 동일 유지할 것) |
| `routine/DesktopNotifier.kt` | **신규(52차)** `Main.kt`가 등록한 `TrayState`를 들고 있다가 Composable이 아닌 곳에서도 `sendNotification()` 호출 — 새 `TrayIcon`을 안 만드는 이유는 [[DECISIONS.md]] 52차 참고 |
| `routine/RoutineNotifier.kt` | **신규(52차)** 30초 주기 틱(`Main.kt`에서 호출)에서 루틴 `timeSlot`/스트릭 알림 시각을 직접 비교해 `DesktopNotifier` 호출 — 인메모리 중복방지만 함(앱 재시작 시 리셋되지만 무해) |
| `ui/RoutineScreen.kt` | **신규(49차)** 루틴 메인 화면. **50차** 서브탭을 오늘/일과표/습관 3개에서 오늘(항상 시간순 정렬, 요일 피커+주 이동 추가)/통계(지표 카드) 2개로 개편, 체크박스 즉시 미반영 버그를 `refreshTick`+`key()`로 수정([[BUGS.md]] 50차 참고). **52차** 시간대 없는 루틴에만 ▲/▼ 순서변경 버튼(`swapRoutineOrder`) 추가, 아이콘 표시 |
| `ui/RoutineEditScreen.kt` | **신규(49차)** 루틴 추가/수정 `RoutineEditDialog` — `GroupEditScreen`과 달리 전체 화면 아니라 다이얼로그. **50차** 방어권 종류/횟수 입력 UI 제거, 안내 문구로 대체. **52차** 아이콘(텍스트필드+12개 quick-pick)/알림 토글/기간(시작일·종료일) 입력 + "루틴 복사" 버튼 추가 |
| `monitor/EnforcementService.kt` | 포그라운드 프로세스 감시 → 잠금/확인 판정 루프. **37차** `checkStudyLock()`이 로컬 타이머뿐 아니라 `PomodoroSyncClient.isStudyTimerActive()` 원격 신호로도 잠금(`REMOTE_STUDY_SIGNAL_STALE_MS`=20분 컷오프), `effectiveRemainingCooldownSeconds()`/`isRecentlyConfirmedAnyDevice()` 신규(로컬 `ConfirmationGate` vs 동기화 값 중 최신 채택). **42차** `tick()` 맨 앞에서 매번 `repository.applyDailyGroupResetIfNeeded()` 호출, `decide()`가 잠금 판정 시 매칭된 그룹을 함께 반환하도록 바뀌어 `onBlock` 콜백에 `recordBlockAttempt()` 결과(시도 횟수)를 실어 보냄(`onBlock` 시그니처에 `blockAttempts: Int` 추가) |
| `monitor/SiteEnforcement.kt` | 브라우저 확장이 호출하는 사이트 판정 로직. **37차** `isBlockedByStudyLock()`에 원격 신호 OR 추가, `effectiveRemainingCooldownSeconds()`/`isRecentlyConfirmedAnyDevice()` 신규(EnforcementService와 동일 로직 대칭 복제). **42차** `CheckResult.Confirm`에 `level`, `CheckResult.Block`에 `attempts` 필드 추가(실행확인/잠김 화면 문구 강도를 `/check`·`/tick` 응답으로 브라우저 확장에 전달) |
| `monitor/ConfirmationGate.kt` | 그룹별 재확인 쿨다운 관리(원본 유지 필수 — 37차에도 이 원칙 지킴, 호출부에서만 동기화 값과 병합) |
| `monitor/OverlayLevelRatchet.kt` | 오버레이 표시 레벨을 쿨다운 사이클 안에서 캡핑 |
| `monitor/LockEvaluator.kt` | 그룹 잠금/제한 판정. **39차** `isSnoozeActive()`(스누즈, `isPomodoroUnlocked()`와 동일 패턴)와 `isForceEnabledNow()`(기간 지정 자동 강화, `isGroupActive()`에 `now` 파라미터 추가해 통합) 신규 — `evaluate()`/`isCurrentlyRestricting()`/`isConfirmActiveNow()`가 이 둘을 최상단에서 확인(기간 지정이 스누즈보다 우선), `ConfirmationGate.kt`는 안 건드림. **41차** 판정용 private `isSnoozed()`가 `repository.syncedSnoozeUntil(group)`(동기화 값, 네트워크 I/O)를 보도록 변경, UI 표시용 `isSnoozeActive()`(그룹 목록 배지)는 로컬 값만 보도록 분리 유지 |
| `monitor/PomodoroSyncClient.kt` | 실행확인 레벨/일일사용한도 Firebase 동기화 + `pushLocalStudyStatus`(로컬 타이머 상태를 크로스디바이스 신호로 write). **34차** `pushLocalStudyStatus`에 `phaseStartedAt`/`taskName` 파라미터 추가, `remoteUpdatedAtMillis()`(신호 신선도 판정)·`writeStudyLogForDate`/`readStudyLogForDate`(공부기록 동기화) 신규. **37차**부터 `isStudyTimerActive()`/`remoteUpdatedAtMillis()`가 미러 표시뿐 아니라 실제 잠금 판정에도 쓰임(함수 자체는 변경 없음, 호출부만 확장). **41차** `readSnoozeSync`/`writeSnoozeSync` 신규(`users/{user}/snoozeSync/{그룹명}`, `readConfirmSync`/`writeConfirmSync`와 동일 패턴) |
| `net/LocalApiServer.kt` | 로컬 HTTP API(127.0.0.1:47882). **37차** `/overlay-status` 응답에 `levelStepsToMax` 필드 추가 |
| `net/ApiToken.kt` | 토큰 발급/영구 저장 |
| `ui/UsageOverlayContent.kt` | 데스크탑 오버레이 위젯 UI. **37차** 오버레이 알파 증가폭을 고정 상수 대신 `status.levelStepsToMax`로부터 매번 계산. **49차** 타이머 숫자 accent 색을 옛 파랑(`#4F8EF7`)에서 새 Primary 초록(`#8BC34A`)으로 교체 |
| `ui/StudyTimerScreen.kt` | **24차** 네이티브 타이머/뽀모도로 탭, **25차**부터 업무 선택이 오늘 캘린더 일정 드롭다운, **28차** 좌우 분할, **32차** 웹앱 CSS 기준 전면 재작성(phase 배지 공부=파랑/휴식=초록, 타이머 숫자 실행중 항상 파랑, 전환버튼 파랑틴트, 뽀모도로 ON/OFF 알약버튼, 공부기록 카드+합계행, 허용앱/사이트 입력+리스트+삭제 UI로 교체) + 드롭다운 선택 버그 수정(`ExposedDropdownMenuBox`). **34차** phase 배지 dot pulse 애니메이션, 로컬 타이머 꺼져 있을 때 다른 기기 상태를 메인 화면에 그대로 미러링(정지/전환 버튼 숨김), 공부기록 5초 주기 크로스디바이스 동기화. **38차** "오늘 한눈에" 요약 카드 신규(39차에 문자열 템플릿 컴파일 버그 수정). **39차** "정지" 버튼이 즉시 정지 대신 회고 입력 다이얼로그를 열도록 변경(`timerStop(note)`), "오늘의 공부 기록"에 최근 회고 표시 |
| `ui/CalendarScreen.kt` | **신규(25차)** `dateKey+ordinal` 기준 조작, **28차** 좌우 분할, **32차** 웹앱 CSS 기준 전면 재작성(오늘=날짜숫자 원형배지, 월그리드 일정=회독색 채운 배지, 날짜상세 일정명=색텍스트만, 완료/미완료/이동/복사/삭제 버튼 색 틴트, 다음회독을 헤더줄 소형 알약으로) + 그리드 세로 스트레치(행마다 weight). **34차** 날짜 상세 제목에 그날 총 공부시간, 업무 이름 옆에 개별 시간 표시 + 5초 주기 공부기록 동기화(진입 시 1회만 하던 버그 수정). **49차** "오늘" 배지의 하드코딩 `Color.White` 텍스트가 새 밝은 초록 Primary 배경 위에서 대비가 나빠져 `MaterialTheme.colorScheme.onPrimary`로 교체. **50차** 월그리드 날짜 칸에 완료 배지(개수+색상) 추가(안드로이드 대칭), `LinkedCalcSection`(계산기 연동 업무 선택+수량 입력) 신규, `stageTextColor`/색상 선택 UI를 8단계 무지개로 전면 갱신 |
| `calc/CalcEngine.kt` | **신규(27차)** 계산기 순수 계산 로직(웹앱 calculate() 이식) — `totalCapacity`/`finishDiffDays` 필드가 32차에야 UI에서 처음 쓰임 |
| `ui/CalculatorScreen.kt` | **신규(27차)**, **28차** 3탭+그리드, **31차** 웹앱 사이드바 구조(좌2:우8)로 회귀, **32차** 폴더접기 영속화·카드접기·모두펴기/접기·전체저장·입력하단버튼고정 추가 + 결과 카드를 웹앱 CSS(`.result-block` 등) 기준 전면 재작성(카드는 무채색, 색은 D배지/진행바/필요행/판정배너/마감배지 5곳에만). **34차** 저장됨 폴더 지정을 인라인 목록→`DropdownMenu` 플로팅 팝오버로 교체, 결과 카드 진입 애니메이션(fadeIn+slideUp) |
| `ui/TimetableScreen.kt` | **신규(29차)** 계산기 draft 업무 기준 이번 주 고정 테이블, **32차** 오늘 칸 값 빨강/합계열 파랑 색상 추가. **34차** 이전주/다음주 이동 버튼(`weekOffset`) 추가. **50차** 계산기-캘린더 연동 목표 달성 시 ✅ 표시+초록 강조 |
| `ui/StudyStatsScreen.kt` | **신규(30차)** 캘린더 일정 전체 기준 완료율/스트릭/30일 추이, **32차** 지표 타일 배경 틴트 추가 |
| `ui/GroupListScreen.kt` | 그룹 목록. **34차** `selectedGroupId` 파라미터 추가, 선택된 그룹은 accent 테두리 강조(관리앱 좌우 분할용). **39차** "😴 스누즈" 버튼 신규(제한 중이거나 스누즈 중일 때만 표시, 하루 3회 초과면 실패 메시지) |
| `ui/StatsScreen.kt` | 사용 통계. **34차** 왼쪽 그룹별 요약(진행바만)/오른쪽 선택한 그룹 상세로 좌우 분할 재작성(새 데이터 없이 선택 기반 확대만) |
| `ui/StudyLockScreen.kt` | 공부 잠금 전체화면 UI(위: 타이머, 아래: 허용 프로그램 실행 버튼) — 정지/전환 버튼이 로컬 `Repository` 직접 호출. **37차** `StudyLockStatus.isRemote`가 true면(다른 기기 신호로 잠긴 경우) 정지/전환 버튼을 숨기고 안내 문구로 대체 |
| `ui/components/SectionCard.kt` | **32차** `accentColor` 옵션 파라미터 추가(기본은 무채색 유지, 지정 시 배경 10%/테두리 70% 틴트) |
| `ui/components/WatchAndWaitScreen.kt` | 차단/실행확인/종료확인 공용 카운트다운 셸 (title/quote 별도 슬롯 — 42차에 title이 조롱 문구 자리로 확정, quote는 미사용). **43차** 체크포인트 힌트 문구를 bodySmall→bodyMedium+SemiBold로 가독성 개선(★아직 데스크탑 앱 재배포 안 함) |
| `ui/MotivationalQuotes.kt` | **신규(42차)** 조롱조 문구 5단계(순한~극한, 각 20개)와 `confirmQuoteTier(level)`/`blockQuoteTier(attempts)` 매핑. 안드로이드/`quotes.js`와 내용 동일하게 유지할 것. **44차** 각 단계 5개씩 총 25개 추가(124→149개, 아직 이 데스크탑판만 재빌드/재배포 안 됨) |
| `ui/GroupEditScreen.kt` | 그룹 생성/수정 화면 — **안드로이드 허용앱 선택 버그 조사 시 이 화면의 앱 목록 구현을 참고할 것**(사용자가 이 화면은 정상 작동한다고 확인, [[BUGS.md]] 참고). **37차** "실행 확인" 섹션에 `overlayLevelStepsToMax` 입력 필드 추가(사용 중 남은 시간 오버레이 표시 토글 아래). **39차** "일시정지(스누즈) 시간"/"기간 지정 자동 강화" `SectionCard` 2개 신규. **52차** "복사" 버튼 추가(안드로이드판과 대칭) |
| `ui/SettingsScreen.kt` | 설정 화면(Firebase 연동 설정 포함). **39차** "설정/그룹 내보내기·가져오기" 카드 신규(`java.awt.FileDialog`, #6). **50차** "테마" `SectionCard` 신규 — 라이트+그린/다크+파랑/화이트+오렌지 3종 `FilterChip`, `repository.themeMode` 즉시 저장. **52차** "루틴 스트릭 알림" 토글 `SectionCard` 신규(안드로이드판과 대칭, `repository.routineStreakNotifyEnabled`) |
| `ui/MainScreen.kt` | **28차 개편** 왼쪽 `NavigationRail`(관리앱/공부앱/설정) + 섹션별 내부 `TabRow` 서브탭 구조, **30차**부터 공부앱 서브탭 5개(타이머/캘린더/계산기/일정표/통계). **49차** `TopSection.ROUTINE`("🌱 루틴") 추가. **50차** `TopSection` 순서를 루틴→공부→관리→설정으로 재배열, "관리앱"/"공부앱" 라벨을 "관리"/"공부"로 축약, `onThemeChange` 콜백 연결 |
| `ui/theme/Color.kt`/`Theme.kt`/`Shape.kt` | **28차 개편** 당시 공부앱(index.html)과 동일한 다크+파랑 팔레트(`darkColorScheme`), 반경 12dp. **49차** 라이트+그린으로 전면 교체(`lightColorScheme`, Primary `#8BC34A`, 배경 `#FAFBF6`). **50차** `ThemeMode` 상수(`LIGHT_GREEN`/`DARK_BLUE`/`LIGHT_ORANGE`)+`PhoneLockPalette` 데이터클래스+팔레트 3개 val로 재구조화(다크+파랑=28~48차 옛 팔레트, 화이트+오렌지=최초 테마 복원), `PhoneLockTheme(themeMode: String, content)`가 `paletteFor(themeMode)`로 선택 — [[DECISIONS.md]] 49~50차 참고 |
| `Main.kt` | 앱 진입점, 창 구성. **33차** `resolveAppPath()`(공부 잠금 허용 프로그램을 레지스트리 App Paths→시작 메뉴 `.lnk`→PATH 순으로 찾는 이름 해석 함수) 추가 — [[DECISIONS.md]] 참고. **46차** 앱 이름 "갓생살기종합세트"로 통일, 트레이/창 아이콘을 `ColorPainter`(단색)에서 `PixelSunriseIcon`으로 교체. **52차** `rememberTrayState()`를 `Tray(state=...)`에 연결하고 `DesktopNotifier.trayState`에 등록, 30초 주기 `RoutineNotifier.tick()` 루프 추가 |
| `ui/PixelSunriseIcon.kt` | **신규(46차)** 픽셀아트 일출 트레이/창 아이콘(`Painter` 구현) — 안드로이드 `ic_launcher_background.xml`/`ic_launcher_foreground.xml`과 동일한 108x108 좌표를 공유, `packaging/app-icon.ico`(exe 아이콘, `build.gradle.kts`의 `windows.iconFile`)와도 좌표 동일 |

## 브라우저 확장 (`phone-lock-desktop/browser-extension/`)
| 파일 | 역할 |
|------|------|
| `overlay.js` | content script, 페이지 위 오버레이 표시. **35차** 남은시간 숫자 색 주황→파란 accent(`#4F8EF7`). **37차** 알파 증가폭을 고정 상수 대신 `/overlay-status` 응답의 `levelStepsToMax`로부터 계산. **43차** 제목/문구 완전 삭제, 타이머만 중앙에 96px로 확대, 타이머 텍스트 색도 배경과 같은 opacity 적용(레벨 연동 투명도). **49차** accent를 파랑에서 새 Primary 초록(`rgb(139,195,74)`)으로 교체(★ `chrome://extensions` 재로드 필요, 아래 미반영 항목 참고) |
| `background.js` | 서비스워커, 토큰/heartbeat/tick/네비게이션 감시. **Alt-Tab 버그(35차 원인 확정, BUGS.md 참고)**: `onBeforeNavigate`에만 반응해 이미 열려있던 탭 전환은 최대 60초(tick 주기) 지연 |
| `confirm.html`/`blocked.html` | 실행확인/차단 페이지. **35차** 28차 다크테마 개편에서 빠져있던 걸 공부앱과 동일 팔레트로 통일. **42차** 버튼 라벨 "전자"/"후자"→"진행"/"중단", `<h1 id="title">`에 조롱 문구 로테이션(quote 문단은 삭제). **43차** `confirm.html` 제목 24px→30px, 체크포인트 힌트 문구 13px 흐린 회색→15px 굵은 밝은색으로 가독성 개선 |
| `quotes.js` | **신규(42차)** 조롱조 문구 5단계 + `confirmQuoteTier`/`blockQuoteTier` — Kotlin판(`MotivationalQuotes.kt`)과 내용 동일 유지, `overlay.js`용 `MOTIVATIONAL_QUOTES`(=순한 맛 전체) 호환 alias 포함. **44차** 각 단계 5개씩 총 25개 추가(124→149개, 아직 확장 재로드 안 함) |
| `onboarding.html` | 설치 완료 안내(시크릿모드 허용 유도). **35차** 다크 팔레트 통일(예전엔 다른 남색 톤이었음) |
| `config.js` | 공용 `API_BASE`/`fetchWithTimeout` |
| `manifest.json` | content_scripts 순서: `config.js`→`quotes.js`→`overlay.js` |

## Android (`phone-lock-android/app/src/main/java/com/phonelock/app/`)
| 파일 | 역할 |
|------|------|
| `data/PhoneLockRepository.kt` | 그룹/사용기록/실행확인 레벨 조회·저장(escalationMutex로 직렬화). **35차** `applyIncompleteCarryOver`/`revertIncompleteCarryOver` 추가(데스크탑판과 대칭). **37차** `syncedLastConfirmedAtEpochMillis(group)` 추가(데스크탑판과 대칭, `mergedEscalation()` 재사용). **39차** `snoozeGroup()`/`snoozeRemainingToday()` 신규(데스크탑판과 대칭). **41차** `mergedSnooze()`/`syncedSnoozeUntil(group)` 신규(데스크탑판과 대칭, 별도 `snoozeMutex`+10초 캐시), `snoozeGroup()`이 병합값 기준 하루 3회 판정하도록 변경. **42차** `applyDailyGroupResetIfNeeded()`/`recordBlockAttempt(groupId)` 신규(데스크탑판과 대칭). **48차** `routineDao`/`routineLogDao` CRUD 함수 신규(데스크탑판과 대칭, 루틴앱 v1). **50차** `pushRoutinesToFirebase()`/`syncRoutinesFromFirebase()`(데스크탑판과 대칭, 인덱스 참조 방식) + 위젯 갱신용 `refreshRoutineWidget()`(생성자에 `appContext` 추가) + 계산기-캘린더 연동 함수 4종(데스크탑판과 대칭) + `AppPreferences.themeMode` 연동 프로퍼티. **52차** `copyGroup()`/`copyRoutine()`/`swapRoutineOrder()` 신규, `addRoutine`/`updateRoutine`/`deleteRoutine`이 `RoutineAlarmScheduler`도 함께 호출(예약/취소), `restoreGroupsFromBackup()` 신규(그룹 복구, [[BUGS.md]] 52차 참고) |
| `routine/RoutineEngine.kt` | 루틴 스트릭 순수 계산(`currentStreak`) — 데스크탑판과 동일 로직 대칭 복제, [[DECISIONS.md]] 48차 참고. **49차**부터 `RoutineScreen.kt`가 실제로 호출. **50차** 방어권 "주 2회" 고정(데스크탑판과 대칭). **52차** `isScheduledOn`을 public으로 열어 기간 검사 추가, `RoutineAlarmScheduler`도 재사용 |
| `routine/RoutineQuotes.kt` | **신규(52차)** 스트릭 알림 문구 — `forStreak(streak, broken)`, 데스크탑판과 내용 동일 유지할 것 |
| `routine/RoutineAlarmScheduler.kt` | **신규(52차)** 루틴/스트릭 알림 예약·취소. `scheduleNext(routine)`는 8일 이내에서 요일마스크+기간을 만족하는 가장 가까운 시각을 찾아 예약, `rescheduleAll()`은 부팅/앱시작 시 전체 재예약. **56차** Doze 지연(약 2분) 확인돼 `setAndAllowWhileIdle`→`setExactAndAllowWhileIdle`로 전환(`scheduleAlarm()` 헬퍼가 `canScheduleExactAlarms()` 확인 후 권한 없으면 자동 폴백) |
| `routine/RoutineReminderReceiver.kt` | **신규(52차)** `ACTION_BOOT_COMPLETED`(재예약)/`ACTION_ROUTINE_REMINDER`/`ACTION_STREAK_CHECK`(알림 표시+다음 예약) 처리, `RoutineWidgetToggleReceiver`와 같은 `goAsync()`+코루틴 패턴 |
| `ui/RoutineScreen.kt` | **신규(49차)** 데스크탑판과 대칭 — Repository 루틴 함수가 전부 suspend라 `LaunchedEffect(refreshTick)`으로 완료 날짜 집합을 미리 캐싱(StudyStatsScreen과 같은 패턴). **50차** 오늘/통계 2탭 개편(데스크탑판과 대칭) — 요일 피커는 안드로이드만 `weight(1f)` 균등분할 커스텀 칩으로 구현(스크롤 기반 시도 실패 후 확정, [[BUGS.md]] 50차 참고), 30일 통계 그래프는 `horizontalScroll`+고정폭 `20.dp`로 수정. **52차** 시간대 없는 루틴에만 ▲/▼ 순서변경 버튼 추가, 아이콘 표시 |
| `ui/RoutineEditScreen.kt` | **신규(49차)** 데스크탑판과 대칭 다이얼로그. **50차** 방어권 입력 UI 제거(데스크탑판과 대칭). **52차** 아이콘/알림 토글/기간 입력 + "루틴 복사" 버튼 추가(데스크탑판과 대칭) |
| `widget/RoutineWidgetProvider.kt` / `RoutineWidgetService.kt` / `RoutineWidgetFactory.kt` / `RoutineWidgetToggleReceiver.kt` | **신규(50차)** 안드로이드 홈스크린 위젯 — Jetpack Glance 대신 classic `RemoteViews`/`AppWidgetProvider`/`RemoteViewsService`/`RemoteViewsFactory` 사용(신규 Gradle 의존성 회피, [[DECISIONS.md]] 50차 참고), 리스트 항목 클릭은 `PendingIntent` 템플릿+`RoutineWidgetToggleReceiver`(`goAsync()`)로 토글. 관련 리소스: `res/layout/routine_widget.xml`/`routine_widget_item.xml`, `res/xml/routine_widget_info.xml`, `res/drawable/widget_background.xml` 등. **52차** `RoutineWidgetFactory.kt`의 `isScheduledOn`도 기간 검사 추가. 실기기 미검증 |
| `service/AppMonitorAccessibilityService.kt` | 접근성 서비스, 포그라운드 앱/사이트 감시 + 오버레이. **37차** `checkStudyLock()`/`checkStudyLockSite()`가 로컬 타이머뿐 아니라 `PomodoroSyncClient.isStudyTimerActive()` 원격 신호로도 잠금(`REMOTE_STUDY_SIGNAL_STALE_MS`=20분 컷오프), `effectiveRemainingCooldownSeconds()`/`isRecentlyConfirmedAnyDevice()` 신규(데스크탑 EnforcementService와 동일 로직 대칭 복제). **42차** `tick()` 맨 앞에서 `repository.applyDailyGroupResetIfNeeded()` 호출, `launchBlock()`에 `blockAttempts`/`launchConfirm()`·`launchConfirmSite()`에 `level` 파라미터 추가(SCHEDULE/LIMIT 판정에서만 실제 값 전달, STUDY_LOCK/REELS/SHORTS는 기본값 0). **43차** `ensureUsageOverlayView()`에서 제목/문구 TextView 완전 제거, 타이머만 64sp로 중앙 배치, `applyOverlayOpacityForLevel()`/뽀모도로 오버레이 둘 다 타이머 텍스트 색상에도 배경과 같은 alpha 적용. **52차** `REELS_KEYWORDS`/`SHORTS_KEYWORDS` 문자열이 파일 바이트 단위로 깨져있던(mojibake) 버그 수정 — 감지가 전혀 작동 안 했던 근본 원인, [[BUGS.md]] 52차 참고 |
| `service/ConfirmationGate.kt` | 그룹별 재확인 쿨다운 관리(원본 유지 필수 — 37차에도 지킴) |
| `service/LockEvaluator.kt` | 그룹 잠금/제한 판정. **39차** `isSnoozeActive()`/`isForceEnabledNow()` 신규(데스크탑판과 대칭). **41차** `isConfirmActiveNow()`가 `fun`→`suspend fun`으로 변경(판정용 private `isSnoozed()`가 동기화 값을 봐야 해서), UI 표시용 `isSnoozeActive()`는 로컬 값만 보도록 분리 유지 |
| `service/PomodoroSyncClient.kt` | 실행확인 레벨 Firebase 동기화(HttpURLConnection 기반) + `pushLocalStudyStatus`. **37차**부터 `isStudyTimerActive()`가 미러 표시뿐 아니라 실제 잠금 판정에도 쓰임(함수 자체는 변경 없음). **41차** `readSnoozeSync`/`writeSnoozeSync` 신규(데스크탑판과 대칭) |
| `service/IntentExtras.kt` | Activity 간 전달 extra 키 모음. **37차** `EXTRA_STUDY_LOCK_IS_REMOTE` 추가 |
| `ui/components/InterstitialScreen.kt` | 차단/실행확인 공용 카운트다운 셸 (title/quote 별도 슬롯 — 42차에 title이 조롱 문구 자리로 확정, quote는 미사용). **43차** 체크포인트 힌트 문구 가독성 개선(bodySmall→bodyMedium+SemiBold), 제목이 한 줄에 안 들어가면(`didOverflowWidth`) 최소 55%까지 자동 축소하는 로직 추가(`maxLines=1`+`softWrap=false`) — 처음엔 `maxLines=2`+`didOverflowHeight`로 짰다가 "2줄로 꺾이는 것 자체"가 문제였다는 지적을 받고 재작업. **44차** `LocalConfiguration.current.screenWidthDp >= 600`이면 태블릿으로 판정해 축소 로직을 건너뛰고 데스크탑 `WatchAndWaitScreen.kt`와 동일한 무조건부 `Text`로 분기 |
| `ui/MotivationalQuotes.kt` | **신규(42차)** 조롱조 문구 5단계와 `confirmQuoteTier`/`blockQuoteTier` — 데스크탑판과 내용 동일 유지. **44차** 각 단계 5개씩 총 25개 추가(124→149개) |
| `data/Entities.kt` / `data/AppDatabase.kt` / `data/AppPreferences.kt` | 데이터 모델(`StudyLogEntry`/**25차** `CalendarTask`/**27차** `CalcTask`+`CalcSavedItem`/**37차** `AppGroup.overlayLevelStepsToMax`/**39차** `StudyLogEntry.note`+`AppGroup` 스누즈·기간지정 필드 6종/**42차** `AppGroup.blockAttemptDate`/`blockAttemptCount`+`AppPreferences.lastGroupAutoResetDate`/**48차** `Routine`+`RoutineLog` 포함, **52차** `Routine.icon`/`notifyEnabled`/`startDate`/`endDate` 추가)/Room DB(**version 27** — 38차 21→22, 39차 22→23→24, 42차 24→25, 48차 25→26, 52차 26→27)/SharedPreferences(`studyLockAllowedPackages`/`studyLockAllowedSites`/타이머 상태/`calendarTs`/**27차** 계산기 ts+폴더 트리 JSON 필드/**50차** `themeMode`/`routinesTs` 포함, **52차** `routineStreakNotifyEnabled`/`lastRoutineStreak`/`lastRoutineStreakNotifyDate` + `resetSyncTimestamps()`(Room destructive migration 후 강제 재동기화, [[BUGS.md]]/[[DECISIONS.md]] 52차 참고)) |
| `data/PreMigrationBackup.kt` | 앱 버전 변경 감지 시 Room 열기 전에 raw SQLite를 JSON으로 백업(`backupIfVersionChanged`). **52차** `listBackups()` 신규(설정 화면 그룹 복구 카드용), `MainActivity.onCreate`가 백업 성공 시 `AppPreferences.resetSyncTimestamps()`도 함께 호출하도록 연결 |
| `ui/GroupListScreen.kt` | 그룹 목록. **39차** "😴 스누즈" 버튼 신규(데스크탑판과 대칭). **50차** 좌상단 "🗂️ 그룹" headline 추가(데스크탑판과 대칭) |
| `ui/GroupEditScreen.kt` / `ui/SettingsScreen.kt` | 그룹 편집/설정 화면. **37차** "실행 확인" 섹션에 `overlayLevelStepsToMax` 입력 필드 추가(데스크탑판과 대칭). **39차** `GroupEditScreen.kt`에 스누즈/기간지정 `SectionCard` 2개 추가(데스크탑판과 대칭) — `SettingsScreen.kt`의 백업/복원(#6)은 이미 있었음. **50차** `SettingsScreen.kt`에 테마 3종 선택 `SectionCard` 추가(데스크탑판과 대칭). **52차** `GroupEditScreen.kt`에 "복사" 버튼 추가(데스크탑판과 대칭), `SettingsScreen.kt` 최상단에 "⚠ 그룹 데이터 복구" 카드(`PreMigrationBackup.listBackups()`로 찾은 최신 백업에서 1클릭 복구, 백업 있을 때만 노출) + "루틴 스트릭 알림" 토글 신규. **56차** `SettingsScreen.kt` "권한 / 백그라운드 보호" 섹션에 "정확한 알람" 상태 표시 + `ACTION_REQUEST_SCHEDULE_EXACT_ALARM` 허용 버튼 신규(배터리 최적화 예외 UI와 동일 패턴) |
| `ui/StudyTimerScreen.kt` | **24차** 네이티브 타이머/뽀모도로 탭, **25차**부터 업무 선택이 오늘 캘린더 일정 드롭다운, **32차** 데스크탑판과 동일하게 웹앱 CSS 기준 전면 재작성 + 인라인 접이식 허용앱 선택 섹션(`AllowedAppsCollapsibleSection`) 신규 + 드롭다운 버그 수정. **34차** 데스크탑판과 동일하게 pulse 애니메이션+타이머 미러+공부기록 동기화. **39차** 정지 버튼이 회고 입력 다이얼로그를 열도록 변경(데스크탑판과 대칭). **50차** `TIMER_COLOR_LABEL` 8단계로 확장(데스크탑판과 대칭) |
| `ui/CalendarScreen.kt` | **신규(25차)** `CalendarTask.id`+`sortOrder` 기준 조작, **32차** 데스크탑판과 동일하게 웹앱 CSS 기준 전면 재작성. **34차** 데스크탑판과 동일하게 날짜 상세 시간 표시+5초 주기 공부기록 동기화. **50차** `LinkedCalcSection`(계산기 연동, 업무 선택 버튼은 `FlowRow`로 오버플로우 방지) 신규, 8단계 무지개 색상 선택 UI로 갱신(월그리드 완료 배지는 안드로이드에 이미 있었음) |
| `calc/CalcEngine.kt` | **신규(27차)** 계산기 순수 계산 로직(데스크탑판과 동일 로직, 플랫폼 공유 모듈 없어 대칭 복제) — `totalCapacity`/`finishDiffDays`가 32차에야 UI에서 처음 쓰임 |
| `ui/CalculatorScreen.kt` | **신규(27차)**, **32차** 폴더접기 영속화·카드접기·모두펴기/접기·전체저장·입력하단버튼고정 추가 + 결과 카드 웹앱 CSS 기준 전면 재작성(데스크탑판과 대칭). **34차** 저장됨 폴더 지정 `DropdownMenu` 팝오버, 결과 카드 진입 애니메이션(데스크탑판과 대칭) |
| `ui/TimetableScreen.kt` | **신규(29차)** 계산기 draft 업무 기준 일 단위 뷰(◀/▶ 날짜 이동), **32차** 오늘 값 색상 추가. **50차** 계산기-캘린더 연동 목표 달성 시 ✅ 표시(`isLinkedGoalAchieved`가 suspend라 `LaunchedEffect(cursor, dayTasks)`로 `achievedMap` 선계산) |
| `ui/StudyStatsScreen.kt` | **신규(30차)** 데스크탑판과 동일 로직 대칭 복제, **32차** 지표 타일 배경 틴트 추가. **50차** 30일 완료 추이 그래프를 `horizontalScroll`+고정폭 `20.dp`로 수정(뭉개짐 버그, [[BUGS.md]] 참고) |
| `ui/StudyLockActivity.kt` / `ui/StudyLockAppsScreen.kt` | 공부 잠금 전체화면(정지/전환이 로컬 `PhoneLockRepository` 직접 호출, 비활성화되면 스스로 `finish()`) / 허용 앱 선택 화면(**32차**부터 `AllowedAppsPickerBody`로 목록 로직 분리해 타이머 탭과 공유, **33차**에 중첩 `LazyColumn`+`weight()` 렌더링 버그 수정 완료). **37차** `isStillActive`가 suspend로 바뀌어 로컬+원격 신호 둘 다 확인, `isRemote`면 정지/전환 버튼 숨기고 안내 문구 표시(데스크탑판과 대칭) |
| `ui/MainActivity.kt` | **28차 개편** 하단 `NavigationBar` 3탭(관리앱/공부앱/설정) + `ManageSection`/`StudySection` 내부 `TabRow` 서브탭, **30차**부터 공부앱 서브탭 5개(타이머/캘린더/계산기/일정표/통계). **49차** `Tab.Routine`("🌱 루틴") 추가로 4탭. **50차** 탭 순서를 루틴→공부→관리→설정으로 재배열, "관리앱"/"공부앱" 라벨을 "관리"/"공부"로 축약, `onThemeChange` 콜백 연결(데스크탑판과 대칭). **56차** 앱 시작 시 루틴 알림 재예약(`rescheduleAll`) 전에 `repository.syncRoutinesFromFirebase()`를 먼저 호출 — 이전엔 "루틴" 탭을 직접 열기 전엔 다른 기기발 변경이 반영 안 돼 알림이 예약조차 안 됐음 |
| `ui/theme/Color.kt`/`Theme.kt`/`Shape.kt` | **28차 개편** 당시 데스크탑판과 동일한 다크+파랑 팔레트(`darkColorScheme`), 반경 12dp. **49차** 라이트+그린으로 전면 교체(데스크탑판과 동일 값). **50차** `ThemeMode`/`PhoneLockPalette` 3종 선택제로 재구조화(데스크탑판과 대칭) |
| `AndroidManifest.xml` | `INTERNET` 퍼미션 포함. **50차** 홈스크린 위젯용 `<receiver>`/`<service>` 엔트리 추가 |
| `res/values/strings.xml` / `res/drawable/ic_launcher_background.xml` / `ic_launcher_foreground.xml` | **46차** `app_name`을 "갓생살기종합세트"로 변경, 런처 아이콘을 기존 흰색 자물쇠 벡터에서 픽셀아트 일출(데스크탑 `PixelSunriseIcon.kt`와 동일 108x108 좌표)로 전면 교체 — `colors.xml`의 옛 단색 `ic_launcher_background`는 미사용이라 제거 |

**38차(2026-08-10) 변경 파일**(위 표엔 기존 역할 설명만 유지, 세부 diff는 [[CHANGELOG.md]] 38차 참고): Desktop `data/Repository.kt`(push 비동기화, `ConfirmCounter`/백업/CSV/평균사용량 함수 신규), `data/Models.kt`+`data/JsonStore.kt`(`ConfirmCounter` 모델+parse/save, 일일백업 함수), `ui/StudyTimerScreen.kt`+`ui/StudyStatsScreen.kt`+`ui/StatsScreen.kt`+`ui/SettingsScreen.kt`(신규 UI). Android `data/PhoneLockRepository.kt`(대칭 함수 전부, `recordConfirmFireAndForget`/`usageMutex` 등 버그수정 포함), `data/Entities.kt`+`data/Daos.kt`+`data/AppDatabase.kt`(`ConfirmCounter` 엔티티, Room version 21→22), `data/AppPreferences.kt`(`autoDndEnabled`), `data/PreMigrationBackup.kt`(테이블 5개 추가), `service/PomodoroSyncClient.kt`(`@Volatile`), `ui/ConfirmOpenActivity.kt`(스코프 수정), `ui/StudyLockActivity.kt`(DND 자동 적용), `ui/StudyTimerScreen.kt`+`ui/StudyStatsScreen.kt`+`ui/StatsScreen.kt`+`ui/SettingsScreen.kt`(신규 UI, 데스크탑판과 대칭).

---

# 실행 방법

**아래 robocopy 명령들은 반드시 PowerShell 도구로 실행할 것 — Bash 도구로 실행하면 한글 경로 때문에 조용히 아무것도 복사되지 않는다(36차 세션 발견, 위 "현재 주의사항" 참고).**

## 데스크탑 빌드/배포 (반드시 이 순서)
```powershell
# 1. watchdog 예약 작업 비활성화 (안 하면 Stop-Process해도 곧바로 재실행시켜서 5번 robocopy가
#    exe/dll "파일 사용 중"으로 반복 실패함 — BUGS.md 참고)
Disable-ScheduledTask -TaskName "PhoneLockDesktopWatchdog"

# 2. 앱 종료
Get-Process -Name "PhoneLockDesktop" -ErrorAction SilentlyContinue | Stop-Process -Force

# 3. 소스 복사 (OneDrive → AndroidBuilds, 한글 경로 빌드 불가 우회)
robocopy "C:\Users\sunae\OneDrive\바탕 화면\클로드\관리앱\phone-lock-desktop\src" "C:\Users\sunae\AndroidBuilds\phone-lock-desktop\src" /MIR

# 4. 빌드 (JAVA_HOME 경로는 세션마다 없어져 있을 수 있음 — 없으면 project_build_toolchain_missing 메모리 절차 참고)
$env:JAVA_HOME = "C:\Users\sunae\AppData\Local\Temp\jdk21_extract_new\jdk-21.0.12+8"
$gradle = "C:\Users\sunae\.gradle\wrapper\dists\gradle-8.7-bin\bhs2wmbdwecv87pi65oeuq5iu\gradle-8.7\bin\gradle.bat"
Set-Location "C:\Users\sunae\AndroidBuilds\phone-lock-desktop"
& $gradle createDistributable

# 5. 배포 (동기 실행 후 FAILED 0 확인 필수)
robocopy "C:\Users\sunae\AndroidBuilds\phone-lock-desktop\build\compose\binaries\main\app\PhoneLockDesktop" "C:\Users\sunae\PhoneLockDesktopApp" /MIR

# 6. 앱 실행 후 watchdog 예약 작업 재활성화
Enable-ScheduledTask -TaskName "PhoneLockDesktopWatchdog"

# 7. 앱 실행(6번과 순서 무관, 아래 명령)
Start-Process "C:\Users\sunae\PhoneLockDesktopApp\PhoneLockDesktop.exe"
```
컴파일만 빠르게 확인하려면 watchdog 비활성화/앱 종료/배포 없이 4단계에서 `& $gradle compileKotlin`만 실행.

## Android APK 빌드 (★ 마지막 Copy-Item 2줄 둘 다 반드시 실행)
```powershell
robocopy "C:\Users\sunae\OneDrive\바탕 화면\클로드\관리앱\phone-lock-android" "C:\Users\sunae\AndroidBuilds\phone-lock-android" /MIR /XD ".git" "build"
$env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"  # JDK 21.0.10, jpackage 없어도 assembleDebug는 문제없음
$gradle = "C:\Users\sunae\.gradle\wrapper\dists\gradle-8.7-bin\bhs2wmbdwecv87pi65oeuq5iu\gradle-8.7\bin\gradle.bat"
Set-Location "C:\Users\sunae\AndroidBuilds\phone-lock-android"
& $gradle assembleDebug

# 결과 APK를 반드시 두 곳 모두에 복사할 것:
Copy-Item "app\build\outputs\apk\debug\app-debug.apk" "C:\Users\sunae\AndroidBuilds\phone-lock-app.apk" -Force
Copy-Item "app\build\outputs\apk\debug\app-debug.apk" "C:\Users\sunae\OneDrive\바탕 화면\클로드\관리앱\phone-lock-android\app\build\outputs\apk\debug\app-debug.apk" -Force
```

## 로컬 API 수동 테스트
```bash
curl -s "http://127.0.0.1:47882/pair"
curl -s "http://127.0.0.1:47882/overlay-status?domain=youtube.com"
```
`/confirm`, `/tick`, `/heartbeat`는 POST + `/pair`로 받은 토큰을 `X-PhoneLock-Token` 헤더에 넣어야 통과함.

## 브라우저 확장 재로드
`chrome://extensions` → "폰컨트롤 사이트 차단" 카드 재로드 → 대상 사이트 탭 새로고침. (서버 쪽 코드가 원인인 버그는 확장 재로드로 안 고쳐짐.)

---

# 다음 세션에서 반드시 알아야 하는 내용

0. **최신(2026-08-14, 55차 세션 완료)**: 스누즈 진행 중 그룹 설정 변경 자유화(`detectWeakeningEdit()`에 스누즈 예외 추가) + 스케줄=>일일한도=>실행확인 우선순위 정렬(잠금 판정을 확인 판정보다 먼저 검사하도록 안드로이드/데스크탑 4곳 호출 순서 재배치) — 양 플랫폼 컴파일/배포 완료, **실기기 미검증**(위 "다음 작업 우선순위" 최상단 참고).
0-1. **(2026-08-14, 54차 세션 완료)**: 데스크탑 루틴 아이콘이 재시작하면 사라지는 버그 수정 — 원인은 로컬 저장(`JsonStore.kt`)에 52차 신규 필드 4개(icon/notifyEnabled/startDate/endDate) 누락 + LWW 타임스탬프가 push 시점에 원격과 똑같아져서 재시작해도 자동 복구가 안 되는 함정. 이미 손상된 로컬 데이터는 `routinesTs`를 0으로 강제 리셋해 복구, **사용자가 직접 확인 완료**(이 프로젝트에서 드물게 실기기 검증까지 끝난 케이스). 앞으로 로컬+Firebase 양쪽에 저장되는 모델에 필드 추가 시 두 경로 모두 확인하는 습관이 [[DECISIONS.md]] 54차에 원칙으로 남음.
0-2. **(2026-08-13, 50차 세션 완료)**: 루틴앱 오늘/통계 2탭 개편(일과표 흡수, 습관→통계) + 요일 피커/주 이동 + 방어권 주 2회 고정 + Firebase 동기화, 계산기↔캘린더 연동 기능 이식(웹앱엔 있었지만 25~30차 재구현 때 누락됐던 기능), 회독 8단계 무지개(망각곡선 기반 1,3,7,14,30,60,120일 간격), 테마 3종 선택제(라이트+그린/다크+파랑/화이트+오렌지), 탭 순서 재배열+"앱" 단어 제거, 데스크탑 캘린더 월그리드 완료 배지, 안드로이드 홈스크린 위젯 — **전부 코드/빌드 완료, 이번 세션 신규 기능은 전부 실기기 미검증**("다음 작업 우선순위" 최상단 참고). Room DB 스키마 변경 없음(버전 26 유지). 자세한 설계 근거는 [[DECISIONS.md]] 50차, 세부 변경은 [[CHANGELOG.md]] 50차 참고.
1. **48차 세션**: 47차 설계(루틴앱 v1)를 바탕으로 실제 착수 — `Routine`/`RoutineLog` 데이터 모델(양 플랫폼), CRUD(`Repository`/`PhoneLockRepository`), 스트릭 계산 엔진(`RoutineEngine.kt`)까지 구현. Room DB 25→26.
2. **44차 세션(2026-08-12)**: 독립된 두 요청 처리 — ① 43차의 제목 자동 축소가 태블릿에도 똑같이 적용돼 있었는데, 태블릿은 폭이 넉넉해 한 줄로 욱여넣을 필요가 없다는 지적을 받고 `LocalConfiguration.current.screenWidthDp >= 600`(sw600dp 기준)으로 분기 — 태블릿이면 데스크탑처럼 축소 없이 기본 줄바꿈(`InterstitialScreen.kt`). ② 5단계 조롱조 문구에 각 단계 5개씩 총 25개 추가(124→149개, `MotivationalQuotes.kt` 양 플랫폼+`quotes.js` 3곳 동일 유지). 안드로이드는 두 변경 모두 포함해 APK 두 위치 재빌드/배포 완료. **데스크탑(`MotivationalQuotes.kt`)과 브라우저 확장(`quotes.js`)은 문구 소스만 갱신, 아직 재빌드/재배포·재로드 안 함** — 43차부터 밀린 데스크탑 재배포와 함께 처리할 것. 실기기/실사용 검증은 요청받지 않아 수행 안 함.
3. **43차 세션**: 세 가지 요청 처리 — ① 사용 중 오버레이 문구("무시당하면서 살기...") 완전 삭제, 타이머만 중앙에 훨씬 크게(안드로이드 64sp/확장 96px), 타이머 텍스트도 배경과 같은 alpha를 적용해 레벨 오를수록 함께 또렷해지도록 재설계. ② 실행확인 화면 체크포인트 힌트 문구 가독성 개선(작고 흐린 텍스트→굵고 밝은 텍스트, 안드로이드/데스크탑/브라우저 확장 3곳). ③ 제목이 길면 2줄로 꺾여 가독성이 떨어지던 걸 안드로이드에 자동 축소로 해결 — `maxLines=2`+`didOverflowHeight`로 처음 구현했다가 "2줄에 정확히 들어가는 대부분 케이스엔 발동 안 한다"는 사용자 지적으로 `maxLines=1`+`didOverflowWidth`로 재작업(교훈: "N줄로 꺾이는 것 자체"가 문제일 땐 `maxLines=N`이 아니라 `maxLines=1`을 기준으로 축소 여부를 판단해야 함). 안드로이드 APK 두 위치 재빌드/배포 완료(축소 로직 재작업 포함 총 2회). **데스크탑은 `WatchAndWaitScreen.kt` 힌트 가독성 변경분이 아직 재빌드/재배포 안 됨** — 다음에 데스크탑을 만질 일이 있으면 44차의 문구 추가분과 함께 반영할 것. 브라우저 확장은 빌드 불필요(재로드만 하면 반영). 실기기/실사용 검증은 요청받지 않아 수행 안 함.
4. **42차 세션**: 독립된 세 요청 — ① 그룹 자동 재활성화(`dailyResetHour` 지나면 꺼둔 그룹도 자동 재활성화, 하루 1회) 신규, ② 실행확인/잠김 화면 버튼 "전자"/"후자"→"진행"/"중단" 고정, ③ 조롱조 문구 124개를 5단계 강도로 나눠 실행확인(레벨 기준)/잠김(오늘 시도 횟수 기준) 로테이션 신규(도중 title이 아니라 quote에 잘못 걸었던 걸 사용자 지적으로 정정, [[DECISIONS.md]] 42차 참고). 양 플랫폼 컴파일·빌드 산출물 바이트 레벨 확인 후 3회 배포 완료(안드로이드 APK 두 위치, 데스크탑 표준 절차), Room DB 24→25.
5. **`dailyResetHour=9` 설정(35차 발견)**: "오늘"이 자정이 아니라 오전 9시 기준으로 판정됨 — 캘린더/공부기록/일일한도 전부 공유. 리셋 시각 전엔 어제 기록/일정이 계속 "오늘" 것으로 보이는 게 정상 동작(버그 아님). 사용자가 유지하기로 결정. 위 "현재 주의사항" 참고.
6. **크로스디바이스 동기화 추가 원칙이 34차에 확립됨**: "신호"(다른 기기 상태를 알려주기만 하면 됨)는 24차 패턴(로컬이 source of truth, Firebase는 부가 채널), "실제 누적 데이터"(여러 기기가 각자 추가)는 `dailyUsage`/`studyLog`처럼 기기별 키에만 쓰고 읽는 쪽이 합산하는 패턴 — 문서 전체 LWW(캘린더/계산기 방식)를 쓰면 안 됨. 원격 명령(remoteCommand)은 19차 신뢰성 문제로 24차에 제거된 전례가 있어 다시 쓰지 않음(표시는 하되 제어는 안 함). [[DECISIONS.md]] "공부 기록 크로스디바이스 동기화"/"타이머 크로스디바이스 '미러' 표시" 참고.
7. **Firebase 관련 버그는 REST API로 서버 상태를 직접 조회해서 디버깅하는 게 효과적임이 34차에 확인됨**: 데스크탑 로컬 데이터파일(`%APPDATA%\PhoneLockDesktop\data.json`)에서 `fbDatabaseUrl`/`fbApiKey`/`fbUser`를 읽어 anonymous 인증 후 `{databaseUrl}/{경로}.json?auth={idToken}`으로 조회하면 어느 기기/단계에서 막히는지 빠르게 좁혀짐 — 이 방법으로 캘린더 날짜 상세의 "진입 시 1회만 동기화" 버그를 찾았음([[BUGS.md]] Fixed(34차) 참고). 35차에도 같은 파일로 `dailyResetHour` 확인에 활용.
8. **공부 잠금 허용 프로그램 실행 로직이 33차에 크게 바뀜**: `ProcessBuilder(appName)` 단독 호출 → `resolveAppPath()`(레지스트리 App Paths→시작 메뉴 `.lnk`→PATH) 3단계 폴백으로 교체됨. 이 영역을 다시 건드릴 일이 있으면 [[DECISIONS.md]] "공부 잠금 허용 프로그램 실행..." 항목부터 볼 것.
9. **UI를 웹앱과 맞출 땐 스크린샷 대신 `공부앱/index.html`의 CSS/JS부터 Grep할 것** — 32차 세션 교훈, 34차의 애니메이션 이식/폴더 팝오버 작업, 35차의 확장 다크테마 통일 작업에도 그대로 적용함. [[DECISIONS.md]] 참고.
10. **데스크탑은 42차까지 재빌드·재배포됐지만 43~44차 변경분(`WatchAndWaitScreen.kt` 힌트 가독성, `MotivationalQuotes.kt` 문구 25개 추가)은 아직 미반영**(위 "다음 작업 우선순위" 참고). jpackage 포함 JDK/gradle 캐시는 세션마다 스크래치에 없을 수 있음 — 재배포 필요해지면 메모리 `project_build_toolchain_missing` 절차대로 다시 준비(필요시 사용자 승인).
11. **절대 하지 말 것**: 재확인/escalation 판정 로직(`ConfirmationGate.kt` 등) 수정 — [[DECISIONS.md]] "표시값과 판정 로직 분리" 참고. 37차에 크로스디바이스 유예시간 공유 기능을 추가할 때도 이 파일은 원본 그대로 두고 호출부에서만 값을 병합했음 — "절대 손대지 말 것" 파일을 확장해야 할 땐 이 패턴(37차) 우선 검토.
12. **빌드 환경**: 이 프로젝트는 세션마다 새 샌드박스일 수 있어 `gradlew`/jpackage 포함 JDK가 없을 수 있음 — 메모리 `project_build_toolchain_missing` 절차 우선 시도. jpackage 없이도 `compileKotlin`/`compileDebugKotlin`/`assembleDebug`는 Android Studio JBR(`C:\Program Files\Android\Android Studio\jbr`)만으로 충분.
13. **문서 체계**: 이 프로젝트는 HANDOFF/CHANGELOG/DECISIONS/BUGS/IDEAS 5종 문서 체계를 씀(2026-08-05부터). 세션 종료 시 각 문서를 역할에 맞게 갱신할 것 — 자세한 규칙은 `CLAUDE.md` 참고.
14. 과거 세션 상세 기록은 전부 `CHANGELOG.md`로 이동됨. "그때 무슨 일이 있었는지" 필요하면 거기서 검색.
15. **robocopy는 반드시 PowerShell 도구로(36차 발견, 위 "현재 주의사항"·"실행 방법" 상단 참고)**: Bash 도구로 실행하면 이 프로젝트의 한글 경로에서 조용히 아무것도 복사 안 되고, Gradle의 UP-TO-DATE 스킵과 겹치면 "빌드/배포 성공" 로그만 보고 옛날 코드를 계속 재배포하게 된다. 36차 세션에 이걸로 시간을 크게 허비했음 — 다음 세션에서 "고쳤는데 반영이 안 된다"는 보고가 반복되면 로직을 더 파기 전에 이것부터 의심할 것. 37차엔 PowerShell+절대경로 절차를 처음부터 지켜서 문제 없었음.
16. **원격 신호로 실제 잠금/차단 같은 강제 동작을 거는 기능은 반드시 신선도 컷오프를 넣을 것(37차)**: `REMOTE_STUDY_SIGNAL_STALE_MS`(20분, `remoteUpdatedAtMillis()` 기준)처럼 — 신호를 올리던 기기가 정지 없이 꺼지면 유령 신호가 남아 이 기기가 영구히 잠길 수 있다. 34차의 "미러 표시"는 이 위험이 없었지만(화면이 이상해 보이는 정도), 37차부터 실제 잠금에 신호를 쓰기 시작했으니 앞으로 이 종류의 기능을 늘릴 때 항상 염두에 둘 것.
17. **Room DB 버전은 48차에 26으로 올라감**(38차 22 → 39차 23(`StudyLogEntry.note`) → 24(`AppGroup` 스누즈/기간지정 필드) → 42차 25(`AppGroup.blockAttemptDate`/`blockAttemptCount`) → 48차 26(`Routine`/`RoutineLog`)) — `fallbackToDestructiveMigration()`이라 마이그레이션 코드는 불필요하지만, 스키마 변경 시 항상 버전을 올려야 하는 규칙은 계속 유지.
18. **38차의 "컴파일 미검증" 문제는 39차에 해결됨**: 이 세션 환경엔 빌드 툴체인이 있었고, 실제로 발견된 오류는 문자열 템플릿에 한글이 바로 붙어 식별자로 오인식되던 것 1건뿐이었다(다른 5건은 정상 컴파일). 다만 이 프로젝트 세션 환경은 매번 툴체인이 있다는 보장이 없으므로([[project_build_toolchain_missing]] 메모리 참고), 다음 세션에서도 코드 변경 후엔 항상 컴파일 확인부터 시도할 것.
19. **크로스디바이스 동기화 기능을 추가할 때 "UI 표시용"과 "판정 게이트용" 함수를 반드시 분리할 것(41차 확립)**: 동기화 값을 읽는 함수는 네트워크 I/O가 있어서(캐시가 있어도) Compose 리컴포지션마다 직접 호출되는 자리(그룹 목록 배지 등)에서 쓰면 UI가 버벅인다. 41차엔 `isSnoozeActive()`(UI, 로컬 값만)와 판정용 private `isSnoozed()`(EnforcementService/AppMonitorAccessibilityService의 백그라운드 tick에서만, 동기화 값)를 분리해서 해결했다 — 판정 시점에 병합값을 로컬로 persist해두므로 UI는 다음 재조회 때 자연히 따라잡는다(confirmSync/escalation과 같은 지연 특성). 앞으로 비슷한 동기화 기능을 추가할 때 이 분리 패턴을 기본으로 검토할 것. [[DECISIONS.md]] "스누즈(#1) 크로스디바이스 동기화..." 참고.
20. **AppGroup(그룹) 자체는 원래부터 Firebase에 안 올라간다**: 그룹 설정(스케줄/한도/실행확인 설정값 등)은 기기별 로컬 전용이고, Firebase로 동기화되는 건 `dailyUsage`/`confirmSync`/`studyLog`/`pomodoro`/`calendar`/`calculator`/**41차부터** `snoozeSync`뿐이다. 새 그룹 필드를 추가할 때 "이것도 동기화해야 하나?"라는 질문이 나오면, 그룹 전체를 동기화 대상으로 만들지 말고(설계가 크게 바뀜) 41차처럼 해당 필드만 별도의 작은 sync 경로로 분리해서 추가할 것.
21. **화면 문구 교체 요청은 title/quote 슬롯을 먼저 구분할 것(42차 교훈)**: `WatchAndWaitScreen`/`InterstitialScreen`은 title(큰 헤드라인)과 quote(작은 부제) 두 슬롯이 있는데, 사용자가 인용한 문구가 실제로 코드의 어느 파라미터에 들어있는지 대조하지 않고 진행하면 엉뚱한 슬롯을 바꾸게 된다. 42차에 title 교체 요청을 quote에 잘못 반영했다가 재작업했음 — 위 "현재 주의사항" 참고.
22. **텍스트 자동 축소(shrink-to-fit) 로직을 짤 땐 "무엇을 막으려는지"에 맞는 조건을 고를 것(43차 교훈)**: "N줄을 넘칠 때" 줄이는 로직(`maxLines=N`+`didOverflowHeight`)과 "1줄에 안 들어갈 때" 줄이는 로직(`maxLines=1`+`didOverflowWidth`)은 다르다 — "짧은 줄바꿈 자체가 보기 싫다"는 요구엔 후자가 맞는데, 전자로 짜면 대부분 케이스에서 로직이 아예 발동하지 않아 "안 바뀐 것 같다"는 피드백을 받게 된다.
23. **화면 폭 기준으로 기기 종류를 나눌 땐 표준 브레이크포인트(sw600dp)부터 검토할 것(44차)**: 이 프로젝트엔 태블릿/폰을 구분하는 기존 로직이 전혀 없었는데, 43차의 제목 축소가 태블릿에는 불필요하다는 요청을 받고 `LocalConfiguration.current.screenWidthDp >= 600`으로 새로 판정 기준을 만들었다 — 앞으로 폼팩터별 분기가 또 필요해지면 이 임계값과 위치(`InterstitialScreen.kt`)부터 재사용 검토할 것.

---

*마지막 업데이트: 2026-08-14 (54차 세션. 데스크탑 루틴 아이콘이 재시작하면 사라지는 버그 수정 — 로컬 저장(`JsonStore.kt`)에 52차 신규 필드 4개 누락 + LWW 타임스탬프 동일값 트랩, 코드 수정 + 손상된 로컬 데이터 강제 리셋으로 복구. **이 세션은 드물게 사용자 실기기 검증까지 완료됨**. 자세한 원인은 [[DECISIONS.md]]/[[BUGS.md]] 54차 참고. 50차까지의 나머지 인계 사항(위 항목들)은 그대로 유효.)*
